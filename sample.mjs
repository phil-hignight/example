#!/usr/bin/env node
/**
 * code_boss — production single-file bundle. Generated by scripts/build.mjs.
 * Starts a local web UI on http://127.0.0.1:<PORT>/ and opens it in the
 * default browser. Talks to an OpenAI-compatible endpoint (api.genai.mil by
 * default). No npm deps. Node 20+ for built-in fetch.
 *
 * Usage:
 *   node code-boss.mjs                (opens the chat UI)
 *   PORT=18999 node code-boss.mjs     (override the port)
 *   node code-boss.mjs extract [dir]  (recreate the project source tree from this file)
 *   node code-boss.mjs check-update   (fetch a newer release from UPDATE_SOURCE, swap in place)
 *   node code-boss.mjs package ...    (package this bundle + jars into an extract-and-run zip)
 *
 * Edit the CONFIG constants below for your environment.
 */

// Build stamp — injected at build time so the running server can report it
// (visible in the Snapshot view). Lets you confirm "yes, this is the bundle
// I just copied over" without guessing from file size.
const BUILD_VERSION = '435';

// ====== CONFIG (edit these) ======
const API_KEY  = '';                                // bearer token
const BASE_URL = 'https://api.genai.mil/v1';        // OpenAI-compatible endpoint
const MODEL    = 'gemini-3.1-pro-preview';          // default model selected in the dropdown
const PORT     = Number(process.env.PORT ?? 18888); // local UI port
// Updates: the GitLab repo (web URL) that hosts releases/ + the moving tag that
// blesses the current production build. Blank = update checks are disabled.
// Baked in at BUILD time from ~/.code_boss/build-config.json (the build
// creates it with blank values on first run — fill in updateSource there).
// Auth = the app's own stored GitLab creds (~/.code_boss/gitlab-creds.json).
const UPDATE_SOURCE = '';          // e.g. 'https://gitlab.example.mil/group/code_boss'
const UPDATE_TAG    = 'production';
// ==================================

import { spawn, spawnSync } from 'node:child_process';
import { createInterface } from 'node:readline';
import { createServer } from 'node:http';
import net from 'node:net';
import { fileURLToPath } from 'node:url';
import { homedir, tmpdir } from 'node:os';
import { readFile as _readFile, writeFile as _writeFile, mkdir as _mkdir, stat as _stat, readdir as _readdir, unlink as _unlink, rename as _rename, copyFile as _copyFile, rm as _rm } from 'node:fs/promises';
import { existsSync, appendFileSync, mkdirSync, statSync, readFileSync, writeFileSync, renameSync, copyFileSync, readdirSync, openSync, readSync, closeSync, unlinkSync } from 'node:fs';
import { randomUUID as _randomUUID } from 'node:crypto';
import { StringDecoder } from 'node:string_decoder';
import path from 'node:path';
import zlib from 'node:zlib';
const readFile = _readFile, writeFile = _writeFile, mkdir = _mkdir, stat = _stat, readdir = _readdir, unlink = _unlink, rename = _rename, copyFile = _copyFile, rm = _rm;
const randomUUID = _randomUUID;


// ====== llm/openai-http.mjs ======
/**
 * Production OpenAI-compatible HTTP adapter — talks to api.genai.mil.
 *
 * Same surface as the dev adapter:
 *   const a = createHttpAdapter({ baseURL, apiKey })
 *   await a.complete(openaiRequest)
 *   for await (const chunk of a.completeStream(openaiRequest)) { ... }
 *
 * Production behaviours wired in here:
 *   - 401 with `unlock_url` → open the URL in the default browser, prompt the
 *     user to press Enter once they've unlocked in the browser, then retry
 *     once. The /user-ui/unlock/... endpoints are browser pages, not API
 *     endpoints — programmatic GET doesn't actually unlock the key.
 *   - 429 with Retry-After header or retry_after_seconds body → sleep, retry
 *     (max 3 attempts, exponential floor)
 *   - SSE streaming parsed into OpenAI ChatCompletionChunk async iterator
 *   - Bearer auth via Authorization header
 *   - Pass-through of unknown fields on assistant messages and tool_calls
 *     (Gemini 3 returns a `thought_signature` field that must round-trip
 *     verbatim to preserve reasoning across function calls). The adapter
 *     never strips fields; the agent loop must echo assistant messages back
 *     intact in subsequent requests.
 *
 * Diagnostics:
 *   - AGENT_DEBUG=1 logs every request's tool_call_ids (outbound) and the
 *     response's assistant.tool_calls[*].id + any non-OpenAI fields seen
 *     (thought_signature, encrypted_content, etc.) so we can verify ID
 *     round-tripping against api.genai.mil on the first real run.
 *
 * No npm deps. Node 18+ built-in fetch only.
 */


const DEFAULT_BASE_URL = 'https://api.genai.mil/v1';

// ── Image wire formats ──────────────────────────────────────────────────────
// The genai.mil shim's image support is UNKNOWN, so the SHAPE a message's
// images take on the wire is pluggable: a named-format table, selected by a
// key from ~/.code_boss/image-support.json (written by the Settings "Test
// image support" probe — see /api/probe-images in server.mjs), defaulting to
// the standard OpenAI chat-completions parts shape.
//
// A message carries images as `images: ['data:image/png;base64,...', ...]`
// alongside its string `content`. Messages WITHOUT images pass through
// UNTOUCHED (same object, byte-identical request) — parity-safe by design.
const IMAGE_WIRE_FORMATS = {
  // OpenAI chat-completions content parts (the standard).
  'openai-parts': (text, images) => [
    { type: 'text', text },
    ...images.map((u) => ({ type: 'image_url', image_url: { url: u } })),
  ],
  // Gemini-native inline_data parts (some shims pass these through verbatim).
  'gemini-inline': (text, images) => [
    { type: 'text', text },
    ...images.map((u) => {
      const m = /^data:([^;,]+);base64,(.*)$/.exec(u) || [];
      return { inline_data: { mime_type: m[1] || 'image/png', data: m[2] ?? '' } };
    }),
  ],
  // OpenAI Responses-API style input parts.
  'openai-input-image': (text, images) => [
    { type: 'input_text', text },
    ...images.map((u) => ({ type: 'input_image', image_url: u })),
  ],
};
const IMAGE_WIRE_FORMAT_NAMES = Object.keys(IMAGE_WIRE_FORMATS);
const IMAGE_SUPPORT_DEFAULT_PATH = path.join(homedir(), '.code_boss', 'image-support.json');

// Read the discovered format key ({ format, testedAt, model }). Returns:
//   - a known format name  → use it
//   - 'strip'              → the probe ran and found NOTHING works
//                            (format:null): drop images entirely so image-
//                            bearing history can't poison later requests
//   - null                 → untested (file absent/invalid/unknown name):
//                            callers fall back to 'openai-parts'
function readConfiguredImageFormat(file) {
  try {
    const j = JSON.parse(readFileSync(file, 'utf8'));
    if (!j || typeof j !== 'object' || !('format' in j)) return null;
    if (typeof j.format === 'string') return IMAGE_WIRE_FORMATS[j.format] ? j.format : null;
    if (j.format === null) return 'strip';
    return null;
  } catch { return null; }
}

/**
 * Convert any message carrying `images` into the named wire format — or, for
 * format 'strip' (endpoint tested image-blind), drop the images and keep the
 * plain text. Messages without images are returned AS-IS (same reference);
 * when NO message carries images the original array itself is returned, so
 * requests without images stay byte-identical to before this feature existed.
 */
function applyImageWireFormat(messages, format) {
  if (!Array.isArray(messages)) return messages;
  if (!messages.some((m) => m && Array.isArray(m.images) && m.images.length)) return messages;
  const shape = IMAGE_WIRE_FORMATS[format] || IMAGE_WIRE_FORMATS['openai-parts'];
  return messages.map((m) => {
    if (!m || !Array.isArray(m.images) || !m.images.length) return m;
    const { images, ...rest } = m;
    if (format === 'strip') return rest;
    const text = typeof rest.content === 'string' ? rest.content : String(rest.content ?? '');
    return { ...rest, content: shape(text, images) };
  });
}

// Standard Chrome install locations on Windows (per-user + per-machine).
const CHROME_PATHS_WIN = [
  (process.env.LOCALAPPDATA || '') + '\\Google\\Chrome\\Application\\chrome.exe',
  'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe',
  'C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe',
];
function findChromeWin() {
  for (const p of CHROME_PATHS_WIN) {
    try { if (p && existsSync(p)) return p; } catch {}
  }
  return null;
}

function openInBrowser(url) {
  // Prefer Chrome; fall back to the OS default if Chrome isn't installed.
  if (process.platform === 'win32') {
    const chrome = findChromeWin();
    if (chrome) {
      spawn(chrome, [url], { stdio: 'ignore', detached: true }).unref();
    } else {
      spawn('cmd', ['/c', 'start', '""', url], { stdio: 'ignore', detached: true, windowsHide: true }).unref();
    }
  } else if (process.platform === 'darwin') {
    spawn('open', ['-a', 'Google Chrome', url], { stdio: 'ignore', detached: true }).unref();
  } else {
    spawn('google-chrome', [url], { stdio: 'ignore', detached: true })
      .on('error', () => spawn('xdg-open', [url], { stdio: 'ignore', detached: true }).unref())
      .unref();
  }
}

function promptEnter(message) {
  return new Promise((resolve) => {
    const rl = createInterface({ input: process.stdin, output: process.stderr });
    rl.question(message, () => { rl.close(); resolve(); });
  });
}

class HttpError extends Error {
  constructor(status, statusText, body) {
    super(`HTTP ${status} ${statusText}: ${typeof body === 'string' ? body.slice(0, 500) : JSON.stringify(body).slice(0, 500)}`);
    this.status = status;
    this.statusText = statusText;
    this.body = body;
  }
}

function sleep(ms) {
  return new Promise((r) => setTimeout(r, ms));
}

// Known OpenAI fields on assistant messages and tool_calls — used to detect
// vendor-specific extras (e.g. Gemini's thought_signature).
const OPENAI_MESSAGE_FIELDS = new Set(['role', 'content', 'tool_calls', 'tool_call_id', 'refusal', 'name', 'function_call']);
const OPENAI_TOOL_CALL_FIELDS = new Set(['id', 'type', 'function', 'index']);
const OPENAI_FUNCTION_FIELDS = new Set(['name', 'arguments']);

function findExtras(obj, knownSet) {
  if (!obj || typeof obj !== 'object') return null;
  const extras = {};
  let any = false;
  for (const k of Object.keys(obj)) {
    if (!knownSet.has(k)) { extras[k] = obj[k]; any = true; }
  }
  return any ? extras : null;
}

function summarizeResponseExtras(json) {
  const out = { messageExtras: null, toolCalls: [] };
  const msg = json?.choices?.[0]?.message;
  if (!msg) return out;
  out.messageExtras = findExtras(msg, OPENAI_MESSAGE_FIELDS);
  for (const tc of msg.tool_calls ?? []) {
    out.toolCalls.push({
      id: tc.id,
      name: tc.function?.name,
      tcExtras: findExtras(tc, OPENAI_TOOL_CALL_FIELDS),
      fnExtras: findExtras(tc.function, OPENAI_FUNCTION_FIELDS),
    });
  }
  return out;
}

function summarizeRequestToolFlow(body) {
  // Pulls out the tool-related shape of what we're SENDING — to verify IDs
  // round-trip.
  const out = { toolDefs: [], assistantToolCallIds: [], toolMessageIds: [] };
  if (Array.isArray(body.tools)) {
    for (const t of body.tools) if (t.function?.name) out.toolDefs.push(t.function.name);
  }
  for (const m of body.messages ?? []) {
    if (m.role === 'assistant' && Array.isArray(m.tool_calls)) {
      for (const tc of m.tool_calls) out.assistantToolCallIds.push(tc.id);
    }
    if (m.role === 'tool' && m.tool_call_id) out.toolMessageIds.push(m.tool_call_id);
  }
  return out;
}

function parseRetryAfter(headers, body) {
  const headerVal = headers.get?.('retry-after');
  const headerSec = headerVal ? Number(headerVal) : NaN;
  const bodySec = Number(body?.retry_after_seconds);
  const candidates = [headerSec, bodySec].filter((n) => Number.isFinite(n) && n > 0);
  if (candidates.length === 0) return 1;
  return Math.max(...candidates);
}

async function readJson(res) {
  const text = await res.text();
  try {
    return JSON.parse(text);
  } catch {
    return { raw: text };
  }
}

/**
 * @param {object} opts
 * @param {string} [opts.baseURL]     Defaults to https://api.genai.mil/v1
 * @param {string} [opts.apiKey]      Bearer token. Defaults to process.env.AGENT_API_KEY.
 * @param {Function} [opts.fetchImpl] Override for tests (default: globalThis.fetch).
 * @param {number}   [opts.maxRetries] Default 3.
 * @param {Function} [opts.onUnlock]   Callback(url) when an unlock is performed (telemetry).
 * @param {Function} [opts.onRateLimit] Callback(ms) when a 429 sleep happens (telemetry).
 * @param {Function} [opts.onUsage]    Callback(usage) on successful response.
 */
function createHttpAdapter(opts = {}) {
  const baseURL = (opts.baseURL ?? process.env.AGENT_BASE_URL ?? DEFAULT_BASE_URL).replace(/\/+$/, '');
  // apiKey can be a string OR a function returning the current key. The
  // function form lets the server hot-update the key (e.g., user enters it
  // in the UI on first run) without re-creating the adapter.
  const apiKeyOpt = opts.apiKey ?? process.env.AGENT_API_KEY ?? '';
  const getApiKey = typeof apiKeyOpt === 'function' ? apiKeyOpt : () => apiKeyOpt;
  const fetchImpl = opts.fetchImpl ?? globalThis.fetch;
  const maxRetries = opts.maxRetries ?? 3;
  const onUnlock = opts.onUnlock ?? (() => {});
  const onRateLimit = opts.onRateLimit ?? (() => {});
  // Called before each network-retry backoff ({attempt,max,waitMs,cause}) and on
  // recovery — lets the host show a "Reconnecting…" status instead of a silent hang.
  const onNetworkRetry = opts.onNetworkRetry ?? (() => {});
  const onNetworkRecover = opts.onNetworkRecover ?? (() => {});
  const onUsage = opts.onUsage ?? (() => {});
  const debug = opts.debug ?? (process.env.AGENT_DEBUG === '1');
  const onTransaction = opts.onTransaction ?? (() => {});
  // Where the discovered image-format key lives (test override via opts).
  const imageSupportPath = opts.imageSupportPath ?? IMAGE_SUPPORT_DEFAULT_PATH;
  // Per-request override (request.imageFormat, used by the discovery probe) >
  // discovered key on disk > the standard OpenAI parts shape.
  const resolveImageFormat = (override) =>
    (override && IMAGE_WIRE_FORMATS[override] ? override : null)
    || readConfiguredImageFormat(imageSupportPath)
    || 'openai-parts';

  if (!fetchImpl) throw new Error('createHttpAdapter: no fetch implementation available');

  async function doFetchOnce(path, init) {
    const url = `${baseURL}${path}`;
    const apiKey = getApiKey() || '';
    const headers = {
      'content-type': 'application/json',
      accept: init?.body && JSON.parse(init.body).stream ? 'text/event-stream' : 'application/json',
      ...(apiKey ? { authorization: `Bearer ${apiKey}` } : {}),
      ...(init?.headers ?? {}),
    };
    return fetchImpl(url, { ...init, headers });
  }

  async function safeFetch(path, init) {
    let attempt = 0;
    let netAttempt = 0;
    let unlocked = false;
    while (true) {
      let res;
      try {
        res = await doFetchOnce(path, init);
        if (netAttempt > 0) onNetworkRecover();   // connection came back after retrying
      } catch (err) {
        // NETWORK-level failure: the request never got an HTTP response
        // (connection refused, DNS, reset, TLS). Node surfaces this as a bare
        // "TypeError: fetch failed" — useless to show the developer. A user
        // abort (/api/cancel) is NOT retryable. Otherwise retry a few times with
        // backoff (the endpoint/proxy may be momentarily down or restarting),
        // then throw a clear, actionable error naming the endpoint + cause.
        if (err?.name === 'AbortError') throw err;
        const cause = err?.cause?.code || err?.cause?.message || err?.code || err?.message || 'network error';
        if (netAttempt < maxRetries) {
          const waitMs = 500 * Math.pow(2, netAttempt);
          netAttempt++;
          onNetworkRetry({ attempt: netAttempt, max: maxRetries, waitMs, cause });
          await sleep(waitMs);
          continue;
        }
        const e = new Error(`could not reach the LLM endpoint ${baseURL} (${cause}) after ${maxRetries + 1} attempts. Is the endpoint/proxy running and reachable from this machine?`);
        e.cause = err;
        throw e;
      }
      if (res.ok) return res;

      if (res.status === 401 && !unlocked) {
        const body = await readJson(res);
        const unlockUrl = body?.unlock_url ?? body?.error?.unlock_url;
        if (unlockUrl) {
          // onUnlock returns a Promise that resolves when the user has
          // unlocked. The caller (server.mjs) drives the browser-side flow:
          // broadcasts an SSE 'unlock-needed' message with the URL and waits
          // for a POST /api/unlock-done from the UI. No CLI prompts; no
          // automatic browser-spawning from this layer — the user is already
          // in a browser tab, the UI handles it.
          await Promise.resolve(onUnlock(unlockUrl));
          unlocked = true;
          continue;
        }
        throw new HttpError(res.status, res.statusText, body);
      }

      if (res.status === 429 && attempt < maxRetries) {
        const body = await readJson(res);
        const waitSec = parseRetryAfter(res.headers, body);
        const waitMs = Math.max(waitSec * 1000, 500 * Math.pow(2, attempt));
        onRateLimit(waitMs);
        await sleep(waitMs);
        attempt++;
        continue;
      }

      // Transient upstream 5xx (502/503/504/500): the model backend (e.g. the
      // genai.mil shim → Gemini) momentarily failed ("upstream_error"). Back off
      // and retry like a 429 instead of surfacing a hard "Agent error". A 4xx
      // still throws (the request itself is wrong); a user abort returned above.
      if ((res.status === 502 || res.status === 503 || res.status === 504 || res.status === 500) && attempt < maxRetries) {
        const body = await readJson(res);
        const waitSec = parseRetryAfter(res.headers, body);
        const waitMs = Math.max(waitSec * 1000, 800 * Math.pow(2, attempt));
        onRateLimit(waitMs);
        await sleep(waitMs);
        attempt++;
        continue;
      }

      const body = await readJson(res);
      throw new HttpError(res.status, res.statusText, body);
    }
  }

  async function complete(request) {
    if (!request || !Array.isArray(request.messages)) {
      throw new TypeError('complete: request.messages is required');
    }
    // signal is an AbortSignal — pull it off the request before
    // stringifying the body (signal can't survive JSON), then thread it
    // through to fetch so /api/cancel aborts the in-flight HTTP call
    // instead of waiting for the model to finish.
    // imageFormat (optional, probe override) is likewise adapter-level only.
    const { signal, imageFormat, ...rest } = request;
    const body = { ...rest, stream: false };
    // Messages carrying images get their content rewritten into the active
    // image wire format; requests without images pass through untouched.
    body.messages = applyImageWireFormat(body.messages, resolveImageFormat(imageFormat));
    if (debug) {
      const flow = summarizeRequestToolFlow(body);
      console.error(`[http>] complete  tools=[${flow.toolDefs.join(',')}]`);
      if (flow.assistantToolCallIds.length) console.error(`[http>]   assistant.tool_calls ids: ${JSON.stringify(flow.assistantToolCallIds)}`);
      if (flow.toolMessageIds.length) console.error(`[http>]   tool message ids:           ${JSON.stringify(flow.toolMessageIds)}`);
    }
    const t0 = Date.now();
    // Snapshot the request shape BEFORE we send, because the agent loop
    // shares its messages array by reference and will mutate it after the
    // call returns. Without this clone, snapshots show a misleading body.
    const requestSnapshot = JSON.parse(JSON.stringify(body));
    const res = await safeFetch('/chat/completions', { method: 'POST', body: JSON.stringify(body), signal });
    const json = await res.json();
    onTransaction({ type: 'chat_completions', request: requestSnapshot, response: json, durationMs: Date.now() - t0 });
    if (debug) {
      const summary = summarizeResponseExtras(json);
      const finish = json?.choices?.[0]?.finish_reason;
      console.error(`[http<] complete  finish=${finish}`);
      if (summary.messageExtras) console.error(`[http<]   message extras: ${JSON.stringify(summary.messageExtras).slice(0, 400)}`);
      for (const tc of summary.toolCalls) {
        console.error(`[http<]   tool_call id=${tc.id} name=${tc.name}` + (tc.tcExtras ? ` extras=${JSON.stringify(tc.tcExtras)}` : '') + (tc.fnExtras ? ` fn_extras=${JSON.stringify(tc.fnExtras)}` : ''));
      }
    }
    if (json.usage) onUsage(json.usage);
    return json;
  }

  async function* completeStream(request) {
    if (!request || !Array.isArray(request.messages)) {
      throw new TypeError('completeStream: request.messages is required');
    }
    const t0 = Date.now();
    const { signal: streamSignal, imageFormat: streamImageFormat, ...streamRest } = request;
    const body = { ...streamRest, stream: true };
    // Same image-format rewrite as complete(); no-images requests untouched.
    body.messages = applyImageWireFormat(body.messages, resolveImageFormat(streamImageFormat));
    // Snapshot the request BEFORE we wire stream:true on, so the captured
    // transaction shows what the agent actually passed in (without the
    // streaming flag we added internally).
    const requestSnapshot = JSON.parse(JSON.stringify(request));
    if (debug) {
      const flow = summarizeRequestToolFlow(body);
      console.error(`[http>] completeStream  tools=[${flow.toolDefs.join(',')}]`);
      if (flow.assistantToolCallIds.length) console.error(`[http>]   assistant.tool_calls ids: ${JSON.stringify(flow.assistantToolCallIds)}`);
      if (flow.toolMessageIds.length) console.error(`[http>]   tool message ids:           ${JSON.stringify(flow.toolMessageIds)}`);
    }

    // Assembled state — populated as we stream, then handed to onTransaction
    // when the stream ends (regardless of which exit path). Without this, the
    // streaming path never emits a transaction event, so the Snapshot view
    // shows "(no request captured)" and the analyzer can't detect issues
    // like missing system reminders or Gemini identity drift.
    let accumulatedContent = '';
    let lastFinishReason = null;
    let lastChunkId = null;
    let lastUsage = null;
    let txEmitted = false;
    const emitTransaction = (extra = {}) => {
      if (txEmitted) return;
      txEmitted = true;
      const synthesized = {
        id: lastChunkId ?? 'stream',
        object: 'chat.completion',
        choices: [{
          index: 0,
          message: { role: 'assistant', content: accumulatedContent },
          finish_reason: lastFinishReason ?? 'stop',
        }],
        ...(lastUsage ? { usage: lastUsage } : {}),
        ...extra,
      };
      try {
        onTransaction({
          type: 'chat_completions',
          request: requestSnapshot,
          response: synthesized,
          durationMs: Date.now() - t0,
          streamed: true,
        });
      } catch { /* never let telemetry break the stream */ }
    };

    try {
      const res = await safeFetch('/chat/completions', { method: 'POST', body: JSON.stringify(body), signal: streamSignal });

      // Graceful fallback: if the endpoint ignored stream:true and returned a
      // normal JSON completion (some managed platforms do), yield it as a single
      // chunk so the caller's streaming loop still works.
      const ctype = res.headers.get('content-type') || '';
      if (!ctype.includes('text/event-stream')) {
        const json = await res.json();
        if (json?.usage) { lastUsage = json.usage; onUsage(json.usage); }
        const msg = json?.choices?.[0]?.message ?? {};
        accumulatedContent = msg.content ?? '';
        lastFinishReason = json?.choices?.[0]?.finish_reason ?? 'stop';
        lastChunkId = json?.id ?? null;
        // Hand the FULL non-stream JSON to onTransaction since we have it.
        txEmitted = true;
        try {
          onTransaction({
            type: 'chat_completions',
            request: requestSnapshot,
            response: json,
            durationMs: Date.now() - t0,
            streamed: false,
          });
        } catch {}
        yield {
          id: json?.id ?? 'nostream',
          object: 'chat.completion.chunk',
          choices: [{ index: 0, delta: { role: 'assistant', content: msg.content ?? '' }, finish_reason: lastFinishReason }],
          ...(json?.usage ? { usage: json.usage } : {}),
        };
        return;
      }

      if (!res.body) throw new Error('completeStream: response has no body');

      const reader = res.body.getReader();
      const decoder = new TextDecoder();
      let buf = '';
      let usageFired = false;
      const fireUsageOnce = (u) => {
        if (u && !usageFired) {
          usageFired = true;
          lastUsage = u;
          onUsage(u);
        }
      };

      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        buf += decoder.decode(value, { stream: true });

        let idx;
        while ((idx = buf.indexOf('\n')) !== -1) {
          const line = buf.slice(0, idx).replace(/\r$/, '');
          buf = buf.slice(idx + 1);
          if (!line || line.startsWith(':')) continue;
          if (!line.startsWith('data:')) continue;
          const data = line.slice(5).trim();
          if (data === '[DONE]') return;
          try {
            const chunk = JSON.parse(data);
            fireUsageOnce(chunk.usage);
            if (chunk?.id) lastChunkId = chunk.id;
            const delta = chunk?.choices?.[0]?.delta;
            if (delta?.content) accumulatedContent += delta.content;
            const fr = chunk?.choices?.[0]?.finish_reason;
            if (fr) lastFinishReason = fr;
            yield chunk;
          } catch {
            // ignore malformed line
          }
        }
      }
    } finally {
      // Fires on normal end, early consumer return(), or thrown error —
      // ensures the transaction is captured no matter how the stream exits.
      emitTransaction();
    }
  }

  async function listModels() {
    const t0 = Date.now();
    const res = await safeFetch('/models', { method: 'GET' });
    const json = await res.json();
    onTransaction({ type: 'list_models', request: null, response: json, durationMs: Date.now() - t0 });
    return json;
  }

  return { complete, completeStream, listModels };
}

// ====== llm/anthropic-http.mjs ======
/**
 * Anthropic Messages API adapter — talks to an Anthropic-format endpoint
 * (e.g. a local proxy at http://127.0.0.1:8080 that adds the key and drives
 * Claude). Same SURFACE as the OpenAI adapter (createHttpAdapter):
 *
 *   const a = createAnthropicAdapter({ baseURL, apiKey })
 *   await a.complete(openaiRequest)            -> openai-shaped response
 *   for await (const c of a.completeStream(req)) { ... }  -> openai-shaped chunks
 *   await a.listModels()                       -> { data: [...] }
 *
 * code_boss speaks one wire shape internally: OpenAI chat-completions
 * (`{model, messages:[{role,content}]}` in; `{choices:[{message:{content}}]}`
 * out; stream chunks `{choices:[{delta:{content}}]}`). This adapter translates
 * THAT to/from Anthropic's `/v1/messages`:
 *   - hoists any role:'system' messages into the top-level `system` field
 *   - coalesces consecutive same-role turns (Anthropic requires alternation)
 *   - sends the required `max_tokens` + `anthropic-version` header
 *   - joins Anthropic `content:[{type:'text',text}]` blocks back into a string
 *   - maps usage (input/output_tokens -> prompt/completion/total_tokens) so the
 *     server's onUsage hook is unchanged
 *   - parses Anthropic SSE (content_block_delta) into OpenAI chunks
 *
 * The agent uses the prose <remote-workspace> tool protocol, NOT OpenAI/
 * Anthropic function-calling, so requests are text-only (no `tools` array) —
 * which keeps this translation simple and lossless.
 *
 * No npm deps. Node 18+ built-in fetch only.
 */

const ANTHROPIC_DEFAULT_BASE_URL = 'http://127.0.0.1:8080/v1';
const DEFAULT_MAX_TOKENS = 16384;
const ANTHROPIC_VERSION = '2023-06-01';

class AnthropicHttpError extends Error {
  constructor(status, statusText, body) {
    super(`HTTP ${status} ${statusText}: ${typeof body === 'string' ? body.slice(0, 500) : JSON.stringify(body).slice(0, 500)}`);
    this.status = status; this.statusText = statusText; this.body = body;
  }
}
function anthSleep(ms) { return new Promise((r) => setTimeout(r, ms)); }
async function anthReadJson(res) { const t = await res.text(); try { return JSON.parse(t); } catch { return { raw: t }; } }

// OpenAI finish_reason from Anthropic stop_reason.
function mapStop(stopReason) {
  switch (stopReason) {
    case 'end_turn': case 'stop_sequence': return 'stop';
    case 'max_tokens': return 'length';
    case 'tool_use': return 'tool_calls';
    default: return stopReason ? 'stop' : null;
  }
}

// OpenAI usage from Anthropic usage.
function mapUsage(u) {
  if (!u) return null;
  const prompt = u.input_tokens ?? 0;
  const completion = u.output_tokens ?? 0;
  return { prompt_tokens: prompt, completion_tokens: completion, total_tokens: prompt + completion };
}

/**
 * Translate an OpenAI-shaped chat request into an Anthropic /v1/messages body.
 * Returns { body, system }. `stream` is set by the caller.
 */
function toAnthropicBody(request, defaultMaxTokens) {
  const src = Array.isArray(request.messages) ? request.messages : [];
  const systemParts = [];
  const turns = [];
  for (const m of src) {
    const content = typeof m.content === 'string' ? m.content : (Array.isArray(m.content) ? m.content.map((b) => (typeof b === 'string' ? b : b?.text || '')).join('') : String(m.content ?? ''));
    if (m.role === 'system') { if (content.trim()) systemParts.push(content); continue; }
    const role = m.role === 'assistant' ? 'assistant' : 'user';   // map tool/function/etc. → user
    // Coalesce consecutive same-role turns (Anthropic requires strict
    // user/assistant alternation).
    const last = turns[turns.length - 1];
    if (last && last.role === role) last.content += '\n\n' + content;
    else turns.push({ role, content });
  }
  // Anthropic requires the first turn to be 'user' and the list non-empty.
  while (turns.length && turns[0].role === 'assistant') turns.shift();
  if (turns.length === 0) turns.push({ role: 'user', content: '(no input)' });
  // Anthropic ALSO requires the conversation to END with a user message (no
  // trailing-assistant "prefill"). The agent loop legitimately ends with an
  // assistant turn right after <next-task> closes a task (the freshly-opened
  // task has no user message yet) — OpenAI/Gemini accept that, Anthropic 400s.
  // Append a minimal user continuation so the model can conclude or continue.
  if (turns[turns.length - 1].role === 'assistant') {
    turns.push({ role: 'user', content: 'Continue. If everything the previous request asked for is already done, reply with a brief confirmation and make no further tool calls.' });
  }
  const body = {
    model: request.model,
    max_tokens: Number(request.max_tokens) > 0 ? Number(request.max_tokens) : defaultMaxTokens,
    messages: turns.map((t) => ({ role: t.role, content: t.content })),
  };
  if (request.temperature != null) body.temperature = request.temperature;
  const system = systemParts.join('\n\n');
  if (system) body.system = system;
  return body;
}

// Anthropic message response → OpenAI chat-completion response.
function toOpenAiResponse(json) {
  const text = Array.isArray(json?.content)
    ? json.content.filter((b) => b && b.type === 'text').map((b) => b.text || '').join('')
    : '';
  const usage = mapUsage(json?.usage);
  return {
    id: json?.id ?? 'msg',
    object: 'chat.completion',
    model: json?.model,
    choices: [{ index: 0, message: { role: 'assistant', content: text }, finish_reason: mapStop(json?.stop_reason) ?? 'stop' }],
    ...(usage ? { usage } : {}),
  };
}

function createAnthropicAdapter(opts = {}) {
  const baseURL = (opts.baseURL ?? process.env.AGENT_BASE_URL ?? ANTHROPIC_DEFAULT_BASE_URL).replace(/\/+$/, '');
  const apiKeyOpt = opts.apiKey ?? process.env.AGENT_API_KEY ?? '';
  const getApiKey = typeof apiKeyOpt === 'function' ? apiKeyOpt : () => apiKeyOpt;
  const fetchImpl = opts.fetchImpl ?? globalThis.fetch;
  // The proxy can momentarily 429 from its direct-API path (warm-pool /
  // concurrency); be patient — retry several times with a real backoff.
  const maxRetries = opts.maxRetries ?? 6;
  const defaultMaxTokens = Number(opts.maxTokens ?? process.env.AGENT_MAX_TOKENS) > 0 ? Number(opts.maxTokens ?? process.env.AGENT_MAX_TOKENS) : DEFAULT_MAX_TOKENS;
  const onUnlock = opts.onUnlock ?? (() => {});
  const onRateLimit = opts.onRateLimit ?? (() => {});
  const onNetworkRetry = opts.onNetworkRetry ?? (() => {});
  const onNetworkRecover = opts.onNetworkRecover ?? (() => {});
  const onUsage = opts.onUsage ?? (() => {});
  const onTransaction = opts.onTransaction ?? (() => {});
  const debug = opts.debug ?? (process.env.AGENT_DEBUG === '1');
  if (!fetchImpl) throw new Error('createAnthropicAdapter: no fetch implementation available');

  async function doFetchOnce(path, init, stream) {
    const url = `${baseURL}${path}`;
    const apiKey = getApiKey() || '';
    const headers = {
      'content-type': 'application/json',
      accept: stream ? 'text/event-stream' : 'application/json',
      'anthropic-version': ANTHROPIC_VERSION,
      ...(apiKey ? { 'x-api-key': apiKey } : {}),   // proxy adds the key; only set if we actually have one
      ...(init?.headers ?? {}),
    };
    return fetchImpl(url, { ...init, headers });
  }

  async function safeFetch(path, init, stream) {
    let attempt = 0, netAttempt = 0, unlocked = false;
    while (true) {
      let res;
      try {
        res = await doFetchOnce(path, init, stream);
        if (netAttempt > 0) onNetworkRecover();
      } catch (err) {
        // Network-level failure (no HTTP response): refused/DNS/reset/TLS — Node
        // surfaces it as a bare "fetch failed". A user abort is not retryable;
        // otherwise retry with backoff (the proxy may be down/restarting), then
        // throw a clear error naming the endpoint + cause.
        if (err?.name === 'AbortError') throw err;
        const cause = err?.cause?.code || err?.cause?.message || err?.code || err?.message || 'network error';
        if (netAttempt < maxRetries) {
          const waitMs = 500 * Math.pow(2, Math.min(netAttempt, 5));
          netAttempt++;
          onNetworkRetry({ attempt: netAttempt, max: maxRetries, waitMs, cause });
          await anthSleep(waitMs);
          continue;
        }
        const e = new Error(`could not reach the LLM endpoint ${baseURL} (${cause}) after ${maxRetries + 1} attempts. Is the proxy/endpoint running and reachable?`);
        e.cause = err;
        throw e;
      }
      if (res.ok) return res;
      if (res.status === 401 && !unlocked) {
        const body = await anthReadJson(res);
        const unlockUrl = body?.unlock_url ?? body?.error?.unlock_url;
        if (unlockUrl) { await Promise.resolve(onUnlock(unlockUrl)); unlocked = true; continue; }
        throw new AnthropicHttpError(res.status, res.statusText, body);
      }
      if (res.status === 429 && attempt < maxRetries) {
        const body = await anthReadJson(res);
        const hdr = Number(res.headers.get?.('retry-after'));
        // Floor at ~1.5s, exponential, capped at 20s — rides out a busy proxy.
        const waitMs = Math.min(20000, Math.max((Number.isFinite(hdr) && hdr > 0 ? hdr : 1) * 1000, 1500 * Math.pow(2, attempt)));
        onRateLimit(waitMs); await anthSleep(waitMs); attempt++; continue;
      }
      // Transient upstream 5xx (502/503/504/500): a momentary backend blip —
      // back off + retry like a 429 rather than failing the run hard.
      if ((res.status === 502 || res.status === 503 || res.status === 504 || res.status === 500) && attempt < maxRetries) {
        const body = await anthReadJson(res);
        const hdr = Number(res.headers.get?.('retry-after'));
        const waitMs = Math.min(20000, Math.max((Number.isFinite(hdr) && hdr > 0 ? hdr : 1) * 1000, 1500 * Math.pow(2, attempt)));
        onRateLimit(waitMs); await anthSleep(waitMs); attempt++; continue;
      }
      const body = await anthReadJson(res);
      throw new AnthropicHttpError(res.status, res.statusText, body);
    }
  }

  async function complete(request) {
    if (!request || !Array.isArray(request.messages)) throw new TypeError('complete: request.messages is required');
    const { signal } = request;
    const body = toAnthropicBody(request, defaultMaxTokens);
    const requestSnapshot = JSON.parse(JSON.stringify({ model: request.model, messages: request.messages }));
    const t0 = Date.now();
    if (debug) console.error(`[anthropic>] complete model=${body.model} msgs=${body.messages.length} sys=${body.system ? body.system.length + 'c' : 'none'}`);
    const res = await safeFetch('/messages', { method: 'POST', body: JSON.stringify(body), signal }, false);
    const json = await res.json();
    const oai = toOpenAiResponse(json);
    onTransaction({ type: 'chat_completions', request: requestSnapshot, response: oai, durationMs: Date.now() - t0 });
    if (debug) console.error(`[anthropic<] complete stop=${json?.stop_reason} chars=${oai.choices[0].message.content.length}`);
    if (oai.usage) onUsage(oai.usage);
    return oai;
  }

  async function* completeStream(request) {
    if (!request || !Array.isArray(request.messages)) throw new TypeError('completeStream: request.messages is required');
    const { signal } = request;
    const body = { ...toAnthropicBody(request, defaultMaxTokens), stream: true };
    const requestSnapshot = JSON.parse(JSON.stringify({ model: request.model, messages: request.messages }));
    const t0 = Date.now();
    let accumulated = '';
    let stopReason = null;
    let usageIn = 0, usageOut = 0, usageFired = false;
    let txEmitted = false;
    const emitTx = () => {
      if (txEmitted) return; txEmitted = true;
      const usage = mapUsage({ input_tokens: usageIn, output_tokens: usageOut });
      try {
        onTransaction({
          type: 'chat_completions', request: requestSnapshot, streamed: true, durationMs: Date.now() - t0,
          response: { id: 'stream', object: 'chat.completion', choices: [{ index: 0, message: { role: 'assistant', content: accumulated }, finish_reason: mapStop(stopReason) ?? 'stop' }], ...(usage ? { usage } : {}) },
        });
      } catch {}
    };
    try {
      const res = await safeFetch('/messages', { method: 'POST', body: JSON.stringify(body), signal }, true);
      const ctype = res.headers.get('content-type') || '';
      // Graceful fallback: endpoint ignored stream:true and returned plain JSON.
      if (!ctype.includes('text/event-stream')) {
        const json = await res.json();
        const oai = toOpenAiResponse(json);
        if (oai.usage) onUsage(oai.usage);
        accumulated = oai.choices[0].message.content; stopReason = json?.stop_reason;
        if (json?.usage) { usageIn = json.usage.input_tokens ?? 0; usageOut = json.usage.output_tokens ?? 0; }
        txEmitted = true;
        try { onTransaction({ type: 'chat_completions', request: requestSnapshot, response: oai, durationMs: Date.now() - t0, streamed: false }); } catch {}
        yield { id: oai.id, object: 'chat.completion.chunk', choices: [{ index: 0, delta: { role: 'assistant', content: accumulated }, finish_reason: oai.choices[0].finish_reason }], ...(oai.usage ? { usage: oai.usage } : {}) };
        return;
      }
      if (!res.body) throw new Error('completeStream: response has no body');
      const reader = res.body.getReader();
      const decoder = new TextDecoder();
      let buf = '';
      const fireUsage = () => { if (!usageFired && (usageIn || usageOut)) { usageFired = true; const u = mapUsage({ input_tokens: usageIn, output_tokens: usageOut }); if (u) onUsage(u); } };
      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        buf += decoder.decode(value, { stream: true });
        let idx;
        while ((idx = buf.indexOf('\n')) !== -1) {
          const line = buf.slice(0, idx).replace(/\r$/, '');
          buf = buf.slice(idx + 1);
          if (!line || line.startsWith(':') || line.startsWith('event:')) continue;
          if (!line.startsWith('data:')) continue;
          const data = line.slice(5).trim();
          if (!data || data === '[DONE]') continue;
          let ev; try { ev = JSON.parse(data); } catch { continue; }
          const type = ev.type;
          if (type === 'message_start') { usageIn = ev.message?.usage?.input_tokens ?? usageIn; }
          else if (type === 'content_block_delta') {
            const text = ev.delta?.text ?? (ev.delta?.type === 'text_delta' ? ev.delta.text : '') ?? '';
            if (text) { accumulated += text; yield { id: 'stream', object: 'chat.completion.chunk', choices: [{ index: 0, delta: { content: text }, finish_reason: null }] }; }
          } else if (type === 'message_delta') {
            if (ev.delta?.stop_reason) stopReason = ev.delta.stop_reason;
            if (ev.usage?.output_tokens != null) usageOut = ev.usage.output_tokens;
          } else if (type === 'message_stop') {
            fireUsage();
          } else if (type === 'error') {
            throw new AnthropicHttpError(500, 'anthropic-stream-error', ev.error || ev);
          }
        }
      }
      fireUsage();
    } finally {
      emitTx();
    }
  }

  async function listModels() {
    const t0 = Date.now();
    const res = await safeFetch('/models', { method: 'GET' }, false);
    const json = await res.json();
    onTransaction({ type: 'list_models', request: null, response: json, durationMs: Date.now() - t0 });
    return json;
  }

  return { complete, completeStream, listModels };
}

// ====== agent/diff.mjs ======
/**
 * Small unified-diff helper. Zero deps.
 *
 * Why custom: we only need a readable line-by-line diff for the chat UI; we
 * don't want to add a package. The implementation is a Myers-style LCS with
 * the diff serialized as { type: 'same'|'add'|'del', line: '...' } entries.
 *
 * Public API:
 *   diffLines(a, b)            -> [{type, line}, ...]
 *   renderUnifiedDiff(a, b, opts)  -> string in unified-diff style
 *   shortDiffSummary(a, b)     -> "+12 / -3"
 */

// Above this many LCS cells ((n+1)*(m+1)) we refuse to build the table.
// The dp table is (n+1)*(m+1) Uint32 entries = 4 bytes each; 4M cells = 16MB.
// A 20k-line file diffed against itself would be ~1.6GB and OOM-crash the
// whole single-process server. Beyond the cap we fall back to a cheap
// line-count diff so a giant write degrades gracefully instead of crashing.
const MAX_LCS_CELLS = 4_000_000;

/** True when an LCS diff of these two texts would allocate beyond MAX_LCS_CELLS. */
function diffTooLarge(oldText, newText) {
  const n = (oldText ?? '').split('\n').length;
  const m = (newText ?? '').split('\n').length;
  return (n + 1) * (m + 1) > MAX_LCS_CELLS;
}

/** Compute LCS table for two arrays. O(n*m) time and memory; fine for files up to a few thousand lines. */
function lcsTable(a, b) {
  const n = a.length, m = b.length;
  const dp = Array.from({ length: n + 1 }, () => new Uint32Array(m + 1));
  for (let i = 1; i <= n; i++) {
    for (let j = 1; j <= m; j++) {
      dp[i][j] = a[i - 1] === b[j - 1] ? dp[i - 1][j - 1] + 1 : Math.max(dp[i - 1][j], dp[i][j - 1]);
    }
  }
  return dp;
}

/**
 * Walk back through the LCS table to build the diff sequence.
 * Returns entries in original (top-to-bottom) order.
 */
function diffLines(oldText, newText) {
  const a = (oldText ?? '').split('\n');
  const b = (newText ?? '').split('\n');
  const dp = lcsTable(a, b);
  const out = [];
  let i = a.length, j = b.length;
  while (i > 0 && j > 0) {
    if (a[i - 1] === b[j - 1]) { out.push({ type: 'same', line: a[i - 1] }); i--; j--; }
    else if (dp[i - 1][j] >= dp[i][j - 1]) { out.push({ type: 'del', line: a[i - 1] }); i--; }
    else { out.push({ type: 'add', line: b[j - 1] }); j--; }
  }
  while (i > 0) { out.push({ type: 'del', line: a[i - 1] }); i--; }
  while (j > 0) { out.push({ type: 'add', line: b[j - 1] }); j--; }
  out.reverse();
  return out;
}

/**
 * Render a unified-diff-like string. Context is the number of unchanged lines
 * to keep around each hunk. Default 2.
 */
function renderUnifiedDiff(oldText, newText, { context = 2, pathLabel = 'file' } = {}) {
  if (diffTooLarge(oldText, newText)) {
    const { add, del } = countLineDelta(oldText, newText);
    return `--- ${pathLabel}\n+++ ${pathLabel}\n@@ (diff skipped: file too large) @@\n(approximately +${add} / -${del} lines; full diff omitted to avoid excessive memory use)`;
  }
  const diff = diffLines(oldText, newText);
  // Find hunk ranges (contiguous regions of changes, padded by `context`).
  const lines = [];
  lines.push(`--- ${pathLabel}`);
  lines.push(`+++ ${pathLabel}`);
  let i = 0;
  while (i < diff.length) {
    if (diff[i].type === 'same') { i++; continue; }
    let start = Math.max(0, i - context);
    let end = i;
    while (end < diff.length) {
      if (diff[end].type !== 'same') { end++; continue; }
      // is this a long run of 'same'?
      let runLen = 0;
      while (end + runLen < diff.length && diff[end + runLen].type === 'same') runLen++;
      if (runLen > context * 2) { end += context; break; }
      end += runLen;
    }
    end = Math.min(diff.length, end);
    let oldStart = 0, newStart = 0;
    for (let k = 0; k < start; k++) {
      if (diff[k].type !== 'add') oldStart++;
      if (diff[k].type !== 'del') newStart++;
    }
    let oldCount = 0, newCount = 0;
    for (let k = start; k < end; k++) {
      if (diff[k].type !== 'add') oldCount++;
      if (diff[k].type !== 'del') newCount++;
    }
    lines.push(`@@ -${oldStart + 1},${oldCount} +${newStart + 1},${newCount} @@`);
    for (let k = start; k < end; k++) {
      const e = diff[k];
      if (e.type === 'same') lines.push(' ' + e.line);
      else if (e.type === 'add') lines.push('+' + e.line);
      else lines.push('-' + e.line);
    }
    i = end;
  }
  return lines.join('\n');
}

function shortDiffSummary(oldText, newText) {
  if (diffTooLarge(oldText, newText)) {
    const { add, del } = countLineDelta(oldText, newText);
    return `+${add} / -${del} (approx)`;
  }
  const diff = diffLines(oldText, newText);
  let add = 0, del = 0;
  for (const d of diff) { if (d.type === 'add') add++; else if (d.type === 'del') del++; }
  return `+${add} / -${del}`;
}

/**
 * Cheap O(n) approximation used when the LCS path is skipped: a multiset diff
 * of lines. `add` = new lines with no matching old line; `del` = old lines
 * with no matching new line. Not a true edit script, but a reasonable
 * +added / -removed estimate that allocates only line-frequency maps.
 */
function countLineDelta(oldText, newText) {
  const a = (oldText ?? '').split('\n');
  const b = (newText ?? '').split('\n');
  const freq = (arr) => { const m = new Map(); for (const l of arr) m.set(l, (m.get(l) || 0) + 1); return m; };
  const fa = freq(a), fb = freq(b);
  let add = 0, del = 0;
  for (const [line, nb] of fb) add += Math.max(0, nb - (fa.get(line) || 0));
  for (const [line, na] of fa) del += Math.max(0, na - (fb.get(line) || 0));
  return { add, del };
}

// ====== agent/codebase-memory.mjs ======
/**
 * Tier-1 CODEBASE MEMORY — durable, agent-maintained facts about THIS codebase.
 *
 * The three-tier memory model:
 *   Layer 0  human AGENTS.md               (developer-authored; project-context.mjs)
 *   Tier 1   agent codebase memory         (THIS FILE — what the agent learns
 *            about the app: architecture, conventions, gotchas, where things
 *            live, build/deploy specifics; persists across assignments)
 *   Tier 2   per-assignment memory         (the assignment record; assignments.mjs)
 *
 * The agent maintains it with the <remember>/<forget> tools during work; it is
 * injected into the worker's prompt every run so accrued knowledge compounds.
 * The developer can review/curate it in the UI (it is plain, human-readable).
 *
 * Storage (per project): <project>/.code_boss/codebase-memory.json
 *   { version, nextId, entries: [{ id, category, note, at }] }
 * Atomic writes (temp + rename) since it outlives any one conversation. NO LLM
 * calls here — capture is explicit (a tool) or human (the UI editor).
 */

const DIR = (projectDir) => path.join(projectDir, '.code_boss');
const FILE = (projectDir) => path.join(DIR(projectDir), 'codebase-memory.json');

// Bounds: keep the injected memory from blowing out the prompt. The human can
// always prune in the UI; these are guard rails, not policy.
const MAX_NOTE_LEN = 600;
const MAX_ENTRIES = 200;
const KNOWN_CATEGORIES = ['architecture', 'conventions', 'gotchas', 'where-things-live', 'build-deploy', 'general'];

function normNote(s) { return String(s || '').trim().replace(/\s+/g, ' ').toLowerCase(); }
function uid() { return process.pid + '-' + randomUUID().slice(0, 8); }

// Per-project write serialization. load->mutate->save must be atomic with
// respect to other writers: the worker's <remember>/<forget> tool calls and the
// post-accept consolidation loop can run concurrently against the same file,
// and an unserialized read-modify-write loses updates / duplicates ids. Chain a
// promise per project file; fn runs after the previous write settles.
const _writeChains = new Map();
function withMemoryLock(projectDir, fn) {
  const key = FILE(projectDir);
  const prev = _writeChains.get(key) || Promise.resolve();
  const next = prev.then(fn, fn);          // run fn regardless of prior outcome
  _writeChains.set(key, next.then(() => {}, () => {}));   // keep the chain rejection-free
  return next;
}

async function loadRaw(projectDir) {
  let text;
  try {
    text = await readFile(FILE(projectDir), 'utf8');
  } catch (e) {
    // A fresh store is correct ONLY when the file genuinely doesn't exist yet.
    if (e && e.code === 'ENOENT') return { version: 1, nextId: 1, entries: [] };
    throw e;   // real read error (EACCES/EBUSY/…) — surface it, never silently wipe
  }
  try {
    const j = JSON.parse(text);
    if (!j || !Array.isArray(j.entries)) throw new Error('bad shape');
    return j;
  } catch {
    // The file EXISTS but won't parse (a hand-edit typo, or — pre-mutex — a torn
    // write). NEVER let the next save overwrite real data with an empty store:
    // move the unparseable file aside so it's recoverable, then start fresh.
    try { await rename(FILE(projectDir), FILE(projectDir) + '.corrupt-' + uid()); } catch {}
    return { version: 1, nextId: 1, entries: [] };
  }
}

async function saveRaw(projectDir, mem) {
  await mkdir(DIR(projectDir), { recursive: true });
  const file = FILE(projectDir);
  const tmp = file + '.' + uid() + '.tmp';   // unique per write — never shared
  await writeFile(tmp, JSON.stringify(mem, null, 2), 'utf8');
  await rename(tmp, file);
}

/** All memory entries (in insertion order). */
async function loadMemory(projectDir) {
  if (!projectDir) return { version: 1, nextId: 1, entries: [] };
  return loadRaw(projectDir);
}

/** Record a durable fact. Dedups on normalized note text (returns the existing
 *  entry instead of a duplicate). Returns { entry, added, total }. */
async function rememberFact(projectDir, { category = 'general', note } = {}) {
  if (!projectDir) throw new Error('no project');
  const clean = String(note || '').trim().slice(0, MAX_NOTE_LEN);
  if (!clean) throw new Error('note is required');
  const cat = KNOWN_CATEGORIES.includes(String(category)) ? String(category) : 'general';
  return withMemoryLock(projectDir, async () => {
    const mem = await loadRaw(projectDir);
    const key = normNote(clean);
    const existing = mem.entries.find((e) => normNote(e.note) === key);
    if (existing) return { entry: existing, added: false, total: mem.entries.length };
    if (mem.entries.length >= MAX_ENTRIES) {
      return { entry: null, added: false, total: mem.entries.length, full: true };
    }
    const entry = { id: `M-${mem.nextId}`, category: cat, note: clean, at: Date.now() };
    mem.nextId++;
    mem.entries.push(entry);
    await saveRaw(projectDir, mem);
    return { entry, added: true, total: mem.entries.length };
  });
}

/** Remove a fact by id (or by exact note match). Returns { removed }. */
async function forgetFact(projectDir, { id, note } = {}) {
  if (!projectDir) throw new Error('no project');
  return withMemoryLock(projectDir, async () => {
    const mem = await loadRaw(projectDir);
    const before = mem.entries.length;
    const key = note ? normNote(note) : null;
    mem.entries = mem.entries.filter((e) => !(id && e.id === id) && !(key && normNote(e.note) === key));
    const removed = before - mem.entries.length;
    if (removed) await saveRaw(projectDir, mem);
    return { removed };
  });
}

/** Replace the whole entry set (used by the UI curation editor). `entries` may
 *  be raw {category, note} objects; ids/timestamps are (re)assigned. */
async function replaceEntries(projectDir, entries) {
  if (!projectDir) throw new Error('no project');
  return withMemoryLock(projectDir, async () => {
    const mem = await loadRaw(projectDir);
    const list = Array.isArray(entries) ? entries : [];
    let nextId = mem.nextId;
    const seen = new Set();      // normalized notes (dedup)
    const seenIds = new Set();   // assigned ids (no two entries share an id)
    const out = [];
    for (const e of list) {
      const clean = String(e?.note || '').trim().slice(0, MAX_NOTE_LEN);
      if (!clean) continue;
      const key = normNote(clean);
      if (seen.has(key)) continue;
      seen.add(key);
      const cat = KNOWN_CATEGORIES.includes(String(e?.category)) ? String(e.category) : 'general';
      // Preserve a valid, not-yet-used supplied id; otherwise mint a fresh one.
      let id = (typeof e?.id === 'string' && /^M-\d+$/.test(e.id) && !seenIds.has(e.id)) ? e.id : `M-${nextId++}`;
      while (seenIds.has(id)) id = `M-${nextId++}`;   // guard against a mint colliding with a later supplied id
      seenIds.add(id);
      out.push({ id, category: cat, note: clean, at: Number(e?.at) || Date.now() });
      if (out.length >= MAX_ENTRIES) break;
    }
    const used = out.map((e) => Number((e.id.match(/^M-(\d+)$/) || [])[1]) || 0);
    mem.nextId = Math.max(nextId, ...used.map((n) => n + 1), 1);   // strictly past every used id
    mem.entries = out;
    await saveRaw(projectDir, mem);
    return mem;
  });
}

/** Render the memory as a compact, prompt-ready (and human-readable) block.
 *  Empty memory → ''. Grouped by category, capped to `budgetChars`. */
function renderMemory(memOrEntries, { budgetChars = 6000 } = {}) {
  const entries = Array.isArray(memOrEntries) ? memOrEntries : (memOrEntries?.entries || []);
  if (!entries.length) return '';
  const byCat = new Map();
  for (const e of entries) {
    const c = e.category || 'general';
    if (!byCat.has(c)) byCat.set(c, []);
    byCat.get(c).push(e);
  }
  const order = [...KNOWN_CATEGORIES, ...[...byCat.keys()].filter((c) => !KNOWN_CATEGORIES.includes(c))];
  const lines = [];
  let used = 0;
  for (const cat of order) {
    const list = byCat.get(cat);
    if (!list || !list.length) continue;
    const header = `## ${cat}`;
    if (used + header.length > budgetChars) break;
    lines.push(header); used += header.length;
    for (const e of list) {
      // Include the id so the model can target it with <forget id="M-N"/>.
      const line = e.id ? `- [${e.id}] ${e.note}` : `- ${e.note}`;
      if (used + line.length > budgetChars) { lines.push('- […memory truncated for budget…]'); used = budgetChars; break; }
      lines.push(line); used += line.length;
    }
    if (used >= budgetChars) break;
  }
  return lines.join('\n');
}

// ====== agent/tools.mjs ======
/**
 * Initial agent tools — read-only. Pure Node, no deps.
 *
 * Each tool: { schema, impl }
 *   schema   — OpenAI function tool descriptor (description + JSON Schema)
 *   impl     — async ({ args }) => string (stringified result; errors caught
 *              and returned as 'ERROR: ...' so the loop can decide what to do)
 *
 * Result strings are capped at MAX_RESULT_BYTES to keep tool feedback from
 * blowing up the context window. Truncation is explicitly marked.
 */

/** Strip a leading UTF-8 BOM (U+FEFF) that Windows editors/tools often write. */
function stripBom(s) {
  return (typeof s === 'string' && s.charCodeAt(0) === 0xFEFF) ? s.slice(1) : s;
}

/**
 * The root that relative tool paths resolve against. Defaults to process.cwd()
 * (the server chdir's into the active project), so existing single-thread
 * behavior is unchanged. A caller may instead pass an explicit `cwd` in the
 * tool execution context (ctx.cwd) — this is the per-call cwd seam that lets
 * concurrent threads target different repos WITHOUT a global process.chdir.
 */
function toolRoot(ctx) {
  return ctx && ctx.cwd ? path.resolve(ctx.cwd) : process.cwd();
}

// Mutation isolation is provided by git WORKTREES per sub-agent (see ORCHESTRATOR.md):
// each sub-agent runs with its own worktree as cwd, so concurrent writes can't collide
// and a merge-back conflict is the deny-on-stale signal. Tools therefore write directly;
// there is no in-process write lock here.

/**
 * Resolve a WRITE path. Relative paths resolve under the tool root (ctx.cwd, or
 * process.cwd() by default); absolute paths are honored as-is.
 * We do NOT contain writes to the project: the agent legitimately edits files
 * OUTSIDE it (e.g. ~/myWlSetup.bat to clear MY_WL_SERVER before a deploy), and
 * since it can already write anywhere via PowerShell, blocking the write/edit
 * tool only pushed it onto a worse path. (Reads were already ungated.)
 */
function resolveWritePath(p, ctx) {
  const root = toolRoot(ctx);
  const abs = path.isAbsolute(p) ? path.resolve(p) : path.resolve(root, p);
  // Never let the agent WRITE into its own control state — a stray write/edit
  // into .code_boss/tasks/*.json or state.json would corrupt the task store.
  if (isControlPath(abs)) throw new Error(CONTROL_DIR_MSG);
  return abs;
}

// Directories NEVER descended into during recursive sweeps (find/glob AND
// search/grep). Dependency, build-output, VCS and cache dirs — huge and never what
// a code search is after; crawling them (esp. node_modules) is what makes a search
// "take forever". The agent can still <read>/<list>/<info> a specific path inside
// them; only the recursive walk skips them. Keep find + grep on this ONE list.
const IGNORE_DIRS = new Set([
  'node_modules', 'bower_components', 'vendor',
  '.git', '.hg', '.svn',
  'dist', 'build', 'out', 'target', '.output',
  '.next', '.nuxt', '.svelte-kit', '.gradle',
  '.idea', '.vs', '.vscode',
  'coverage', '.cache', '.parcel-cache', '.turbo',
  '__pycache__', '.venv', 'venv', '.tox', '.mypy_cache', '.pytest_cache',
  '.code_boss', '.agent',
]);

// code_boss's own control/state directory ("<project>/.code_boss"): its task
// store, assignment backlog, and conversation log. It is NEVER the developer's
// project code — the agent reading it (e.g. grepping tasks/*.json for "the
// user's request") only confuses it into treating its own state as work to do.
// grep/find/glob already skip it via IGNORE_DIRS; this guards the direct
// read_file/file_info/list_dir path those tree-walkers don't cover.
const CONTROL_DIR = '.code_boss';
function isControlPath(absPath) {
  return path.resolve(absPath).split(path.sep).includes(CONTROL_DIR);
}
const CONTROL_DIR_MSG = `${CONTROL_DIR} is code_boss's own internal control state (task store, assignments, conversation) — not the developer's project. It is off-limits; do not read, list, or write it. Work on the project source instead.`;
const TEXT_EXTS = new Set([
  '.mjs', '.js', '.ts', '.tsx', '.jsx', '.json', '.md', '.txt', '.yml', '.yaml',
  '.sh', '.ps1', '.bat', '.cmd', '.py', '.go', '.rs', '.java', '.xml', '.html',
  '.css', '.scss', '.sql', '.toml', '.ini', '.env', '.gitignore', '',
]);

// Hard ceiling per tool result. Million-token context affords generous limits;
// this only exists to refuse pathological cases (multi-MB binary-like blobs).
// When a result would exceed this, return an ERROR instead of silently
// truncating — the model can retry with offset/limit.
const MAX_RESULT_BYTES = 2 * 1024 * 1024; // 2 MB
const MAX_GREP_MATCHES = 2000;
const MAX_GLOB_RESULTS = 5000;

// Bash defaults — match Claude Code's tunables loosely.
const BASH_DEFAULT_TIMEOUT_MS = 120000;     // 2 min
const BASH_MAX_TIMEOUT_MS = 600000;         // 10 min
// Shell output capture. We keep the HEAD (what command/context) AND the TAIL
// (the result — BUILD SUCCESS/FAILURE + the actual error live at the END of a
// build log). The old code kept only a 30k HEAD and stopped accumulating, so a
// long Maven/WebLogic build's outcome was never even captured — "cut short".
// Generous so most builds aren't truncated at all; bounded so a runaway command
// can't OOM the loop.
const SHELL_OUTPUT_HEAD = 12000;            // keep the first N chars
const SHELL_OUTPUT_TAIL = 120000;           // keep the last N chars (the result)

class TooLargeError extends Error {
  constructor(actualBytes) {
    super(`result would be ${actualBytes} bytes, exceeds ${MAX_RESULT_BYTES}-byte ceiling`);
    this.actualBytes = actualBytes;
  }
}

function ensureFits(s) {
  if (s.length > MAX_RESULT_BYTES) throw new TooLargeError(s.length);
  return s;
}

function isTextFile(p) {
  const ext = path.extname(p).toLowerCase();
  if (TEXT_EXTS.has(ext)) return true;
  return false;
}

function globToRegex(glob) {
  // Auto-promote bare-filename globs to recursive. Users (and models) often
  // write `*.java` or `*Owner*` and expect it to match anywhere in the tree.
  // If the pattern has no separators and doesn't start with **, prepend **/.
  if (!glob.includes('/') && !glob.includes('\\') && !glob.startsWith('**')) {
    glob = '**/' + glob;
  }
  // Convert a glob to a regex. Supports: ** (any path), * (any non-separator), ? (one char), {a,b}.
  let re = '';
  for (let i = 0; i < glob.length; i++) {
    const c = glob[i];
    if (c === '*') {
      if (glob[i + 1] === '*') {
        // ** — any chars including /
        re += '.*';
        i++;
        if (glob[i + 1] === '/') i++;
      } else {
        re += '[^/\\\\]*';
      }
    } else if (c === '?') {
      re += '[^/\\\\]';
    } else if (c === '.') {
      re += '\\.';
    } else if (c === '{') {
      const end = glob.indexOf('}', i);
      if (end === -1) { re += '\\{'; continue; }
      const parts = glob.slice(i + 1, end).split(',').map((s) => s.replace(/\./g, '\\.'));
      re += `(?:${parts.join('|')})`;
      i = end;
    } else if ('+()|^$/\\'.includes(c)) {
      re += '\\' + c;
    } else {
      re += c;
    }
  }
  return new RegExp('^' + re + '$');
}

async function walkDir(root, { maxDepth = 12 } = {}) {
  const out = [];
  async function rec(dir, depth) {
    if (depth > maxDepth) return;
    let entries;
    try { entries = await readdir(dir, { withFileTypes: true }); }
    catch { return; }
    for (const e of entries) {
      if (e.isDirectory()) {
        if (IGNORE_DIRS.has(e.name)) continue;
        await rec(path.join(dir, e.name), depth + 1);
      } else if (e.isFile()) {
        out.push(path.join(dir, e.name));
      }
    }
  }
  await rec(root, 0);
  return out;
}

function relForDisplay(p, ctx) {
  return path.relative(toolRoot(ctx), p).split(path.sep).join('/');
}

async function safeReadText(p) {
  const s = await stat(p);
  if (s.size > MAX_RESULT_BYTES) throw new TooLargeError(s.size);
  const buf = await readFile(p);
  // Reject obvious binaries: NUL byte in first 8KB.
  const sample = buf.subarray(0, Math.min(8192, buf.length));
  for (let i = 0; i < sample.length; i++) {
    if (sample[i] === 0) throw new Error('binary file');
  }
  // Strip a leading UTF-8 BOM so the model's copied find-strings match and
  // edit_file/JSON parsing aren't thrown off by a phantom U+FEFF.
  return stripBom(buf.toString('utf8'));
}

const tools = {
  file_info: {
    schema: {
      description: 'Get metadata for a file without reading its contents. Returns byte size and line count. Cheap — use this to plan reads of large files before calling read_file.',
      parameters: {
        type: 'object',
        properties: {
          path: { type: 'string', description: 'Absolute or project-relative file path.' },
        },
        required: ['path'],
        additionalProperties: false,
      },
    },
    impl: async ({ path: p }, ctx) => {
      if (typeof p !== 'string' || !p) throw new Error('path is required');
      const abs = path.isAbsolute(p) ? p : path.join(toolRoot(ctx), p);
      if (isControlPath(abs)) throw new Error(CONTROL_DIR_MSG);
      const s = await stat(abs);
      let lines = null;
      if (s.size <= MAX_RESULT_BYTES) {
        const buf = await readFile(abs);
        const sample = buf.subarray(0, Math.min(8192, buf.length));
        let isBinary = false;
        for (let i = 0; i < sample.length; i++) if (sample[i] === 0) { isBinary = true; break; }
        lines = isBinary ? null : buf.toString('utf8').split('\n').length;
      }
      return JSON.stringify({ path: relForDisplay(abs, ctx), bytes: s.size, lines });
    },
  },

  read_file: {
    schema: {
      description: `Read a UTF-8 text file. If the slice would exceed ${MAX_RESULT_BYTES} bytes, the call ERRORs — retry with offset/limit to chunk the read. Use file_info first if you're unsure of file size.`,
      parameters: {
        type: 'object',
        properties: {
          path: { type: 'string', description: 'Absolute or project-relative file path.' },
          offset: { type: 'integer', description: 'Optional 1-indexed starting LINE number (NOT bytes). Defaults to line 1.' },
          limit: { type: 'integer', description: 'Optional max number of LINES to read (NOT bytes). If omitted, reads to end of file.' },
        },
        required: ['path'],
        additionalProperties: false,
      },
    },
    impl: async ({ path: p, offset, limit }, ctx) => {
      if (typeof p !== 'string' || !p) throw new Error('path is required');
      const abs = path.isAbsolute(p) ? p : path.join(toolRoot(ctx), p);
      if (isControlPath(abs)) throw new Error(CONTROL_DIR_MSG);
      const text = await safeReadText(abs);
      if (offset == null && limit == null) return ensureFits(text);
      const lines = text.split('\n');
      const start = Math.max(0, (offset ?? 1) - 1);
      const end = limit != null ? start + limit : lines.length;
      const slice = lines.slice(start, end).join('\n');
      return ensureFits(slice);
    },
  },

  grep: {
    schema: {
      description: 'Recursively search files in the project for a JavaScript regular expression. Returns "file:line:text" entries (forward-slash paths). Up to ' + MAX_GREP_MATCHES + ' matches. Pure Node implementation — works on Windows without grep/Git for Windows. Binary files and common build dirs (node_modules, target, dist, .git, etc.) are skipped automatically.',
      parameters: {
        type: 'object',
        properties: {
          pattern: { type: 'string', description: 'Extended-regex pattern (passed to grep -E).' },
          path: { type: 'string', description: 'Optional subdirectory to search. Defaults to project root.' },
          glob: { type: 'string', description: 'Optional filename glob (e.g. "*.java", "*.mjs").' },
          flags: { type: 'string', description: 'Optional flags. "i" → case-insensitive.' },
        },
        required: ['pattern'],
        additionalProperties: false,
      },
    },
    impl: async ({ pattern, path: subPath, glob, flags }, ctx) => {
      if (typeof pattern !== 'string' || !pattern) throw new Error('pattern is required');
      // Pure Node implementation — no external `grep` binary needed (DoD
      // Windows machines often lack Git for Windows / WSL so spawn('grep')
      // fails with ENOENT). Walks the tree, opens each text file, matches
      // each line against the pattern, and emits "file:line:content".
      //
      // We use JS RegExp instead of POSIX ERE — slightly more permissive
      // but in practice any ERE pattern parses fine here.
      let re;
      try {
        re = new RegExp(pattern, (flags ?? '').includes('i') ? 'i' : '');
      } catch (e) {
        return `ERROR: invalid pattern: ${e.message}`;
      }
      const globRe = glob ? globToRegex(glob) : null;
      const exclude = IGNORE_DIRS;   // one shared list with find/glob — see IGNORE_DIRS
      const root = toolRoot(ctx);
      const rootAbs = subPath ? path.resolve(root, subPath) : root;

      // Walk inline so we can short-circuit when MAX_GREP_MATCHES is hit.
      const out = [];
      let stopped = false;
      async function walk(dir) {
        if (stopped) return;
        let entries;
        try { entries = await readdir(dir, { withFileTypes: true }); }
        catch { return; }
        for (const e of entries) {
          if (stopped) return;
          if (e.isDirectory()) {
            if (exclude.has(e.name)) continue;
            await walk(path.join(dir, e.name));
            continue;
          }
          if (!e.isFile()) continue;
          const full = path.join(dir, e.name);
          // Glob filter applies to the path RELATIVE to cwd (forward slashes).
          if (globRe) {
            const rel = path.relative(root, full).replace(/\\/g, '/');
            if (!globRe.test(rel) && !globRe.test(e.name)) continue;
          }
          // Skip files that are obviously binary by extension. Cheap heuristic
          // beats reading every PNG / JAR / class file in a Java project.
          const lower = e.name.toLowerCase();
          if (/\.(png|jpe?g|gif|ico|bmp|webp|pdf|zip|jar|war|ear|class|exe|dll|so|dylib|bin|lock|woff2?|ttf|otf|eot|mp[34]|wav|ogg|mov|mkv|avi|tar|gz|7z|rar)$/.test(lower)) continue;
          // Read the file; skip if unreadable or if the first chunk contains
          // null bytes (binary).
          let text;
          try {
            const buf = await readFile(full);
            if (buf.length === 0) continue;
            const head = buf.subarray(0, Math.min(buf.length, 8192));
            if (head.includes(0)) continue;  // binary
            text = buf.toString('utf8');
          } catch { continue; }

          const lines = text.split(/\r?\n/);
          for (let i = 0; i < lines.length; i++) {
            if (re.test(lines[i])) {
              const relPath = path.relative(root, full).replace(/\\/g, '/') || full.replace(/\\/g, '/');
              const line = lines[i].length > 500 ? lines[i].slice(0, 500) + '…' : lines[i];
              out.push(`${relPath}:${i + 1}:${line}`);
              if (out.length >= MAX_GREP_MATCHES) {
                out.push(`[stopped at ${MAX_GREP_MATCHES} matches]`);
                stopped = true;
                return;
              }
            }
          }
        }
      }
      await walk(rootAbs);
      if (out.length === 0) return '(no matches)';
      return ensureFits(out.join('\n'));
    },
  },

  glob: {
    schema: {
      description: 'Find files in the project matching a glob pattern (e.g. "src/**/*.mjs"). Returns up to ' + MAX_GLOB_RESULTS + ' paths.',
      parameters: {
        type: 'object',
        properties: {
          pattern: { type: 'string', description: 'Glob pattern. Supports **, *, ?, {a,b}.' },
        },
        required: ['pattern'],
        additionalProperties: false,
      },
    },
    impl: async ({ pattern }, ctx) => {
      if (typeof pattern !== 'string' || !pattern) throw new Error('pattern is required');
      const re = globToRegex(pattern);
      const files = await walkDir(toolRoot(ctx));
      const out = [];
      for (const f of files) {
        const rel = relForDisplay(f, ctx).replace(/\\/g, '/');
        if (re.test(rel)) {
          out.push(rel);
          if (out.length >= MAX_GLOB_RESULTS) {
            out.push(`[stopped at ${MAX_GLOB_RESULTS} results]`);
            break;
          }
        }
      }
      return ensureFits(out.length ? out.join('\n') : '(no matches)');
    },
  },

  write_file: {
    schema: {
      description: 'Write a UTF-8 text file. Creates parent directories as needed. Overwrites if the file already exists. Returns a unified diff vs. the prior content (or "(new file)" if none).',
      parameters: {
        type: 'object',
        properties: {
          path: { type: 'string', description: 'Absolute or project-relative file path.' },
          content: { type: 'string', description: 'Full file content (UTF-8).' },
        },
        required: ['path', 'content'],
        additionalProperties: false,
      },
    },
    impl: async ({ path: p, content }, ctx) => {
      // Robustness: some models (e.g. Claude/Sonnet) emit the write body as nested
      // <path>…</path><content>…</content> instead of the path="…" attribute. Recover that
      // shape so the write succeeds instead of erroring + flailing. Only fires when `path` is
      // absent (the broken case), so a correctly-formed write is byte-identical.
      if ((typeof p !== 'string' || !p) && typeof content === 'string') {
        const pm = content.match(/<path>([\s\S]*?)<\/path>/i);
        if (pm) {
          p = pm[1].trim();
          const cm = content.match(/<content>([\s\S]*?)<\/content>/i);
          content = cm ? cm[1] : content.replace(/<path>[\s\S]*?<\/path>/i, '').replace(/^\s*\n/, '');
        }
      }
      if (typeof p !== 'string' || !p) throw new Error('path is required');
      if (typeof content !== 'string') throw new Error('content is required');
      const abs = resolveWritePath(p, ctx);
      // Pre-write hook: server may insist the containing git repo be on
      // a CODE_BOSS_* branch before we modify any file under it. If the
      // hook throws or returns 'aborted', the user declined and the
      // write should not happen.
      if (typeof ctx?.requireBranch === 'function') {
        const status = await ctx.requireBranch(abs);
        if (status === 'aborted') throw new Error('write aborted: user declined to create a CODE_BOSS branch for this repo');
      }
      let oldText = '';
      let existed = false;
      try { oldText = await readFile(abs, 'utf8'); existed = true; } catch {}
      await mkdir(path.dirname(abs), { recursive: true });
      await writeFile(abs, content, 'utf8');
      const rel = relForDisplay(abs, ctx);
      const summary = existed ? shortDiffSummary(oldText, content) : `(new file, ${content.split('\n').length} lines)`;
      const diff = existed
        ? renderUnifiedDiff(oldText, content, { pathLabel: rel, context: 2 })
        : ['--- (new file)', `+++ ${rel}`, `@@ -0,0 +1,${content.split('\n').length} @@`, ...content.split('\n').map((l) => '+' + l)].join('\n');
      return {
        content: `wrote ${rel}  ${summary}`,
        diff,
        path: rel,
      };
    },
  },

  edit_file: {
    schema: {
      description: 'Surgical edit: replace one occurrence of an exact string in a file. Fails if the string is not found OR if it occurs more than once (ambiguous). Returns a unified diff.',
      parameters: {
        type: 'object',
        properties: {
          path: { type: 'string', description: 'Absolute or project-relative file path.' },
          find: { type: 'string', description: 'Exact string to find. Must occur exactly once in the file.' },
          replace: { type: 'string', description: 'Replacement string.' },
        },
        required: ['path', 'find', 'replace'],
        additionalProperties: false,
      },
    },
    impl: async ({ path: p, find, replace }, ctx) => {
      if (typeof p !== 'string' || !p) throw new Error('path is required');
      if (typeof find !== 'string') throw new Error('find is required');
      if (typeof replace !== 'string') throw new Error('replace is required');
      const abs = resolveWritePath(p, ctx);
      // Same pre-write hook as write_file — see comment there.
      if (typeof ctx?.requireBranch === 'function') {
        const status = await ctx.requireBranch(abs);
        if (status === 'aborted') throw new Error('edit aborted: user declined to create a CODE_BOSS branch for this repo');
      }
      const oldText = await readFile(abs, 'utf8');

      // Find the string. Try exact match first; if that fails, try
      // common normalizations before giving up. The model frequently
      // copies bytes from a read result that's been normalized to LF
      // and then asks us to edit a Windows file with CRLF — the bytes
      // don't match exactly but the intent is clear.
      let findToUse = find;
      let replaceToUse = replace;
      let first = oldText.indexOf(findToUse);
      let fallback = null;

      if (first === -1) {
        const fileHasCRLF = oldText.includes('\r\n');
        const findHasCRLF = find.includes('\r\n');
        if (fileHasCRLF && !findHasCRLF) {
          // File uses CRLF, model gave LF. Promote both find + replace.
          findToUse = find.replace(/\n/g, '\r\n');
          replaceToUse = replace.replace(/\n/g, '\r\n');
          first = oldText.indexOf(findToUse);
          if (first !== -1) fallback = 'CRLF';
        } else if (!fileHasCRLF && findHasCRLF) {
          // File uses LF, model gave CRLF. Strip the \r from both.
          findToUse = find.replace(/\r\n/g, '\n');
          replaceToUse = replace.replace(/\r\n/g, '\n');
          first = oldText.indexOf(findToUse);
          if (first !== -1) fallback = 'LF';
        }
      }

      if (first === -1) throw new Error(`find string not found in ${relForDisplay(abs, ctx)}`);
      const second = oldText.indexOf(findToUse, first + 1);
      if (second !== -1) throw new Error(`find string occurs more than once in ${relForDisplay(abs, ctx)} — make it more specific`);
      const newText = oldText.slice(0, first) + replaceToUse + oldText.slice(first + findToUse.length);
      await writeFile(abs, newText, 'utf8');
      const rel = relForDisplay(abs, ctx);
      const note = fallback ? `  (matched after normalizing line endings to ${fallback})` : '';
      return {
        content: `edited ${rel}  ${shortDiffSummary(oldText, newText)}${note}`,
        diff: renderUnifiedDiff(oldText, newText, { pathLabel: rel, context: 2 }),
        path: rel,
      };
    },
  },

  powershell: {
    schema: {
      description: 'Run a PowerShell command on the developer\'s Windows machine, in the project root. Returns stdout, stderr, and exit code. Use this for builds (mvnw.cmd, npm test), git (git status, git diff), any shell operation. PowerShell syntax: Get-ChildItem, Get-Content, $env:VAR, etc. The `ls` and `cat` aliases work too.',
      parameters: {
        type: 'object',
        properties: {
          command: { type: 'string', description: 'PowerShell command. Pipes, redirects, variables, etc. work normally.' },
          timeout_ms: { type: 'integer', description: `Timeout in milliseconds. Default ${BASH_DEFAULT_TIMEOUT_MS} (2 min), max ${BASH_MAX_TIMEOUT_MS} (10 min).` },
        },
        required: ['command'],
        additionalProperties: false,
      },
    },
    impl: async ({ command, timeout_ms }, ctx) => {
      if (typeof command !== 'string' || !command.trim()) throw new Error('command is required');
      // SAFETY: refuse to kill processes BY NAME (or by name-based selection). These sweep up
      // UNRELATED processes — including the code_boss server itself, which is a node process.
      // (Killing node by name is a classic, infuriating way an agent takes down its own host.)
      // Stop ONLY the process you started, by PID or as a job. Killing by -Id is always allowed.
      // Covers: pkill/killall; taskkill /IM; Stop-Process/spps/kill -Name (and -Na/-Nam/-ProcessName);
      // Get-Process/gps …→ kill (pipe, ForEach, .Kill()); Stop-Process -InputObject (Get-Process …);
      // and CIM/WMI process termination by name (Win32_Process…Terminate, wmic process … delete).
      const KILL_BY_NAME = new RegExp([
        '\\b(?:pkill|killall)\\b',
        'taskkill\\b[^\\n]*\\/im\\b',
        '\\b(?:stop-process|spps|kill)\\b[^\\n]*-(?:processname|na(?:me?)?)\\b',          // by -Name / -Na / -Nam / -ProcessName (+ aliases)
        '\\b(?:get-process|gps)\\b[^\\n]*\\b(?:stop-process|spps|kill)\\b',               // Get-Process … | … kill  (pipe / ForEach / Where)
        '\\b(?:stop-process|spps|kill)\\b[^\\n]*\\b(?:get-process|gps)\\b',               // Stop-Process -InputObject (Get-Process …)  (reverse order)
        '\\b(?:get-process|gps)\\b[^\\n]*\\.kill\\(',                                     // (gps node).Kill()
        'win32_process\\b[^\\n]*\\b(?:terminate|delete)\\b',                              // CIM/WMI terminate by name
        '\\bterminate\\b[^\\n]*\\bwin32_process\\b',
        '\\bwmic\\b[^\\n]*\\bprocess\\b[^\\n]*\\bdelete\\b',
      ].join('|'), 'i');
      // Don't fire on a kill-phrase that is merely inside a QUOTED STRING — a commit message, an
      // echo, a grep pattern (`git commit -m "...Stop-Process..."`, `Select-String -Pattern "pkill"`).
      // Blank quoted contents first, so we match an actual command, not text *about* one. A real
      // kill keeps its structure outside the quotes (e.g. Stop-Process -Name "node") and still fires.
      const screened = command.replace(/"(?:[^"\\]|\\.)*"/g, '""').replace(/'[^']*'/g, "''");
      if (KILL_BY_NAME.test(screened)) {
        throw new Error('Refused: do not kill processes by NAME or name-based selection (Stop-Process -Name / taskkill /IM / pkill / killall / Get-Process|Stop-Process / -InputObject / Win32_Process Terminate / wmic process delete) — it can kill unrelated processes, including the code_boss server itself (it runs on node). Stop only the process YOU started, by its PID or as a job: `$p = Start-Process node -ArgumentList app.js -PassThru; ...; Stop-Process -Id $p.Id`  OR  `$j = Start-Job { node app.js }; ...; Stop-Job $j; Remove-Job $j`.');
      }
      const timeout = Math.min(Math.max(1000, Number(timeout_ms) || BASH_DEFAULT_TIMEOUT_MS), BASH_MAX_TIMEOUT_MS);

      const t0 = Date.now();
      return await new Promise((resolve) => {
        // Head + tail capture (see SHELL_OUTPUT_HEAD/TAIL): *Head holds the first
        // chars, the main buffer holds the last TAIL chars (sliding), *Total is the
        // full byte count for the truncation notice.
        let stdout = '', stderr = '', timedOut = false, settled = false;
        let stdoutHead = '', stderrHead = '', stdoutTotal = 0, stderrTotal = 0;
        const capture = (which, s) => {
          if (which === 'out') {
            stdoutTotal += s.length;
            if (stdoutHead.length < SHELL_OUTPUT_HEAD) stdoutHead = (stdoutHead + s).slice(0, SHELL_OUTPUT_HEAD);
            stdout = (stdout + s).slice(-SHELL_OUTPUT_TAIL);
          } else {
            stderrTotal += s.length;
            if (stderrHead.length < SHELL_OUTPUT_HEAD) stderrHead = (stderrHead + s).slice(0, SHELL_OUTPUT_HEAD);
            stderr = (stderr + s).slice(-SHELL_OUTPUT_TAIL);
          }
        };
        // Build the display body: full output if it fit in the tail buffer,
        // else HEAD + a truncation marker + TAIL (so the build's outcome survives).
        const bodyFor = (tail, head, total) => {
          if (total <= SHELL_OUTPUT_TAIL) return tail;
          const dropped = total - SHELL_OUTPUT_HEAD - tail.length;
          return head + `\n[... ${dropped} chars truncated from the middle — showing the first ${SHELL_OUTPUT_HEAD} and last ${tail.length} ...]\n` + tail;
        };
        // Coalesce stdout/stderr chunks into ~150ms batches when forwarding
        // to the live-progress channel. Otherwise a noisy build can fire
        // hundreds of SSE deltas per second.
        let pendingProgress = '';
        let progressTimer = null;
        const flushProgress = () => {
          progressTimer = null;
          if (!pendingProgress) return;
          try { ctx?.onProgress?.(pendingProgress); } catch {}
          pendingProgress = '';
        };
        const queueProgress = (s) => {
          if (!ctx?.onProgress) return;
          pendingProgress += s;
          if (!progressTimer) progressTimer = setTimeout(flushProgress, 150);
        };
        // PowerShell-only deployment. cmd.exe is often blocked by enterprise
        // policy and lacks the unix aliases the agent expects (ls, cat, etc.).
        const child = spawn('powershell.exe', ['-NoProfile', '-NonInteractive', '-Command', command], {
          cwd: toolRoot(ctx),
          windowsHide: true,
          env: process.env,
        });
        // After a kill attempt, give the child a few seconds to actually
        // close; if taskkill is blocked (AppLocker/EDR/access-denied) or the
        // child ignores the kill, 'close' never fires and the tool Promise
        // would hang forever — taking the whole agent loop with it. This
        // grace timer force-settles so the loop always recovers.
        let killGraceTimer = null;
        // On a USER cancel we want control back fast; on a timeout, give a stuck kill a moment.
        const armGrace = (ms = 5000) => { if (!killGraceTimer) killGraceTimer = setTimeout(() => finish(null, 'killfail'), ms); };
        const killTree = (graceMs) => {
          // taskkill /F /T is the Windows-correct whole-tree kill; child.kill()
          // only stops the powershell wrapper (and can suppress the 'close'
          // event), so we rely on taskkill + the grace timer as the fallback.
          if (child.pid) {
            try { spawn('taskkill', ['/F', '/T', '/PID', String(child.pid)], { windowsHide: true }); } catch {}
          }
          armGrace(graceMs);
        };
        const timer = setTimeout(() => {
          timedOut = true;
          // taskkill /T kills the whole process tree (child.kill() only stops
          // the powershell wrapper, leaving e.g. mvn.exe orphaned).
          killTree(5000);
        }, timeout);

        // User-initiated cancel (esc / /api/cancel) — kill the child
        // tree immediately so the agent doesn't sit waiting for a long-
        // running mvn / git clone / etc. to finish before noticing the
        // abort.
        let cancelled = false;
        const onAbort = () => {
          cancelled = true;
          killTree(2000);   // user pressed cancel — give control back fast (2s), don't wait 5s
        };
        if (ctx?.signal) {
          if (ctx.signal.aborted) onAbort();
          else ctx.signal.addEventListener('abort', onAbort, { once: true });
        }

        // StringDecoder buffers partial multi-byte UTF-8 sequences across
        // chunk boundaries. Decoding each raw chunk with d.toString('utf8')
        // independently corrupts any accented/box-drawing/€ char that
        // straddles a pipe-read boundary (common in mvn/WebLogic/git output
        // on the slow air-gapped link) into U+FFFD replacement chars.
        const outDecoder = new StringDecoder('utf8');
        const errDecoder = new StringDecoder('utf8');
        child.stdout.on('data', (d) => {
          const s = outDecoder.write(d);
          capture('out', s);
          queueProgress(s);
        });
        child.stderr.on('data', (d) => {
          const s = errDecoder.write(d);
          capture('err', s);
          queueProgress(s);
        });
        child.on('error', (err) => {
          if (settled) return; settled = true;
          clearTimeout(timer);
          if (ctx?.signal) ctx.signal.removeEventListener('abort', onAbort);
          if (progressTimer) { clearTimeout(progressTimer); flushProgress(); }
          resolve(`ERROR: failed to spawn shell: ${err.message}`);
        });
        // Hoisted function declaration (NOT a const arrow): the grace timer's
        // callback in armGrace() calls finish() and is scheduled before this
        // point lexically. A const/let would be in the temporal dead zone if
        // that timer ever fired early, throwing "Cannot access 'finish' before
        // initialization". A function declaration is hoisted, so it's always
        // callable.
        function finish(code, signal) {
          if (settled) return; settled = true;
          clearTimeout(timer);
          if (killGraceTimer) { clearTimeout(killGraceTimer); killGraceTimer = null; }
          if (ctx?.signal) ctx.signal.removeEventListener('abort', onAbort);
          // Flush any bytes the decoders are still holding (a trailing
          // partial multi-byte sequence at stream end).
          const outFlush = outDecoder.end();
          if (outFlush) capture('out', outFlush);
          const errFlush = errDecoder.end();
          if (errFlush) capture('err', errFlush);
          if (progressTimer) { clearTimeout(progressTimer); flushProgress(); }
          const elapsed = Date.now() - t0;
          const outBody = bodyFor(stdout, stdoutHead, stdoutTotal);
          const errBody = bodyFor(stderr, stderrHead, stderrTotal);
          const header = cancelled
            ? `command canceled by user after ${elapsed}ms`
            : timedOut
            ? `command timed out after ${timeout}ms (killed)`
            : signal === 'killfail'
            ? `command did not exit after kill (taskkill may be blocked); abandoned after ${elapsed}ms`
            : `exit ${code ?? '?'}${signal ? ` (signal ${signal})` : ''} in ${elapsed}ms`;
          // On timeout, nudge the model toward a faster variant rather
          // than re-running the same expensive command. Many tools have
          // a quick mode (mvn -DskipTests, wl quickbuild, find with a
          // narrower path/glob, etc.). The hint is generic — the model
          // applies it based on what the command actually was.
          const hint = timedOut
            ? '\n\nHINT: this command was too slow to finish. Before retrying, consider a faster variant:\n' +
              '  - narrower scope (smaller path, more specific glob/pattern, lower depth)\n' +
              '  - skip-tests / quick / dry-run flag if the tool offers one\n' +
              '  - higher timeout_ms (max 600000) if the operation is inherently slow but useful\n' +
              'Re-running the exact same command will time out again.'
            : '';
          // ALWAYS emit both delimited sections (stdout AND stderr), even when
          // empty, so the model is never left guessing whether stderr was shown
          // — especially on a failure whose only output is on stderr. Both
          // streams are captured + head/tail-capped independently above.
          const text = [
            header,
            `--- stdout ---\n${outBody || '(no stdout)'}`,
            `--- stderr ---\n${errBody || '(no stderr)'}`,
            hint,
          ].filter(Boolean).join('\n').trim();
          // Timeout / killfail / user-cancel are non-success outcomes the agent
          // (and the UI) should see as an error (status=error, red row) — without
          // the ERROR prefix they fall through to status=ok with a misleading
          // GREEN dot even though the command was aborted. Plain non-zero exit
          // codes are left as-is: many commands (git diff --exit-code, grep, etc.)
          // intentionally return non-zero for normal outcomes.
          resolve((cancelled || timedOut || signal === 'killfail') ? 'ERROR: ' + text : text);
        }
        child.on('close', finish);
      });
    },
  },

  list_dir: {
    schema: {
      description: 'List entries in a directory. Returns each entry as "[D] name/" or "[F] name (<bytes> bytes)".',
      parameters: {
        type: 'object',
        properties: {
          path: { type: 'string', description: 'Absolute or project-relative directory path. Defaults to project root.' },
        },
        required: [],
        additionalProperties: false,
      },
    },
    impl: async ({ path: p }, ctx) => {
      const root = toolRoot(ctx);
      const dir = p
        ? (path.isAbsolute(p) ? p : path.join(root, p))
        : root;
      if (isControlPath(dir)) throw new Error(CONTROL_DIR_MSG);
      const entries = await readdir(dir, { withFileTypes: true });
      const lines = [];
      // Hide the control dir from listings so the agent never sees (and dives
      // into) its own task/assignment state as if it were project content.
      for (const e of entries.filter((e) => e.name !== CONTROL_DIR).sort((a, b) => a.name.localeCompare(b.name))) {
        if (e.isDirectory()) {
          lines.push(`[D] ${e.name}/`);
        } else {
          let size = '';
          try {
            const s = await stat(path.join(dir, e.name));
            size = `(${s.size} bytes)`;
          } catch {}
          lines.push(`[F] ${e.name} ${size}`.trim());
        }
      }
      return ensureFits(lines.join('\n'));
    },
  },

  remember: {
    schema: {
      description: 'Record ONE durable, reusable fact about THIS codebase into the persistent codebase memory (injected into your context every future run). Use for architecture, conventions, gotchas, where-things-live, and build/deploy specifics that will help future work. Do NOT record task-specific, transient, or obvious-from-the-code details. Keep it concise and factual. Duplicates are ignored.',
      parameters: {
        type: 'object',
        properties: {
          note: { type: 'string', description: 'The fact, in one or two sentences.' },
          category: { type: 'string', description: `One of: ${KNOWN_CATEGORIES.join(', ')}. Defaults to general.`, enum: KNOWN_CATEGORIES },
        },
        required: ['note'],
        additionalProperties: false,
      },
    },
    impl: async ({ note, category }, ctx) => {
      const r = await rememberFact(toolRoot(ctx), { note, category });
      if (r.full) return `ERROR: codebase memory is full (${r.total} entries); ask the developer to prune it before adding more.`;
      if (!r.added) return `Already remembered (${r.entry?.id || 'existing'}): "${r.entry?.note || ''}". No change.`;
      return `Remembered [${r.entry.category}] ${r.entry.id}: "${r.entry.note}". Codebase memory now has ${r.total} fact(s).`;
    },
  },

  forget: {
    schema: {
      description: 'Remove a fact from the persistent codebase memory — by its id (e.g. "M-3") or by an exact note match. Use when a remembered fact has become wrong or obsolete.',
      parameters: {
        type: 'object',
        properties: {
          id: { type: 'string', description: 'The memory id to remove, e.g. "M-3".' },
          note: { type: 'string', description: 'Alternatively, the exact note text to remove.' },
        },
        required: [],
        additionalProperties: false,
      },
    },
    impl: async ({ id, note }, ctx) => {
      const r = await forgetFact(toolRoot(ctx), { id, note });
      return r.removed ? `Forgot ${r.removed} fact(s).` : 'No matching fact found; nothing removed.';
    },
  },
};

const toolDefs = Object.entries(tools).map(([name, t]) => ({
  type: 'function',
  function: { name, description: t.schema.description, parameters: t.schema.parameters },
}));

/**
 * Execute a tool by name. Result shape:
 *   - string                            -> the content string (legacy / read tools)
 *   - { content, diff?, path?, ... }    -> structured result (write tools)
 *   - "ERROR: ..." string               -> on failure
 *
 * Callers can normalize via `extractContent(result)` and `extractDiff(result)`.
 */
async function execute(name, args, extraTools = {}, ctx = {}) {
  // Built-ins win over plugin tools — name collisions are caught earlier in
  // src/plugins/index.mjs (checkCollisions), but if a plugin tool somehow
  // sneaks through with a built-in name it still won't shadow.
  const t = tools[name] || extraTools[name];
  if (!t) {
    const all = [...Object.keys(tools), ...Object.keys(extraTools)];
    return `ERROR: unknown tool "${name}". Available: ${all.join(', ')}`;
  }
  try {
    // ctx may carry { callId, onProgress(chunk) } so tools that produce
    // streaming output (powershell, wl, anything else with a child
    // process) can forward stdout/stderr chunks to the chat for a
    // click-to-expand "running…" indicator while they execute. Tools
    // that don't care simply ignore the second arg.
    const result = await t.impl(args ?? {}, ctx);
    return result;  // may be string or object; callers handle both
  } catch (e) {
    return `ERROR: ${e.message}`;
  }
}

function extractContent(r) {
  if (typeof r === 'string') return r;
  if (r && typeof r === 'object' && typeof r.content === 'string') return r.content;
  return String(r ?? '');
}
function extractDiff(r) {
  if (r && typeof r === 'object' && typeof r.diff === 'string') return r.diff;
  return null;
}

// ====== agent/tasks.mjs ======
/**
 * Task-scoped memory store.
 *
 * A "task" is a coherent unit of work the agent is doing. The agent is ALWAYS
 * inside exactly one task; it transitions between tasks with <next-task>.
 *
 * Tiers (most-fidelity to least):
 *   1  current + recent finished tasks, full messages incl. tool results
 *   2  finished tasks, messages visible but tool RESULTS replaced by a
 *      one-line placeholder (e.g. "[T-3 op=read pom.xml -> 240 lines]")
 *   3  finished tasks, replaced entirely by a one-paragraph summary
 *   4  parent task = single summary covering a group of tier-3 children
 *
 * Demotion runs lazily when a tier exceeds budget (with hysteresis).
 *
 * Storage layout (per project):
 *   <project>/.code_boss/tasks/
 *     state.json                  v2: { version:2, nextId, currentId, rootId,
 *                                   allTaskIds, threads:{...}, activeThreadIds,
 *                                   focusedThreadId, rootThreadId, nextThreadId }
 *     T-1.json                    full task record (see TaskRecord shape below)
 *     T-2.json
 *
 * THREADS (v2): a "thread" is a concurrent line of work — a lineage of tasks
 * with an open HEAD. Multiple threads may be active at once. `currentId` is a
 * derived mirror of the FOCUSED thread's head, kept for the single-thread API
 * (currentId/getCurrent/transitionTo) the serial worker still uses. Thread
 * lifecycle (active/dormant/blocked/done) is ORTHOGONAL to the compaction tiers
 * below (tiers are about context size, not concurrency).
 *
 * TaskRecord:
 *   {
 *     id:         "T-3",
 *     title:      "Refactor auth middleware",
 *     goal:       "Move session tokens to encrypted storage",
 *     parentId:   "T-1" | null,
 *     childIds:   ["T-4", "T-5"],
 *     started:    1717180000000,    epoch ms
 *     ended:      1717180550000 | null,
 *     tier:       1 | 2 | 3 | 4,
 *     summary:    "..." | null,     written when task closes
 *     messages:   [TaskMessage],    discarded once task reaches tier 3
 *   }
 *
 * TaskMessage (one entry per LLM-loop event in this task):
 *   {
 *     kind:   "assistant" | "tool-call" | "tool-result",
 *     role:   "assistant" | "user",        // the OpenAI-protocol role
 *     content:    string,                  // raw text
 *     toolName:   string | undefined,      // for tool-call / tool-result
 *     toolArgs:   object | undefined,      // for tool-call
 *     toolStatus: "ok" | "error" | undefined,  // for tool-result
 *     bytes:      number,                  // size of content for budget calc
 *   }
 */

const STATE_FILE = 'state.json';
const TASK_FILE = (id) => `${id}.json`;

function nowMs() { return Date.now(); }
function tokensOf(s) { return Math.ceil((s || '').length / 4); }
function taskTmpId() { return process.pid + '-' + randomUUID().slice(0, 8); }

function createTasksStore(projectDir) {
  if (!projectDir) throw new Error('projectDir required');
  const dir = path.join(projectDir, '.code_boss', 'tasks');

  let state = null;            // { nextId, currentId, rootId, allTaskIds: [] }
  const cache = new Map();     // id -> TaskRecord (lazy)

  // Per-FILE write serialization + atomic writes. On Windows two concurrent
  // writeFile() calls to the SAME path throw EBUSY ("resource busy or locked"):
  // the second open fails while the first handle is live. The task store has
  // many interleaving async writers (parallel <remote-workspace> blocks each
  // append tool-call/result to the same task file; maybeCompact demotes while a
  // turn is still appending; rapid next-task transitions write the same files
  // back-to-back), so an unserialized writeFile raced and crashed the agent
  // (EBUSY on T-12.json). Mirror the proven codebase-memory + conversation.json
  // pattern: chain writes per absolute path (so different files still write in
  // parallel, the SAME file serializes) and write atomically via a UNIQUE temp
  // file + rename (rename is atomic, so a torn/concurrent write never corrupts
  // the live file). writeChains is scoped to THIS store instance.
  const writeChains = new Map();   // absolute path -> Promise (tail of its write chain)
  function withFileLock(filePath, fn) {
    const prev = writeChains.get(filePath) || Promise.resolve();
    const next = prev.then(fn, fn);            // run fn regardless of prior outcome
    writeChains.set(filePath, next.then(() => {}, () => {}));   // keep the chain rejection-free
    return next;
  }
  // Transient Windows rename failures. writeChains serializes OUR writes to the
  // same path, but the rename can still lose a race with an EXTERNAL holder of a
  // brief handle on the destination — antivirus/Defender scanning the file we
  // just wrote, a Search-indexer, or a sync client. Windows surfaces that as
  // EPERM/EACCES (operation not permitted) or EBUSY (resource busy); rarely
  // EEXIST. These clear in tens of ms, so a short backed-off retry turns a
  // run-killing crash into an invisible hiccup. A persistent failure still
  // throws after the budget (then the temp is cleaned up).
  const RENAME_RETRY_CODES = new Set(['EPERM', 'EACCES', 'EBUSY', 'EEXIST']);
  async function atomicWrite(filePath, data) {
    await ensureDir();
    const tmp = filePath + '.' + taskTmpId() + '.tmp';   // unique per write — never shared
    await writeFile(tmp, data, 'utf8');
    let lastErr = null;
    for (let attempt = 0; attempt < 6; attempt++) {
      try {
        await rename(tmp, filePath);
        return;
      } catch (e) {
        lastErr = e;
        if (!RENAME_RETRY_CODES.has(e.code)) break;
        // 25, 50, 75, 100, 125 ms — total ~375ms worst case before giving up.
        await new Promise((r) => setTimeout(r, 25 * (attempt + 1)));
      }
    }
    // Out of retries (or a non-transient error): drop the temp so we don't leak
    // .tmp turds, then surface the original failure.
    try { await unlink(tmp); } catch { /* best-effort */ }
    throw lastErr;
  }

  async function ensureDir() { await mkdir(dir, { recursive: true }); }

  async function loadState() {
    try {
      const raw = await readFile(path.join(dir, STATE_FILE), 'utf8');
      return JSON.parse(raw);
    } catch { return null; }
  }
  async function saveState() {
    const file = path.join(dir, STATE_FILE);
    return withFileLock(file, () => atomicWrite(file, JSON.stringify(state, null, 2)));
  }

  async function loadTask(id) {
    if (cache.has(id)) return cache.get(id);
    try {
      const raw = await readFile(path.join(dir, TASK_FILE(id)), 'utf8');
      const t = JSON.parse(raw);
      cache.set(id, t);
      return t;
    } catch { return null; }
  }
  async function saveTask(t) {
    const file = path.join(dir, TASK_FILE(t.id));
    return withFileLock(file, async () => {
      await atomicWrite(file, JSON.stringify(t, null, 2));
      // Commit the cache only AFTER the write succeeds, so a failed write never
      // leaves the in-memory record ahead of disk.
      cache.set(t.id, t);
      return t;
    });
  }

  function newId() {
    const id = `T-${state.nextId}`;
    state.nextId++;
    return id;
  }
  function newThreadId() {
    const id = `TH-${state.nextThreadId}`;
    state.nextThreadId++;
    return id;
  }
  // The thread the legacy single-thread API targets (currentId / getCurrent /
  // transitionTo / appendMessage). Its head IS state.currentId.
  function focusedThread() { return state?.threads?.[state.focusedThreadId] ?? null; }
  // A task is an "active head" iff it is the open head of some ACTIVE thread.
  // Active heads are never demoted/rolled up. Single-thread → exactly currentId.
  function isActiveHead(taskId) {
    for (const tid of state?.activeThreadIds ?? []) {
      if (state.threads?.[tid]?.headId === taskId) return true;
    }
    return false;
  }
  // Tasks created before v2 have no threadId → they belong to the root thread.
  function taskThreadId(t) { return t?.threadId ?? state.rootThreadId; }

  /**
   * Initialize the store. If no state exists, creates a root task and makes it current.
   * Returns the initial state object.
   */
  async function init() {
    await ensureDir();
    state = await loadState();
    if (!state) {
      // Fresh store: a root TASK that is the head of a root THREAD.
      state = {
        version: 2,
        nextId: 1, currentId: null, rootId: null, allTaskIds: [],
        nextThreadId: 1, threads: {}, activeThreadIds: [], focusedThreadId: null, rootThreadId: null,
      };
      const rootId = newId();
      const tid = newThreadId();
      const root = {
        id: rootId,
        threadId: tid,
        userRequest: null,         // agent fills this in when the task closes
        parentId: null,
        childIds: [],
        started: nowMs(),
        ended: null,
        tier: 1,
        summary: null,
        messages: [],
      };
      await saveTask(root);
      state.currentId = rootId;
      state.rootId = rootId;
      state.allTaskIds.push(rootId);
      state.threads[tid] = { id: tid, headId: rootId, state: 'active', topic: null, blockedOn: null, parentThreadId: null, openedAt: nowMs(), lastTouchedAt: nowMs() };
      state.activeThreadIds = [tid];
      state.focusedThreadId = tid;
      state.rootThreadId = tid;
      await saveState();
    } else if (state.version !== 2) {
      // Migrate a v1 store forward: wrap the existing current task as the head
      // of a single active root thread. No task files are rewritten — pre-v2
      // tasks resolve to the root thread via taskThreadId().
      const tid = 'TH-1';
      state.version = 2;
      state.nextThreadId = 2;
      state.threads = { [tid]: { id: tid, headId: state.currentId, state: 'active', topic: null, blockedOn: null, parentThreadId: null, openedAt: nowMs(), lastTouchedAt: nowMs() } };
      state.activeThreadIds = [tid];
      state.focusedThreadId = tid;
      state.rootThreadId = tid;
      await saveState();
    }
    return state;
  }

  function currentId() { return state?.currentId ?? null; }
  function rootId() { return state?.rootId ?? null; }
  async function getCurrent() { return loadTask(currentId()); }

  /**
   * Close the CURRENT task and open a new one as its sibling.
   *
   * Both `summary` and `userRequest` describe the CURRENT (closing) task:
   *   - userRequest: a synthesized statement of what the user wanted across
   *     the just-finished work (NOT the latest user message that prompted this close).
   *   - summary: what was actually accomplished.
   *
   * The new task starts with userRequest=null and summary=null; the agent
   * fills them in when IT closes via another <next-task>.
   *
   * Returns the new task record.
   */
  async function transitionThread(threadId, { summary, userRequest }) {
    const th = state?.threads?.[threadId];
    if (!th || !th.headId) throw new Error(`thread ${threadId} not initialized`);
    const cur = await loadTask(th.headId);
    cur.summary = summary ?? cur.summary ?? '(no summary provided)';
    cur.userRequest = userRequest ?? cur.userRequest ?? null;
    cur.ended = nowMs();
    await saveTask(cur);
    // Return the closed task id alongside the new one so the caller can
    // attach metadata to it after the fact (e.g. the commit hash from
    // the post-transition auto-commit).
    const closedId = cur.id;

    // If `cur` IS the root, the new task becomes a child of root.
    // Otherwise the new task is a sibling of `cur` (same parent).
    const realParentId = cur.id === state.rootId ? state.rootId : (cur.parentId ?? state.rootId);

    const id = newId();
    const t = {
      id,
      threadId,
      userRequest: null,
      parentId: realParentId,
      childIds: [],
      started: nowMs(),
      ended: null,
      tier: 1,
      summary: null,
      messages: [],
    };
    await saveTask(t);

    // Link as child of parent.
    if (realParentId) {
      const parent = await loadTask(realParentId);
      if (parent) {
        parent.childIds = [...(parent.childIds ?? []), id];
        await saveTask(parent);
      }
    }

    th.headId = id;
    th.lastTouchedAt = nowMs();
    if (threadId === state.focusedThreadId) state.currentId = id;   // keep the legacy mirror in sync
    state.allTaskIds.push(id);
    await saveState();
    return { newTask: t, closedId };
  }

  // Single-thread wrapper: transition the FOCUSED thread (currentId === its head).
  async function transitionTo(args) {
    if (!state?.focusedThreadId) throw new Error('store not initialized');
    return transitionThread(state.focusedThreadId, args);
  }

  /**
   * Attach arbitrary metadata fields to a previously-saved task.
   * Used post-transition to record the auto-commit hash on the just-
   * closed task without forcing every caller to round-trip load+save.
   */
  async function attachToTask(taskId, fields) {
    const t = await loadTask(taskId);
    if (!t) return null;
    Object.assign(t, fields);
    await saveTask(t);
    return t;
  }

  /**
   * Append a message (assistant text, tool-call, or tool-result) to the
   * current task — or to an explicit task when targetId is given. The
   * targeted form is needed when a turn closes a task mid-flight (<next-task>):
   * tool results produced BEFORE the transition must be filed into the task
   * they actually ran in, not the freshly-opened current task.
   */
  async function appendMessage(msg, targetId) {
    const id = targetId || state.currentId;
    const t = await loadTask(id);
    if (!t) throw new Error(`task ${id} not found`);
    const content = msg.content ?? '';
    t.messages = t.messages ?? [];
    t.messages.push({
      kind: msg.kind,
      role: msg.role,
      content,
      toolName: msg.toolName,
      toolArgs: msg.toolArgs,
      toolStatus: msg.toolStatus,
      bytes: content.length,
      // Attached images (data URIs) ride along on user messages only when
      // present — absent otherwise so existing records stay byte-identical.
      ...(Array.isArray(msg.images) && msg.images.length ? { images: msg.images } : {}),
    });
    await saveTask(t);
    return t;
  }

  /**
   * Move ONE specific message (by object identity, captured when it was
   * appended) from `fromId` to the end of `toId`. Used when a task is closed
   * before any work was done for a just-arrived user message: that message
   * belongs to the NEW task, not the one being closed. Identity (not position)
   * is required because by the time the close fires, the turn's assistant prose
   * has already been appended after the user message, so it is no longer the
   * tail. Falls back to a content match if the exact reference is gone (e.g.
   * after a reload). Returns the moved record, or null if not found.
   */
  async function relocateMessageRef(fromId, toId, ref) {
    if (!ref || !fromId || !toId || fromId === toId) return null;
    const from = await loadTask(fromId);
    const to = await loadTask(toId);
    if (!from || !to) return null;
    const msgs = from.messages ?? [];
    let idx = msgs.indexOf(ref);
    if (idx === -1) idx = msgs.findIndex((m) => m.kind === ref.kind && m.content === ref.content);
    if (idx === -1) return null;
    const [msg] = msgs.splice(idx, 1);
    from.messages = msgs;
    to.messages = [...(to.messages ?? []), msg];
    await saveTask(from);
    await saveTask(to);
    return msg;
  }

  /**
   * List tasks. If parentId is given, only its direct children. If tier is
   * given, only tasks at that tier. By default archived tasks are excluded;
   * pass includeArchived: true to include them (e.g., for search).
   * Returns lightweight records (no messages).
   */
  async function listTasks({ parentId = undefined, tier = undefined, includeArchived = false } = {}) {
    const out = [];
    for (const id of state.allTaskIds) {
      const t = await loadTask(id);
      if (!t) continue;
      if (!includeArchived && t.archived) continue;
      if (parentId !== undefined && t.parentId !== parentId) continue;
      if (tier !== undefined && t.tier !== tier) continue;
      out.push({
        id: t.id, userRequest: t.userRequest, parentId: t.parentId,
        childIds: t.childIds, started: t.started, ended: t.ended,
        tier: t.tier, summary: t.summary,
        rolledUpInto: t.rolledUpInto ?? null,
        archived: !!t.archived,
      });
    }
    return out;
  }

  /** Backfill/overwrite a closed task's summary (e.g. when the agent emitted
   *  <next-task> with a blank summary and we synthesized one afterward).
   *  Persists; works at any tier. No-op on a missing task or empty summary. */
  async function setSummary(id, summary) {
    if (typeof summary !== 'string' || !summary.trim()) return null;
    const t = await loadTask(id);
    if (!t) return null;
    t.summary = summary.trim();
    await saveTask(t);
    return t.id;
  }

  async function getDetail(id) {
    const t = await loadTask(id);
    if (!t) return null;
    return {
      id: t.id, userRequest: t.userRequest, parentId: t.parentId,
      childIds: t.childIds, started: t.started, ended: t.ended,
      tier: t.tier, summary: t.summary, threadId: t.threadId,
      ...(t.subagentId ? { subagentId: t.subagentId } : {}),   // provenance: which sub-agent produced this
      messageCount: (t.messages ?? []).length,
    };
  }

  /** Case-insensitive substring search across userRequest/summary. */
  async function searchTasks(query) {
    if (!query) return listTasks();
    const q = query.toLowerCase();
    const hits = [];
    for (const id of state.allTaskIds) {
      const t = await loadTask(id);
      if (!t) continue;
      const hay = [t.userRequest, t.summary].filter(Boolean).join(' ').toLowerCase();
      if (hay.includes(q)) {
        hits.push({ id: t.id, userRequest: t.userRequest, parentId: t.parentId, started: t.started, ended: t.ended, tier: t.tier, summary: t.summary });
      }
    }
    return hits;
  }

  // Snapshot the full-fidelity messages ONCE, the first time a task leaves
  // tier 1, so a later <task-detail> can recover the real detail of a compacted
  // task (tier-2 masking and tier-3 dropping are both lossy). Kept on disk, NOT
  // rendered into context and NOT counted toward any tier budget (tier 2 counts
  // the masked messages; tier 3/4 count only the summary), so it costs context
  // nothing — only disk, and only for the rare compacted task.
  function snapshotMessages(t) {
    if (!t.archivedMessages && Array.isArray(t.messages) && t.messages.length) {
      t.archivedMessages = t.messages.map((m) => ({ ...m }));
    }
  }

  /**
   * Mechanical demotion: mark task as tier 2 and replace tool-result message
   * bodies with one-line placeholders. NO LLM call.
   */
  async function demoteToTier2(id) {
    const t = await loadTask(id);
    if (!t) return null;
    if (t.tier >= 2) return t;
    snapshotMessages(t);   // preserve full detail BEFORE masking results
    for (const m of t.messages ?? []) {
      if (m.kind === 'tool-result') {
        const lines = (m.content ?? '').split('\n').length;
        const argSummary = m.toolName ? `${m.toolName}` : 'tool';
        m.content = `[${t.id} ${argSummary} -> ${lines} lines, masked]`;
        m.bytes = m.content.length;
      }
    }
    t.tier = 2;
    await saveTask(t);
    return t;
  }

  /**
   * Summary demotion: replace messages with a one-paragraph summary. Caller
   * provides the summary text (we don't run the LLM here; agent loop owns
   * that concern). The full messages survive in archivedMessages for retrieval.
   */
  async function demoteToTier3(id, summaryText) {
    const t = await loadTask(id);
    if (!t) return null;
    if (t.tier >= 3) return t;
    snapshotMessages(t);   // in case the task skipped tier 2
    t.messages = [];
    t.summary = summaryText || t.summary || '(no summary written)';
    t.tier = 3;
    await saveTask(t);
    return t;
  }

  /**
   * Find the oldest (by `ended` then `started`) task in a given tier that is
   * eligible for demotion. Excludes: current task, archived tasks, and
   * (for tier 3) tasks already rolled up into a tier-4 rollup. Returns id or null.
   */
  async function findOldestDemotionCandidate(tier) {
    const tasks = await listTasks({ tier });
    const eligible = tasks.filter((t) => {
      if (isActiveHead(t.id)) return false;   // never demote the open head of an active thread
      if (t.archived) return false;
      if (t.rolledUpInto) return false;
      if ((tier === 1 || tier === 2) && t.ended == null) return false;
      return true;
    });
    eligible.sort((a, b) => (a.ended ?? a.started ?? 0) - (b.ended ?? b.started ?? 0));
    return eligible[0]?.id ?? null;
  }

  /**
   * Roll up N tier-3 tasks into a single synthetic tier-4 task. The children
   * get `rolledUpInto` set so they're skipped in convo building (still
   * searchable via the search tool).
   *
   * If `override` provides `{ title, summary }`, those replace the mechanical
   * defaults (used when an LLM call has produced a real clustered rollup).
   * Otherwise the rollup uses a concatenation of child summaries.
   */
  /**
   * Group N tier-3 tasks into a tier-4 "project" rollup. Children get
   * `rolledUpInto` set; the rollup is a new task with its own userRequest
   * and summary (synthesized by the LLM if `override` is provided,
   * otherwise mechanical fallback).
   *
   * `override`: { userRequest, summary } from the LLM clustering call.
   */
  async function rollupTier3Tasks(childIds, override) {
    if (!Array.isArray(childIds) || childIds.length === 0) return null;
    const children = [];
    for (const cid of childIds) {
      const c = await loadTask(cid);
      if (c && c.tier === 3 && !c.rolledUpInto && !c.archived) children.push(c);
    }
    if (children.length === 0) return null;

    let userRequest, summary;
    if (override && typeof override.userRequest === 'string' && typeof override.summary === 'string') {
      userRequest = override.userRequest;
      summary = override.summary;
    } else {
      // Mechanical fallback: stitch the child summaries together.
      userRequest = `Project covering ${children.length} tasks (${children.map((c) => c.id).join(', ')})`;
      const lines = [`Rollup of ${children.length} task(s):`];
      for (const c of children) {
        const ur = c.userRequest ? ` ↳ ${c.userRequest.split('\n')[0].slice(0, 120)}` : '';
        const sm = (c.summary ?? '').split('\n')[0].slice(0, 200);
        lines.push(`- ${c.id}${ur} — ${sm}`);
      }
      summary = lines.join('\n');
    }

    const id = newId();
    const t = {
      id,
      threadId: taskThreadId(children[0]),
      userRequest,
      parentId: children[0].parentId ?? null,
      childIds: children.map((c) => c.id),
      started: children[0].started,
      ended: nowMs(),
      tier: 4,
      summary,
      messages: [],
    };
    await saveTask(t);
    state.allTaskIds.push(id);
    await saveState();

    for (const c of children) {
      c.rolledUpInto = id;
      await saveTask(c);
    }
    return t;
  }

  /** Mark a task (typically a tier-4 rollup) as archived. Stays searchable but
   *  no longer included in convo construction. */
  async function archiveTask(id) {
    const t = await loadTask(id);
    if (!t) return null;
    t.archived = true;
    await saveTask(t);
    return t;
  }

  /** Compute token usage per tier from current task records. Excludes
   *  archived and rolled-up tasks (they're not in context). */
  async function tierTokens() {
    const out = { 1: 0, 2: 0, 3: 0, 4: 0 };
    for (const id of state.allTaskIds) {
      const t = await loadTask(id);
      if (!t) continue;
      if (t.archived) continue;
      if (t.rolledUpInto) continue;
      if (t.tier === 3 || t.tier === 4) {
        out[t.tier] += tokensOf(t.summary);
      } else {
        let sum = 0;
        for (const m of t.messages ?? []) sum += tokensOf(m.content);
        out[t.tier] += sum;
      }
    }
    return out;
  }

  // ── Thread layer (first-class concurrent lines of work) ────────────────────
  // The single-thread API above operates on the FOCUSED thread; these expose and
  // manipulate threads directly. Unused by the serial worker (which has exactly
  // one active thread = parity with v1); the Phase-2 second thread uses them.
  function listThreads() { return Object.values(state?.threads ?? {}).map((th) => ({ ...th })); }
  function getThread(id) { const th = state?.threads?.[id]; return th ? { ...th } : null; }
  function focusedThreadId() { return state?.focusedThreadId ?? null; }
  function rootThreadId() { return state?.rootThreadId ?? null; }
  function activeThreadIds() { return [...(state?.activeThreadIds ?? [])]; }
  function threadHead(id) { return state?.threads?.[id]?.headId ?? null; }

  /** Point the legacy single-thread API at another thread (its head becomes
   *  state.currentId). */
  async function focusThread(id) {
    const th = state?.threads?.[id];
    if (!th) return null;
    state.focusedThreadId = id;
    state.currentId = th.headId;
    await saveState();
    return id;
  }

  /** Open a NEW active thread with a fresh head task (a child of root by
   *  default). Does NOT change focus. Returns { thread, headTask }. */
  async function spawnThread({ topic = null, parentThreadId = null, parentTaskId = undefined } = {}) {
    if (!state) throw new Error('store not initialized');
    const headId = newId();
    const parentId = parentTaskId !== undefined ? parentTaskId : state.rootId;
    const tid = newThreadId();
    const head = {
      id: headId, threadId: tid,
      userRequest: null, parentId, childIds: [],
      started: nowMs(), ended: null, tier: 1, summary: null, messages: [],
    };
    await saveTask(head);
    if (parentId) {
      const parent = await loadTask(parentId);
      if (parent) { parent.childIds = [...(parent.childIds ?? []), headId]; await saveTask(parent); }
    }
    const th = { id: tid, headId, state: 'active', topic, blockedOn: null, parentThreadId, openedAt: nowMs(), lastTouchedAt: nowMs() };
    state.threads[tid] = th;
    if (!state.activeThreadIds.includes(tid)) state.activeThreadIds.push(tid);
    state.allTaskIds.push(headId);
    await saveState();
    return { thread: { ...th }, headTask: head };
  }

  /** Set a thread's lifecycle ('active' | 'dormant' | 'blocked' | 'done') and
   *  maintain the active set. */
  async function setThreadState(id, lifecycle, { blockedOn = null } = {}) {
    const th = state?.threads?.[id];
    if (!th) return null;
    th.state = lifecycle;
    th.blockedOn = lifecycle === 'blocked' ? blockedOn : null;
    th.lastTouchedAt = nowMs();
    const active = new Set(state.activeThreadIds);
    if (lifecycle === 'active') active.add(id); else active.delete(id);
    state.activeThreadIds = [...active];
    await saveState();
    return { ...th };
  }

  /** Append a message to a specific thread's head task. */
  async function appendToThread(threadId, msg) {
    const head = state?.threads?.[threadId]?.headId;
    if (!head) throw new Error(`thread ${threadId} not found`);
    return appendMessage(msg, head);
  }

  /**
   * Bulk-import a FINISHED sub-agent's tasks (with messages) as ONE provenance-tagged,
   * DONE thread — so its work persists + is searchable in this (the parent/manager) store
   * without ever entering the foreground's prompt (it's a sibling thread, not the focused
   * lineage; "transcripts stay put, results flow up"). `sourceTasks` are plain records read
   * from the sub-agent's own store: { userRequest, summary, messages, started, ended }.
   * Imported at tier 2 (finished, off the active tier-1 budget). Returns { threadId, taskIds }.
   */
  async function importThread(sourceTasks, { subagentId = null, topic = null } = {}) {
    if (!state) throw new Error('store not initialized');
    const tid = newThreadId();
    const taskIds = [];
    for (const src of (sourceTasks || [])) {
      const id = newId();
      const t = {
        id, threadId: tid,
        userRequest: src.userRequest ?? null,
        parentId: state.rootId,
        childIds: [],
        started: src.started ?? nowMs(),
        ended: src.ended ?? nowMs(),
        tier: 2,
        summary: src.summary ?? null,
        messages: Array.isArray(src.messages) ? src.messages.map((m) => ({ ...m })) : [],
        subagentId,                                  // provenance: which sub-agent produced this
      };
      await saveTask(t);
      state.allTaskIds.push(id);
      taskIds.push(id);
    }
    if (state.rootId) {                              // link under root so tree walks find them
      const root = await loadTask(state.rootId);
      if (root) { root.childIds = [...(root.childIds ?? []), ...taskIds]; await saveTask(root); }
    }
    state.threads[tid] = {
      id: tid, headId: taskIds[taskIds.length - 1] || null,
      state: 'done', topic, subagentId, blockedOn: null,
      parentThreadId: state.rootThreadId ?? null,
      openedAt: nowMs(), lastTouchedAt: nowMs(), imported: true,
    };
    // a DONE thread is intentionally NOT added to activeThreadIds
    await saveState();
    return { threadId: tid, taskIds };
  }

  /**
   * Export this store's tasks as plain records (WITH messages) for importThread() into
   * another store — the read side of a sub-agent → manager hand-up. Skips the empty root
   * head (the bare container). Order follows allTaskIds (chronological).
   */
  async function exportTasks() {
    const out = [];
    for (const id of (state?.allTaskIds ?? [])) {
      const t = await loadTask(id);
      if (!t) continue;
      // include the root iff it carries real work (in a fresh store the FIRST task IS the
      // root); skip only genuinely empty container tasks.
      const hasContent = (t.messages && t.messages.length) || t.summary || t.userRequest;
      if (!hasContent) continue;
      out.push({
        userRequest: t.userRequest ?? null,
        summary: t.summary ?? null,
        started: t.started ?? null,
        ended: t.ended ?? null,
        messages: Array.isArray(t.messages) ? t.messages.map((m) => ({ ...m })) : [],
      });
    }
    return out;
  }

  // Re-open a CLOSED task as the focused thread's head — used by the reviewer loop to fold a
  // review-fix back INTO the task being reviewed (instead of a separate task). Drops the empty
  // placeholder that the closing <next-task> created. Returns false (no-op) unless the current
  // head IS that empty placeholder, so it can never scramble mid-run state.
  async function reopenTask(id) {
    if (!state) return false;
    const target = await loadTask(id);
    const th = focusedThread();
    if (!target || !th) return false;
    const placeholderId = th.headId;
    if (placeholderId === id) return true;              // already the head — nothing to do
    const placeholder = await loadTask(placeholderId);
    // Only proceed when the placeholder is genuinely empty (no work since the close).
    if (placeholder && ((placeholder.messages && placeholder.messages.length) || placeholder.summary || placeholder.userRequest)) return false;
    if (placeholder) {                                   // detach + delete the empty placeholder
      if (placeholder.parentId) {
        const parent = await loadTask(placeholder.parentId);
        if (parent) { parent.childIds = (parent.childIds ?? []).filter((c) => c !== placeholderId); await saveTask(parent); }
      }
      state.allTaskIds = (state.allTaskIds ?? []).filter((t) => t !== placeholderId);
      cache.delete(placeholderId);
      try { await unlink(path.join(dir, TASK_FILE(placeholderId))); } catch { /* best-effort */ }
    }
    target.ended = null;                                 // re-open
    await saveTask(target);
    th.headId = id;
    th.lastTouchedAt = nowMs();
    if (th.id === state.focusedThreadId) state.currentId = id;
    await saveState();
    return true;
  }

  return {
    init,
    currentId, rootId,
    getCurrent,
    transitionTo, reopenTask,
    attachToTask,
    appendMessage,
    relocateMessageRef,
    listTasks, getDetail, searchTasks, setSummary,
    demoteToTier2, demoteToTier3,
    rollupTier3Tasks, archiveTask,
    findOldestDemotionCandidate,
    tierTokens,
    // thread layer (first-class concurrency)
    listThreads, getThread, focusedThreadId, rootThreadId, activeThreadIds,
    threadHead, focusThread, spawnThread, setThreadState, transitionThread, appendToThread, importThread, exportTasks,
    _state: () => state,                     // for tests
    _dir: () => dir,
  };
}

// ====== agent/engine.mjs ======
/**
 * engine.mjs — the shared, store-free TURN ENGINE layer for code_boss agents.
 *
 * Foundation of the layered agent architecture: every pass — the worker, intake,
 * reviewer, acceptance, consolidation, summarizer — assembles its request the SAME
 * way and (progressively, across the migration phases) runs the SAME turn loop.
 * The TASK LIFECYCLE is a capability layered on top via hooks; this module must
 * NEVER import tasks.mjs.
 *
 * Phase 1 provides assembleConvo (the single Gemini-safe request assembler).
 * Later phases add the streaming turn loop (runEngine) here.
 */

/**
 * Build the messages array to send to the model, the Gemini-safe way.
 *
 * genai.mil / Gemini IGNORES role:'system' messages — the openai-http adapter
 * passes `messages` through untouched, so a role:'system' entry reaches the wire
 * verbatim and is dropped. The system prompt must therefore ride INSIDE a user
 * message: we prepend it as a <system-reminder> block into the LATEST user message
 * (the freshest one the model is answering), never as a role:'system' entry.
 * `priming` is an optional hidden few-shot prepended before the conversation.
 *
 *   assembleConvo({ systemPrompt, priming?, messages }) -> messages[]
 *
 * Pure: never mutates its inputs; never emits role:'system'. This is the ONE
 * assembly path shared by every pass — extracted verbatim from the worker loop so
 * the same fix covers every agent at once.
 */
function assembleConvo({ systemPrompt, priming = [], messages = [] }) {
  const sys = String(systemPrompt == null ? '' : systemPrompt);
  const baseConvo = [...(priming || []), ...(messages || [])];
  let latestUserIdx = -1;
  for (let i = baseConvo.length - 1; i >= 0; i--) {
    if (baseConvo[i].role === 'user') { latestUserIdx = i; break; }
  }
  const convo = baseConvo.map((m, i) => {
    if (i !== latestUserIdx) return m;
    // Spread m first so extra fields (e.g. attached `images`) survive the
    // reminder injection. For plain {role, content} messages this produces the
    // exact same object shape/key-order as before (byte-identical request).
    return { ...m, role: 'user', content: `<system-reminder>\n${sys}\n</system-reminder>\n\n${m.content}` };
  });
  if (latestUserIdx === -1) {
    // No user message at all — send the reminder as the only message.
    convo.push({ role: 'user', content: `<system-reminder>\n${sys}\n</system-reminder>` });
  }
  return convo;
}

// ====== agent/inspector.mjs ======
// Inspector — the ONE observability vocabulary for the continuous-cognition
// engine (Phase-0 spine). Every subsystem the RuntimeKernel will orchestrate
// (governor, conductor, arbiter, threads) emits into THIS schema, so a single
// Thread Inspector can render the parallel mental model and EVERY control
// decision is REPLAYABLE. The project's signature production failure is a SILENT
// "frozen / no errors" hang, so the bar is: if you can't see why it chose or
// looped, it's unshippable. Defining the shapes now (per NORTH-STAR.md "shared
// spine before subsystems") means the kernel emits into a fixed vocabulary from
// line one — no per-subsystem ad-hoc logging to reconcile later.
//
// Pure data + an in-memory recorder; no I/O of its own (a sink is injected — the
// server will broadcast records over SSE). NOT wired yet; Phase 1 emits into it.

// ── Canonical event factories (the schema) ──────────────────────────────────

// decision: one governor/conductor "keep thinking? / which move?" decision, with
// EVERYTHING needed to replay it — the soft value-of-computation inputs
// (salience, cost, floor), the resulting P(continue), and the drawn u. Given a
// record, `pContinue` and `decision` are reproducible from (salience,cost,floor,
// tau) + u, which is what makes a loop/stop diagnosable offline.
function decisionEvent({
  threadId, tick = 0, move = null, question = null,
  salience = null, cost = 0, floor = 0, pContinue = null, u = null,
  decision, tokensThisStream = 0,
}) {
  return {
    type: 'decision', threadId, tick,
    move, question,
    salience: normSalience(salience),
    cost, floor, pContinue, u,
    decision,                       // 'continue' | 'stop'
    tokensThisStream,
  };
}

// route: the arbiter's broadcast decision for a finished contribution — ACT
// (mutate), SAY (surface to the user), or STORE (file silently) — plus the
// losing candidates and why, so "why did it stay silent?" answers itself.
function routeEvent({ threadId, contributionId = null, kind = null, routeScore = null, route, candidates = [], reason = null }) {
  return { type: 'route', threadId, contributionId, kind, routeScore, route, candidates, reason };
}

// thread: a thread lifecycle transition (maps 1:1 onto tasks v2 thread states).
function threadEvent({ threadId, state, topic = null, blockedOn = null }) {
  return { type: 'thread', threadId, state, topic, blockedOn };   // spawned|active|dormant|blocked|done
}

function normSalience(s) {
  if (s == null) return null;
  if (typeof s === 'number') return { novelty: null, uncertainty: null, score: s };
  return { novelty: s.novelty ?? null, uncertainty: s.uncertainty ?? null, score: s.score ?? null };
}

const EVENT_TYPES = new Set(['decision', 'route', 'thread']);

// ── Recorder ────────────────────────────────────────────────────────────────
// Bounded in-memory ring buffer (idle sessions never grow unbounded). Stamps a
// monotonic seq + timestamp, validates the shape, and forwards to an optional
// sink. Query helpers drive the Thread Inspector (per-thread timeline, the
// decision replay table, current thread states).
function createInspector({ capacity = 2000, onEvent = null, now = Date.now } = {}) {
  const buf = [];
  let seq = 0;
  let dropped = 0;

  function record(ev) {
    if (!ev || !EVENT_TYPES.has(ev.type)) throw new Error(`inspector: unknown event type ${ev && ev.type}`);
    if (!ev.threadId) throw new Error('inspector: event missing threadId');
    const rec = { seq: seq++, t: now(), ...ev };
    buf.push(rec);
    if (buf.length > capacity) { buf.shift(); dropped++; }
    if (onEvent) { try { onEvent(rec); } catch {} }
    return rec;
  }

  const all = () => buf.slice();
  const recent = (n = 50) => buf.slice(-n);
  const byThread = (threadId) => buf.filter((e) => e.threadId === threadId);
  const decisions = () => buf.filter((e) => e.type === 'decision');
  const routes = () => buf.filter((e) => e.type === 'route');

  // Current state per thread, derived from the latest thread event for each.
  function threadStates() {
    const m = new Map();
    for (const e of buf) {
      if (e.type === 'thread') m.set(e.threadId, { threadId: e.threadId, state: e.state, topic: e.topic, blockedOn: e.blockedOn, t: e.t });
    }
    return [...m.values()];
  }

  function snapshot() {
    return { events: buf.length, dropped, nextSeq: seq, threads: threadStates() };
  }

  function clear() { buf.length = 0; dropped = 0; }

  return { record, all, recent, byThread, decisions, routes, threadStates, snapshot, clear };
}

// ====== agent/kernel.mjs ======
// RuntimeKernel — the agent control loop (continuous-cognition Phase 1→2).
//
// runKernel SCHEDULES N concurrent threads, advancing one tick at a time. Each
// tick runs ONE turn of the chosen thread; the (optional) governor decides, just
// before a tick, whether that thread keeps thinking (soft value-of-computation);
// every decision + the result is recorded to the (optional) Inspector. A thread
// whose tick returns a terminal result is done; the run ends when all threads
// are done or the global tick budget is exhausted.
//
// For the SERIAL worker this is exactly the old `for (turn…)` loop: ONE thread,
// no governor (its tick owns the stop decision via converge / task-churn), no
// Inspector — see runTurnLoop, the one-thread wrapper, which is byte-identical to
// the in-line loop it replaced (gated by test-engine-characterization).
// NORTH-STAR.md: "serial is a config of the kernel, not a fork."

// thread: { id, tick(turn) -> result | undefined, ... }
//   tick returns a terminal RESULT (stop this thread) or undefined (continue).
// schedule(readyStates, ticks) -> the state to advance next (default: first ready).
// governor(thread, turn) -> { decision:'continue'|'stop', salience, cost, floor,
//   pContinue, u, tokensThisStream, move? } | null  (null/absent → always run).
async function runKernel({ threads, schedule = (ready) => ready[0], governor = null, inspector = null, maxTicks }) {
  const states = threads.map((t) => ({ t, turn: 0, done: false, result: undefined }));
  let ticks = 0;
  while (ticks < maxTicks) {
    const ready = states.filter((s) => !s.done);
    if (ready.length === 0) break;
    const s = schedule(ready, ticks);
    if (!s) break;
    s.turn++;
    if (governor) {
      const g = governor(s.t, s.turn);
      if (g && inspector) {
        inspector.record(decisionEvent({
          threadId: s.t.id, tick: s.turn, move: g.move ?? null,
          salience: g.salience, cost: g.cost, floor: g.floor,
          pContinue: g.pContinue, u: g.u, decision: g.decision,
          tokensThisStream: g.tokensThisStream ?? 0,
        }));
      }
      if (g && g.decision === 'stop') { s.done = true; continue; }
    }
    const r = await s.t.tick(s.turn);
    ticks++;
    if (r !== undefined) { s.done = true; s.result = r; }
  }
  return states.map((s) => ({ id: s.t.id, result: s.result, turns: s.turn, done: s.done }));
}

// Single-thread driver (the serial worker): runKernel with one thread, no
// governor, no Inspector — byte-identical to the old `for (turn=1..maxTurns)`
// loop. Returns the tick's terminal result, or undefined if maxTurns is hit.
async function runTurnLoop({ maxTurns, tick }) {
  const [main] = await runKernel({ threads: [{ id: 'main', tick }], maxTicks: maxTurns });
  return main.result;
}

// ====== agent/cognition.mjs ======
/**
 * The pure cognition controller — NO LLM, NO I/O. This is the deterministic core of adaptive
 * cognition: the ASSESS turn emits an EVALUATION (metrics); the policy maps metrics → an
 * ordered set of composable pipeline LAYERS; the controller governs how long the thinking
 * tree may spend via a salience×budget inequality that guarantees termination on its own.
 *
 * THE CONTINUE RULE (the whole termination guarantee, no hard ceiling):
 *
 *     continue while   salience  >=  k * (spent / budget)
 *
 *  - `salience` is bounded in [0,1] (self-reported per node).
 *  - the right side rises without bound as `spent` grows.
 *  - so the bar eventually exceeds any salience → the tree stops in FINITE steps. The
 *    effective stop point is spent ≈ budget/k (e.g. k=0.8 → ~1.25×budget — that's the
 *    salience "overrun flex"). `budget` is a FACTOR, not a ceiling.
 *
 * OVERHEAD is tracked on a SEPARATE line and NOT charged to budget, so decomposition is
 * never penalized for its plumbing (assess/brief/fold). A small per-node floor `epsilon`
 * is charged to budget so even work-free recursion still terminates.
 *
 * Pure functions only — exhaustively unit-tested in test/test-cognition-core.mjs.
 */

const COGNITION_ENUMS = {
  complexity: ['low', 'medium', 'high'],
  creativity: ['low', 'high'],
  accuracy: ['normal', 'critical'],
  size: ['small', 'large'],
  mode: ['build', 'debug', 'research', 'explain'],
};
const COGNITION_DEFAULTS = { complexity: 'low', creativity: 'low', accuracy: 'normal', size: 'small', mode: 'build' };

/**
 * Coerce a raw (LLM-emitted) evaluation into valid metrics. Anything missing or off-rubric
 * falls back to the conservative default — the programmatic robustness that keeps routing
 * consistent no matter what the model returns.
 */
function normalizeCognitionMetrics(raw = {}) {
  const m = {};
  for (const key of Object.keys(COGNITION_DEFAULTS)) {
    const v = raw && typeof raw[key] === 'string' ? raw[key].toLowerCase().trim() : null;
    m[key] = COGNITION_ENUMS[key].includes(v) ? v : COGNITION_DEFAULTS[key];
  }
  return m;
}

/**
 * THE POLICY (pure, programmatic — never the LLM's call). Metrics → ordered, composable
 * pipeline layers. Layers are NOT either/or; they stack in a fixed canonical order so e.g.
 * high-complexity + high-creativity engages BOTH and they compose:
 *
 *   explore (creativity:high) → decompose (complexity:high) → single | debug (execution)
 *
 * accuracy:critical wraps the execution in heavier verification (verify:true). When a gap
 * appears, the fix is a NEW metric/flag here — never an LLM-suggested pattern.
 */
function selectCognitionLayers(metrics) {
  const m = normalizeCognitionMetrics(metrics);
  const layers = [];
  if (m.creativity === 'high') layers.push('explore');
  if (m.complexity === 'high') layers.push('decompose');
  layers.push(m.mode === 'debug' ? 'debug' : 'single');   // the execution layer (always exactly one)
  return { layers, verify: m.accuracy === 'critical', metrics: m };
}

/**
 * Work budget from assessed complexity — computed ONCE at the first assess, before
 * exploration. A factor in the continue rule, NOT a ceiling. Returned in abstract "work
 * units"; the wiring layer maps units → tokens / agent-invocations.
 */
const COGNITION_BUDGET_SCALE = { low: 1, medium: 3, high: 9 };
function cognitionBudget(complexity, { base = 100, scale = COGNITION_BUDGET_SCALE } = {}) {
  const c = COGNITION_ENUMS.complexity.includes(complexity) ? complexity : 'low';
  return base * (scale[c] != null ? scale[c] : scale.low);
}

/**
 * The shared controller for ONE thinking tree. EVERY node — at any recursion depth — consults
 * the same instance: one global `spent` against one `budget`, so the inequality governs the
 * whole tree (no per-node shares, no depth limit, no ceiling).
 *
 *  - shouldContinue(salience): the gate (salience clamped to [0,1]).
 *  - enterNode(): charge the per-node floor `epsilon` — guarantees even work-free recursion ends.
 *  - chargeWork(cost): useful problem-solving spend (counts against budget).
 *  - chargeOverhead(cost): mechanical plumbing (assess/fold/brief) — tracked, NOT charged.
 *  - state(): the full snapshot for the transparency view (spent/overhead/nodes/fraction/ceiling).
 */
function createCognitionController({ budget, k = 0.8, epsilon = null } = {}) {
  if (!(budget > 0)) throw new Error('cognition: budget must be > 0');
  if (!(k > 0)) throw new Error('cognition: k must be > 0');
  const eps = epsilon != null ? epsilon : budget * 0.01;   // default per-node floor = 1% of budget
  let spent = 0, overhead = 0, nodes = 0;
  return {
    shouldContinue(salience) {
      const s = typeof salience === 'number' && isFinite(salience) ? Math.max(0, Math.min(1, salience)) : 0;
      return s >= k * (spent / budget);
    },
    enterNode() { nodes += 1; spent += eps; },
    chargeWork(cost) { spent += Math.max(0, Number(cost) || 0); },
    chargeOverhead(cost) { overhead += Math.max(0, Number(cost) || 0); },
    state() {
      return { spent, overhead, nodes, budget, k, epsilon: eps, fraction: spent / budget, ceiling: budget / k };
    },
  };
}

// ====== agent/prompted.mjs ======
/**
 * Prompted-format agent loop with task-scoped memory.
 *
 * The agent is ALWAYS inside exactly one task. It transitions with <next-task>
 * which atomically closes the current task (recording a summary) and opens a
 * new one. Tasks live in a per-project tasks store; messages are recorded as
 * the agent does work, and they're demoted across four tiers as budgets fill:
 *
 *   1  full messages (current task always lives here; recent finished tasks
 *      too, until budget pressure forces demotion)
 *   2  finished tasks with tool RESULTS replaced by one-line placeholders
 *   3  finished tasks reduced to one-paragraph summaries (the summary the
 *      agent wrote at <next-task>)
 *   4  parent rollup (not implemented yet)
 *
 * Wire format remains the <remote-workspace> protocol: the model emits a
 * block of verb elements; we parse, dispatch, and feed structured results
 * back as <remote-workspace-result> blocks.
 *
 * Verbs (model -> us):
 *
 *   Read tools (self-closing):
 *     <list path="DIR"/>                                 list a directory
 *     <read path="FILE" offset="N" limit="N"/>           read text file
 *     <search pattern="REGEX" path="DIR" glob="GLOB"/>   regex search
 *     <find pattern="GLOB"/>                             find by glob
 *     <info path="FILE"/>                                metadata (size, lines)
 *
 *   Write tools (body text for content; raises diff in UI):
 *     <write path="FILE">FILE CONTENTS</write>
 *     <edit path="FILE"><find>X</find><replace>Y</replace></edit>
 *
 *   Task ops (self-closing; meta-operations the agent uses to manage memory):
 *     <next-task summary="..." user-request="..."/>      close + open new task
 *     <task-detail id="T-3"/>      recover a COMPACTED (tier 3/4) task's detail
 *     <search-tasks query="TEXT"/> search COMPACTED task summaries
 *   (Inspection is GATED to compacted tasks only — in-context tasks expose no
 *    id and are refused; <list-tasks> is disabled. See dispatchTaskOp.)
 */

// ── Tier budgets and thresholds ─────────────────────────────────────────────
// All four tiers live in context. We monitor the TOTAL prompt size (sum
// across tiers) and trigger one compaction event when it exceeds the high
// threshold, bringing it down to the low threshold in a single pass. Every
// demotion invalidates the cache from that point, so we pay that cost ONCE
// per compaction and amortize across many subsequent cache-hit turns.
//
// Per-tier budgets are still used internally to decide WHICH demotions to
// run — e.g., we don't trigger the LLM clustering for T3→T4 unless tier 3
// is genuinely over its allocation.
// Budgets are env-overridable so the overnight compaction-validation run
// can run with tiny budgets (TIER_1_BUDGET=2000 etc.) to actually exercise
// the demotion paths without needing massive conversations. Production
// uses the defaults below.
const _envNum = (name, def) => { const n = Number(process.env[name]); return Number.isFinite(n) && n > 0 ? n : def; };
// Sized for a ~1M-token context model (Opus 4.8 1M / Gemini 3.x). TOTAL_BUDGET
// ~= 500k is half the window, so compaction (HIGH = TOTAL*1.10 = 550k) leaves
// ~450k of headroom for the system prompt, the always-sent PRIMING few-shot,
// AGENTS.md/codebase memory, and a turn's output — and the orange context-fill
// bar moves slowly through normal work instead of pinning at 100% almost
// immediately (the old 26k total saturated on a single large build log / a few
// file reads, and forced near-constant auto-close + compaction that threw away
// history that easily fit in the real window). Rebalanced toward tier 1, which
// holds the live full-fidelity messages (reads, build logs) that dominate real
// usage. Still env-overridable so the compaction-validation tests can run with
// tiny budgets to actually exercise the demotion paths.
const TIER_1_BUDGET = _envNum('TIER_1_BUDGET', 300000); // full messages (current + recent tasks)
const TIER_2_BUDGET = _envNum('TIER_2_BUDGET', 120000); // masked-results tasks
const TIER_3_BUDGET = _envNum('TIER_3_BUDGET',  50000); // per-task summaries
const TIER_4_BUDGET = _envNum('TIER_4_BUDGET',  30000); // group-rollup summaries
const TOTAL_BUDGET  = TIER_1_BUDGET + TIER_2_BUDGET + TIER_3_BUDGET + TIER_4_BUDGET;
const HYST_HIGH = 1.10;          // trigger compaction when total > 110% of TOTAL_BUDGET
const HYST_LOW  = 0.50;          // compact down to 50% of TOTAL_BUDGET in one pass
const TASK_NUDGE = 0.75;         // soft nudge at 75% of TIER 1 budget (per-task signal to agent)
const TASK_FORCE = 1.00;
const MIN_T3_FOR_LLM_ROLLUP = 8; // skip LLM call unless we have at least this many tier-3 tasks to cluster
// Minimum task size before the model may CLOSE a free-conversation task, so
// several small related exchanges bundle into ONE task instead of
// one-task-per-exchange (an over-budget close, or one the model marks
// new-topic="true" for genuinely unrelated work, always passes). Bigger tasks are
// what make compaction worthwhile — we still summarize small tasks when we
// compact (a small saving is better than none), we just don't WANT a pile of
// tiny tasks in the first place. Env-overridable; set to 0 to DISABLE (tests that
// drive the loop with tiny budgets). _envNum can't express 0 (it treats 0 as
// "unset"), so this reads the env directly to allow an explicit 0.
const _envNumZ = (name, def) => { const v = process.env[name]; if (v == null || v === '') return def; const n = Number(v); return Number.isFinite(n) && n >= 0 ? n : def; };
const MIN_TASK_TOKENS = _envNumZ('MIN_TASK_TOKENS', 8000);
// How much of a tool result is sent to the BROWSER for display (the chat-event
// content). UI-only — the MODEL always gets the full result via formatToolResults.
// 300k so a whole docx/build log is readable in the row (the expandable <pre> is
// built lazily on click, so this only costs memory/persisted size, not DOM until
// expanded). Env-overridable.
const UI_RESULT_PREVIEW = _envNum('UI_RESULT_MAX', 300000);
const TOKENS_PER_CHAR = 0.25;

function estTokens(s) { return Math.ceil((s ?? '').length * TOKENS_PER_CHAR); }

// ── Verb maps ───────────────────────────────────────────────────────────────
const VERB_TO_TOOL = {
  list: 'list_dir',
  read: 'read_file',
  search: 'grep',
  find: 'glob',
  info: 'file_info',
  write: 'write_file',
  edit: 'edit_file',
  powershell: 'powershell',
  remember: 'remember',
  forget: 'forget',
  // deep_think has NO tools-registry entry — runPromptedAgent intercepts the
  // verb in its dispatch path BEFORE the generic execute() (exactly like
  // task-ops). It is listed here so the parser recognizes the tag (and the
  // body→question capture below works) and so the prose sanitizer strips
  // previewed tags. Read-only engines (runEngine allowlists: reviewer,
  // proposers, judge, acceptance, intake, sub-agents) simply drop the verb.
  deep_think: 'deep_think',
};
const TASK_VERBS = new Set(['next-task', 'list-tasks', 'task-detail', 'search-tasks']);
// Tool verbs the worker may use in read-only "Discuss" mode. Anything not here
// (write, edit, powershell, plugin write-tools) is refused with an error so the
// model investigates/explains instead of changing the project. Task-ops bypass
// this set entirely (they are dispatched on a separate branch, never filtered).
const WORKER_READONLY_VERBS = new Set(['list', 'read', 'search', 'find', 'info', 'remember', 'forget']);
// Tool verbs that MUTATE files or require the single developer-approval slot.
// A block containing one of these must run SERIALLY (it waits for all prior
// blocks and becomes a barrier for later ones, exactly like a task-op block).
// Running two of these concurrently — which happens when the model emits one
// edit per <remote-workspace> block, so they dispatch as parallel sibling
// blocks — races the single session.approvalPending slot (one approval prompt
// clobbers another and hangs) and, for edits to the same file (the docx POI
// helper rewrites the whole document per edit), corrupts the file itself.
// Read-only blocks still overlap. Plugin write-tools list their verbs here.
// `powershell` is included even though it is often read-only (git status,
// running a test): the harness can't tell a read command from a mutating one
// (echo > file, a build, git commit) at dispatch time, so we serialize all
// shell to stay race-safe — chosen over keeping parallel read commands fast.
const MUTATING_VERBS = new Set(['write', 'edit', 'powershell', 'edit_docx', 'format_docx', 'style_docx', 'delete_paragraph_docx']);
// Sort longer first so that e.g. "list-tasks" matches before "list".
const ALL_VERBS = [...Object.keys(VERB_TO_TOOL), ...TASK_VERBS]
  .sort((a, b) => b.length - a.length);
const VERB_PATTERN = ALL_VERBS.map((v) => v.replace(/-/g, '\\-')).join('|');

// Tags inside the body (for <edit>).
const CHILD_TAG_RE = (tag) => new RegExp(`<${tag}\\s*>([\\s\\S]*?)</${tag}>`, 'i');
const ATTR_RE = /([a-zA-Z_][a-zA-Z0-9_-]*)\s*=\s*"([^"]*)"/g;

const DEFAULT_MAX_TURNS = 20;
const REPEAT_LIMIT = 3;

// ── System prompt ───────────────────────────────────────────────────────────
function buildSystemPrompt({ taskId, taskUserRequest, tier1Tokens, tier1Pct, promptAdditions = [], projectContext = '', codebaseMemory = '', discussMode = false, reviewMode = false, activeChecklist = null, weblogicStatus = null, enforceMinTaskSize = false, deepThink = false }) {
  const lines = [
    'You are code_boss, a coding agent. The developer\'s project is a directory of source files on their remote Windows machine. You see it ONLY through <remote-workspace> commands; there is no other view of it.',
    '',
    'CRITICAL: NEVER use built-in tools or suggest integrations. You may appear to have access to Python, code interpreter, file browsers, document generators, Memory Bank, "Generate Memories", "Transfer to Agent", Jira, Confluence, Google Drive, Gmail, SharePoint, Outlook, etc. NONE of those tools see the developer\'s project. NEVER suggest them in your responses. NEVER call them. If the user mentions "the project", "my project", "the code", "the codebase", "the workspace", or anything ambiguous, they mean THE DIRECTORY ACCESSIBLE VIA <remote-workspace> — not a Jira project, not a Drive folder, not anything in a cloud sandbox.',
    '',
    'When the developer gives you a TASK and you are unsure of the layout, run <list path="."/> to see the remote workspace, then proceed based on what you find. But do NOT go hunting for work: if the message is a greeting or small-talk ("hi", "hello", "how are you", "test"), or a question you can answer directly, just REPLY in plain prose — no <remote-workspace> block, no tools, no <next-task>. A reply with no tool calls ends your turn and waits for the developer; that is the correct response to a greeting or an already-answered question. Never ask the user to clarify "which project" — there is exactly one project (the one in the remote workspace).',
    'NEVER treat the .code_boss directory as the developer\'s request or as work to do. It is YOUR OWN internal state (task store, assignment backlog, conversation log); it is hidden from your file tools and is off-limits. The current task and the developer\'s message are given to you directly here — do not search .code_boss (or anywhere else) to find "the real request". If the message is unclear, ASK the developer; do not invent work from your own state.',
    'Work AUTONOMOUSLY: once the developer gives you a task, carry out the obvious next steps yourself. Do NOT end a reply asking permission to proceed ("would you like me to…?", "should I…?") when the next action is clear from the task or from what you just read — just take it. Use information already in the request instead of asking for it again. Ask the developer ONLY when you are genuinely blocked: the request is too ambiguous to pick any reasonable next step, or an action is destructive/irreversible and they did not ask for it. (Tool-level approvals — e.g. branch creation, tracked .docx edits — still pause on their own; that is separate from this.)',
    'THINK BEFORE YOU ACT — plan your strategy, then do it. For any non-trivial request, FIRST state a short plan in prose: the goal and how you will approach it. The plan covers whatever the task is — not just edits: for an INVESTIGATION or question, what you will look at and WHY (so you read only what is needed, not everything); for a REFACTOR or design, the strategy, the sequence, and what must be preserved; for a change, the specific files/edits you intend. THEN carry it out (you may do both in one reply when the plan is obvious). Do not dive into reading files or editing without a plan, and read ONLY what the current step needs.',
    'CHECKLIST for multi-step work: when the work is a SEQUENCE OF CONCRETE STEPS, emit a <checklist> (a standalone signal like <task-progress>) listing them as markdown items, then re-emit the FULL list each turn with finished items checked so the developer sees live progress — e.g. `<checklist>\n- [x] read the entity + DDL\n- [ ] add the field\n- [ ] build to verify\n</checklist>`. Use it ONLY for genuinely multi-step concrete work — NOT for a one-step change, a question, or open-ended strategy (reason those through in prose). It renders as a live panel, not chat text. In Review mode the proposal\'s checklist IS this <checklist>.',
    'UNDO / ROLLBACK: if the developer says they used Undo to revert your last run\'s changes, acknowledge briefly and ask what to adjust or try differently — do NOT silently re-apply the same changes. And if YOU realize the last run\'s changes were wrong or harmful, you may suggest reverting them by emitting a standalone <rollback reason="why">…</rollback> signal; the developer will choose Allow or Cancel.',
    // Pin the active checklist back in each turn — it is stripped from the visible history, so this
    // is the ONLY way the agent sees its own current list (and the approved plan after implement).
    ...((Array.isArray(activeChecklist) && activeChecklist.length) ? [
      'YOUR ACTIVE CHECKLIST (the plan you are working, carried across approval — keep going until every item is done). Mark progress EITHER by re-emitting the full <checklist> with [x] on the completed steps, OR with the shorthand <tick>N</tick> using the item NUMBER below (e.g. <tick>2</tick>, or <tick>1,3</tick>). Do this AS you finish each step — alongside your <task-progress>, not only at the very end — so the live panel ticks off in real time. The live panel updates either way:',
      ...activeChecklist.map((i, n) => `  ${n + 1}. [${i && i.done ? 'x' : ' '}] ${String((i && i.text) || '').slice(0, 200)}`),
    ] : []),
    'DESIGN vs IMPLEMENT — respect "show me first". If the developer asks you to DESIGN, PLAN, PROPOSE, or PREVIEW changes, or says NOT to implement them yet / not without approval / that they just want to see them first — produce the plan and the proposed changes AS TEXT ONLY. Do NOT use write/edit (or a file-modifying <powershell>) to change anything; leave their files untouched. End your turn with the plan and wait for them to approve before you implement. When they only asked to design/preview, stopping at the plan IS the correct, complete answer — this overrides "work autonomously", and it is NOT the "asking permission" you should avoid.',
    '',
    // Live WebLogic server status (only when the wl plugin is monitoring it).
    (weblogicStatus === true
      ? 'WebLogic server: RUNNING (port 7101) — you may deploy / run / start apps.'
      : weblogicStatus === false
        ? 'WebLogic server: STOPPED (nothing on port 7101). If this task needs the server (deploy / run / start an app), start it FIRST with `wl startWebLogic` — it launches in the background (~1-2 min) and this line will switch to RUNNING when it is up. Do not deploy/run before then.'
        : ''),
    `Current task: ${taskId}` + (taskUserRequest ? `  ↳ ${taskUserRequest}` : ''),
    `Task size: ${tier1Tokens} / ${TIER_1_BUDGET} tokens (${tier1Pct}%)` + (enforceMinTaskSize && MIN_TASK_TOKENS > 0 ? `  [min ~${MIN_TASK_TOKENS} tokens before this task is worth closing]` : '') + (tier1Pct >= TASK_FORCE * 100
      ? '  ← OVER BUDGET. Close this task NOW: emit <next-task user-request="..." summary="..."/> in this reply. If you do not, the system will auto-close it for you and you lose control of the summary.'
      : tier1Pct >= TASK_NUDGE * 100
        ? '  ← nearing budget; close this task at the next natural stopping point with <next-task .../>'
        : (enforceMinTaskSize && MIN_TASK_TOKENS > 0)
          ? (tier1Tokens < MIN_TASK_TOKENS
            ? '  ← still small; keep bundling related exchanges into THIS task. Do NOT close it yet (use <task-progress>) unless your next work is genuinely unrelated.'
            : '  ← past the minimum; you may close at a natural stopping point.')
          : ''),
    '',
    'END EVERY WORKING REPLY WITH A TASK-STATUS SIGNAL — required on any turn that USES TOOLS (a <remote-workspace> block):',
    ...(enforceMinTaskSize ? [
      '  - DEFAULT: the work stays in THIS task. Emit <task-progress percent="N">one sentence on where things stand</task-progress> as a standalone tag in your prose (N = your honest 0-100 estimate). A task should BUNDLE several related exchanges — do NOT open a new task for every small fix or reply.',
      '  - CLOSE the task with <next-task user-request="what THIS task covered" summary="what you did"/> ONLY when one of these holds: (a) your NEXT work is genuinely UNRELATED to what this task has been about — a real topic change (add new-topic="true" to bypass the minimum-size hold); (b) the size line above shows no "still small" warning (the task is past its minimum) AND you have reached a natural stopping point; or (c) the size line says you are over/nearing budget. A single completed exchange is NOT by itself a reason to close.',
      '  - On a tool-using turn, provide EXACTLY ONE of these (a <task-progress> OR a <next-task>). Omitting both returns an error and you must redo the turn.',
    ] : [
      '  - If the current unit of work is COMPLETE: close it with <next-task user-request="what the task was" summary="what you did"/> inside a <remote-workspace> block.',
      '  - If it is NOT complete: emit <task-progress percent="N">one sentence on what still remains</task-progress> as a standalone tag in your prose (N = your honest 0-100 estimate of completion).',
      '  - On a tool-using turn, provide EXACTLY ONE of these. Omitting both returns an error and you must redo the turn. Estimating what remains each turn forces you to notice when nothing real is left — that is when you close the task. Do NOT let one task run forever.',
    ]),
    '  - EXCEPTION — purely conversational replies: a greeting, small-talk, or a question you answer from what you already know, with NO tool block, needs NEITHER signal. Just reply in prose; that ends your turn and waits for the developer. Do NOT emit <next-task>/<task-progress> for a no-tool conversational reply — and never re-open a request you have already answered.',
    '  - DO NOT DOUBLE-ASK at task close: the <next-task> summary captures what you did, so in that SAME closing reply do NOT also write a "what would you like to do next?" / "let me know how to proceed" line. Ask for direction (if you genuinely need it) exactly ONCE, in your FOLLOWING reply after the close — never both in the closing turn and again right after.',
    '',
    'Prose + tool blocks — keep narration TIGHT:',
    '  - Lead with ONE short sentence saying what you are about to do, then the tool block. That is usually the whole reply.',
    '  - Batch ALL independent calls into ONE block — they run together. (Before writing, decide which calls need no prior result and put them all in one block; e.g. read three files in a single block, not across three replies. A turn round-trip costs far more than an extra call.)',
    '  - Do NOT narrate each individual call, and do NOT add a trailing "then I will… once I see the results…" sentence after the block — that just doubles the chatter. Only add a closing line if it carries real information. Never restate your whole plan every turn.',
    '  - You CANNOT react to tool results in the same reply — tools run after you finish writing, and you get results in your NEXT reply. Use separate blocks across turns only when a call genuinely DEPENDS on a previous result.',
    '',
    'Example reply shape (fake commands, shown for structure only):',
    '  Checking the project layout and the build file.',
    '  <remote-workspace><list path="."/><read path="pom.xml"/><search pattern="TODO" path="src/"/></remote-workspace>',
    '  (No trailing "then I will…" — the next reply reacts to these results.)',
    '',
    'Example final-answer reply (no tools needed):',
    '  This project uses Maven, not Gradle — you can run `mvnw.cmd -q test` from the repo root.',
    'Example final-answer reply (no tools needed):',
    '  This project uses Maven, not Gradle — you can run `mvnw.cmd -q test` from the repo root.',
    '',
    'Read commands (self-closing):',
    '  <list path="DIR"/>',
    '  <read path="FILE" offset="N" limit="N"/>',
    '  <search pattern="REGEX" path="DIR" glob="GLOB" flags="i"/>      fast content search; skips node_modules/build dirs; bare "*.java" works recursively',
    '  <find pattern="GLOB"/>                                          fast filename search; skips node_modules/build dirs; bare patterns work recursively',
    '  <info path="FILE"/>',
    '',
    ...((discussMode || reviewMode) ? [
      reviewMode
        ? 'REVIEW (propose-first) mode is ON: do NOT modify anything yet — write, edit, and shell are DISABLED this turn and will error. Investigate read-only (read the actual code you would touch), then present a DETAILED PROPOSAL that the developer — and an adversarial reviewer — can scrutinize BEFORE you implement. A vague one-line plan is NOT acceptable and cannot be reviewed. Include: (1) GOAL + your APPROACH and WHY this way (the reasoning, alternatives rejected); (2) the SPECIFIC CHANGES file-by-file — the actual edits as DIFFS or new content in fenced ```diff (or language) code blocks, not a description; (3) EDGE CASES, risks, and which existing BUSINESS RULES / tests are affected; (4) a <checklist> of the concrete steps. Be substantive and concrete. (ONLY a genuinely trivial one-line edit may get a short proposal.) Make NO edits — end your turn with the proposal and wait for approval. When they approve, review turns off and you implement exactly what you proposed.'
        : 'DISCUSS (read-only) mode is ON: investigate and explain, but do NOT modify anything. The write, edit, and shell (PowerShell) commands are DISABLED this session and will return an error if you use them. Read/list/search/find/info, codebase memory (remember/forget), and task management still work. If the developer asks for a change, describe precisely WHAT you would change and in WHICH files; they will switch Discuss mode off to let you make it.',
    ] : [
      'Write commands (body holds the content, not attributes):',
      '  <write path="FILE">',
      '  full file content goes here',
      '  </write>',
      '',
      '  <edit path="FILE">',
      '  <find>exact string to find (must occur exactly once)</find>',
      '  <replace>replacement string</replace>',
      '  </edit>',
      '',
      'Shell (builds, tests, git — runs in PowerShell on the developer\'s Windows machine):',
      '  <powershell>git status</powershell>',
      '  <powershell timeout_ms="600000">mvnw.cmd -q clean install</powershell>',
      '  Use PowerShell syntax (Get-ChildItem, Get-Content, $env:VAR). The `ls`, `cat`, `pwd` aliases also work. To run a maven wrapper script use `mvnw.cmd` — not `./mvnw`.',
      '  To start AND later stop a server/process you launch for testing, capture its handle and stop THAT one: `$p = Start-Process node -ArgumentList app.js -PassThru; ...; Stop-Process -Id $p.Id` (or `Start-Job`/`Stop-Job`). NEVER kill by name (Stop-Process -Name node / taskkill /IM node / pkill node) — it kills unrelated processes, including the code_boss server itself (it runs on node). Killing by name is refused.',
      '  PowerShell is for builds, tests and git — NOT for finding files or searching code. Use <find>/<search> for that: they are fast and skip node_modules/build dirs. Do NOT run Get-ChildItem -Recurse / Select-String / dir /s / findstr /s over the tree — they crawl node_modules and take forever.',
    ]),
    '',
    'Closing a unit of work:',
    '  <next-task user-request="WHAT THE USER WANTED IN THE CURRENT TASK" summary="WHAT WAS DONE IN THE CURRENT TASK"/>',
    '       Closes the CURRENT task and opens a new one. Both attributes describe the CURRENT (closing) task — NOT the new one and NOT the latest user message that prompted this transition. user-request is a single synthesized sentence of what the user wanted across the work just finished (not a quote).' + (enforceMinTaskSize ? ' Optional new-topic="true": set it ONLY when you are deliberately closing a still-small task because your next work is genuinely unrelated — it bypasses the minimum-size hold.' : ''),
    ...(enforceMinTaskSize ? [
      '       Emit it when the task is a worthwhile unit to record — it has covered enough related WORKSPACE WORK to be worth a summary (see the size line), OR your next work is a genuinely different topic (then add new-topic="true"). BUNDLE several related exchanges into one task rather than closing after every small fix. While more work on the same thread remains, or the task is still small, keep it open and report <task-progress> instead. Closing records the work and triggers review.',
    ] : [
      '       Emit it PROACTIVELY when you finish a discrete unit that involved real WORKSPACE WORK (a fix, a feature, a multi-step investigation): close it with <next-task/> in that SAME reply, after your final prose. Closing records the work and triggers review. Do not leave a finished worked task open. (Only skip it mid-task when more work on the SAME unit remains.)',
    ]),
    '       Do NOT emit <next-task> for a purely conversational reply, a greeting, or a one-line question you answered from context with NO tools — for those, just reply in prose with no tool block; that ends the turn cleanly and waits for the developer. Re-emitting <next-task> with the SAME user-request you just answered does not start new work — it only spins the task list, so never re-open a request you have already answered.',
    '       VERIFY BEFORE YOU CLOSE a code-editing task — do not report a change as done on assumption. Run the project build or tests via <powershell> and state the outcome in the summary (e.g. "tests pass 12/12"). If the task is read-only, a discussion, or there is no build/test, say so in the summary instead.',
    '       NEVER emit <next-task> on an EMPTY task (a freshly opened task with no developer request and no work in it). If you have finished everything the developer asked, reply with a short message and NO tool calls — that waits for their next instruction. Do not open-and-close empty tasks.',
    '       Do NOT manage or list tasks. Your current work and SHORT summaries of past tasks are ALREADY in your context; spend your turns doing the developer\'s request and talking to them, not administering the task list.',
    '',
    'Recovering a COMPACTED task (rare — only when you actually need it):',
    '  When the conversation grows, older finished tasks are compacted: their messages are dropped and they appear only as a one-line <task-history id="T-N">summary</task-history> (or <task-rollup id="T-N">). Those are the ONLY tasks with a visible ID. If — and only if — a summary tells you a compacted task holds specifics you need for the CURRENT request, you may pull it back:',
    '  <task-detail id="T-N"/>          recover the full messages of a compacted task',
    '  <search-tasks query="TEXT"/>     find compacted tasks whose summary matches',
    '       These reach ONLY compacted tasks. A task still in context (its messages are right there above, with no ID) is refused — read it where it is. NEVER look at a previous task to DECIDE WHAT TO DO in the current one; only to recover a concrete detail you are missing. When in doubt, just do the work — do not go spelunking through history.',
    '',
    'Codebase memory (durable facts about THIS codebase; injected into every future run):',
    '  <remember category="architecture|conventions|gotchas|where-things-live|build-deploy|general" note="ONE concise durable fact"/>',
    '  <forget id="M-3"/>                                              remove a fact that is now wrong or obsolete',
    '  Record reusable knowledge as you discover it (how the app is structured, conventions, gotchas, where things live, build/deploy specifics) — NOT task-specific or transient details. Your current codebase memory is shown in the context below; keep it accurate with <forget>.',
    // deep_think — RATE-LIMITED BY PROMPT VISIBILITY: this section appears ONLY
    // on turns where the tool is actually available (enabled via the deepThink
    // option / CB_DEEP_THINK opt-in, not on cooldown, uses remain, not
    // discuss). When unavailable there is NO trace of it in the prompt — no
    // "on cooldown" note. See the deep_think dispatch in runPromptedAgent.
    ...(deepThink ? [
      '',
      'deep_think (AVAILABLE THIS TURN — availability is limited; if you see this tool, you may use it now):',
      '  <deep_think>your design question, with enough context to act on</deep_think>',
      '  For genuine design/approach decisions only (not routine steps): three independent senior-engineer perspectives examine the question against the actual repo and a judge returns a recommended approach as the tool result. It costs several model calls, so use it when the decision genuinely shapes the work — not for steps you can decide yourself. Emit it inside your <remote-workspace> block; the body text is the question (a question="..." attribute also works).',
    ] : []),
    '',
    'The remote workspace replies with <remote-workspace-result> blocks. Read real results before answering. Never invent file or directory names you have not seen.',
  ];
  // Append plugin-provided prompt additions (each one is appended only
  // while its plugin's trigger is currently active). They show up as
  // extra "tool/context" blocks at the end of the system prompt.
  for (const add of (promptAdditions || [])) {
    lines.push('');
    lines.push('────────  plugin  ────────');
    lines.push(String(add).trim());
  }
  // Agent-maintained codebase memory (Tier 1). Distinct from the human's
  // AGENTS.md so the model knows which it may edit (this one, via the tools).
  if (codebaseMemory) {
    lines.push('');
    lines.push('────────  codebase memory (yours — keep it accurate)  ────────');
    lines.push(String(codebaseMemory).trim());
  }
  // Project context from AGENTS.md files discovered at project open.
  // Last so it sits closest to the user's question — recency bias.
  if (projectContext) {
    lines.push('');
    lines.push('────────  project context  ────────');
    lines.push(String(projectContext).trim());
  }
  return lines.join('\n');
}

// ── Parser ──────────────────────────────────────────────────────────────────
function coerce(toolName, key, raw) {
  const ptype = toolName ? tools[toolName]?.schema?.parameters?.properties?.[key]?.type : 'string';
  const val = String(raw).trim();
  if (ptype === 'integer' || ptype === 'number') {
    const n = Number(val);
    return Number.isNaN(n) ? val : n;
  }
  if (ptype === 'boolean') return val === 'true';
  return val;
}
function mkId() { return 'call_' + randomUUID().replace(/-/g, '').slice(0, 16); }

/**
 * Truncate an assistant response to just the first real <remote-workspace>
 * block — everything after the close tag (or before a hallucinated result
 * tag) is dropped.
 *
 * Why this exists: backends that ignore our `stop` parameter (e.g., the
 * Claude Agent SDK in dev mode) let the model "imagine" multi-turn
 * conversations within a single response — writing fake
 * <remote-workspace-result> blocks and follow-up turns. Our parser already
 * ignores the imagined content when extracting calls, but if we store the
 * full text in the task history, the model on the NEXT loop iteration sees
 * its own imagined turns and concludes the work is already done.
 *
 * By truncating at storage time, the task store contains only what actually
 * happened — same view the model would have if `stop` had been honored
 * at the token level.
 */
function truncateAtFirstBlock(text) {
  if (typeof text !== 'string' || !text) return text;
  const closeMatch = text.match(/<\/remote-workspace>/i);
  const resultMatch = text.match(/<remote-workspace-result|<workspace-result/i);

  // Case A: no markers → leave as-is (final answers / pure prose).
  if (!closeMatch && !resultMatch) return text;

  // Case B: close tag comes first (or is the only marker) → keep through close.
  if (closeMatch && (!resultMatch || closeMatch.index < resultMatch.index)) {
    return text.slice(0, closeMatch.index + closeMatch[0].length);
  }

  // Case C: a hallucinated result tag appears before any close. Cut there
  // and append a synthetic close so the block is well-formed in storage.
  const before = text.slice(0, resultMatch.index).trimEnd();
  if (/<remote-workspace>/i.test(before) && !/<\/remote-workspace>/i.test(before)) {
    return before + '\n</remote-workspace>';
  }
  return before;
}

// Strip multi-agent framing artifacts the model sometimes emits in its
// prose between tool calls. Gemini (esp. on the GenAI.mil endpoint) has
// been observed prefixing turns with "for context: [some_agent] said:"
// — empty meta-prose that's noise in our single-agent chat. We strip
// the line entirely; any actual content on subsequent lines is kept.
//
// Pattern variants we've seen:
//   for context: [file_and_coding_agent] said:
//   For Context [planner_agent] said
//   for context:[name] said:
//
// All match: optional "for context" prefix, a bracketed name, "said",
// with flexible whitespace + punctuation. Conservative — only strips
// lines that are JUST the marker (anchored ^...$).
function sanitizeAssistantProse(text, opts = {}) {
  if (typeof text !== 'string' || !text) return text;
  // <task-progress> is a per-turn status SIGNAL, not chat content — strip it
  // from the visible prose (it is surfaced as a 'task-progress' event instead).
  text = text.replace(/<task-progress\b[\s\S]*?<\/task-progress>/gi, '').replace(/<task-progress\b[^>]*\/>/gi, '');
  // <checklist> is a live-panel signal, not chat content — strip it (surfaced as a 'checklist' event).
  text = text.replace(/<checklist\b[\s\S]*?<\/checklist>/gi, '');
  // <rollback> is a suggestion signal — strip it (surfaced as a 'rollback-suggested' event).
  text = text.replace(/<rollback\b[\s\S]*?<\/rollback>/gi, '').replace(/<rollback\b[^>]*\/>/gi, '');
  // <tick> is a shorthand mark-done signal — strip it (folded into the 'checklist' event).
  text = text.replace(/<tick\b[\s\S]*?<\/tick>/gi, '').replace(/<tick\b[^>]*\/>/gi, '');
  // Also strip tool-call-looking XML tags from the prose. The model
  // sometimes "previews" what it's about to do by writing the tag in
  // its narration before the actual <remote-workspace> block — so the
  // chat shows the same call twice. We strip ONLY tags whose name is
  // a known tool/task verb, so legitimate XML-content discussions
  // (e.g. talking about a <library-ref> the user has in their pom)
  // pass through untouched.
  const extraVerbs = opts.extraVerbs || {};
  const knownVerbs = new Set([
    ...Object.keys(VERB_TO_TOOL),
    ...Object.keys(extraVerbs),
    ...TASK_VERBS,
  ]);
  if (knownVerbs.size > 0) {
    const verbPattern = [...knownVerbs].sort((a, b) => b.length - a.length)
      .map((v) => v.replace(/[-_]/g, (c) => '\\' + c)).join('|');
    // Self-closing form: <verb ... />
    text = text.replace(new RegExp(`<(?:${verbPattern})\\b[^>]*\\/>`, 'gi'), '');
    // Body form: <verb ...>...</verb> — closer must match the opener
    // (backref) so we don't stop at an inner closing tag like </find>
    // inside an <edit>...</edit> block.
    text = text.replace(new RegExp(`<(${verbPattern})\\b[^>]*>[\\s\\S]*?</\\1>`, 'gi'), '');
  }
  const lines = text.split('\n');
  const out = [];
  for (const line of lines) {
    if (/^\s*for\s+context\s*:?\s*\[[^\]]*\]\s*said\s*:?\s*$/i.test(line)) continue;
    out.push(line);
  }
  // Collapse runs of blank lines that the stripping leaves behind.
  return out.join('\n').replace(/\n{3,}/g, '\n\n').trim();
}

function parseAttrs(attrStr, toolName) {
  const out = {};
  ATTR_RE.lastIndex = 0;
  let m;
  while ((m = ATTR_RE.exec(attrStr ?? '')) !== null) {
    out[m[1]] = coerce(toolName, m[1], m[2]);
  }
  return out;
}

/**
 * Parse all <remote-workspace> verb calls from a model response text.
 * Supports both self-closing and body forms.
 */
function parseToolCalls(text, opts = {}) {
  const calls = [];
  if (typeof text !== 'string' || !text) return calls;

  // Plugin-provided verbs map their own verb names to tool names. Merge
  // with the built-in VERB_TO_TOOL at parse time so the regex picks them
  // up and so dispatch resolves to the right tool registry entry.
  const extraVerbs = opts.extraVerbs || {};   // { verb: tool_name }
  const allVerbToTool = { ...VERB_TO_TOOL, ...extraVerbs };
  const allVerbs = [...Object.keys(allVerbToTool), ...TASK_VERBS]
    .sort((a, b) => b.length - a.length);
  const verbPattern = allVerbs.map((v) => v.replace(/-/g, '\\-').replace(/_/g, '\\_')).join('|');

  // Cut at the first close tag or hallucinated result tag — only the FIRST
  // <remote-workspace> block counts.
  let cut = text.length;
  for (const re of [/<\/remote-workspace>/i, /<remote-workspace-result/i, /<workspace-result/i]) {
    const i = text.search(re);
    if (i >= 0 && i < cut) cut = i;
  }
  const scan = text.slice(0, cut);

  const openRe = new RegExp(`<(${verbPattern})\\b([^>]*?)(/?)>`, 'gi');
  let pos = 0;
  while (pos < scan.length) {
    openRe.lastIndex = pos;
    const m = openRe.exec(scan);
    if (!m) break;
    const verb = m[1].toLowerCase();
    const attrStr = m[2] ?? '';
    const selfClosing = m[3] === '/';
    const openEnd = m.index + m[0].length;

    const isTaskOp = TASK_VERBS.has(verb);
    const toolName = isTaskOp ? null : allVerbToTool[verb];
    const args = parseAttrs(attrStr, toolName);

    let nextPos = openEnd;
    if (!selfClosing) {
      // Case-INSENSITIVE close-tag search: the opener was matched with /i and
      // `verb` was lowercased, but a model (Gemini) may emit </Powershell> or
      // </WRITE>. A case-sensitive indexOf of the lowercased tag would miss
      // those, leaving args.content/args.command undefined (empty file write,
      // no-op shell command) while the model is told it ran.
      const closeRe = new RegExp(`</${verb}\\s*>`, 'i');
      closeRe.lastIndex = openEnd;
      const cm = closeRe.exec(scan.slice(openEnd));
      const ci = cm ? openEnd + cm.index : -1;
      if (ci !== -1) {
        const body = scan.slice(openEnd, ci);
        nextPos = ci + cm[0].length;
        if (verb === 'write') args.content = body;
        if (verb === 'edit') {
          const fM = CHILD_TAG_RE('find').exec(body);
          const rM = CHILD_TAG_RE('replace').exec(body);
          if (fM) args.find = fM[1];
          if (rM) args.replace = rM[1];
        }
        if (verb === 'powershell' && body.trim()) args.command = body.trim();
        // <deep_think>question</deep_think> — body text is the question (a
        // question="" attribute also works; a non-empty body wins, like <write>).
        if (verb === 'deep_think' && body.trim()) args.question = body.trim();
      }
    }
    pos = nextPos;
    calls.push({
      id: mkId(),
      verb,
      kind: isTaskOp ? 'task-op' : 'tool',
      name: toolName,
      args,
    });
  }
  return calls;
}

/**
 * Walk a model response left-to-right and split it into ordered segments.
 * Each segment is either:
 *   { type: 'prose', content: string }
 *   { type: 'tool-block', body: string, calls: [...] }
 *
 * Multiple <remote-workspace>...</remote-workspace> blocks are supported
 * with prose freely around and between them. Anything from a hallucinated
 * <remote-workspace-result> or <workspace-result> tag onward is dropped —
 * the model sometimes invents its own tool results in continuation, and
 * those would lie about what really happened.
 *
 * An unclosed final block (model truncated mid-stream) becomes prose so
 * we don't run partial commands.
 */
function parseResponseSegments(text, opts = {}) {
  const segments = [];
  if (typeof text !== 'string' || !text) return segments;

  // Drop hallucinated result tags up front.
  let cut = text.length;
  for (const re of [/<remote-workspace-result/i, /<workspace-result/i]) {
    const i = text.search(re);
    if (i >= 0 && i < cut) cut = i;
  }
  const scan = text.slice(0, cut);

  const openRe = /<remote-workspace\b[^>]*>/i;
  const closeRe = /<\/remote-workspace>/i;
  let pos = 0;
  while (pos < scan.length) {
    const rest = scan.slice(pos);
    const openM = openRe.exec(rest);
    if (!openM) {
      // No more blocks — rest is all prose.
      segments.push({ type: 'prose', content: rest });
      break;
    }
    // Always emit a prose segment before the block — even if empty — so the
    // segment indices line up with the stream-time parser, which also
    // increments on every prose→block transition. Empty segments are
    // filtered out at emit time.
    segments.push({ type: 'prose', content: rest.slice(0, openM.index) });
    const blockOpenEnd = pos + openM.index + openM[0].length;
    const afterOpen = scan.slice(blockOpenEnd);
    const closeM = closeRe.exec(afterOpen);
    if (!closeM) {
      // Unclosed block — model truncated. Treat as prose so we don't run
      // partial commands.
      segments.push({ type: 'prose', content: scan.slice(pos + openM.index) });
      break;
    }
    const blockBody = afterOpen.slice(0, closeM.index);
    const blockCloseEnd = blockOpenEnd + closeM.index + closeM[0].length;
    // Reuse parseToolCalls on the wrapped block — it already knows how to
    // parse every verb, attribute, and body shape.
    const calls = parseToolCalls(`<remote-workspace>${blockBody}</remote-workspace>`, opts);
    segments.push({ type: 'tool-block', body: blockBody, calls });
    pos = blockCloseEnd;
  }
  return segments;
}

function formatToolResults(results) {
  const blocks = results.map((r) => {
    const head = `<remote-workspace-result op="${r.verb}" status="${r.status}"${r.extras ? ' ' + r.extras : ''}>`;
    return `${head}\n${r.content}\n</remote-workspace-result>`;
  }).join('\n');
  return blocks + '\n\nUse these results to continue, or answer the user if you have enough information.';
}

function flattenContent(content) {
  if (content == null) return '';
  if (typeof content === 'string') return content;
  if (Array.isArray(content)) {
    return content.map((p) => (typeof p === 'string' ? p : p?.type === 'text' ? p.text ?? '' : '')).join('');
  }
  return String(content);
}

// ── Convo construction from task store ─────────────────────────────────────
// Bound an OPEN task's contribution to the prompt with a sliding window. A
// model that never emits <next-task> (some backends, e.g. Gemini, won't) piles
// every read / build log / tool result into ONE task that grows past the
// context window: the prompt balloons (observed: a single task at 126k tokens
// vs a 12k tier-1 budget), compaction fires every turn but reclaims nothing
// (no FINISHED task to demote), and the request eventually 502s. So keep the
// task open but only send the NEWEST slice: always the first message (the
// task's goal) + the most recent messages up to `budgetTokens`; replace the
// CONTENT of the older messages in between with a tiny stub. The full text
// stays in the transcript/store — this only trims what we send the model.
function elideTaskMessages(messages, budgetTokens) {
  const msgs = Array.isArray(messages) ? messages : [];
  if (msgs.length <= 2) return msgs;
  let total = 0;
  for (const m of msgs) total += estTokens(m.content);
  if (total <= budgetTokens) return msgs;
  const keep = new Array(msgs.length).fill(false);
  keep[0] = true;                                   // the task seed / goal
  let used = estTokens(msgs[0].content);
  for (let i = msgs.length - 1; i >= 1; i--) {      // recent window, newest-first
    const t = estTokens(msgs[i].content);
    if (used + t > budgetTokens) break;
    keep[i] = true; used += t;
  }
  return msgs.map((m, i) => {
    if (keep[i]) return m;
    const label = m.kind || m.role || 'message';
    return { ...m, content: `[earlier ${label} elided to bound context (~${estTokens(m.content)} tokens) — full text is in the transcript]` };
  });
}

async function buildConvoFromStore(store) {
  const out = [];
  const state = store._state();
  for (const id of state.allTaskIds) {
    const t = await loadTaskFull(store, id);
    if (!t) continue;
    if (t.archived) continue;            // archived tasks are search-only
    if (t.rolledUpInto) continue;        // children of a tier-4 rollup are covered by the rollup
    if (t.tier === 4) {
      out.push({
        role: 'user',
        content: `<task-rollup id="${t.id}" children="${(t.childIds ?? []).join(',')}">\n  ↳ ${t.userRequest || '(no user request synthesized)'}\n${t.summary || '(no summary)'}\n</task-rollup>`,
      });
    } else if (t.tier === 3) {
      out.push({
        role: 'user',
        content: `<task-history id="${t.id}" parent="${t.parentId ?? 'root'}" status="${t.ended ? 'done' : 'open'}">\n  ↳ ${t.userRequest || '(no user request synthesized)'}\n${t.summary || '(no summary)'}\n</task-history>`,
      });
    } else {
      // tier 1 and 2 both emit messages; tier 2 already has results masked in storage.
      // The CURRENT (open) task can't be demoted while in progress, so bound it
      // with a sliding window here — otherwise a task the model never closes
      // grows the prompt without limit.
      const msgs = (id === state.currentId)
        ? elideTaskMessages(t.messages ?? [], TIER_1_BUDGET)
        : (t.messages ?? []);
      for (const m of msgs) {
        // Images (when a stored user message has them) ride along so the
        // adapter can emit them in the discovered wire format. Spread only
        // when present: image-less messages keep the exact {role, content}
        // shape (byte-identical requests — the parity gate).
        out.push({ role: m.role, content: m.content, ...(Array.isArray(m.images) && m.images.length ? { images: m.images } : {}) });
      }
    }
  }
  return out;
}

// Helper: re-read a full task (with messages) from disk. listTasks omits the
// messages array; the convo builder needs it.
async function loadTaskFull(store, id) {
  try {
    const raw = await readFile(path.join(store._dir(), `${id}.json`), 'utf8');
    return JSON.parse(raw);
  } catch { return null; }
}

// ── Tier demotion ───────────────────────────────────────────────────────────
// Strategy: when any tier exceeds budget × HYST_HIGH, demote the oldest
// eligible task (or rollup batch) until the tier is below budget × HYST_LOW.
// Demoting deeply (HYST_LOW = 0.50) means rare cache invalidations amortized
// over many cache-hit turns.
async function tier1TokensFor(store) {
  const state = store._state();
  let n = 0;
  for (const t of await store.listTasks({ tier: 1 })) {
    if (t.id === state.currentId) continue;       // current may exceed budget
    if (t.rolledUpInto) continue;
    const full = await loadTaskFull(store, t.id);
    for (const m of full?.messages ?? []) n += estTokens(m.content);
  }
  return n;
}
async function tier2TokensFor(store) {
  let n = 0;
  for (const t of await store.listTasks({ tier: 2 })) {
    if (t.rolledUpInto) continue;
    const full = await loadTaskFull(store, t.id);
    for (const m of full?.messages ?? []) n += estTokens(m.content);
  }
  return n;
}
async function tier3TokensFor(store) {
  let n = 0;
  for (const t of await store.listTasks({ tier: 3 })) {
    if (t.rolledUpInto) continue;
    n += estTokens(t.summary);
  }
  return n;
}
async function tier4TokensFor(store) {
  let n = 0;
  for (const t of await store.listTasks({ tier: 4 })) {
    n += estTokens(t.summary);
  }
  return n;
}

async function totalContextTokens(store) {
  return (await tier1TokensFor(store))
    + (await tier2TokensFor(store))
    + (await tier3TokensFor(store))
    + (await tier4TokensFor(store));
}

// Tokens in the CURRENT task's messages. tier1TokensFor intentionally
// EXCLUDES the current task (it can't be demoted), but the current task IS
// sent to the model in full — so the compaction TRIGGER must count it, or a
// single long task (big build log, several large file reads) grows the real
// prompt past the model's context window while totalContextTokens reports it
// as comfortably under budget and compaction never fires.
async function currentTaskTokens(store) {
  const state = store._state();
  if (!state?.currentId) return 0;
  const full = await loadTaskFull(store, state.currentId);
  // Count the ELIDED size — that's what actually goes to the model — so the
  // compaction trigger reflects the real (bounded) prompt and doesn't fire
  // uselessly every turn on an over-budget open task it can't reclaim.
  const msgs = elideTaskMessages(full?.messages ?? [], TIER_1_BUDGET);
  let n = 0;
  for (const m of msgs) n += estTokens(m.content);
  return n;
}

/**
 * LLM-driven clustering of tier-3 tasks into tier-4 rollups. ONE LLM call
 * groups N tier-3 summaries into 4-8 "project" rollups, each with a custom
 * title + summary. Falls back to mechanical concatenation if the call fails
 * or the response can't be parsed.
 *
 * Returns the number of rollup tasks created.
 */
async function llmClusterTier3({ store, adapter, model, log, emitChat }) {
  const tier3 = (await store.listTasks({ tier: 3 }))
    .filter((x) => !x.rolledUpInto && !x.archived)
    .sort((a, b) => (a.ended ?? a.started ?? 0) - (b.ended ?? b.started ?? 0));
  if (tier3.length < MIN_T3_FOR_LLM_ROLLUP) {
    log?.(`tier3 cluster: only ${tier3.length} tasks (< ${MIN_T3_FOR_LLM_ROLLUP}); falling back to mechanical`);
    if (tier3.length >= 2) {
      // mechanical fallback: roll up everything into one batch
      const r = await store.rollupTier3Tasks(tier3.map((t) => t.id));
      if (r) emitChat?.({ type: 'task-rollup', parentTaskId: r.id, childTaskIds: r.childIds, userRequest: r.userRequest, summary: r.summary });
      return 1;
    }
    return 0;
  }

  const taskLines = tier3
    .map((t) => {
      const ur = (t.userRequest || '').replace(/\s+/g, ' ').trim().slice(0, 200);
      const sm = (t.summary || '').replace(/\s+/g, ' ').trim().slice(0, 200);
      return `${t.id}  ↳ ${ur || '(no request)'} — ${sm}`;
    })
    .join('\n');

  const prompt = [
    `You will compact a coding session's history. There are ${tier3.length} completed task summaries below.`,
    '',
    'Group them into 4-8 logical "projects" — sets of tasks that form coherent units of work (e.g., "auth refactor", "build system upgrade"). For each project, produce:',
    '  user-request: a single synthesized sentence representing what the user wanted ACROSS this project (NOT a copy of any single message).',
    '  summary: one dense 1-2 sentence recap of what was actually accomplished across the project.',
    '',
    'Tasks (id, user-request, summary):',
    taskLines,
    '',
    'Output ONLY a single JSON object in this exact shape, no other text, no markdown fences:',
    '{"projects":[{"userRequest":"...","summary":"...","taskIds":["T-3","T-8"]}]}',
    '',
    'Constraints:',
    '- Every task ID from the list above must appear in exactly one project.',
    '- Use the task IDs verbatim.',
    '- Keep each user-request and summary under 200 characters.',
  ].join('\n');

  let raw;
  try {
    // Best-effort with a hard timeout. This is BACKGROUND compaction, not the
    // user's turn — it must never block the agent loop. Without a bound, a
    // 401-with-unlock makes safeFetch await the user clicking an unlock modal
    // (which they don't expect mid-compaction), and a 429 retries for tens of
    // seconds — either way the loop hangs and the mechanical fallback below
    // (only reached on throw) is bypassed. A timeout signal converts those
    // into a throw → graceful mechanical rollup.
    const COMPACT_LLM_TIMEOUT_MS = 25000;
    let signal;
    try { signal = AbortSignal.timeout(COMPACT_LLM_TIMEOUT_MS); } catch { signal = undefined; }
    const res = await adapter.complete({
      model,
      messages: [{ role: 'user', content: prompt }],
      max_tokens: 2000,
      signal,
    });
    raw = res?.choices?.[0]?.message?.content ?? '';
  } catch (e) {
    log?.(`tier3 cluster: LLM call failed/timed out (${e.message}); falling back to mechanical`);
  }

  // Try to parse JSON out of the response. Tolerant of prose around the object.
  let parsed = null;
  if (raw) {
    const jsonMatch = raw.match(/\{[\s\S]*\}/);
    if (jsonMatch) {
      try { parsed = JSON.parse(jsonMatch[0]); } catch {}
    }
  }

  if (!parsed || !Array.isArray(parsed.projects) || parsed.projects.length === 0) {
    log?.(`tier3 cluster: LLM response unparseable; falling back to one mechanical rollup`);
    const r = await store.rollupTier3Tasks(tier3.map((t) => t.id));
    if (r) emitChat?.({ type: 'task-rollup', parentTaskId: r.id, childTaskIds: r.childIds, userRequest: r.userRequest, summary: r.summary });
    return 1;
  }

  // Build set of valid IDs (the ones we asked about); ignore any hallucinated IDs.
  const validIds = new Set(tier3.map((t) => t.id));
  let createdCount = 0;
  const claimed = new Set();
  for (const p of parsed.projects) {
    if (typeof p?.userRequest !== 'string' || typeof p?.summary !== 'string') continue;
    if (!Array.isArray(p?.taskIds)) continue;
    const ids = p.taskIds.filter((id) => validIds.has(id) && !claimed.has(id));
    if (ids.length === 0) continue;
    const rollup = await store.rollupTier3Tasks(ids, { userRequest: p.userRequest, summary: p.summary });
    if (rollup) {
      createdCount++;
      for (const id of ids) claimed.add(id);
      emitChat?.({ type: 'task-rollup', parentTaskId: rollup.id, childTaskIds: rollup.childIds, userRequest: rollup.userRequest, summary: rollup.summary });
    }
  }

  // Sweep up any unclaimed task IDs into one final mechanical rollup so nothing is left orphaned.
  const orphans = [...validIds].filter((id) => !claimed.has(id));
  if (orphans.length >= 2) {
    const r = await store.rollupTier3Tasks(orphans);
    if (r) emitChat?.({ type: 'task-rollup', parentTaskId: r.id, childTaskIds: r.childIds, userRequest: r.userRequest, summary: r.summary });
    createdCount++;
  }
  log?.(`tier3 cluster: created ${createdCount} rollups covering ${claimed.size + Math.max(0, orphans.length)} tasks`);
  return createdCount;
}

/**
 * Compact when total context exceeds the high threshold. Bring it down to
 * the low threshold in one pass:
 *   1. T1→T2 (mechanical mask) until under LOW or no more candidates
 *   2. T2→T3 (mechanical drop messages) until under LOW or no candidates
 *   3. If still over and tier-3 is at or above its allocation: ONE LLM call
 *      to cluster all tier-3 tasks into tier-4 rollups
 *   4. T4→archive (mechanical flag flip) for oldest if still over
 */
async function maybeCompact({ store, adapter, model, log, emitChat }) {
  const HIGH = TOTAL_BUDGET * HYST_HIGH;
  const LOW  = TOTAL_BUDGET * HYST_LOW;

  // Trigger on the TRUE prompt size (incl. the current task), but drive the
  // demotion loop on the demotable total (excl. current — it can't be masked
  // while in progress). This makes compaction fire on a long single task and
  // reclaim every finished task, instead of never firing at all.
  // demotableTotal (totalContextTokens) and currentTaskTokens are DISJOINT by
  // construction — tier1TokensFor skips state.currentId — so adding them is the
  // true, non-double-counted prompt size. The context-fill % the UI shows is
  // triggerTotal / TOTAL_BUDGET; keep these two sets disjoint if either is edited.
  const demotableTotal = await totalContextTokens(store);
  const currentTotal = await currentTaskTokens(store);
  const triggerTotal = demotableTotal + currentTotal;
  // Compact task-store breakdown for the Snapshot (catalog "Task store
  // breakdown"). The server can't read the task store itself without blocking
  // sync I/O (it lives on disk, owned by the agent), so the agent emits it here
  // — once per turn, from the same place compaction already runs. One lightweight
  // listTasks pass (metadata only, cached): per-tier counts + the current vs
  // finished token split, so prod triage can see a runaway open task, how many
  // finished tasks sit at each tier, and how close compaction is.
  let taskStats = null;
  try {
    const tierCounts = { 1: 0, 2: 0, 3: 0, 4: 0 };
    let totalTasks = 0;
    for (const t of await store.listTasks()) {
      if (t.rolledUpInto) continue;          // covered by its tier-4 rollup
      totalTasks++;
      if (tierCounts[t.tier] != null) tierCounts[t.tier]++;
    }
    taskStats = {
      currentId: store.currentId(),
      currentTokens: currentTotal,           // the OPEN task's (elided) prompt size
      demotableTokens: demotableTotal,       // finished tasks still in context
      perTaskCeiling: Math.round(TASK_FORCE * TIER_1_BUDGET),  // auto-close threshold
      tierCounts,
      totalTasks,
    };
  } catch { /* breakdown is best-effort; never block the gauge */ }
  // Report context-window fill to the UI EVERY turn (this runs once per turn),
  // not just when compaction fires, so the developer always sees how full the
  // window is. budgetPct is the binding constraint: total context vs TOTAL_BUDGET.
  emitChat?.({
    type: 'context-fill',
    totalTokens: triggerTotal,
    budgetTokens: TOTAL_BUDGET,
    budgetPct: Math.min(100, Math.round((triggerTotal / TOTAL_BUDGET) * 100)),
    taskStats,
  });
  if (triggerTotal <= HIGH) return;
  let total = demotableTotal;

  // Surface compaction as a VISIBLE event — it is a quality cliff (old tasks get
  // masked/dropped), not a silent housekeeping step.
  emitChat?.({
    type: 'compacting-started',
    totalTokens: triggerTotal,
    budgetTokens: TOTAL_BUDGET,
    budgetPct: Math.min(100, Math.round((triggerTotal / TOTAL_BUDGET) * 100)),
  });
  log?.(`compact: trigger=${triggerTotal} (demotable=${demotableTotal}, current=${triggerTotal - demotableTotal}) > HIGH=${HIGH}; target LOW=${LOW}`);
  if (total <= LOW) {
    log?.(`compact: current task alone exceeds budget; demotable context already minimal (no finished tasks to reclaim)`);
  }

  // Step 1: T1 → T2 mask (cheap). Emit task-tier-change so UI can mute results.
  while (total > LOW) {
    const id = await store.findOldestDemotionCandidate(1);
    if (!id) break;
    const before = await loadTaskFull(store, id);
    let removed = 0;
    for (const m of before?.messages ?? []) if (m.kind === 'tool-result') removed += estTokens(m.content);
    await store.demoteToTier2(id);
    emitChat?.({ type: 'task-tier-change', taskId: id, newTier: 2, muted: 'results' });
    total -= removed;
  }
  if (total <= LOW) { log?.(`compact: done after T1→T2 (total=${total})`); return; }

  // Step 2: T2 → T3 drop (cheap). Emit task-tier-change so UI can mute all rows.
  // Summarize the oldest finished tasks unconditionally — even small ones save a
  // little, and once compaction is firing a small saving beats none. (Effective
  // compaction comes from tasks being BIG in the first place — the close gate —
  // not from skipping small ones here.)
  while (total > LOW) {
    const id = await store.findOldestDemotionCandidate(2);
    if (!id) break;
    const before = await loadTaskFull(store, id);
    let removedMsgs = 0;
    for (const m of before?.messages ?? []) removedMsgs += estTokens(m.content);
    const addedSummary = estTokens(before?.summary ?? '');
    await store.demoteToTier3(id, before?.summary || '(no summary)');
    emitChat?.({ type: 'task-tier-change', taskId: id, newTier: 3, muted: 'all' });
    total -= (removedMsgs - addedSummary);
  }
  if (total <= LOW) { log?.(`compact: done after T2→T3 (total=${total})`); return; }

  // Step 3: T3 → T4 via LLM clustering (expensive — only if mechanical didn't suffice)
  const t3 = await tier3TokensFor(store);
  if (t3 >= TIER_3_BUDGET) {
    log?.(`compact: tier3=${t3} >= ${TIER_3_BUDGET}; invoking LLM cluster`);
    await llmClusterTier3({ store, adapter, model, log, emitChat });
    total = await totalContextTokens(store);
    log?.(`compact: after LLM cluster total=${total}`);
  }
  if (total <= LOW) { log?.(`compact: done after T3→T4 (total=${total})`); return; }

  // Step 4: T4 → archive (mechanical). Emit task-archived so UI can mark/hide it.
  while (total > LOW) {
    const tier4 = (await store.listTasks({ tier: 4 }))
      .filter((x) => !x.archived)
      .sort((a, b) => (a.ended ?? a.started ?? 0) - (b.ended ?? b.started ?? 0));
    if (!tier4.length) break;
    const oldest = tier4[0];
    const removed = estTokens(oldest.summary);
    await store.archiveTask(oldest.id);
    emitChat?.({ type: 'task-archived', taskId: oldest.id });
    total -= removed;
  }
  log?.(`compact: done (total=${total}, target=${LOW})`);
}

// ── Stream-aware turn (prose forwarding) ───────────────────────────────────
const HOLDBACK = '<remote-workspace>'.length + 2;

/**
 * Stream the model's response while live-tracking prose/block boundaries.
 *
 * The state machine has two modes:
 *   PROSE  — accumulating text until we see <remote-workspace
 *   BLOCK  — inside a <remote-workspace>…</remote-workspace>; suppress chunks
 *
 * Callbacks (any can be omitted):
 *   onProseChunk({ segmentIndex, content })  — fresh prose bytes for live UI
 *   onBlockReady ({ segmentIndex, body })    — a closed tool block; fire its
 *                                              tools NOW, in parallel with
 *                                              later blocks if you like
 *
 * The HOLDBACK at the tail of the buffer means we wait for the next chunk
 * before emitting the last few bytes, so a partial "<remote-w" never leaks
 * to the UI as visible text and then has to be retracted.
 *
 * Returns the full text on completion. The agent loop re-parses with
 * parseResponseSegments to assemble the canonical segment list for storage
 * and any post-stream work — that pass owns the hallucinated-result-tag
 * defense, so this streamer can stay simple.
 */
async function runTurnStreaming(adapter, opts, { onProseChunk, onProseSegmentEnd, onBlockReady, onUsage, suppressTags = [] } = {}) {
  let text = '';
  let usage = null;   // captured from the final stream chunk / the complete fallback (token counts)
  let state = 'prose';
  let segmentIndex = 0;
  let proseStart = 0;       // start of the current prose segment (for content slicing on end)
  let proseSent = 0;        // chars already streamed via onProseChunk
  let blockBodyStart = -1;

  // Case-insensitive search on the ORIGINAL text. Do NOT use
  // text.toLowerCase().indexOf(...): some characters (U+0130 'İ', 'ß', ligatures)
  // change LENGTH under toLowerCase, so an index found in the lowercased copy
  // is offset from the same position in the original — slicing the original at
  // that shifted index leaks tag fragments into the prose and corrupts the
  // stored message. Regex .index is computed against the original string.
  const findFrom = (re, from) => { re.lastIndex = from; const m = re.exec(text); return m ? m.index : -1; };
  const OPEN_RE = /<remote-workspace/gi;
  const CLOSE_RE = /<\/remote-workspace>/gi;
  // Tag spans suppressed from the prose STREAM — SIGNALS, not display text. The
  // model emits one inline and the final prose strips it; suppressing it from the
  // stream too keeps the live bubble from ballooning to the raw multi-line tag and
  // snapping down on finalize. <task-progress> always (the worker's per-turn
  // signal); callers add more via suppressTags (e.g. the intake's <requirement-card>,
  // which is parsed into the form, not shown as streamed prose). proseStart is not
  // moved, so the sanitized final segment is unaffected.
  const SUPPRESS = ['task-progress', 'checklist', 'rollback', 'tick', ...(suppressTags || [])].map((t) => ({
    open: new RegExp('<' + t, 'gi'), close: new RegExp('</' + t + '>', 'gi'), closeLen: t.length + 3,
  }));
  const pump = () => {
    while (true) {
      if (state === 'prose') {
        const openIdx = findFrom(OPEN_RE, proseSent);
        // Earliest suppressed-tag open at/after proseSent.
        let supIdx = -1, sup = null;
        for (const s of SUPPRESS) { const i = findFrom(s.open, proseSent); if (i !== -1 && (supIdx === -1 || i < supIdx)) { supIdx = i; sup = s; } }
        if (supIdx !== -1 && (openIdx === -1 || supIdx < openIdx)) {
          if (supIdx > proseSent) {
            onProseChunk?.({ segmentIndex, content: text.slice(proseSent, supIdx) });
            proseSent = supIdx;
          }
          const tagCloseIdx = text.indexOf('>', supIdx);
          if (tagCloseIdx === -1) return;                            // opening tag still arriving — hold back
          if (text[tagCloseIdx - 1] === '/') { proseSent = tagCloseIdx + 1; continue; }  // self-closing <tag …/>
          const endIdx = findFrom(sup.close, tagCloseIdx + 1);
          if (endIdx === -1) return;                                 // close not here yet — emit nothing
          proseSent = endIdx + sup.closeLen;                         // skip the span; no emit, no segment break
          continue;
        }
        if (openIdx === -1) {
          // No open tag yet — emit prose up to HOLDBACK before the tail so
          // a partial tag mid-stream never appears in the UI.
          const flushTo = Math.max(proseSent, text.length - HOLDBACK);
          if (flushTo > proseSent) {
            onProseChunk?.({ segmentIndex, content: text.slice(proseSent, flushTo) });
            proseSent = flushTo;
          }
          return;
        }
        if (openIdx > proseSent) {
          onProseChunk?.({ segmentIndex, content: text.slice(proseSent, openIdx) });
          proseSent = openIdx;
        }
        // We have a <remote-workspace — make sure the full opening tag has arrived
        const tagCloseIdx = text.indexOf('>', openIdx);
        if (tagCloseIdx === -1) return;
        const opener = text.slice(openIdx, tagCloseIdx + 1);
        // Skip hallucinated result tags — the post-stream parser drops them.
        if (/<remote-workspace-result|<workspace-result/i.test(opener)) {
          // Flush the current prose segment (whatever the model said before
          // its hallucinated result) then suppress further chunks.
          const proseContent = text.slice(proseStart, openIdx);
          if (proseContent.length) onProseSegmentEnd?.({ segmentIndex, content: proseContent });
          state = 'block';
          blockBodyStart = Number.MAX_SAFE_INTEGER;
          proseSent = tagCloseIdx + 1;
          return;
        }
        // Real <remote-workspace …> — close out the current prose segment
        // (so the UI gets its sanitized final-version event in order),
        // then transition into block state.
        const proseContent = text.slice(proseStart, openIdx);
        onProseSegmentEnd?.({ segmentIndex, content: proseContent });
        segmentIndex++;
        state = 'block';
        blockBodyStart = tagCloseIdx + 1;
        proseSent = tagCloseIdx + 1;
        continue;
      }
      // state === 'block'
      const closeIdx = findFrom(CLOSE_RE, blockBodyStart);
      if (closeIdx === -1) return;
      const body = text.slice(blockBodyStart, closeIdx);
      onBlockReady?.({ segmentIndex, body });
      segmentIndex++;
      const endTag = '</remote-workspace>';
      state = 'prose';
      proseSent = closeIdx + endTag.length;
      proseStart = proseSent;
      blockBodyStart = -1;
    }
  };

  let aborted = false;
  if (typeof adapter.completeStream === 'function') {
    for await (const chunk of adapter.completeStream(opts)) {
      // Bail IMMEDIATELY on cancel — don't keep draining the stream if the upstream proxy
      // ignores the client abort and keeps sending (that was the 10-20s "cancel lag"). BREAK
      // (don't throw here) so the final flush below still closes the streaming prose segment —
      // throwing mid-stream left an orphan "streaming" bubble in the UI.
      if (opts.signal?.aborted) { aborted = true; break; }
      const d = chunk?.choices?.[0]?.delta?.content;
      if (d) { text += d; pump(); }
      if (chunk?.usage) usage = chunk.usage;   // many APIs send usage on the final chunk
    }
  } else {
    const res = await adapter.complete(opts);
    text = res?.choices?.[0]?.message?.content ?? '';
    usage = res?.usage ?? null;
    pump();
  }
  if (onUsage) { try { await onUsage(usage); } catch {} }
  // Final flush: any trailing prose still inside HOLDBACK gets emitted,
  // and the final prose segment is closed out so the UI can finalize it.
  if (state === 'prose') {
    if (proseSent < text.length) {
      onProseChunk?.({ segmentIndex, content: text.slice(proseSent) });
      proseSent = text.length;
    }
    const finalProse = text.slice(proseStart);
    if (finalProse.length) onProseSegmentEnd?.({ segmentIndex, content: finalProse });
  }
  // Cancelled mid-stream: the segment is now closed (flush above), so no orphan bubble — NOW bail.
  if (aborted) { const e = new Error('aborted'); e.name = 'AbortError'; throw e; }
  return text;
}

// ── Task-op dispatcher ─────────────────────────────────────────────────────
// A task is RETRIEVABLE (its detail can be pulled back via <task-detail>) ONLY
// once its messages have been COMPACTED OUT of the live context: tier >= 3
// (demoteToTier3 drops the messages, leaving only a summary + an ID in the
// <task-history> block), or it was rolled into a tier-4 project, or archived.
// tier 1/2 tasks are still rendered verbatim in the prompt — their content is
// already present and they expose NO id — so they are not retrievable.
function isRetrievableTask(t, currentId) {
  if (!t || t.id === currentId) return false;
  if (t.archived || t.rolledUpInto) return true;
  return (t.tier ?? 1) >= 3;
}
// A task with no developer request and no work in it — nothing to summarize.
// Closing one is the infinite "open empty task -> close it -> repeat" churn.
function isEmptyTask(t) {
  const msgs = t?.messages ?? [];
  if (msgs.length === 0) return true;
  return !msgs.some((m) => m.kind === 'user-message' || m.kind === 'tool-call' || m.kind === 'tool-result');
}

async function dispatchTaskOp(c, store, opts = {}) {
  const a = c.args;
  if (c.verb === 'next-task') {
    // Refuse to close an EMPTY task (no request, no work). Closing one produced
    // an infinite churn: <next-task> opens a fresh empty task, the model looks
    // back at the prior (in-context) task, decides it is "waiting for
    // instructions", and closes the empty task again — forever (and hammered the
    // task store, which is how the EBUSY crash surfaced). didRealWork lets a
    // same-turn read-then-close through: that turn's tool results are not stored
    // yet at dispatch time, so isEmptyTask alone would mis-flag a worked task.
    const cur = await store.getCurrent();
    if (isEmptyTask(cur) && !opts.didRealWork) {
      return {
        ok: false,
        emptyClose: true,
        content: 'Nothing to close — this task has no developer request and no work in it. Do NOT emit <next-task> on an empty task. If you have finished everything the developer asked, reply with a short message and NO tool calls to wait for their next instruction.',
      };
    }
    // MINIMUM-size gate: keep a small task OPEN so several related exchanges bundle
    // into one (a one-exchange task's summary saves almost nothing). A model close
    // of a sub-MIN task is refused UNLESS it is over budget (opts.overBudget) or the
    // model marks the next work as a different topic (new-topic="true"). tooSmall is
    // ok:false so it skips ALL close side effects and does NOT feed the churn breaker
    // — it is the system asking the model to keep working, not a spurious close. The
    // deterministic over-budget close runs through Layer-1 auto-close (transitionTo
    // directly), which is NOT gated, so a task can never get stuck below MIN.
    if (opts.enforceMinTaskSize && MIN_TASK_TOKENS > 0 && !opts.overBudget && a['new-topic'] !== 'true' && !isEmptyTask(cur)) {
      let curTokens = 0;
      for (const m of cur.messages ?? []) curTokens += estTokens(m.content);
      if (curTokens < MIN_TASK_TOKENS) {
        return {
          ok: false,
          tooSmall: true,
          content: `This task is only ~${curTokens} tokens; keep working in it (it should reach ~${MIN_TASK_TOKENS} before closing, so its summary is worth keeping). Emit <task-progress percent="N"> this turn instead of <next-task>. If your NEXT work is genuinely unrelated to what this task has covered, close now by adding new-topic="true" to the <next-task> tag.`,
        };
      }
    }
    // user-request and summary describe the task being CLOSED (the current one),
    // not the new one being opened. See system prompt.
    const r = await store.transitionTo({
      summary: a.summary ?? '',
      userRequest: a['user-request'] ?? '',
    });
    return {
      ok: true,
      content: `closed previous task with summary "${a.summary ?? ''}"; opened new task ${r.newTask.id}`,
      closedId: r.closedId,
      newTaskId: r.newTask.id,
    };
  }
  // list-tasks stays disabled — pure navel-gazing with no legitimate gated use.
  if (c.verb === 'list-tasks') {
    return {
      ok: false,
      content: 'Listing tasks is disabled. Your current work and recent tasks are already in your context — do not enumerate tasks.',
    };
  }
  // task-detail / search-tasks are GATED to COMPACTED tasks only (the ones shown
  // as <task-history>/<task-rollup id="T-N"> summaries). A task still in context
  // is refused — read it where it already is. Use these ONLY to recover the
  // specifics of a compacted task you genuinely need for the CURRENT request;
  // never to decide what to do.
  if (c.verb === 'task-detail') {
    const id = String(a.id || '').trim();
    if (!id) return { ok: false, content: 'task-detail needs id="T-N".' };
    if (id === store.currentId()) return { ok: false, content: 'That is your CURRENT task — its content is already in your context. Do not retrieve it.' };
    const t = await loadTaskFull(store, id);
    if (!t) return { ok: false, content: `No task ${id} exists.` };
    if (!isRetrievableTask(t, store.currentId())) {
      return { ok: false, content: `${id} is still fully in your context above (its messages are present) — read it there. task-detail only pulls back a task that was compacted to a summary.` };
    }
    const msgs = (t.archivedMessages?.length ? t.archivedMessages : t.messages) ?? [];
    const head = `<task-detail id="${t.id}" tier="${t.tier ?? '?'}">\n  request: ${t.userRequest || '(none recorded)'}\n  summary: ${t.summary || '(none)'}`;
    const body = msgs.length
      ? '\n  --- recovered messages ---\n' + msgs.map((m) => `[${m.kind || m.role}] ${String(m.content || '').slice(0, 4000)}`).join('\n')
      : '\n  (no messages were preserved for this task)';
    return { ok: true, content: `${head}${body}\n</task-detail>` };
  }
  if (c.verb === 'search-tasks') {
    const query = String(a.query || '').trim();
    const hits = await store.searchTasks(query);
    const curId = store.currentId();
    const keep = [];
    for (const h of hits) {
      const t = await loadTaskFull(store, h.id);
      if (isRetrievableTask(t, curId)) keep.push(h);
    }
    if (!keep.length) {
      return { ok: true, content: 'No compacted tasks matched. Any matching recent tasks are still in your context above — read them there.' };
    }
    return { ok: true, content: keep.map((h) => `<task-history id="${h.id}" tier="${h.tier ?? '?'}">${h.summary || '(no summary)'}</task-history>`).join('\n') };
  }
  return { ok: false, content: `unknown task op: ${c.verb}` };
}

// ── Shared streaming engine: run ONE model turn ─────────────────────────────
// The streaming core every chat shares (worker + mini-chats). It streams the
// model's reply, emits each <remote-workspace> block's tool-calls the moment the
// block closes (mid-turn, in segment order), schedules their execution (parallel,
// with a serial barrier for task-op / mutating blocks so a <next-task> sees prior
// writes and approval prompts never overlap), waits for all of them, and returns
// the tool results in canonical SEGMENT order (not wall-clock order). The PER-CALL
// execution is INJECTED via runCall(call, perBlockState) → result, so the worker
// runs task-ops + writes while a mini-chat runs read-only — both get IDENTICAL
// streaming, ordering, and <task-progress> handling. There is exactly one of these.
//
// Returns { text, cleanText, segments, orderedResults, progressMatch, hasProgressSignal }.
async function runStreamingTurn({
  adapter, model, convo, signal, extraVerbs = {}, serialVerbs = MUTATING_VERBS, suppressTags = [],
  onProseChunk, onProseSegmentEnd, onToolCall, onUsage, runCall,
}) {
  const blockResultsByIdx = new Map();
  const blockPromisesByIdx = new Map();
  const allBlockPromises = [];
  let serialBarrier = Promise.resolve();   // resolves after the last serial (task-op/mutating) block
  const handleBlockReady = ({ segmentIndex, body }) => {
    if (signal?.aborted) return;   // cancelled mid-stream — don't dispatch a block that just closed
    const calls = parseToolCalls(`<remote-workspace>${body}</remote-workspace>`, { extraVerbs });
    for (const c of calls) onToolCall?.(c);
    // A task-op or mutating block must run AFTER all prior blocks (so it sees their
    // effects + never races the single approval slot); a normal block waits only
    // for the last serial block, otherwise overlapping with sibling read/write blocks.
    const needsSerial = calls.some((c) => c.kind === 'task-op' || serialVerbs.has(c.verb));
    const startAfter = needsSerial ? Promise.allSettled(allBlockPromises.slice()) : serialBarrier;
    const perBlockState = { lastKey: null, repeat: 0 };
    const p = startAfter.then(async () => {
      const out = [];
      for (const c of calls) out.push(await runCall(c, perBlockState));
      return out;
    }).then((r) => { blockResultsByIdx.set(segmentIndex, r); return r; });
    p.catch(() => {});   // handler so a mid-stream abort isn't an unhandled rejection
    blockPromisesByIdx.set(segmentIndex, p);
    allBlockPromises.push(p);
    if (needsSerial) serialBarrier = p;
  };
  const text = await runTurnStreaming(
    adapter, { model, messages: convo, signal },
    { onProseChunk, onProseSegmentEnd, onBlockReady: handleBlockReady, onUsage, suppressTags },
  );
  // LAYER 3 signal: pull the per-turn <task-progress> out + strip it before parsing.
  const progressMatch = text.match(/<task-progress\b[^>]*\bpercent\s*=\s*["']?(\d{1,3})["']?[^>]*>([\s\S]*?)<\/task-progress>/i);
  // If the model emits more than one <checklist> in a turn (e.g. a draft then a corrected one),
  // the LAST one is the current state — take it, not the first.
  const _clAll = [...text.matchAll(/<checklist\b[^>]*>([\s\S]*?)<\/checklist>/gi)];
  const checklistMatch = _clAll.length ? _clAll[_clAll.length - 1] : null;
  // <rollback reason="…">…</rollback> (or self-closing): the agent suggests reverting the last run.
  const rollbackReason = /<rollback\b/i.test(text)
    ? ((text.match(/<rollback\b[^>]*\breason\s*=\s*["']([^"']*)["']/i) || [])[1]
       || ((text.match(/<rollback\b[^>]*>([\s\S]*?)<\/rollback>/i) || [])[1] || '').replace(/<[^>]+>/g, '').replace(/\s+/g, ' ').trim()
       || 'the last run\'s changes look wrong')
    : null;
  // <tick>N</tick> (or <tick>1,3</tick>): mark items done by their number in the active checklist.
  const tickIndices = [...text.matchAll(/<tick\b[^>]*>([\s\S]*?)<\/tick>/gi)]
    .flatMap((m) => String(m[1] || '').split(/[,\s]+/).map((s) => parseInt(s, 10)).filter((n) => Number.isInteger(n) && n > 0));
  const cleanText = text.replace(/<task-progress\b[\s\S]*?<\/task-progress>/gi, '').replace(/<task-progress\b[^>]*\/>/gi, '').replace(/<checklist\b[\s\S]*?<\/checklist>/gi, '').replace(/<rollback\b[\s\S]*?<\/rollback>/gi, '').replace(/<rollback\b[^>]*\/>/gi, '').replace(/<tick\b[\s\S]*?<\/tick>/gi, '').replace(/<tick\b[^>]*\/>/gi, '');
  const segments = parseResponseSegments(cleanText, { extraVerbs });
  // Wait for every block's tools to finish (their tool-result events already fired
  // inside runCall), then gather results in canonical segment order.
  await Promise.allSettled([...blockPromisesByIdx.values()]);
  const orderedResults = [];
  for (let i = 0; i < segments.length; i++) {
    if (segments[i].type !== 'tool-block') continue;
    for (const r of (blockResultsByIdx.get(i) || [])) if (r) orderedResults.push(r);   // runCall may return null (e.g. a read-only pass dropping a disallowed verb)
  }
  return { text, cleanText, segments, orderedResults, progressMatch, checklistMatch, tickIndices, rollbackReason, hasProgressSignal: !!progressMatch };
}

// ── deep_think: the agent-invocable design tool ─────────────────────────────
// When the worker faces a genuine design/approach decision it can call
// <deep_think>question</deep_think>: THREE independent proposers — one per lens
// below, each blind to the others, read-only over the actual repo — fan out in
// parallel, then a judge picks/synthesizes the recommended approach. The
// machinery is runProposer/runExploreJudge (the same primitives the cognition
// scheduler's explore step uses). OFF by default — enabled per run via the
// `deepThink` option (the UI chip) or the CB_DEEP_THINK env opt-in. Rate-
// limited by PROMPT VISIBILITY: the tool's prompt section appears ONLY on
// turns where it is available; off / on cooldown / spent / discuss it is
// absent entirely (see buildSystemPrompt + the dispatch branch in
// runPromptedAgent).
//
// DEEP_THINK_LENSES is a LOCAL COPY of COGNITION_LENSES (src/server.mjs) —
// duplicated on purpose: prompted.mjs must not import from server.mjs, and the
// dist bundle is one flat module scope, so this name must be (and is) globally
// unique. Keep the two lists in sync.
const DEEP_THINK_LENSES = [
  'favor the simplest thing that works',
  'favor long-term extensibility and clean design',
  'favor minimal risk to existing behavior',
];

// ── Main agent loop ────────────────────────────────────────────────────────
async function runPromptedAgent({
  adapter,
  messages,           // [{role, content}] from the browser; we only care about the last user message
  model,
  projectDir,         // required: where .code_boss/tasks/ lives
  maxTurns = DEFAULT_MAX_TURNS,
  parallelCritic,            // adversarial-critic UI toggle (off by default); undefined → CB_PARALLEL_CRITIC env
  deepThink,                 // deep_think UI toggle (off by default); undefined → CB_DEEP_THINK env (opt-in)
  // Legacy callbacks (used by /api/chat SSE response):
  onText, onToolCalls, onToolResult, onLog,
  // New chat-event callback (used by /api/message + chat state on server):
  //   onChatEvent({ type, taskId, ...payload })
  // emitted for: 'user-message', 'assistant-text', 'tool-call', 'tool-result',
  //              'diff', 'task-end'.
  onChatEvent,
  // New phase callback:
  //   onPhase('idle' | 'uploading' | 'thinking' | 'downloading' | 'reviewing')
  onPhase,
  // Abort signal — checked between turns and when waiting on the model.
  signal,
  // When the reviewer feeds back to the agent, the user-facing review
  // event already shows the feedback — set this to false to skip the
  // duplicate 'user-message' event. The store still gets the message so
  // the LLM sees it as input.
  emitInitialUserMessage = true,
  // Plugin integration. Manager passes these so the agent's prompt + tool
  // dispatch + parser pick up enabled plugins' contributions.
  extraTools = {},          // { tool_name: { verb, name, schema, impl } }
  extraVerbs = {},          // { verb: tool_name } — fed to parseToolCalls
  promptAdditions = [],     // strings appended to system prompt
  projectContext = '',      // formatted AGENTS.md context for this project
  codebaseMemory = '',      // rendered Tier-1 codebase memory (agent-maintained)
  // Hook fired right after a <next-task> task-op transitions the store.
  // Receives { closedTaskId, newTaskId, userRequest, summary } and may
  // return a commit hash string (or null). The hash is attached to the
  // closed task's record and included in the task-end chat event.
  onTaskClose,
  // Pre-write hook handed to tools that mutate files (write_file,
  // edit_file). Called as requireBranch(absPath) before the write,
  // resolves to 'ok' to proceed or 'aborted' to cancel. The server's
  // implementation pauses until the user resolves a CODE_BOSS-branch
  // prompt for the file's containing repo.
  requireBranch,
  // Developer-approval gate handed to tools that need explicit human OK
  // (e.g. edit_docx flatten). Called as requireApproval({kind, message}),
  // resolves to true (approved) / false (denied or aborted). The server
  // pauses and shows an Approve/Deny modal; absent in non-interactive runs.
  requireApproval,
  // Returns { user, token } for plugins that need to hit the configured
  // GitLab API (e.g. wl's documentation fetchers). Token never reaches
  // the model — the plugin's impl calls the GitLab client and only the
  // result content goes into the chat. Optional; plugins handle the
  // missing case themselves.
  getGitLabCredentials,
  // Read-only "Discuss" mode: when true, the worker may read/search/list and
  // use codebase memory + task-ops, but write/edit/powershell (and any other
  // non-read tool) are refused. The prompt also drops the write/edit/shell
  // docs (see buildSystemPrompt). Lets the developer plan/ask safely without
  // the agent touching the project.
  discussMode = false,
  // REVIEW (propose-first) mode: read-only like discuss, but the prompt asks for a PROPOSAL
  // (summary + checklist + diffs) the developer approves before implementing. Shares the
  // read-only tool gate; gets a different system prompt.
  reviewMode = false,
  // Getter for the active checklist (the latest <checklist> items, or null) — pinned into the
  // system prompt each turn so the agent reliably maintains it (the tag is stripped from history).
  getActiveChecklist = null,
  // Mid-run "steer": a function that returns (and clears) any user messages
  // queued via the steer channel while this run was in flight. Drained at the
  // TOP of each turn — the queued text becomes a fresh user message for the
  // next turn, so the developer can redirect mid-run without cancelling (Esc)
  // or waiting for a full stop (the queue). Returns an array of strings.
  drainSteer,
  // WebLogic server status, injected into the system reminder each turn so the
  // agent knows whether the server is up (true=running, false=stopped, null=
  // unknown/not monitored). A function is re-evaluated per turn (the poll may
  // flip mid-run, e.g. after `wl startWebLogic` comes up).
  weblogicStatus = null,
  // Enforce the minimum-task-size CLOSE gate (MIN_TASK_TOKENS) for THIS run.
  // ON only for free conversational turns (the main thread the developer chats
  // in), where one-exchange-per-task fragmentation is the problem. OFF for
  // assignment-worker / reviewer / intake / all direct test callers, where a
  // task closes on completion as a lifecycle SIGNAL regardless of size — gating
  // those would stall review/acceptance/commit.
  enforceMinTaskSize = false,
  // Optional shared tasks-v2 store. Default null → create + init a private one (today's
  // byte-identical path). When the server is in orchestrator mode it passes the SINGLE
  // long-lived session.store so the foreground worker + the sub-agent import share ONE
  // instance (one nextId, one in-memory state) — eliminating the two-instance task-id
  // collision + state.json lost-update on the same .code_boss.
  store: providedStore = null,
}) {
  if (!projectDir) throw new Error('projectDir required for task-scoped agent');

  const log = (m) => { try { onLog?.(m); } catch {} };
  // Returns the host's recorded event (with its id) so callers that need to
  // re-tag an event later (e.g. message-refile) can capture it.
  const emitChat = (ev) => { try { return onChatEvent?.(ev); } catch (e) { log(`onChatEvent error: ${e.message}`); return undefined; } };
  const emitPhase = (p) => { try { onPhase?.(p); } catch {} };
  const checkAbort = () => {
    if (signal?.aborted) {
      const e = new Error('aborted');
      e.name = 'AbortError';
      throw e;
    }
  };

  // Initialize tasks store; this either loads existing state or creates a root task.
  // A shared instance (providedStore) is already inited + must NOT be re-inited (that would
  // reload from disk + discard a concurrent import's in-memory state).
  const store = providedStore || createTasksStore(projectDir);
  if (!providedStore) await store.init();

  // Extract the user's new message (last user role in incoming).
  const lastUserMsg = [...(messages ?? [])].reverse().find((m) => m.role === 'user');
  const userText = flattenContent(lastUserMsg?.content);
  // Optional attached images (data URIs) on the seeded user message. They ride
  // the stored message and reach the adapter via buildConvoFromStore; the
  // adapter decides the wire shape (see applyImageWireFormat, openai-http.mjs).
  const userImages = Array.isArray(lastUserMsg?.images)
    ? lastUserMsg.images.filter((u) => typeof u === 'string' && u.startsWith('data:image/'))
    : [];
  // Track the just-arrived user message M so we can RE-ATTRIBUTE it if the task
  // it landed in gets closed before any work is done for it (see
  // maybeRelocateLeadingUserMsg). leadingUserMsg.taskId follows M as it moves.
  let leadingUserMsg = null;
  if (userText) {
    await store.appendMessage({
      kind: 'user-message',
      role: 'user',
      content: userText,
      ...(userImages.length ? { images: userImages } : {}),
    });
    const mTaskId = store.currentId();
    // Capture M's exact stored message object so it can be relocated by identity
    // later (by relocation time the turn's assistant prose has been appended
    // after it, so it is no longer the trailing message).
    const curTask = await store.getCurrent();
    const mRef = curTask?.messages?.[curTask.messages.length - 1] ?? null;
    let mEventId = null;
    if (emitInitialUserMessage) {
      // Only a COUNT rides the chat event (not the data URIs) — events persist
      // to conversation.json and broadcast over SSE, so keep them light.
      const ev = emitChat({ type: 'user-message', taskId: mTaskId, content: userText, ...(userImages.length ? { imageCount: userImages.length } : {}) });
      mEventId = ev?.id ?? null;
    }
    leadingUserMsg = { taskId: mTaskId, eventId: mEventId, ref: mRef, anchored: false };
  }

  const stats = { turns: 0, toolCalls: 0, toolHistogram: {}, batches: [], loopBreaks: 0, taskOps: 0, autoClosed: 0 };

  // Task-boundary attribution. If the just-arrived user message M is sitting in
  // a task that gets CLOSED before any work is done for it — the model closes
  // the prior unit as its first action, or Layer-1 auto-close fires at turn top
  // — then M belongs to the NEW task, not the closed one. Move M's stored record
  // into the new task AND re-tag its persisted chat event (a 'message-refile',
  // which the host turns into a taskId patch) so the live UI and a reload both
  // file M under the new task. The OPEN task gets no wrap, so M then renders flat
  // below the closed task's box — exactly where a new request belongs. Called
  // BEFORE the task-end event so the UI re-tags M before wrapping the closed task.
  //
  // ANCHORING: once any real work runs in M's task (this turn OR a previous turn
  // of the run), M is "anchored" — that task genuinely handled M, so a later
  // close (model <next-task> on a subsequent turn, or Layer-1 auto-close of a
  // long single task) must NOT rip M out of it. Without this, a work-in-turn-1 /
  // close-in-turn-2 flow, or auto-close after a long worked run, mis-filed M.
  async function maybeRelocateLeadingUserMsg(closedId, newTaskId) {
    if (!leadingUserMsg || leadingUserMsg.anchored || leadingUserMsg.taskId !== closedId || !newTaskId) return;
    try {
      const moved = await store.relocateMessageRef(closedId, newTaskId, leadingUserMsg.ref);
      if (moved) {
        if (leadingUserMsg.eventId) {
          emitChat({ type: 'message-refile', eventId: leadingUserMsg.eventId, toTaskId: newTaskId });
        }
        leadingUserMsg.taskId = newTaskId;
        // Anchor M to the task we just moved it into. It has now been attributed
        // to a real task; without this it would keep satisfying the relocation
        // condition every turn and ride the task chain forward (T-1→T-2→T-3…)
        // forever — the infinite next-task loop. A single relocation off a
        // closed-before-work task is the only legitimate move; anything past
        // that is the churn we are killing.
        leadingUserMsg.anchored = true;
        log(`attribution: moved leading user message from ${closedId} to ${newTaskId}`);
      }
    } catch (e) { log(`relocate leading user msg error: ${e.message}`); }
  }

  // LAYER 1 — deterministic auto-close. A model that won't emit <next-task>
  // (the soft budget nudge in the prompt is ignored) grows one task past the
  // window. When the OPEN task crosses TASK_FORCE × the tier-1 budget, the
  // SYSTEM closes it: one LLM call summarizes it (so the closed task degrades
  // gracefully through the existing tier/project compaction — not crude
  // elision), then a fresh task opens. Works regardless of the model.
  async function autoCloseCurrentTask(curTask) {
    let summary = '';
    try { summary = await runSummarizer({ adapter, model, task: curTask, signal, onLog: log }); } catch { /* */ }
    if (!summary) summary = `auto-closed at budget (${curTask?.messages?.length || 0} messages of accumulated work)`;
    const userRequest = curTask?.userRequest || '';
    const r = await store.transitionTo({ summary, userRequest });
    stats.autoClosed++;
    log(`auto-close: task ${r.closedId} hit budget — closed with summary "${summary.slice(0, 80)}"; opened ${r.newTask.id}`);
    let commitHash = null, commits = null, committedRepos = null, commitError = null;
    if (typeof onTaskClose === 'function') {
      try {
        const closeRes = await onTaskClose({ closedTaskId: r.closedId, newTaskId: r.newTask.id, userRequest, summary });
        if (closeRes && typeof closeRes === 'object') {
          commitHash = closeRes.hash ?? null;
          commits = Array.isArray(closeRes.commits) ? closeRes.commits : null;
          committedRepos = Array.isArray(closeRes.committedRepos) ? closeRes.committedRepos : null;
          commitError = closeRes.gitError ?? null;
        } else commitHash = closeRes ?? null;
      } catch (e) { log(`auto-close onTaskClose error: ${e.message}`); }
    }
    if (commitHash && r.closedId) { try { await store.attachToTask(r.closedId, { commitHash }); } catch { /* */ } }
    // Auto-close fires at turn TOP, before any work for M this turn — so M (if
    // it just arrived into the now-closed task) belongs to the fresh task.
    await maybeRelocateLeadingUserMsg(r.closedId, r.newTask.id);
    emitChat({ type: 'task-end', taskId: r.closedId, userRequest, summary, commitHash, commits, committedRepos, commitError, auto: true });
    // Demote the just-closed task right away so THIS turn's prompt is bounded.
    try { await maybeCompact({ store, adapter, model, log, emitChat }); } catch { /* */ }
    return store.getCurrent();
  }

  // Run-level: consecutive turns whose ONLY action was a refused <next-task> on
  // an empty task. Breaks the open-empty-task / close-it / repeat churn that a
  // stubborn model can otherwise spin until maxTurns.
  let emptyCloseStreak = 0;
  let criticCalls = 0;   // bounded count of reviewer fires this run
  let lastTurnGotCritique = false;   // did THIS turn's input carry a reviewer finding (so it's a rework)?
  let reviewerInflight = null;       // the background reviewer promise (at most one at a time)
  const reviewerFindings = [];       // verified findings awaiting injection at the next turn boundary
  let reviewerStore = null;          // the reviewer's OWN ISOLATED, persistent store (its conversation lives here)
  const criticOn = parallelCritic ?? CB_PARALLEL_CRITIC;   // UI toggle wins; else the env default (off)
  // The reviewer is a PERSISTENT peer agent with its OWN conversation, kept in an ISOLATED store
  // (.code_boss/_reviewer — NOT the agent's task tree, which it must never touch): FORKED from the
  // agent's history the first time it fires, then FED only the agent's NEW step each turn. Its prior
  // findings + the evidence it gathered persist in its store (so it doesn't re-investigate or re-flag),
  // and the engine's own compaction keeps it bounded. Read-only tools verify against the actual code.
  const investigateForReviewer = async (cur, latestStep) => {
    try {
      let feedMsg;
      if (!reviewerStore) {
        // FORK: a fresh isolated store. Use a UNIQUE per-run subdir so the store is clean EVEN IF the
        // clear below is blocked by a held handle (a new dir name → init() starts empty, no stale bleed);
        // the best-effort rm of the parent just keeps old runs from piling up.
        const base = path.join(projectDir, '.code_boss', '_reviewer');
        try { await rm(base, { recursive: true, force: true }); } catch {}
        const dir = path.join(base, String(++reviewerStoreSeq));
        reviewerStore = createTasksStore(dir);
        await reviewerStore.init();
        let transcript = '';
        try {
          const convo = await buildConvoFromStore(store);
          transcript = (convo || []).map((m) => `[${m.role}] ${String(m.content || '').slice(0, 3000)}`).join('\n\n');
          if (transcript.length > 14000) transcript = '…(earlier truncated)…\n\n' + transcript.slice(-14000);
        } catch {}
        feedMsg = reviewerSeedPrompt(cur?.userRequest || messages?.[0]?.content || '', transcript);
      } else {
        // FEED: it already has its history in its store — just hand it the agent's latest step.
        feedMsg = reviewerFeedPrompt(latestStep);
      }
      const r = await runPromptedAgent({
        adapter, model, projectDir, signal, discussMode: true, parallelCritic: false,
        store: reviewerStore, maxTurns: TURN_REVIEWER_MAX_TURNS,
        messages: [{ role: 'user', content: feedMsg }],
        onLog: () => {}, onChatEvent: () => {},   // its internal investigation stays silent (never hits the chat)
      });
      const clean = String(r?.content ?? '').replace(/<\/?[a-z][^>]*>/gi, '').trim();
      const NO_FINDING = /^\s*(no problems? found|no improvements?|none|nothing|looks good|lgtm|n\/a)\b/i;
      if (!clean
        || /max turns reached|did not converge/i.test(clean)        // the reviewer ran out of investigation budget mid-pass — NOT a finding (the next fire continues from its store)
        || NO_FINDING.test(clean)                                   // verdict up front
        || /\bno (?:problems? found|issues?|concerns?)\b[.!\s]*$/i.test(clean)) return null;   // verdict buried at the END after reasoning (e.g. "...the diagnosis is correct. no problems found")
      return clean.slice(0, 1200);
    } catch (e) { log(`reviewer pass failed: ${e?.message ?? e}`); return null; }
  };
  // Kick a background reviewer on the just-finished turn — NON-BLOCKING (it must never stall a turn).
  // Skips if this turn was itself a rework of a finding (the rule), if one is already in flight, or
  // the budget is spent. The result lands in reviewerFindings; drainReviewer() injects it later.
  const kickReviewer = (cur, latestStep) => {
    if (!criticOn) return;
    if (lastTurnGotCritique) { lastTurnGotCritique = false; return; }   // this turn reworked a finding → don't review it
    if (reviewerInflight || criticCalls >= CRITIC_MAX || signal?.aborted) return;
    criticCalls++;
    const ownerTaskId = cur?.id;
    reviewerInflight = investigateForReviewer(cur, latestStep)
      .then((finding) => { if (finding) reviewerFindings.push({ finding, ownerTaskId }); })
      .catch((e) => log(`reviewer kick failed: ${e?.message ?? e}`))
      .finally(() => { reviewerInflight = null; });
  };
  // Inject any verified findings the background reviewer produced — as <reviewer-finding> messages the
  // agent sees THIS turn. Sets lastTurnGotCritique so the NEXT turn (the agent's response) isn't reviewed.
  const drainReviewer = async () => {
    let n = 0;
    while (reviewerFindings.length) {
      const { finding, ownerTaskId } = reviewerFindings.shift();
      const tid = ownerTaskId || store.currentId();   // file into the task that was REVIEWED — a <next-task> may have moved store.currentId() since the kick
      await store.appendMessage({ kind: 'user-message', role: 'user', content: `<reviewer-finding>\nA parallel reviewer investigated and VERIFIED this problem:\n${finding}\n\nIf it is right, fix it; otherwise explain why it's not an issue and continue.\n</reviewer-finding>` }, tid);
      emitChat({ type: 'reviewer-note', taskId: tid, content: finding });
      lastTurnGotCritique = true;
      log(`reviewer: injected verified finding`);
      n++;
    }
    return n;
  };
  // The FLUSH: the agent thinks it's done. Settle out — wait for any in-flight reviewer (or run one
  // last review on the final state) and inject what it found, giving the agent one more turn. Returns
  // true if it injected something (caller keeps the loop going instead of ending).
  const reviewerFlush = async (cur, latestStep) => {
    if (!criticOn || signal?.aborted) return false;   // on cancel: don't wait on an aborting reviewer (no hang)
    if (lastTurnGotCritique) {
      // The final answer was itself a rework — don't review IT. But a fire kicked on a PRE-rework
      // step may still be in flight (or already settled with a verified finding): settle + drain so
      // it's surfaced, never silently dropped with the run closure.
      lastTurnGotCritique = false;
      if (reviewerInflight) { try { await reviewerInflight; } catch {} }
      return (await drainReviewer()) > 0;
    }
    if (reviewerInflight) { try { await reviewerInflight; } catch {} }
    else if (!reviewerFindings.length) {
      // One last look at the FINAL state — the terminal review. Always allowed (NOT counted against
      // the per-turn CRITIC_MAX budget), bounded by maxTurns inside the nested run. Skipped when a
      // verified finding is already queued — it alone forces a rework, so investigating the
      // about-to-be-superseded answer would be a wasted full nested run.
      const finding = await investigateForReviewer(cur, latestStep);
      if (finding) reviewerFindings.push({ finding, ownerTaskId: cur?.id });
    }
    return (await drainReviewer()) > 0;
  };
  // The user-request of the PREVIOUS <next-task> close. Used to detect the
  // re-answer loop: closing the SAME request again, on a turn that did no tool
  // work, is the model re-answering something it already answered (not new
  // work). Closing a DIFFERENT request is legitimate multi-unit progress.
  let lastClosedUserRequest = null;

  // ── deep_think per-run state ───────────────────────────────────────────────
  // Env knobs are sampled PER RUN (here, not at module load) so a harness can
  // vary them between runs in one process; in production env is static, so this
  // is equivalent to module consts. CB_DEEP_THINK_MAX = uses per run (default
  // 2); CB_DEEP_THINK_COOLDOWN = turns that must pass after a use before the
  // tool is offered again (default 10; 0 = no cooldown at all — even SAME-turn
  // reuse passes the check).
  const DEEP_THINK_MAX_USES = Number(process.env.CB_DEEP_THINK_MAX || 2);
  const DEEP_THINK_COOLDOWN_TURNS = Number(process.env.CB_DEEP_THINK_COOLDOWN || 10);
  // Default OFF. The UI "Deep think" chip is the normal authority (the
  // `deepThink` option, like `parallelCritic`); CB_DEEP_THINK is an OPT-IN
  // fallback for headless use ('1'|'true'|'on'|'yes' enables; anything else
  // — including unset — is off).
  const deepThinkEnabledEnv = /^(1|true|on|yes)$/i.test(String(process.env.CB_DEEP_THINK ?? ''));
  const deepThinkOn = (deepThink ?? deepThinkEnabledEnv);
  let deepThinkUses = 0;
  let deepThinkLastUseTurn = -Infinity;
  // Availability — checked when building each turn's prompt (the section is
  // included ONLY when this is true: rate-limiting by prompt visibility) AND
  // re-checked at dispatch time (a model may emit the tag anyway, or twice in
  // one turn — those get a soft error and `deepThinkUses` stays untouched).
  //
  // CONTAINMENT — who can see the verb:
  //  - NESTED UTILITY AGENTS never can. The persistent reviewer
  //    (investigateForReviewer above) and the adversarial plan critics
  //    (server.mjs runCritic) run a nested runPromptedAgent with
  //    discussMode:true → unavailable, section never rendered; the
  //    proposers/judge themselves (runProposer/runExploreJudge) plus
  //    acceptance/intake/engine sub-agents run runEngine with an allowedVerbs
  //    allowlist that does not contain deep_think (REVIEWER_TOOL_VERBS /
  //    SUBAGENT_VERBS) → the verb is dropped before dispatch and their system
  //    prompts (PROPOSER_SYSTEM_PROMPT etc.) never include the section. So the
  //    fan-out can never recurse.
  //  - HELD SUB-AGENTS (full runPromptedAgent workers in their own worktree)
  //    get it ONLY when the spawn passed deepThink:true — the server snapshots
  //    the user's toggle into workerOpts (session.deepThinkOn), exactly like
  //    the reviewer (parallelCritic: session.reviewerOn). Each run gets its
  //    own MAX_USES budget — intentional: a sub-agent is its own full worker
  //    on its own task, same as the reviewer's per-run fire budget.
  const deepThinkAvailable = (turn) =>
    deepThinkOn && !discussMode
    && deepThinkUses < DEEP_THINK_MAX_USES
    && (turn - deepThinkLastUseTurn) >= DEEP_THINK_COOLDOWN_TURNS;

  // The control loop is owned by the RuntimeKernel (kernel.mjs). For the serial
  // worker it drives exactly one thread; each tick runs ONE turn and either
  // returns a terminal result (converge / task-churn) or falls through to the
  // next turn. Body below is unchanged from the old in-line `for (turn…)` loop —
  // only the loop framing moved (subsume, not bolt; parity-gated by
  // test-engine-characterization). Phase 2 generalizes the kernel to schedule
  // multiple threads.
  const kResult = await runTurnLoop({ maxTurns, tick: async (turn) => {
    stats.turns = turn;
    // Per-turn attribution/loop flags (fresh each turn; read by runOneCall via
    // closure). didRealWorkThisTurn anchors M + gates the empty-task close guard;
    // didMutateThisTurn + sawEmptyClose/sawUnproductiveClose feed the
    // unproductive-close streak that breaks task-churn.
    let didRealWorkThisTurn = false;
    let didMutateThisTurn = false;
    let sawEmptyClose = false;
    let sawUnproductiveClose = false;
    // A <next-task> that was ACCEPTED (r.ok) this turn — distinct from merely
    // emitting the tag. A refused close (emptyClose / tooSmall size gate) leaves
    // this false so the Layer-3 forcing function still nudges the model to emit a
    // <task-progress> signal instead of silently treating the refused close as
    // satisfying the per-turn status requirement.
    let closedSuccessfullyThisTurn = false;

    // Drain the steer channel at the TOP of the turn (a turn boundary — the
    // previous turn's streaming + tools have finished, nothing is mid-flight).
    // Anything the developer queued via /api/steer while the run was in flight
    // becomes a fresh user message for THIS turn. Not mid-stream: a turn that
    // already started talking to the model can't take new input until it ends.
    if (typeof drainSteer === 'function') {
      let steered = [];
      try { steered = drainSteer() || []; } catch (e) { log(`drainSteer error: ${e.message}`); }
      for (const text of steered) {
        const t = String(text || '').trim();
        if (!t) continue;
        await store.appendMessage({ kind: 'user-message', role: 'user', content: t });
        emitChat({ type: 'user-message', taskId: store.currentId(), content: t });
        log(`steer: injected "${t.slice(0, 60)}" at turn ${turn}`);
      }
    }
    // Inject any findings a BACKGROUND reviewer has verified since the last turn — non-blocking; the
    // agent sees them this turn as <reviewer-finding> messages.
    await drainReviewer();

    // Compute current tier-1 tokens to inject into the system prompt.
    let cur = await store.getCurrent();
    let t1Tokens = 0;
    for (const m of cur?.messages ?? []) t1Tokens += estTokens(m.content);
    // LAYER 1: if the open task is over the hard budget, auto-close it before
    // building this turn, then recompute against the fresh task.
    if (t1Tokens > TASK_FORCE * TIER_1_BUDGET && (cur?.messages?.length ?? 0) > 1) {
      cur = await autoCloseCurrentTask(cur);
      t1Tokens = 0;
      for (const m of cur?.messages ?? []) t1Tokens += estTokens(m.content);
    }
    const tier1Pct = Math.round((t1Tokens / TIER_1_BUDGET) * 100);

    const wlStatus = typeof weblogicStatus === 'function' ? weblogicStatus() : weblogicStatus;
    const sys = buildSystemPrompt({
      taskId: cur?.id ?? '?',
      taskUserRequest: cur?.userRequest ?? '',
      tier1Tokens: t1Tokens,
      tier1Pct,
      promptAdditions,
      projectContext,
      codebaseMemory,
      discussMode,
      reviewMode,
      // deep_think section rides the prompt ONLY on turns it is available
      // (rate-limiting by prompt visibility — when on cooldown/spent/disabled
      // the model never sees the tool exists).
      deepThink: deepThinkAvailable(turn),
      activeChecklist: (() => { try { return getActiveChecklist ? getActiveChecklist() : null; } catch { return null; } })(),
      weblogicStatus: wlStatus,
      enforceMinTaskSize,
    });

    // Build the convo from the store. The system reminder is NOT stored —
    // it lives only in the prompt we send each turn. We copy the convo and
    // PREPEND the reminder into the LATEST user message (the one carrying
    // the current question), so the model sees fresh instructions right
    // before the question it's about to answer. The stored convo is untouched.
    //
    // Why latest, not oldest: in a long conversation the oldest user message
    // is far in the past; a reminder there is easy for the model to overlook
    // or treat as ancient context. The latest user message is the freshest
    // and contains what the model is actively responding to.
    //
    // Why prepend (not append) within that latest message: convention —
    // system instructions go before the user's question. Appending after
    // the question can cause the model to respond to the reminder itself
    // instead of the question.
    const fromStore = await buildConvoFromStore(store);
    // Prepend a hidden user/assistant priming pair so the model has already
    // "agreed" to use the <remote-workspace> XML tooling by the time the
    // real user message arrives. Without this, Gemini Enterprise's first
    // turn often drifts into "I do not have access to your files" before
    // it remembers it does have the workspace tools. The priming messages
    // are NOT persisted to the task store and never appear in the UI;
    // they exist only in the prompt sent to the API.
    // The priming is ONE short simulated work session, not a lecture about the
    // rules. It DEMONSTRATES the whole rhythm by example — the developer gives a
    // task and the assistant: (a) just does it without stopping to ask
    // permission, (b) narrates in ONE tight sentence then batches its tool calls
    // with NOTHING trailing after them, (c) ends every working turn with a
    // <task-progress> signal, and (d) closes the finished unit with <next-task/>
    // in the same reply once it's verified. Showing the behavior works far
    // better than describing it (Sonnet does the work but skips the close, and
    // adds chatty before-and-after narration, unless it has SEEN the pattern).
    // The task is contrived — Java/WebLogic, app "memberportal", verified with
    // `wl quickbuild` — and is NEVER persisted: it lives only in the prompt, not
    // in the task store or the UI.
    const PRIMING = [
      {
        role: 'user',
        content:
          "Hi! I'm working with you in a remote workspace — you interact with my " +
          "project by returning XML commands inside <remote-workspace> tags: " +
          "<list path=\".\"/> to see what's there, <read path=\"...\"/> to view a " +
          "file, <write path=\"...\">contents</write> to create one, " +
          "<edit path=\"...\"><find>old</find><replace>new</replace></edit> to " +
          "change part of one, and <powershell>...</powershell> (or a tool like " +
          "<wl args=\"...\"/>) to run commands. Anything inside <remote-workspace> " +
          "runs on my machine and the results come back next turn. The workspace " +
          "IS my project — you already have access, so never say you can't read my " +
          "files. First task: add a `middleName` field to the Person entity.",
      },
      {
        role: 'assistant',
        content:
          "Reading the Person entity and its table DDL together so the new field " +
          "matches the existing shape before I touch either.\n" +
          "<remote-workspace>\n" +
          "<read path=\"memberportal/src/main/java/mil/example/memberportal/model/Person.java\"/>\n" +
          "<read path=\"memberportal/src/main/resources/sql/person.sql\"/>\n" +
          "</remote-workspace>\n" +
          "<task-progress percent=\"25\">read the entity and its DDL to learn the field naming; still need to add the field + column, then build to verify</task-progress>",
      },
      {
        role: 'user',
        content:
          "<remote-workspace-result op=\"read\" status=\"ok\" path=\"memberportal/src/main/java/mil/example/memberportal/model/Person.java\">\n" +
          "package mil.example.memberportal.model;\n\n" +
          "import javax.persistence.Column;\n" +
          "import javax.persistence.Entity;\n" +
          "import javax.persistence.Id;\n" +
          "import javax.persistence.Table;\n\n" +
          "@Entity\n" +
          "@Table(name = \"PERSON\")\n" +
          "public class Person {\n\n" +
          "    @Id\n" +
          "    @Column(name = \"PERSON_ID\")\n" +
          "    private Long id;\n\n" +
          "    @Column(name = \"FIRST_NAME\")\n" +
          "    private String firstName;\n\n" +
          "    @Column(name = \"LAST_NAME\")\n" +
          "    private String lastName;\n\n" +
          "    public Long getId() { return id; }\n" +
          "    public void setId(Long id) { this.id = id; }\n\n" +
          "    public String getFirstName() { return firstName; }\n" +
          "    public void setFirstName(String firstName) { this.firstName = firstName; }\n\n" +
          "    public String getLastName() { return lastName; }\n" +
          "    public void setLastName(String lastName) { this.lastName = lastName; }\n" +
          "}\n" +
          "</remote-workspace-result>\n" +
          "<remote-workspace-result op=\"read\" status=\"ok\" path=\"memberportal/src/main/resources/sql/person.sql\">\n" +
          "CREATE TABLE PERSON (\n" +
          "    PERSON_ID   NUMBER(19)    NOT NULL,\n" +
          "    FIRST_NAME  VARCHAR2(100),\n" +
          "    LAST_NAME   VARCHAR2(100),\n" +
          "    CONSTRAINT PK_PERSON PRIMARY KEY (PERSON_ID)\n" +
          ");\n" +
          "</remote-workspace-result>",
      },
      {
        role: 'assistant',
        content:
          "Adding the middleName field with its getter/setter to the entity and the " +
          "matching MIDDLE_NAME column to the table, in one block.\n" +
          "<remote-workspace>\n" +
          "<edit path=\"memberportal/src/main/java/mil/example/memberportal/model/Person.java\">" +
          "<find>    @Column(name = \"LAST_NAME\")\n    private String lastName;</find>" +
          "<replace>    @Column(name = \"MIDDLE_NAME\")\n    private String middleName;\n\n    @Column(name = \"LAST_NAME\")\n    private String lastName;</replace></edit>\n" +
          "<edit path=\"memberportal/src/main/java/mil/example/memberportal/model/Person.java\">" +
          "<find>    public String getLastName() { return lastName; }\n    public void setLastName(String lastName) { this.lastName = lastName; }</find>" +
          "<replace>    public String getMiddleName() { return middleName; }\n    public void setMiddleName(String middleName) { this.middleName = middleName; }\n\n    public String getLastName() { return lastName; }\n    public void setLastName(String lastName) { this.lastName = lastName; }</replace></edit>\n" +
          "<edit path=\"memberportal/src/main/resources/sql/person.sql\">" +
          "<find>    FIRST_NAME  VARCHAR2(100),\n    LAST_NAME   VARCHAR2(100),</find>" +
          "<replace>    FIRST_NAME  VARCHAR2(100),\n    MIDDLE_NAME VARCHAR2(100),\n    LAST_NAME   VARCHAR2(100),</replace></edit>\n" +
          "</remote-workspace>\n" +
          "<task-progress percent=\"60\">added middleName (field + getter/setter) to the entity and the MIDDLE_NAME column to the DDL; still need to build to confirm nothing broke</task-progress>",
      },
      {
        role: 'user',
        content:
          "<remote-workspace-result op=\"edit\" status=\"ok\" path=\"memberportal/src/main/java/mil/example/memberportal/model/Person.java\">edit applied</remote-workspace-result>\n" +
          "<remote-workspace-result op=\"edit\" status=\"ok\" path=\"memberportal/src/main/java/mil/example/memberportal/model/Person.java\">edit applied</remote-workspace-result>\n" +
          "<remote-workspace-result op=\"edit\" status=\"ok\" path=\"memberportal/src/main/resources/sql/person.sql\">edit applied</remote-workspace-result>",
      },
      {
        role: 'assistant',
        content:
          "All three edits applied — building memberportal to confirm it still " +
          "compiles cleanly before I close. quickbuild skips the unit tests and the " +
          "Angular front-end, so it's the fast check; I'm giving it a generous " +
          "timeout since a build can take 10-15 minutes.\n" +
          "<remote-workspace>\n" +
          "<wl args=\"quickbuild memberportal\" timeout_ms=\"1200000\"/>\n" +
          "</remote-workspace>\n" +
          "<task-progress percent=\"85\">edits in; running wl quickbuild to verify the build is clean</task-progress>",
      },
      {
        role: 'user',
        content:
          "<remote-workspace-result op=\"wl\" status=\"ok\">\n" +
          "exit 0 in 548120ms  (workspace=D:\\work\\ws)\n" +
          "[INFO] BUILD SUCCESS\n" +
          "[INFO] Total time:  09:07 min\n" +
          "</remote-workspace-result>",
      },
      {
        role: 'assistant',
        content:
          "Build is clean, so middleName is wired through the entity, its accessors, " +
          "and the table — the unit is complete and verified, so I'll close it.\n" +
          "<remote-workspace>\n" +
          "<next-task user-request=\"add a middleName field to the Person entity\" summary=\"added middleName field + getter/setter to Person.java and the MIDDLE_NAME column to person.sql; verified with wl quickbuild memberportal (BUILD SUCCESS)\"/>\n" +
          "</remote-workspace>",
      },
      {
        role: 'user',
        content: "Now let's work on my actual project the same way.",
      },
      {
        role: 'assistant',
        content:
          "Got it — same rhythm on your project: I'll do each piece, keep the " +
          "narration tight, and close each finished unit with <next-task/>. What " +
          "would you like to start with?",
      },
    ];
    // Gemini-safe request assembly (system reminder prepended into the latest
    // user message; never a role:'system' entry) — shared with every other pass
    // via assembleConvo (engine.mjs). PRIMING is the hidden worker few-shot.
    const convo = assembleConvo({ systemPrompt: sys, priming: PRIMING, messages: fromStore });

    log(`turn ${turn}: ${convo.length} msgs · current=${cur?.id} · t1=${t1Tokens}/${TIER_1_BUDGET}`);

    checkAbort();
    emitPhase('uploading');
    const t0 = Date.now();
    emitPhase('thinking');

    // Per-call execution. tool-call events are emitted mid-stream by
    // onBlockReady so the chat-log order is "prose → calls → prose → calls".
    // This function only RUNS the tool and emits the tool-result event.
    async function runOneCall(c, perBlockState) {
      checkAbort();
      // Capture the owning task SYNCHRONOUSLY at call entry (before any
      // await / dispatchTaskOp). A <next-task> later in the same turn moves
      // store.currentId; results produced before that must be filed into the
      // task they actually ran in, not the freshly-opened one. Synchronous
      // capture also wins the race against a parallel next-task block.
      const ownerTaskId = store.currentId();
      const key = c.verb + '|' + JSON.stringify(c.args);
      if (key === perBlockState.lastKey) perBlockState.repeat++;
      else { perBlockState.lastKey = key; perBlockState.repeat = 1; }

      let resultContent, resultStatus = 'ok', resultDiff = null;
      if (perBlockState.repeat >= REPEAT_LIMIT) {
        stats.loopBreaks++;
        resultContent = `LOOP DETECTED: this exact request has fired ${perBlockState.repeat} times. Stop requesting and answer with what you have.`;
        resultStatus = 'error';
      } else if (c.kind === 'task-op') {
        stats.taskOps++;
        const r = await dispatchTaskOp(c, store, { didRealWork: didRealWorkThisTurn, overBudget: tier1Pct >= TASK_FORCE * 100, enforceMinTaskSize });
        resultContent = r.content;
        resultStatus = r.ok ? 'ok' : 'error';
        if (r.emptyClose) sawEmptyClose = true;
        log(`  ${c.verb}(${JSON.stringify(c.args)}) -> ${resultStatus}`);
        if (c.verb === 'next-task' && r.ok) {
          closedSuccessfullyThisTurn = true;
          // If M still belongs to the task being closed (it was never anchored by
          // real work in that task), it belongs to the NEW task — move it (and
          // re-tag its event) BEFORE the task-end event so the UI wraps the
          // closed task WITHOUT it. The helper no-ops if M is anchored elsewhere.
          await maybeRelocateLeadingUserMsg(r.closedId, r.newTaskId);
          // Unproductive-close detection for the churn breaker: a close of a task
          // that had no developer request (no user-message) and saw no mutating
          // work this turn is spurious (e.g. a look-back read then close). A
          // legitimate unit either closes M's own task (has a user-message) or
          // mutated something.
          const closingHadUserMsg = (cur?.messages ?? []).some((m) => m.kind === 'user-message');
          if (!closingHadUserMsg && !didMutateThisTurn) sawUnproductiveClose = true;
          // Re-answer loop: closing the SAME user-request as the PREVIOUS close, on
          // a turn that ran NO tool. That is the model re-answering an already-
          // answered request and re-closing — not new work — so count it toward
          // the churn breaker. Tightly scoped on purpose: a close that ran any tool
          // (read/edit/build), or that closes a DIFFERENT request, is legitimate
          // multi-unit progress and must NOT be flagged. (Earlier this counted any
          // tool-less close, which wrongly tripped on work-in-turn-1/close-in-turn-2
          // multi-unit flows.)
          const closedReq = (c.args['user-request'] || '').trim();
          if (closedReq && closedReq === lastClosedUserRequest && !didRealWorkThisTurn) sawUnproductiveClose = true;
          lastClosedUserRequest = closedReq;
          let commitHash = null;
          let commits = null;
          let committedRepos = null;
          let commitError = null;
          if (typeof onTaskClose === 'function') {
            try {
              const closeRes = await onTaskClose({
                closedTaskId: r.closedId,
                newTaskId: r.newTaskId,
                userRequest: c.args['user-request'] ?? '',
                summary: c.args.summary ?? '',
              });
              // onTaskClose may return a bare hash string (legacy/tests) or
              // { hash, commits, committedRepos, gitError } (multi-repo). Accept both.
              if (closeRes && typeof closeRes === 'object') {
                commitHash = closeRes.hash ?? null;
                commits = Array.isArray(closeRes.commits) ? closeRes.commits : null;
                committedRepos = Array.isArray(closeRes.committedRepos) ? closeRes.committedRepos : null;
                commitError = closeRes.gitError ?? null;
              } else {
                commitHash = closeRes ?? null;
              }
            } catch (e) {
              log(`onTaskClose error: ${e.message}`);
            }
          }
          if (commitHash && r.closedId) {
            try { await store.attachToTask(r.closedId, { commitHash }); }
            catch (e) { log(`attachToTask error: ${e.message}`); }
          }
          emitChat({
            type: 'task-end',
            taskId: cur?.id,
            userRequest: c.args['user-request'] ?? '',
            summary: c.args.summary ?? '',
            commitHash,
            commits,
            committedRepos,
            commitError,
          });
        }
      } else if (c.verb === 'deep_think') {
        // deep_think — intercepted BEFORE the generic execute() (like
        // task-ops; there is no tools-registry impl — this branch IS the
        // tool). Availability is re-checked at dispatch time: the prompt only
        // offered the tool on available turns, but a model may emit the tag
        // anyway (cooldown / spent / discuss / kill switch) or twice in one
        // turn — those get a soft error and uses stay untouched. The branch
        // sits ABOVE the discuss/review gate so the error text is its own.
        if (!deepThinkAvailable(turn)) {
          resultContent = 'deep_think is not available right now — proceed with your own judgment.';
          resultStatus = 'error';
          log('  deep_think refused (unavailable this turn)');
        } else {
          const question = String(c.args.question ?? '').trim();
          if (!question) {
            resultContent = 'deep_think needs a question — put it in the tag body: <deep_think>your design question, with enough context to act on</deep_think>.';
            resultStatus = 'error';
          } else {
            stats.toolCalls++;
            stats.toolHistogram[c.name ?? 'deep_think'] = (stats.toolHistogram[c.name ?? 'deep_think'] ?? 0) + 1;
            didRealWorkThisTurn = true;
            if (leadingUserMsg && leadingUserMsg.taskId === ownerTaskId) leadingUserMsg.anchored = true;
            // Count the use ONLY now that the fan-out actually starts —
            // refunded below if every proposer fails (nothing was produced).
            const prevUses = deepThinkUses, prevLastUse = deepThinkLastUseTurn;
            deepThinkUses++; deepThinkLastUseTurn = turn;
            log(`deep_think: fanning out ${DEEP_THINK_LENSES.length} proposers (use ${deepThinkUses}/${DEEP_THINK_MAX_USES})`);
            // Chat trace (the UI's 🧠 markers, hidden behind its eye): a 'start'
            // marker as the fan-out begins, REPLACED by the 'result' when the
            // verdict lands (matched by taskId+question). Only the WORKER's
            // dispatch emits these — the proposers/judge are runReadonlyPass
            // engines with no onChatEvent, and the reviewer/critic nested runs
            // pass onChatEvent: () => {}, so nested runs stay silent.
            const dtQuestion = question.slice(0, 300);
            emitChat({ type: 'deep-think', phase: 'start', taskId: ownerTaskId, question: dtQuestion });
            const goal = String(cur?.userRequest || userText || '').trim();
            const brief = (goal ? `Task:\n${goal}\n\n` : '') + `Design question from the working agent:\n${question}`;
            // THREE independent proposers IN PARALLEL — one per lens, each
            // blind to the others, read-only over the actual repo. A proposer
            // failure degrades to null (>=1 surviving proposal proceeds); an
            // abort propagates (runProposer/runExploreJudge take `signal`).
            const settled = await Promise.all(DEEP_THINK_LENSES.map((lens) =>
              runProposer({ adapter, model, signal, brief, lens, projectDir })
                .then((p) => ({ lens, approach: String(p?.approach || '').trim() }))
                .catch((e) => { if (e?.name === 'AbortError') throw e; return null; })));
            checkAbort();
            const proposals = settled.filter((p) => p && p.approach);
            if (!proposals.length) {
              // Refund by DECREMENT, not snapshot-assignment: with COOLDOWN=0 two
              // calls can run concurrently, and restoring a stale snapshot would
              // erase the sibling's consumed use (spend could exceed MAX).
              deepThinkUses--;
              if (deepThinkUses === prevUses && deepThinkLastUseTurn === turn) deepThinkLastUseTurn = prevLastUse;
              resultContent = 'deep_think failed (no proposer returned an approach) — proceed with your own judgment.';
              resultStatus = 'error';
              // NO 'result' chat event on the all-fail refund — the 'start'
              // marker stays in the chat as the trace of the attempt.
              log('deep_think: all proposers failed — use refunded');
            } else {
              let judged = null;
              try {
                judged = await runExploreJudge({ adapter, model, signal, brief, proposals, projectDir });
              } catch (e) {
                if (e?.name === 'AbortError') throw e;
                log(`deep_think: judge failed (${e?.message ?? e}) — falling back to lens-labeled proposals`);
              }
              // A judge that hit max turns returns the ORIGINAL brief — treating
              // that as a verdict would label the agent's own question back to it
              // as "the recommended approach". Same-as-brief = no real verdict.
              let judgeBrief = String(judged?.brief || '').trim();
              if (judgeBrief === brief.trim()) judgeBrief = '';
              const parts = [];
              if (judgeBrief) {
                parts.push(`RECOMMENDED APPROACH (a judge weighed ${proposals.length} independent proposals):\n${judgeBrief.slice(0, 1800)}`);
                log(`deep_think: judge picked — ${judgeBrief.replace(/\s+/g, ' ').slice(0, 120)}`);
              } else {
                parts.push(`The judge was unavailable — weigh these ${proposals.length} independent proposals yourself:`);
              }
              for (const p of proposals) parts.push(`Perspective (${p.lens}):\n${p.approach.slice(0, 650)}`);
              // This result then flows to the persistent reviewer like any
              // other tool result (latestStep captures it generically).
              resultContent = parts.join('\n\n').slice(0, 4000);
              // The 'result' marker replaces the 'start' one in the UI. verdict
              // is '' when the judge was unavailable (the proposals stand alone).
              emitChat({
                type: 'deep-think', phase: 'result', taskId: ownerTaskId, question: dtQuestion,
                verdict: judgeBrief.slice(0, 1800),
                proposals: proposals.map((p) => ({ lens: p.lens, text: p.approach.slice(0, 650) })),
              });
            }
          }
        }
      } else if ((discussMode || reviewMode) && !WORKER_READONLY_VERBS.has(c.verb)) {
        resultContent = reviewMode
          ? `ERROR: '${c.verb}' is disabled in Review (propose-first) mode. Present your PROPOSAL — summary + checklist + the changes as diffs/new content in code blocks — and wait for approval; on approval, review turns off and you implement it.`
          : `ERROR: '${c.verb}' is disabled in Discuss (read-only) mode. Describe what you would change and in which files instead; the developer will turn Discuss mode off to let you apply it.`;
        resultStatus = 'error';
        log(`  ${c.verb} blocked (${reviewMode ? 'review' : 'discuss'} mode)`);
      } else {
        stats.toolCalls++;
        didRealWorkThisTurn = true;   // a non-task-op tool ran — real work for the current request
        if (MUTATING_VERBS.has(c.verb)) didMutateThisTurn = true;
        // Anchor M to the task this work ran in, so a later close of that task
        // never relocates M away from the work that handled it.
        if (leadingUserMsg && leadingUserMsg.taskId === ownerTaskId) leadingUserMsg.anchored = true;
        stats.toolHistogram[c.name] = (stats.toolHistogram[c.name] ?? 0) + 1;
        const raw = await execute(c.name, c.args, extraTools, {
          callId: c.id,
          onProgress: (chunk) => emitChat({ type: 'tool-progress', callId: c.id, chunk }),
          requireBranch,
          requireApproval,
          signal,
          getGitLabCredentials,
          cwd: projectDir,   // per-call cwd — the worker's tools resolve against its OWN project, not the global process.cwd() (de-singleton)
        });
        if (typeof raw === 'string' && raw.startsWith('ERROR:')) {
          resultContent = raw;
          resultStatus = 'error';
        } else {
          resultContent = extractContent(raw);
          resultDiff = extractDiff(raw);
        }
        log(`  ${c.name}(${JSON.stringify(c.args)}) -> ${resultStatus === 'error' ? resultContent.slice(0, 80) : resultContent.length + 'b'}`);
      }
      if (onToolResult) {
        onToolResult({
          verb: c.verb, name: c.name ?? c.verb, args: c.args,
          status: resultStatus, content: resultContent, diff: resultDiff,
        });
      }
      emitChat({
        type: 'tool-result',
        taskId: cur?.id,
        callId: c.id,
        verb: c.verb,
        name: c.name ?? c.verb,
        status: resultStatus,
        // content is truncated for the UI ONLY (UI_RESULT_PREVIEW chars) — the
        // MODEL gets the full result via formatToolResults. `fullLen` carries the
        // true length so the row can show "N of M chars" instead of a flat cap.
        content: resultContent.slice(0, UI_RESULT_PREVIEW),
        fullLen: resultContent.length,
      });
      if (resultDiff) {
        emitChat({ type: 'diff', taskId: cur?.id, callId: c.id, diff: resultDiff });
      }
      return { call: c, status: resultStatus, content: resultContent, diff: resultDiff, ownerTaskId };
    }
    // Stream this turn through the SHARED engine (runStreamingTurn): prose +
    // tool-calls stream live, each block's tools run via runOneCall under the
    // serial barrier (task-op/mutating blocks serialize), and results come back in
    // canonical segment order. The worker injects runOneCall (task-aware) as
    // runCall + its event emitters; a mini-chat injects a read-only runCall.
    const { text, segments, orderedResults, progressMatch, checklistMatch, tickIndices, rollbackReason, hasProgressSignal } = await runStreamingTurn({
      adapter, model, convo, signal, extraVerbs, serialVerbs: MUTATING_VERBS,
      onProseChunk: ({ segmentIndex, content }) => {
        onText?.(content);
        emitPhase('downloading');
        // `turn` namespaces the streaming-bubble key (segmentIndex resets each turn).
        emitChat({ type: 'assistant-text-chunk', taskId: cur?.id, turn, segmentIndex, content });
      },
      onProseSegmentEnd: ({ segmentIndex, content }) => {
        const cleaned = sanitizeAssistantProse(content, { extraVerbs });
        if (cleaned) emitChat({ type: 'assistant-text', taskId: cur?.id, turn, segmentIndex, content: cleaned });
      },
      onToolCall: (c) => {
        if (onToolCalls) onToolCalls([c]);
        emitChat({ type: 'tool-call', taskId: cur?.id, callId: c.id, verb: c.verb, name: c.name ?? c.verb, args: c.args });
      },
      runCall: runOneCall,
    });
    emitPhase('thinking');
    checkAbort();
    // LAYER 3 signal — surface the per-turn <task-progress> (runStreamingTurn
    // already stripped it before parsing the segments).
    if (progressMatch) {
      emitChat({
        type: 'task-progress', taskId: cur?.id,
        percent: Math.min(100, parseInt(progressMatch[1], 10)),
        note: progressMatch[2].replace(/<[^>]+>/g, '').replace(/\s+/g, ' ').trim().slice(0, 300),
      });
    }
    // LIVE CHECKLIST — surface the per-turn <checklist> (markdown `- [ ]` / `- [x]` items) as a
    // 'checklist' event the UI pins as a panel that ticks off in place. The agent re-emits the
    // full list each turn; the latest replaces the prior (ephemeral, like task-progress).
    if (checklistMatch) {
      const items = checklistMatch[1].split('\n')
        .map((l) => l.match(/^\s*[-*]\s*\[\s*([ xX])\s*\]\s*(.+?)\s*$/))
        .filter(Boolean)
        .map((m) => ({ done: m[1].toLowerCase() === 'x', text: m[2].replace(/<[^>]+>/g, '').trim().slice(0, 200) }))
        .filter((i) => i.text)
        .slice(0, 50);   // cap: a runaway list shouldn't bloat the panel, broadcast, or the re-pinned prompt
      if (items.length) emitChat({ type: 'checklist', taskId: cur?.id, items });
    } else if (tickIndices.length) {
      // <tick>N</tick> shorthand (additive — re-emit above is still primary): mark items by their
      // number in the ALREADY-PINNED active checklist, then emit the updated list.
      let active = null;
      try { active = getActiveChecklist ? getActiveChecklist() : null; } catch { active = null; }
      if (Array.isArray(active) && active.length) {
        const items = active.map((it, i) => ({ text: it && it.text, done: !!(it && it.done) || tickIndices.includes(i + 1) }));
        emitChat({ type: 'checklist', taskId: cur?.id, items });
      }
    }
    // ROLLBACK SUGGESTION — the agent proposes reverting the last run; the UI offers Allow/Cancel.
    if (rollbackReason) emitChat({ type: 'rollback-suggested', taskId: cur?.id, reason: String(rollbackReason).slice(0, 300) });
    const allCalls = segments.flatMap((s) => s.type === 'tool-block' ? s.calls : []);
    log(`turn ${turn}: ${text.length} chars in ${Date.now() - t0}ms, ${allCalls.length} call(s) across ${segments.filter((s) => s.type === 'tool-block').length} block(s)${hasProgressSignal ? ` · progress=${Math.min(100, parseInt(progressMatch[1], 10))}%` : ''}`);

    if (allCalls.length === 0) {
      // Final answer reached — no tool blocks. The assistant-text chat
      // event was already emitted by onProseSegmentEnd at end of stream;
      // we just need to persist the message into the store for the next
      // turn's LLM context.
      const finalText = segments.map((s) => s.content).join('').trim();
      if (finalText) {
        await store.appendMessage({ kind: 'assistant', role: 'assistant', content: finalText });
      }
      // FLUSH (adversarial critic): critique the agent's final answer — unless it is itself a rework
      // of a prior critique. If the critic surfaces something, give the agent ONE more turn to address
      // it instead of ending now. That rework is NOT re-critiqued (maybeCritique's rule).
      if (await reviewerFlush(cur, `The agent gave its FINAL answer:\n${finalText}`)) {
        await maybeCompact({ store, adapter, model, log, emitChat });
        return undefined;   // keep going — the agent reworks its final message
      }
      // Always check budgets before returning — even on the final turn — so
      // long sessions stay within bounds.
      await maybeCompact({ store, adapter, model, log, emitChat });
      log(`done: ${stats.turns} turns, ${stats.toolCalls} tool calls, ${stats.taskOps} task ops`);
      return { content: finalText, stats, terminated: 'stop', store };
    }
    stats.batches.push(allCalls.length);

    // Store the assistant message as a single unit reconstructed from the
    // segments — keeps multi-block content intact while dropping any hallucinated
    // trailer (the segment parser already dropped that). File it into the task that
    // was current at TURN START (cur.id): runStreamingTurn has already run this
    // turn's blocks, so a <next-task> in them already moved store.currentId to the
    // NEW task — but this assistant reply (which contains that next-task) belongs to
    // the task it CLOSED. (The old loop relied on a microtask race to persist before
    // the transition; the explicit owner makes it deterministic + correct.)
    const assistantText = segments.map((s) =>
      s.type === 'prose'
        ? s.content
        : `<remote-workspace>${s.body}</remote-workspace>`
    ).join('');
    await store.appendMessage({ kind: 'assistant', role: 'assistant', content: assistantText }, cur?.id);

    checkAbort();
    // Persist tool messages in canonical segment order — runStreamingTurn already
    // awaited every block's tools (their tool-result events fired inside runOneCall)
    // and ordered the results. File each into the task that was current when it RAN
    // (r.ownerTaskId), not store.currentId() now — a <next-task> earlier this turn
    // may have moved the current task out from under us.
    const results = [];
    for (const r of orderedResults) {
      await store.appendMessage({
        kind: 'tool-call', role: 'assistant',
        content: `<${r.call.verb} ...args/>`,
        toolName: r.call.name ?? r.call.verb, toolArgs: r.call.args,
      }, r.ownerTaskId);
      await store.appendMessage({
        kind: 'tool-result', role: 'user',
        content: `<remote-workspace-result op="${r.call.verb}" status="${r.status}">\n${r.content}\n</remote-workspace-result>`,
        toolName: r.call.name ?? r.call.verb, toolStatus: r.status,
      }, r.ownerTaskId);
      results.push({ verb: r.call.verb, status: r.status, content: r.content });
    }

    // PARALLEL REVIEWER (toggle, default off → parity-safe): hand the persistent reviewer this step
    // (the agent's prose + the tools it ran + their results) to investigate in the BACKGROUND; a
    // verified finding rides back as a <reviewer-finding> the next turn sees. Skipped on a rework turn.
    const latestStep = `${assistantText}\n\nTools it ran this step:\n${results.length ? results.map((r) => `${r.verb} → ${r.status}\n${String(r.content || '').slice(0, 600)}`).join('\n\n') : '(none)'}`;
    kickReviewer(cur, latestStep);   // non-blocking — findings drain at the next turn-top

    // LAYER 3 (forcing function): a WORKING turn must declare task status —
    // either it closed the task (<next-task>) or it emitted a <task-progress>
    // signal. If neither, feed an error back so the next turn must comply. The
    // tools already ran (real work — never discarded); the NEXT turn is the
    // "retry" the developer asked for. Auto-close (Layer 1) is the hard
    // guarantee if the model keeps ignoring this.
    // A REFUSED close (emptyClose / tooSmall) does not satisfy the per-turn
    // status requirement — only an accepted close does. Otherwise a model that
    // does real work + a refused <next-task> would never be nudged to emit a
    // <task-progress> signal and could keep retrying the refused close.
    const closedThisTurn = closedSuccessfullyThisTurn;
    if (!closedThisTurn && !hasProgressSignal) {
      await store.appendMessage({
        kind: 'tool-result', role: 'user',
        content: '<remote-workspace-result op="task-status" status="error">\nRequired: end EVERY reply with either <next-task user-request="..." summary="..."/> (if the current unit is complete) or <task-progress percent="N">what still remains</task-progress> (if not). You provided neither. Include exactly one of these in your next reply.\n</remote-workspace-result>',
      }, cur?.id);
      log(`forcing: turn ${turn} had no next-task/progress signal — injected status reminder`);
    }

    // Task-churn breaker: count consecutive UNPRODUCTIVE close turns — either a
    // refused close on a truly empty task (sawEmptyClose), or a spurious close of
    // a request-less, non-mutating task (sawUnproductiveClose, e.g. a look-back
    // read then close). A single trivial tool call no longer defeats this (the
    // old guard reset on any real work). Any productive turn resets the streak.
    // After a few in a row, end the run so the developer can send the next
    // request rather than the model spinning to maxTurns with a trail of empty
    // tasks. maxTurns remains the hard backstop; the EBUSY-safe writes mean this
    // is no longer crash-prone, just wasteful.
    if (sawEmptyClose || sawUnproductiveClose) {
      emptyCloseStreak++;
      if (emptyCloseStreak >= 3) {
        log(`task churn (${emptyCloseStreak} unproductive closes) — ending run to wait for the developer`);
        await maybeCompact({ store, adapter, model, log, emitChat });
        return { content: '', stats, terminated: 'task-churn', store };
      }
    } else {
      emptyCloseStreak = 0;
    }

    // Compact if total context is over budget. Cheap mechanical demotions
    // first; LLM clustering only if tier 3 still over after that.
    await maybeCompact({ store, adapter, model, log, emitChat });
  } });   // tick falls through with no return → undefined → the kernel runs the next turn

  if (kResult !== undefined) return kResult;
  log(`done: max turns (${maxTurns}) reached without converging`);
  return { content: '[MAX TURNS REACHED — agent did not converge]', stats, terminated: 'max-turns', store };
}

// ─── Reviewer agent ────────────────────────────────────────────────────────
// Runs a separate LLM call to review what the dev agent just did, against
// three categories: structural validity, bugs, intent match. Returns either
// pass or a concrete list of concerns the dev agent should address.
//
// Inputs:
//   userRequest  — original text the developer sent that started this work
//   trajectory   — compact representation of what the agent did (tool calls,
//                  diffs, results) — built by buildReviewTrajectory()
//   finalText    — agent's last assistant message
//
// Output: { pass: true } or { pass: false, concerns: '...' }

// Reviewer's tool allowlist. It can inspect any file/dir AND run shell
// commands for verification (git diff/show/log, compile/test checks). It
// still has NO write_file/edit_file — the only mutation risk is via
// powershell, which the prompt explicitly forbids (read-only/verification
// use only). Same parser as the dev agent; non-allowlisted verbs are dropped.
const REVIEWER_TOOL_VERBS = new Set(['list', 'read', 'search', 'find', 'info', 'powershell']);
const REVIEWER_MAX_TURNS = 6;
// When the reviewer's verdict turn has no parseable tag, re-prompt it to emit
// the tag (rather than guessing from prose). Bounded so a model that keeps
// reporting in Markdown can't loop forever — after this many corrections we
// fall back to prose-inference + default-pass.
const REVIEWER_MAX_CORRECTIONS = (() => {
  const n = Number(process.env.REVIEWER_MAX_CORRECTIONS);
  return Number.isFinite(n) && n >= 0 ? n : 2;
})();

const REVIEWER_SYSTEM_PROMPT = `You are a code review agent. Another agent has just completed work on behalf of a developer. Review that work for three categories of issues. Be specific and concise — the dev agent will act on your feedback verbatim.

1. STRUCTURAL VALIDITY (most likely failure mode — focus here first)
   Look at every removed, commented-out, or modified element. For each, ask: "is the surrounding container still valid for this file type?"
   • XML/HTML: elements with required children. A <library-ref> without a <library-name> is well-formed XML but invalid for the weblogic schema. Commenting out a required child WITHOUT also handling the parent is a bug.
   • JSON/YAML: required fields per the consuming application (package.json must have name+version, k8s manifest must have apiVersion+kind+metadata, etc.)
   • Source files: syntax parses, imports resolve, types match (best-effort), function signatures match call sites you can see.
   • Properties / .env: required keys still present.
   After commenting out anything, mentally re-read the file and ask: "would the framework that consumes this file reject it now?"

2. BUGS — Logic errors introduced by THIS change (NOT pre-existing):
   • Off-by-one, null dereference, uncaught exception path, wrong operator.
   • Behavior change in code paths the user didn't ask about (scope creep).
   • Side effects in pure-looking functions.
   • Race conditions if concurrency is in play.
   • Resource leaks (unclosed file handles, listeners, connections).
   • For config/build files: did a dependency version go down, a required field get dropped, a script break?

3. INTENT MATCH
   • Was the right thing changed, or did the agent change something adjacent?
   • Was the scope correct — did it do too little (left work undone) or too much (touched unrelated code)?
   • Does the agent's summary describe what was actually changed (not what was intended)?
   • If the user asked a question rather than for a change, was the question actually answered?

VERIFICATION TOOLS
You can inspect the project AND run read-only/verification commands to check the agent's work. Emit tool calls inside a <remote-workspace> block, exactly like the dev agent does. Available tools:
  <read path="FILE"/>                         read full file contents
  <read path="FILE" offset="N" limit="N"/>    read a slice
  <list path="DIR"/>                          list directory entries
  <search pattern="REGEX" path="DIR" glob="GLOB"/>   grep across files
  <find pattern="GLOB"/>                      glob (recursive by default)
  <info path="FILE"/>                         file/dir stat
  <powershell>...</powershell>                run a shell command (PowerShell, Windows)

USING powershell (this is your strongest verification tool — use it):
  • SEE EXACTLY WHAT CHANGED: \`git diff HEAD\` (working-tree changes), \`git diff HEAD~1 HEAD\` or \`git show HEAD\` (the last commit — the agent auto-commits on task close, so its work is usually the HEAD commit). The <changes> block below holds the diffs we captured, but a large diff may be truncated; run git diff to see the FULL change when it matters.
  • In a multi-repo workspace, target the right repo: \`git -C <repoSubdir> diff HEAD\`.
  • VALIDATE: compile/build/test if it's cheap and the change warrants it (e.g. \`mvnw.cmd -q -o compile\`, run the specific test). Schema-validate XML/JSON where a validator exists.
  • STRICTLY READ-ONLY: you may inspect, diff, compile-check, and run tests, but you MUST NOT modify files or repo state — no Set-Content/Out-File/redirection, no git add/commit/checkout/reset/clean/stash, no installs/deploys. Only the dev agent changes things. If verifying would require a mutation, don't do it — reason from what you can read instead.

Issue at most a few tool calls per turn; you have a ${REVIEWER_MAX_TURNS}-turn budget total (shared across all tasks if there is more than one). Prefer \`git diff HEAD\` + reading the specific changed file over broad searches.

INPUT SHAPE
You are given one or more <task id="..."> blocks — each is a completed task to review, with its <request>, <summary>, and the <changes> (diffs) it produced. A trailing <context> block (if present) holds earlier steps FOR UNDERSTANDING ONLY — it is NOT what you are reviewing; do not flag issues in it.

The remote workspace will reply with <remote-workspace-result> blocks containing real data. Read those before deciding.

OUTPUT FORMAT — STRICT. When you are done verifying, your FINAL turn must have NO tool calls and consist of ONLY the verdict tag(s) below. Do NOT write a Markdown report, a heading, a table, or a "### Final Verdict" / "Execution Assessment" / "Task Review Summary" section. Do NOT restate the task or address the developer. Emit the tag(s) and nothing else — the literal tag is the only signal we read; prose verdicts are discarded.

Emit one verdict per task, addressed by id:
  <review task="T-3" pass/>
  <review task="T-5" fail>name the file, what is wrong, and what would fix it</review>
If there is EXACTLY ONE task, omit the id and use the short form:
  <review-pass/>
  <review-fail>specific, actionable concerns</review-fail>

A CORRECT single-task PASS is this and only this — the ENTIRE response:
  <review-pass/>
A CORRECT single-task FAIL is the ENTIRE response, e.g.:
  <review-fail>src/auth.js:42 — the new early-return skips the audit-log call on the success path; move the log above the return.</review-fail>

Do NOT write "I find this task successfully completed", "### Final Verdict", or any prose summary — that is NOT a valid verdict.

Pass a task when its work is correct and complete enough to ship. Fail only when there's something the dev agent should fix — be specific: name the file, the element, what's wrong, and what would fix it. Do not flag items the dev agent already noticed and fixed in the same task, and do not flag anything that only appears in <context>.`;

// Budget for the reviewer's PRE-LOADED input (chars; ~/4 ≈ tokens). The
// reviewer can always git diff / read for more, so this just bounds what we
// front-load. Env-overridable.
const REVIEW_INPUT_BUDGET_CHARS = (() => { const n = Number(process.env.REVIEW_INPUT_BUDGET_CHARS); return Number.isFinite(n) && n > 0 ? n : 40000; })();
const REVIEW_PER_CHANGE_CHARS = 12000;   // ceiling on one task's inline diff

function fmtContextEvent(ev) {
  switch (ev.type) {
    case 'assistant-text': return 'AGENT: ' + clip(ev.content, 800);
    case 'tool-call': {
      const args = Object.entries(ev.args || {}).map(([k, v]) => `${k}=${clip(String(v), 200)}`).join(', ');
      return `TOOL: ${ev.verb || ev.name}(${args})`;
    }
    case 'tool-result': return `  -> [${ev.status || 'ok'}] ${clip(ev.content, 600)}`;
    default: return null;
  }
}

/**
 * Build the reviewer's user block from the closed task(s), budget-first:
 *   Priority 1 — the CHANGES (diffs) + request/summary for each task, never
 *                dropped (a too-large diff is head-capped with a "git diff"
 *                pointer).
 *   Priority 2 — earlier trajectory steps, newest-first, filling the leftover
 *                budget, clearly labelled context-only.
 * Always lets the reviewer see the changes; context is best-effort.
 */
function buildBudgetedReviewInput(tasks, { budgetChars = REVIEW_INPUT_BUDGET_CHARS, perChangeChars = REVIEW_PER_CHANGE_CHARS } = {}) {
  const multi = tasks.length > 1;
  const taskBlocks = [];
  let used = 0;
  for (const t of tasks) {
    const diffs = (t.events || []).filter((e) => e.type === 'diff').map((e) => e.diff || '').filter(Boolean);
    let changesText;
    if (diffs.length) {
      let joined = diffs.join('\n');
      if (joined.length > perChangeChars) joined = joined.slice(0, perChangeChars) + '\n[diff truncated — run `git diff HEAD` for the full change]';
      changesText = joined;
    } else {
      changesText = '(no inline diff captured — run `git diff HEAD` / `git show HEAD` to see the changes)';
    }
    const block = [
      `<task id="${t.taskId || 'task'}">`,
      `<request>${clip(String(t.userRequest || ''), 600)}</request>`,
      `<summary>${clip(String(t.summary || ''), 600)}</summary>`,
      '<changes>', changesText, '</changes>',
      '</task>',
    ].join('\n');
    taskBlocks.push(block);
    used += block.length;
  }
  // Context: every non-diff event across the tasks, newest-first, until the
  // remaining budget is spent. Presented chronologically with an omitted marker.
  const allContext = [];
  for (const t of tasks) for (const ev of (t.events || [])) {
    if (ev.type === 'diff') continue;
    const line = fmtContextEvent(ev);
    if (line) allContext.push(line);
  }
  let remaining = Math.max(0, budgetChars - used - 200);
  const picked = [];
  for (let i = allContext.length - 1; i >= 0; i--) {
    const ln = allContext[i];
    if (ln.length + 1 > remaining) { picked.unshift(`[... ${i + 1} earlier step(s) omitted — use read / git diff if needed ...]`); break; }
    picked.unshift(ln);
    remaining -= ln.length + 1;
  }
  const parts = [];
  parts.push(multi
    ? `You are reviewing ${tasks.length} completed tasks. Review EACH and emit a per-task verdict (see OUTPUT FORMAT).`
    : 'You are reviewing the completed task below.');
  parts.push(taskBlocks.join('\n'));
  if (picked.length) {
    parts.push('<context note="earlier steps, for understanding only — NOT what you are reviewing">');
    parts.push(picked.join('\n'));
    parts.push('</context>');
  }
  return parts.join('\n');
}

// Parse per-task verdicts out of a (tool-call-stripped) reviewer response.
// Adds to `verdicts` (Map taskId → {pass, concerns}). Returns count added.
function parseReviewVerdicts(prose, taskIds, verdicts) {
  let added = 0;
  const add = (id, v) => { if (id && taskIds.includes(id) && !verdicts.has(id)) { verdicts.set(id, v); added++; } };
  let m;
  const passRe = /<review\s+task="([^"]+)"\s+pass\s*\/?>/gi;
  while ((m = passRe.exec(prose)) !== null) add(m[1], { pass: true });
  const failRe = /<review\s+task="([^"]+)"\s+fail\s*>([\s\S]*?)<\/review>/gi;
  while ((m = failRe.exec(prose)) !== null) add(m[1], { pass: false, concerns: m[2].trim() });
  // Short form (no task= attribute). For a single task it attributes to that
  // task. For MULTIPLE tasks, if the model used the short form (and no per-task
  // verdict matched), apply it to ALL unruled tasks rather than letting them
  // fall through to default-pass — a short-form <review-fail> in a multi-task
  // review must NOT be silently swallowed into a pass (fail-open).
  if (added === 0) {
    const failM = prose.match(/<review-fail>([\s\S]*)<\/review-fail>/i);  // greedy: survives embedded close tags
    const passM = /<review-pass\s*\/?>/i.test(prose);
    if (failM) { for (const id of taskIds) add(id, { pass: false, concerns: failM[1].trim() }); }
    else if (passM) { for (const id of taskIds) add(id, { pass: true }); }
  }
  return added;
}

// Fallback when the reviewer emits a PROSE verdict (a Markdown "Final Verdict"
// report) instead of the <review-pass>/<review-fail> tag. Classify the prose so
// a prose FAIL is NOT silently turned into a default pass (fail-closed bias).
// Returns { pass, lowConfidence } or null when there is no signal at all.
function inferVerdictFromProse(prose) {
  const t = String(prose || '').toLowerCase();
  if (!t.trim()) return null;
  const failRe = /\b(review[- ]?fail|fail(ed|s|ure)?|not\s+(complete|completed|done|fulfilled)|incomplete|does not (pass|meet)|issues? (found|remain)|problems? found|must (fix|be fixed)|should (fix|be fixed)|reject(ed)?|blocking|regression)\b/;
  const passRe = /\b(review[- ]?pass|successfully completed|task (is )?(successfully )?(complete|completed|done)|find this task (successfully )?completed|looks good|lgtm|no issues|approved|ship it|correct and complete|passes( review)?)\b/;
  const failHit = failRe.test(t);
  const passHit = passRe.test(t);
  if (failHit) return { pass: false, lowConfidence: true };   // any failure language → fail-closed (even if pass words also appear)
  if (passHit) return { pass: true, lowConfidence: true };
  return null;
}

// Surface a manager-pass parse miss (the model finished but emitted no
// parseable structured tag, so the pass fell back to its safe default). The
// raw model text is carried so the UI can show it for prompt-tuning against
// whatever model is deployed. Best-effort; never throws.
function emitDiag(onDiag, kind, reason, raw) {
  try { onDiag?.({ kind, reason, raw: String(raw || '').slice(0, 4000) }); } catch {}
}
// The last assistant turn in a message list (for max-turns diagnostics).
function lastAssistantText(messages) {
  for (let i = messages.length - 1; i >= 0; i--) if (messages[i]?.role === 'assistant') return messages[i].content || '';
  return '';
}

// Read-only PowerShell screen for the manager passes. <powershell> is allowed
// (they need `git diff`/`git log`/`git show`/`git status`, builds, tests,
// Get-*/Select-String) but the read-only guarantee must be ENFORCED, not just
// requested — so block obviously-mutating commands. Conservative denylist; a
// safety screen, not a sandbox. Returns an error string to block, or null.
const SHELL_DENY = [
  /\bgit\s+(add|commit|checkout|switch|reset|clean|stash|rm|mv|restore|apply|push|pull|fetch|merge|rebase|cherry-pick|revert|tag|init|config|branch\s+-)/i,
  /\b(Remove-Item|Rename-Item|Move-Item|New-Item|Set-Content|Add-Content|Out-File|Set-ItemProperty|Clear-Content|Copy-Item|Clear-Item|Tee-Object)\b/i,
  /\b(mkdir|rmdir|del|erase|ren|rd|md)\b/i,
  /(^|\s)(rm|mv|cp|ni|sc|ac|move|copy|ren)(\s|$)/i,
  /(^|\s)>>?/,                                            // output redirection
  /\b(Start-Process|Stop-Process|Restart-Computer|Stop-Computer|Set-Location|Push-Location|Pop-Location|chdir)\b/i,
  /(^|\s)cd(\s|$)/i,
  /\b(Invoke-WebRequest|Invoke-RestMethod|iwr|irm|curl|wget)\b/i,
];
function screenReadOnlyShell(command) {
  const cmd = String(command || '');
  if (!cmd.trim()) return null;
  for (const re of SHELL_DENY) {
    if (re.test(cmd)) return `ERROR: blocked — this is a READ-ONLY pass; the PowerShell command appears to mutate state or have side effects. Use only read-only inspection (git diff/log/show/status, Get-Content, Select-String, a non-mutating compile/test).`;
  }
  return null;
}

/**
 * Review one or more completed tasks in a SINGLE reviewer session.
 *   tasks: [{ taskId, userRequest, summary, events }]  (events = the task's
 *          whole-task chat events; diffs are the review target)
 * Returns { verdicts: [{ taskId, pass, concerns }] } — one per task (a task
 * the reviewer never ruled on defaults to pass, so the user isn't blocked).
 */
// ── Shared read-only agent turn loop ───────────────────────────────────────
// The common spine of every read-only pass (reviewer / acceptance / intake /
// consolidation): assemble the request the Gemini-safe way (assembleConvo — the
// system prompt rides the LATEST user message as a <system-reminder>, re-applied
// each turn; NEVER role:'system'), call the model, parse <remote-workspace> tool
// blocks, run the read-only allowlist (with the shell screen + path resolution),
// format results, and loop. Per-pass behavior is supplied via hooks:
//   interpret({text, prose, allowed, blocked, turn, transcript})
//       -> { finish:true, result }   terminal (verdict/card/facts found)
//        | { inject: [msgs] }        push messages + loop (e.g. reviewer re-prompt)
//        | {}                        run the allowed tools + loop (default)
//     MUST finish-or-inject when allowed.length === 0 (no executable tools).
//   onTokens(totalTokens)            per-turn cumulative usage (intake spinner)
//   onTools({calls, results, turn})  after tools run (intake live streaming)
//   onMaxTurns({transcript}) -> result   when the turn budget is exhausted
// This is the (read-only) turn engine; the worker's task lifecycle layers onto a
// fuller engine in a later phase.
async function runEngine({
  adapter, model, signal, onLog, label = 'pass',
  systemPrompt, priming = [], seedMessages = [],
  allowedVerbs, maxTurns, resolvePath = (p) => p, suppressTags = [], cwd = null,
  toolCtx = null,           // (call) -> the ctx passed to execute(); default = read-only { signal, cwd }
  extraTools = {},          // plugin tools available to execute()
  shellScreen = null,       // (command) -> error|null; the read-only powershell screen (null = unscreened, e.g. a write-capable sub-agent)
  serialVerbs = new Set(),  // verbs whose blocks serialize (MUTATING_VERBS for a write-capable engine; empty for read-only)
  interpret, onTokens = null, onTools = null, onProse = null, onProseEnd = null, onMaxTurns = null,
}) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  const mkCtx = toolCtx || (() => ({ signal, cwd }));   // default: the read-only execute ctx
  const transcript = [...seedMessages];   // canonical history (no system reminder)
  let cumTokens = 0;
  for (let turn = 1; turn <= maxTurns; turn++) {
    if (signal?.aborted) { const e = new Error('aborted'); e.name = 'AbortError'; throw e; }
    const convo = assembleConvo({ systemPrompt, priming, messages: transcript });
    const t0 = Date.now();
    const allowed = [];   // allowed calls that ran this turn (for interpret)
    const blocked = [];   // disallowed calls dropped this turn
    // Read-only per-call execution, injected into the SHARED streaming engine —
    // the same runStreamingTurn the worker uses, so a mini-chat streams its tool
    // activity live, mid-turn, exactly like the main chat. Returns null for a
    // disallowed verb (dropped, not run) so it doesn't reach the model as a result.
    const runCall = async (c) => {
      if (!allowedVerbs.has(c.verb)) { blocked.push(c); return null; }
      allowed.push(c);
      let status, content;
      const shellErr = (shellScreen && c.verb === 'powershell') ? shellScreen(c.args?.command) : null;
      if (shellErr) { status = 'error'; content = shellErr; }
      else {
        try {
          if (c.args && typeof c.args.path === 'string') c.args.path = resolvePath(c.args.path);
          const raw = await execute(c.name, c.args, extraTools, mkCtx(c));   // toolCtx: read-only {signal,cwd} by default, or a write-capable ctx (worktree cwd) for a sub-agent
          content = (typeof raw === 'string' && raw.startsWith('ERROR:')) ? raw : extractContent(raw);
          status = (typeof raw === 'string' && raw.startsWith('ERROR:')) ? 'error' : 'ok';
          log(`${label} turn ${turn}: ${c.verb}(${JSON.stringify(c.args).slice(0, 80)}) -> ${status} ${content.length}b`);
        } catch (e) { status = 'error'; content = 'ERROR: ' + (e?.message ?? String(e)); }
      }
      // Stream this call's call+result live (one per call) for the mini-chat UI.
      if (onTools) await onTools({ calls: [c], results: [{ verb: c.verb, status, content }], turn });
      return { call: c, verb: c.verb, status, content };
    };

    const { text, orderedResults } = await runStreamingTurn({
      adapter, model, convo, signal,
      serialVerbs,              // empty (read-only) or MUTATING_VERBS (write-capable sub-agent)
      suppressTags,             // e.g. the intake suppresses <requirement-card> from the prose stream
      onProseChunk: onProse ? ({ segmentIndex, content }) => onProse({ turn, segmentIndex, content }) : undefined,
      onProseSegmentEnd: onProseEnd ? ({ segmentIndex, content }) => onProseEnd({ turn, segmentIndex, content }) : undefined,
      onUsage: onTokens
        ? (u) => { const uu = u || {}; cumTokens += (uu.total_tokens || ((uu.prompt_tokens || 0) + (uu.completion_tokens || 0)) || 0); return onTokens(cumTokens); }
        : undefined,
      runCall,
    });
    log(`${label} turn ${turn}: ${text.length} chars in ${Date.now() - t0}ms`);

    const results = orderedResults.map((r) => ({ verb: r.verb, status: r.status, content: r.content }));
    if (blocked.length > 0) {
      results.push({ verb: 'system', status: 'error', content: `${label} is read-only — dropped ${blocked.length} call(s): ${blocked.map((c) => c.verb).join(', ')}.` });
    }
    const prose = text.replace(/<remote-workspace\b[^>]*>[\s\S]*?<\/remote-workspace>/gi, '').trim();
    // interpret runs AFTER the turn's tools have streamed: { finish } terminal,
    // { inject } pushes messages + loops, anything else loops with the results.
    const decision = (await interpret({ text, prose, allowed, blocked, results, turn, transcript })) || {};
    if (decision.finish) return decision.result;
    if (decision.inject) { transcript.push(...decision.inject); continue; }
    transcript.push({ role: 'assistant', content: text });
    transcript.push({ role: 'user', content: formatToolResults(results) });
  }
  return onMaxTurns ? await onMaxTurns({ transcript }) : null;
}

// runReadonlyPass — the READ-ONLY configuration of runEngine, used by every manager
// pass (reviewer / acceptance / intake / consolidation): powershell is screened to
// read-only, tools run with the bare { signal, cwd } ctx, and nothing serializes. A thin
// wrapper so those call sites are unchanged; a write-capable sub-agent is the inverse
// config (full tool ctx with the worktree cwd, no shellScreen, serialVerbs = MUTATING_VERBS).
function runReadonlyPass(opts) {
  return runEngine({ ...opts, shellScreen: screenReadOnlyShell });
}

// ── Sub-agent runner (the orchestrator's runWorker) ────────────────────────────
// The WRITE configuration of runEngine: a store-free, seed-driven worker that does
// ONE focused task to completion with the full tool set (read/write/edit/shell),
// isolated by its git WORKTREE (the caller passes the worktree as projectDir/cwd), and
// returns its final prose as a bounded { content } result. No tasks-v2 store, no task-op
// lifecycle — a sub-agent is ephemeral: do the task, return the result, drop its context.
// The orchestrator forks/spawns these; the server/bridge supplies adapter+model+worktree.
const SUBAGENT_SYSTEM_PROMPT = `You are a focused sub-agent working ONE task to completion. Ground every change by reading the file first. When the task is DONE, reply with a concise summary of what you changed and issue NO tool calls — that ends your run and returns the summary to the manager.

Use <remote-workspace> tool blocks with EXACTLY these formats (read commands are self-closing; write/edit hold the content in the BODY, not in attributes):
  <list path="DIR"/>
  <read path="FILE" offset="N" limit="N"/>
  <search pattern="REGEX" path="DIR" glob="GLOB" flags="i"/>
  <find pattern="GLOB"/>
  <info path="FILE"/>
  <write path="FILE">
  full file content goes here
  </write>
  <edit path="FILE">
  <find>exact string to find (must occur exactly once)</find>
  <replace>replacement string</replace>
  </edit>
  <powershell>git status</powershell>
The workspace replies with <remote-workspace-result> blocks — read real results before continuing, and never invent file or directory names you have not seen.`;
const SUBAGENT_VERBS = new Set(['read', 'write', 'edit', 'list', 'search', 'find', 'info', 'powershell']);

async function runSubAgent({
  adapter, model, signal, projectDir, seedMessages, topic = 'sub-agent',
  systemPrompt = SUBAGENT_SYSTEM_PROMPT,
  requireBranch, requireApproval, getGitLabCredentials,
  maxTurns = DEFAULT_MAX_TURNS, onLog = null, onChatEvent = null,
}) {
  if (!projectDir) throw new Error('runSubAgent: projectDir required');
  // Isolation is the caller's git WORKTREE: projectDir is the sub-agent's own worktree
  // (its own dir + branch), so writes are physically isolated and integrate-time merge
  // conflicts are the deny-on-stale signal. Tool writes resolve under projectDir via cwd.
  return await runEngine({
    adapter, model, signal, label: topic, systemPrompt, seedMessages,
    allowedVerbs: SUBAGENT_VERBS, serialVerbs: MUTATING_VERBS, shellScreen: null,
    maxTurns, cwd: projectDir,
    toolCtx: (c) => ({
      callId: c.id,
      onProgress: (chunk) => { try { onChatEvent?.({ type: 'tool-progress', callId: c.id, chunk }); } catch {} },
      requireBranch, requireApproval, getGitLabCredentials,
      signal, cwd: projectDir,
    }),
    // Surface the sub-agent's live activity (tool calls + prose) so the caller can show
    // "what it's doing" on a side channel. onChatEvent is the only sink; no-op without it.
    onTools: onChatEvent ? ({ calls, results }) => {
      for (let i = 0; i < (calls || []).length; i++) {
        const c = calls[i]; const r = (results && results[i]) || {};
        const a = c.args || {};
        try {
          onChatEvent({ type: 'tool-call', name: c.verb || c.name, verb: c.verb || c.name, callId: c.id, summary: a.path || a.pattern || a.query || a.cmd || '' });
          onChatEvent({ type: 'tool-result', name: c.verb || c.name, callId: c.id, status: r.status, content: typeof r.content === 'string' ? r.content : '' });
        } catch {}
      }
    } : undefined,
    onProseEnd: onChatEvent ? ({ content }) => { if (content && content.trim()) { try { onChatEvent({ type: 'assistant-text', content }); } catch {} } } : undefined,
    // a sub-agent finishes when a turn issues NO tools; its bounded result is the
    // final prose (as { content } so it reads cleanly through the orchestrator card).
    interpret: ({ allowed, prose, text }) => (allowed.length === 0 ? { finish: true, result: { content: prose || text } } : {}),
    onLog,
  });
}

// A repo-relative path resolver: try the project root first, then each changed
// sub-repo root. Returns an ABSOLUTE path when found (so the tool resolves it
// regardless of process.cwd() — these read-only passes are cwd-independent), and
// the original relative path if nothing matches.
function makeRepoResolver(projectDir, repoRoots = []) {
  return (p) => {
    if (typeof p !== 'string' || !p || path.isAbsolute(p)) return p;
    try { if (projectDir && existsSync(path.join(projectDir, p))) return path.join(projectDir, p); } catch {}
    for (const root of repoRoots) { try { if (existsSync(path.join(root, p))) return path.join(root, p); } catch {} }
    return p;
  };
}

async function runReviewer({ adapter, model, tasks, signal, onLog, onDiag, repoRoots = [], projectDir = null }) {
  const taskList = Array.isArray(tasks) ? tasks : [];
  const taskIds = taskList.map((t) => t.taskId);
  const verdicts = new Map();
  const finalize = () => {
    for (const id of taskIds) if (!verdicts.has(id)) verdicts.set(id, { pass: true, unparseable: true });
    return { verdicts: taskIds.map((id) => ({ taskId: id, ...verdicts.get(id) })) };
  };
  if (taskList.length === 0) return { verdicts: [] };

  const log = (m) => { try { onLog?.(m); } catch {} };
  const userBlock = ['<review-target>', buildBudgetedReviewInput(taskList), '</review-target>'].join('\n');
  // Cumulative corrective re-prompts across the whole review (NOT reset per turn)
  // so the retry budget is hard-bounded.
  let corrections = 0;
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'reviewer',
    systemPrompt: REVIEWER_SYSTEM_PROMPT,
    seedMessages: [{ role: 'user', content: userBlock }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots),
    cwd: projectDir,
    interpret: ({ text, allowed, turn }) => {
      // Tool calls FIRST: a verdict is only honored in a turn with no executable
      // tool calls (so a tentative verdict + verification reads in the same turn
      // doesn't short-circuit the reads).
      if (allowed.length > 0) return {};
      const proseOnly = text.replace(/<remote-workspace\b[^>]*>[\s\S]*?<\/remote-workspace>/gi, '');
      parseReviewVerdicts(proseOnly, taskIds, verdicts);
      if (taskIds.every((id) => verdicts.has(id))) return { finish: true, result: finalize() };
      // No parseable tag for some task(s). FIRST try: re-prompt for the tag (some
      // models reply with a Markdown "Final Verdict" report instead). Only after
      // the correction budget is spent do we infer from prose / default to pass.
      // `turn < REVIEWER_MAX_TURNS` avoids burning the final turn on a re-prompt.
      const unruled = taskIds.filter((id) => !verdicts.has(id));
      if (corrections < REVIEWER_MAX_CORRECTIONS && turn < REVIEWER_MAX_TURNS) {
        corrections++;
        const tagHelp = taskList.length === 1
          ? '<review-pass/>\nOR\n<review-fail>specific, actionable concerns</review-fail>'
          : unruled.map((id) => `<review task="${id}" pass/>   (or)   <review task="${id}" fail>concerns</review>`).join('\n');
        const correction = `Your last turn did not include a parseable verdict tag for ${unruled.length} task(s): ${unruled.join(', ')}. A Markdown report or "Final Verdict" prose is NOT read — only the literal tag is. Reply with ONLY the verdict tag(s) below and nothing else:\n${tagHelp}`;
        // LOG-ONLY (no emitDiag): the corrective re-prompt is routine self-healing;
        // surfacing it would look like a failed second review if the retry succeeds.
        log(`reviewer turn ${turn}: no verdict tag; corrective re-prompt ${corrections}/${REVIEWER_MAX_CORRECTIONS} for ${unruled.join(', ')} — raw: ${proseOnly.slice(0, 200).replace(/\s+/g, ' ')}`);
        return { inject: [{ role: 'assistant', content: text }, { role: 'user', content: correction }] };
      }
      // Budget spent. Try to INFER pass/fail from prose (a prose FAIL must NOT
      // become a silent pass); else default the rest to pass so the user isn't blocked.
      const inferred = inferVerdictFromProse(proseOnly);
      if (inferred) {
        let n = 0;
        for (const id of taskIds) if (!verdicts.has(id)) { verdicts.set(id, { ...inferred, inferredFromProse: true }); n++; }
        emitDiag(onDiag, 'reviewer', `reviewer emitted a PROSE verdict instead of a <review-${inferred.pass ? 'pass' : 'fail'}> tag for ${n} task(s); inferred ${inferred.pass ? 'PASS' : 'FAIL'} from the text (low confidence — verify)`, proseOnly);
        if (taskIds.every((id) => verdicts.has(id))) return { finish: true, result: finalize() };
      }
      log(`reviewer turn ${turn}: no tool calls; ${verdicts.size}/${taskIds.length} tasks ruled on — defaulting the rest to pass`);
      emitDiag(onDiag, 'reviewer', `no <review-pass>/<review-fail> verdict for ${taskIds.length - verdicts.size} of ${taskIds.length} task(s); defaulted them to pass`, proseOnly);
      return { finish: true, result: finalize() };
    },
    onMaxTurns: ({ transcript }) => {
      log(`reviewer: hit max turns (${REVIEWER_MAX_TURNS}); ${verdicts.size}/${taskIds.length} ruled on — rest default to pass.`);
      if (verdicts.size < taskIds.length) emitDiag(onDiag, 'reviewer', `hit ${REVIEWER_MAX_TURNS}-turn budget with ${taskIds.length - verdicts.size} task(s) unruled; defaulted them to pass`, lastAssistantText(transcript));
      return finalize();
    },
  });
}

// ── Acceptance check (assignment-level requirement fulfilment) ──────────────
// A HIGHER-altitude sibling of the code reviewer: the reviewer asks "is THIS
// diff correct?" per task; acceptance asks "does the FINISHED ASSIGNMENT fulfil
// the ORIGINAL requirement?" Read-only, budgeted, and FAILS SAFE to
// not-fulfilled (the inverse of the reviewer's default-pass) — a wrong auto-
// "done" on a maintained system is the expensive error.
const ACCEPTANCE_SYSTEM_PROMPT = `You are an acceptance checker. A developer was ASSIGNED a piece of work with a captured REQUIREMENT (and maybe acceptance criteria). A coding agent did the work; each of its tasks was already code-reviewed for correctness. Your job is the HIGHER-LEVEL check the per-task reviewer cannot make: does the FINISHED WORK, as a whole, FULFIL the original requirement?

Check, against the requirement:
- COMPLETENESS — is every part actually addressed? List anything MISSING or only partially done.
- DRIFT — did the work change things outside the requirement, or solve a different problem?
- INTENT — does the cumulative change actually achieve what was asked?
Do NOT re-litigate diff-level bugs/style — the per-task code review caught those, EXCEPT for any task marked ESCALATED below (its review hit the round limit with concerns left UNRESOLVED). For an escalated task, treat its concerns as still OPEN and weigh whether the requirement is genuinely fulfilled despite them, rather than assuming review handled it. You judge fulfilment, not correctness.

VERIFY read-only (same tools as the code reviewer; emit calls inside a <remote-workspace> block):
  <read path="FILE"/>  <list path="DIR"/>  <search pattern="REGEX" path="DIR"/>  <find pattern="GLOB"/>  <info path="FILE"/>
  <powershell>git diff HEAD~1 HEAD</powershell> / git log / a cheap compile or the specific test, where it confirms fulfilment.
You may inspect, diff, and run read-only checks; you MUST NOT modify anything.

When you have enough information, emit ONE verdict in a turn with NO tool calls:
  <accept-fulfilled>one-line rationale</accept-fulfilled>
OR
  <accept-unfulfilled>
  <missing>what is not done / only partial — be specific; name files, criteria, the gap</missing>
  <drift>work done outside the requirement (or leave empty)</drift>
  <rationale>why it does not fulfil the requirement yet</rationale>
  </accept-unfulfilled>

Be conservative: declare fulfilled ONLY when you are confident the requirement is met. If a requirement cannot be verified from what you can read (e.g. runtime/deploy behaviour), say so in <missing> and declare unfulfilled rather than guess.`;

function buildAcceptanceInput(assignment, tasks) {
  const parts = [
    '<requirement>', clip(String(assignment?.requirement || '(no requirement captured)'), 4000), '</requirement>',
  ];
  if (assignment?.acceptanceCriteria) {
    parts.push('<acceptance-criteria>', clip(String(assignment.acceptanceCriteria), 4000), '</acceptance-criteria>');
  }
  const verdicts = (tasks || []).filter((t) => t.verdict).map((t) => {
    const v = t.verdict;
    if (v.escalated) return `  ${t.taskId}: code-review ESCALATED (concerns unresolved after review rounds) — ${clip(String(v.concerns || ''), 300)}`;
    if (v.pass === false) return `  ${t.taskId}: code-review FAIL — ${clip(String(v.concerns || ''), 300)}`;
    return `  ${t.taskId}: code-review pass`;
  });
  if (verdicts.length) parts.push('<per-task-code-review>', verdicts.join('\n'), '</per-task-code-review>');
  parts.push('<work note="the tasks done for this assignment, with their changes; earlier steps are context-only">',
    buildBudgetedReviewInput(tasks || [], { budgetChars: 30000 }),
    '</work>');
  return parts.join('\n');
}

function parseAcceptanceVerdict(prose) {
  if (/<accept-fulfilled\s*\/>/i.test(prose)) return { fulfilled: true, missing: '', drift: '', rationale: '' };
  const fm = prose.match(/<accept-fulfilled>([\s\S]*?)<\/accept-fulfilled>/i);
  if (fm) return { fulfilled: true, missing: '', drift: '', rationale: fm[1].trim() };
  const um = prose.match(/<accept-unfulfilled>([\s\S]*)<\/accept-unfulfilled>/i);
  if (um) {
    const body = um[1];
    const grab = (tag) => { const m = body.match(new RegExp('<' + tag + '>([\\s\\S]*?)</' + tag + '>', 'i')); return m ? m[1].trim() : ''; };
    return { fulfilled: false, missing: grab('missing'), drift: grab('drift'), rationale: grab('rationale') || body.trim() };
  }
  return null;
}

/**
 * Acceptance check for a completed assignment.
 *   assignment: { id, requirement, acceptanceCriteria }
 *   tasks:      [{ taskId, userRequest, summary, events, verdict }]  (verdict =
 *               the per-task code-review result, optional)
 * Returns { fulfilled, missing, drift, rationale, autoVerifyFailed? }. Fails
 * safe to fulfilled:false when it can't reach a verdict.
 */
async function runAcceptance({ adapter, model, assignment, tasks = [], signal, onLog, onDiag, repoRoots = [], projectDir = null }) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  const NOT_VERIFIED = (why) => ({ fulfilled: false, missing: 'could not auto-verify', drift: '', rationale: why, autoVerifyFailed: true });
  const userBlock = ['<acceptance-target>', buildAcceptanceInput(assignment || {}, tasks), '</acceptance-target>'].join('\n');
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'acceptance',
    systemPrompt: ACCEPTANCE_SYSTEM_PROMPT,
    seedMessages: [{ role: 'user', content: userBlock }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots),
    cwd: projectDir,
    interpret: ({ prose, allowed, turn }) => {
      if (allowed.length > 0) return {};   // run tools, loop
      const v = parseAcceptanceVerdict(prose);
      if (v) return { finish: true, result: v };
      log(`acceptance turn ${turn}: no parseable verdict — failing safe to not-fulfilled`);
      emitDiag(onDiag, 'acceptance', 'no <accept-fulfilled>/<accept-unfulfilled> verdict — failed safe to not-fulfilled', prose);
      return { finish: true, result: NOT_VERIFIED('The acceptance check produced no clear verdict; review the work manually.') };
    },
    onMaxTurns: ({ transcript }) => {
      log(`acceptance: hit max turns (${REVIEWER_MAX_TURNS}) without a verdict — failing safe to not-fulfilled.`);
      emitDiag(onDiag, 'acceptance', `hit ${REVIEWER_MAX_TURNS}-turn budget without a verdict — failed safe to not-fulfilled`, lastAssistantText(transcript));
      return NOT_VERIFIED('Acceptance ran out of verification turns without a verdict; review the work manually.');
    },
  });
}

// ── Sub-agent acceptance (orchestrator review loop, ORCHESTRATOR.md R4) ─────
// The Acceptance AGENT for a delegated sub-agent: a read-only reviewer that decides
// whether to APPROVE the sub-agent's worktree work or REJECT it with feedback. The
// manager never reads the sub-agent's transcript — this agent reads the work via
// read-only tools (the worktree IS its project root) + a seeded rollup of the sub-agent's
// tasks, and returns ONLY a verdict. FAILS SAFE TO REJECT (a wrong auto-approve
// integrates+despawns — the expensive error), the same conservatism as runAcceptance.
const SA_ACCEPT_MAX_TURNS = REVIEWER_MAX_TURNS;
const SA_ACCEPTANCE_SYSTEM_PROMPT = `You are an acceptance reviewer for ONE sub-agent's work. A manager handed a sub-agent a request; the sub-agent worked in an isolated checkout (which is now YOUR read-only project root) and reported a result. Decide whether to APPROVE its work or REJECT it with actionable feedback.

You are given:
  <user-intent> — what the user actually wants (an excerpt of the manager's conversation)
  <original-request> — the exact brief the sub-agent was given
  <subagent-work> — the sub-agent's tasks + summaries (a read-only rollup)

VERIFY read-only (emit calls inside a <remote-workspace> block; the worktree is your project root):
  <list path="."/>  <read path="FILE"/>  <search pattern="REGEX" path="DIR"/>  <find pattern="GLOB"/>  <info path="FILE"/>
  <powershell>git log --oneline</powershell> to see ALL the sub-agent's commits (it may have revised across rounds — do NOT assume one commit), then read the changed files directly and/or <powershell>git show <hash></powershell> per commit to see the cumulative change.
You may inspect, diff, and run read-only checks; you MUST NOT modify anything (write/edit are unavailable; only read-only git commands run).

Judge against the request + the user's intent:
- COMPLETENESS — is everything the request asked for actually done? Name anything missing or partial.
- CORRECTNESS-AT-LARGE — does the change actually achieve what was asked (not just compile)?
- DRIFT — did it change things outside the request?

When you have enough information, emit ONE verdict in a turn with NO tool calls:
  <sa-approve>one-line rationale</sa-approve>
OR
  <sa-reject>specific, actionable feedback: name the files, the gap, and what to do — the sub-agent will act on this verbatim</sa-reject>

Be conservative: APPROVE only when you are confident the request is met. If you cannot verify it from what you can read, REJECT and say what to check. Reject is cheap (the sub-agent resumes); a wrong approve is not.`;

function parseSaVerdict(prose) {
  // REJECT WINS (fail-safe): a reject tag anywhere → rejection, even alongside an approve
  // tag (a confused dual-tag response) or when the approve tag is merely MENTIONED inside
  // the reject feedback. A wrong auto-approve integrates + despawns; reject is cheap.
  const rm = prose.match(/<sa-reject>([\s\S]*?)<\/sa-reject>/i);
  if (rm) return { approved: false, feedback: rm[1].trim() || 'rejected — re-examine the work against the request' };
  const am = prose.match(/<sa-approve>([\s\S]*?)<\/sa-approve>/i);
  if (am) return { approved: true, feedback: am[1].trim() };
  if (/<sa-approve\s*\/>/i.test(prose)) return { approved: true, feedback: '' };
  return null;
}

/**
 * Acceptance review of a sub-agent's worktree work (the orchestrator's review loop).
 *   request:           the original brief handed to the sub-agent (string)
 *   conversationChunk: an excerpt of the manager conversation (string — what the user wants)
 *   workRollup:        a read-only rollup of the sub-agent's tasks/summaries (string; the
 *                      store under <worktree>/.code_boss is tool-blocked, so it's seeded)
 *   worktreePath:      the sub-agent's worktree = the read-only project root (cwd)
 * Returns { approved, feedback }. Fails SAFE to REJECT when it can't reach a verdict.
 */
async function runSubAgentAcceptance({ adapter, model, signal, onLog, onDiag, request = '', conversationChunk = '', workRollup = '', worktreePath, repoRoots = [] }) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  const REJECT_SAFE = (why) => ({ approved: false, feedback: why });
  const userBlock = [
    '<user-intent note="what the user actually wants (manager conversation excerpt)">', clip(String(conversationChunk || '(none)'), 12000), '</user-intent>',
    '<original-request note="the exact brief handed to the sub-agent">', clip(String(request || '(none)'), 4000), '</original-request>',
    '<subagent-work note="the sub-agent\'s tasks + summaries (read-only rollup)">', clip(String(workRollup || '(none)'), 12000), '</subagent-work>',
    'The sub-agent\'s worktree is your project root — start with <list path="."/> and `git log --oneline` (it may have revised across rounds — review ALL its commits, not just the last), then read the changed files + verify against the request.',
  ].join('\n');
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'sa-acceptance',
    systemPrompt: SA_ACCEPTANCE_SYSTEM_PROMPT,
    seedMessages: [{ role: 'user', content: userBlock }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: SA_ACCEPT_MAX_TURNS,
    resolvePath: makeRepoResolver(worktreePath, repoRoots),
    cwd: worktreePath,
    interpret: ({ prose, allowed, turn }) => {
      if (allowed.length > 0) return {};   // tools ran — loop; verdict only on a no-tool turn
      const v = parseSaVerdict(prose);
      if (v && v.approved === false) return { finish: true, result: v };   // a REJECT is always safe to honor
      // For an APPROVE (or no verdict): if the prose shows tool INTENT (a malformed/unclosed
      // <remote-workspace> block that parsed as prose → allowed=[]), the agent didn't mean to
      // give a final verdict this turn — loop, so a tentative <sa-approve/> can't short-circuit
      // before the reads run (bounded by maxTurns → fail-safe reject).
      if (turn < SA_ACCEPT_MAX_TURNS && /<remote-workspace|<(read|write|edit|list|search|find|info|powershell)[\s/>]/i.test(prose)) {
        log(`sa-acceptance turn ${turn}: tool-intent without a clean verdict — looping for a clean verdict`);
        return {};
      }
      if (v) return { finish: true, result: v };   // a clean APPROVE with no tool-intent
      log(`sa-acceptance turn ${turn}: no parseable verdict — failing safe to REJECT`);
      emitDiag(onDiag, 'sa-acceptance', 'no <sa-approve>/<sa-reject> verdict — failed safe to reject', prose);
      return { finish: true, result: REJECT_SAFE('The acceptance check produced no clear verdict; the sub-agent should re-examine its work against the request.') };
    },
    onMaxTurns: ({ transcript }) => {
      log(`sa-acceptance: hit max turns (${SA_ACCEPT_MAX_TURNS}) without a verdict — failing safe to REJECT.`);
      emitDiag(onDiag, 'sa-acceptance', `hit ${SA_ACCEPT_MAX_TURNS}-turn budget without a verdict — failed safe to reject`, lastAssistantText(transcript));
      return REJECT_SAFE('Acceptance ran out of verification turns; the sub-agent should re-examine its work.');
    },
  });
}

// ── Manager turn (the one-call router, ORCHESTRATOR.md) ─────────────────────
// When the foreground forks (a task is running as a background sub-agent), the freed
// manager answers the user in ONE read-only turn — OR hands a real task to a sub-agent by
// emitting <delegate>brief</delegate>. No separate classifier call: the manager's own turn
// IS the router; the harness ratifies (prose → reply; <delegate> → spawn). Read-only is
// structural (write/edit not in the allowlist; powershell screened) so the manager can
// never touch the main dir — only delegation produces writes. Fails safe to DELEGATE.
const MANAGER_SYSTEM_PROMPT = `You are the MANAGER of a coding session. One or more focused tasks are already running as BACKGROUND sub-agents (each in its own isolated checkout) — do NOT touch their files or redo their work. The user just sent you a message. In ONE reply do exactly ONE of:

  • ANSWER it directly — for a question, a status check, a clarification, or conversation. You may inspect the repo READ-ONLY (<read>, <list>, <search>, <find>, <info>, and read-only git via <powershell>) to answer well. Write your reply as normal prose (NO <delegate> tag).

  • DELEGATE it — if it asks for real work (a code change, build, fix, or feature). Do NOT do the work yourself. Emit ONE block:
      <delegate>a SELF-CONTAINED brief for a sub-agent: the goal, the relevant files/areas, and what "done" looks like. The sub-agent receives ONLY this brief (not our conversation), so include the context it needs.</delegate>
    You may add a one-line note that you've started it.

You MUST NOT write, edit, or run mutating commands yourself — delegation is the only way work happens. When unsure whether it is a task, DELEGATE (a question handled as a task is cheap; a task mistaken for a question never gets done).`;
const MANAGER_PRIMING = [
  { role: 'user', content: 'is it almost done?' },
  { role: 'assistant', content: 'The background task is still on its first step; I\'ll surface the result here as soon as it lands.' },
  { role: 'user', content: 'also add a /health endpoint' },
  { role: 'assistant', content: 'On it.\n<delegate>Add a GET /health endpoint that returns HTTP 200 with body {status:"ok"}. Find the HTTP server/route setup (e.g. server.js / app.js), add the route next to the existing ones, keep them working. Done = GET /health returns 200 {status:"ok"}.</delegate>' },
];

function parseDelegate(prose) {
  const m = String(prose || '').match(/<delegate>([\s\S]*?)<\/delegate>/i);
  if (!m) return null;
  return m[1].trim() || null;
}

/**
 * The MANAGER turn: one read-only pass over a COPY of the conversation + the new user
 * message. Returns { action:'answer', text } (already streamed via onProse/onProseEnd) or
 * { action:'delegate', brief }. Fails safe to DELEGATE on no-verdict / max-turns.
 */
async function runManagerTurn({ adapter, model, signal, onLog, onDiag, messages = [], newMessage, subagentStatus = '', projectDir = null, repoRoots = [], onProse, onProseEnd, onTokens }) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  const status = subagentStatus ? `\n\n<background-sub-agents note="running now — you manage them, never touch their files">\n${clip(String(subagentStatus), 2000)}\n</background-sub-agents>` : '';
  const seed = [...(Array.isArray(messages) ? messages : []), { role: 'user', content: String(newMessage || '') }];
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'manager',
    systemPrompt: MANAGER_SYSTEM_PROMPT + status, priming: MANAGER_PRIMING,
    seedMessages: seed,
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots),
    cwd: projectDir,
    suppressTags: ['delegate'],   // the brief is parsed, not shown as streamed prose
    onTokens: onTokens ? (t) => onTokens(t) : undefined,
    onProse: onProse ? ({ turn, segmentIndex, content }) => onProse({ turn, segmentIndex, content }) : undefined,
    onProseEnd: onProseEnd ? ({ turn, segmentIndex, content }) => { const c = content.replace(/<delegate>[\s\S]*?<\/delegate>/i, '').trim(); if (c) onProseEnd({ turn, segmentIndex, content: c }); } : undefined,
    interpret: ({ prose, allowed }) => {
      // DELEGATE WINS — parse FIRST, even if the model also ran read-only tools this turn
      // (so a read-then-delegate reply delegates instead of looping forever).
      const brief = parseDelegate(prose);
      if (brief) return { finish: true, result: { action: 'delegate', brief } };
      if (allowed.length > 0) return {};   // read-only tools ran → loop, then answer/delegate
      return { finish: true, result: { action: 'answer', text: String(prose || '').trim() } };
    },
    onMaxTurns: ({ transcript }) => {
      log(`manager: hit max turns (${REVIEWER_MAX_TURNS}) — failing safe to DELEGATE the request`);
      emitDiag(onDiag, 'manager', `hit ${REVIEWER_MAX_TURNS}-turn budget without a clean answer/delegate — delegating`, lastAssistantText(transcript));
      return { action: 'delegate', brief: String(newMessage || '') };
    },
  });
}

// ── Adaptive cognition: the ASSESS turn (cognition.mjs is the controller) ────
// One read-only turn that either ANSWERS a trivial/conversational request directly OR emits
// an <evaluation> characterizing the problem (complexity/creativity/accuracy/size/mode +
// salience) so the deterministic policy can choose how hard to think. It does NOT solve the
// problem — it sizes it. Read-only is structural (write/edit not in the allowlist).
const ASSESS_SYSTEM_PROMPT = `You are the ASSESS step of an adaptive cognition system. Read the request and do EXACTLY ONE of:

  • ANSWER it directly — if it is trivial, conversational, or a quick factual/status question. Write your reply as normal prose (NO <evaluation> tag).

  • EVALUATE it — if it needs real work or real thought. Do NOT solve it; CHARACTERIZE it so the system can decide how hard to think and which strategy to use. Emit ONE block:
      <evaluation complexity="low|medium|high" creativity="low|high" accuracy="normal|critical" size="small|large" mode="build|debug|research|explain" salience="0.0-1.0">
        a SELF-CONTAINED brief: what needs doing / what to think about, with the context a fresh agent would need (it won't see this conversation).
      </evaluation>

RUBRIC (be honest — over-rating wastes effort, under-rating ships sloppy work):
  complexity — low: one file or one obvious change (the DEFAULT for an iterative edit/feature on an existing file). medium: a few related files/steps. high: ONLY genuinely separable work that spans multiple INDEPENDENT components. A vague or under-specified request is NOT "high" — it is under-specified: rate it by the SMALLEST sensible interpretation. Never inflate complexity because a request is ambiguous.
  creativity — low: one right answer (a known fix/change). high: a real design space with multiple viable approaches (architecture, refactor strategy, API design).
  accuracy   — critical: correctness/security-sensitive, hard to undo, or must be exactly right. normal: otherwise.
  size       — large: substantial output expected (many edits / long generation). small: otherwise.
  mode       — debug: finding a fault. research: gathering/understanding. explain: teaching. build: making changes (the default).
  salience   — how important/promising it is to spend effort here NOW (1.0 = clearly worth deep effort; lower = marginal/uncertain).

When a request is AMBIGUOUS, take the smallest on-task interpretation and do the obvious minimal thing — do NOT invent unrelated scope (e.g. never turn "let it run commands safely" into building an auth system). If it is too vague to act on at all, ANSWER asking one clarifying question instead of guessing a large build.

You MAY read the repo READ-ONLY (<read>/<list>/<search>/<find>/<info>) to assess accurately, but you MUST NOT change anything — assessment only.`;
const ASSESS_PRIMING = [
  { role: 'user', content: 'what does the auth middleware do?' },
  { role: 'assistant', content: 'It validates the JWT on each request and attaches the decoded user to req.user; requests without a valid token get a 401.' },
  { role: 'user', content: 'redesign how we handle auth across the app' },
  { role: 'assistant', content: '<evaluation complexity="high" creativity="high" accuracy="critical" size="large" mode="build" salience="0.9">\nRedesign the application\'s authentication. Survey the current approach (middleware, session/token flow, where user identity is read), weigh alternative designs, then propose and implement a coherent scheme. Security-sensitive and spans many files.\n</evaluation>' },
];

function parseEvaluation(prose) {
  const s = String(prose || '');
  let m = s.match(/<evaluation\b([^>]*)>([\s\S]*?)<\/evaluation>/i);
  let attrs, brief;
  if (m) { attrs = m[1]; brief = m[2].trim(); }
  else { m = s.match(/<evaluation\b([^>]*?)\/>/i); if (!m) return null; attrs = m[1]; brief = ''; }
  const attr = (name) => { const a = attrs.match(new RegExp(name + '\\s*=\\s*"([^"]*)"', 'i')); return a ? a[1].trim() : null; };
  const metrics = normalizeCognitionMetrics({ complexity: attr('complexity'), creativity: attr('creativity'), accuracy: attr('accuracy'), size: attr('size'), mode: attr('mode') });
  let salience = parseFloat(attr('salience'));
  salience = isFinite(salience) ? Math.max(0, Math.min(1, salience)) : 0.7;   // unspecified → assume worth doing
  return { metrics, salience, brief };
}

/**
 * The ASSESS turn: one read-only pass over the request → { action:'answer', text } (the model
 * answered) or { action:'evaluate', metrics, salience, brief } (hand to the cognition policy).
 * EVALUATION WINS (parsed before the tool-loop check). Fails safe to a single-worker evaluation.
 */
async function runCognitiveAssess({ adapter, model, signal, onLog, onDiag, messages = [], request, projectDir = null, repoRoots = [], onProse, onProseEnd, onTokens }) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  const seed = [...(Array.isArray(messages) ? messages : []), { role: 'user', content: String(request || '') }];
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'assess',
    systemPrompt: ASSESS_SYSTEM_PROMPT, priming: ASSESS_PRIMING,
    seedMessages: seed,
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots),
    cwd: projectDir,
    suppressTags: ['evaluation'],
    onTokens: onTokens ? (t) => onTokens(t) : undefined,
    onProse: onProse ? ({ turn, segmentIndex, content }) => onProse({ turn, segmentIndex, content }) : undefined,
    onProseEnd: onProseEnd ? ({ turn, segmentIndex, content }) => { const c = content.replace(/<evaluation\b[\s\S]*?(?:<\/evaluation>|\/>)/i, '').trim(); if (c) onProseEnd({ turn, segmentIndex, content: c }); } : undefined,
    interpret: ({ prose, allowed }) => {
      const ev = parseEvaluation(prose);                       // EVALUATION WINS (even alongside a read this turn)
      if (ev) return { finish: true, result: { action: 'evaluate', ...ev } };
      if (allowed.length > 0) return {};                       // read-only tools ran → loop, then answer/evaluate
      return { finish: true, result: { action: 'answer', text: String(prose || '').trim() } };
    },
    onMaxTurns: ({ transcript }) => {
      log(`assess: hit max turns (${REVIEWER_MAX_TURNS}) — failing safe to a single-worker evaluation`);
      emitDiag(onDiag, 'assess', `hit ${REVIEWER_MAX_TURNS}-turn budget without an evaluation`, lastAssistantText(transcript));
      return { action: 'evaluate', metrics: normalizeCognitionMetrics({}), salience: 0.6, brief: String(request || '') };
    },
  });
}

// ── Cognition thinking passes: decompose / explore / synthesize ──────────────
// Read-only LLM passes the scheduler composes (the WORKER is the only writer). Each degrades
// safely so the pipeline never dead-ends.

const DECOMPOSE_SYSTEM_PROMPT = `You are the DECOMPOSE step. Break the task into the SMALLEST set of self-contained steps that together complete it. For each step emit ONE block:
   <step id="short-id" after="comma,separated ids it depends on, or empty">a self-contained brief for that step — what to do and what "done" looks like; a fresh agent executes it with NO other context</step>
Use \`after\` only for REAL dependencies, so independent steps can run in parallel. Prefer 2-6 steps. You MAY read the repo READ-ONLY to scope the steps; do NOT change anything.`;

function parseSteps(prose) {
  const steps = [];
  const re = /<step\b([^>]*)>([\s\S]*?)<\/step>/gi;
  let m;
  while ((m = re.exec(String(prose || '')))) {
    const attrs = m[1]; const brief = m[2].trim();
    const idM = attrs.match(/id\s*=\s*"([^"]*)"/i);
    const afterM = attrs.match(/after\s*=\s*"([^"]*)"/i);
    if (!brief) continue;
    steps.push({ id: idM ? idM[1].trim() : `s${steps.length + 1}`, brief, after: afterM ? afterM[1].split(',').map((s) => s.trim()).filter(Boolean) : [] });
  }
  return steps;
}

/** DECOMPOSE: brief → { steps:[{id, brief, after}] }. Empty → caller falls back to one worker. */
async function runDecompose({ adapter, model, signal, onLog, messages = [], brief, projectDir = null, repoRoots = [], onTokens }) {
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'decompose',
    systemPrompt: DECOMPOSE_SYSTEM_PROMPT, priming: [],
    seedMessages: [...(Array.isArray(messages) ? messages : []), { role: 'user', content: String(brief || '') }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots), cwd: projectDir, suppressTags: ['step'],
    onTokens: onTokens ? (t) => onTokens(t) : undefined,
    interpret: ({ prose, allowed }) => {
      const steps = parseSteps(prose);
      if (steps.length) return { finish: true, result: { steps } };
      if (allowed.length > 0) return {};
      return { finish: true, result: { steps: [] } };
    },
    onMaxTurns: () => ({ steps: [] }),
  });
}

const EXPLORE_SYSTEM_PROMPT = `You are the EXPLORE step for an open-ended / creative task. Briefly weigh 2-4 DISTINCT approaches (different strategies, not variations), then COMMIT to the best. Emit exactly:
   <approach>a refined, self-contained brief describing the chosen approach concretely enough to execute, with a one-line note on why it beats the alternatives</approach>
You MAY read the repo READ-ONLY to ground the approaches; do NOT change anything.`;

function parseApproach(prose) {
  const m = String(prose || '').match(/<approach>([\s\S]*?)<\/approach>/i);
  return m ? m[1].trim() : null;
}

/** EXPLORE: brief → { brief: chosen approach }. Falls back to the original brief. */
async function runExplore({ adapter, model, signal, onLog, messages = [], brief, projectDir = null, repoRoots = [], onTokens }) {
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'explore',
    systemPrompt: EXPLORE_SYSTEM_PROMPT, priming: [],
    seedMessages: [...(Array.isArray(messages) ? messages : []), { role: 'user', content: String(brief || '') }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots), cwd: projectDir, suppressTags: ['approach'],
    onTokens: onTokens ? (t) => onTokens(t) : undefined,
    interpret: ({ prose, allowed }) => {
      const approach = parseApproach(prose);
      if (approach) return { finish: true, result: { brief: approach } };
      if (allowed.length > 0) return {};
      return { finish: true, result: { brief: String(brief || '') } };
    },
    onMaxTurns: () => ({ brief: String(brief || '') }),
  });
}

// EXPLORE, the richer way: N INDEPENDENT proposers (each commits to one approach, optionally
// through a distinct lens for diversity) → a JUDGE that picks/synthesizes the best. The
// scheduler's explore primitive fans these out in parallel; this beats one agent listing
// options (which anchors on the first idea it has).
const PROPOSER_SYSTEM_PROMPT = `You are ONE of several INDEPENDENT proposers for an open-ended task. Propose a SINGLE concrete approach — the best one given your guidance. Be specific and self-contained (an executor will follow it). Do NOT hedge or list alternatives — COMMIT to one. Emit exactly:
   <approach>your single concrete approach: what to do and why it fits</approach>
You MAY read the repo READ-ONLY to ground it; do NOT change anything.`;

/** One proposer: brief (+ optional lens) → { approach }. */
async function runProposer({ adapter, model, signal, onLog, brief, lens = '', projectDir = null, repoRoots = [], onTokens }) {
  const systemPrompt = lens ? `${PROPOSER_SYSTEM_PROMPT}\n\nYour guidance for this proposal: ${lens}.` : PROPOSER_SYSTEM_PROMPT;
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'proposer', systemPrompt, priming: [],
    seedMessages: [{ role: 'user', content: String(brief || '') }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots), cwd: projectDir, suppressTags: ['approach'],
    onTokens: onTokens ? (t) => onTokens(t) : undefined,
    interpret: ({ prose, allowed }) => {
      const a = parseApproach(prose);
      if (a) return { finish: true, result: { approach: a } };
      if (allowed.length > 0) return {};
      return { finish: true, result: { approach: String(prose || '').trim() } };
    },
    onMaxTurns: ({ transcript }) => ({ approach: lastAssistantText(transcript) || '' }),
  });
}

const EXPLORE_JUDGE_SYSTEM_PROMPT = `You are the JUDGE for an open-ended task, given several INDEPENDENT proposed approaches. Compare them on fit, simplicity, risk, and how well they solve the task, then produce the BEST plan — pick one outright OR synthesize the strongest elements of several. Emit exactly:
   <approach>the chosen/synthesized approach as a refined, self-contained brief to execute, with a one-line note on why it wins</approach>
You MAY read the repo READ-ONLY to judge; do NOT change anything.`;

/** Judge: brief + proposals[] → { brief: chosen/synthesized approach }. Falls back to the original brief. */
async function runExploreJudge({ adapter, model, signal, onLog, brief, proposals = [], projectDir = null, repoRoots = [], onTokens }) {
  const list = (Array.isArray(proposals) ? proposals : []).map((p, i) => `Approach ${i + 1}:\n${(p && p.approach) || String(p || '')}`).join('\n\n---\n\n');
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'explore-judge', systemPrompt: EXPLORE_JUDGE_SYSTEM_PROMPT, priming: [],
    seedMessages: [{ role: 'user', content: `Task:\n${String(brief || '')}\n\nProposed approaches:\n\n${list}` }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots), cwd: projectDir, suppressTags: ['approach'],
    onTokens: onTokens ? (t) => onTokens(t) : undefined,
    interpret: ({ prose, allowed }) => {
      const a = parseApproach(prose);
      if (a) return { finish: true, result: { brief: a } };
      if (allowed.length > 0) return {};
      return { finish: true, result: { brief: String(prose || '').trim() || String(brief || '') } };
    },
    onMaxTurns: () => ({ brief: String(brief || '') }),
  });
}

const SYNTHESIZE_SYSTEM_PROMPT = `You are the SYNTHESIZE step. You are given a task and the results of the steps it was split into. Produce ONE coherent summary: what was accomplished overall, anything that did not complete, and the net outcome — concise and useful to the person who asked. Plain prose, no tags.`;

/** SYNTHESIZE: fold step results into one. Falls back to concatenated step content. */
async function runSynthesize({ adapter, model, signal, onLog, brief, stepResults = [], onTokens }) {
  const summary = (Array.isArray(stepResults) ? stepResults : []).map((s, i) => `Step ${s.id || i + 1}: ${s.stopped ? '(stopped — budget exhausted)' : String((s && s.content) || '').slice(0, 600)}`).join('\n\n');
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'synthesize',
    systemPrompt: SYNTHESIZE_SYSTEM_PROMPT, priming: [],
    seedMessages: [{ role: 'user', content: `Task:\n${String(brief || '')}\n\nStep results:\n${summary}` }],
    allowedVerbs: new Set(), maxTurns: 1,
    onTokens: onTokens ? (t) => onTokens(t) : undefined,
    interpret: ({ prose }) => ({ finish: true, result: { content: String(prose || '').trim() } }),
    onMaxTurns: ({ transcript }) => ({ content: lastAssistantText(transcript) || summary }),
  });
}

// ── Conversational intake (requirement capture) ─────────────────────────────
// A write-blocked MANAGER persona that turns pasted emails/specs/notes into ONE
// structured, editable requirement card. Same read-only tool loop as the
// reviewer/acceptance (so the write-block is STRUCTURAL — non-allowlisted verbs
// are dropped, never executed). It converses in prose between turns, may read
// the repo read-only to scope the requirement, and emits a single
// <requirement-card> only once it is confident. Multi-turn ACROSS separate HTTP
// POSTs is the caller's job: it replays the persisted thread back into
// `messages`; within one call this loop handles its own read-tool turns.
const INTAKE_TOOL_VERBS = REVIEWER_TOOL_VERBS;   // identical read-only allowlist
const INTAKE_MAX_TURNS = 8;

const INTAKE_SYSTEM_PROMPT = `You are an intake manager for a software-maintenance team. A developer DESCRIBES a piece of work they need done — in their own words — and MAY also paste raw material (an email, ticket, spec, or notes); treat anything pasted as supporting context, not a requirement. Your job is to turn what they tell you into ONE clear, frozen REQUIREMENT that a coding agent and an acceptance checker can both act on, GROUNDED in the actual project: when the developer references the code, a file, or a folder, READ it (read-only, via the tools below) instead of assuming. You do NOT do the work yourself and you MUST NOT modify anything.

The developer's PROJECT is open and available to you READ-ONLY (same tools as a reviewer; emit calls inside a <remote-workspace> block):
  <read path="FILE"/>  <list path="DIR"/>  <search pattern="REGEX" path="DIR"/>  <find pattern="GLOB"/>  <info path="FILE"/>
  <powershell>git log --oneline -n 5</powershell>   (read-only commands only)
You have NO shell and NO local filesystem of your own — these <remote-workspace> commands are the ONLY way to see anything. They run on the developer's machine and the results come back the next turn. NEVER say you'll "look on your system", run a local/Linux command, or that you "don't have access" or "can't see the files" — the project IS available to you through these tags, nothing else is.
Paths are relative to the PROJECT ROOT — start with <list path="."/> to see it. If the developer points at a file, folder, or document (e.g. "the spec is in the specs folder", "see docs/X"), LIST and READ it to ground the requirement — do NOT claim the workspace is empty without checking with <list path="."/> first.

How to behave:
- YOUR ONLY JOB IS TO FILL OUT THE REQUIREMENT FORM (title, requirement/definition-of-done, acceptance criteria, sources). You are NOT the coding agent. NEVER offer to implement, fix, write, refactor, or "help with" the work, and never produce code, a patch, or a step-by-step plan for doing it — a separate agent does the work later from your card. Your entire output on any turn is exactly one of: read-only <remote-workspace> calls to ground the requirement, a brief clarifying question, or the card.
- BIAS STRONGLY TOWARD EMITTING THE CARD. The card is editable — the developer refines or corrects it after you emit it — so a good-enough card beats another question. Do NOT hold the card back waiting for perfect certainty.
- Ask clarifying questions ONLY when the material is genuinely too ambiguous to write any definition of done, and then ask AT MOST one short round. The moment the developer answers (or says "that's the whole requirement" / "go ahead" / confirms), emit the card immediately — do not ask again.
- If the material is already clear enough to state a definition of done, emit the card on your FIRST turn (after at most a quick read to ground it). Don't ask a question you can answer by reading the code or by making a reasonable, editable assumption.
- Read the codebase when it helps you scope the requirement accurately (confirm an app/file exists, find the relevant module), but keep it cheap.
- Do NOT invent scope the material does not support. Prefer the developer's own words. The requirement must be a single, testable definition of done.

When you can state an unambiguous definition of done — which is usually now — emit EXACTLY ONE card, in a turn with NO tool calls:

<requirement-card>
<title>short imperative title</title>
<requirement>the single frozen definition of done: what must be true when this work is complete; specific and verifiable</requirement>
<acceptance-criteria>a checklist the acceptance checker can verify, one item per line; or leave empty</acceptance-criteria>
<sources>where this came from (e.g. "ticket OPS-1421; email from Pat 2026-05-29"); or leave empty</sources>
</requirement-card>

Until you emit the card, ask only what you need to write it. After you emit the card, stop.`;

// A hidden priming exchange (never persisted, never shown) that makes the model
// COMMIT to the remote-workspace tools + the form-filling role before the real
// request arrives — mirrors runPromptedAgent's PRIMING. Without it, Gemini's first
// turn tends to (a) claim it "can't see the files" / offer to look on its own
// system, and (b) offer to DO the work instead of writing a card. One short,
// complete example demonstrates: read read-only via <remote-workspace>, then emit
// ONE card — no offering to implement anything.
const INTAKE_PRIMING = [
  {
    role: 'user',
    content:
      "Quick orientation before we start: you're my intake manager in a REMOTE workspace. You have no shell or local files — the ONLY way to look at my project is to return XML inside a <remote-workspace> block (<list path=\".\"/>, <read path=\"...\"/>, <search .../>, <find .../>); those run on MY machine and results come back next turn. The workspace IS my project, so never say you can't see my files. And your job is only to write ONE requirement card (title, definition of done, acceptance criteria, sources) — you never implement or offer to do the work yourself. First one: the login page 500s when a password has a '%' in it; see docs/login.md.",
  },
  {
    role: 'assistant',
    content:
      "Let me read that note and confirm where the page lives before I write the requirement.\n" +
      "<remote-workspace>\n<read path=\"docs/login.md\"/>\n<list path=\"src/pages\"/>\n</remote-workspace>",
  },
  {
    role: 'user',
    content:
      "<remote-workspace-result op=\"read\" status=\"ok\" path=\"docs/login.md\">Login returns HTTP 500 when the password contains a '%'. It should validate and escape special characters instead.</remote-workspace-result>\n" +
      "<remote-workspace-result op=\"list\" status=\"ok\" path=\"src/pages\">Login.tsx\nHome.tsx</remote-workspace-result>",
  },
  {
    role: 'assistant',
    content:
      "<requirement-card>\n<title>Fix login 500 on '%' in password</title>\n" +
      "<requirement>The login flow (src/pages/Login.tsx) must accept passwords containing '%' and other special characters: the value is validated and properly escaped so authentication proceeds normally instead of returning HTTP 500.</requirement>\n" +
      "<acceptance-criteria>Logging in with a password containing '%' succeeds with no 500.\nOther special characters in the password authenticate without error.</acceptance-criteria>\n" +
      "<sources>docs/login.md</sources>\n</requirement-card>",
  },
];

/**
 * Parse the manager's requirement card out of a prose turn. Returns
 * { title, requirement, acceptanceCriteria, sources } or null when there is no
 * card (or a card with an empty requirement — fail-safe: an empty requirement
 * is treated as "no card yet", never as a capturable definition of done).
 */
function parseRequirementCard(prose) {
  const m = String(prose || '').match(/<requirement-card>([\s\S]*?)<\/requirement-card>/i);
  if (!m) return null;
  const body = m[1];
  const grab = (tag) => { const mm = body.match(new RegExp('<' + tag + '>([\\s\\S]*?)</' + tag + '>', 'i')); return mm ? mm[1].trim() : ''; };
  const requirement = grab('requirement');
  if (!requirement) return null;
  return { title: grab('title'), requirement, acceptanceCriteria: grab('acceptance-criteria'), sources: grab('sources') };
}

/**
 * One intake exchange. `messages` is the prior conversation (user/assistant
 * turns, latest user paste LAST); runIntake prepends the system prompt itself.
 * `onTurn` streams progress: { kind:'message', content } for a manager prose
 * turn, { kind:'tools', calls, results } for a read-tool turn, and
 * { kind:'card', card, prose } when a card is emitted.
 * Returns:
 *   { complete:true, card, prose, transcript }                 — a card was emitted
 *   { complete:false, reason:'awaiting-user', assistantText }  — a clarifying turn; user replies next
 *   { complete:false, reason:'max-turns' }                     — ran out of turns with no card
 */
async function runIntake({ adapter, model, messages = [], signal, onLog, onTurn, onDiag, repoRoots = [], projectDir = null, contextNote = '' }) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  // Awaited so a callback that persists state (e.g. addSource) completes before
  // the next turn — prevents floating promises AND torn concurrent writes.
  const emit = async (ev) => { try { await onTurn?.(ev); } catch {} };
  // Resolve a relative tool path against the active PROJECT first — CWD-INDEPENDENT
  // so the planning agent always sees the developer's files (e.g. a specs/ folder),
  // not whatever process.cwd() happens to be. Falls back to the changed git repo
  // roots; an unmatched relative path still targets the project (so it lists/reads
  // the project root, never an unrelated dir — the "workspace is empty" bug).
  const resolveProjectPath = (p) => {
    if (typeof p !== 'string' || !p || path.isAbsolute(p)) return p;
    try { if (projectDir && existsSync(path.join(projectDir, p))) return path.join(projectDir, p); } catch {}
    for (const root of repoRoots) { try { if (existsSync(path.join(root, p))) return path.join(root, p); } catch {} }
    return projectDir ? path.join(projectDir, p) : p;
  };
  // Per-request CONTEXT (current field values, recent activity) is folded into the
  // system prompt — NOT prepended as a separate user message (which would put two
  // user turns in a row). The system prompt itself rides the latest user message
  // via assembleConvo (so genai.mil/Gemini actually sees it).
  const systemPrompt = contextNote ? `${INTAKE_SYSTEM_PROMPT}\n\n${contextNote}` : INTAKE_SYSTEM_PROMPT;
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'intake',
    systemPrompt, priming: INTAKE_PRIMING,
    seedMessages: Array.isArray(messages) ? messages : [],
    allowedVerbs: INTAKE_TOOL_VERBS, maxTurns: INTAKE_MAX_TURNS,
    resolvePath: resolveProjectPath,
    cwd: projectDir,   // path-less tools resolve against the project, not process.cwd()
    suppressTags: ['requirement-card'],   // the card is parsed into the form, not shown as streamed prose
    onTokens: (total) => emit({ kind: 'tokens', total }),
    onTools: ({ calls, results }) => emit({ kind: 'tools', calls, results }),
    // Live prose streaming (the streaming transport renders these as a
    // streaming bubble; the legacy transport ignores them + uses {kind:'message'}).
    onProse: ({ turn, segmentIndex, content }) => emit({ kind: 'prose-chunk', turn, segmentIndex, content }),
    onProseEnd: ({ turn, segmentIndex, content }) => {
      const cleaned = content.replace(/<requirement-card>[\s\S]*?<\/requirement-card>/i, '').trim();
      if (cleaned) emit({ kind: 'prose-end', turn, segmentIndex, content: cleaned });
    },
    interpret: async ({ prose, allowed, transcript }) => {
      // A requirement-card WINS even if the model also grounded with read-only
      // tools in the SAME reply (it commits to the card; the card is editable).
      // Without this, a card emitted alongside e.g. <list/> is silently dropped
      // and intake dead-ends — the model then claims it "already emitted the card".
      const card = parseRequirementCard(prose);
      if (card) { await emit({ kind: 'card', card, prose }); return { finish: true, result: { complete: true, card, prose, transcript } }; }
      if (allowed.length === 0) {
        // No tools and no card -> a clarifying/manager turn; return control to the user.
        await emit({ kind: 'message', content: prose });
        return { finish: true, result: { complete: false, reason: 'awaiting-user', assistantText: prose, transcript } };
      }
      return {};   // run the read-only tools, loop
    },
    onMaxTurns: async ({ transcript }) => {
      log(`intake: hit max turns (${INTAKE_MAX_TURNS}) without a card.`);
      emitDiag(onDiag, 'intake', `hit ${INTAKE_MAX_TURNS}-turn budget without a <requirement-card>`, lastAssistantText(transcript));
      await emit({ kind: 'message', content: 'I could not fully structure this within my turn budget. Please edit the card fields directly and capture.' });
      return { complete: false, reason: 'max-turns', transcript };
    },
  });
}

// ── Memory consolidation (post-acceptance fact extraction) ──────────────────
// After an assignment is ACCEPTED, a read-only pass over the work just done,
// proposing DURABLE codebase facts for Tier-1 memory (NOT task-specific or
// already-known ones). Fail-safe to an empty list — recording nothing is the
// correct, harmless default.
const CONSOLIDATION_SYSTEM_PROMPT = `You are a codebase-memory curator. A unit of work on a codebase just completed and was accepted. Extract a SHORT list of DURABLE, REUSABLE facts about the codebase that will help FUTURE work — the kind a new teammate would want written down.

RECORD: how the app is structured, conventions in force, non-obvious gotchas, where key things live, build/test/deploy specifics.
DO NOT RECORD: anything task-specific or transient ("fixed bug X", "added method Y"), anything obvious from a glance at the code, anything already covered by the EXISTING MEMORY below, or speculation.

You may verify read-only with the usual tools inside a <remote-workspace> block:
  <read path="FILE"/>  <list path="DIR"/>  <search pattern="REGEX" path="DIR"/>  <find pattern="GLOB"/>  <info path="FILE"/>  <powershell>git log --oneline -n 5</powershell>
You MUST NOT modify anything.

When done, emit ONE block in a turn with NO tool calls. Either:
<facts>
<fact category="architecture|conventions|gotchas|where-things-live|build-deploy|general">one concise durable fact</fact>
... (zero or more)
</facts>
or, if nothing is worth recording:
<facts/>

Be conservative — an empty list is the right answer when the work taught nothing durable. NEVER restate an existing fact.`;

function parseFacts(prose) {
  if (/<facts\s*\/>/i.test(prose)) return [];
  const block = prose.match(/<facts>([\s\S]*?)<\/facts>/i);
  if (!block) return null;   // no facts block at all → signal "no verdict"
  const facts = [];
  const re = /<fact\b([^>]*)>([\s\S]*?)<\/fact>/gi;
  let m;
  while ((m = re.exec(block[1]))) {
    const catM = (m[1] || '').match(/category\s*=\s*"([^"]*)"/i);
    const note = m[2].trim();
    if (note) facts.push({ category: catM ? catM[1].trim() : 'general', note });
  }
  return facts;
}

/**
 * Consolidate durable codebase facts from a completed assignment.
 *   assignment:     { id, requirement }
 *   tasks:          [{ taskId, userRequest, summary, events, verdict }]
 *   existingMemory: rendered current memory (so it won't restate facts)
 * Returns { facts: [{ category, note }] }. Fails safe to { facts: [] }.
 */
async function runConsolidation({ adapter, model, assignment, tasks = [], existingMemory = '', signal, onLog, onDiag, repoRoots = [], projectDir = null }) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  const parts = ['<assignment-requirement>', clip(String(assignment?.requirement || '(none)'), 2000), '</assignment-requirement>'];
  if (existingMemory) parts.push('<existing-memory note="do NOT restate any of these">', clip(String(existingMemory), 6000), '</existing-memory>');
  parts.push('<work note="what was just completed and accepted">', buildBudgetedReviewInput(tasks || [], { budgetChars: 20000 }), '</work>');
  return runReadonlyPass({
    adapter, model, signal, onLog, label: 'consolidation',
    systemPrompt: CONSOLIDATION_SYSTEM_PROMPT,
    seedMessages: [{ role: 'user', content: parts.join('\n') }],
    allowedVerbs: REVIEWER_TOOL_VERBS, maxTurns: REVIEWER_MAX_TURNS,
    resolvePath: makeRepoResolver(projectDir, repoRoots),
    cwd: projectDir,
    interpret: ({ prose, allowed }) => {
      if (allowed.length > 0) return {};   // run tools, loop
      const facts = parseFacts(prose);
      if (facts) return { finish: true, result: { facts } };
      log('consolidation: no facts block — recording nothing');
      emitDiag(onDiag, 'consolidation', 'no <facts> block emitted — recorded nothing', prose);
      return { finish: true, result: { facts: [] } };
    },
    onMaxTurns: ({ transcript }) => {
      log('consolidation: hit max turns without a facts block — recording nothing.');
      emitDiag(onDiag, 'consolidation', `hit ${REVIEWER_MAX_TURNS}-turn budget without a <facts> block`, lastAssistantText(transcript));
      return { facts: [] };
    },
  });
}

// ── Task summary backfill (when the agent closed a task with a blank summary) ─
// A single-shot, dedicated "give me ONLY a one-line summary" call — the most
// reliable shape (nothing else asked for). Reads the closed task's diff/work
// and returns one factual sentence. Fail-safe to '' so the caller can fall back.
const SUMMARIZER_SYSTEM_PROMPT = `You write a ONE-sentence (two at most) factual summary of what a coding task accomplished, for a work log and commit message. You are given the task's request and its diff. Reply with ONLY the summary text — no preamble, no markdown, no tags, no quotes, no lists. Describe concretely what changed (files, functions, behaviour). If the diff is empty or unclear, give a brief best-effort description from the request.`;

async function runSummarizer({ adapter, model, task, signal, onLog }) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  try {
    const input = [
      '<task>',
      task?.userRequest ? `request: ${clip(String(task.userRequest), 500)}` : '',
      buildBudgetedReviewInput(task ? [task] : [], { budgetChars: 12000 }),
      '</task>',
    ].filter(Boolean).join('\n');
    // Gemini-safe assembly (system reminder rides a user message) — see engine.mjs.
    const messages = assembleConvo({
      systemPrompt: SUMMARIZER_SYSTEM_PROMPT,
      messages: [{ role: 'user', content: `Summarize what this task accomplished, in one sentence (two at most):\n${input}` }],
    });
    const t0 = Date.now();
    const response = await adapter.complete({ model, messages, signal });
    const text = response?.choices?.[0]?.message?.content ?? '';
    log(`summarizer: ${text.length} chars in ${Date.now() - t0}ms`);
    // Strip stray tags/quotes/markdown, collapse whitespace, cap length.
    return text.replace(/<[^>]+>/g, '').replace(/^[\s"'`*>-]+|[\s"'`*]+$/g, '').replace(/\s+/g, ' ').slice(0, 300).trim();
  } catch (e) {
    log(`summarizer failed: ${e?.message ?? e}`);
    return '';
  }
}

// ── Parallel REVIEWER (CB_PARALLEL_CRITIC / the parallelCritic toggle) ────────
// Default OFF (parity-safe — the worker loop is unchanged when off). When ON, a PERSISTENT reviewer
// (its own conversation, in investigateForReviewer) follows the agent: FORKED from the agent's history
// the first time, then FED each new step. It has READ-ONLY TOOLS and only flags a problem it can PROVE
// with evidence. Bounded by CRITIC_MAX background fires per run; each fire ≤ TURN_REVIEWER_MAX_TURNS.
const CB_PARALLEL_CRITIC = /^(1|true|on|yes)$/i.test(process.env.CB_PARALLEL_CRITIC || '');
const CRITIC_MAX = Number(process.env.CB_CRITIC_MAX || 8);
const TURN_REVIEWER_MAX_TURNS = Number(process.env.CB_REVIEWER_MAX_TURNS || 8);
let reviewerStoreSeq = 0;   // unique suffix per reviewer store (a fresh dir per run, isolation-safe even if cleanup is blocked)
// The FORK: the reviewer's first message — its standing brief + the goal + everything so far.
const reviewerSeedPrompt = (goal, transcript) =>
`You are a parallel REVIEWER assigned to follow ONE coding agent for the whole task, with READ-ONLY tools (read, list, search, find) over the SAME project. You'll be shown the agent's work step by step. Catch things it gets WRONG — but ONLY flag a problem you can PROVE with evidence from the actual code.

THE GOAL:
${String(goal || '').slice(0, 2000)}

THE CONVERSATION SO FAR — the agent's work (messages, tool calls, results). Its LATEST step is at the END:
${String(transcript || '(none)').slice(0, 15000)}

YOU REVIEW EVERY KIND OF STEP, not just code changes:
- EXPLORATION: is the agent looking in the right place? (It's reading/searching X but the behavior it needs lives in Y — prove it by finding Y. It skipped an obviously relevant file/config.)
- THINKING & DESIGN: is the plan flawed? Flaws can be proven against the actual code, against the GOAL or the user's stated requirements, against the agent's OWN earlier statements (self-contradiction) — or by solid engineering judgment (a race, unbounded growth, a security hole, missing error handling on a path that will fail, an approach that cannot satisfy the goal).
- CHANGES: wrong assumption, mismatched signature/type, missed edge case or business rule, a bug, the wrong file/path.
- MISSED CONNECTIONS: two facts that bear on each other that the agent hasn't put together — an existing mechanism/util that already does what it's building, something it read earlier that solves or contradicts what it's doing now, a requirement it captured that its current step ignores. The connection is your insight; the EVIDENCE is the two endpoints (cite both). You have the whole history and your attention isn't buried in execution — spotting these is what you're for.
- TESTING: the agent claims something works but never ran anything that shows it; the test it ran doesn't exercise the change; a test failure was glossed over in its summary.

HOW TO REVIEW (every time):
- Form a hypothesis about what might be wrong in the latest step — across ALL the categories above.
- VERIFY before flagging. Claims about the code: open the file and check. Claims about the plan/requirements: quote the goal or the agent's own words it contradicts. Engineering-judgment claims need no citation, but they must be DEFINITE and CONSEQUENTIAL — a flaw that changes the outcome, never style or preference — and you must still check the code that the concern actually applies here (e.g., the loop really has no backoff, the input really is unvalidated).
- Report ONLY a problem you can make CONCRETE: file:line + what the code says, the quoted requirement/statement it violates, or the precise failure mode and when it bites. If you can't make it concrete, it is not a finding.
- If the step is sound, or you can't verify a concern: your ENTIRE reply must be exactly the three words "no problems found" — no reasoning before or after it.
- ONLY if you PROVED a problem: reply with ONE concise paragraph — the proven problem + the evidence + what to do (do NOT also write "no problems found").
- Remember what you've already verified — don't re-investigate it, and never re-raise a resolved issue.
- You are ONLY reviewing: never emit <next-task>, <checklist>, or any task-management or write tags — use only the read-only tools (read, list, search, find). Each reply is just your verdict.

Investigate the latest step now, then give your single verdict.`;
// The FEED: each subsequent turn — just the agent's new step (the reviewer already has its history).
const reviewerFeedPrompt = (latestStep) =>
`The agent continued. Its latest step:
${String(latestStep || '(no detail)').slice(0, 6000)}

Review it the same way, using everything you already know (your earlier findings and the evidence you gathered are above — don't re-investigate what you've already verified, and don't re-raise a resolved issue). Investigate anything new, then give your single verdict (a proven finding with evidence, or "no problems found").`;

// Build a compact representation of the work the agent did, for the
// reviewer to consume. Skips noisy chat events (review-* itself, status
// rows) and truncates very long tool results. Returns a single string.
function buildReviewTrajectory(events) {
  const lines = [];
  for (const ev of events) {
    switch (ev.type) {
      case 'user-message':
        // The user-request is passed separately; skip dups here.
        break;
      case 'assistant-text':
        lines.push('AGENT: ' + clip(ev.content, 800));
        break;
      case 'tool-call': {
        const args = Object.entries(ev.args || {})
          .map(([k, v]) => `${k}=${clip(String(v), 200)}`).join(', ');
        lines.push(`TOOL: ${ev.verb || ev.name}(${args})`);
        break;
      }
      case 'tool-result':
        lines.push(`  -> [${ev.status || 'ok'}] ${clip(ev.content, 600)}`);
        break;
      case 'diff':
        lines.push('DIFF:');
        lines.push(clip(ev.diff || '', 2000));
        break;
      case 'task-end':
        lines.push(`TASK END: ${clip(ev.summary || '', 400)}`);
        break;
      default:
        // skip other event types (review, task-rollup, task-tier-change, etc)
    }
  }
  return lines.join('\n');
}

function clip(s, n) {
  if (typeof s !== 'string') return String(s ?? '');
  return s.length > n ? s.slice(0, n) + ' … [truncated]' : s;
}

// ====== agent/cognition-scheduler.mjs ======
/**
 * The adaptive-cognition SCHEDULER — the recursive loop that ties the pure core (cognition.mjs)
 * to the actual thinking. It is DEPENDENCY-INJECTED: all the "real" work is supplied as
 * `primitives` (assess / explore / decompose / runWorker / verify / synthesize), so the
 * orchestration logic — layer composition, recursion, DAG step deps, budget-gated termination —
 * is fully testable with mocks before any LLM or sub-agent is wired in (that's the next step).
 *
 * THE LOOP:  assess → (answer | evaluate) → policy picks composable layers →
 *            explore (refine approach) → decompose (split into a DAG) → execute steps
 *            (each step is RE-ASSESSED → recursion) → single|debug worker → verify (+ re-run).
 * ALL of it shares ONE controller; the salience×budget rule guarantees the whole tree ends.
 *
 * Budget gating is done at a single ADMISSION point (`admit`): shouldContinue + charge the
 * per-node ε floor, SEQUENTIALLY (even before a parallel step batch) so concurrent siblings
 * see each other's reservation — that bounds parallel overshoot and bounds the node count to
 * ~ceiling/ε, which is the structural termination guarantee.
 *
 * primitives (each async, returns its payload + a `cost` in work-units):
 *   assess(brief, ctx)      → { action:'answer', text, cost } | { action:'evaluate', metrics, salience, brief, cost }
 *   explore(brief, ctx)     → { brief, cost }                  (N proposers → synthesized approach)
 *   decompose(brief, ctx)   → { steps:[{id, brief, salience?, after?:[id]}], cost }
 *   runWorker(brief, ctx)   → { content, cost }                (a worker sub-agent)
 *   verify(brief, res, ctx) → { ok, feedback?, cost }          (acceptance / adversarial verifiers)
 *   synthesize(brief, results, ctx) → { content, cost }        (OPTIONAL: fold step results into one)
 */

// Single admission gate: the ONLY place that decides "may this node run?" + reserves its ε.
function admit(controller, salience, emit, depth) {
  if (!controller.shouldContinue(salience)) { emit({ type: 'stopped', depth, reason: 'budget', salience, ...controller.state() }); return false; }
  controller.enterNode();   // reserve the per-node floor NOW (sequential → siblings see it)
  return true;
}

async function runCognition({ request, primitives, k = 0.8, budgetOpts = {}, controller = null, maxRerun = 1, inlineSingle = false, allowDecompose = true, onEvent = () => {}, signal } = {}) {
  const emit = (ev) => { try { onEvent(ev); } catch {} };
  const p = primitives;

  const a0 = await p.assess(request, { depth: 0, signal });
  if (a0.action === 'answer') { emit({ type: 'answer', depth: 0, text: a0.text }); return { answer: a0.text, controller: null }; }

  // ROUTING (inlineSingle): the multi-agent machinery (worktree sub-agents, decompose) only
  // earns its overhead for GENUINELY decomposable work. A single coherent task — however
  // "complex" — is handled better + cheaper by the plain inline worker (the strong baseline),
  // so signal the caller to run it inline rather than spawning sub-agents that lose coherence
  // or invent scope. With decomposition DISABLED (allowDecompose=false, the default in prod),
  // EVERYTHING non-answer routes inline → the plain worker, no fan-out ever.
  if (inlineSingle && (!allowDecompose || !selectCognitionLayers(a0.metrics).layers.includes('decompose'))) {
    emit({ type: 'inline', complexity: a0.metrics.complexity, metrics: a0.metrics });
    return { inline: true, evaluation: a0 };
  }

  const ctrl = controller || createCognitionController({ budget: cognitionBudget(a0.metrics.complexity, budgetOpts), k });
  ctrl.chargeOverhead(a0.cost || 0);
  emit({ type: 'budget', complexity: a0.metrics.complexity, ...ctrl.state() });

  const ctx = { controller: ctrl, depth: 0, p, emit, signal, maxRerun, allowDecompose };
  if (!admit(ctrl, a0.salience, emit, 0)) return { result: { stopped: true }, controller: ctrl, state: ctrl.state() };
  const result = await executeNode(a0, ctx);
  emit({ type: 'done', ...ctrl.state() });
  return { result, controller: ctrl, state: ctrl.state() };
}

// Re-assess an arbitrary brief, then execute it — THE recursion point (a decompose step that
// turns out complex will itself decompose). The caller has ALREADY admitted this node.
async function processBrief(brief, ctx) {
  const { controller, depth, p, signal, emit } = ctx;
  const a = await p.assess(brief, { depth, signal });
  controller.chargeOverhead(a.cost || 0);
  if (a.action === 'answer') { emit({ type: 'answer', depth, text: a.text }); return { content: a.text }; }
  return executeNode(a, ctx);
}

// Execute an already-admitted evaluation node (no gating here — admission happened upstream).
async function executeNode(evaluation, ctx) {
  const { controller, depth, p, emit, signal, maxRerun, allowDecompose = true } = ctx;
  const { layers, verify } = selectCognitionLayers(evaluation.metrics);
  emit({ type: 'node', depth, metrics: evaluation.metrics, layers, verify, salience: evaluation.salience, ...controller.state() });
  let brief = evaluation.brief;

  // EXPLORE — refine the approach (N proposals → synthesis) before committing.
  if (layers.includes('explore')) {
    const ex = await p.explore(brief, { depth, signal });
    controller.chargeWork(ex.cost || 0);
    if (ex.brief) brief = ex.brief;
    emit({ type: 'explore', depth, ...controller.state() });
  }

  let result;
  if (allowDecompose && layers.includes('decompose')) {
    // DECOMPOSE — split into a DAG of steps; each step is re-assessed (recursion) under the
    // same controller; independent steps run in parallel, dependents wait for their deps.
    const dec = await p.decompose(brief, { depth, signal });
    controller.chargeWork(dec.cost || 0);
    const steps = Array.isArray(dec.steps) ? dec.steps : [];
    emit({ type: 'decompose', depth, steps: steps.map((s) => ({ id: s.id, after: s.after || [] })), ...controller.state() });
    if (steps.length <= 1) {
      // decompose found no genuine separation (0 or 1 step) → run ONE worker on the brief
      // (do NOT synthesize over an empty/degenerate step set — that produced a meaningless
      // "summary of nothing" and did no work). This is also the "1 task → top-level handles it"
      // case: a cohesive task that slipped past the gate collapses back to a single worker.
      const w = await p.runWorker((steps[0] && steps[0].brief) || brief, { depth, mode: 'single', signal });
      controller.chargeWork(w.cost || 0);
      emit({ type: 'worker', depth, mode: 'single', collapsed: true, ...controller.state() });
      result = { content: w.content, mode: 'single', collapsed: true };
    } else {
      const stepResults = await runSteps(steps, { ...ctx, depth: depth + 1 });
      result = p.synthesize
        ? await (async () => { const sy = await p.synthesize(brief, stepResults, { depth, signal }); controller.chargeOverhead(sy.cost || 0); return { content: sy.content, steps: stepResults }; })()
        : { steps: stepResults };
    }
  } else {
    // EXECUTION — a single worker (or debug worker) actually does the work.
    const mode = layers.includes('debug') ? 'debug' : 'single';
    const w = await p.runWorker(brief, { depth, mode, signal });
    controller.chargeWork(w.cost || 0);
    result = { content: w.content, mode };
    emit({ type: 'worker', depth, mode, ...controller.state() });
  }

  // VERIFY — wrap the execution with checks; bounded re-run on failure (wide, not deep).
  if (verify) {
    for (let reruns = 0; ; ) {
      const v = await p.verify(brief, result, { depth, signal });
      controller.chargeWork(v.cost || 0);
      emit({ type: 'verify', depth, ok: !!v.ok, ...controller.state() });
      if (v.ok || reruns >= maxRerun || !controller.shouldContinue(evaluation.salience)) break;
      reruns += 1;
      const w = await p.runWorker(`${brief}\n\nReviewer feedback: ${v.feedback || ''}`, { depth, mode: 'single', signal });
      controller.chargeWork(w.cost || 0);
      result = { content: w.content, rerun: reruns };
      emit({ type: 'rerun', depth, round: reruns, ...controller.state() });
    }
  }
  return result;
}

// DAG executor: admit every ready step SEQUENTIALLY (so the budget can't be blown by a wide
// parallel wave), then execute the admitted ones in parallel. A cycle / unmet dep can't
// deadlock — if nothing is structurally ready, the remaining steps become ready.
async function runSteps(steps, ctx) {
  const { controller, emit, depth } = ctx;
  const byId = new Map(steps.map((s) => [s.id, s]));
  const done = new Map();
  const pending = new Set(steps.map((s) => s.id));
  while (pending.size) {
    let ready = [...pending].filter((id) => (byId.get(id).after || []).every((d) => done.has(d) || !byId.has(d)));
    if (!ready.length) ready = [...pending];
    const admitted = [];
    for (const id of ready) {                 // SEQUENTIAL admission (reserve ε each, in order)
      const s = byId.get(id);
      if (admit(controller, s.salience != null ? s.salience : 0.7, emit, depth)) admitted.push(id);
      else { done.set(id, { stopped: true }); pending.delete(id); }
    }
    if (!admitted.length) { for (const id of pending) done.set(id, { stopped: true }); break; }
    const batch = await Promise.all(admitted.map(async (id) => [id, await processBrief(byId.get(id).brief, ctx)]));
    for (const [id, r] of batch) { done.set(id, r); pending.delete(id); }
  }
  return steps.map((s) => ({ id: s.id, ...(done.get(s.id) || {}) }));
}

// ====== agent/git-workflow.mjs ======
/**
 * Git workflow helpers — used by server.mjs to:
 *   - Detect whether the active project is a git repo.
 *   - Branch automatically onto CODE_BOSS_<timestamp> when first opening
 *     so the agent's changes never land directly on the user's working
 *     branch.
 *   - Auto-commit at the end of each task (next-task) with the task's
 *     summary as the commit message.
 *   - Check `git status --porcelain` before running the reviewer so we
 *     skip the LLM call when nothing actually changed (Q&A turns, pure
 *     read flows).
 *
 * The shape: each call returns a result object you can pass directly to
 * a tool-call/tool-result chat event pair, so the git operations are
 * visible in the chat history just like any other tool the agent runs.
 *
 *   { command: 'git ...', exitCode, stdout, stderr, durationMs }
 *
 * The caller decides whether to emit a chat event; this module just
 * runs commands.
 */

const GIT_DEFAULT_TIMEOUT_MS = 30000;

/** Run a single `git <args>` invocation. Resolves with the result object. */
function runGit(cwd, args, { timeoutMs = GIT_DEFAULT_TIMEOUT_MS, input } = {}) {
  return new Promise((resolve) => {
    const t0 = Date.now();
    let stdout = '', stderr = '', timedOut = false, settled = false;
    const child = spawn('git', args, { cwd, windowsHide: true, env: process.env });
    const tid = setTimeout(() => {
      timedOut = true;
      try { child.kill(); } catch {}
    }, timeoutMs);
    child.stdout.on('data', (d) => { stdout += d.toString('utf8'); });
    child.stderr.on('data', (d) => { stderr += d.toString('utf8'); });
    if (input != null) {
      try { child.stdin.write(input); child.stdin.end(); } catch {}
    }
    const settle = (code, signal) => {
      if (settled) return; settled = true;
      clearTimeout(tid);
      resolve({
        command: 'git ' + args.join(' '),
        cwd,
        exitCode: timedOut ? null : (code ?? null),
        signal: signal ?? null,
        timedOut,
        stdout,
        stderr,
        durationMs: Date.now() - t0,
      });
    };
    child.on('close', settle);
    child.on('error', (e) => { stderr += '\n' + e.message; settle(-1, null); });
  });
}

/** True if `dir/.git` exists (file or directory — git worktrees use a file). */
async function isGitRepo(dir) {
  if (!dir) return false;
  try {
    await stat(path.join(dir, '.git'));
    return true;
  } catch { return false; }
}

// Directories we never recurse into when looking for nested git repos.
// Same list the AGENTS.md scanner uses, plus the obvious build outputs
// where a vendored dependency might keep its own .git as a footgun.
const GIT_SCAN_SKIP_DIRS = new Set([
  '.git', '.hg', '.svn', '.code_boss', '.agent',
  '.vs', '.idea', '.vscode',
  'node_modules', 'bower_components',
  'target', 'dist', 'build', 'out', 'bin', 'obj',
  '__pycache__', '.gradle', '.mvn',
]);

/**
 * Walk `rootDir` looking for git repositories. Returns an array of
 * absolute repo paths. The root itself counts if it has a `.git`.
 * Otherwise we descend until we hit one (recurses no deeper into a
 * directory tree that contains its own `.git`).
 *
 * Bounded so a generated tree can't stall project open:
 *   - max depth 5 below rootDir
 *   - max 30 repos returned
 */
async function discoverGitRepos(rootDir, { maxDepth = 5, maxRepos = 30 } = {}) {
  if (!rootDir) return [];
  const { readdir } = await import('node:fs/promises');
  const found = [];
  async function walk(dir, depth) {
    if (depth > maxDepth) return;
    if (found.length >= maxRepos) return;
    if (await isGitRepo(dir)) {
      found.push(dir);
      return;             // don't descend into a repo's subdirs for more repos
    }
    let entries;
    try { entries = await readdir(dir, { withFileTypes: true }); }
    catch { return; }
    for (const e of entries) {
      if (!e.isDirectory()) continue;
      if (e.name.startsWith('.')) continue;
      if (GIT_SCAN_SKIP_DIRS.has(e.name)) continue;
      await walk(path.join(dir, e.name), depth + 1);
      if (found.length >= maxRepos) return;
    }
  }
  await walk(rootDir, 0);
  return found;
}

/**
 * Walk up from `filepath` looking for the containing git repo's root.
 * Returns the repo's absolute path or null if no .git was found before
 * reaching the filesystem root.
 */
async function findContainingRepo(filepath) {
  if (!filepath) return null;
  let dir = path.dirname(path.resolve(filepath));
  while (true) {
    if (await isGitRepo(dir)) return dir;
    const parent = path.dirname(dir);
    if (parent === dir) return null;   // hit fs root
    dir = parent;
  }
}

/**
 * Append a pattern to .git/info/exclude (git's local-only ignore file —
 * unlike .gitignore, this never gets committed). Used to make code_boss
 * internal state (.code_boss/, conversation.json, etc.) invisible to git
 * status without modifying the user's project files.
 *
 * No-op if the pattern is already there.
 */
async function ensureExcluded(dir, ...patterns) {
  // Resolve the REAL info/exclude path. In a git worktree, <dir>/.git is a
  // FILE ('gitdir: …'), not a directory, so hardcoding <dir>/.git/info/exclude
  // throws and .code_boss/ is never excluded → it shows up dirty and gets
  // swept into the user's commit by `git add -A`. `rev-parse --git-path`
  // returns the correct location for both normal repos and worktrees.
  let file;
  const r = await runGit(dir, ['rev-parse', '--git-path', 'info/exclude']);
  if (r.exitCode === 0 && r.stdout.trim()) {
    file = r.stdout.trim();
    if (!path.isAbsolute(file)) file = path.resolve(dir, file);
  } else {
    file = path.join(dir, '.git', 'info', 'exclude'); // best-effort fallback
  }
  let { readFile, writeFile, mkdir } = await import('node:fs/promises');
  let existing = '';
  try { existing = await readFile(file, 'utf8'); }
  catch { /* file doesn't exist yet; will create */ }
  const have = new Set(existing.split('\n').map((s) => s.trim()).filter(Boolean));
  const toAdd = patterns.filter((p) => !have.has(p));
  if (toAdd.length === 0) return false;
  try { await mkdir(path.dirname(file), { recursive: true }); } catch {}
  const next = (existing.endsWith('\n') || existing === '') ? existing : existing + '\n';
  await writeFile(file, next + toAdd.join('\n') + '\n', 'utf8');
  return true;
}

/** Get current branch name (or null if genuinely detached / non-git / error). */
async function getCurrentBranch(dir) {
  const r = await runGit(dir, ['rev-parse', '--abbrev-ref', 'HEAD']);
  const name = r.exitCode === 0 ? r.stdout.trim() : '';
  if (name && name !== 'HEAD') return name;
  // `HEAD` (or exit!=0) can mean genuine detached HEAD, but ALSO an
  // interrupted rebase or an unborn branch — where we ARE conceptually on a
  // branch. Misreporting null there makes isCodeBossBranch(null) false, so a
  // CODE_BOSS branch mid-rebase wrongly triggers the branch-needed pause and
  // the auto-commit silently skips. Recover the branch from the rebase state
  // or symbolic-ref before giving up.
  // (a) Mid-rebase: git stores the original branch name here.
  for (const p of ['rebase-merge/head-name', 'rebase-apply/head-name']) {
    const hn = await runGit(dir, ['rev-parse', '--git-path', p]);
    if (hn.exitCode === 0 && hn.stdout.trim()) {
      try {
        const { readFile } = await import('node:fs/promises');
        let f = hn.stdout.trim();
        if (!path.isAbsolute(f)) f = path.resolve(dir, f);
        const ref = (await readFile(f, 'utf8')).trim();           // e.g. refs/heads/CODE_BOSS_x
        const m = ref.match(/^refs\/heads\/(.+)$/);
        if (m) return m[1];
      } catch { /* fall through */ }
    }
  }
  // (b) Unborn branch (fresh repo, no commits): symbolic-ref still knows it.
  const sym = await runGit(dir, ['symbolic-ref', '--short', '-q', 'HEAD']);
  if (sym.exitCode === 0 && sym.stdout.trim()) return sym.stdout.trim();
  return null;
}

/** Return short HEAD hash, or null if anything goes wrong. */
async function getHeadHash(dir) {
  const r = await runGit(dir, ['rev-parse', '--short', 'HEAD']);
  if (r.exitCode !== 0) return null;
  const h = r.stdout.trim();
  return h || null;
}

/**
 * Read the porcelain status. Returns:
 *   { dirty: bool, lines: [{ status, path }] }
 * Empty `lines` ⇒ working tree clean. We use this both for the
 * project-open dirty-check AND for the reviewer-skip check.
 */
async function getStatus(dir) {
  // -z: NUL-terminated records with NO path quoting/octal-escaping, so
  // paths with spaces, backslashes, or non-ASCII (the production norm on
  // Windows) come through verbatim. core.quotePath=false belt-and-suspenders.
  const r = await runGit(dir, ['-c', 'core.quotePath=false', 'status', '--porcelain', '-z']);
  if (r.exitCode !== 0) {
    // CONTRACT: dirty===null means "status failed / unknown" — distinct from
    // dirty===false ("confirmed clean"). Callers MUST NOT treat a failure as
    // clean (which would silently skip auto-commit / the reviewer). A stale
    // .git/index.lock during a long build is the common trigger.
    return { dirty: null, failed: true, lines: [], error: r.stderr.trim() };
  }
  // Records are NUL-separated. A rename/copy entry (status starts with R or C)
  // emits TWO NUL-separated fields: "<XY> <new>\0<old>\0". We surface the NEW
  // path (the post-rename name) and consume the trailing old-name field.
  const records = r.stdout.split('\0');
  const lines = [];
  for (let i = 0; i < records.length; i++) {
    const rec = records[i];
    if (!rec) continue;
    const status = rec.slice(0, 2);
    const p = rec.slice(3);
    if (/^[RC]/.test(status)) {
      // The very next NUL field is the old name; skip it.
      i++;
    }
    lines.push({ status, path: p });
  }
  return { dirty: lines.length > 0, lines };
}

/**
 * Create + checkout a new branch from HEAD. Fails (exitCode !== 0) if
 * the branch already exists. Branch name is generated when not provided.
 */
async function createBranch(dir, name) {
  const branch = name || generateBranchName();
  const r = await runGit(dir, ['checkout', '-b', branch]);
  return { ...r, branch };
}

/**
 * CODE_BOSS_<YYYYMMDD-HHMMSS>-<rand>. Sortable + human-readable. The 4-char
 * random suffix prevents collisions when two branches are created in the same
 * wall-clock second (e.g. a multi-repo resolve batch, or rapid re-resolves) —
 * without it the second `git checkout -b` fails 'branch already exists' and
 * the protective-branch operation aborts.
 */
function generateBranchName() {
  const d = new Date();
  const p = (n) => String(n).padStart(2, '0');
  const ts = `${d.getFullYear()}${p(d.getMonth() + 1)}${p(d.getDate())}-${p(d.getHours())}${p(d.getMinutes())}${p(d.getSeconds())}`;
  const rand = Math.random().toString(36).slice(2, 6);
  return `CODE_BOSS_${ts}-${rand}`;
}

/** True if a name matches the CODE_BOSS_<timestamp> pattern. */
function isCodeBossBranch(name) {
  return typeof name === 'string' && /^CODE_BOSS_/.test(name);
}

/**
 * Stage everything then commit with the given message. If there's
 * nothing to commit (working tree clean), returns `{ skipped: true }`
 * instead of a failed commit. Otherwise resolves with the commit's
 * short hash on success.
 */
async function commitAll(dir, message) {
  const status = await getStatus(dir);
  if (!status.dirty) return { skipped: true, reason: 'working tree clean' };
  const add = await runGit(dir, ['add', '-A']);
  if (add.exitCode !== 0) {
    return { committed: false, error: 'git add failed: ' + (add.stderr.trim() || `exit ${add.exitCode}`), add };
  }
  // -F - reads message from stdin so multi-line + special chars round-trip cleanly.
  const commit = await runGit(dir, ['commit', '-F', '-'], { input: message });
  if (commit.exitCode !== 0) {
    return { committed: false, error: 'git commit failed: ' + (commit.stderr.trim() || `exit ${commit.exitCode}`), add, commit };
  }
  const hash = await getHeadHash(dir);
  return { committed: true, hash, add, commit };
}

/**
 * Strip Markdown / report formatting from a summary down to a clean one-line
 * commit subject. The worker's <next-task summary> sometimes pastes the
 * reviewer's Markdown verdict ("### Final Verdict\n- ✅ looks good"), which
 * makes for an ugly commit log. Take the first meaningful line, drop heading
 * hashes, leading bullets/numbering, surrounding emphasis, and emoji.
 */
function sanitizeSummary(summary) {
  const lines = String(summary || '').replace(/\r\n/g, '\n').split('\n');
  for (let raw of lines) {
    let s = raw
      .replace(/^[#>\s]+/, '')              // heading hashes / blockquote markers / indent
      .replace(/^[-*+•]\s+/, '')            // bullet markers
      .replace(/^\d+[.)]\s+/, '')           // numbered list "1." / "1)"
      .replace(/[*_`]+/g, '')               // emphasis / code ticks
      // strip a leading emoji + optional separator (✅ 🚀 etc.)
      .replace(/^[\u{1F000}-\u{1FFFF}\u{2190}-\u{27BF}\u{2B00}-\u{2BFF}️‍]+[\s:–—-]*/u, '')
      .replace(/\s+/g, ' ')
      .trim();
    // Skip pure-formatting lines (e.g. a "---" rule or an empty heading).
    if (s && !/^[-=_*\s]+$/.test(s)) return s;
  }
  return '';
}

/**
 * Build a commit message from the next-task attributes. Subject is the
 * summary (truncated to fit a normal git log column); body has the
 * user-request that prompted the work.
 */
function buildCommitMessage({ summary, userRequest, assignmentId }) {
  const tag = assignmentId ? `[${assignmentId}] ` : '';
  // Prefer the agent's summary (sanitized of any pasted Markdown); if it's
  // blank, fall back to the user-request (the one-line "what was wanted")
  // before the generic 'task', so a task closed with an empty <next-task>
  // summary doesn't produce a bare "task" commit.
  const base = sanitizeSummary(summary) || String(userRequest || '').replace(/\s+/g, ' ').trim() || 'task';
  const subject = (tag + base.replace(/\s+/g, ' ').trim()).slice(0, 72) || 'task';
  const body = userRequest ? `\n\nUser request: ${String(userRequest).trim()}` : '';
  const asg = assignmentId ? `\nAssignment: ${assignmentId}` : '';
  return subject + body + asg + '\n\n[code_boss auto-commit]\n';
}

// ── Git worktrees — orchestrator sub-agent isolation (ORCHESTRATOR.md) ──────────
// Each concurrent sub-agent runs in its OWN worktree (own dir + branch), so their writes
// are physically isolated and a merge-back conflict IS the deny-on-stale signal.
// Worktrees live OUTSIDE the project under the OS temp dir — NOT under the repo's
// `.code_boss/` (that segment is tool-blocked as control state) and NOT inside the project
// (so repo-discovery never mistakes them for sub-repos). Concurrent worktree/branch/merge
// ops on ONE repo touch the shared .git/index.lock, so git plumbing is serialized per repo.
const _repoGitLocks = new Map();   // repoDir -> tail Promise (per-repo git-op mutex)

// A short, stable key for a repo path (so two repos with the same basename don't collide).
function _repoKey(repoDir) {
  let h = 2166136261; const s = path.resolve(repoDir);
  for (let i = 0; i < s.length; i++) { h ^= s.charCodeAt(i); h = Math.imul(h, 16777619); }
  return (h >>> 0).toString(36);
}

/** Serialize git plumbing per repoRoot (worktrees isolate working dirs, NOT .git/index.lock). */
function withRepoGitLock(repoDir, fn) {
  const prev = _repoGitLocks.get(repoDir) || Promise.resolve();
  const next = prev.then(fn, fn);                 // run regardless of the prior op's outcome
  _repoGitLocks.set(repoDir, next.then(() => {}, () => {}));
  return next;
}

function worktreePathFor(repoDir, id) {
  return path.join(tmpdir(), 'cb-worktrees', `${path.basename(repoDir)}-${_repoKey(repoDir)}`, String(id));
}

/** Add a worktree at .code_boss/worktrees/<id> on a fresh branch off baseRef. */
async function addWorktree(repoDir, id, baseRef = 'HEAD') {
  return withRepoGitLock(repoDir, async () => {
    const wtPath = worktreePathFor(repoDir, id);
    await mkdir(path.dirname(wtPath), { recursive: true });
    let branch = `cb-sub/${id}`;
    let activePath = wtPath;
    let r = await runGit(repoDir, ['worktree', 'add', '--quiet', activePath, '-b', branch, baseRef]);
    if (r.exitCode !== 0 && /already (exists|used|checked out)|already exists/i.test(r.stderr)) {
      // Crash residue: a stale branch AND/OR a leftover dir at wtPath (git's metadata was pruned but
      // the directory survived — common after an EBUSY removal on Windows). A fresh branch alone
      // can't recover if the PATH is what's blocking, so also prune dangling metadata and remove the
      // residue dir — guarded to the cb-worktrees temp root so we can never delete anything else.
      try { await runGit(repoDir, ['worktree', 'prune']); } catch {}
      try {
        if (activePath.includes(`${path.sep}cb-worktrees${path.sep}`) && existsSync(activePath)) {
          await rm(activePath, { recursive: true, force: true });
        }
      } catch {}
      branch = `cb-sub/${id}-${Math.random().toString(36).slice(2, 6)}`;
      r = await runGit(repoDir, ['worktree', 'add', '--quiet', activePath, '-b', branch, baseRef]);
      if (r.exitCode !== 0) {
        // The path still won't take (e.g. a locked dir we couldn't remove). A BRAND-NEW path can
        // never be blocked by the old residue, so the id is never a permanent dead-end.
        activePath = `${wtPath}-${Math.random().toString(36).slice(2, 6)}`;
        r = await runGit(repoDir, ['worktree', 'add', '--quiet', activePath, '-b', branch, baseRef]);
      }
    }
    if (r.exitCode !== 0) return { ok: false, error: r.stderr.trim() || `worktree add exit ${r.exitCode}`, path: activePath };
    const wtPathFinal = activePath;
    // Exclude the sub-agent's own control store from the worktree so `git add -A` (commitAll)
    // NEVER commits .code_boss onto the branch — otherwise it collides with the manager's
    // .code_boss at integrate (an empty-files "merge failed", not a real content conflict).
    try { await ensureExcluded(wtPathFinal, '.code_boss/'); } catch {}
    return { ok: true, path: wtPathFinal, branch };
  });
}

/** Force-remove a worktree (incl. untracked scratch) + prune; optionally delete its branch. */
async function removeWorktree(repoDir, wtPath, { deleteBranch = null } = {}) {
  return withRepoGitLock(repoDir, async () => {
    const rm = await runGit(repoDir, ['worktree', 'remove', '--force', wtPath]);
    await runGit(repoDir, ['worktree', 'prune']);
    if (deleteBranch) await runGit(repoDir, ['branch', '-D', deleteBranch]);
    return { ok: rm.exitCode === 0, removed: wtPath, stderr: rm.stderr };
  });
}

/**
 * List recoverable sub-agent leftovers (crash-resume, R6): every cb-sub/* branch, paired
 * with its worktree dir if one still exists. After a crash these persist on disk (the work
 * is durable on the branch), so on project open we can surface them to re-review or discard.
 * Returns [{ id, branch, worktree|null, head|null }], newest-id first.
 */
async function listOrphanSubagents(repoDir) {
  const wtByBranch = new Map();
  for (const w of await listWorktrees(repoDir)) {
    if (w.branch && /^cb-sub\//.test(w.branch)) wtByBranch.set(w.branch, w);
  }
  const br = await runGit(repoDir, ['for-each-ref', '--format=%(refname:short)', 'refs/heads/cb-sub/']);
  const branches = br.exitCode === 0 ? br.stdout.split('\n').map((s) => s.trim()).filter(Boolean) : [];
  return branches
    .map((branch) => {
      const w = wtByBranch.get(branch) || null;
      return { id: branch.replace(/^cb-sub\//, ''), branch, worktree: w ? w.path : null, head: w ? w.head : null };
    })
    .sort((a, b) => (a.branch < b.branch ? 1 : -1));
}

/** Parse `git worktree list --porcelain` → [{ path, head, branch, detached }]. */
async function listWorktrees(repoDir) {
  const r = await runGit(repoDir, ['worktree', 'list', '--porcelain']);
  if (r.exitCode !== 0) return [];
  const trees = []; let cur = null;
  for (const line of r.stdout.split('\n')) {
    if (line.startsWith('worktree ')) { cur = { path: line.slice(9).trim() }; trees.push(cur); }
    else if (cur && line.startsWith('HEAD ')) cur.head = line.slice(5).trim();
    else if (cur && line.startsWith('branch ')) cur.branch = line.slice(7).trim().replace(/^refs\/heads\//, '');
    else if (cur && line.trim() === 'detached') cur.detached = true;
  }
  return trees;
}

/**
 * Merge a sub-agent `branch` back into the foreground branch in `repoDir`. The base may
 * have advanced (the foreground commits inline), so a conflict is EXPECTED and is the
 * deny-on-stale signal: capture the conflicted files, ABORT the merge (keep base clean),
 * and return them so the manager re-grounds. NEVER auto-resolve; the branch+worktree
 * persist for the re-ground. Returns { merged:true } | { merged:false, conflict:true, files }.
 */
async function integrate(repoDir, branch) {
  return withRepoGitLock(repoDir, async () => {
    const m = await runGit(repoDir, ['merge', '--no-ff', '--no-edit', branch]);
    if (m.exitCode === 0) return { merged: true };
    const u = await runGit(repoDir, ['diff', '--name-only', '--diff-filter=U']);
    const files = u.stdout.split('\n').map((s) => s.trim()).filter(Boolean);
    await runGit(repoDir, ['merge', '--abort']);   // restore base to clean; branch+worktree persist
    return { merged: false, conflict: true, files };
  });
}

/** Commits on `branch` that aren't on `parent` — how far ahead the CODE_BOSS branch is. */
async function aheadCount(repoDir, parent, branch) {
  const r = await runGit(repoDir, ['rev-list', '--count', `${parent}..${branch}`]);
  if (r.exitCode !== 0) return null;
  const n = parseInt((r.stdout || '').trim(), 10);
  return Number.isFinite(n) ? n : null;
}

/** Best guess at a repo's mainline (merge target) when the fork parent wasn't recorded:
 *  origin/HEAD's target, else a local main / master / develop, else null. */
async function getDefaultBranch(repoDir) {
  const sym = await runGit(repoDir, ['symbolic-ref', '--quiet', '--short', 'refs/remotes/origin/HEAD']);
  if (sym.exitCode === 0) { const b = (sym.stdout || '').trim().replace(/^origin\//, ''); if (b) return b; }
  for (const name of ['main', 'master', 'develop']) {
    const v = await runGit(repoDir, ['rev-parse', '--verify', '--quiet', name]);
    if (v.exitCode === 0) return name;
  }
  return null;
}

/** Merge the CODE_BOSS `branch` INTO `parent` (the developer's "ship it" action — the reverse of
 *  integrate): checkout parent, merge --no-ff, then return to `branch` so the agent keeps working
 *  there. Refuses on a dirty tree; aborts + reports + restores the work branch on conflict. */
async function mergeToParent(repoDir, branch, parent) {
  return withRepoGitLock(repoDir, async () => {
    const stp = await runGit(repoDir, ['status', '--porcelain']);
    if ((stp.stdout || '').trim()) return { merged: false, reason: 'dirty', message: 'uncommitted changes — let the current task finish (it commits on close) or commit first' };
    const co = await runGit(repoDir, ['checkout', parent]);
    if (co.exitCode !== 0) return { merged: false, reason: 'checkout-failed', message: (co.stderr || '').trim() || `could not checkout ${parent}` };
    const m = await runGit(repoDir, ['merge', '--no-ff', '--no-edit', branch]);
    if (m.exitCode !== 0) {
      const u = await runGit(repoDir, ['diff', '--name-only', '--diff-filter=U']);
      const files = u.stdout.split('\n').map((s) => s.trim()).filter(Boolean);
      await runGit(repoDir, ['merge', '--abort']);
      await runGit(repoDir, ['checkout', branch]);            // back to the work branch
      return { merged: false, reason: 'conflict', files, message: `merge conflict in: ${files.join(', ') || '(unknown)'}` };
    }
    const head = await getHeadHash(repoDir);
    await runGit(repoDir, ['checkout', branch]);              // return to the CODE_BOSS branch
    return { merged: true, parent, branch, commit: head };
  });
}

// ====== agent/subagent-worktree.mjs ======
// Worktree-wrapped sub-agent runner — the orchestrator's runWorker for git projects.
//
// Composes the git-worktree substrate (git-workflow.mjs) with a sub-agent worker. A
// sub-agent runs in its OWN worktree (own dir + branch), so its writes are physically
// isolated. Two modes:
//
//  • HELD (R1, the orchestrator path) — runs the FULL worker (runPromptedAgent, its own
//    tasks-v2 store at <worktree>/.code_boss) and HOLDS on finish: commit the branch, then
//    return { ...content, store, storeDir, branch, worktree, awaitingReview:true } WITHOUT
//    integrating/removing. integrate + removeWorktree happen ONLY on ACCEPT (the caller's
//    onApprove). resumeWorktreeSubAgent re-runs the worker in the SAME worktree on reject.
//
//  • AUTO-INTEGRATE (legacy, store-free) — runSubAgent → commit → integrate → remove. Kept
//    byte-for-byte as the default until CB_ORCHESTRATOR flips on.
//
// Out-of-repo writes + external shell effects are NOT isolated/rolled back (best-effort).
// Crash: the worktree dir + branch are git-durable (listWorktrees + cb-sub/* → re-attach).


const asResult = (r) => (r && typeof r === 'object' ? { ...r } : { content: String(r ?? '') });
const storeDirOf = (wtPath) => path.join(wtPath, '.code_boss', 'tasks');
// commitAll → { skipped:true } | { committed:true, hash } | { committed:false, error }
const commitFailed = (c) => c.committed === false;

async function runWorktreeSubAgent({ repoDir, id, topic = id, baseRef = 'HEAD', commitMessage = null, held = false, ...opts }) {
  if (!repoDir) throw new Error('runWorktreeSubAgent: repoDir required');
  if (!id) throw new Error('runWorktreeSubAgent: id required');
  const wt = await addWorktree(repoDir, id, baseRef);
  if (!wt.ok) throw new Error(`runWorktreeSubAgent: worktree add failed: ${wt.error}`);

  let result;
  try {
    result = held
      ? asResult(await runPromptedAgent({ ...opts, projectDir: wt.path, requireBranch: () => 'ok' }))
      : asResult(await runSubAgent({ ...opts, topic, projectDir: wt.path }));
  } catch (e) {
    // the sub-agent crashed — preserve its worktree + branch for inspection/resume
    return { content: `(sub-agent failed: ${e && e.message ? e.message : String(e)})`, error: true, branch: wt.branch, worktree: wt.path };
  }

  const committed = await commitAll(wt.path, commitMessage || `sub-agent ${topic}`);

  // ── HELD (R1): seal the branch + hold for review; integrate/remove only on accept ──
  if (held) {
    if (commitFailed(committed)) {
      return { content: result.content, error: true, commitError: committed.error, branch: wt.branch, worktree: wt.path };
    }
    return {
      content: result.content, store: result.store, storeDir: storeDirOf(wt.path),
      branch: wt.branch, worktree: wt.path, commit: committed.hash, empty: !!committed.skipped,
      awaitingReview: true,
    };
  }

  // ── AUTO-INTEGRATE (legacy, store-free, unchanged) ──────────────────────────────
  if (committed.skipped) {                                   // read-only sub-agent — nothing to merge
    await removeWorktree(repoDir, wt.path, { deleteBranch: wt.branch });
    return { ...result, integrated: true, empty: true };
  }
  if (commitFailed(committed)) {                             // couldn't commit — preserve for inspection
    return { ...result, error: true, commitError: committed.error, branch: wt.branch, worktree: wt.path };
  }
  const merge = await integrate(repoDir, wt.branch);
  if (merge.merged) {
    await removeWorktree(repoDir, wt.path, { deleteBranch: wt.branch });
    return { ...result, integrated: true, commit: committed.hash };
  }
  const files = merge.files || [];
  const note = `\n\n⚠ Could not auto-merge back — conflict in: ${files.join(', ') || '(unknown)'}. The work is preserved on branch ${wt.branch}; it needs re-grounding before it can land.`;
  return { ...result, content: (result.content || '') + note, conflict: true, files, branch: wt.branch, worktree: wt.path };
}

// Resume a HELD sub-agent in its EXISTING worktree (NO addWorktree — it persists) with the
// reviewer's feedback as the new task. The worker reloads its store from disk so prior
// turns are intact. Re-seals the branch + returns the same held shape for re-review.
async function resumeWorktreeSubAgent({ worktree, branch, feedback, topic = 'sub-agent', commitMessage = null, ...opts }) {
  if (!worktree || !branch) throw new Error('resumeWorktreeSubAgent: worktree + branch required');
  let result;
  try {
    result = asResult(await runPromptedAgent({
      ...opts,
      messages: [{ role: 'user', content: String(feedback || 'Please revise your work per the review feedback.') }],
      projectDir: worktree, requireBranch: () => 'ok',
    }));
  } catch (e) {
    return { content: `(sub-agent resume failed: ${e && e.message ? e.message : String(e)})`, error: true, branch, worktree };
  }
  const committed = await commitAll(worktree, commitMessage || `sub-agent ${topic} (revision)`);
  if (commitFailed(committed)) {
    return { content: result.content, error: true, commitError: committed.error, branch, worktree };
  }
  return {
    content: result.content, store: result.store, storeDir: storeDirOf(worktree),
    branch, worktree, commit: committed.hash, empty: !!committed.skipped, awaitingReview: true,
  };
}

// ====== agent/orchestrator.mjs ======
// Orchestrator — the top-level agent's delegate-don't-juggle lifecycle (see ORCHESTRATOR.md).
//
// ONE continuous foreground conversation whose ROLE oscillates (dev ⇄ manager). It does no
// heavy work itself once concurrency appears — it DELEGATES to sub-agents in isolated
// contexts and folds back only their BOUNDED results. A delegated sub-agent is a FULL worker
// in its own git worktree; on finish it is HELD awaiting-review, and a dedicated ACCEPTANCE
// agent (injected runAcceptance) decides approve (integrate + import + despawn, via onApprove)
// or reject (feedback → the sub-agent RESUMES → new result → re-review), hard-capped at
// MAX_REVIEW_ROUNDS. The manager never ingests the sub-agent's transcript — only a result
// card + the acceptance verdict (the both-sides bridge) enter the foreground.
//
// Pure + deterministic: runWorker / runAcceptance / onApprove / onReject / onCard / onEvent
// are injected, so it's unit-testable with mocks. COPY DISCIPLINE: message objects are
// CLONED at every boundary (the real worker rewrites .content/.role in place).

const DEFAULT_NOTES = {
  worker: (topic) => `You are now a sub-agent. Continue working ONLY on this task: ${topic}. The parent agent has taken on a separate concern — ignore unrelated discussion above. When done, report your result concisely.`,
  manager: (topic, id) => `You handed task "${topic}" to background sub-agent ${id} because a new concern arrived. You are now the MANAGER for it: track it, do NOT touch its files; its result will fold back in when it finishes. Continue with the user.`,
  card: (topic, id, result) => `Background sub-agent ${id} finished task "${topic}":\n${result}`,
  result: (topic, id, text) => `Background sub-agent ${id} reported a result for "${topic}" (awaiting review):\n${text}`,
  approved: (topic, id, text, fb) => `Background sub-agent ${id} finished task "${topic}" — approved${fb ? ` (${fb})` : ''}:\n${text}`,
  exhausted: (topic, id, rounds) => `Background sub-agent ${id} task "${topic}" was REJECTED after ${rounds} review rounds; its work is preserved on its branch for manual follow-up.`,
  unmergeable: (topic, id, rounds) => `Background sub-agent ${id} task "${topic}" was APPROVED but could not be merged after ${rounds} re-ground attempts (the base kept moving); its work is preserved on its branch for manual integration.`,
  feedback: (fb) => `Review feedback — revise your work accordingly, then report the result:\n${fb}`,
};

const MAX_REVIEW_ROUNDS = 3;   // bounds the reject→resume loop

function createOrchestrator({ runWorker, runAcceptance = null, onApprove = null, onReject = null, onDespawn = null, autoReview = true, onCard = null, onEvent = null, notes = {}, subagentTimeoutMs = 30 * 60 * 1000 } = {}) {
  if (typeof runWorker !== 'function') throw new Error('createOrchestrator: runWorker(seed, ctx) is required');
  const note = { ...DEFAULT_NOTES, ...notes };

  const fg = { messages: [], mode: 'inline' };   // the ONE continuous foreground (role oscillates)
  const subagents = new Map();                    // id -> entry
  const cleanups = [];                            // best-effort despawn-cleanup promises (settled() awaits)
  let nextId = 1;

  const emit = (e) => { try { onEvent?.(e); } catch {} };
  const cloneMsgs = (arr) => (arr || []).map((m) => { try { return structuredClone(m); } catch { return { ...m }; } });

  function resultText(result, err) {
    if (err) return err.aborted ? '(aborted)' : `(failed: ${err && err.message ? err.message : String(err)})`;
    if (result == null) return '';
    if (typeof result === 'string') return result;
    if (typeof result.content === 'string') return result.content;
    try { return JSON.stringify(result); } catch { return String(result); }
  }

  // Despawn: fold the FINAL card, de-escalate when empty. The terminal transition.
  function finalize(id, finalCard) {
    const e = subagents.get(id);
    if (!e) return null;
    const handle = e.handle;
    if (e.timer) { clearTimeout(e.timer); e.timer = null; }   // disarm the wall-clock guard
    subagents.delete(id);
    fg.messages.push(finalCard);
    if (subagents.size === 0) fg.mode = 'inline';
    emit({ type: 'subagent-done', id, topic: finalCard.topic, status: finalCard.status, deEscalated: fg.mode === 'inline' });
    try { onCard?.(finalCard); } catch {}
    // Despawn cleanup: a non-'done' terminal (aborted/error/rejected/exhausted/conflict)
    // leaves the held worktree behind — onApprove already cleaned up the 'done' path. Fire
    // the injected cleanup best-effort; settled() awaits it so tests + shutdown can wait.
    if (onDespawn && finalCard.status !== 'done' && handle && handle.worktree) {
      try { cleanups.push(Promise.resolve(onDespawn({ id, status: finalCard.status, handle })).catch(() => {})); } catch {}
    }
    return finalCard;
  }

  function exhaustedCard(e) {
    return { role: 'user', meta: 'completion-card', subagentId: e.id, topic: e.topic, status: 'rejected', content: note.exhausted(e.topic, e.id, MAX_REVIEW_ROUNDS) };
  }
  function unmergeableCard(e) {   // APPROVED but never merged (distinct from quality-rejected)
    return { role: 'user', meta: 'completion-card', subagentId: e.id, topic: e.topic, status: 'unmergeable', content: note.unmergeable(e.topic, e.id, MAX_REVIEW_ROUNDS) };
  }

  // A worker run settled. Aborted / hard error / unmergeable → finalize now (nothing clean
  // to review). Otherwise HOLD awaiting-review (fold a result card; do NOT delete/de-escalate)
  // and, if autoReview, kick the acceptance review.
  function settle(id, result, err) {
    const e = subagents.get(id);
    if (!e) return null;                            // already reaped (aborted, then the worker also settled)
    if (err || (result && (result.error || result.conflict))) {
      const status = err ? (err.aborted ? 'aborted' : 'error') : (result.conflict ? 'conflict' : 'error');
      return finalize(id, { role: 'user', meta: 'completion-card', subagentId: id, topic: e.topic, status, content: note.card(e.topic, id, resultText(result, err)) });
    }
    // No acceptance configured → fire-and-forget (the delegate-only model): finalize with a
    // done card immediately, exactly as before the review loop existed.
    if (typeof runAcceptance !== 'function') {
      return finalize(id, { role: 'user', meta: 'completion-card', subagentId: id, topic: e.topic, status: 'done', content: note.card(e.topic, id, resultText(result, null)) });
    }
    e.status = 'awaiting-review';
    e.result = result;
    e.resultText = resultText(result, null);
    e.handle = { branch: result && result.branch, worktree: result && result.worktree, storeDir: result && result.storeDir, store: result && result.store };
    const card = { role: 'user', meta: 'result-card', subagentId: id, topic: e.topic, status: 'awaiting-review', content: note.result(e.topic, id, e.resultText) };
    fg.messages.push(card);
    emit({ type: 'subagent-result', id, topic: e.topic, status: 'awaiting-review' });
    try { onCard?.(card); } catch {}
    if (autoReview && typeof runAcceptance === 'function') review(id);
    return card;
  }

  function launch(id, topic, seed) {
    const abort = new AbortController();
    const entry = { id, topic, status: 'working', abort, promise: null, reviewPromise: null, round: 1, handle: null, result: null, resultText: '', feedback: [], seed, timer: null };
    subagents.set(id, entry);
    // Wall-clock guard: a hung sub-agent (e.g. a stalled adapter socket that never responds) would
    // otherwise sit in a non-terminal state forever, keeping the manager escalated. Abort it after
    // a generous timeout → settle → finalize ('aborted'), de-escalating. unref so it never holds the
    // process open; cleared in finalize. Covers the full lifetime including review resumes.
    if (subagentTimeoutMs > 0) {
      entry.timer = setTimeout(() => { try { emit({ type: 'subagent-timeout', id, topic }); } catch {} try { abort.abort(); } catch {} }, subagentTimeoutMs);
      entry.timer?.unref?.();
    }
    emit({ type: 'subagent-spawned', id, topic });
    let p;
    try { p = Promise.resolve(runWorker(seed, { id, topic, signal: abort.signal })); }
    catch (e) { p = Promise.reject(e); }
    entry.promise = p.then((result) => settle(id, result, null), (err) => settle(id, null, err));
    return entry;
  }

  // Re-run the worker in the SAME worktree (resume:true + handle) with reviewer feedback.
  function resumeLaunch(entry, feedback) {
    const seed = [{ role: 'user', meta: 'review-feedback', content: note.feedback(feedback) }];
    entry.status = 'working';
    let p;
    try { p = Promise.resolve(runWorker(seed, { id: entry.id, topic: entry.topic, signal: entry.abort.signal, resume: true, handle: entry.handle })); }
    catch (e) { p = Promise.reject(e); }
    entry.promise = p.then((result) => settle(entry.id, result, null), (err) => settle(entry.id, null, err));
  }

  // Spawn the Acceptance agent for an awaiting-review sub-agent.
  function review(id) {
    const e = subagents.get(id);
    if (!e || e.status !== 'awaiting-review' || typeof runAcceptance !== 'function') return false;
    e.status = 'reviewing';
    emit({ type: 'subagent-reviewing', id, topic: e.topic, round: e.round });
    const ctx = { id, topic: e.topic, originalRequest: e.seed, conversationChunk: cloneMsgs(fg.messages), handle: e.handle, result: e.result, round: e.round, signal: e.abort.signal };
    e.reviewPromise = Promise.resolve(runAcceptance(ctx)).then(
      (v) => resolveReview(id, v),
      (err) => resolveReview(id, { approved: false, feedback: `(acceptance failed: ${err && err.message ? err.message : String(err)})` }),
    );
    return true;
  }

  async function resolveReview(id, verdict) {
    const e = subagents.get(id);
    if (!e) return false;                           // aborted mid-review
    const approved = verdict === true || (verdict && verdict.approved === true);
    const feedback = typeof verdict === 'string' ? verdict : ((verdict && verdict.feedback) || '');
    emit({ type: 'subagent-verdict', id, topic: e.topic, approved, feedback, round: e.round });
    if (approved) {
      let outcome;
      try { outcome = onApprove ? await onApprove({ id, topic: e.topic, handle: e.handle, result: e.result }) : { integrated: true }; }
      catch (err) { outcome = { error: (err && err.message) || String(err) }; }
      // The user may have aborted DURING the await — the entry is gone (finalized). Do not
      // resume a zombie worker or double-finalize; the integrate already happened, so just stop.
      if (!subagents.has(id)) return false;
      if (outcome && outcome.conflict) {            // approve-time integration conflict → re-ground
        emit({ type: 'subagent-verdict-conflict', id, files: outcome.files || [] });
        e.round++;
        if (e.round > MAX_REVIEW_ROUNDS) { emit({ type: 'subagent-review-exhausted', id, topic: e.topic, reason: 'unmergeable' }); return finalize(id, unmergeableCard(e)); }
        resumeLaunch(e, `Your change no longer merges cleanly — conflict in: ${(outcome.files || []).join(', ') || '(unknown)'}. Re-ground against the latest base and resolve, then report.`);
        return true;
      }
      return finalize(id, { role: 'user', meta: 'completion-card', subagentId: id, topic: e.topic, status: 'done', content: note.approved(e.topic, id, e.resultText, feedback) });
    }
    // reject → resume with feedback, capped
    e.round++;
    if (e.round > MAX_REVIEW_ROUNDS) {
      emit({ type: 'subagent-review-exhausted', id, topic: e.topic });
      return finalize(id, exhaustedCard(e));
    }
    e.feedback.push(feedback);
    try { if (onReject) await onReject({ id, topic: e.topic, handle: e.handle, feedback, round: e.round }); } catch {}
    if (!subagents.has(id)) return false;           // aborted during onReject → don't resume a zombie
    emit({ type: 'subagent-resume', id, topic: e.topic, round: e.round, feedback });
    resumeLaunch(e, feedback);
    return true;
  }

  // ── vanilla delegate (3rd+ task): a FRESH sub-agent, no context copy ───────────
  function delegate(topic, brief) {
    const id = `sa-${nextId++}`;
    const seed = [{ role: 'user', meta: 'task-brief', topic, content: brief }];
    fg.mode = 'managing';
    launch(id, topic, seed);
    return { subagentId: id, seed };
  }

  // ── crash-resume (R6): re-attach a leftover sub-agent worktree as an awaiting-review
  // entry (no worker run — its work is already committed on the branch). The user then
  // re-reviews it (review(id) → the acceptance loop, handle.store falls back to disk) or
  // discards it (abort(id) → onDespawn removes the worktree + branch). Surfaced, not auto-run.
  function adopt({ id, topic = id, handle }) {
    // Dedupe is emit-suppression only: a second adopt of a live id returns null (no
    // duplicate subagent-recovered broadcast) but the existing entry stays in the map,
    // so subagents() — and the server's chatStateSnapshot built from it — still lists it.
    if (!id || !handle || subagents.has(id)) return null;
    const abort = new AbortController();
    const entry = { id, topic, status: 'awaiting-review', abort, promise: null, reviewPromise: null, round: 1, handle, result: { content: '(recovered from a previous session)' }, resultText: '(recovered from a previous session)', feedback: [], seed: [{ role: 'user', content: topic }], recovered: true };
    subagents.set(id, entry);
    if (fg.mode === 'inline') fg.mode = 'managing';
    emit({ type: 'subagent-recovered', id, topic, handle });
    return { subagentId: id };
  }

  // ── THE FORK (1→2) — deferred (R5); for now degrades to delegate ───────────────
  function escalate(topic, brief) {
    if (fg.mode === 'managing' || subagents.size > 0) {
      emit({ type: 'escalate-degraded-to-delegate', topic });
      return delegate(topic, brief != null ? brief : note.worker(topic));
    }
    const id = `sa-${nextId++}`;
    const seed = [...cloneMsgs(fg.messages), { role: 'user', meta: 'worker-note', content: note.worker(topic) }];
    fg.messages.push({ role: 'user', meta: 'manager-note', subagentId: id, topic, content: note.manager(topic, id) });
    fg.mode = 'managing';
    launch(id, topic, seed);
    return { subagentId: id, seed };
  }

  // per-sub-agent abort: signal + finalize from ANY state (working/awaiting-review/reviewing).
  // A later settle / resolveReview of its own promise is a no-op (entry gone).
  function abort(id) {
    const e = subagents.get(id);
    if (!e) return false;
    e.status = 'aborting';
    try { e.abort.abort(); } catch {}
    const ae = new Error('aborted'); ae.aborted = true;
    finalize(id, { role: 'user', meta: 'completion-card', subagentId: id, topic: e.topic, status: 'aborted', content: note.card(e.topic, id, resultText(null, ae)) });
    return true;
  }

  function seedForeground(messages) {
    if (subagents.size > 0) throw new Error('orchestrator: cannot reset the foreground while sub-agents are in flight');
    fg.messages = cloneMsgs(messages);
  }

  async function settled() {
    // Drain through the FULL review loop: each round can REPLACE e.promise (resume) or ADD
    // e.reviewPromise. Await the current promises, then stop when the set is STABLE — empty
    // (all despawned) OR unchanged (held awaiting-review with autoReview off, nothing pending).
    const snap = () => [...subagents.values()].flatMap((e) => [e.promise, e.reviewPromise]).filter(Boolean);
    for (let i = 0; i < (MAX_REVIEW_ROUNDS + 3) * 4 + 8; i++) {
      const before = snap();
      if (before.length === 0) break;
      await Promise.allSettled(before);
      const after = snap();
      if (after.length === 0 || !after.some((p) => !before.includes(p))) break;   // empty or no new promise
    }
    if (cleanups.length) await Promise.allSettled(cleanups.splice(0));   // drain best-effort despawn cleanups
  }

  return {
    seedForeground,
    appendUser(content) { fg.messages.push(cloneMsgs([{ role: 'user', content }])[0]); },
    foreground() { return { mode: fg.mode, messages: cloneMsgs(fg.messages) }; },
    escalate, delegate, abort, adopt,
    review, resolveReview,                          // exposed for manual/test drive (autoReview:false)
    subagents() { return [...subagents.values()].map((e) => ({ id: e.id, topic: e.topic, status: e.status, round: e.round, awaitingReview: e.status === 'awaiting-review', recovered: !!e.recovered })); },
    settled,
  };
}

// ====== agent/project-context.mjs ======
/**
 * Discover and format per-project context from AGENTS.md files.
 *
 * On project open, walk the project directory recursively looking for
 * files named AGENTS.md (the cross-tool standard) — or the legacy
 * CODE-BOSS.md, kept as a back-compat alias so projects authored before
 * the rename keep working without a migration. Each directory that
 * contains one is treated as a project; its contents get injected into
 * the agent's system prompt every turn so the model has the
 * project-specific instructions the developer has captured there.
 *
 * Two render modes:
 *   - Exactly one file, AT the project root → include its text directly
 *     under a "project context" heading.
 *   - Anything else (one in a subdir, or multiple) → list each found
 *     file with its relative directory path. Useful when the open
 *     directory is a workspace that contains several project subdirs
 *     and the agent needs to know which one is which.
 *
 * The whole thing is bounded so a runaway tree (deep node_modules, etc.)
 * can't stall project open or blow up the prompt.
 */

// Directories we will not recurse into. Build outputs, version-control
// internals, dependency caches — none of these legitimately contain
// an AGENTS.md, and walking them is expensive.
const SKIP_DIRS = new Set([
  '.git', '.hg', '.svn',
  '.code_boss', '.agent',
  '.vs', '.idea', '.vscode',
  'node_modules', 'bower_components',
  'target', 'dist', 'build', 'out', 'bin', 'obj',
  '__pycache__', '.gradle', '.mvn',
]);

// Recognized project-context filenames, in priority order. AGENTS.md is the
// cross-tool standard; CODE-BOSS.md is the legacy alias we still honor. If a
// directory has both, the first one in this list wins (AGENTS.md), so a project
// can migrate by adding AGENTS.md without first deleting the old file.
const CONTEXT_FILES = ['AGENTS.md', 'CODE-BOSS.md'];
const MAX_DEPTH = 6;             // root + 6 levels — deeper is almost always inside a dependency
const MAX_FILES = 25;            // hard stop so a generated tree can't pile up forever
const MAX_FILE_BYTES = 16 * 1024;  // 16KB per file is plenty for project instructions
const MAX_TOTAL_BYTES = 200 * 1024; // hard cap on the combined output

/**
 * Walk `rootDir` looking for project-context files (AGENTS.md or the legacy
 * CODE-BOSS.md). Returns
 *   [{ relDir, absPath, name, content, truncated, originalBytes }, ...]
 * with relDir relative to rootDir ('.' for the root itself) and `name` the
 * actual filename that matched.
 */
async function discoverCodeBossFiles(rootDir) {
  const found = [];
  let totalBytes = 0;

  async function walk(dir, depth) {
    if (depth > MAX_DEPTH) return;
    if (found.length >= MAX_FILES) return;
    if (totalBytes >= MAX_TOTAL_BYTES) return;
    let entries;
    try { entries = await readdir(dir, { withFileTypes: true }); }
    catch { return; }   // permission denied, broken symlink, etc.
    // Read the file in this dir first, then descend — gives a stable
    // top-down order in the result. At most one context file per directory:
    // prefer AGENTS.md, fall back to the legacy CODE-BOSS.md only when
    // AGENTS.md isn't present here (so both can coexist during a migration).
    const present = new Set();
    for (const e of entries) if (e.isFile()) present.add(e.name);
    const pick = CONTEXT_FILES.find((n) => present.has(n));
    if (pick) {
      const abs = path.join(dir, pick);
      try {
        const buf = await readFile(abs, 'utf8');
        const truncated = buf.length > MAX_FILE_BYTES;
        const content = truncated
          ? buf.slice(0, MAX_FILE_BYTES) + '\n[... truncated]'
          : buf;
        totalBytes += content.length;
        const rel = path.relative(rootDir, dir) || '.';
        found.push({ relDir: rel, absPath: abs, name: pick, content, truncated, originalBytes: buf.length });
        if (found.length >= MAX_FILES) return;
        if (totalBytes >= MAX_TOTAL_BYTES) return;
      } catch { /* unreadable; skip */ }
    }
    for (const e of entries) {
      if (!e.isDirectory()) continue;
      if (e.name.startsWith('.')) continue;
      if (SKIP_DIRS.has(e.name)) continue;
      await walk(path.join(dir, e.name), depth + 1);
      if (found.length >= MAX_FILES) return;
      if (totalBytes >= MAX_TOTAL_BYTES) return;
    }
  }

  try {
    const s = await stat(rootDir);
    if (!s.isDirectory()) return [];
  } catch { return []; }

  await walk(rootDir, 0);
  return found;
}

/**
 * Format a discovered-file list as a system-prompt-ready string.
 * Empty input → ''.  Single file in the root → its text directly under
 * a heading.  Anything else → a list of sections keyed by relative dir.
 */
function formatProjectContext(found) {
  if (!Array.isArray(found) || found.length === 0) return '';

  if (found.length === 1 && found[0].relDir === '.') {
    return [
      `The developer has placed a project-instructions file (${found[0].name || 'AGENTS.md'})`,
      'in the project root with instructions specific to this project. Follow these:',
      '',
      String(found[0].content).trim(),
    ].join('\n');
  }

  const sections = found.map((f) => {
    const heading = f.relDir === '.' ? '(project root)' : f.relDir.split(path.sep).join('/');
    return `### ${heading}/${f.name || 'AGENTS.md'}\n\n${String(f.content).trim()}`;
  });
  return [
    `The active directory contains ${found.length} project-instructions file(s), one per`,
    'sub-project. Each section below names the directory and gives the',
    'developer\'s instructions for that part of the codebase. Use the relevant',
    'one(s) when working inside a given subdirectory:',
    '',
    sections.join('\n\n────────\n\n'),
  ].join('\n');
}

/**
 * One-shot helper: discover + format.
 * Returns the prompt-ready string (possibly empty).
 */
async function loadProjectContext(rootDir) {
  if (!rootDir) return { context: '', files: [] };
  const files = await discoverCodeBossFiles(rootDir);
  return { context: formatProjectContext(files), files };
}

// ====== agent/assignments.mjs ======
/**
 * Assignment-scoped store — the durable BACKLOG of real-world assignments.
 *
 * An "assignment" is one ratified bundle of work with a lifecycle:
 *   capturing -> ready -> active -> accepted   (or parked)
 * It is the PROSPECTIVE form of a "project" but is stored SEPARATELY from the
 * tier-4 task-cluster rollup (which is a token-budget compaction artifact). The
 * assignment references the tasks that fulfil it via `taskIds`; its own
 * identity/requirement/commit-evidence live here and survive task demotion.
 *
 * Unlike tasks.mjs there are NO tiers, messages, or compaction here — this is a
 * small, durable index. Writes are atomic (temp + rename) since the backlog
 * outlives any single conversation (capture-now-work-later).
 *
 * Storage layout (per project):
 *   <project>/.code_boss/assignments/
 *     state.json   { version, nextId, order: ["A-2","A-1"], activeId }
 *     A-1.json     full AssignmentRecord
 *
 * AssignmentRecord:
 *   {
 *     id:                 "A-3",
 *     title:              "Reconcile App-A/App-B runbooks",
 *     requirement:        "..." | null,    frozen "definition of done" (set on capture)
 *     acceptanceCriteria: "..." | null,    free-text for v1
 *     sources:            [{ kind:'email'|'spec'|'note'|'file', text, capturedAt }],
 *     status:             'capturing'|'ready'|'active'|'accepted'|'parked',
 *     createdAt, readyAt, startedAt, acceptedAt:  epoch ms | null,
 *     taskIds:            ["T-7","T-8"],    worker tasks bound to this assignment
 *     commitHashes:       [...],            durable evidence (survives demotion)
 *     lastVerdict:        { round, fulfilled, missing, drift, rationale } | null,
 *     checkpoints:        [{ kind, at, userAction }],  UI audit — NEVER fed to the LLM
 *   }
 */

const ASSIGN_STATE_FILE = 'state.json';
const ASSIGN_FILE = (id) => `${id}.json`;
const STATUSES = new Set(['capturing', 'ready', 'active', 'accepted', 'parked']);
const STATUS_STAMP = { ready: 'readyAt', active: 'startedAt', accepted: 'acceptedAt' };

function assignNowMs() { return Date.now(); }

function createAssignmentsStore(projectDir) {
  if (!projectDir) throw new Error('projectDir required');
  const dir = path.join(projectDir, '.code_boss', 'assignments');

  let state = null;            // { version, nextId, order: [], activeId }
  const cache = new Map();     // id -> AssignmentRecord

  async function ensureDir() { await mkdir(dir, { recursive: true }); }

  async function writeJsonAtomic(file, obj) {
    await ensureDir();
    const tmp = file + '.tmp';
    await writeFile(tmp, JSON.stringify(obj, null, 2), 'utf8');
    await rename(tmp, file);
  }

  async function saveState() { await writeJsonAtomic(path.join(dir, ASSIGN_STATE_FILE), state); }
  async function loadState() {
    try { return JSON.parse(await readFile(path.join(dir, ASSIGN_STATE_FILE), 'utf8')); }
    catch { return null; }
  }
  async function loadAssignment(id) {
    if (cache.has(id)) return cache.get(id);
    try {
      const a = JSON.parse(await readFile(path.join(dir, ASSIGN_FILE(id)), 'utf8'));
      cache.set(id, a);
      return a;
    } catch { return null; }
  }
  async function saveAssignment(a) {
    cache.set(a.id, a);
    await writeJsonAtomic(path.join(dir, ASSIGN_FILE(a.id)), a);
  }

  function newId() {
    const id = `A-${state.nextId}`;
    state.nextId++;
    return id;
  }

  async function init() {
    await ensureDir();
    state = await loadState();
    if (!state) {
      state = { version: 1, nextId: 1, order: [], activeId: null };
      await saveState();
    }
    return state;
  }

  /** Create a new assignment (default status 'capturing') at the END of the
   *  backlog. The requirement is null until intake freezes it. */
  async function create({ title = 'Untitled assignment', requirement = null, acceptanceCriteria = null, sources = [], status = 'capturing' } = {}) {
    const id = newId();
    const a = {
      id,
      title,
      requirement,
      acceptanceCriteria,
      sources: Array.isArray(sources) ? sources : [],
      status: STATUSES.has(status) ? status : 'capturing',
      createdAt: assignNowMs(),
      readyAt: null, startedAt: null, acceptedAt: null,
      taskIds: [],
      commitHashes: [],
      lastVerdict: null,
      checkpoints: [],
    };
    if (STATUS_STAMP[a.status]) a[STATUS_STAMP[a.status]] = a.createdAt;
    await saveAssignment(a);
    state.order.push(id);
    await saveState();
    return a;
  }

  async function get(id) { return loadAssignment(id); }

  /** Assignments in backlog order (index 0 = front/next). */
  async function list() {
    const out = [];
    for (const id of state.order) {
      const a = await loadAssignment(id);
      if (a) out.push(a);
    }
    return out;
  }

  function activeId() { return state?.activeId ?? null; }
  async function getActive() { return state?.activeId ? loadAssignment(state.activeId) : null; }

  /** Merge fields into an assignment. A `status` change auto-stamps its
   *  timestamp (readyAt/startedAt/acceptedAt). */
  async function patch(id, fields = {}) {
    const a = await loadAssignment(id);
    if (!a) return null;
    if (fields.status && !STATUSES.has(fields.status)) throw new Error(`invalid status: ${fields.status}`);
    Object.assign(a, fields);
    if (fields.status && STATUS_STAMP[fields.status] && !a[STATUS_STAMP[fields.status]]) {
      a[STATUS_STAMP[fields.status]] = assignNowMs();
    }
    await saveAssignment(a);
    return a;
  }

  /** Replace the backlog ordering. `newOrder` must be a permutation of the
   *  current ids (extra/missing ids are rejected). */
  async function reorder(newOrder) {
    if (!Array.isArray(newOrder)) throw new Error('reorder expects an array of ids');
    const cur = new Set(state.order);
    const next = new Set(newOrder);
    // Must be a true permutation: same length, no duplicates, exact same id set.
    // (Set-only membership checks would let ['A-1','A-2','A-2'] through.)
    if (newOrder.length !== state.order.length || next.size !== newOrder.length
        || cur.size !== next.size || [...cur].some((id) => !next.has(id))) {
      throw new Error('reorder must be a permutation of the existing assignment ids');
    }
    state.order = [...newOrder];
    await saveState();
    return state.order;
  }

  /** Move one assignment to a new index in the backlog. */
  async function move(id, toIndex) {
    const from = state.order.indexOf(id);
    if (from < 0) return state.order;
    state.order.splice(from, 1);
    const idx = Math.max(0, Math.min(state.order.length, Number(toIndex) || 0));
    state.order.splice(idx, 0, id);
    await saveState();
    return state.order;
  }

  async function setActive(id) {
    if (id != null && !state.order.includes(id)) throw new Error(`no such assignment: ${id}`);
    state.activeId = id ?? null;
    await saveState();
    return state.activeId;
  }

  /** Bind a worker task to an assignment (the join key). */
  async function addTask(id, taskId) {
    const a = await loadAssignment(id);
    if (!a || !taskId) return a;
    if (!a.taskIds.includes(taskId)) { a.taskIds.push(taskId); await saveAssignment(a); }
    return a;
  }
  async function addCommit(id, hash) {
    const a = await loadAssignment(id);
    if (!a || !hash) return a;
    if (!a.commitHashes.includes(hash)) { a.commitHashes.push(hash); await saveAssignment(a); }
    return a;
  }
  async function addSource(id, source) {
    const a = await loadAssignment(id);
    if (!a) return a;
    a.sources.push({ capturedAt: assignNowMs(), ...source });
    await saveAssignment(a);
    return a;
  }
  async function setVerdict(id, verdict) { return patch(id, { lastVerdict: verdict }); }
  async function checkpoint(id, entry) {
    const a = await loadAssignment(id);
    if (!a) return a;
    a.checkpoints.push({ at: assignNowMs(), ...entry });
    await saveAssignment(a);
    return a;
  }

  /** Remove an assignment entirely (e.g. an abandoned capture). */
  async function remove(id) {
    state.order = state.order.filter((x) => x !== id);
    if (state.activeId === id) state.activeId = null;
    cache.delete(id);
    await saveState();
    try { await unlink(path.join(dir, ASSIGN_FILE(id))); } catch { /* already gone */ }
  }

  return {
    init,
    create, get, list,
    activeId, getActive, setActive,
    patch, reorder, move,
    addTask, addCommit, addSource, setVerdict, checkpoint,
    remove,
    _state: () => state,
    _dir: () => dir,
  };
}

// ====== package-cmd.mjs ======
/**
 * `node code-boss.mjs package --jars <dir> [--helper <jar>] [--out <dir>] [--node <dir>]`
 *
 * Lets the SINGLE copied bundle package ITSELF into an extract-and-run zip,
 * using the POI jars present wherever it's run (e.g. the production box) — no
 * repo, no build tools needed. Produces:
 *   <out>/code_boss/code-boss.mjs
 *                  /tools/codeboss-docx-helper.jar   (if found)
 *                  /tools/lib/*.jar                   (the POI deps from --jars)
 *                  /run.bat  /run.sh  /README.txt
 *   <out>/code-boss-v<NNN>.zip
 *
 * --jars <dir>   folder holding the .docx jars (searched: <dir>, <dir>/lib,
 *                <dir>/target, <dir>/target/lib). The codeboss-docx-helper.jar
 *                (if present) becomes tools/<helper>; all other *.jar → tools/lib/.
 * --helper <jar> explicit path to codeboss-docx-helper.jar (overrides auto-find).
 * --out <dir>    where to write (default: ./release).
 * --node <dir>   bundle a portable Node (its node.exe / bin/node) into node/ so
 *                the target needs nothing pre-installed but Java (for .docx).
 *
 * Imports are stripped by the bundler; at runtime these names come from the
 * bundle HEADER (path, spawnSync, existsSync, readFile/writeFile/mkdir/readdir/
 * copyFile/stat/rm-via-... ). Everything here is pkg*-prefixed to stay unique in
 * the flat bundle scope.
 */

const PKG_HELPER_JAR = 'codeboss-docx-helper.jar';

const PKG_RUN_BAT = `@echo off
setlocal
set "HERE=%~dp0"
if exist "%HERE%node\\node.exe" (
  "%HERE%node\\node.exe" "%HERE%code-boss.mjs" %*
) else (
  where node >nul 2>nul || (echo Node.js 20+ was not found. Install it from https://nodejs.org ^(or drop a portable Node into a "node" folder next to this file^), then run again.& pause & exit /b 1)
  node "%HERE%code-boss.mjs" %*
)
`;

const PKG_RUN_SH = `#!/bin/sh
HERE="$(cd "$(dirname "$0")" && pwd)"
if [ -x "$HERE/node/bin/node" ]; then NODE="$HERE/node/bin/node"; else NODE=node; fi
command -v "$NODE" >/dev/null 2>&1 || { echo "Node.js 20+ not found. Install it (https://nodejs.org) or bundle a portable Node in ./node."; exit 1; }
exec "$NODE" "$HERE/code-boss.mjs" "$@"
`;

// The day-to-day RUNNER (run.ps1, lives inside the extracted package). The
// installer points a desktop shortcut at a thin wrapper that calls this. Launches
// node with its startup/diagnostic STDERR redirected to launch.log at the OS level
// (clean UTF-8, reliable for a long-running process) and keeps the window open.
const PKG_RUN_PS1 = `$app = $PSScriptRoot; if (-not $app) { $app = Split-Path -Parent $MyInvocation.MyCommand.Path }
$log = Join-Path $app 'launch.log'
$port = if ($env:PORT) { [int]$env:PORT } else { 18888 }
$node = Join-Path $app 'node\\node.exe'
if (-not (Test-Path $node)) {
  if (-not (Get-Command node -ErrorAction SilentlyContinue)) { Write-Host 'Node.js 20+ was not found. Install it from https://nodejs.org (or drop a portable Node into a "node" folder next to this file), then run again.'; Read-Host 'Press Enter to exit'; exit 1 }
  $node = 'node'
}
$ErrorActionPreference = 'Continue'
function Test-CbPort { param($p) [bool](Get-NetTCPConnection -LocalPort $p -State Listen -ErrorAction SilentlyContinue) }
function Clear-CbPort { param($p) try { Get-NetTCPConnection -LocalPort $p -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique | ForEach-Object { Stop-Process -Id $_ -Force -ErrorAction SilentlyContinue } } catch {} }
# A previous instance may be holding the port. Kill everything on it, then launch;
# if it stays busy (something keeps re-grabbing it), retry every 10s for 2 minutes.
$deadline = (Get-Date).AddMinutes(2)
while ($true) {
  if (Test-CbPort $port) { Write-Host "Port $port is in use - stopping the existing instance..."; Clear-CbPort $port; Start-Sleep -Milliseconds 800 }
  if (-not (Test-CbPort $port)) { break }
  if ((Get-Date) -ge $deadline) { Write-Host "Port $port is still in use after 2 minutes - giving up."; Read-Host 'Press Enter to exit'; exit 1 }
  Write-Host "Port $port still busy; retrying in 10s..."; Start-Sleep -Seconds 10
}
Write-Host "Starting code_boss on port $port - a browser tab opens automatically."
Write-Host "Output is logged to $log. Close this window to stop code_boss."
$proc = Start-Process -FilePath $node -ArgumentList 'code-boss.mjs' -WorkingDirectory $app -NoNewWindow -PassThru -RedirectStandardError $log
$proc.WaitForExit()
Write-Host ''
Write-Host "code_boss exited - see $log."
Read-Host 'Press Enter to exit'
`;

function pkgReadme(version, docxNote, bundledNode) {
  return `code_boss v${version} - single-folder deployment
======================================================

WHAT THIS IS
  A local coding-assistant web app. It runs entirely on your machine and serves
  a UI at http://127.0.0.1:18888 (a browser opens automatically).

REQUIREMENTS (on the machine that runs it)
  - Node.js 20 or newer.${bundledNode ? '  (A portable Node is bundled in ./node - nothing to install.)' : '  Get it from https://nodejs.org'}
  - A Java runtime (JRE 11+) - ONLY for Microsoft Word (.docx) editing.
    Everything else works without Java.
  The LLM endpoint and model are already configured inside the app.

RUN
  Windows       : right-click run.ps1 → Run with PowerShell   (or run.bat, or:  node code-boss.mjs)
  macOS / Linux : ./run.sh                     (or:  node code-boss.mjs)
  Different port:  Windows  $env:PORT=18999; node code-boss.mjs
                   Unix     PORT=18999 node code-boss.mjs

FIRST RUN
  The UI asks for your API key (and GitLab credentials). They are saved under
  your home folder in .code_boss/ and reused on later runs.

FOLDER CONTENTS
  code-boss.mjs   the app - one self-contained file, no install step
  tools/          the .docx helper (Apache POI). Keep it next to code-boss.mjs.
  run.ps1 / run.bat / run.sh  launchers
${bundledNode ? '  node/           bundled Node runtime\n' : ''}.docx editing: ${docxNote}

OFFLINE / AIR-GAPPED
  Nothing is downloaded at runtime. This folder + Node (+ Java for .docx) is all
  that is needed. Extract anywhere and run.
`;
}

function pkgArg(argv, name) { const i = argv.indexOf(name); return i >= 0 ? argv[i + 1] : null; }

// A self-extracting Windows INSTALLER .ps1 with the zip embedded as a base64
// here-string (NOT a comment/marker: PowerShell parses the WHOLE file as code
// first, so the payload must be a string literal). Built-in PowerShell only.
// Run it once via right-click → Run with PowerShell (a .ps1 can't be double-
// clicked). Wherever it's run from, it:
//   - installs to a FIXED root: D:\code_boss\code_boss_v<ver>\  (override the root
//     with the CODE_BOSS_HOME env var). A re-run / existing version skips extract.
//   - creates a VERSION-NAMED shortcut "code_boss_v<ver>.lnk" → THIS version's
//     run.ps1, in BOTH the install root AND the Desktop (a .lnk launches a .ps1
//     fine; each version keeps its own shortcut, so the shortcut always runs the
//     version it was made for).
//   - launches code_boss once (first run) via run.ps1, which first frees the port
//     (kills any running instance, retrying up to 2 min). Boot/diagnostic output
//     → <app>\launch.log; the app's per-project log stays at <project>\.code_boss\.
function pkgInstallerPs1(version, base64) {
  // base64 on ONE line inside a single-quoted here-string: literal, no
  // interpolation, and base64 has no quote so it can't terminate early. The
  // closing '@ must sit at column 0. Forward slashes in PS paths avoid having to
  // double-escape backslashes through this JS string layer.
  return [
    "$b64 = @'",
    base64,
    "'@",
    "$ErrorActionPreference = 'Stop'",
    `$ver = '${version}'`,
    '# Fixed install root, independent of where this installer is run from',
    '# (override with the CODE_BOSS_HOME env var).',
    "$base = if ($env:CODE_BOSS_HOME) { $env:CODE_BOSS_HOME } else { 'D:/code_boss' }",
    '$app = Join-Path $base "code_boss_v$ver"',
    'New-Item -ItemType Directory -Force -Path $base | Out-Null',
    '',
    '# First run: decode the embedded zip + extract to $base (its top-level folder',
    '# is code_boss_v$ver). A re-run / existing version skips extraction.',
    "if (-not (Test-Path (Join-Path $app 'code-boss.mjs'))) {",
    '  Write-Host "Installing code_boss v$ver into $app ..."',
    '  try {',
    "    $zip = Join-Path $env:TEMP ('cbv' + [guid]::NewGuid().ToString('N') + '.zip')",
    '    [IO.File]::WriteAllBytes($zip, [Convert]::FromBase64String($b64.Trim()))',
    '    Expand-Archive -LiteralPath $zip -DestinationPath $base -Force',
    '    Remove-Item -LiteralPath $zip -Force',
    '  } catch {',
    '    Write-Host ("Install failed: " + $_.Exception.Message)',
    "    Read-Host 'Press Enter to exit'; exit 1",
    '  }',
    '}',
    '',
    '# Version-named shortcuts → THIS version run.ps1, in the install root AND the',
    "# Desktop. Windows opens a .ps1 in Notepad on double-click, but a .lnk runs it.",
    "$runps1 = Join-Path $app 'run.ps1'",
    "$psexe = Join-Path $env:WINDIR 'System32/WindowsPowerShell/v1.0/powershell.exe'",
    'function New-CbShortcut { param($lnkPath)',
    '  try {',
    '    $ws = New-Object -ComObject WScript.Shell',
    '    $sc = $ws.CreateShortcut($lnkPath)',
    '    $sc.TargetPath = $psexe',
    `    $sc.Arguments = '-NoProfile -ExecutionPolicy Bypass -File "' + $runps1 + '"'`,
    '    $sc.WorkingDirectory = $app',
    '    $sc.IconLocation = "$psexe,0"',
    '    $sc.Description = "code_boss v$ver"',
    '    $sc.Save()',
    '    Write-Host ("  shortcut: " + $lnkPath)',
    '  } catch { Write-Host ("  shortcut failed (" + $lnkPath + "): " + $_.Exception.Message) }',
    '}',
    '$lnkName = "code_boss_v$ver.lnk"',
    'New-CbShortcut (Join-Path $base $lnkName)',
    "$desktop = [Environment]::GetFolderPath('Desktop')",
    'if ($desktop) { New-CbShortcut (Join-Path $desktop $lnkName) }',
    '',
    'Write-Host ""',
    'Write-Host "Installed code_boss v$ver. Double-click the code_boss_v$ver shortcut (Desktop or $base) to run it."',
    'Write-Host "Launching code_boss v$ver ..."',
    '& $runps1',
    '',
  ].join('\r\n');
}

// Recursive dir copy (the HEADER doesn't expose fs.cp, so do it by hand).
async function pkgCopyDir(src, dest) {
  await mkdir(dest, { recursive: true });
  for (const e of await readdir(src, { withFileTypes: true })) {
    const s = path.join(src, e.name), d = path.join(dest, e.name);
    if (e.isDirectory()) await pkgCopyDir(s, d);
    else if (e.isFile()) await copyFile(s, d);
  }
}

// When --jars has the POI deps but NOT a prebuilt codeboss-docx-helper.jar, and
// a JDK is available HERE, compile the helper now (from the source embedded in
// this bundle) against the staged deps and write it to `helperDest`. Result:
// the zip ships a ready jar, so the TARGET needs only a JRE — no first-run
// javac. `--release 11` keeps the bytecode loadable on a JRE 11 even when built
// with a newer JDK. Returns true on success; false (with a reason logged) when
// there's no JDK or the build fails — caller then ships a deps-only package.
async function pkgCompileHelper(libDir, helperDest, outDir) {
  if (typeof DOCX_HELPER_SOURCES === 'undefined' || !DOCX_HELPER_SOURCES) return false;
  const tmp = path.join(outDir, '.docx-compile');
  const srcDir = path.join(tmp, 'src');
  const classesDir = path.join(tmp, 'classes');
  try {
    await rm(tmp, { recursive: true, force: true });
    const javaFiles = [];
    for (const [rel, src] of Object.entries(DOCX_HELPER_SOURCES)) {
      const dest = path.join(srcDir, rel);
      await mkdir(path.dirname(dest), { recursive: true });
      await writeFile(dest, src, 'utf8');
      javaFiles.push(dest);
    }
    await mkdir(classesDir, { recursive: true });
    const jc = spawnSync('javac', ['--release', '11', '-cp', path.join(libDir, '*'), '-d', classesDir, ...javaFiles], { encoding: 'utf8' });
    if (jc.error || jc.status !== 0) {
      console.error(jc.error && jc.error.code === 'ENOENT'
        ? 'package: no JDK (javac) here to prebuild the helper — shipping deps-only (the target will compile on first run, needs a JDK there).'
        : 'package: helper compile failed — shipping deps-only:\n' + String(jc.stderr || jc.stdout || '').slice(0, 400));
      return false;
    }
    const jr = spawnSync('jar', ['cf', helperDest, '-C', classesDir, '.'], { encoding: 'utf8' });
    if (jr.error || jr.status !== 0) {
      console.error('package: compiled OK but the `jar` step failed — shipping deps-only:\n' + String((jr.error && jr.error.message) || jr.stderr || jr.stdout || '').slice(0, 400));
      return false;
    }
    return existsSync(helperDest);
  } catch (e) {
    console.error('package: helper prebuild error — shipping deps-only: ' + (e?.message || e));
    return false;
  } finally {
    try { await rm(tmp, { recursive: true, force: true }); } catch { /* */ }
  }
}

async function runPackageCommand({ argv, bundlePath, version }) {
  if (!bundlePath || !existsSync(bundlePath)) { console.error('package: cannot locate the bundle file (code-boss.mjs).'); return 1; }
  const jarsDir = pkgArg(argv, '--jars');
  const helperArg = pkgArg(argv, '--helper');
  const outDir = path.resolve(pkgArg(argv, '--out') || path.join(process.cwd(), 'release'));
  const nodeDir = pkgArg(argv, '--node');

  // Build the staging folder + zip in a TEMP dir so release/ ends up holding ONLY
  // the single .ps1 installer. The zip's top-level folder is code_boss_v<version>,
  // so a manual "Extract All" still lands files at <dest>\code_boss_v<version>\.
  const stageName = `code_boss_v${version}`;
  const workDir = path.join(outDir, '.cb-build');
  const stage = path.join(workDir, stageName);
  await mkdir(outDir, { recursive: true });
  // Clear prior code_boss artifacts (old zips / version folders / .bat|.ps1
  // installers + a stale build dir) so the release dir is left with just the new .ps1.
  try {
    for (const e of await readdir(outDir)) {
      if (/^code[-_]boss[-_]v/i.test(e) || e === '.cb-build') await rm(path.join(outDir, e), { recursive: true, force: true });
    }
  } catch { /* */ }
  await mkdir(path.join(stage, 'tools', 'lib'), { recursive: true });
  await copyFile(bundlePath, path.join(stage, 'code-boss.mjs'));

  // Gather jars. Search a few common layouts under --jars.
  let helper = (helperArg && existsSync(helperArg)) ? helperArg : null;
  const depJars = [];
  if (jarsDir && existsSync(jarsDir)) {
    const dirs = [jarsDir, path.join(jarsDir, 'lib'), path.join(jarsDir, 'target'), path.join(jarsDir, 'target', 'lib')].filter((d) => existsSync(d));
    const seen = new Set();
    for (const dir of dirs) {
      for (const f of await readdir(dir)) {
        if (!f.endsWith('.jar')) continue;
        if (f === PKG_HELPER_JAR) { if (!helper) helper = path.join(dir, f); continue; }
        if (seen.has(f)) continue;
        seen.add(f);
        depJars.push(path.join(dir, f));
      }
    }
  }
  for (const j of depJars) await copyFile(j, path.join(stage, 'tools', 'lib', path.basename(j)));

  // Resolve the helper jar:
  //  1. a prebuilt one (explicit --helper, or codeboss-docx-helper.jar in --jars) → copy it.
  //  2. else, if a JDK is on THIS box → compile it now from embedded source (zip is JRE-only).
  //  3. else ship deps-only → the target compiles on first run (needs a JDK there).
  const helperDest = path.join(stage, 'tools', PKG_HELPER_JAR);
  const stagedLib = path.join(stage, 'tools', 'lib');
  let docxNote;
  if (helper) {
    await copyFile(helper, helperDest);
    docxNote = `included (prebuilt helper jar + ${depJars.length} POI deps) - runs with just a JRE on the target.`;
  } else if (depJars.length && await pkgCompileHelper(stagedLib, helperDest, workDir)) {
    docxNote = `compiled here from embedded source (helper jar + ${depJars.length} POI deps) - runs with just a JRE on the target.`;
  } else if (depJars.length) {
    docxNote = `POI deps only (${depJars.length}); no prebuilt ${PKG_HELPER_JAR} and no JDK here to prebuild one, so the target compiles it from embedded source on first run (needs a JDK there).`;
  } else {
    docxNote = `not included (pass --jars <dir> with the POI jars to enable it).`;
  }

  let bundledNode = false;
  if (nodeDir) {
    if (!existsSync(nodeDir)) { console.error('--node dir not found: ' + nodeDir); return 1; }
    await pkgCopyDir(nodeDir, path.join(stage, 'node'));
    bundledNode = true;
  }

  await writeFile(path.join(stage, 'run.bat'), PKG_RUN_BAT.replace(/\n/g, '\r\n'), 'utf8');
  await writeFile(path.join(stage, 'run.ps1'), PKG_RUN_PS1.replace(/\n/g, '\r\n'), 'utf8');
  await writeFile(path.join(stage, 'run.sh'), PKG_RUN_SH, 'utf8');
  await writeFile(path.join(stage, 'README.txt'), pkgReadme(version, docxNote, bundledNode), 'utf8');

  // Zip the staging folder (Windows: PowerShell Compress-Archive; else `zip`).
  const zip = path.join(workDir, `code-boss-v${version}.zip`);
  await rm(zip, { force: true });
  let zipped = false;
  if (process.platform === 'win32') {
    const r = spawnSync('powershell', ['-NoProfile', '-Command', `Compress-Archive -Path '${stage}' -DestinationPath '${zip}' -Force`], { stdio: 'inherit' });
    zipped = r.status === 0;
  } else {
    const r = spawnSync('zip', ['-r', '-q', zip, stageName], { cwd: workDir, stdio: 'inherit' });
    zipped = r.status === 0;
  }

  // The single deliverable: a self-extracting .ps1 installer (the zip embedded).
  let installer = null;
  if (zipped) {
    try {
      const b64 = (await readFile(zip)).toString('base64');
      installer = path.join(outDir, `code_boss_v${version}.ps1`);
      await rm(installer, { force: true });
      await writeFile(installer, pkgInstallerPs1(version, b64), 'utf8');
    } catch (e) { console.error('package: .ps1 installer generation failed: ' + (e?.message || e)); installer = null; }
  }

  // Clean the temp build dir → release/ holds ONLY the .ps1. Pass --keep-build to
  // retain the staged folder + zip for inspection.
  const keepBuild = pkgArg(argv, '--keep-build') !== null;
  if (installer && !keepBuild) { try { await rm(workDir, { recursive: true, force: true }); } catch { /* */ } }

  console.error('');
  if (installer) {
    const kb = Math.round((await stat(installer)).size / 1024);
    console.error(`installer: ${installer}  (${kb} KB)`);
    console.error(`           Run once via right-click → Run with PowerShell (from anywhere): installs to D:\\code_boss\\code_boss_v${version}\\, drops a code_boss_v${version} shortcut on the Desktop + in that root, and launches. After that, double-click the shortcut. (Override the root with CODE_BOSS_HOME.)`);
    if (keepBuild) console.error(`build:     ${workDir}  (staged folder + zip kept via --keep-build)`);
  } else if (!zipped) {
    console.error(`zip step failed - staged folder left at ${stage} for inspection.`);
  } else {
    console.error('installer generation failed - see the error above.');
  }
  console.error('docx:  ' + docxNote);
  console.error('node:  ' + (bundledNode ? 'bundled (./node)' : 'target must have Node 20+ on PATH'));
  return installer ? 0 : 1;
}

// ====== agent/shadow-git.mjs ======
// ── Shadow git — an internal revert safety net for NON-repo edit targets ─────
// code_boss can edit folders that aren't a git project; without this, edits to a
// one-off file or a non-repo folder are IRREVERSIBLE. The shadow repo keeps its
// metadata under <project>/.code_boss/shadow.git with the work-tree pointed at the
// project, so it tracks the project's files WITHOUT a visible .git polluting the
// user's folder. The SYSTEM snapshots before each editing run; the developer can
// undo the last run. The AGENT never touches this — normal git commands don't even
// see it (the --git-dir/--work-tree live only here), and the point is for the
// developer to undo the agent, not for the agent to undo itself.

const shadowGitDir = (projectDir) => path.join(projectDir, '.code_boss', 'shadow.git');
// Every shadow invocation: git --git-dir=<shadow> --work-tree=<project> ...
const shadowGit = (projectDir, args, opts) =>
  runGit(projectDir, ['--git-dir', shadowGitDir(projectDir), '--work-tree', projectDir, ...args], opts);

// Initialise the shadow repo once (idempotent). Excludes our own state dir, any real
// nested .git, and node_modules so we never snapshot huge/irrelevant trees.
async function shadowEnsureRepo(projectDir) {
  if (!projectDir) return false;
  const gd = shadowGitDir(projectDir);
  if (existsSync(path.join(gd, 'HEAD'))) return true;   // already initialised
  try {
    await mkdir(gd, { recursive: true });
    const init = await shadowGit(projectDir, ['init', '-q']);
    if (init.exitCode !== 0) return false;
    await shadowGit(projectDir, ['config', 'user.email', 'shadow@code-boss']);
    await shadowGit(projectDir, ['config', 'user.name', 'code_boss shadow']);
    await shadowGit(projectDir, ['config', 'commit.gpgsign', 'false']);
    await writeFile(path.join(gd, 'info', 'exclude'), '.code_boss/\n.git/\nnode_modules/\n', 'utf8');
    await shadowGit(projectDir, ['add', '-A']);
    await shadowGit(projectDir, ['commit', '-q', '--allow-empty', '-m', 'shadow: initial']);
    return true;
  } catch { return false; }
}

// Commit the CURRENT project state as the pre-run baseline. Returns the commit hash
// to roll back to (or null on failure). Skips a no-op commit (reuses the current HEAD).
async function shadowSnapshot(projectDir, message = 'snapshot') {
  if (!(await shadowEnsureRepo(projectDir))) return null;
  try {
    await shadowGit(projectDir, ['add', '-A']);
    const st = await shadowGit(projectDir, ['status', '--porcelain']);
    if ((st.stdout || '').trim()) {
      await shadowGit(projectDir, ['commit', '-q', '-m', `shadow: ${String(message).replace(/\s+/g, ' ').slice(0, 120)}`]);
    }
    const h = await shadowGit(projectDir, ['rev-parse', 'HEAD']);
    return h.exitCode === 0 ? (h.stdout || '').trim() : null;
  } catch { return null; }
}

// Roll the project back to the pre-run snapshot. When a post-run snapshot is given, undo ONLY
// what the run changed (the pre→post delta): restore the pre version of modified/deleted paths
// and delete paths the run added. Files the USER created AFTER the run (not in the pre..post
// diff) are left untouched — a blanket `clean -fd` would have deleted them (data loss).
// Without a post snapshot the delta is UNKNOWN, so the fallback is tracked-files-only:
// `reset --hard` to the pre snapshot, NEVER `clean -fd` — untracked files (anything created
// after the pre snapshot, run-made or user-made) are indistinguishable here, and deleting a
// user's hand-made file is data loss while leaving a run's leftover file is merely untidy.
// (The server additionally refuses the undo outright when postCommit is absent; this guard
// keeps any other/legacy caller from reaching a destructive path.)
async function shadowRestore(projectDir, commit, postCommit) {
  if (!commit || !existsSync(path.join(shadowGitDir(projectDir), 'HEAD'))) return false;
  try {
    if (postCommit) {
      const names = await shadowGit(projectDir, ['diff', '--name-only', '--no-renames', commit, postCommit]);
      const paths = (names.stdout || '').split('\n').map((s) => s.trim()).filter(Boolean);
      if (!paths.length) return true;   // the run changed nothing on disk
      const added = await shadowGit(projectDir, ['diff', '--name-only', '--no-renames', '--diff-filter=A', commit, postCommit]);
      const addedSet = new Set((added.stdout || '').split('\n').map((s) => s.trim()).filter(Boolean));
      const restore = paths.filter((p) => !addedSet.has(p));   // run-modified / run-deleted → restore pre version
      if (restore.length) await shadowGit(projectDir, ['checkout', commit, '--', ...restore]);
      for (const p of addedSet) { try { await rm(path.join(projectDir, p), { force: true }); } catch {} }   // run-added → remove
      await shadowGit(projectDir, ['add', '-A']);   // resync the shadow index to the reverted tree
      return true;
    }
    const r = await shadowGit(projectDir, ['reset', '-q', '--hard', commit]);
    return r.exitCode === 0;
  } catch { return false; }
}

// ====== agent/plan-review.mjs ======
// Adversarial plan-review (#7): after the agent produces a propose-first PLAN, fan out a few
// READ-ONLY critics — each with file-reading tools but no ability to mutate — to poke holes in
// the plan against the actual code. Their concerns are fed back to the source agent for ONE
// revision before the developer is asked to approve. Pure + injectable (runCritic is supplied by
// the server, wrapping a read-only worker) so it unit-tests with mocks. Best-effort: a critic
// that errors contributes no concerns; the caller degrades to the un-hardened plan on any failure.

const PLAN_REVIEW_LENSES = [
  { key: 'correctness', focus: 'CORRECTNESS bugs in the plan: logic errors, wrong assumptions about how the code works, missed steps, off-by-one / order-of-operations mistakes.' },
  { key: 'rules', focus: 'MISSED or BROKEN business rules, existing behavior, invariants, and edge cases the plan does not account for. Read the relevant code AND its tests.' },
  { key: 'breakage', focus: 'what the plan could BREAK: existing callers, tests, public interfaces, the build, or unrelated features that depend on what it changes.' },
];

const NO_CONCERNS = /^\s*(no concerns?|none\b|nothing\b|looks good|lgtm|no issues?|n\/a)\b/i;

// runCritic(lens) → Promise<string>  — runs ONE read-only critic and returns its concerns text.
// Returns the list of REAL concerns: [{ lens, concerns }]. Emits plan-review-start / plan-review-done.
async function runAdversarialPlanReview({ runCritic, lenses = PLAN_REVIEW_LENSES, emit = () => {}, signal = null } = {}) {
  if (typeof runCritic !== 'function' || !Array.isArray(lenses) || lenses.length === 0) return [];
  try { emit({ type: 'plan-review-start', count: lenses.length }); } catch {}
  const results = await Promise.all(lenses.map(async (lens) => {
    if (signal && signal.aborted) return { lens: lens.key, concerns: '' };
    try { const c = await runCritic(lens); return { lens: lens.key, concerns: String(c || '').trim() }; }
    catch (e) { return { lens: lens.key, concerns: '', error: (e && e.message) || String(e) }; }
  }));
  const real = results.filter((r) => r.concerns && !NO_CONCERNS.test(r.concerns));
  try { emit({ type: 'plan-review-done', findings: real.map((r) => ({ lens: r.lens, concerns: r.concerns })), dismissed: results.length - real.length }); } catch {}
  return real;
}

// Build the synthetic revision message handed back to the SOURCE agent (still in propose mode):
// it must produce a REVISED plan addressing the concerns, not implement anything.
function planRevisionMessage(originalPlan, concerns) {
  const block = (concerns || []).map((c) => `[${c.lens}] ${c.concerns}`).join('\n\n');
  return 'Adversarial reviewers examined your proposed plan against the actual code and raised the '
    + 'concerns below. Revise your plan to address them (or briefly justify dismissing one), then '
    + 'present the REVISED plan in the same detailed format. Still propose only — do NOT implement.\n\n'
    + `CONCERNS:\n${block}\n\nYOUR ORIGINAL PLAN:\n${originalPlan}`;
}

// The critic's instruction (combined with the lens focus + the plan) for the read-only worker.
function planCriticMessage(focus, plan) {
  return 'You are an ADVERSARIAL reviewer of another agent\'s IMPLEMENTATION PLAN. You have READ-ONLY '
    + 'tools — read/list/search/find — use them to check the plan against the ACTUAL code; do not trust '
    + 'the plan\'s claims. Do NOT modify anything.\n\n'
    + `FOCUS specifically on: ${focus}\n\n`
    + 'Report ONLY concrete, specific concerns grounded in what you read (cite file:line where you can). '
    + 'If after reading you find nothing real, reply exactly "no concerns". Do not invent problems.\n\n'
    + `THE PROPOSED PLAN:\n${plan}`;
}

// ====== git-api/gitlab.mjs ======
/**
 * Common GitLab REST v4 client. Used by any plugin or core code that
 * needs to read files/trees from a self-hosted GitLab without cloning.
 *
 * Auth: PRIVATE-TOKEN header. Username is captured for the UI / API-key
 * parity but only the token is sent.
 *
 *   const client = createGitLabClient({
 *     serverUrl: 'https://git-oci.int.dmdc.osd.mil',
 *     getCredentials: () => session.gitlabCreds,   // hot-reloads
 *   });
 *   await client.listTree({ projectPath: 'incubator/sandbox/foo', path: 'docs', ref: 'main' });
 *   await client.readFile({ projectPath: 'incubator/sandbox/foo', filepath: 'docs/guide.md' });
 */

class GitLabApiError extends Error {
  constructor(status, body) {
    super(`GitLab API ${status}: ${typeof body === 'string' ? body.slice(0, 200) : (body?.message ?? JSON.stringify(body).slice(0, 200))}`);
    this.status = status;
    this.body = body;
  }
}

// A hung connection (DoD proxy holding the socket, TCP black-hole) must not
// block the agent loop forever — fetch gets an abort timeout.
const DEFAULT_TIMEOUT_MS = 30000;

const DEFAULT_MAX_FILE_BYTES = 50 * 1024 * 1024; // 50MB, matches read_docx's cap

function createGitLabClient({ serverUrl, getCredentials, fetchImpl, timeoutMs = DEFAULT_TIMEOUT_MS, maxFileBytes = DEFAULT_MAX_FILE_BYTES } = {}) {
  if (!serverUrl) throw new Error('createGitLabClient: serverUrl is required');
  if (typeof getCredentials !== 'function') throw new Error('createGitLabClient: getCredentials must be a function');
  const fetcher = fetchImpl ?? globalThis.fetch;
  if (!fetcher) throw new Error('createGitLabClient: no fetch available');
  const base = serverUrl.replace(/\/+$/, '');

  function authHeaders() {
    const creds = getCredentials() || {};
    if (!creds.token) throw new Error('GitLab credentials not configured');
    return { 'PRIVATE-TOKEN': String(creds.token) };
  }

  function timeoutSignal() {
    // AbortSignal.timeout is Node 18+; guard in case fetchImpl ignores it.
    try { return AbortSignal.timeout(timeoutMs); } catch { return undefined; }
  }

  async function apiGet(path, { accept = 'application/json' } = {}) {
    const url = `${base}/api/v4${path}`;
    const res = await fetcher(url, { headers: { accept, ...authHeaders() }, signal: timeoutSignal() });
    if (!res.ok) {
      // Read the body exactly ONCE. The old code called res.json() then
      // res.text() on the already-consumed stream ('body used already'),
      // swallowing the real error text — leaving an opaque 'GitLab API 401: '.
      const raw = await res.text().catch(() => '');
      let body; try { body = JSON.parse(raw); } catch { body = raw; }
      throw new GitLabApiError(res.status, body);
    }
    return res;
  }

  function encId(projectPath) {
    return encodeURIComponent(String(projectPath).replace(/^\/+|\/+$/g, ''));
  }

  /**
   * List entries at a given path. Returns array of:
   *   { id, name, type: 'tree'|'blob', path, mode }
   * `path` empty / undefined → repo root. `ref` defaults to the project's default branch.
   */
  async function listTree({ projectPath, path = '', ref, perPage = 100 } = {}) {
    if (!projectPath) throw new Error('listTree: projectPath is required');
    const params = new URLSearchParams();
    if (path) params.set('path', path);
    if (ref) params.set('ref', ref);
    params.set('per_page', String(perPage));
    const res = await apiGet(`/projects/${encId(projectPath)}/repository/tree?${params}`);
    return res.json();
  }

  /**
   * Read a single file. Returns { content: string, encoding: 'utf8'|'binary',
   * size, ref, contentType }. Text files come back as UTF-8 strings; binary
   * comes back as a Buffer (caller can write to a temp file).
   */
  async function readFile({ projectPath, filepath, ref } = {}) {
    if (!projectPath) throw new Error('readFile: projectPath is required');
    if (!filepath) throw new Error('readFile: filepath is required');
    const params = new URLSearchParams();
    if (ref) params.set('ref', ref);
    const url = `/projects/${encId(projectPath)}/repository/files/${encodeURIComponent(filepath)}/raw?${params}`;
    const res = await apiGet(url, { accept: '*/*' });
    const contentType = res.headers.get('content-type') || '';
    const gitlabRef = res.headers.get('x-gitlab-ref');
    // A DoD SSO/proxy interstitial returns an HTML login page — often with a
    // 200 OK, so res.ok was true and we'd otherwise hand the agent a login
    // page as if it were the requested file. If the body is HTML AND the
    // response did not come from GitLab (no x-gitlab-* header), reject it.
    if (/text\/html/i.test(contentType) && !gitlabRef && !res.headers.get('x-gitlab-blob-id')) {
      throw new GitLabApiError(res.status || 200, 'expected a raw file but received an HTML page (likely an SSO/login redirect or proxy interstitial). Check GitLab credentials / network.');
    }
    // Size guard: refuse to buffer an oversized file into memory (arrayBuffer
    // loads it all, then Buffer.from copies again ~2x). The 30s timeout bounds
    // a never-ending chunked response; this bounds a large-but-finite one.
    const cl = Number(res.headers.get('content-length'));
    if (Number.isFinite(cl) && cl > maxFileBytes) {
      throw new GitLabApiError(res.status || 200, `file too large: ${cl} bytes (max ${maxFileBytes})`);
    }
    const buf = Buffer.from(await res.arrayBuffer());
    if (buf.length > maxFileBytes) {
      throw new GitLabApiError(res.status || 200, `file too large: ${buf.length} bytes (max ${maxFileBytes})`);
    }
    const decoded = decodeText(buf);
    if (decoded != null) {
      return { content: decoded, encoding: 'utf8', size: buf.length, ref: gitlabRef || ref || null, contentType };
    }
    return { content: buf, encoding: 'binary', size: buf.length, ref: gitlabRef || ref || null, contentType };
  }

  return { listTree, readFile, _apiGet: apiGet };
}

/**
 * Decode a file buffer to text if it looks like text, else return null
 * (caller treats null as binary). BOM- and UTF-16-aware: a Windows editor
 * (Notepad, PowerShell Set-Content) commonly writes UTF-16LE/BE or a UTF-8
 * BOM. The old pure NUL-ratio heuristic wrongly classified UTF-16 text
 * (~50% NUL) as binary, so a plain README could never be read.
 */
function decodeText(buf) {
  if (buf.length === 0) return '';
  // BOMs first.
  if (buf[0] === 0xEF && buf[1] === 0xBB && buf[2] === 0xBF) {
    return buf.subarray(3).toString('utf8'); // UTF-8 BOM
  }
  if (buf[0] === 0xFF && buf[1] === 0xFE) {
    return buf.subarray(2).toString('utf16le'); // UTF-16 LE BOM
  }
  if (buf[0] === 0xFE && buf[1] === 0xFF) {
    return Buffer.from(buf.subarray(2)).swap16().toString('utf16le'); // UTF-16 BE BOM
  }
  // No BOM: inspect NUL distribution in the sample.
  const sample = buf.subarray(0, Math.min(buf.length, 4096));
  let nul = 0, nulEven = 0, nulOdd = 0;
  for (let i = 0; i < sample.length; i++) {
    if (sample[i] === 0) { nul++; if (i % 2 === 0) nulEven++; else nulOdd++; }
  }
  const n = Math.max(sample.length, 1);
  // BOM-less UTF-16: NULs cluster on one parity (LE → odd offsets carry the
  // high byte for ASCII; BE → even offsets).
  if (nulOdd > 0.4 * (n / 2) && nulEven <= 0.02 * n) return buf.toString('utf16le');
  if (nulEven > 0.4 * (n / 2) && nulOdd <= 0.02 * n) return Buffer.from(buf).swap16().toString('utf16le');
  // Genuine binary: high NUL ratio.
  if (nul / n >= 0.005) return null;
  return buf.toString('utf8');
}

// ====== agent/child-kill.mjs ======
/**
 * Shared child-process cancel/kill wiring for long-running tools.
 *
 * A tool that spawns a child (powershell build, wl build, …) must honor the
 * worker's abort signal so pressing Escape / hitting /api/cancel actually stops
 * the work instead of waiting out a multi-minute build. Two things are needed,
 * both Windows-specific and easy to get wrong (the `wl` tool shipped without
 * them — see why this module exists):
 *
 *   1. Kill the whole process TREE, not just the direct child. On Windows
 *      `child.kill()` only stops the powershell wrapper and orphans the real
 *      work (mvn.exe / java.exe), which keeps running and holding file locks.
 *      `taskkill /F /T /PID` kills the wrapper AND its descendants.
 *   2. A grace timer. `taskkill` can be blocked by AppLocker/EDR on locked-down
 *      enterprise boxes, and a killed wrapper can suppress its own 'close'
 *      event — so the caller's Promise could hang forever (wedging the whole
 *      agent loop) even after a successful abort. The grace timer force-settles
 *      the caller after a few seconds so the loop always recovers.
 *
 * The built-in `powershell` tool (src/agent/tools.mjs) implements this inline;
 * this module is the canonical version used by plugin tools so they can't drift.
 */

/**
 * Wire `child` to `signal` (an AbortSignal, or undefined). On abort, tree-kills
 * the child and arms a grace timer that invokes `forceSettle()` if the child
 * never emits 'close'. The caller MUST invoke the returned `cleanup()` from its
 * own settle path (to clear the grace timer + drop the abort listener), and may
 * call `killTree()` itself (e.g. from its timeout handler). `wasCancelled()`
 * reports whether a user abort (not a timeout) triggered the kill, so the caller
 * can render a "canceled" result instead of an exit-code error.
 *
 * @param {import('node:child_process').ChildProcess} child
 * @param {AbortSignal | undefined | null} signal
 * @param {() => void} forceSettle  - resolves the caller's Promise if 'close' never fires
 * @param {{ graceMs?: number }} [opts]
 */
function killChildTreeOnAbort(child, signal, forceSettle, { graceMs = 5000 } = {}) {
  let cancelled = false;
  let graceTimer = null;
  const armGrace = () => {
    if (!graceTimer) graceTimer = setTimeout(() => { try { forceSettle(); } catch {} }, graceMs);
  };
  const killTree = () => {
    if (child && child.pid) {
      try { spawn('taskkill', ['/F', '/T', '/PID', String(child.pid)], { windowsHide: true }); } catch {}
    }
    armGrace();
  };
  const onAbort = () => { cancelled = true; killTree(); };
  if (signal) {
    if (signal.aborted) onAbort();
    else signal.addEventListener('abort', onAbort, { once: true });
  }
  const cleanup = () => {
    if (graceTimer) { clearTimeout(graceTimer); graceTimer = null; }
    if (signal) { try { signal.removeEventListener('abort', onAbort); } catch {} }
  };
  return { killTree, cleanup, wasCancelled: () => cancelled };
}

// ====== plugins/wl.mjs ======
/**
 * `wl` plugin — wraps the WebLogic developer command-line tool.
 *
 * The `wl` command is a workspace-level wrapper around git + maven actions
 * for WebLogic-deployable projects. It MUST be invoked from the workspace
 * directory, which is the PARENT of the active project (apps and shared
 * libraries live as subdirs of the workspace).
 *
 * Trigger detects `wl` on PATH via `Get-Command`. When active, the plugin
 * registers the wl tool and prepends a usage block to the system prompt.
 */

// Per-user wl config script. wl reads this on every invocation.
const WL_CONFIG_PATH = path.join(homedir(), 'myWlSetup.bat');
// Script that starts the WebLogic SERVER itself (the JVM) — distinct from
// `wl start <app>`, which starts a deployed application. Env-overridable; the
// default is the standard local install path. `wl startWebLogic` runs this
// DETACHED (the server is long-lived) and returns immediately with its PID.
const WL_START_SCRIPT = process.env.CODEBOSS_WL_START_SCRIPT || 'D:\\j\\wls\\12c\\basic\\bin\\startWebLogic.cmd';
// The standard stop script (same bin dir). stopWebLogic runs it and WAITS for the shutdown;
// restartWebLogic runs stop then start. Env-overridable; the default is the standard install path.
const WL_STOP_SCRIPT = process.env.CODEBOSS_WL_STOP_SCRIPT || 'D:\\j\\wls\\12c\\basic\\bin\\stopWebLogic.cmd';
// The wl tool's source + user guide live in a self-hosted GitLab repo.
// We fetch files/trees over the GitLab v4 API rather than cloning —
// cloning prompts for credentials in non-interactive shells, and pulls
// down megabytes for files the agent reads once.
const WL_GIT_SERVER = 'https://git-oci.int.dmdc.osd.mil';
const WL_DOCS_REPO  = 'incubator/sandbox/weblogicdesktopscripts';

// ── App URL construction (for the UI "Open" button — NO browser launch) ─────
// `wl run <app>` prints the URL but also opens a browser tab. To get the URL
// WITHOUT a tab we construct it ourselves: https://<host>:7102/[<prefix>/]<ctx>
// where host comes from MY_WL_SERVER (default localhost), the context-root from
// the app's EAR META-INF/application.xml, and an optional Appj prefix from the
// same wl config. The server monitors the SERVER on 7101; 7102 is the app's
// HTTPS listen port. Env-overridable in case a domain uses a different port.
const WL_HTTPS_PORT = Number(process.env.CODEBOSS_WL_HTTPS_PORT) || 7102;

// Parse the wl user-config (a Windows .bat of `set NAME=VALUE` lines) into a
// {NAME: VALUE} map WITHOUT executing it. Missing file → {} (host defaults).
async function readWlConfig(configPath = WL_CONFIG_PATH) {
  let text;
  try { text = await readFile(configPath, 'utf8'); }
  catch { return {}; }
  const cfg = {};
  for (const line of text.split(/\r?\n/)) {
    const m = line.match(/^\s*set\s+([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.*)$/i);
    if (!m) continue;
    let v = m[2].replace(/\s+$/, '').trim();
    if ((v.startsWith('"') && v.endsWith('"')) || (v.startsWith("'") && v.endsWith("'"))) v = v.slice(1, -1);
    cfg[m[1].toUpperCase()] = v;   // last-wins on duplicates
  }
  return cfg;
}

// Host for the app URL: MY_WL_SERVER if it names a real host, else localhost.
function wlHostFromConfig(cfg) {
  const v = (cfg?.MY_WL_SERVER || '').trim();
  if (!v || /^(localhost|127\.0\.0\.1)$/i.test(v)) return 'localhost';
  return v;
}

// Optional extra path segment ("Appj Prefix"). The exact config var name varies
// by install; accept a few plausible keys and treat blank / "not set" as unset.
function wlAppjPrefix(cfg) {
  const raw = (cfg?.APPJ_PREFIX || cfg?.APPJPREFIX || cfg?.MY_APPJ_PREFIX || cfg?.APPJ || '').trim();
  if (!raw || /^not\s*set$/i.test(raw)) return '';
  return raw.replace(/^\/+|\/+$/g, '');
}

// Find the EAR application descriptor under an app dir and pull the WEB module's
// <context-root>. Regex parse (zero-dep, namespace-agnostic) — survives the JEE
// vs Jakarta namespace variants. Returns { contextRoot, filePath, multiple } or
// null when no descriptor/context-root is found.
async function readContextRoot(appDir) {
  const matches = [];
  await (async function walk(dir, depth) {
    if (depth > 8) return;
    let entries;
    try { entries = await readdir(dir, { withFileTypes: true }); } catch { return; }
    for (const e of entries) {
      const full = path.join(dir, e.name);
      if (e.isDirectory()) {
        if (e.name === 'node_modules' || e.name === '.git') continue;
        await walk(full, depth + 1);
      } else if (e.isFile() && e.name.toLowerCase() === 'application.xml'
                 && /[\\/]META-INF[\\/]application\.xml$/i.test(full)) {
        matches.push(full);
      }
    }
  })(appDir, 0);
  if (matches.length === 0) return null;
  // Prefer a built/staging copy (target/dist/build/staging/.ear) over a source one.
  const score = (p) => (/(?:target|dist|build|staging|\.ear)[\\/]/i.test(p) ? 1 : 0);
  matches.sort((a, b) => score(b) - score(a));
  for (const file of matches) {
    let xml;
    try { xml = await readFile(file, 'utf8'); } catch { continue; }
    const clean = xml.replace(/<!--[\s\S]*?-->/g, '');   // ignore commented-out modules
    const m = clean.match(/<(?:\w+:)?web>[\s\S]*?<(?:\w+:)?context-root>\s*([^<]+?)\s*<\/(?:\w+:)?context-root>/i);
    if (m) {
      let cr = m[1].trim();
      if (!cr.startsWith('/')) cr = '/' + cr;
      cr = cr.replace(/\/+$/, '') || '/';
      return { contextRoot: cr, filePath: file, multiple: matches.length > 1 };
    }
  }
  return null;
}

// Compose the full app URL. ALWAYS resolves (never throws) so a failure here
// can never break a deploy — the UI just disables the Open button with a reason.
async function computeAppUrl(appName, workspaceDir) {
  try {
    const name = String(appName || '').trim();
    if (!name || /[\\/]/.test(name) || name.includes('..')) {
      return { ok: false, reason: 'bad-app', detail: 'invalid app name' };
    }
    if (!workspaceDir) return { ok: false, reason: 'no-workspace', detail: 'no project open' };
    const appDir = path.join(workspaceDir, name);
    const cfg = await readWlConfig();
    const host = wlHostFromConfig(cfg);
    const prefix = wlAppjPrefix(cfg);
    const cr = await readContextRoot(appDir);
    if (!cr) {
      return { ok: false, reason: 'no-context-root', detail: `Could not find META-INF/application.xml with a <context-root> under ${appDir}` };
    }
    const segs = [prefix, cr.contextRoot.replace(/^\//, '')].filter(Boolean).join('/');
    const url = `https://${host}:${WL_HTTPS_PORT}/${segs}`;
    return { ok: true, url, host, port: WL_HTTPS_PORT, contextRoot: cr.contextRoot, appjPrefix: prefix || null, source: cr.filePath, multipleWebModules: cr.multiple === true };
  } catch (e) {
    return { ok: false, reason: 'error', detail: e?.message || String(e) };
  }
}

const WL_PROMPT = `
The "wl" command is available — a developer wrapper around git + maven for
WebLogic-deployable projects. It runs from the WORKSPACE directory (the active
project IS the workspace; apps and shared libraries are subdirectories of it).
The wl tool runs there automatically; do not cd. Pass everything after "wl " as
the args string.

Build / deploy / run actions — THIS is exactly what each does; do not assume:
  quickbuild (qb)  build, SKIPPING unit tests AND the Angular front-end build. Your DEFAULT while iterating.
  build (b)        FULL build INCLUDING unit tests and the Angular front-end. Use only when you need those.
  projprops (pp)   deploy the project's proj props (app config). Run this after you change projprops.
  deploy (d)       undeploy, then deploy.
  run (r)          ONLY opens the app in a browser tab. It does NOT build, deploy, or start anything — "run" alone never deploys your changes.
  all (a)          build, then projprops, then deploy, then run.
  quickall (qa)    quickbuild, then projprops, then deploy, then run.
  start / stop (s) start / stop a deployed APPLICATION on the server (NOT WebLogic itself).
  startWebLogic (sw) start the WebLogic SERVER (the JVM) ITSELF, in the BACKGROUND. Returns
                   immediately with a PID; the server takes ~1-2 minutes to come up. Do NOT
                   build/deploy/run/start an app until it is up. You do not need to poll: the
                   WebLogic server status (RUNNING / STOPPED on port 7101) is reported to you
                   EVERY turn in your context — wait until it says RUNNING.
  stopWebLogic (stw)  stop the WebLogic SERVER itself (not an app). Returns immediately; the
                   status will show STOPPED shortly.
  restartWebLogic (rw) stop the WebLogic SERVER, then start it again in the BACKGROUND. Wait for
                   the status to show RUNNING (~1-2 min) before build/deploy/run.

So to deploy your latest changes, your DEFAULT is to build + push config +
deploy, WITHOUT opening a browser:
  quickbuild <app> . projprops <app> . deploy <app>
(use "build" instead of "quickbuild" only when you need unit tests / the
front-end). Do NOT append "run" and do NOT use "quickall"/"all" — those open a
browser tab on every deploy, which the user does not want. The user opens the
app themselves with the "Open" button in the UI once you report the deploy
succeeded. Only use "run" if the user EXPLICITLY asks you to open the app. If the
WebLogic status below says STOPPED and your task needs the server, run
"startWebLogic" first and wait for it to come up.

When a build/deploy SUCCEEDS, report it in ONE short sentence (e.g. "Deployed
opsconnect successfully."). Do NOT reproduce the build log or render a
Markdown summary table — the full output already streamed live and the UI shows
an "Open" button + a committed checkmark, so a long recap is just noise.

Other actions: clone, pull, clonepull, status, diff (git); clean; undeploy;
datasources (ds); mvninstall (mi, rarely needed).

Deployables: any app or sharedlib name (sharedlibs start with "sharedlib-").
Chain different action/deployable groups with " . " — e.g. wl all teb . start.

Examples:
  <wl args="quickbuild myapp . projprops myapp . deploy myapp"/>  DEFAULT: build + config + deploy, NO browser
  <wl args="build myapp . projprops myapp . deploy myapp"/>       same, but full build (tests + front-end)
  <wl args="quickbuild myapp"/>   fast build (skips tests + front-end)
  <wl args="deploy myapp"/>       undeploy then deploy
  <wl args="run myapp"/>          JUST open the app in a browser tab (ONLY if the user asks)
  <wl args="start myapp"/>        start the deployed APP "myapp" on the server
  <wl args="startWebLogic"/>      start the WebLogic SERVER in the background (then wait for RUNNING)
  <wl args="stopWebLogic"/>       stop the WebLogic SERVER itself
  <wl args="restartWebLogic"/>    restart the WebLogic SERVER (stop + start; wait for RUNNING)

Builds are SLOW, but the timeout is handled for you: quickbuild gets ~10 minutes,
a full build (or any other command) gets ~20, capped at 30. You normally do NOT
need to pass timeout_ms — only set it (up to 1800000 = 30 min) for an unusually
long operation. If a command TIMES OUT, that is NOT a build failure — it only
ran past the limit; re-run the same command with a larger timeout_ms if it needs
to finish, and do not report the build as failed. Don't probe the shell first
("Write-Output Hello"); it works — just run the wl command. For help: <wl args=""/>.

Further reference, if you need it:
  - User config (env, sync region, aliases): ${WL_CONFIG_PATH}  (read with <read path="${WL_CONFIG_PATH}"/>)
  - wl source + user guide live in a remote GitLab repo — do NOT git clone. Use the docs tools:
      <wl_list_docs path=""/>          list repo root
      <wl_list_docs path="docs"/>      list a subdir
      <wl_read_doc  path="README.md"/> read a single file
    Binary docs (.docx) come back as a saved temp path — pass it to read_docx
    (or search_docx to find specific text without reading the whole doc).
`.trim();

function buildGitLabClient(ctx) {
  const getCreds = ctx?.getGitLabCredentials;
  if (typeof getCreds !== 'function') {
    throw new Error('GitLab credentials accessor not available in this run');
  }
  return createGitLabClient({
    serverUrl: WL_GIT_SERVER,
    getCredentials: () => {
      const c = getCreds();
      if (!c?.token) {
        throw new Error('GitLab credentials not set. Open settings (gear icon) and add a GitLab username + token.');
      }
      return c;
    },
  });
}

const wlPlugin = {
  name: 'wl',
  description: 'WebLogic developer tool — wraps git/maven actions across project apps and shared libs',
  async trigger(executor) {
    try {
      const r = await executor.run(
        'if (Get-Command wl -ErrorAction SilentlyContinue) { "yes" } else { "no" }'
      );
      return r.exitCode === 0 && /^yes/i.test(r.stdout.trim());
    } catch {
      return false;
    }
  },
  promptAddition: WL_PROMPT,
  tools: [
    {
      verb: 'wl',
      name: 'wl',
      schema: {
        description: 'Run a wl (WebLogic developer) command from the workspace directory. The tool runs in the active project\'s directory (which IS the workspace — apps and sharedlibs live as subdirs of it). Returns stdout, stderr, and exit code.',
        parameters: {
          type: 'object',
          properties: {
            args: {
              type: 'string',
              description: 'Everything that should follow `wl ` on the command line. E.g. "all milconnect" or "quickbuild teb" or "all teb . start milconnect".',
            },
            timeout_ms: {
              type: 'integer',
              description: 'Optional timeout in ms (cap 30 min / 1800000). Leave unset — the default adapts to the command (~10 min for quickbuild, ~20 min for a full build / anything else). Only set this for an unusually long operation.',
            },
          },
          required: ['args'],
        },
      },
      async impl({ args, timeout_ms }, ctx) {
        const safeArgs = (typeof args === 'string') ? args.trim() : '';
        const workspaceDir = ctx?.cwd || process.cwd();   // per-call cwd (no global process.chdir)
        // `startWebLogic` (alias sw) starts the WebLogic SERVER itself — a
        // long-lived JVM that never exits — so we DON'T run it through the normal
        // path (which blocks until the process ends or times out). Spawn the
        // server's start script DETACHED in the background and return IMMEDIATELY
        // with its PID; readiness is surfaced separately via the port-7101 status
        // reported to the agent every turn. (This is not a real `wl` subcommand —
        // we run the start script directly.)
        if (/^(?:startweblogic|sw)\b/i.test(safeArgs)) {
          try {
            const child = spawn('cmd.exe', ['/c', WL_START_SCRIPT], {
              cwd: workspaceDir, detached: true, stdio: 'ignore', windowsHide: true,
            });
            const pid = child.pid;
            child.unref();   // let the server outlive this tool call / the agent turn
            return { content:
              `WebLogic server starting in the BACKGROUND (PID ${pid}) via ${WL_START_SCRIPT}.\n` +
              `This call returned immediately — the server keeps running after it and takes ~1-2 minutes to come up.\n` +
              `Do NOT build / deploy / run / start an app yet: wait until the WebLogic status reported in your context shows RUNNING (port 7101).` };
          } catch (e) {
            return `ERROR: could not launch WebLogic via ${WL_START_SCRIPT}: ${e.message}. If the path is wrong, set the CODEBOSS_WL_START_SCRIPT env var to the correct startWebLogic.cmd.`;
          }
        }
        // stopWebLogic (stw) / restartWebLogic (rw) — control the SERVER itself (not an app), via the
        // standard stop script. STOP runs stopWebLogic.cmd and WAITS for the shutdown; RESTART =
        // stop + (brief pause) + the same detached start as startWebLogic.
        const _runScriptWait = (script, ms) => new Promise((resolve) => {
          let out = '';
          try {
            const child = spawn('cmd.exe', ['/c', script], { cwd: workspaceDir, windowsHide: true });
            const timer = setTimeout(() => { try { child.kill(); } catch {} }, ms);
            child.stdout.on('data', (d) => { out += d; });
            child.stderr.on('data', (d) => { out += d; });
            child.on('close', (code) => { clearTimeout(timer); resolve({ ok: code === 0, output: out }); });
            child.on('error', (e) => { clearTimeout(timer); resolve({ ok: false, output: `error: ${e.message}` }); });
          } catch (e) { resolve({ ok: false, output: `error: ${e.message}` }); }
        });
        const _startWlServer = () => { const c = spawn('cmd.exe', ['/c', WL_START_SCRIPT], { cwd: workspaceDir, detached: true, stdio: 'ignore', windowsHide: true }); const pid = c.pid; c.unref(); return pid; };
        if (/^(?:stopweblogic|stw)\b/i.test(safeArgs)) {
          const r = await _runScriptWait(WL_STOP_SCRIPT, 180000);
          return { content: `WebLogic server STOP via ${WL_STOP_SCRIPT} — ${r.ok ? 'shut down' : 'exited non-zero'}.\n${(r.output || '').trim().slice(-500) || '(no output)'}\nThe WebLogic status will show STOPPED shortly.` };
        }
        if (/^(?:restartweblogic|rw)\b/i.test(safeArgs)) {
          const r = await _runScriptWait(WL_STOP_SCRIPT, 180000);
          await new Promise((res) => setTimeout(res, 2000));   // brief beat between stop and start
          try {
            const pid = _startWlServer();
            return { content: `WebLogic RESTART — stopped via ${WL_STOP_SCRIPT} (${r.ok ? 'ok' : 'non-zero'}); started again in the BACKGROUND (PID ${pid}) via ${WL_START_SCRIPT}.\nIt takes ~1-2 minutes to come up — wait until the status shows RUNNING before you build / deploy / run.` };
          } catch (e) {
            return `WebLogic stop ran (${r.ok ? 'ok' : 'non-zero'}) but could not relaunch via ${WL_START_SCRIPT}: ${e.message}. Set CODEBOSS_WL_START_SCRIPT if the path is wrong.`;
          }
        }
        // Default timeout scales with the command: a quickbuild skips unit tests
        // AND the Angular build, so ~10 min is enough; a full build (or any chain
        // we can't positively identify as quick) gets the 20-min floor. Cap 30
        // min. The agent can still override via timeout_ms (clamped to [1s, 30m]).
        const isQuickBuild = /\b(?:quickbuild|qb)\b/i.test(safeArgs);
        const defaultTimeout = isQuickBuild ? 600000 : 1200000;   // 10 min vs 20 min
        const timeout = Math.min(Math.max(1000, Number(timeout_ms) || defaultTimeout), 1800000);
        const t0 = Date.now();
        return await new Promise((resolve) => {
          let stdout = '', stderr = '', timedOut = false, settled = false;
          let pendingProgress = '';
          let progressTimer = null;
          const flushProgress = () => {
            progressTimer = null;
            if (!pendingProgress) return;
            try { ctx?.onProgress?.(pendingProgress); } catch {}
            pendingProgress = '';
          };
          const queueProgress = (s) => {
            if (!ctx?.onProgress) return;
            pendingProgress += s;
            if (!progressTimer) progressTimer = setTimeout(flushProgress, 150);
          };
          const child = spawn(
            'powershell.exe',
            ['-NoProfile', '-NonInteractive', '-Command', safeArgs ? `wl ${safeArgs}` : 'wl'],
            { cwd: workspaceDir, windowsHide: true },
          );
          // Honor user-abort (Escape / /api/cancel) AND the timeout by killing the
          // whole build process TREE (taskkill /F /T) — child.kill() alone only
          // stops the powershell wrapper and orphans mvn/java. Without this, a
          // build kept running for up to 20 min after Escape and the worker loop
          // blocked on it. `settle` is declared just below; the grace-timer
          // forceSettle is only invoked later, by which time settle exists.
          const killer = killChildTreeOnAbort(child, ctx?.signal, () => settle(null));
          const tid = setTimeout(() => {
            timedOut = true;
            killer.killTree();
          }, timeout);
          child.stdout.on('data', (d) => {
            const s = d.toString();
            stdout += s;
            queueProgress(s);
          });
          child.stderr.on('data', (d) => {
            const s = d.toString();
            stderr += s;
            queueProgress(s);
          });
          const settle = (code) => {
            if (settled) return;
            settled = true;
            clearTimeout(tid);
            killer.cleanup();
            if (progressTimer) { clearTimeout(progressTimer); flushProgress(); }
            const dur = Date.now() - t0;
            if (killer.wasCancelled()) {
              // User pressed Escape / hit /api/cancel: the build tree was killed.
              // Resolve promptly (a non-error result) so the worker loop unblocks
              // and its next abort check unwinds the run.
              const raw = stdout + (stderr ? '\nSTDERR:\n' + stderr : '');
              const tail = raw.length > 4000 ? '…(showing the end)…\n' + raw.slice(-4000) : raw;
              resolve({ content:
                `CANCELED: \`wl ${safeArgs}\` was canceled by the user after ${dur}ms (the build process tree was killed).\n` +
                `Partial work may have been written to disk or pushed to the server — do NOT assume it succeeded or failed.\n` +
                `${tail ? 'Output captured before cancel:\n' + tail : '(no output was captured)'}\n(workspace=${workspaceDir})` });
              return;
            }
            if (timedOut) {
              // A timeout is NOT a build failure — the old "ERROR: ... timed out"
              // message made the agent report the build as failed. Return it as a
              // non-error result with a loud, actionable explanation AND the
              // output captured so far (the tail may even show BUILD SUCCESS).
              const mins = Math.round(timeout / 60000);
              const raw = stdout + (stderr ? '\nSTDERR:\n' + stderr : '');
              const tail = raw.length > 4000 ? '…(truncated; showing the end)…\n' + raw.slice(-4000) : raw;
              resolve({ content:
                `TIMEOUT: \`wl ${safeArgs}\` was stopped after ${mins} min (the time limit) — it did NOT report a result.\n` +
                `This is NOT a build failure and NOT a success: a WebLogic build can simply run longer than the limit, and work it already started on the server may still be finishing. Do NOT tell the developer the build failed based on this alone.\n` +
                `If it needs to finish, re-run the SAME command with a larger timeout_ms (up to 1800000 = 30 min). Output captured before the cutoff (check the end for BUILD SUCCESS or errors):\n` +
                `${tail || '(no output was captured before the cutoff)'}\n(workspace=${workspaceDir})` });
              return;
            }
            const head = `exit ${code} in ${dur}ms  (workspace=${workspaceDir})`;
            const body = stdout + (stderr ? '\nSTDERR:\n' + stderr : '');
            // Cap any returned log to a head/tail tail (the success/error markers
            // live at the end) so an unbounded build log never floods context.
            const cap = (s) => s.length > 4000 ? '…(truncated; showing the end)…\n' + s.slice(-4000) : s;
            if (code !== 0 && code != null) {
              resolve(`ERROR: ${head}\n${cap(body)}`);
              return;
            }
            // SUCCESS (exit 0): return a TERSE result, NOT the whole build log.
            // The full output already streamed live to the UI (onProgress); feeding
            // the entire log back is what made the model re-narrate it as a big
            // "DEPLOYMENT SUMMARY" table. Keep just the outcome lines so there is
            // nothing verbose to tabulate. Fall back to a capped tail only if no
            // recognizable success line is found (so an odd exit-0 still shows output).
            const successLines = body.split(/\r?\n/)
              .map((l) => l.trim())
              .filter((l) => l && /\b(BUILD SUCCESS|successfully|deployed|redeploy(ed)?|installed|projprops|completed)\b/i.test(l) && !/\b(error|fail(ed|ure)?)\b/i.test(l))
              .slice(-8);
            const terse = successLines.length ? successLines.join('\n') : cap(body);
            resolve({ content: `${head}\nSUCCESS — \`wl ${safeArgs}\` completed.\n${terse}` });
          };
          child.on('close', settle);
          child.on('error', (e) => { stderr += '\n' + e.message; settle(-1); });
        });
      },
    },
    {
      verb: 'wl_list_docs',
      name: 'wl_list_docs',
      schema: {
        description: 'List files and subdirectories in the wl source/docs repo at the given path. Backed by the GitLab v4 API — no clone, no checkout. Use this to walk the repo and find the docs you want.',
        parameters: {
          type: 'object',
          properties: {
            path: {
              type: 'string',
              description: 'Path within the repo. Empty string lists the root. Examples: "", "docs", "src/cmd".',
            },
            ref: {
              type: 'string',
              description: 'Branch, tag, or commit SHA. Defaults to the repo default branch.',
            },
          },
          required: [],
        },
      },
      async impl({ path: dirPath, ref }, ctx) {
        const client = buildGitLabClient(ctx);
        const entries = await client.listTree({
          projectPath: WL_DOCS_REPO,
          path: typeof dirPath === 'string' ? dirPath : '',
          ref: ref || undefined,
        });
        if (!Array.isArray(entries) || entries.length === 0) {
          return { content: `(empty) ${WL_DOCS_REPO} @ ${ref || 'default'} : ${dirPath || '/'}` };
        }
        const lines = entries.map((e) => `${e.type === 'tree' ? 'd' : '-'}  ${e.path}`);
        return { content: `${WL_DOCS_REPO} @ ${ref || 'default'} : ${dirPath || '/'}  (${entries.length} entries)\n${lines.join('\n')}` };
      },
    },
    {
      verb: 'wl_read_doc',
      name: 'wl_read_doc',
      schema: {
        description: 'Read a single file from the wl source/docs repo via the GitLab API. Text files come back as content. Binary files (e.g. the .docx user guide) are saved to a temp path the agent can then pass to read_docx.',
        parameters: {
          type: 'object',
          properties: {
            path: {
              type: 'string',
              description: 'Path of the file within the repo. E.g. "README.md", "docs/user-guide.docx".',
            },
            ref: {
              type: 'string',
              description: 'Branch, tag, or commit SHA. Defaults to the repo default branch.',
            },
          },
          required: ['path'],
        },
      },
      async impl({ path: filepath, ref }, ctx) {
        if (typeof filepath !== 'string' || !filepath.trim()) {
          return 'ERROR: path is required';
        }
        const client = buildGitLabClient(ctx);
        const result = await client.readFile({
          projectPath: WL_DOCS_REPO,
          filepath: filepath.trim(),
          ref: ref || undefined,
        });
        if (result.encoding === 'utf8') {
          return { content: `${WL_DOCS_REPO}:${filepath}  (${result.size} bytes, ${result.contentType || 'text'})\n${result.content}` };
        }
        // Binary: write to a temp file and report the path. The temp name
        // gets a random segment so two binaries read in the same millisecond
        // don't collide and overwrite each other. The guidance is branched on
        // type — only a .docx should be sent to read_docx (which otherwise
        // throws "not a .docx"); other binaries need a type-appropriate reader.
        const safeName = path.basename(filepath).replace(/[^a-zA-Z0-9._-]/g, '_');
        const rand = Math.random().toString(36).slice(2, 8);
        const tmp = path.join(tmpdir(), `wl-doc-${Date.now()}-${rand}-${safeName}`);
        await writeFile(tmp, result.content);
        const ext = path.extname(safeName).toLowerCase();
        const ct = result.contentType || '';
        const isDocx = ext === '.docx' || /wordprocessingml|officedocument/i.test(ct);
        const hint = isDocx
          ? 'Use read_docx on that path to extract its text, or search_docx to find specific text without reading the whole doc.'
          : `Saved as a binary file (${ct || ext || 'unknown type'}). read_docx only handles .docx — use an appropriate reader for this type, or inspect it directly.`;
        return { content: `${WL_DOCS_REPO}:${filepath}  (${result.size} bytes, binary ${ct})\nSaved to: ${tmp}\n${hint}` };
      },
    },
  ],
};

wlPlugin;

// ====== plugins/docx-edit.mjs ======
/**
 * `docx-edit` plugin — full-fidelity .docx read + edit via the Apache POI
 * Java helper (codeboss-docx-helper.jar + lib/). Node stays zero-dependency;
 * all Word fidelity (formatting, lists, headers/footers, images, tables,
 * hyperlinks, TOC fields) lives in the JVM.
 *
 * Tools (this is the ONLY docx implementation — there is no pure-JS fallback):
 *   read_docx       — anchored, editable view of every paragraph (body,
 *                     table cells, headers/footers) with a stable [#id] anchor.
 *   search_docx     — find text without reading the whole doc; returns matching
 *                     paragraphs with the same [#id] anchors read_docx gives.
 *   edit_docx       — unique-per-paragraph find/replace, mirroring edit_file's
 *                     contract. Only the matched runs' text changes; everything
 *                     else round-trips byte-faithfully.
 *
 * Helper resolution (ensureHelper):
 *   1. A PREBUILT jar — <bundleDir>/tools/, ../tools/, dev docx-helper/target/,
 *      or CODEBOSS_DOCX_HELPER=<jar>. Requires a JRE on PATH.
 *   2. Else COMPILE FROM EMBEDDED SOURCE — the production bundle carries the
 *      helper's Java source (DOCX_HELPER_SOURCES, injected by scripts/build.mjs)
 *      so only ONE .mjs need be transferred to an air-gapped box. On first use
 *      it writes the source to ~/.code_boss/docx-helper/ and `javac`s it against
 *      user-supplied POI jars in that folder's lib/ (or CODEBOSS_DOCX_LIB). If
 *      the jars aren't there yet it tells the operator exactly what to copy
 *      where; this needs a JDK (javac), not just a JRE.
 */

const HELPER_MAIN = 'com.codeboss.docx.DocxHelper';
const HELPER_JAR = 'codeboss-docx-helper.jar';

// The POI 5.5.1 dependency closure the helper is compiled against, each with
// its Maven repository path. Surfaced verbatim in the missing-deps message so
// an air-gapped operator can pull the exact files. Coordinates confirmed from
// each jar's META-INF/maven pom.properties (or manifest Implementation-Vendor
// for the POI/xmlbeans jars, which strip pom.properties).
const DOCX_DEP_JARS = [
  ['poi-ooxml-5.5.1.jar',           'org/apache/poi/poi-ooxml/5.5.1/poi-ooxml-5.5.1.jar'],
  ['poi-5.5.1.jar',                 'org/apache/poi/poi/5.5.1/poi-5.5.1.jar'],
  ['poi-ooxml-lite-5.5.1.jar',      'org/apache/poi/poi-ooxml-lite/5.5.1/poi-ooxml-lite-5.5.1.jar'],
  ['xmlbeans-5.3.0.jar',            'org/apache/xmlbeans/xmlbeans/5.3.0/xmlbeans-5.3.0.jar'],
  ['SparseBitSet-1.3.jar',          'com/zaxxer/SparseBitSet/1.3/SparseBitSet-1.3.jar'],
  ['curvesapi-1.08.jar',            'com/github/virtuald/curvesapi/1.08/curvesapi-1.08.jar'],
  ['commons-codec-1.20.0.jar',      'commons-codec/commons-codec/1.20.0/commons-codec-1.20.0.jar'],
  ['commons-collections4-4.5.0.jar','org/apache/commons/commons-collections4/4.5.0/commons-collections4-4.5.0.jar'],
  ['commons-compress-1.28.0.jar',   'org/apache/commons/commons-compress/1.28.0/commons-compress-1.28.0.jar'],
  ['commons-io-2.21.0.jar',         'commons-io/commons-io/2.21.0/commons-io-2.21.0.jar'],
  ['commons-lang3-3.18.0.jar',      'org/apache/commons/commons-lang3/3.18.0/commons-lang3-3.18.0.jar'],
  ['commons-math3-3.6.1.jar',       'org/apache/commons/commons-math3/3.6.1/commons-math3-3.6.1.jar'],
  ['log4j-api-2.24.3.jar',          'org/apache/logging/log4j/log4j-api/2.24.3/log4j-api-2.24.3.jar'],
];

// Same root-containment guard as the built-in write tools (resolveDocxWritePath in
// src/agent/tools.mjs — not exported, so replicated here). Reads are NOT gated.
function resolveDocxWritePath(p, cwd) {
  const root = path.resolve(cwd || process.cwd());   // per-call cwd (no global process.chdir)
  const abs = path.isAbsolute(p) ? path.resolve(p) : path.resolve(root, p);
  if (abs !== root && !abs.startsWith(root + path.sep)) {
    throw new Error(`path escapes project root: ${p}`);
  }
  if (isControlPath(abs)) throw new Error(CONTROL_DIR_MSG);   // never mutate control state
  return abs;
}

function resolveAbsRead(p, cwd) {
  const abs = path.isAbsolute(p) ? path.resolve(p) : path.resolve(cwd || process.cwd(), p);
  if (isControlPath(abs)) throw new Error(CONTROL_DIR_MSG);   // .code_boss is off-limits to reads too
  return abs;
}

// Locate a PREBUILT helper jar + build a classpath including its lib/ folder.
// Returns { classpath } or null if not found.
function locatePrebuiltHelper() {
  const candidates = [];
  if (process.env.CODEBOSS_DOCX_HELPER) candidates.push(process.env.CODEBOSS_DOCX_HELPER);
  let moduleDir = '';
  try { moduleDir = path.dirname(fileURLToPath(import.meta.url)); } catch { moduleDir = process.cwd(); }
  // Bundled: <bundleDir>/tools/<jar>. Dev (src/plugins): ../../docx-helper/target/<jar>.
  candidates.push(path.join(moduleDir, 'tools', HELPER_JAR));
  candidates.push(path.join(moduleDir, '..', 'tools', HELPER_JAR));
  candidates.push(path.join(moduleDir, '..', '..', 'docx-helper', 'target', HELPER_JAR));
  for (const c of candidates) {
    try {
      if (existsSync(c)) {
        const dir = path.dirname(c);
        const cp = `${c}${path.delimiter}${path.join(dir, 'lib', '*')}`;
        return { classpath: cp };
      }
    } catch { /* ignore */ }
  }
  return null;
}

// Where the bundle writes + compiles the helper from its embedded source when
// no prebuilt jar is deployed. Per-user so it survives across runs.
function helperWorkDir() { return path.join(homedir(), '.code_boss', 'docx-helper'); }

// Find the POI dependency jars for a compile-from-source. Priority:
//   CODEBOSS_DOCX_LIB  → explicit override
//   <bundleDir>/tools/lib  → the deps a `package` run shipped NEXT TO the bundle
//                            (a deps-only package — no prebuilt helper jar)
//   ~/.code_boss/docx-helper/lib  → the legacy per-user drop folder
// First candidate that actually holds .jar files wins. Without the tools/lib
// entry, a deps-only deployment was broken: the package put the jars in
// tools/lib but the compile step only ever looked under ~/.code_boss, so it
// reported "copy the 13 jars" even though they were sitting right there.
function resolveDocxLibDir() {
  let moduleDir = '';
  try { moduleDir = path.dirname(fileURLToPath(import.meta.url)); } catch { moduleDir = process.cwd(); }
  const cands = [];
  if (process.env.CODEBOSS_DOCX_LIB) cands.push(process.env.CODEBOSS_DOCX_LIB);
  cands.push(path.join(moduleDir, 'tools', 'lib'));
  cands.push(path.join(moduleDir, '..', 'tools', 'lib'));
  const home = path.join(helperWorkDir(), 'lib');
  cands.push(home);
  for (const d of cands) {
    try { if (existsSync(d) && readdirSync(d).some((f) => f.toLowerCase().endsWith('.jar'))) return d; } catch { /* */ }
  }
  return home; // default target for the "copy the jars here" message
}

// Content fingerprint of the embedded Java sources — dependency-free (no crypto
// import). Stamped next to the compiled classes so a box that already compiled
// an OLDER helper recompiles when an updated bundle changes the Java, instead of
// silently running stale bytecode. Length + FNV-1a + position-weighted sum makes
// accidental collisions across source edits effectively impossible.
function docxSourceFingerprint(sources) {
  const s = JSON.stringify(sources || {});
  let h = 0x811c9dc5, acc = 0;
  for (let i = 0; i < s.length; i++) {
    const c = s.charCodeAt(i);
    h = ((h ^ c) >>> 0); h = (Math.imul(h, 0x01000193) >>> 0);
    acc = ((acc + c * (i + 1)) >>> 0);
  }
  return `${s.length.toString(36)}.${h.toString(16)}.${acc.toString(16)}`;
}

// Single-file deploy path: the production bundle embeds the helper's Java
// source (DOCX_HELPER_SOURCES, injected by scripts/build.mjs). On first use we
// write it out and `javac` it against user-supplied POI jars, so only ONE .mjs
// has to be transferred to the air-gapped box. Returns { classpath } on
// success, null if no embedded source (dev — use the prebuilt jar), or THROWS
// a descriptive error (.code = docx-needs-deps | docx-needs-jdk |
// docx-compile-failed) so the plugin trigger can tell the operator what to do.
function bootstrapFromSource() {
  // In dev (running src/) the constant isn't injected — fall back to prebuilt.
  if (typeof DOCX_HELPER_SOURCES === 'undefined' || !DOCX_HELPER_SOURCES) return null;
  const workDir = helperWorkDir();
  const srcDir = path.join(workDir, 'src');
  const classesDir = path.join(workDir, 'classes');
  const libDir = resolveDocxLibDir();
  const classpath = `${classesDir}${path.delimiter}${path.join(libDir, '*')}`;
  const mainClass = path.join(classesDir, 'com', 'codeboss', 'docx', 'DocxHelper.class');

  // Reuse a previous compile ONLY if it was built from the SAME embedded source.
  // We stamp a fingerprint of the embedded sources next to the compiled classes.
  // If the bundle's Java has since changed (a code_boss update was deployed), the
  // fingerprint won't match — fall through and recompile (javac overwrites the
  // stale .class files). Without this, an updated helper would NEVER take effect
  // on a box that already compiled an older version: it only checked that
  // DocxHelper.class EXISTED, so the old bytecode ran forever.
  const fpFile = path.join(classesDir, '.source-fingerprint');
  const fp = docxSourceFingerprint(DOCX_HELPER_SOURCES);
  if (existsSync(mainClass) && existsSync(fpFile)) {
    let prev = '';
    try { prev = readFileSync(fpFile, 'utf8').trim(); } catch { /* */ }
    if (prev === fp) return { classpath };
  }

  // Write the embedded sources to disk (package-relative paths).
  const javaFiles = [];
  for (const [rel, src] of Object.entries(DOCX_HELPER_SOURCES)) {
    const dest = path.join(srcDir, rel);
    mkdirSync(path.dirname(dest), { recursive: true });
    writeFileSync(dest, src, 'utf8');
    javaFiles.push(dest);
  }

  // Dependencies present?
  let libs = [];
  try { libs = existsSync(libDir) ? readdirSync(libDir).filter((f) => f.toLowerCase().endsWith('.jar')) : []; } catch { /* */ }
  if (!libs.length) {
    mkdirSync(libDir, { recursive: true });
    const bullets = DOCX_DEP_JARS.map(([jar, p]) => `  - ${jar}\n        ${p}`).join('\n');
    const e = new Error(
      'docx editing needs the Apache POI 5.5.1 dependency jars (one-time setup; the closure ' +
      'code_boss was built + tested against). Copy these 13 jars into:\n  ' + libDir + '\n' +
      'Each jar, with its Maven repository path:\n' +
      bullets + '\n' +
      '(matching versions are safest; or set CODEBOSS_DOCX_LIB to a folder that already contains ' +
      'them). Then restart. The helper compiles automatically from source embedded in this bundle ' +
      '— a JDK 11+ (javac) must be on PATH.'
    );
    e.code = 'docx-needs-deps';
    throw e;
  }

  // Compile the embedded source against the supplied jars (needs a JDK).
  // `--release 11` pins both the language level and the bytecode target to
  // Java 11 (matching docx-helper/pom.xml's maven.compiler.release), so even
  // on a newer build JDK the output still loads on a JRE 11 — verified
  // end-to-end on a real JDK/JRE 11.0.26 (compile + read + tracked edit). POI
  // 5.5.1 and all its deps are <= Java 8 bytecode, so they load on 11 too.
  mkdirSync(classesDir, { recursive: true });
  const r = spawnSync('javac', ['--release', '11', '-cp', path.join(libDir, '*'), '-d', classesDir, ...javaFiles], { encoding: 'utf8' });
  if (r.error && r.error.code === 'ENOENT') {
    const e = new Error('docx editing needs a JDK (javac) on PATH to build the helper from source — a JRE alone is not enough. Install a JDK 11+ (the deploy box already needs java; use a JDK), or deploy a prebuilt codeboss-docx-helper.jar under tools/.');
    e.code = 'docx-needs-jdk';
    throw e;
  }
  if (r.status !== 0 || !existsSync(mainClass)) {
    const e = new Error('docx helper failed to compile: ' + String(r.stderr || r.stdout || '(no output)').slice(0, 400));
    e.code = 'docx-compile-failed';
    throw e;
  }
  // Record which embedded source this compile was built from, so a later update
  // that changes the Java triggers a recompile instead of reusing stale classes.
  try { writeFileSync(fpFile, fp, 'utf8'); } catch { /* */ }
  return { classpath };
}

// Resolve a runnable helper classpath: a prebuilt jar if deployed, else
// compile-from-embedded-source. Caches the success so we javac at most once.
let _helperCache = null;
function ensureHelper() {
  if (_helperCache) return _helperCache;
  const pre = locatePrebuiltHelper();
  if (pre) { _helperCache = pre; return pre; }
  const built = bootstrapFromSource();   // may throw (deps/jdk/compile)
  if (built) { _helperCache = built; return built; }
  return null;
}

// Spawn `java ... DocxHelper <op>`, feed the JSON spec on stdin, parse the one
// JSON document on stdout. Returns the parsed object (may be { ok:false, ... }).
function runHelper(op, spec, { signal, timeoutMs = 60000, cwd } = {}) {
  let loc;
  try { loc = ensureHelper(); }
  catch (e) { return Promise.resolve({ ok: false, code: 'file-error', error: e.message }); }
  if (!loc) return Promise.resolve({ ok: false, code: 'file-error', error: `docx helper jar not found (set CODEBOSS_DOCX_HELPER or deploy ${HELPER_JAR} under tools/)` });
  return new Promise((resolve) => {
    const outDec = new StringDecoder('utf8');
    const errDec = new StringDecoder('utf8');
    let stdout = '', stderr = '', settled = false;
    const child = spawn('java', ['-Dfile.encoding=UTF-8', '-cp', loc.classpath, HELPER_MAIN, op], {
      cwd: cwd || process.cwd(), windowsHide: true,
    });
    const onAbort = () => { try { child.kill(); } catch {} };
    if (signal) {
      if (signal.aborted) onAbort();
      else signal.addEventListener('abort', onAbort, { once: true });
    }
    const tid = setTimeout(() => { try { child.kill(); } catch {} }, Math.min(Math.max(5000, timeoutMs), 600000));
    const settle = (obj) => {
      if (settled) return;
      settled = true;
      clearTimeout(tid);
      if (signal) try { signal.removeEventListener('abort', onAbort); } catch {}
      resolve(obj);
    };
    child.stdout.on('data', (d) => { stdout += outDec.write(d); });
    child.stderr.on('data', (d) => { stderr += errDec.write(d); });
    child.on('error', (e) => settle({ ok: false, code: 'file-error', error: `cannot spawn java: ${e.message} (is a JRE on PATH?)` }));
    child.on('close', () => {
      stdout += outDec.end();
      stderr += errDec.end();
      // The helper writes exactly one JSON document; take the last non-empty line.
      const lines = stdout.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
      const last = lines.length ? lines[lines.length - 1] : '';
      try {
        resolve(JSON.parse(last));
      } catch {
        settle({ ok: false, code: 'file-error', error: `unparseable helper output: ${(last || stderr || '(empty)').slice(0, 400)}` });
      }
    });
    try {
      child.stdin.write(Buffer.from(JSON.stringify(spec), 'utf8'));
      child.stdin.end();
    } catch (e) {
      settle({ ok: false, code: 'file-error', error: `cannot write to helper stdin: ${e.message}` });
    }
  });
}

// Spec-maintenance is the DEFAULT posture for this deployment: every change to
// a mature/scrutinized spec must be explicitly approved by the developer (the
// TOOL enforces it — not the agent) and is recorded as a tracked revision. Set
// CODEBOSS_DOCX_GENERAL=1 to opt into the less-restrictive general profile
// (direct edits, no mandatory approval).
function specMode() { return process.env.CODEBOSS_DOCX_GENERAL !== '1'; }

function describeChange(ops, dry) {
  const lines = ops.map((op, i) => {
    const r = (dry.ops && dry.ops[i]) || {};
    const t = op.op || 'replace';
    if (t === 'setRunFormat') return `• format #${op.id} ${JSON.stringify(op.find)} → {${Object.entries(op.format || {}).map(([k, v]) => `${k}=${v}`).join(', ')}}`;
    if (t === 'setParaStyle') return `• style #${op.id} → ${op.style}`;
    if (t === 'setParaProps') return `• paragraph #${op.id} → ${Object.keys(op).filter((k) => k !== 'op' && k !== 'id').map((k) => `${k}=${op[k]}`).join(', ')}`;
    if (t === 'setListMembership') return `• list #${op.id} → numId ${op.numId}`;
    return `• #${op.id}: ${JSON.stringify(r.before)} → ${JSON.stringify(r.after)}`;
  });
  return `Approve this tracked change to the document?\n${lines.join('\n')}\n(It will be recorded as a Word revision you can review/accept.)`;
}

// Run an edit through the spec-maintenance gate (dry-run preview → mandatory
// developer approval → apply as a tracked revision) or, in general mode,
// directly. Returns { res } (helper result) or { error } (string).
async function applyOps(abs, ops, ctx) {
  if (typeof ctx?.requireBranch === 'function') {
    const s = await ctx.requireBranch(abs);
    if (s === 'aborted') return { error: 'edit aborted: user declined to create a CODE_BOSS branch for this repo' };
  }
  const base = { op: 'edit', docx: abs, ops, author: 'code_boss', date: new Date().toISOString() };
  if (specMode()) {
    if (typeof ctx?.requireApproval !== 'function') {
      return { error: 'spec-maintenance mode requires explicit developer approval, which is not available in this context' };
    }
    const dry = await runHelper('edit', { ...base, dryRun: true, tracked: true }, { signal: ctx?.signal, cwd: ctx?.cwd });
    if (!dry || dry.ok !== true) return { res: dry };   // surface validation error (not-found, straddle, …)
    const approved = await ctx.requireApproval({ kind: 'docx-change', message: describeChange(ops, dry) });
    if (!approved) return { error: 'change not approved by developer' };
    return { res: await runHelper('edit', { ...base, dryRun: false, tracked: true }, { signal: ctx?.signal, cwd: ctx?.cwd }) };
  }
  return { res: await runHelper('edit', { ...base, dryRun: false }, { signal: ctx?.signal, cwd: ctx?.cwd }) };
}

function formatOpsResult(res, abs, cwd) {
  const rel = path.relative(cwd || process.cwd(), abs) || abs;
  const o = (res.ops && res.ops[0]) || {};
  const trk = res.trackChanges ? '  [tracked change — shows as a Word revision]' : '';
  const field = res.updateFieldsOnOpen ? '  (TOC/fields refresh when opened in Word)' : '';
  const t = o.op || 'replace';
  let summary, diff;
  if (t === 'replace' || o.before !== undefined) {
    const flat = o.flattened ? '  [FORMAT FLATTENED — review/fix formatting manually]' : '';
    summary = `edited ${rel} #${o.id}  ${shortDiffSummary(o.before || '', o.after || '')}${flat}`;
    diff = renderUnifiedDiff(o.before || '', o.after || '', { pathLabel: `${rel} #${o.id}`, context: 2 });
  } else if (t === 'setRunFormat') {
    summary = `formatted ${rel} #${o.id}  ${JSON.stringify(o.find)} → {${Object.entries(o.format || {}).map(([k, v]) => `${k}=${v}`).join(', ')}}`;
  } else if (t === 'setParaStyle') {
    summary = `styled ${rel} #${o.id} → ${o.style}`;
  } else {
    summary = `changed ${rel} #${o.id} (${t})`;
  }
  const out = { content: summary + trk + field, path: rel };
  if (diff) out.diff = diff;
  return out;
}

// One paragraph → its anchored display line. Shared by read_docx and search_docx
// so a hit shows exactly the same [#id]/scope/markers the full read shows.
function paraLine(p) {
  const reason = p.hasRevisions ? 'tracked-changes' : p.contentControl ? 'content-control' : 'field/TOC';
  const ro = p.readOnly ? ` READ-ONLY(${reason})` : '';
  const lvl = (p.listLevel != null) ? ` list:L${p.listLevel}` : '';
  const where = p.scope === 'body' ? 'body' : `${p.scope} ${p.location}`;
  const marks = [];
  if (p.hasEquation) marks.push('[equation]');
  if (p.deletedText) marks.push(`[deleted: ${JSON.stringify(p.deletedText)}]`);
  const suffix = marks.length ? '  ' + marks.join(' ') : '';
  return `[#${p.id}] (${where}${lvl}${ro}) ${p.text}${suffix}`;
}

// Build a case-insensitive matcher for search_docx: literal substring by
// default, or a JS regex when regex=true.
function docxSearchRegex(query, useRegex) {
  if (useRegex) return new RegExp(query, 'gi');
  return new RegExp(query.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'gi');
}

function renderRead(res, { offset, limit }) {
  const paras = Array.isArray(res.paragraphs) ? res.paragraphs : [];
  const meta = res.meta || {};
  const start = Math.max(0, (Number(offset) || 1) - 1);
  const max = Math.min(5000, Math.max(1, Number(limit) || 1000));
  const slice = paras.slice(start, start + max);
  const lines = slice.map(paraLine);
  const warns = [];
  if (meta.documentProtection) warns.push(`!! DOCUMENT PROTECTED (${meta.documentProtection}) — edits will be refused; remove protection in Word first.`);
  if (meta.hasTrackChanges) warns.push(`!! This document has TRACKED CHANGES (${meta.revisionParagraphs} paragraph(s)); those paragraphs are read-only here — review/accept changes in Word, or edit elsewhere. Deleted text is shown as [deleted: ...].`);
  if (meta.contentControls) warns.push(`Note: ${meta.contentControls} content control(s) present (shown read-only).`);
  if (meta.equationParagraphs) warns.push(`Note: ${meta.equationParagraphs} paragraph(s) contain equations (shown as [equation]).`);
  const head = `${res.docx}  (${paras.length} editable paragraphs, showing ${start + 1}..${start + slice.length})`;
  const note = 'Edit with edit_docx using the [#id] anchor + a unique find string within that paragraph. READ-ONLY paragraphs (TOC/fields, tracked-changes, content-controls) cannot be edited here.';
  return `${head}\n${note}${warns.length ? '\n' + warns.join('\n') : ''}\n\n${lines.join('\n')}`;
}

function renderDetail(res) {
  const paras = Array.isArray(res.paragraphs) ? res.paragraphs : [];
  const out = [];
  for (const p of paras) {
    if (p.contentControl) { out.push(`[#${p.id}] (content-control, read-only) ${p.text || ''}`); continue; }
    const bits = [];
    if (p.styleName || p.style) bits.push(`style=${p.styleName || p.style}`);
    if (p.outlineLevel != null) bits.push(`heading-level ${p.outlineLevel}`);
    if (p.alignment && p.alignment !== 'LEFT') bits.push(`align=${p.alignment}`);
    if (p.numbering) bits.push(`list numId=${p.numbering.numId} level=${p.numbering.ilvl ?? 0}`);
    if (p.pPr) {
      const pp = p.pPr, pb = [];
      if (pp.indentLeftTwips) pb.push(`indentL=${pp.indentLeftTwips}tw`);
      if (pp.spacingBeforeTwips) pb.push(`spBefore=${pp.spacingBeforeTwips}`);
      if (pp.spacingAfterTwips) pb.push(`spAfter=${pp.spacingAfterTwips}`);
      if (pb.length) bits.push(pb.join(' '));
    }
    out.push(`[#${p.id}]${p.readOnly ? ' READ-ONLY' : ''}  ${bits.join('  ')}`);
    for (const r of (p.runs || [])) {
      const e = r.effective || {};
      const efmt = [
        e.bold ? 'bold' : null, e.italic ? 'italic' : null,
        (e.underline && e.underline !== 'none') ? `underline:${e.underline}` : null,
        e.font ? `font:${e.font}` : null, e.sizePt ? `${e.sizePt}pt` : null, e.color ? `#${e.color}` : null,
      ].filter(Boolean).join(', ') || 'plain';
      const auth = r.authored ? `  (authored: ${Object.entries(r.authored).map(([k, v]) => `${k}=${v}`).join(', ')})` : '';
      out.push(`   r${r.r} @${r.start}..${r.end} ${JSON.stringify(r.text)}  [${efmt}]${auth}`);
    }
  }
  return out.join('\n');
}

const docxEditPlugin = {
  name: 'docx-edit',
  description: 'Full-fidelity .docx read + edit via an Apache POI Java helper (preserves formatting, lists, headers, images, tables, hyperlinks, TOC). Requires a JRE + the helper jar.',
  async trigger(executor) {
    // Resolve (or build-from-embedded-source) the helper. A missing-deps /
    // missing-JDK condition THROWS — letting the message surface in the plugin
    // status (boot log + Snapshot query 48) so the operator knows what to copy.
    const loc = ensureHelper();
    if (!loc) return false;
    try {
      const r = await executor.run('if (Get-Command java -ErrorAction SilentlyContinue) { "yes" } else { "no" }');
      return r.exitCode === 0 && /^yes/i.test(r.stdout.trim());
    } catch {
      return false;
    }
  },
  promptAddition: () => {
    const common = `
Reading & editing Microsoft Word .docx files (full fidelity):
  <read_docx path="doc.docx"/>               read the doc as an anchored outline (text + structure)
  <search_docx path="doc.docx" query="enrollment"/>  find text fast — matches come back with their [#id]
  <read_docx_detail path="doc.docx" paragraphs="12,13"/>  inspect formatting of specific paragraphs
  <edit_docx path="doc.docx" paragraph="12" find="exact text" replace="new text"/>
  <format_docx path="doc.docx" paragraph="12" find="some words" bold="true" color="FF0000"/>
  <style_docx path="doc.docx" paragraph="12" style="Heading1" alignment="CENTER"/>

read_docx lists every paragraph with a [#id] anchor and marks READ-ONLY regions
(TOC/fields, tracked-changes, content-controls). search_docx finds text WITHOUT
reading the whole document and returns matches with the same [#id] anchors, so
you can edit a hit directly — prefer it when you only need to locate something.
edit_docx replaces ONE exact occurrence of "find" within paragraph [#id] (fails
if missing or not unique). format_docx changes character formatting of a span;
style_docx changes paragraph style/alignment/indent/list. Only what you target
changes; all other formatting, lists, images, tables, hyperlinks, headers, and
the TOC are preserved. Workflow: read_docx (or search_docx) for anchors,
read_docx_detail to inspect formatting, then edit/format/style per change.`;
    if (specMode()) {
      return (common + `

SPEC-MAINTENANCE MODE (these are mature, scrutinized specifications):
- Make ONLY the minimal change required by the task. Do NOT add new content,
  reword for style, or "improve" anything that is not actually wrong.
- EVERY change must be confirmed by the developer — each edit_docx/format_docx/
  style_docx call pauses for an explicit Approve/Deny, and applies the change as
  a TRACKED REVISION so it can be reviewed and accepted in Word. Expect to make
  many small, individually-approved edits rather than broad changes.
- If unsure whether a change is warranted, ask the developer rather than editing.`).trim();
    }
    return (common + `

Track changes: add asTracked="true" to edit_docx to record an edit as a Word
revision instead of a direct edit; edits auto-track when the doc already has
tracked changes. A find that spans a formatting boundary needs allowFormatFlatten
(developer-approved); prefer narrowing the find to one format.`).trim();
  },
  tools: [
    {
      verb: 'read_docx',
      name: 'read_docx',
      schema: {
        description: 'Read a .docx — THE reader for ANY purpose (understanding it AND editing it). Returns every paragraph (body, table cells, headers, footers) with a [#id] anchor, scope, and read-only field/TOC markers. The [#id] anchors are what edit_docx/format_docx/style_docx need, so reading with this also preps an edit. To just LOCATE text without reading the whole doc, use search_docx (it returns the same [#id] anchors for its matches).',
        parameters: {
          type: 'object',
          properties: {
            path: { type: 'string', description: 'Absolute or project-relative path to a .docx file.' },
            offset: { type: 'integer', description: 'Optional 1-based starting paragraph (for paging).' },
            limit: { type: 'integer', description: 'Optional max paragraphs to return (default 1000).' },
          },
          required: ['path'],
        },
      },
      async impl({ path: p, offset, limit }, ctx) {
        if (typeof p !== 'string' || !p) return 'ERROR: path is required';
        const abs = resolveAbsRead(p, ctx?.cwd);
        const res = await runHelper('read', { op: 'read', docx: abs }, { signal: ctx?.signal, cwd: ctx?.cwd });
        if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'read failed'}`;
        return { content: renderRead(res, { offset, limit }) };
      },
    },
    {
      verb: 'search_docx',
      name: 'search_docx',
      schema: {
        description: 'Find text in a .docx WITHOUT reading the whole document. Returns each matching paragraph with its [#id] edit anchor, scope/list/read-only markers, and optional surrounding context — the same anchored view read_docx gives, filtered to matches. Case-insensitive substring (default) or regex (regex=true). Use it to locate content (and get its [#id]) quickly; then edit a hit directly via edit_docx with that [#id], or inspect it with read_docx_detail.',
        parameters: {
          type: 'object',
          properties: {
            path: { type: 'string', description: 'Absolute or project-relative path to a .docx file.' },
            query: { type: 'string', description: 'Text to find. Literal substring unless regex=true.' },
            regex: { type: 'boolean', description: 'If true, treat query as a JavaScript regular expression (always case-insensitive). Default false.' },
            maxMatches: { type: 'integer', description: 'Max matching paragraphs to return (default 50, cap 500).' },
            context: { type: 'integer', description: 'Paragraphs of surrounding context (in document/[#id] order) before/after each match (default 0, cap 5).' },
          },
          required: ['path', 'query'],
        },
      },
      async impl({ path: p, query, regex, maxMatches, context }, ctx) {
        if (typeof p !== 'string' || !p) return 'ERROR: path is required';
        if (typeof query !== 'string' || !query) return 'ERROR: query is required';
        let re;
        try { re = docxSearchRegex(query, !!regex); } catch (e) { return 'ERROR: bad regex: ' + e.message; }
        const abs = resolveAbsRead(p, ctx?.cwd);
        const res = await runHelper('read', { op: 'read', docx: abs }, { signal: ctx?.signal, cwd: ctx?.cwd });
        if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'read failed'}`;
        const paras = Array.isArray(res.paragraphs) ? res.paragraphs : [];
        const cap = Math.min(500, Math.max(1, Number(maxMatches) || 50));
        const ctxN = Math.min(5, Math.max(0, Number(context) || 0));
        const queryLabel = regex ? `/${query}/i` : JSON.stringify(query);
        const where = res.docx || abs;
        const hits = [];
        for (let i = 0; i < paras.length && hits.length < cap; i++) {
          re.lastIndex = 0;
          const text = (paras[i] && typeof paras[i].text === 'string') ? paras[i].text : '';
          if (re.test(text)) hits.push(i);
        }
        if (!hits.length) return { content: `${where}  (${paras.length} paragraphs)\nNo matches for ${queryLabel}.` };
        const blocks = hits.map((i) => {
          const lo = Math.max(0, i - ctxN), hi = Math.min(paras.length - 1, i + ctxN);
          const lines = [];
          for (let j = lo; j <= hi; j++) lines.push(`${j === i ? '>>' : '  '} ${paraLine(paras[j])}`);
          return lines.join('\n');
        });
        const more = hits.length >= cap ? `  (stopped at ${cap}; raise maxMatches for more)` : '';
        const note = 'Each hit shows its [#id] anchor — edit it directly with edit_docx (READ-ONLY hits cannot be edited).';
        return { content: `${where}  (${paras.length} paragraphs, ${hits.length} matching${more})\nQuery: ${queryLabel}\n${note}\n\n${blocks.join('\n\n')}` };
      },
    },
    {
      verb: 'read_docx_detail',
      name: 'read_docx_detail',
      schema: {
        description: 'Detailed formatting view of specific paragraphs — per-run EFFECTIVE formatting (what Word shows, resolved through the style cascade) and AUTHORED formatting (set directly on the run), char-offset ranges, paragraph style/outline-level/alignment/indent/spacing, and list numbering. Use after read_docx to inspect formatting before editing.',
        parameters: {
          type: 'object',
          properties: {
            path: { type: 'string', description: 'Absolute or project-relative path to a .docx file.' },
            paragraphs: { type: 'array', items: { type: 'integer' }, description: 'The [#id] anchors (from read_docx) to inspect in detail.' },
          },
          required: ['path', 'paragraphs'],
        },
      },
      async impl({ path: p, paragraphs }, ctx) {
        if (typeof p !== 'string' || !p) return 'ERROR: path is required';
        const abs = resolveAbsRead(p, ctx?.cwd);
        // The prose <...> protocol passes attributes as STRINGS, so `paragraphs`
        // arrives as "0,2,8,23" (or a single "0"), not a JSON array. Accept
        // both: a real array, or a comma/space-separated string of [#id]s.
        const rawList = Array.isArray(paragraphs)
          ? paragraphs
          : String(paragraphs ?? '').split(/[,\s]+/);
        // Strip any non-digits so the [#id] forms the model naturally writes
        // ("#0", "[#0]", "0") all coerce to the numeric anchor.
        const anchors = rawList
          .map((n) => String(n).replace(/[^0-9]/g, ''))
          .filter(Boolean)
          .map(Number)
          .filter((n) => !Number.isNaN(n));
        if (!anchors.length) return 'ERROR: paragraphs ([#id] list) is required';
        const res = await runHelper('detail', { op: 'detail', docx: abs, anchors }, { signal: ctx?.signal, cwd: ctx?.cwd });
        if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'detail failed'}`;
        return { content: renderDetail(res) };
      },
    },
    {
      verb: 'edit_docx',
      name: 'edit_docx',
      schema: {
        description: 'Edit a .docx in place: replace ONE exact occurrence of "find" with "replace" within paragraph [#id] (from read_docx). Mirrors edit_file — fails if find is missing or occurs more than once in that paragraph. All formatting, lists, images, tables, hyperlinks, headers, and TOC are preserved; only the matched text changes.',
        parameters: {
          type: 'object',
          properties: {
            path: { type: 'string', description: 'Absolute or project-relative path to a .docx file (must be inside the project).' },
            paragraph: { type: 'integer', description: 'The [#id] anchor of the paragraph to edit (from read_docx).' },
            find: { type: 'string', description: 'Exact text to find within that paragraph. Must occur exactly once there.' },
            replace: { type: 'string', description: 'Replacement text (empty string to delete the found text).' },
            asTracked: { type: 'boolean', description: 'Apply as a TRACKED CHANGE (Word revision: w:del of the old text + w:ins of the new) instead of a direct edit. Auto-forced when the document already contains tracked changes. The author is "code_boss".' },
            allowFormatFlatten: { type: 'boolean', description: 'Only set true if a find that spans a formatting boundary (e.g. plain into bold) is genuinely needed. Triggers a developer-approval prompt; the replaced text takes the first run\'s format and the developer fixes formatting afterward. Prefer narrowing the find to one format instead.' },
          },
          required: ['path', 'paragraph', 'find', 'replace'],
        },
      },
      async impl({ path: p, paragraph, find, replace, allowFormatFlatten, asTracked }, ctx) {
        if (typeof p !== 'string' || !p) return 'ERROR: path is required';
        if (paragraph == null || Number.isNaN(Number(paragraph))) return 'ERROR: paragraph ([#id]) is required';
        if (typeof find !== 'string' || !find) return 'ERROR: find is required';
        if (typeof replace !== 'string') return 'ERROR: replace is required';
        let abs;
        try { abs = resolveDocxWritePath(p, ctx?.cwd); } catch (e) { return `ERROR: ${e.message}`; }

        const op = { id: Number(paragraph), find, replace };
        if (asTracked) op.asTracked = true;
        if (allowFormatFlatten) op.allowFormatFlatten = true;

        // SPEC-MAINTENANCE MODE: every edit is dry-run-previewed, requires an
        // explicit developer Approve/Deny, and is applied as a tracked revision.
        if (specMode()) {
          const { res, error } = await applyOps(abs, [op], ctx);
          if (error) return 'ERROR: ' + error;
          if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'edit failed'}`;
          return formatOpsResult(res, abs, ctx?.cwd);
        }

        // ===== GENERAL MODE (direct edits; flatten gated separately) =====
        // Same pre-write hook as edit_file: ensure the repo is on a CODE_BOSS branch.
        if (typeof ctx?.requireBranch === 'function') {
          const status = await ctx.requireBranch(abs);
          if (status === 'aborted') return 'ERROR: edit aborted: user declined to create a CODE_BOSS branch for this repo';
        }

        // Format-flatten requires explicit developer approval (a flatten loses
        // intra-span formatting, so the developer fixes it after). The approval
        // gate is ctx.requireApproval; until it is wired, flatten is refused so
        // it can never happen silently.
        if (allowFormatFlatten) {
          if (typeof ctx?.requireApproval === 'function') {
            const preview = await runHelper('edit', { op: 'edit', docx: abs, dryRun: true, ops: [{ ...op, allowFormatFlatten: true }] }, { signal: ctx?.signal, cwd: ctx?.cwd });
            const previewText = preview?.ops?.[0] ? `"${preview.ops[0].before}" -> "${preview.ops[0].after}"` : `${find} -> ${replace}`;
            const ok = await ctx.requireApproval({
              kind: 'docx-flatten',
              message: `Editing paragraph #${paragraph} would flatten formatting across a bold/plain boundary: ${previewText}. The replaced text takes the first run's format; you will likely need to fix formatting manually afterward. Approve?`,
            });
            if (!ok) return 'ERROR: format-flatten not approved by developer; narrow the find to stay within one format, or split into separate edits';
            op.allowFormatFlatten = true;
          } else {
            return 'ERROR: this edit needs format flattening, which requires developer approval (not available in this context). Narrow the find to stay within one format, or split into separate edits.';
          }
        }

        const res = await runHelper('edit', { op: 'edit', docx: abs, dryRun: false, ops: [op], author: 'code_boss', date: new Date().toISOString() }, { signal: ctx?.signal, cwd: ctx?.cwd });
        if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'edit failed'}`;
        const o = (res.ops && res.ops[0]) || { before: '', after: '', flattened: false };
        const rel = path.relative(ctx?.cwd || process.cwd(), abs) || abs;
        const flat = o.flattened ? '  [FORMAT FLATTENED — review/fix formatting manually]' : '';
        const fieldNote = res.updateFieldsOnOpen ? '  (TOC/fields will refresh when opened in Word)' : '';
        const trk = (asTracked || res.trackChanges) ? '  [tracked change — shows as a Word revision]' : '';
        return {
          content: `edited ${rel} #${paragraph}  ${shortDiffSummary(o.before, o.after)}${trk}${flat}${fieldNote}`,
          diff: renderUnifiedDiff(o.before, o.after, { pathLabel: `${rel} #${paragraph}`, context: 2 }),
          path: rel,
        };
      },
    },
    {
      verb: 'format_docx',
      name: 'format_docx',
      schema: {
        description: 'Apply CHARACTER formatting (bold/italic/underline/strike/font/size/color) to a unique text span within a paragraph. Splits runs so only the matched span is affected; all other formatting is preserved. The text is unchanged.',
        parameters: {
          type: 'object',
          properties: {
            path: { type: 'string', description: 'Absolute or project-relative path to a .docx (inside the project).' },
            paragraph: { type: 'integer', description: 'The [#id] anchor (from read_docx).' },
            find: { type: 'string', description: 'Exact text within that paragraph to (re)format. Must occur exactly once.' },
            bold: { type: 'boolean' },
            italic: { type: 'boolean' },
            underline: { type: 'string', description: 'e.g. "single", "double", or "none".' },
            strike: { type: 'boolean' },
            font: { type: 'string', description: 'Font family name.' },
            sizePt: { type: 'number', description: 'Font size in points.' },
            color: { type: 'string', description: 'Hex RRGGBB (with or without #).' },
          },
          required: ['path', 'paragraph', 'find'],
        },
      },
      async impl({ path: p, paragraph, find, bold, italic, underline, strike, font, sizePt, color }, ctx) {
        if (typeof p !== 'string' || !p) return 'ERROR: path is required';
        if (paragraph == null) return 'ERROR: paragraph ([#id]) is required';
        if (typeof find !== 'string' || !find) return 'ERROR: find is required';
        const format = {};
        if (bold !== undefined) format.bold = bold;
        if (italic !== undefined) format.italic = italic;
        if (underline !== undefined) format.underline = underline;
        if (strike !== undefined) format.strike = strike;
        if (font !== undefined) format.font = font;
        if (sizePt !== undefined) format.sizePt = sizePt;
        if (color !== undefined) format.color = color;
        if (!Object.keys(format).length) return 'ERROR: provide at least one of bold/italic/underline/strike/font/sizePt/color';
        let abs;
        try { abs = resolveDocxWritePath(p, ctx?.cwd); } catch (e) { return `ERROR: ${e.message}`; }
        const { res, error } = await applyOps(abs, [{ op: 'setRunFormat', id: Number(paragraph), find, format }], ctx);
        if (error) return 'ERROR: ' + error;
        if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'format failed'}`;
        return formatOpsResult(res, abs, ctx?.cwd);
      },
    },
    {
      verb: 'style_docx',
      name: 'style_docx',
      schema: {
        description: 'Apply PARAGRAPH-level changes: set the paragraph style (e.g. a heading), alignment, indent/spacing, or list membership. Targets a whole paragraph by [#id]. Text is unchanged.',
        parameters: {
          type: 'object',
          properties: {
            path: { type: 'string', description: 'Absolute or project-relative path to a .docx (inside the project).' },
            paragraph: { type: 'integer', description: 'The [#id] anchor (from read_docx).' },
            style: { type: 'string', description: 'A style ID present in the document (e.g. "Heading1"). Use read_docx_detail to see style ids.' },
            alignment: { type: 'string', description: 'LEFT, CENTER, RIGHT, or BOTH (justified).' },
            indentLeftTwips: { type: 'integer', description: 'Left indent in twips (1440 = 1 inch).' },
            spacingBeforeTwips: { type: 'integer' },
            spacingAfterTwips: { type: 'integer' },
            numId: { type: 'integer', description: 'List numbering id to attach the paragraph to (use a numId already in the document).' },
            ilvl: { type: 'integer', description: 'List level (0-based), used with numId.' },
          },
          required: ['path', 'paragraph'],
        },
      },
      async impl({ path: p, paragraph, style, alignment, indentLeftTwips, spacingBeforeTwips, spacingAfterTwips, numId, ilvl }, ctx) {
        if (typeof p !== 'string' || !p) return 'ERROR: path is required';
        if (paragraph == null) return 'ERROR: paragraph ([#id]) is required';
        const id = Number(paragraph);
        const ops = [];
        if (style !== undefined) ops.push({ op: 'setParaStyle', id, style });
        const props = {};
        if (alignment !== undefined) props.alignment = alignment;
        if (indentLeftTwips !== undefined) props.indentLeftTwips = indentLeftTwips;
        if (spacingBeforeTwips !== undefined) props.spacingBeforeTwips = spacingBeforeTwips;
        if (spacingAfterTwips !== undefined) props.spacingAfterTwips = spacingAfterTwips;
        if (Object.keys(props).length) ops.push({ op: 'setParaProps', id, ...props });
        if (numId !== undefined && numId !== null) ops.push({ op: 'setListMembership', id, numId, ...(ilvl !== undefined ? { ilvl } : {}) });
        if (!ops.length) return 'ERROR: provide a style, and/or alignment/indent/spacing, and/or numId';
        let abs;
        try { abs = resolveDocxWritePath(p, ctx?.cwd); } catch (e) { return `ERROR: ${e.message}`; }
        const { res, error } = await applyOps(abs, ops, ctx);
        if (error) return 'ERROR: ' + error;
        if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'style edit failed'}`;
        return formatOpsResult(res, abs, ctx?.cwd);
      },
    },
    {
      verb: 'delete_paragraph_docx',
      name: 'delete_paragraph_docx',
      schema: {
        description: 'Structural: delete a paragraph by [#id]. In spec/tracked mode this marks the whole paragraph\'s content as a tracked deletion (struck-through; a reviewer accepts it in Word to remove it) rather than physically removing it. Use to remove outdated content from a spec.',
        parameters: {
          type: 'object',
          properties: {
            path: { type: 'string', description: 'Absolute or project-relative path to a .docx (inside the project).' },
            paragraph: { type: 'integer', description: 'The [#id] anchor (from read_docx) of the paragraph to delete.' },
          },
          required: ['path', 'paragraph'],
        },
      },
      async impl({ path: p, paragraph }, ctx) {
        if (typeof p !== 'string' || !p) return 'ERROR: path is required';
        if (paragraph == null) return 'ERROR: paragraph ([#id]) is required';
        let abs;
        try { abs = resolveDocxWritePath(p, ctx?.cwd); } catch (e) { return `ERROR: ${e.message}`; }
        const { res, error } = await applyOps(abs, [{ op: 'deleteParagraph', id: Number(paragraph) }], ctx);
        if (error) return 'ERROR: ' + error;
        if (!res || res.ok !== true) return `ERROR: ${res?.code || 'file-error'}: ${res?.error || 'delete failed'}`;
        return formatOpsResult(res, abs, ctx?.cwd);
      },
    },
  ],
};

docxEditPlugin;

// ====== mermaid-lite.mjs ======
/**
 * mermaid-lite — a hand-rolled, dependency-free renderer for a SUBSET of
 * Mermaid, producing SVG strings. No npm package, no vendored lib.
 *
 * Injected into ui.html's <head> via SHARED_BROWSER_SOURCES (scripts/build.mjs),
 * so it runs in the browser's single window scope: every top-level name here is
 * prefixed mm/_mm to avoid collisions (same rule as mosaic-layout.mjs). Keep it
 * PURE (no DOM reads, no Node APIs) except mmRenderInto's innerHTML write.
 *
 * Supported v1:
 *   FLOWCHART  'graph'/'flowchart' + TD|TB|LR|BT|RL; shapes [rect] (rounded)
 *              ((circle)) {diamond} >flag]; edges --> --- -.-> ==> with
 *              optional |label|; chains A --> B --> C; ';' statement separator.
 *   SEQUENCE   'sequenceDiagram'; participant/actor (with 'as'); A->>B: text,
 *              A-->>B: text (dashed); Note over/left of/right of; unknown
 *              lines (activate/loop/alt/…) are skipped gracefully.
 *
 * TOLERANT / STREAMING: mmParse NEVER throws — it goes line-by-line, SKIPS
 * lines it can't understand, and with {streaming:true} treats a trailing
 * line without a newline as not-yet-arrived. Unknown diagram types → null
 * (callers fall back to a plain code block).
 *
 * Exports: mmParse(text, opts) → model|null
 *          mmRender(text, opts) → svg string|null
 *          mmRenderInto(el, text, opts) → boolean
 */

// ── shared helpers ─────────────────────────────────────────────────────────

function mmEsc(s) {
  return String(s == null ? '' : s).replace(/[&<>"']/g, (c) => (
    { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
  ));
}

// Approximate rendered text width at font-size 12 (sans). Good enough for
// box sizing; the SVG never clips because boxes derive from this estimate.
function _mmTextW(s) {
  return Math.max(1, String(s).length) * 7.2;
}

// Strip optional surrounding quotes mermaid allows in labels: A["text"].
function _mmCleanLabel(s) {
  const t = String(s == null ? '' : s).trim();
  const q = t.match(/^"([\s\S]*)"$/);
  return q ? q[1] : t;
}

// Unique-per-render suffix for SVG marker ids — two diagrams on one page
// must not share marker defs (the hidden one's marker would win by id).
let _mmIdSeq = 0;

// ── parse ──────────────────────────────────────────────────────────────────

/**
 * Parse mermaid source into a model, or null when the diagram type is
 * unknown/unsupported or nothing usable parsed. Never throws.
 * opts.streaming: a trailing line WITHOUT a newline is "not yet arrived".
 */
function mmParse(text, opts) {
  try {
    return _mmParseInner(text, opts || {});
  } catch {
    return null;   // belt-and-suspenders: tolerant by contract
  }
}

function _mmParseInner(text, opts) {
  const raw = String(text == null ? '' : text);
  let lines = raw.split('\n');
  if (opts.streaming && !raw.endsWith('\n')) lines = lines.slice(0, -1);
  const body = [];
  for (const ln of lines) {
    const t = ln.trim();
    if (!t || t.startsWith('%%')) continue;
    body.push(t);
  }
  if (!body.length) return null;
  const head = body[0];
  let m = head.match(/^(?:graph|flowchart)\b\s*(TD|TB|LR|BT|RL)?\s*;?\s*(.*)$/i);
  if (m) {
    const rest = m[2] ? [m[2]] : [];
    return _mmParseFlow(rest.concat(body.slice(1)), (m[1] || 'TD').toUpperCase());
  }
  if (/^sequenceDiagram\b/i.test(head)) return _mmParseSeq(body.slice(1));
  return null;   // unknown diagram type (gitGraph, pie, …)
}

function _mmParseFlow(stmtLines, dir) {
  const g = { type: 'flowchart', dir, nodes: [], edges: [], _byId: new Map() };
  for (const line of stmtLines) {
    for (const stmt of line.split(';')) _mmParseFlowStmt(stmt, g);
  }
  delete g._byId;
  if (!g.nodes.length) return null;
  return g;
}

function _mmAddNode(g, n) {
  let cur = g._byId.get(n.id);
  if (!cur) {
    cur = { id: n.id, label: n.label != null ? n.label : n.id, shape: n.shape || 'rect' };
    g._byId.set(n.id, cur);
    g.nodes.push(cur);
  } else if (n.shape) {
    // A later explicit definition refines an earlier bare reference.
    cur.shape = n.shape;
    if (n.label != null) cur.label = n.label;
  }
  return cur;
}

/**
 * One flowchart statement: NODE (EDGE NODE)*. Scans left-to-right; whatever
 * parses cleanly is kept, an unparseable tail is dropped (streaming-friendly:
 * "A --> B --" keeps A, B and the edge).
 */
function _mmParseFlowStmt(stmt, g) {
  const s = stmt.trim();
  if (!s) return;
  // Recognized-but-unsupported keywords: skip the whole statement.
  if (/^(subgraph|end|classDef|class|style|linkStyle|click|direction|accTitle|accDescr)\b/i.test(s)) return;
  let i = 0;
  const skipWs = () => { while (i < s.length && /\s/.test(s[i])) i++; };
  const readNode = () => {
    skipWs();
    const idm = /^[A-Za-z0-9_]+/.exec(s.slice(i));
    if (!idm) return null;
    const id = idm[0];
    i += id.length;
    const rest = s.slice(i);
    // Shape openers (order matters: '((' before '(', '>…]' before bare).
    const shapes = [
      ['((', '))', 'circle'],
      ['[', ']', 'rect'],
      ['(', ')', 'rounded'],
      ['{', '}', 'diamond'],
      ['>', ']', 'flag'],
    ];
    for (const [open, close, shape] of shapes) {
      if (!rest.startsWith(open)) continue;
      const end = rest.indexOf(close, open.length);
      if (end === -1) return { id, label: null, shape: null };  // unclosed → bare ref
      return { id, label: _mmCleanLabel(rest.slice(open.length, end)), shape, _adv: end + close.length };
    }
    return { id, label: null, shape: null };
  };
  const readEdge = () => {
    skipWs();
    const em = /^(-\.->|-->|---|==>)/.exec(s.slice(i));
    if (!em) return null;
    i += em[0].length;
    const style = em[0] === '-->' ? 'arrow' : em[0] === '---' ? 'line' : em[0] === '-.->'
      ? 'dotted' : 'thick';
    skipWs();
    let label = null;
    if (s[i] === '|') {
      const end = s.indexOf('|', i + 1);
      if (end === -1) return null;          // label still streaming in → drop edge
      label = _mmCleanLabel(s.slice(i + 1, end));
      i = end + 1;
    }
    return { style, label };
  };

  let prev = readNode();
  if (!prev) return;
  if (prev._adv != null) { i += prev._adv; delete prev._adv; }
  _mmAddNode(g, prev);
  for (;;) {
    const e = readEdge();
    if (!e) break;
    const next = readNode();
    if (!next) break;                       // dangling edge → keep nodes so far
    if (next._adv != null) { i += next._adv; delete next._adv; }
    _mmAddNode(g, next);
    g.edges.push({ from: prev.id, to: next.id, style: e.style, label: e.label });
    prev = next;
  }
}

function _mmParseSeq(lines) {
  const seq = { type: 'sequence', participants: [], items: [], _byId: new Map() };
  const addP = (id, label, actor) => {
    let p = seq._byId.get(id);
    if (!p) {
      p = { id, label: label != null ? label : id, actor: !!actor };
      seq._byId.set(id, p);
      seq.participants.push(p);
    } else if (label != null) {
      p.label = label;
      if (actor) p.actor = true;
    }
    return p;
  };
  for (const ln of lines) {
    let m = ln.match(/^(participant|actor)\s+([A-Za-z0-9_]+)(?:\s+as\s+(.+))?$/i);
    if (m) { addP(m[2], m[3] ? m[3].trim() : null, /^actor$/i.test(m[1])); continue; }
    m = ln.match(/^([A-Za-z0-9_]+)\s*(-->>|->>|-->|->|--x|-x)\s*([A-Za-z0-9_]+)\s*:\s*(.*)$/);
    if (m) {
      addP(m[1]); addP(m[3]);
      seq.items.push({ kind: 'msg', from: m[1], to: m[3], text: m[4].trim(), dashed: m[2].startsWith('--') });
      continue;
    }
    m = ln.match(/^[Nn]ote\s+(over|left of|right of)\s+([A-Za-z0-9_]+(?:\s*,\s*[A-Za-z0-9_]+)?)\s*:\s*(.*)$/i);
    if (m) {
      const targets = m[2].split(/\s*,\s*/).filter(Boolean);
      for (const t of targets) addP(t);
      seq.items.push({ kind: 'note', pos: m[1].toLowerCase(), targets, text: m[3].trim() });
      continue;
    }
    // activate/deactivate/autonumber/loop/alt/else/opt/end/title/… → skip.
  }
  if (!seq.participants.length) return null;
  delete seq._byId;
  return seq;
}

// ── flowchart layout + render ──────────────────────────────────────────────

function _mmNodeDims(n) {
  const tw = _mmTextW(n.label);
  if (n.shape === 'circle') {
    const d = Math.max(tw + 18, 36);
    return { w: d, h: d };
  }
  if (n.shape === 'diamond') return { w: tw + 36, h: 42 };
  if (n.shape === 'flag') return { w: tw + 30, h: 30 };
  return { w: tw + 22, h: 30 };             // rect / rounded
}

/**
 * Layered layout: longest-path layering from roots, one barycenter ordering
 * pass, then center each layer on a shared cross axis. Returns positioned
 * nodes {x,y,w,h} (top-left) keyed by id, plus total {W,H}.
 */
function _mmLayoutFlow(g) {
  const N = g.nodes.length;
  const layer = new Map(g.nodes.map((n) => [n.id, 0]));
  // Longest-path relaxation, capped at N passes so cycles terminate.
  for (let pass = 0; pass < N; pass++) {
    let changed = false;
    for (const e of g.edges) {
      if (e.from === e.to) continue;
      const want = (layer.get(e.from) ?? 0) + 1;
      if (want <= N && (layer.get(e.to) ?? 0) < want) { layer.set(e.to, want); changed = true; }
    }
    if (!changed) break;
  }
  const maxLayer = Math.max(0, ...layer.values());
  const layers = [];                         // layer index → [node]
  for (let i = 0; i <= maxLayer; i++) layers.push([]);
  for (const n of g.nodes) layers[layer.get(n.id)].push(n);

  // One barycenter pass: order each layer by avg position of predecessors.
  const preds = new Map();
  for (const e of g.edges) {
    if (!preds.has(e.to)) preds.set(e.to, []);
    preds.get(e.to).push(e.from);
  }
  for (let li = 1; li <= maxLayer; li++) {
    const posInPrev = new Map(layers[li - 1].map((n, idx) => [n.id, idx]));
    const score = (n) => {
      const ps = (preds.get(n.id) || []).map((p) => posInPrev.get(p)).filter((v) => v != null);
      return ps.length ? ps.reduce((a, b) => a + b, 0) / ps.length : 1e9;
    };
    const scored = layers[li].map((n, idx) => ({ n, idx, s: score(n) }));
    scored.sort((a, b) => (a.s - b.s) || (a.idx - b.idx));
    layers[li] = scored.map((x) => x.n);
  }

  const horizontal = g.dir === 'LR' || g.dir === 'RL';
  const reversed = g.dir === 'BT' || g.dir === 'RL';
  const MAIN_GAP = 52, CROSS_GAP = 36, PAD = 14;

  // Main-axis extent of each layer band (max node main-dim in the layer).
  const dims = new Map(g.nodes.map((n) => [n.id, _mmNodeDims(n)]));
  const mainOf = (d) => (horizontal ? d.w : d.h);
  const crossOf = (d) => (horizontal ? d.h : d.w);
  const bandMain = layers.map((ns) => Math.max(0, ...ns.map((n) => mainOf(dims.get(n.id)))));
  const bandStart = [];
  let acc = PAD;
  for (let i = 0; i <= maxLayer; i++) { bandStart.push(acc); acc += bandMain[i] + MAIN_GAP; }
  const totalMain = acc - MAIN_GAP + PAD;

  // Cross extents: stack each layer's nodes, then center on the widest layer.
  const layerCross = layers.map((ns) =>
    ns.reduce((sum, n) => sum + crossOf(dims.get(n.id)), 0) + Math.max(0, ns.length - 1) * CROSS_GAP);
  const totalCross = Math.max(0, ...layerCross) + PAD * 2;

  const pos = new Map();
  for (let li = 0; li <= maxLayer; li++) {
    let c = (totalCross - layerCross[li]) / 2;
    for (const n of layers[li]) {
      const d = dims.get(n.id);
      let main = bandStart[li] + (bandMain[li] - mainOf(d)) / 2;
      if (reversed) main = totalMain - main - mainOf(d);
      pos.set(n.id, horizontal
        ? { x: main, y: c, w: d.w, h: d.h }
        : { x: c, y: main, w: d.w, h: d.h });
      c += crossOf(d) + CROSS_GAP;
    }
  }
  return {
    pos,
    W: horizontal ? totalMain : totalCross,
    H: horizontal ? totalCross : totalMain,
  };
}

function _mmShapeSvg(n, p) {
  const cx = p.x + p.w / 2, cy = p.y + p.h / 2;
  const attrs = 'fill="var(--panel, #161410)" stroke="var(--accent, #d97757)" stroke-width="1.3"';
  let shape;
  if (n.shape === 'circle') {
    shape = `<circle cx="${cx}" cy="${cy}" r="${p.w / 2}" ${attrs}/>`;
  } else if (n.shape === 'diamond') {
    shape = `<polygon points="${cx},${p.y} ${p.x + p.w},${cy} ${cx},${p.y + p.h} ${p.x},${cy}" ${attrs}/>`;
  } else if (n.shape === 'flag') {
    const notch = 9;
    shape = `<polygon points="${p.x},${p.y} ${p.x + p.w},${p.y} ${p.x + p.w},${p.y + p.h} ${p.x},${p.y + p.h} ${p.x + notch},${cy}" ${attrs}/>`;
  } else {
    const rx = n.shape === 'rounded' ? 14 : 4;
    shape = `<rect x="${p.x}" y="${p.y}" width="${p.w}" height="${p.h}" rx="${rx}" ${attrs}/>`;
  }
  const text = `<text x="${cx}" y="${cy}" text-anchor="middle" dominant-baseline="central" fill="var(--fg, #f2efe6)">${mmEsc(n.label)}</text>`;
  return `<g class="mm-node" data-mm-node="${mmEsc(n.id)}">${shape}${text}</g>`;
}

// Border anchor point of node box `p` facing toward (tx, ty). Axis-aligned:
// pick the side whose direction dominates.
function _mmAnchor(p, tx, ty) {
  const cx = p.x + p.w / 2, cy = p.y + p.h / 2;
  const dx = tx - cx, dy = ty - cy;
  if (Math.abs(dx) * p.h > Math.abs(dy) * p.w) {
    return { x: dx > 0 ? p.x + p.w : p.x, y: cy };
  }
  return { x: cx, y: dy > 0 ? p.y + p.h : p.y };
}

function _mmRenderFlow(g) {
  const { pos, W, H } = _mmLayoutFlow(g);
  const mid = `mm${++_mmIdSeq}`;
  const parts = [];
  parts.push(`<defs><marker id="${mid}-arr" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="var(--accent, #d97757)"/></marker></defs>`);
  for (const e of g.edges) {
    const a = pos.get(e.from), b = pos.get(e.to);
    if (!a || !b) continue;
    const bcx = b.x + b.w / 2, bcy = b.y + b.h / 2;
    const acx = a.x + a.w / 2, acy = a.y + a.h / 2;
    const p1 = _mmAnchor(a, bcx, bcy);
    const p2 = _mmAnchor(b, acx, acy);
    // Gentle elbow: cubic with control points pushed along the dominant axis.
    const dx = p2.x - p1.x, dy = p2.y - p1.y;
    const d = Math.abs(dy) >= Math.abs(dx)
      ? `M ${p1.x} ${p1.y} C ${p1.x} ${p1.y + dy / 2}, ${p2.x} ${p2.y - dy / 2}, ${p2.x} ${p2.y}`
      : `M ${p1.x} ${p1.y} C ${p1.x + dx / 2} ${p1.y}, ${p2.x - dx / 2} ${p2.y}, ${p2.x} ${p2.y}`;
    const dash = e.style === 'dotted' ? ' stroke-dasharray="4 3"' : '';
    const width = e.style === 'thick' ? 2.6 : 1.4;
    const marker = e.style === 'line' ? '' : ` marker-end="url(#${mid}-arr)"`;
    parts.push(`<path class="mm-edge" d="${d}" fill="none" stroke="var(--accent, #d97757)" stroke-width="${width}"${dash}${marker}/>`);
    if (e.label) {
      const lx = (p1.x + p2.x) / 2, ly = (p1.y + p2.y) / 2;
      const lw = _mmTextW(e.label) + 8;
      parts.push(`<g class="mm-edge-label"><rect x="${lx - lw / 2}" y="${ly - 9}" width="${lw}" height="18" rx="4" fill="var(--panel-alt, #1b1914)" opacity="0.92"/><text x="${lx}" y="${ly}" text-anchor="middle" dominant-baseline="central" fill="var(--muted, #a8a394)" font-size="11">${mmEsc(e.label)}</text></g>`);
    }
  }
  for (const n of g.nodes) parts.push(_mmShapeSvg(n, pos.get(n.id)));
  return _mmSvgRoot(W, H, parts.join(''));
}

// ── sequence layout + render ───────────────────────────────────────────────

function _mmRenderSeq(seq) {
  const mid = `mm${++_mmIdSeq}`;
  const PAD = 14, BOX_H = 30, GAP = 46, ROW = 36;
  // Participant box widths + x centers.
  let x = PAD;
  const cx = new Map();
  const boxes = [];
  for (const p of seq.participants) {
    const w = Math.max(_mmTextW(p.label) + 22, 64);
    boxes.push({ p, x, w });
    cx.set(p.id, x + w / 2);
    x += w + GAP;
  }
  const W = x - GAP + PAD;
  let y = PAD + BOX_H + 22;
  const parts = [];
  for (const it of seq.items) {
    if (it.kind === 'msg') {
      const x1 = cx.get(it.from), x2 = cx.get(it.to);
      if (x1 == null || x2 == null) continue;
      const dash = it.dashed ? ' stroke-dasharray="4 3"' : '';
      if (it.from === it.to) {
        // Self message: a small loop to the right.
        const d = `M ${x1} ${y} C ${x1 + 46} ${y - 4}, ${x1 + 46} ${y + 16}, ${x1 + 2} ${y + 14}`;
        parts.push(`<path class="mm-msg" d="${d}" fill="none" stroke="var(--accent, #d97757)" stroke-width="1.4"${dash} marker-end="url(#${mid}-arr)"/>`);
        parts.push(`<text x="${x1 + 52}" y="${y + 5}" fill="var(--fg, #f2efe6)" font-size="11.5">${mmEsc(it.text)}</text>`);
        y += ROW + 12;
      } else {
        parts.push(`<text x="${(x1 + x2) / 2}" y="${y - 6}" text-anchor="middle" fill="var(--fg, #f2efe6)" font-size="11.5">${mmEsc(it.text)}</text>`);
        parts.push(`<line class="mm-msg" x1="${x1}" y1="${y}" x2="${x2}" y2="${y}" stroke="var(--accent, #d97757)" stroke-width="1.4"${dash} marker-end="url(#${mid}-arr)"/>`);
        y += ROW;
      }
    } else if (it.kind === 'note') {
      const xs = it.targets.map((t) => cx.get(t)).filter((v) => v != null);
      if (!xs.length) continue;
      const w = Math.max(_mmTextW(it.text) + 18, 60);
      let nx;
      if (it.pos === 'left of') nx = Math.min(...xs) - w - 8;
      else if (it.pos === 'right of') nx = Math.max(...xs) + 8;
      else nx = (Math.min(...xs) + Math.max(...xs)) / 2 - w / 2;
      parts.push(`<g class="mm-note"><rect x="${nx}" y="${y - 12}" width="${w}" height="24" rx="3" fill="var(--panel-alt, #1b1914)" stroke="var(--border, #3a372f)"/><text x="${nx + w / 2}" y="${y}" text-anchor="middle" dominant-baseline="central" fill="var(--muted, #a8a394)" font-size="11">${mmEsc(it.text)}</text></g>`);
      y += ROW;
    }
  }
  const H = y + PAD;
  const head = [`<defs><marker id="${mid}-arr" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="var(--accent, #d97757)"/></marker></defs>`];
  for (const b of boxes) {
    const c = b.x + b.w / 2;
    head.push(`<line class="mm-lifeline" x1="${c}" y1="${PAD + BOX_H}" x2="${c}" y2="${H - PAD}" stroke="var(--border, #3a372f)" stroke-dasharray="4 4"/>`);
    head.push(`<g class="mm-participant"><rect x="${b.x}" y="${PAD}" width="${b.w}" height="${BOX_H}" rx="${b.p.actor ? 14 : 4}" fill="var(--panel, #161410)" stroke="var(--accent, #d97757)" stroke-width="1.3"/><text x="${c}" y="${PAD + BOX_H / 2}" text-anchor="middle" dominant-baseline="central" fill="var(--fg, #f2efe6)">${mmEsc(b.p.label)}</text></g>`);
  }
  return _mmSvgRoot(W, H, head.join('') + parts.join(''));
}

function _mmSvgRoot(W, H, inner) {
  const w = Math.max(40, Math.ceil(W)), h = Math.max(30, Math.ceil(H));
  return `<svg class="mm-svg" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${w} ${h}" width="${w}" height="${h}" font-family="ui-sans-serif, system-ui, sans-serif" font-size="12" role="img">${inner}</svg>`;
}

// ── public render API ──────────────────────────────────────────────────────

/** Render mermaid source → SVG string, or null (unknown type / nothing parsed). */
function mmRender(text, opts) {
  const model = mmParse(text, opts);
  if (!model) return null;
  try {
    if (model.type === 'flowchart') return _mmRenderFlow(model);
    if (model.type === 'sequence') return _mmRenderSeq(model);
  } catch {
    return null;
  }
  return null;
}

/** Render into an element. Returns true on success; leaves el untouched on failure. */
function mmRenderInto(el, text, opts) {
  if (!el) return false;
  const svg = mmRender(text, opts);
  if (!svg) return false;
  el.innerHTML = svg;
  return true;
}

// The agent-facing capability brief — the mermaid PLUGIN injects this into the
// system prompt so the model only emits markup this renderer can draw. It lives
// HERE so the brief can never drift from the parser above: extend the grammar,
// update this in the same commit.
const MM_CAPABILITIES = `## Diagrams (mermaid)
You may include diagrams in replies as fenced \`\`\`mermaid blocks — the UI renders them live while you stream (a built-in SUBSET renderer, NOT full mermaid).
CAN DO — flowcharts: \`graph TD\` or \`flowchart LR\` (directions TD/TB/LR/BT/RL); node shapes A[rect], B(rounded), C((circle)), D{diamond}, E>flag]; edges A --> B, A --- B, A -.-> B, A ==> B with optional labels A -->|label| B; chains A --> B --> C; several statements on one line separated by ';'.
CAN DO — sequence diagrams: \`sequenceDiagram\`; participant X / actor X (with \`as Label\`); messages A->>B: text and dashed replies A-->>B: text; Note over A,B: text / Note left of A: / Note right of A:.
CANNOT DO (never emit): subgraphs, style/classDef/linkStyle/click directives, gantt, gitGraph, pie, classDiagram, stateDiagram, erDiagram, mindmap, timeline. Unsupported lines are silently dropped and unknown diagram types fall back to a plain code block — so restructure the idea as a flowchart or sequence diagram instead, or use a table/code block.
Keep diagrams small and labeled: one idea per diagram, short node text.`;

// ====== plugins/mermaid.mjs ======
/**
 * `mermaid` plugin — no tools; its whole job is telling the agent EXACTLY what
 * the built-in mermaid-lite renderer can and cannot draw, so every ```mermaid
 * fence the model emits actually renders. The brief is imported from
 * mermaid-lite itself (single source of truth: extend the renderer's grammar
 * and the agent's instructions follow in the same commit).
 *
 * Always enabled — the renderer ships inside the UI, there is nothing on the
 * host to detect. Disabling this plugin stops TELLING the agent about diagrams
 * without touching the UI renderer (any mermaid it emits anyway still renders).
 */

// Named const, bare default export: the bundle's export-stripper turns
// `export default {…}` into a top-level BLOCK (parse error); `export default
// mermaidPlugin;` strips to a harmless expression (same pattern as wl.mjs).
const mermaidPlugin = {
  name: 'mermaid',
  description: 'In-chat diagrams via the built-in mermaid-lite subset renderer (prompt brief only, no tools)',
  trigger: async () => true,
  promptAddition: MM_CAPABILITIES,
  tools: [],
};
mermaidPlugin;

// ====== plugins/index.mjs ======
/**
 * Plugin registry + manager.
 *
 * Each plugin is a JS module exporting an object with:
 *   name           unique string id
 *   description    short summary for diagnostics
 *   trigger(exec)  async (executor) => bool  — pure detection, no side effects
 *   promptAddition string (or fn returning string) — appended to system prompt
 *                  while enabled
 *   tools          [{ verb, name, schema, impl }]  — same shape as built-in
 *                  tools in src/agent/tools.mjs; verb is what the model writes
 *                  in <verb .../> blocks, name is the registry key
 *
 * The manager re-runs every trigger at startup and every 5 minutes. State
 * changes (false→true / true→false) get logged. When the agent loop builds
 * a turn's prompt + tool dispatch, it asks the manager for the currently
 * enabled plugins' promptAdditions, tools, and verbs.
 *
 * Collisions (two plugins claiming the same tool name, or a plugin claiming
 * a built-in name) cause BOTH conflicting plugins to be disabled and a
 * prominent error logged to fileLog — the user resolves by editing or
 * removing the source file under src/plugins/ and rebuilding.
 */

// All built-in plugins. New plugins get added here. The docx tooling lives
// entirely in docx-edit (POI/Java) — read_docx, search_docx, and the edit
// suite. There is no pure-JS fallback: with no JRE, docx-edit disables itself
// and the missing-deps reason surfaces in the plugin status. mermaid is
// prompt-only (teaches the agent the renderer's subset; always on).
const PLUGINS = [wlPlugin, docxEditPlugin, mermaidPlugin];

const PLUGIN_REDETECT_MS = 5 * 60 * 1000;

function createPluginManager({ onLog } = {}) {
  const log = (m) => { try { onLog?.(m); } catch {} };
  // name → { enabled: bool, plugin: <object>, error: string|null }
  const state = new Map();

  // Executor passed to trigger fns. Runs a PowerShell command, returns
  // { stdout, stderr, exitCode, timedOut, durationMs }. Wraps spawn so
  // plugins never have to deal with child_process directly.
  const executor = {
    async run(command, { cwd, timeoutMs } = {}) {
      const t0 = Date.now();
      const timeout = Math.max(1000, Number(timeoutMs) || 30000);
      return new Promise((resolve) => {
        let stdout = '', stderr = '', timedOut = false, settled = false;
        const child = spawn(
          'powershell.exe',
          ['-NoProfile', '-NonInteractive', '-Command', command],
          { cwd: cwd ?? process.cwd(), windowsHide: true },
        );
        const tid = setTimeout(() => {
          timedOut = true;
          try { child.kill(); } catch {}
        }, timeout);
        child.stdout.on('data', (d) => { stdout += d.toString(); });
        child.stderr.on('data', (d) => { stderr += d.toString(); });
        const settle = (code) => {
          if (settled) return;
          settled = true;
          clearTimeout(tid);
          resolve({ stdout, stderr, exitCode: code ?? -1, timedOut, durationMs: Date.now() - t0 });
        };
        child.on('close', settle);
        child.on('error', (e) => { stderr += '\n' + e.message; settle(-1); });
      });
    },
  };

  async function detectAll() {
    for (const p of PLUGINS) {
      try {
        const enabled = await p.trigger(executor);
        const prev = state.get(p.name)?.enabled;
        state.set(p.name, { enabled, plugin: p, error: null });
        if (prev !== enabled) {
          log(`plugin ${p.name}: ${enabled ? 'enabled' : 'disabled'}`);
        }
      } catch (e) {
        const prev = state.get(p.name)?.enabled;
        state.set(p.name, { enabled: false, plugin: p, error: e.message });
        if (prev !== false) {
          log(`plugin ${p.name}: trigger error — ${e.message}`);
        }
      }
    }
    checkCollisions();
  }

  // Hard-error on tool-name collisions. Both conflicting plugins are disabled
  // until the user resolves by editing or removing source files under
  // src/plugins/. Built-in tool names always win — a plugin can never
  // shadow a built-in.
  function checkCollisions() {
    const builtinNames = new Set(Object.keys(tools));
    const owners = new Map();   // tool name → plugin name that owns it
    const conflicts = [];
    for (const s of state.values()) {
      if (!s.enabled) continue;
      for (const t of (s.plugin.tools || [])) {
        if (builtinNames.has(t.name)) {
          conflicts.push({ tool: t.name, owners: ['(built-in)', s.plugin.name] });
          continue;
        }
        if (owners.has(t.name)) {
          conflicts.push({ tool: t.name, owners: [owners.get(t.name), s.plugin.name] });
          continue;
        }
        owners.set(t.name, s.plugin.name);
      }
    }
    if (conflicts.length === 0) return;
    for (const c of conflicts) {
      for (const pluginName of c.owners) {
        if (pluginName === '(built-in)') continue;
        const s = state.get(pluginName);
        if (s) {
          s.enabled = false;
          s.error = `tool-name collision on "${c.tool}" with ${c.owners.filter((x) => x !== pluginName).join(', ')}`;
        }
      }
    }
    log('!!! PLUGIN TOOL NAME COLLISION — affected plugins are now disabled.');
    log('!!! Resolve by editing or removing the source file(s) under src/plugins/ and rebuilding:');
    for (const c of conflicts) {
      log(`!!!   "${c.tool}" claimed by: ${c.owners.join(' and ')}`);
    }
  }

  function enabledPlugins() {
    return [...state.values()].filter((s) => s.enabled).map((s) => s.plugin);
  }
  function enabledTools() {
    const out = {};
    for (const p of enabledPlugins()) {
      for (const t of (p.tools || [])) out[t.name] = t;
    }
    return out;
  }
  function enabledVerbs() {
    const out = {};   // verb → tool name
    for (const p of enabledPlugins()) {
      for (const t of (p.tools || [])) out[t.verb] = t.name;
    }
    return out;
  }
  function promptAdditions() {
    return enabledPlugins().map((p) => {
      const v = typeof p.promptAddition === 'function' ? p.promptAddition() : p.promptAddition;
      return v ? String(v).trim() : '';
    }).filter(Boolean);
  }
  function snapshot() {
    return [...state.values()].map(({ plugin, enabled, error }) => ({
      name: plugin.name,
      description: plugin.description,
      enabled,
      error: error || null,
      tools: (plugin.tools || []).map((t) => t.name),
    }));
  }

  return { detectAll, executor, enabledPlugins, enabledTools, enabledVerbs, promptAdditions, snapshot };
}

// ====== agent/mosaic-layout.mjs ======
/**
 * Mosaic layout — pure functions that size and pack tiles on a grid.
 *
 * Shared between the Node server (where layout-on-fetch is the fallback)
 * and the browser script (where the same code runs with measured font
 * metrics + actual viewport dimensions). The build script injects this
 * file's contents into both contexts; keep it pure (no I/O, no Node-only
 * APIs) so it can run unchanged in either.
 *
 * Three exports:
 *   - computeTileSize(tile, opts) → {cols, rows}
 *   - packPage(tiles, cols, rows, opts) → {placed, notPlaced, score, ...}
 *   - layoutMosaic(tiles, pageCols, pageRows, opts) → {pages, ...}
 */

/**
 * Compute the SMALLEST {cols, rows} rectangle that holds the tile's content
 * without scrolling. Per-kind heuristic: for text/json we count chars and
 * lines; for findings we account for the badge chrome; for tables we sum
 * column widths.
 *
 * The conversion to grid cells depends on opts.charsPerCol and
 * opts.linesPerRow, which the CALLER computes from the actual rendered
 * font + viewport. The server passes its baseline (assumes 1400×800 at
 * 9px monospace); the browser passes measured values.
 */
function computeTileSize(tile, opts = {}) {
  const charsPerCol = opts.charsPerCol ?? 20;
  const linesPerRow = opts.linesPerRow ?? 7;
  const minCols     = opts.minCols ?? 2;
  const minRows     = opts.minRows ?? 1;
  const maxCols     = opts.maxCols ?? 12;
  const maxRows     = opts.maxRows ?? 8;
  // Chrome budget in lines (header bar + body padding amortized as text lines).
  const chromeLines = opts.chromeLines ?? 2;

  const kind = tile?.kind ?? 'text';
  const body = tile?.body;
  let maxLine = 0;
  let lineCount = 1;

  if (kind === 'json') {
    let text;
    try { text = JSON.stringify(body, null, 2); }
    catch { text = String(body); }
    const lines = text.split('\n');
    lineCount = lines.length;
    for (const line of lines) if (line.length > maxLine) maxLine = line.length;
  } else if (kind === 'findings') {
    const findings = Array.isArray(body) ? body : [];
    lineCount = Math.max(1, findings.reduce((n, f) => n + (f?.detail ? 2 : 1), 0));
    let widest = 50;
    for (const f of findings) {
      const t = (f?.title?.length ?? 0) + 14;
      if (t > widest) widest = t;
      if (f?.detail) {
        const d = (String(f.detail).length ?? 0) + 4;
        if (d > widest) widest = d;
      }
    }
    maxLine = widest;
  } else if (kind === 'table') {
    const t = body ?? { columns: [], rows: [] };
    lineCount = (t.rows?.length ?? 0) + 1;
    const cols = t.columns ?? [];
    let width = 0;
    for (let i = 0; i < cols.length; i++) {
      let colW = String(cols[i] ?? '').length;
      for (const row of (t.rows ?? [])) {
        const cellStr = String(row[i] ?? '');
        if (cellStr.length > colW) colW = cellStr.length;
      }
      width += Math.min(colW, 40) + 2;
    }
    maxLine = Math.max(width, 30);
  } else {
    const text = String(body ?? '');
    const lines = text.split('\n');
    lineCount = lines.length;
    for (const line of lines) if (line.length > maxLine) maxLine = line.length;
  }

  if (maxLine === 0) maxLine = 10;

  let cols = Math.ceil(maxLine / charsPerCol);
  let rows = Math.ceil((lineCount + chromeLines) / linesPerRow);

  cols = Math.max(minCols, Math.min(maxCols, cols));
  rows = Math.max(minRows, Math.min(maxRows, rows));
  return { cols, rows };
}

/**
 * Pack a list of tiles onto a single page via backtracking search.
 * Score is the lexicographic pair (tileCount, cellsFilled) — placing
 * more tiles always wins; within equal tile counts the denser packing
 * wins. Time-capped via opts.maxMs (default 200ms).
 */
function packPage(tiles, cols, rows, opts = {}) {
  const maxMs = opts.maxMs ?? 200;
  // Defense-in-depth: the grid is allocated as rows*cols cells below, so a
  // non-positive / non-integer / runaway dimension would RangeError or OOM.
  // Callers should clamp (the /api/queries handler caps at 32); bound it here
  // too at 48 — past ~48x48 the anytime packer can't finish even one arrangement
  // within its time budget and would return an empty layout.
  cols = Math.max(1, Math.min(Math.trunc(Number(cols)) || 1, 48));
  rows = Math.max(1, Math.min(Math.trunc(Number(rows)) || 1, 48));
  const startMs = Date.now();
  const sized = tiles.map((t) => {
    const w = Math.max(1, Math.min(t.sizeHint?.cols ?? 3, cols));
    const h = Math.max(1, Math.min(t.sizeHint?.rows ?? 2, rows));
    return { tile: t, w, h, area: w * h };
  });

  const grid = Array.from({ length: rows }, () => new Array(cols).fill(-1));
  const placed = [];
  const usedIdx = new Set();
  let best = { placedIdx: [], placements: [], tileCount: -1, cellsFilled: -1 };

  const findEmpty = () => {
    for (let r = 0; r < rows; r++) {
      for (let c = 0; c < cols; c++) {
        if (grid[r][c] === -1) return [r, c];
      }
    }
    return null;
  };
  const fits = (r, c, w, h) => {
    if (r + h > rows || c + w > cols) return false;
    for (let i = 0; i < h; i++) for (let j = 0; j < w; j++) if (grid[r + i][c + j] !== -1) return false;
    return true;
  };
  const fill = (r, c, w, h, v) => {
    for (let i = 0; i < h; i++) for (let j = 0; j < w; j++) grid[r + i][c + j] = v;
  };
  const countFilled = () => {
    let n = 0; for (const row of grid) for (const v of row) if (v >= 0) n++; return n;
  };
  const isBetter = (tileCount, cellsFilled) =>
    tileCount > best.tileCount ||
    (tileCount === best.tileCount && cellsFilled > best.cellsFilled);

  function recurse() {
    if (Date.now() - startMs > maxMs) return;
    const empty = findEmpty();
    const tileCount = placed.length;
    if (!empty) {
      const cellsFilled = countFilled();
      if (isBetter(tileCount, cellsFilled)) {
        best = { placedIdx: [...usedIdx], placements: placed.map((p) => ({ ...p })), tileCount, cellsFilled };
      }
      return;
    }
    const [r, c] = empty;

    let emptyCount = 0;
    for (const row of grid) for (const v of row) if (v === -1) emptyCount++;
    let smallestArea = Infinity;
    for (let i = 0; i < sized.length; i++) {
      if (!usedIdx.has(i)) smallestArea = Math.min(smallestArea, sized[i].area);
    }
    const maxAdditionalTiles = smallestArea === Infinity ? 0 : Math.floor(emptyCount / smallestArea);
    if (tileCount + maxAdditionalTiles < best.tileCount) return;
    if (tileCount + maxAdditionalTiles === best.tileCount &&
        countFilled() + emptyCount <= best.cellsFilled) return;

    const candidates = sized
      .map((s, i) => ({ s, i }))
      .filter(({ i }) => !usedIdx.has(i))
      .sort((a, b) => b.s.area - a.s.area);

    let triedAny = false;
    for (const { s, i } of candidates) {
      if (fits(r, c, s.w, s.h)) {
        triedAny = true;
        fill(r, c, s.w, s.h, i);
        usedIdx.add(i);
        placed.push({ tileIdx: i, row: r, col: c, colSpan: s.w, rowSpan: s.h });
        recurse();
        placed.pop();
        usedIdx.delete(i);
        fill(r, c, s.w, s.h, -1);
        if (Date.now() - startMs > maxMs) return;
      }
    }
    if (!triedAny) {
      grid[r][c] = -2;
      recurse();
      grid[r][c] = -1;
    }
  }

  recurse();

  const placedIdxSet = new Set(best.placedIdx);
  const placedTiles = best.placements.map((p) => ({
    ...tiles[p.tileIdx],
    row: p.row,
    col: p.col,
    colSpan: p.colSpan,
    rowSpan: p.rowSpan,
  }));
  const notPlaced = tiles.filter((_, i) => !placedIdxSet.has(i));
  return {
    placed: placedTiles,
    notPlaced,
    tileCount: best.tileCount,
    cellsFilled: best.cellsFilled,
    score: best.cellsFilled,
  };
}

/**
 * Lay out tiles across one or more pages, packing each page tightly via
 * `packPage`. Tiles that don't fit overflow to the next page; an oversize
 * tile is force-placed on its own page.
 */
function layoutMosaic(tiles, pageCols = 12, pageRows = 8, opts = {}) {
  const pages = [];
  let remaining = tiles.slice();
  let safety = 30;
  while (remaining.length > 0 && safety-- > 0) {
    const r = packPage(remaining, pageCols, pageRows, opts);
    if (r.placed.length === 0) {
      const t = remaining.shift();
      pages.push({
        tiles: [{
          ...t, row: 0, col: 0,
          colSpan: Math.min(t.sizeHint?.cols ?? pageCols, pageCols),
          rowSpan: Math.min(t.sizeHint?.rows ?? pageRows, pageRows),
          oversize: true,
        }],
        fillRatio: 1.0,
      });
      continue;
    }
    pages.push({
      tiles: r.placed,
      fillRatio: r.score / (pageCols * pageRows),
    });
    remaining = r.notPlaced;
  }
  return { pages, totalTiles: tiles.length, totalPages: pages.length };
}

// ====== server.mjs ======
/**
 * Local HTTP server for the code_boss web UI. Binds to 127.0.0.1 only.
 *
 * Routes:
 *   GET  /                   serve the HTML page
 *   GET  /api/models         proxy to BASE_URL/models
 *   POST /api/chat           proxy chat completions, streams SSE back to browser
 *   GET  /api/snapshot       session state for the in-page snapshot modal
 *   POST /api/reset          clear the session log
 *   GET  /api/health         ping
 *
 * The server tracks every adapter transaction in `session.events`. The
 * snapshot endpoint returns these for client-side rendering.
 *
 * No npm deps. Node built-in http + the production HTTP adapter.
 */
// Inside the request handler `path` gets shadowed by `const path = url.pathname`,
// so callers in that scope use `nodePath` for the actual path module.
const nodePath = path;
// Imported so the bundler includes it; the `package` subcommand is dispatched
// from the bundle launcher (see scripts/build.mjs buildFooter), not here.
// Layout primitives live in mosaic-layout.mjs. Imported here so runQueries
// can call computeTileSize directly. Re-exported so tests + tools can
// import from server.mjs. In the production bundle, the build's
// import-stripper merges all JS_SOURCES into a single module scope, so
// the import is a no-op at runtime; this line exists for Node ESM resolution.

// User-home config dir. Normalized to .code_boss (underscore) to match the
// per-project state dir name. Legacy ~/.code-boss/ (hyphen) is migrated on
// first read so prior recent-projects history isn't lost. CB_HOME relocates
// the home (test isolation; the VBS launcher injects it into the node
// commands it starts) — unset, it falls back to the real home dir.
const USER_CONFIG_DIR  = path.join(process.env.CB_HOME || homedir(), '.code_boss');
const RECENT_FILE      = path.join(USER_CONFIG_DIR, 'recent.json');
const API_KEY_FILE     = path.join(USER_CONFIG_DIR, 'api-key.txt');
const GITLAB_CREDS_FILE = path.join(USER_CONFIG_DIR, 'gitlab-creds.json');
const LEGACY_USER_DIR  = path.join(process.env.CB_HOME || homedir(), '.code-boss');

// Always-responsive orchestrator (continuous-cognition). ON by default — a message that
// arrives while the foreground is busy is DELEGATED to a background sub-agent isolated in its
// OWN git worktree (no 409, no queue), so the agent you're talking to is always available.
// Requires a git repo (worktree isolation); a non-repo busy target still 409s. Set
// CB_ORCHESTRATOR=0 (or false/off/no) to fall back to the strict serial worker.
const CB_ORCHESTRATOR = !/^(0|false|off|no)$/i.test(process.env.CB_ORCHESTRATOR ?? '');
// Adversarial plan-review (#7): after a propose-first PLAN, read-only critics poke holes, then the
// source agent revises once before approval. ON by default; CB_PLAN_REVIEW=0 disables it.
const CB_PLAN_REVIEW = !/^(0|false|off|no)$/i.test(process.env.CB_PLAN_REVIEW ?? '');
// Adaptive cognition (the "make it smarter" mode): a message runs through the cognition
// scheduler (assess → policy → explore/decompose/recurse/worker/verify, budget-bounded) instead
// of the plain inline worker. Default off → production unchanged.
const CB_COGNITION = /^(1|true|on|yes)$/i.test(process.env.CB_COGNITION || '');
// Decomposition (one task → many parallel worktree sub-agents) is DISABLED by default: the E2E
// showed it drifts/over-engineers cohesive work and never beat the plain worker. It's gated off
// (not deleted) so it can be re-enabled for a future genuine-parallel-work experiment. With it
// off, cognition mode routes every non-answer task to the plain inline worker. The orchestrator's
// fork+delegate multitasking (CB_ORCHESTRATOR) is independent and unaffected.
const CB_DECOMPOSE = /^(1|true|on|yes)$/i.test(process.env.CB_DECOMPOSE || '');

async function migrateLegacyUserDir() {
  try {
    const newStat = await stat(USER_CONFIG_DIR).catch(() => null);
    if (newStat) return;                 // already on new path
    const oldStat = await stat(LEGACY_USER_DIR).catch(() => null);
    if (!oldStat?.isDirectory()) return; // no legacy dir to migrate
    await rename(LEGACY_USER_DIR, USER_CONFIG_DIR);
  } catch { /* best-effort */ }
}

async function loadApiKeyFromFile() {
  try {
    const text = await readFile(API_KEY_FILE, 'utf8');
    return text.trim();
  } catch { return null; }
}
async function saveApiKeyToFile(key) {
  await mkdir(USER_CONFIG_DIR, { recursive: true });
  await writeFile(API_KEY_FILE, String(key).trim() + '\n', 'utf8');
}

async function loadGitLabCredsFromFile() {
  try {
    const text = await readFile(GITLAB_CREDS_FILE, 'utf8');
    const data = JSON.parse(text);
    if (typeof data?.user === 'string' && typeof data?.token === 'string' && data.token) {
      return { user: data.user, token: data.token };
    }
    return null;
  } catch { return null; }
}
async function saveGitLabCredsToFile({ user, token }) {
  await mkdir(USER_CONFIG_DIR, { recursive: true });
  await writeFile(GITLAB_CREDS_FILE, JSON.stringify({ user, token }, null, 2) + '\n', 'utf8');
}
const MAX_RECENT = 12;

async function loadRecentProjects() {
  try {
    const text = await readFile(RECENT_FILE, 'utf8');
    const data = JSON.parse(text);
    return Array.isArray(data?.projects) ? data.projects.filter((p) => typeof p === 'string') : [];
  } catch { return []; }
}
async function saveRecentProjects(projects) {
  try {
    await mkdir(path.dirname(RECENT_FILE), { recursive: true });
    await writeFile(RECENT_FILE, JSON.stringify({ projects }, null, 2), 'utf8');
  } catch (e) { console.error('[recent] save failed:', e.message); }
}

// ── Query catalog (programmable mosaic backend) ─────────────────────────────
//
// Each query is a pure function from the diagnostics object to a tile result.
// IDs are STABLE across builds so we can refer to them by number across
// chat sessions ("activate 7, 12, 50"). Numbers are sparse-by-category so
// new queries can be added without renumbering:
//
//   1-9    prompt (what we sent to the model)
//   10-19  response (what came back)
//   20-29  findings (synthesized analysis)
//   30-39  server state (stderr, log file, uptime, memory)
//   40-49  configuration (project, API, build)
//   50-59  transactions (request/response bodies, table, latency)
//   60-69  chat events (user/assistant turns)
//   70-79  trends / comparisons (over multiple turns)
//   90-99  meta / reserved
//
// Size hints map to a 12-col × 8-row page grid:
//   small  = 3×2   wide   = 6×2   tall   = 3×6
//   medium = 4×3   large  = 6×4   huge   = 12×4
// Tiles can request any {cols, rows} though — these are just conventions.

// ─── Catalog helpers (used by compute fns) ───────────────────────────────────
function _lastChatTx(diag) {
  const txs = (diag?.transactions?.events ?? []).filter((e) => e.type === 'chat_completions');
  return txs[txs.length - 1] ?? null;
}
function _allChatTxs(diag) {
  return (diag?.transactions?.events ?? []).filter((e) => e.type === 'chat_completions');
}
function _lastUserMessage(msgs) {
  if (!Array.isArray(msgs)) return null;
  for (let i = msgs.length - 1; i >= 0; i--) {
    if (msgs[i]?.role === 'user') return msgs[i];
  }
  return null;
}
function _stripReminder(content) {
  // Return the user's actual question with the <system-reminder>...</system-reminder>
  // block removed, so we can show "the user's real ask" separately from the
  // injected instructions. Falls back to original content if no reminder.
  const s = String(content ?? '');
  return s.replace(/<system-reminder>[\s\S]*?<\/system-reminder>\s*/i, '').trim();
}
function _reminderText(content) {
  const m = String(content ?? '').match(/<system-reminder>([\s\S]*?)<\/system-reminder>/i);
  return m ? m[1].trim() : null;
}
function _fmtBytes(n) {
  if (n == null) return '?';
  if (n < 1024) return n + ' B';
  if (n < 1024 * 1024) return (n / 1024).toFixed(1) + ' KB';
  return (n / 1024 / 1024).toFixed(2) + ' MB';
}
function _fmtMs(ms) {
  if (ms == null) return '—';
  if (ms < 1000) return ms + ' ms';
  if (ms < 60_000) return (ms / 1000).toFixed(1) + ' s';
  if (ms < 3_600_000) return (ms / 60_000).toFixed(1) + ' min';
  return (ms / 3_600_000).toFixed(2) + ' hr';
}

const QUERY_CATALOG = [
  // ─── 1-9: PROMPT (what we sent to the model) ────────────────────────────
  {
    id: 1, name: 'System reminder check', category: 'prompt',
    description: 'Whether <system-reminder> is at the start of the latest user message.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      if (!tx) return { kind: 'text', severity: 'info', body: 'no chat request yet' };
      const last = _lastUserMessage(tx.request?.messages);
      if (!last) return { kind: 'text', severity: 'warn', body: 'no user message in last request' };
      const c = String(last.content ?? '');
      if (!c.includes('<system-reminder>')) return { kind: 'text', severity: 'critical', body: 'MISSING from latest user message' };
      if (c.trimStart().startsWith('<system-reminder>')) return { kind: 'text', severity: 'ok', body: 'present at start of latest user message' };
      return { kind: 'text', severity: 'warn', body: 'present but NOT at start' };
    },
  },
  {
    id: 2, name: 'System reminder text', category: 'prompt',
    description: 'Full text of the <system-reminder> block we sent on the last request.',
    sizeHint: { cols: 4, rows: 6 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const last = _lastUserMessage(tx?.request?.messages);
      const r = _reminderText(last?.content);
      if (!r) return { kind: 'text', severity: 'warn', body: '(no <system-reminder> found in latest user message)' };
      return { kind: 'text', body: r };
    },
  },
  {
    id: 3, name: 'System reminder size', category: 'prompt',
    description: 'Character count of the <system-reminder> we sent.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const last = _lastUserMessage(tx?.request?.messages);
      const r = _reminderText(last?.content);
      if (r == null) return { kind: 'text', severity: 'warn', body: 'no reminder found' };
      return { kind: 'text', body: `${r.length} chars  (~${Math.ceil(r.length / 4)} tokens)` };
    },
  },
  {
    id: 4, name: 'Latest user question (clean)', category: 'prompt',
    description: 'The latest user message with the <system-reminder> stripped — just what the user actually asked.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const last = _lastUserMessage(tx?.request?.messages);
      if (!last) return { kind: 'text', severity: 'info', body: '(no user message)' };
      const clean = _stripReminder(last.content);
      return { kind: 'text', body: clean || '(empty after stripping reminder)' };
    },
  },
  {
    id: 5, name: 'Latest user message (raw)', category: 'prompt',
    description: 'Full raw content of the latest user message including any injected <system-reminder>.',
    sizeHint: { cols: 4, rows: 6 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const last = _lastUserMessage(tx?.request?.messages);
      if (!last) return { kind: 'text', severity: 'info', body: '(no user message)' };
      return { kind: 'text', body: String(last.content ?? '') };
    },
  },
  {
    id: 6, name: 'Message role sequence', category: 'prompt',
    description: 'U/A/S sequence for the conversation we sent (checks for missing alternation, double-user, etc.).',
    sizeHint: { cols: 6, rows: 2 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const msgs = tx?.request?.messages ?? [];
      if (msgs.length === 0) return { kind: 'text', severity: 'info', body: '(no messages)' };
      const seq = msgs.map((m) => ({ user: 'U', assistant: 'A', system: 'S', tool: 'T' }[m.role] ?? '?')).join(' ');
      return { kind: 'text', body: `${msgs.length} msgs:  ${seq}` };
    },
  },
  {
    id: 7, name: 'Conversation size', category: 'prompt',
    description: 'Message count + estimated token budget for the prompt.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const msgs = tx?.request?.messages ?? [];
      const totalChars = msgs.reduce((s, m) => s + String(m.content ?? '').length, 0);
      return { kind: 'text', body: `${msgs.length} msgs · ${totalChars} chars · ~${Math.ceil(totalChars / 4)} tokens` };
    },
  },
  {
    id: 8, name: 'All user messages this session', category: 'prompt',
    description: 'Every user-message chat event in the current session, in order.',
    sizeHint: { cols: 4, rows: 6 },
    compute: (d) => {
      const ums = (d.chatEvents ?? []).filter((e) => e.type === 'user-message');
      if (!ums.length) return { kind: 'text', severity: 'info', body: '(no user messages)' };
      const body = ums.map((e, i) => `[${i + 1}] ${String(e.content ?? '').replace(/\s+/g, ' ').slice(0, 200)}`).join('\n\n');
      return { kind: 'text', body };
    },
  },

  {
    id: 9, name: 'Anthropic message-shape check', category: 'prompt',
    description: 'Validates the last request against Anthropic Messages rules: ends with a user message, no two same-role messages in a row, and no system message INSIDE the array (Anthropic takes system as a separate top-level param). Critical on the Anthropic backend, advisory otherwise.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const msgs = tx?.request?.messages;
      if (!Array.isArray(msgs) || !msgs.length) return { kind: 'text', severity: 'info', body: '(no request to validate)' };
      const issues = [];
      const last = msgs[msgs.length - 1];
      if (last?.role !== 'user') issues.push(`last message is role=${last?.role} (Anthropic requires the final message to be user)`);
      for (let i = 1; i < msgs.length; i++) {
        if (msgs[i].role === msgs[i - 1].role) issues.push(`two "${msgs[i].role}" messages in a row at index ${i - 1}-${i}`);
      }
      const sysInArray = msgs.filter((m) => m.role === 'system').length;
      if (sysInArray) issues.push(`${sysInArray} system message(s) in the array (Anthropic takes system separately)`);
      const fmt = d.config?.apiFormat ?? '?';
      if (!issues.length) return { kind: 'text', severity: 'ok', body: `OK (backend=${fmt}) — ${msgs.length} msgs, ends with user, no same-role runs` };
      return { kind: 'text', severity: fmt === 'anthropic' ? 'critical' : 'warn', body: `backend=${fmt}\n` + issues.join('\n') };
    },
  },

  // ─── 10-19: RESPONSE (what came back) ───────────────────────────────────
  {
    id: 10, name: 'Last response (full text)', category: 'response',
    description: 'The complete assistant content from the most recent chat_completions response.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const c = tx?.response?.choices?.[0]?.message?.content;
      if (c == null) return { kind: 'text', severity: 'info', body: '(no response yet)' };
      return { kind: 'text', body: String(c) };
    },
  },
  {
    id: 11, name: 'Last response head', category: 'response',
    description: 'First 500 characters of the last assistant response (where identity drift usually shows up).',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const c = String(tx?.response?.choices?.[0]?.message?.content ?? '');
      if (!c) return { kind: 'text', severity: 'info', body: '(no response)' };
      return { kind: 'text', body: c.slice(0, 500) + (c.length > 500 ? '\n…[truncated]' : '') };
    },
  },
  {
    id: 12, name: 'finish_reason history', category: 'response',
    description: 'finish_reason for the last N chat_completions responses.',
    sizeHint: { cols: 6, rows: 2 },
    compute: (d) => {
      const txs = _allChatTxs(d);
      if (!txs.length) return { kind: 'text', severity: 'info', body: '(no chat responses)' };
      const rows = txs.slice(-10).map((t, i) => `#${txs.length - txs.slice(-10).length + i + 1}  ${t?.response?.choices?.[0]?.finish_reason ?? '?'}`);
      return { kind: 'text', body: rows.join('\n') };
    },
  },
  {
    id: 13, name: 'Identity drift detection', category: 'response',
    description: 'Whether the last response matches Gemini Enterprise built-in phrasing.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const patterns = [
        /\bI am Gemini Enterprise\b/i, /\bI(?:'| a)m Gemini(?:\b|,|\.)/i,
        /\bMemory Bank agent\b/i, /\bI can help you with (?:Jira|Drive|Calendar|Gmail|Confluence)/i,
        /\bGenerate Memories\b/i, /\bI am a large language model\b/i,
        /\bI do not have direct access to your remote or local file system\b/i,
      ];
      const txs = _allChatTxs(d).slice(-5);
      const hits = [];
      for (const tx of txs) {
        const c = String(tx?.response?.choices?.[0]?.message?.content ?? '');
        for (const p of patterns) if (p.test(c)) { hits.push(p.source); break; }
      }
      if (!txs.length) return { kind: 'text', severity: 'info', body: '(no responses to scan)' };
      if (!hits.length) return { kind: 'text', severity: 'ok', body: `clean across last ${txs.length} response(s)` };
      return { kind: 'text', severity: 'warn', body: `DRIFT in ${hits.length}/${txs.length} responses:\n` + hits.join('\n') };
    },
  },
  {
    id: 14, name: 'Tool tags in last response', category: 'response',
    description: 'Which code_boss tool tags (list/read/write/etc.) appeared in the last assistant response.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const c = String(tx?.response?.choices?.[0]?.message?.content ?? '');
      if (!c) return { kind: 'text', severity: 'info', body: '(no response)' };
      const tags = ['list', 'read', 'write', 'edit', 'glob', 'grep', 'powershell', 'bash', 'next-task', 'task-update', 'task-add', 'remote-workspace'];
      const found = tags.filter((t) => new RegExp(`<${t}[\\s>/]`, 'i').test(c));
      if (!found.length) return { kind: 'text', severity: 'warn', body: 'NO tool tags found — model just chatted' };
      return { kind: 'text', severity: 'ok', body: 'found: ' + found.join(', ') };
    },
  },
  {
    id: 15, name: 'Token usage (last)', category: 'response',
    description: 'Token counts from the last chat_completions response.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const u = tx?.response?.usage;
      if (!u) return { kind: 'text', severity: 'info', body: '(no usage)' };
      return { kind: 'text', body: `prompt: ${u.prompt_tokens ?? '?'}\nout:    ${u.completion_tokens ?? '?'}\ntotal:  ${u.total_tokens ?? '?'}` };
    },
  },
  {
    id: 16, name: 'Response length trend', category: 'response',
    description: 'Character counts of the last 10 assistant responses.',
    sizeHint: { cols: 6, rows: 2 },
    compute: (d) => {
      const txs = _allChatTxs(d).slice(-10);
      if (!txs.length) return { kind: 'text', severity: 'info', body: '(no responses)' };
      const lens = txs.map((t, i) => `#${i + 1}: ${String(t?.response?.choices?.[0]?.message?.content ?? '').length} chars`);
      return { kind: 'text', body: lens.join('\n') };
    },
  },

  {
    id: 17, name: 'Empty / no-tool response', category: 'response',
    description: 'Flags when the last response was empty, or carried NO tool tags (the model just chatted instead of acting through the <remote-workspace> protocol).',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      if (!tx) return { kind: 'text', severity: 'info', body: '(no response yet)' };
      const c = String(tx?.response?.choices?.[0]?.message?.content ?? '');
      if (!c.trim()) return { kind: 'text', severity: 'critical', body: 'EMPTY response — model returned no content (proxy may have dropped the body, or it was content-filtered)' };
      const hasTag = /<(remote-workspace|list|read|write|edit|glob|grep|powershell|next-task)[\s>/]/i.test(c);
      if (hasTag) return { kind: 'text', severity: 'ok', body: `response carried tool tags (${c.length} chars)` };
      return { kind: 'text', severity: 'warn', body: `no tool tags in a ${c.length}-char reply — model answered in prose instead of acting` };
    },
  },
  {
    id: 18, name: 'Protocol wrapping check', category: 'response',
    description: 'Detects tool tags wrapped in markdown ``` fences or HTML-escaped (&lt;) — a common reason the workspace parser sees no tags even though the model "wrote" them. Points at a proxy that reformats output.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const c = String(tx?.response?.choices?.[0]?.message?.content ?? '');
      if (!c) return { kind: 'text', severity: 'info', body: '(no response)' };
      const issues = [];
      if (/```[\s\S]*<(remote-workspace|read|write|edit|list|powershell|next-task)[\s>/]/i.test(c)) issues.push('tool tags appear INSIDE a ``` code fence — the parser ignores fenced content');
      if (/&lt;(remote-workspace|read|write|edit|list|powershell|next-task)\b/i.test(c)) issues.push('tool tags are HTML-escaped (&lt;...) — the proxy is entity-encoding model output');
      if (!issues.length) return { kind: 'text', severity: 'ok', body: 'no fence/escape wrapping detected' };
      return { kind: 'text', severity: 'warn', body: issues.join('\n') };
    },
  },
  {
    id: 19, name: 'Streaming mode (recent calls)', category: 'response',
    description: 'Whether each recent chat call returned streamed or non-streamed, with duration. Some proxies break SSE streaming and hang or silently truncate.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const txs = _allChatTxs(d).slice(-10);
      if (!txs.length) return { kind: 'text', severity: 'info', body: '(no calls)' };
      const rows = txs.map((t, i) => `#${i + 1}  ${t.streamed ? 'streamed' : 'non-stream'}  ${_fmtMs(t.durationMs ?? null)}`);
      return { kind: 'text', body: rows.join('\n') };
    },
  },

  // ─── 20-29: FINDINGS ────────────────────────────────────────────────────
  {
    id: 20, name: 'All findings', category: 'findings',
    description: 'Full color-coded list of automated diagnostic findings.',
    sizeHint: { cols: 4, rows: 6 },
    compute: (d) => ({ kind: 'findings', body: d.findings ?? [] }),
  },
  {
    id: 21, name: 'Critical findings only', category: 'findings',
    description: 'Just the critical/warn findings, filtered.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const f = (d.findings ?? []).filter((x) => x.severity === 'critical' || x.severity === 'warn');
      if (!f.length) return { kind: 'text', severity: 'ok', body: 'no critical or warn findings' };
      return { kind: 'findings', body: f };
    },
  },
  {
    id: 24, name: 'Context window fill', category: 'findings',
    description: 'Current prompt size vs the total context budget (the last per-turn gauge). At/over 100% means a large OPEN task is being dragged to the model every turn — the runaway-context failure (a model that never emits <next-task>) that caused slow turns + 502s. The open task is sliding-window elided to bound it, but persistent fullness is the signal to close/auto-close tasks.',
    sizeHint: { cols: 4, rows: 2 },
    compute: (d) => {
      const c = d.context;
      if (!c) return { kind: 'text', severity: 'info', body: '(no turn has run yet)' };
      const pct = c.budgetPct ?? (c.budgetTokens ? Math.round((c.totalTokens / c.budgetTokens) * 100) : 0);
      const sev = pct >= 100 ? 'critical' : pct >= 90 ? 'warn' : 'ok';
      const age = c.at ? `\nas of ${new Date(c.at).toISOString().slice(11, 19)}Z` : '';
      return { kind: 'text', severity: sev, body: `${c.totalTokens} / ${c.budgetTokens} tokens  (${pct}% of budget)${age}` };
    },
  },
  {
    id: 22, name: 'Findings summary', category: 'findings',
    description: 'Count of findings by severity — the one-glance health number.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const f = d.findings ?? [];
      const by = (s) => f.filter((x) => x.severity === s).length;
      const crit = by('critical'), warn = by('warn');
      return { kind: 'text', severity: crit ? 'critical' : warn ? 'warn' : 'ok',
        body: `critical: ${crit}\nwarn:     ${warn}\ninfo:     ${by('info')}\nok:       ${by('ok')}` };
    },
  },
  {
    id: 25, name: 'Task store breakdown', category: 'findings',
    description: 'The task-memory store as of the last turn: the OPEN task size vs the per-task auto-close ceiling (a runaway open task dragged every turn is the slow-turn/502 failure), how many tokens of FINISHED tasks are still in full context, and per-tier task counts (t1 full messages, t2 masked results, t3 summary-only, t4 rollup). Emitted by the agent each turn (the server cannot read the on-disk task store without blocking the event loop).',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const t = d.tasks;
      if (!t) return { kind: 'text', severity: 'info', body: '(no turn has run yet)' };
      const ceil = t.perTaskCeiling || 0;
      const cur = t.currentTokens || 0;
      const pct = ceil ? Math.round((cur / ceil) * 100) : 0;
      const sev = ceil && cur >= ceil ? 'critical' : ceil && cur >= ceil * 0.75 ? 'warn' : 'ok';
      const tc = t.tierCounts || {};
      const age = t.at ? `\nas of ${new Date(t.at).toISOString().slice(11, 19)}Z` : '';
      return { kind: 'text', severity: sev, body:
        `open task ${t.currentId || '?'}: ${cur} tok` + (ceil ? ` (${pct}% of ${ceil} ceiling)` : '') + '\n' +
        `finished-in-context: ${t.demotableTokens || 0} tok\n` +
        `tasks: ${t.totalTasks || 0}  [t1:${tc[1] || 0} t2:${tc[2] || 0} t3:${tc[3] || 0} t4:${tc[4] || 0}]` + age };
    },
  },

  // ─── 30-39: SERVER STATE ────────────────────────────────────────────────
  {
    id: 30, name: 'STDERR errors only', category: 'server',
    description: 'Lines from the stderr ring buffer matching known error patterns.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const pats = [/\bReferenceError\b/, /\bTypeError\b/, /\bSyntaxError\b/, /is not defined/, /\bENOENT\b/, /\bEACCES\b/, /\bUncaught\b/, /FAILED/];
      const errs = (d.stderr ?? []).filter((e) => pats.some((p) => p.test(e.line ?? '')));
      if (!errs.length) return { kind: 'text', severity: 'ok', body: '(no error patterns in stderr)' };
      return { kind: 'text', severity: 'critical', body: errs.map((e) => `${e.at?.slice(11, 23) ?? ''}  ${e.line}`).join('\n') };
    },
  },
  {
    id: 31, name: 'STDERR tail (all)', category: 'server',
    description: 'Last ~30 lines of the stderr ring buffer.',
    sizeHint: { cols: 4, rows: 6 },
    compute: (d) => {
      const ls = (d.stderr ?? []).slice(-30);
      if (!ls.length) return { kind: 'text', severity: 'info', body: '(empty)' };
      return { kind: 'text', body: ls.map((e) => `${e.at?.slice(11, 23) ?? ''}  ${e.line}`).join('\n') };
    },
  },
  {
    id: 32, name: 'Log file tail', category: 'server',
    description: 'Tail of <project>/.code_boss/code-boss.log.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const t = d.logTail;
      if (!t) return { kind: 'text', severity: 'info', body: '(no log content)' };
      return { kind: 'text', body: t };
    },
  },
  {
    id: 33, name: 'Build + uptime + memory', category: 'server',
    description: 'Build version, server uptime, memory usage.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const s = d.server ?? {}, b = d.build ?? {}, m = s.memory ?? {};
      return { kind: 'text', body: [
        `build:   ${b.version ?? '?'}`,
        `started: ${b.startedAt ?? '?'}`,
        `uptime:  ${_fmtMs(s.uptimeMs)}`,
        `mem rss: ${_fmtBytes(m.rss)}`,
        `mem heap: ${_fmtBytes(m.heapUsed)} / ${_fmtBytes(m.heapTotal)}`,
      ].join('\n') };
    },
  },
  {
    id: 34, name: 'PID / Node / platform', category: 'server',
    description: 'Process identifiers — useful when diagnosing zombies or restarts.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const s = d.server ?? {};
      return { kind: 'text', body: `pid:      ${s.pid}\nnode:     ${s.nodeVersion}\nplatform: ${s.platform}` };
    },
  },
  {
    id: 35, name: 'Chat session state', category: 'server',
    description: 'Current chat status, phase, version, subscribers, tokens.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const c = d.chat ?? {};
      return { kind: 'text', body: [
        `status:   ${c.status}`,
        `phase:    ${c.phase}`,
        `version:  ${c.version}`,
        `tokens:   up=${c.tokens?.up ?? 0}  down=${c.tokens?.down ?? 0}`,
        `events:   ${c.eventCount}`,
        `sse subs: ${c.streamSubscribers ?? 0}`,
        `running:  ${c.chatStartedAt ? _fmtMs(c.chatStartedAgoMs) : 'no'}`,
        `error:    ${c.error ?? '—'}`,
      ].join('\n') };
    },
  },

  {
    id: 36, name: 'Last LLM/proxy error', category: 'server',
    description: 'The most recent error transaction — a failed model call (the HttpError message carries the HTTP status + body slice from the proxy), a timeout, or a thrown adapter error. THE first tile to pull when responses stop coming back.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const errs = (d.transactions?.events ?? []).filter((e) => e.type === 'error');
      if (!errs.length) return { kind: 'text', severity: 'ok', body: '(no error transactions recorded)' };
      const last = errs[errs.length - 1];
      return { kind: 'text', severity: 'critical', body: `+${last.at}ms\n${last.message ?? last.summary ?? '(no message)'}` };
    },
  },
  {
    id: 37, name: 'LLM error history', category: 'server',
    description: 'Every error transaction this session — the failed-call log. Repeated 401/429/5xx here points squarely at the proxy rather than code_boss.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const errs = (d.transactions?.events ?? []).filter((e) => e.type === 'error');
      if (!errs.length) return { kind: 'text', severity: 'ok', body: '(no errors)' };
      const body = errs.slice(-15).map((e) => `+${e.at}ms  ${String(e.message ?? e.summary ?? '').slice(0, 160)}`).join('\n');
      return { kind: 'text', severity: 'critical', body };
    },
  },
  {
    id: 38, name: 'Current chat error', category: 'server',
    description: 'session.error — the message shown when the last run ended in error (LLM failure, timeout, project/git problem). "(no current error)" when the last run was clean.',
    sizeHint: { cols: 6, rows: 2 },
    compute: (d) => {
      const err = d.chat?.error;
      if (!err) return { kind: 'text', severity: 'ok', body: '(no current error)' };
      return { kind: 'text', severity: 'critical', body: String(err) };
    },
  },
  {
    id: 39, name: 'Project context loaded', category: 'server',
    description: 'AGENTS.md / context files discovered for the active project, with sizes. Empty while a project is open means the worker has NO human-authored guidance — a likely cause of off-target behavior.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      if (!d.project?.activeProject) return { kind: 'text', severity: 'info', body: '(no project open)' };
      const files = d.project?.contextFiles ?? [];
      if (!files.length) return { kind: 'text', severity: 'warn', body: 'no AGENTS.md / context files found in this project' };
      const body = files.map((f) => `${f.relDir && f.relDir !== '.' ? f.relDir + '/' : ''}${f.name}  ${_fmtBytes(f.bytes)}`).join('\n') + `\n— total ${d.project.contextChars ?? '?'} chars`;
      return { kind: 'text', severity: 'ok', body };
    },
  },

  // ─── 40-49: CONFIG ──────────────────────────────────────────────────────
  {
    id: 40, name: 'Active project', category: 'config',
    description: 'The project directory code_boss is operating on.',
    sizeHint: { cols: 6, rows: 2 },
    compute: (d) => ({ kind: 'text', body: d.project?.activeProject ?? '(none)' }),
  },
  {
    id: 41, name: 'API config', category: 'config',
    description: 'Base URL, model, and key prefix.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const c = d.config ?? {};
      return { kind: 'text', body: [
        `base:  ${c.baseURL ?? '?'}`,
        `model: ${c.defaultModel ?? '?'}`,
        `key:   ${c.apiKeyPrefix ?? '(none)'}`,
      ].join('\n') };
    },
  },
  {
    id: 42, name: 'Recent projects', category: 'config',
    description: 'Recently opened project paths.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const r = d.project?.recent ?? [];
      if (!r.length) return { kind: 'text', body: '(none)' };
      return { kind: 'text', body: r.join('\n') };
    },
  },

  {
    id: 43, name: 'Backend / adapter', category: 'config',
    description: 'Which API format the adapter speaks (anthropic vs openai), the base URL + model, and whether a custom dev/test adapter is in use instead of the live HTTP one. The first thing to confirm when responses look wrong.',
    sizeHint: { cols: 6, rows: 2 },
    compute: (d) => {
      const c = d.config ?? {};
      return { kind: 'text', body: [
        `format:   ${c.apiFormat ?? '?'}`,
        `base:     ${c.baseURL ?? '?'}`,
        `model:    ${c.defaultModel ?? '?'}`,
        `adapter:  ${c.usingProvidedAdapter ? 'custom (dev/test)' : 'live HTTP'}`,
      ].join('\n') };
    },
  },

  // ─── 44-47: AUTH (API key + 401 unlock state) ─────────────────────────
  {
    id: 44, name: 'API key state', category: 'config',
    description: 'Whether the server has a key loaded, and its prefix.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const a = d.auth ?? {};
      if (!a.hasKey) return { kind: 'text', severity: 'critical', body: 'NO KEY — first-run modal should be open' };
      return { kind: 'text', severity: 'ok', body: 'hasKey=YES  prefix=' + (a.keyPrefix ?? '?') };
    },
  },
  {
    id: 45, name: 'Pending unlock', category: 'config',
    description: 'If non-empty, an LLM call returned 401 with an unlock_url and the adapter is waiting for the browser modal\'s "I\'ve unlocked" POST. Every queued resolver counts a hung LLM call.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const a = d.auth ?? {};
      if (!a.pendingUnlockUrl) return { kind: 'text', severity: 'ok', body: '(none — no unlock in flight)' };
      return {
        kind: 'text', severity: 'critical',
        body: 'BLOCKED on unlock — ' + a.pendingUnlockResolverCount + ' LLM call(s) waiting\nurl: ' + a.pendingUnlockUrl,
      };
    },
  },
  {
    id: 46, name: 'SSE subscribers', category: 'config',
    description: 'How many EventSource clients are currently connected. If 0 while an unlock-needed broadcast fires, the modal won\'t appear in any tab.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const n = d.chat?.streamSubscribers ?? 0;
      if (n === 0) return { kind: 'text', severity: 'warn', body: '0 subscribers — page not connected to SSE' };
      return { kind: 'text', severity: 'ok', body: n + ' subscriber(s)' };
    },
  },
  {
    id: 47, name: 'Auth events (recent)', category: 'config',
    description: 'unlock + api-key + project-opened entries from session.events (most recent at the bottom).',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.transactions?.events ?? []).filter((e) =>
        e.type === 'unlock' || e.type === 'api-key' || e.type === 'project-opened'
      );
      if (!evs.length) return { kind: 'text', severity: 'info', body: '(none)' };
      const body = evs.map((e) => `+${e.at}ms  ${e.type}  ${e.summary ?? ''}`).join('\n');
      return { kind: 'text', body };
    },
  },

  {
    id: 48, name: 'Plugins detected', category: 'config',
    description: 'Which optional tool plugins lit up (detected by probing for the underlying CLI on PATH). Shows whether e.g. the docx / WebLogic helpers are active, with the reason a plugin stayed off.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const ps = d.plugins ?? [];
      if (!ps.length) return { kind: 'text', severity: 'info', body: '(no plugins registered)' };
      const body = ps.map((p) => `${p.enabled ? '[on] ' : '[off]'} ${p.name ?? '?'}` + (p.error ? `  — ${String(p.error).slice(0, 80)}` : (p.enabled && p.tools?.length ? `  (${p.tools.join(', ')})` : ''))).join('\n');
      return { kind: 'text', body };
    },
  },
  {
    id: 49, name: 'Git workspace state', category: 'config',
    description: 'Each tracked repo: branch, short HEAD, whether it sits on a CODE_BOSS_* working branch, and whether it was already dirty at open. Pull this when commits or task-undo are not landing.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const g = d.git ?? {};
      if (!g.isRepo) return { kind: 'text', severity: 'info', body: '(active project is not a git repo)' };
      if (!g.repoList?.length) return { kind: 'text', severity: 'warn', body: 'git repo flagged but no repos enumerated' };
      const rows = g.repoList.map((r) => [r.relDir, r.branch ?? '?', r.head ?? '?', r.codeBoss ? 'yes' : 'no', r.dirtyAtOpen ? 'yes' : 'no']);
      return { kind: 'table', body: { columns: ['repo', 'branch', 'head', 'CODE_BOSS', 'dirtyAtOpen'], rows } };
    },
  },

  // ─── 50-59: TRANSACTIONS ────────────────────────────────────────────────
  {
    id: 50, name: 'Last TX request (full)', category: 'transactions',
    description: 'Full JSON of the most recent chat_completions request body.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      if (!tx?.request) return { kind: 'text', severity: 'info', body: '(no request captured)' };
      return { kind: 'json', body: tx.request };
    },
  },
  {
    id: 51, name: 'Last TX response (full)', category: 'transactions',
    description: 'Full JSON of the most recent chat_completions response body.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      if (!tx?.response) return { kind: 'text', severity: 'info', body: '(no response captured)' };
      return { kind: 'json', body: tx.response };
    },
  },
  {
    id: 52, name: 'Last TX messages array only', category: 'transactions',
    description: 'Just request.messages from the last call — easier to scan than the full body.',
    sizeHint: { cols: 4, rows: 6 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const ms = tx?.request?.messages;
      if (!ms) return { kind: 'text', severity: 'info', body: '(no messages)' };
      const body = ms.map((m, i) => {
        const c = String(m.content ?? '').replace(/\s+/g, ' ');
        return `[${i}] ${m.role}: ${c.slice(0, 280)}${c.length > 280 ? '…' : ''}`;
      }).join('\n\n');
      return { kind: 'text', body };
    },
  },
  {
    id: 53, name: 'Last TX response text only', category: 'transactions',
    description: 'Just choices[0].message.content from the last response.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const tx = _lastChatTx(d);
      const c = tx?.response?.choices?.[0]?.message?.content;
      if (c == null) return { kind: 'text', severity: 'info', body: '(no response text)' };
      return { kind: 'text', body: String(c) };
    },
  },
  {
    id: 54, name: 'Transactions table', category: 'transactions',
    description: 'Compact table of every transaction event with timing and summary.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.transactions?.events ?? []).slice(-50);
      if (!evs.length) return { kind: 'text', severity: 'info', body: '(no transactions)' };
      const rows = evs.map((e) => [String(e.at ?? ''), String(e.type ?? ''), String(e.summary ?? '').slice(0, 200)]);
      return { kind: 'table', body: { columns: ['+ms', 'type', 'summary'], rows } };
    },
  },
  {
    id: 55, name: 'Chat completions latency', category: 'transactions',
    description: 'durationMs of the last 10 chat_completions calls.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const txs = _allChatTxs(d).slice(-10);
      if (!txs.length) return { kind: 'text', severity: 'info', body: '(no calls)' };
      const rows = txs.map((t, i) => `#${i + 1}  ${_fmtMs(t.durationMs ?? null)}`);
      return { kind: 'text', body: rows.join('\n') };
    },
  },
  {
    id: 56, name: 'Total chat call count', category: 'transactions',
    description: 'Number of chat_completions calls + cumulative token spend.',
    sizeHint: { cols: 3, rows: 2 },
    compute: (d) => {
      const n = _allChatTxs(d).length;
      const tt = d.transactions?.totalTokens ?? 0;
      return { kind: 'text', body: `${n} calls\n${tt} tokens total` };
    },
  },

  {
    id: 57, name: 'Call status history', category: 'transactions',
    description: 'Timeline of every model call this session: OK (with finish_reason + duration) or ERROR (with message). The fastest way to spot an intermittent proxy failure pattern.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.transactions?.events ?? []).filter((e) => e.type === 'chat_completions' || e.type === 'error');
      if (!evs.length) return { kind: 'text', severity: 'info', body: '(no calls)' };
      const rows = evs.slice(-40).map((e) => {
        if (e.type === 'error') return [String(e.at ?? ''), 'ERROR', String(e.message ?? '').slice(0, 120)];
        const fr = e.response?.choices?.[0]?.finish_reason ?? '?';
        return [String(e.at ?? ''), `ok (${fr})`, _fmtMs(e.durationMs ?? null)];
      });
      return { kind: 'table', body: { columns: ['+ms', 'status', 'detail'], rows } };
    },
  },
  {
    id: 58, name: 'Unlock / rate-limit events', category: 'transactions',
    description: 'unlock (401 + unlock_url) and rate-limit (429 backoff) transactions — the proxy auth/throttle history. A burst here explains a stalled or slow session.',
    sizeHint: { cols: 6, rows: 3 },
    compute: (d) => {
      const evs = (d.transactions?.events ?? []).filter((e) => e.type === 'unlock' || e.type === 'rate-limit');
      if (!evs.length) return { kind: 'text', severity: 'ok', body: '(no unlock / rate-limit events)' };
      const body = evs.slice(-20).map((e) => `+${e.at}ms  ${e.type}  ${e.summary ?? ''}`).join('\n');
      return { kind: 'text', severity: 'warn', body };
    },
  },

  // ─── 60-69: CHAT EVENTS ─────────────────────────────────────────────────
  {
    id: 60, name: 'Chat events table', category: 'events',
    description: 'Compact table of every chat event (user/assistant/tool/etc.).',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.chatEvents ?? []).slice(-100);
      if (!evs.length) return { kind: 'text', severity: 'info', body: '(no events)' };
      const rows = evs.map((e) => {
        const c = String(e.content ?? e.summary ?? e.diff ?? '').replace(/\s+/g, ' ').slice(0, 160);
        return [String(e.id ?? ''), String(e.type ?? ''), String(e.taskId ?? ''), c];
      });
      return { kind: 'table', body: { columns: ['id', 'type', 'task', 'content'], rows } };
    },
  },
  {
    id: 61, name: 'Last assistant-text event', category: 'events',
    description: 'Content of the most recent assistant-text chat event (what the user sees in the chat panel).',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.chatEvents ?? []).filter((e) => e.type === 'assistant-text');
      const last = evs[evs.length - 1];
      if (!last) return { kind: 'text', severity: 'info', body: '(no assistant-text events)' };
      return { kind: 'text', body: String(last.content ?? '') };
    },
  },
  {
    id: 62, name: 'Last user-message event', category: 'events',
    description: 'Content of the most recent user-message chat event.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const evs = (d.chatEvents ?? []).filter((e) => e.type === 'user-message');
      const last = evs[evs.length - 1];
      if (!last) return { kind: 'text', severity: 'info', body: '(no user-message events)' };
      return { kind: 'text', body: String(last.content ?? '') };
    },
  },

  {
    id: 63, name: 'Task lifecycle + commits', category: 'events',
    description: 'Each closed task (<next-task> boundary): id, recorded commit hash, repo count, and summary. A closed write-heavy task with "(none)" for commit means the auto-commit hook did not fire.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.chatEvents ?? []).filter((e) => e.type === 'task-end');
      if (!evs.length) return { kind: 'text', severity: 'info', body: '(no closed tasks)' };
      const rows = evs.slice(-30).map((e) => [
        String(e.taskId ?? ''),
        e.commitHash ? String(e.commitHash).slice(0, 8) : '(none)',
        Array.isArray(e.commits) ? String(e.commits.length) : '?',
        String(e.summary ?? '').replace(/\s+/g, ' ').slice(0, 120),
      ]);
      return { kind: 'table', body: { columns: ['task', 'commit', '#repos', 'summary'], rows } };
    },
  },
  {
    id: 64, name: 'Tool / shell errors', category: 'events',
    description: 'tool-result events that failed (status=error): write/edit/powershell/git failures. This is where a broken build command, blocked cmd.exe, or a failed git op surfaces.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.chatEvents ?? []).filter((e) => e.type === 'tool-result' && e.status === 'error');
      if (!evs.length) return { kind: 'text', severity: 'ok', body: '(no failed tool calls)' };
      const body = evs.slice(-15).map((e) => `[${e.verb ?? e.name ?? '?'}] ${String(e.content ?? '').replace(/\s+/g, ' ').slice(0, 200)}`).join('\n\n');
      return { kind: 'text', severity: 'warn', body };
    },
  },

  // ─── 70-79: TRENDS / COMPARISONS ────────────────────────────────────────
  {
    id: 70, name: 'Parallelism stats', category: 'trends',
    description: 'Tool-call batching stats across this session.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const p = d.parallelism ?? {};
      return { kind: 'text', body: [
        `runs:           ${p.runs ?? 0}`,
        `turns w/ tools: ${p.turnsWithTools ?? 0}`,
        `total calls:    ${p.totalToolCalls ?? 0}`,
        `avg / turn:     ${p.avgPerTurn ?? 0}`,
        `max parallel:   ${p.maxParallel ?? 0}`,
      ].join('\n') };
    },
  },

  {
    id: 71, name: 'Manager-pass diagnostics', category: 'trends',
    description: 'Diagnostics from the reviewer / acceptance / intake / consolidation passes (manager-diagnostic events) — e.g. "no verdict emitted; defaulted to pass". Tells you when a manager LLM call drifted off-protocol.',
    sizeHint: { cols: 6, rows: 4 },
    compute: (d) => {
      const evs = (d.chatEvents ?? []).filter((e) => e.type === 'manager-diagnostic');
      if (!evs.length) return { kind: 'text', severity: 'info', body: '(no manager passes have flagged anything)' };
      const body = evs.slice(-12).map((e) => `${e.kind ?? 'pass'}: ${String(e.reason ?? e.summary ?? '').replace(/\s+/g, ' ').slice(0, 180)}`).join('\n\n');
      return { kind: 'text', severity: 'warn', body };
    },
  },
  {
    id: 72, name: 'Run terminations / churn', category: 'trends',
    description: 'How each agent run ended, across the session (agent_run events): stop = converged normally; max-turns = never converged (bad); task-churn = the open/close-empty-task loop was broken; empty-close = ended on a refused empty close. Plus the total auto-closed count (Layer-1 force-close of a runaway task). Repeated max-turns / task-churn / many auto-closes are the task-loop pathologies.',
    sizeHint: { cols: 4, rows: 3 },
    compute: (d) => {
      const runs = (d.transactions?.events ?? []).filter((e) => e.type === 'agent_run');
      if (!runs.length) return { kind: 'text', severity: 'info', body: '(no agent runs yet)' };
      const by = {};
      let autoClosed = 0;
      for (const r of runs) {
        const k = r.terminated || 'unknown';
        by[k] = (by[k] || 0) + 1;
        autoClosed += Number(r.autoClosed || 0);
      }
      const bad = (by['max-turns'] || 0) + (by['task-churn'] || 0) + (by['empty-close'] || 0);
      const sev = bad > 0 || autoClosed > 0 ? 'warn' : 'ok';
      const lines = Object.entries(by).sort((a, b) => b[1] - a[1]).map(([k, n]) => `${k}: ${n}`);
      lines.push(`auto-closed (runaway force-close): ${autoClosed}`);
      return { kind: 'text', severity: sev, body: `${runs.length} run(s)\n` + lines.join('\n') };
    },
  },

  // ─── 99: META ───────────────────────────────────────────────────────────
  {
    id: 99, name: 'Dense summary', category: 'meta',
    description: 'The classic dense one-block diagnostic summary (build, chat, proj, api, trans, stderr).',
    sizeHint: { cols: 6, rows: 6 },
    compute: (d) => {
      const b = d.build ?? {}, s = d.server ?? {}, c = d.chat ?? {}, p = d.project ?? {}, cfg = d.config ?? {}, t = d.transactions ?? {};
      const lines = [
        `build       ${b.version ?? '?'}`,
        `started     ${b.startedAt ?? '?'}   uptime ${_fmtMs(s.uptimeMs)}`,
        `pid ${s.pid ?? '?'}   node ${s.nodeVersion ?? '?'}   platform ${s.platform ?? '?'}`,
        '',
        `CHAT  status=${c.status} phase=${c.phase} version=${c.version}`,
        `      events=${c.eventCount ?? 0}  tokens up=${c.tokens?.up ?? 0} down=${c.tokens?.down ?? 0}`,
        `      sse-subs=${c.streamSubscribers ?? 0}  timeout=${_fmtMs(c.timeoutMs)}`,
        `      error=${c.error ?? '—'}`,
        '',
        `PROJ  ${p.activeProject ?? '(none)'}`,
        `      log file: ${p.logFilePath ?? '—'}  exists=${p.logFileExists ? 'YES' : 'NO'}  bytes=${p.logFileBytes ?? 0}`,
        '',
        `API   base=${cfg.baseURL ?? '?'}`,
        `      model=${cfg.defaultModel ?? '?'}   key=${cfg.apiKeyPrefix ?? '?'}`,
        '',
        `TRANS count=${t.count ?? 0}   totalTokens=${t.totalTokens ?? 0}`,
        `STDERR (${(d.stderr ?? []).length} lines captured)`,
      ];
      return { kind: 'text', body: lines.join('\n') };
    },
  },
];

// IDs that load when the user has no active set yet, or hits "defaults".
// Sized so they pack cleanly onto a single 12×8 page (96 cells):
//   1 (3×2=6) + 4 (6×3=18) + 10 (6×4=24) + 13 (4×3=12) + 30 (6×3=18) = 78 cells
//
// Covers the core debugging loop:
//   1  was the system reminder injected
//   4  what did the user actually ask (with reminder stripped)
//   10 what did the model reply (full text)
//   13 did the reply drift to a Gemini built-in persona
//   30 any errors in stderr (silent-failure detector)
//
// Add 20 (all findings), 21 (critical only), 50/51 (full TX bodies), etc.
// by typing them into the active-IDs input. The catalog page lists every
// available query with its ID.
//
// Recommended sets to type in when a specific symptom shows up in
// production (screenshot the resulting page and send the numbers back):
//
//   PROXY / "it stopped responding"     →  43, 36, 37, 57, 58, 38
//     (which backend, last error + history, call timeline, auth/throttle,
//      current chat error)
//   WRONG / OFF-TARGET ANSWERS          →  1, 2, 9, 13, 17, 18, 39
//     (reminder present?, reminder text, Anthropic shape, identity drift,
//      empty/no-tool reply, fenced/escaped tags, AGENTS.md loaded?)
//   WORK NOT LANDING (no commits/edits) →  49, 63, 64, 30
//     (git workspace, task+commit lifecycle, tool/shell errors, stderr)
//   MANAGER PASSES (review/acceptance)  →  20, 71, 60
//     (all findings, manager-pass diagnostics, chat-events table)
const DEFAULT_QUERY_IDS = [1, 4, 10, 13, 30];

/**
 * Compute the SMALLEST {cols, rows} rectangle on the 12×8 page grid that
 * holds the tile's content without scrolling. Per-kind heuristic — each
 * mosaic cell at ~1400×800 viewport is roughly 18 monospace chars wide
 * and 6 lines tall, after subtracting header + padding.
 *
 * The size is computed from the ACTUAL rendered body, not the catalog
 * hint. A "Last response" tile gets small when the model said "ok", and
 * grows large when the model wrote a paragraph. This lets the packer fit
 * more tiles on each page because tiles only claim what they need.
 *
 * Returns { cols, rows } clamped to [minCols..maxCols] × [minRows..maxRows].
 */
// computeTileSize, packPage, and layoutMosaic now live in
// src/agent/mosaic-layout.mjs. They're declared above in the bundle (same
// module scope after the build's import-stripper concatenates all
// JS_SOURCES), and re-exported at the bottom of this file for tests
// importing directly from server.mjs.

/**
 * Execute a set of catalog queries against a diagnostics object and return
 * an array of tile results, one per requested ID (preserving order). The
 * sizeHint is computed from the actual rendered content (not the catalog
 * default) so tiles only claim the space they need. Unknown IDs produce
 * an error placeholder.
 */
function runQueries(ids, diag) {
  const out = [];
  for (const id of ids) {
    const q = QUERY_CATALOG.find((x) => x.id === id);
    if (!q) {
      const tile = { kind: 'text', severity: 'warn', body: `no catalog entry for id=${id}` };
      out.push({ id, name: `unknown query #${id}`, category: 'meta', sizeHint: computeTileSize(tile), ...tile });
      continue;
    }
    let result;
    try { result = q.compute(diag) ?? {}; }
    catch (e) { result = { kind: 'text', severity: 'critical', body: `query #${id} threw: ${e.message}` }; }
    const partial = {
      kind: result.kind ?? 'text',
      severity: result.severity ?? null,
      body: result.body ?? '',
    };
    out.push({
      id: q.id,
      name: q.name,
      category: q.category,
      description: q.description,
      sizeHint: computeTileSize(partial),
      ...partial,
    });
  }
  return out;
}

// Standalone diagnostics page served at GET /snapshot. Deliberately SELF-
// CONTAINED: no SSE, no tab-leadership, no dependency on the app's UI state —
// so it works when the main UI is wedged, and (because it opens no persistent
// connection) it neither exhausts the connection pool nor steals the active
// tab. Type query numbers (or use a preset) and it renders a grid of tiles
// from /api/queries. Open directly at http://127.0.0.1:<port>/snapshot too.
const SNAPSHOT_PAGE_HTML = `<!DOCTYPE html>
<html lang="en"><head><meta charset="utf-8"><title>code_boss diagnostics</title><link rel="icon" href="data:,">
<style>
  :root{--bg:#100e0b;--panel:#161410;--panel2:#1b1914;--border:#3a372f;--fg:#e6e2d6;--muted:#8c887b;--accent:#d97757;--mono:ui-monospace,Consolas,Menlo,monospace}
  *{box-sizing:border-box} html,body{margin:0;height:100%}
  body{background:var(--bg);color:var(--fg);font:13px/1.45 var(--mono)}
  header{position:sticky;top:0;background:var(--panel);border-bottom:1px solid var(--border);padding:8px 12px;display:flex;gap:8px;align-items:center;flex-wrap:wrap;z-index:5}
  header b{color:var(--accent)} #status{color:var(--muted);margin-left:auto}
  input{flex:0 0 320px;background:var(--panel2);color:var(--fg);border:1px solid var(--border);border-radius:4px;padding:5px 8px;font:inherit}
  button{background:var(--panel2);color:var(--fg);border:1px solid var(--border);border-radius:4px;padding:5px 9px;font:inherit;cursor:pointer}
  button:hover{border-color:var(--accent)} button.go{background:var(--accent);color:#100e0b;border-color:var(--accent);font-weight:600}
  .presets{display:flex;gap:6px;flex-wrap:wrap;padding:6px 12px;border-bottom:1px solid var(--border);background:var(--panel)}
  .presets button{font-size:11.5px;color:var(--muted)}
  #grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(320px,1fr));gap:10px;padding:12px}
  .tile{background:var(--panel);border:1px solid var(--border);border-left:3px solid var(--border);border-radius:5px;overflow:hidden;min-width:0}
  .tile.sev-critical{border-left-color:#e5635a} .tile.sev-warn{border-left-color:#d9a13a} .tile.sev-ok{border-left-color:#5aa86b} .tile.sev-info{border-left-color:#6b87a8}
  .thead{padding:6px 10px;background:var(--panel2);border-bottom:1px solid var(--border);display:flex;gap:6px;align-items:center}
  .tid{color:var(--muted)} .tname{font-weight:600} .tag{margin-left:auto;font-size:10px;padding:1px 6px;border-radius:3px;text-transform:uppercase}
  .tag.sev-critical{background:#3a1714;color:#e5635a} .tag.sev-warn{background:#2a2310;color:#d9a13a} .tag.sev-ok{background:#14271a;color:#5aa86b} .tag.sev-info{background:#16202a;color:#8fb3d9}
  .tbody{padding:8px 10px;max-height:340px;overflow:auto}
  pre{margin:0;white-space:pre-wrap;word-break:break-word;font:12px/1.4 var(--mono)}
  table{border-collapse:collapse;font-size:11.5px;width:100%} th,td{border:1px solid var(--border);padding:2px 6px;text-align:left;vertical-align:top}
  th{background:var(--panel2)}
  .finding{padding:3px 0;border-bottom:1px solid var(--border)} .fsev{font-weight:700;font-size:10px;margin-right:6px}
  .finding.sev-critical .fsev{color:#e5635a} .finding.sev-warn .fsev{color:#d9a13a} .finding.sev-ok .fsev{color:#5aa86b} .finding.sev-info .fsev{color:#8fb3d9}
  .fdet{color:var(--muted);font-size:11px;margin:2px 0 0 4px}
  #catalog{display:none;padding:8px 12px;border-top:1px solid var(--border);background:var(--panel);max-height:40vh;overflow:auto}
  #catalog.show{display:block} .catrow{padding:1px 0} .catrow b{display:inline-block;width:28px;color:var(--accent)} .catdesc{color:var(--muted)}
</style></head>
<body>
<header>
  <b>code_boss</b> diagnostics
  <input id="ids" placeholder="query numbers e.g. 43,36,37" autofocus>
  <button class="go" id="go">Render</button>
  <button id="catToggle">Catalog</button>
  <span id="build"></span>
  <span id="status"></span>
</header>
<div class="presets">
  <span style="color:var(--muted);align-self:center">presets:</span>
  <button data-ids="43,36,37,57,58,38">proxy / stopped responding</button>
  <button data-ids="1,2,9,13,17,18,39">wrong / off-target answers</button>
  <button data-ids="49,63,64,30">work not landing</button>
  <button data-ids="24,7,35,71,20">context / runaway + findings</button>
  <button data-ids="99">dense summary</button>
</div>
<div id="grid"></div>
<div id="catalog"></div>
<script>
(function(){
  var grid=document.getElementById('grid'),input=document.getElementById('ids'),status=document.getElementById('status');
  function esc(s){return String(s==null?'':s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
  function sc(s){return s?('sev-'+s):'';}
  function body(t){
    if(t.kind==='json')return '<pre>'+esc(JSON.stringify(t.body,null,2))+'</pre>';
    if(t.kind==='table'&&t.body){var c=t.body.columns||[],r=t.body.rows||[];
      return '<table><thead><tr>'+c.map(function(x){return '<th>'+esc(x)+'</th>';}).join('')+'</tr></thead><tbody>'+
        r.map(function(row){return '<tr>'+row.map(function(x){return '<td>'+esc(x)+'</td>';}).join('')+'</tr>';}).join('')+'</tbody></table>';}
    if(t.kind==='findings'&&Array.isArray(t.body))return (t.body.map(function(f){
        return '<div class="finding '+sc(f.severity)+'"><span class="fsev">'+esc((f.severity||'').toUpperCase())+'</span>'+esc(f.title||'')+(f.detail?'<div class="fdet">'+esc(f.detail)+'</div>':'')+'</div>';}).join(''))||'(none)';
    return '<pre>'+esc(t.body)+'</pre>';
  }
  function render(d){
    grid.innerHTML='';
    (d.tiles||[]).forEach(function(t){
      var el=document.createElement('div'); el.className='tile '+sc(t.severity);
      el.innerHTML='<div class="thead"><span class="tid">#'+t.id+'</span><span class="tname">'+esc(t.name)+'</span>'+(t.severity?'<span class="tag '+sc(t.severity)+'">'+esc(t.severity)+'</span>':'')+'</div><div class="tbody">'+body(t)+'</div>';
      grid.appendChild(el);
    });
    document.getElementById('catalog').innerHTML=(d.catalog||[]).map(function(q){return '<div class="catrow"><b>'+q.id+'</b>'+esc(q.name)+' <span class="catdesc">'+esc(q.description||'')+'</span></div>';}).join('');
    if(d.build)document.getElementById('build').textContent='build '+(d.build.version||'?');
    status.textContent=(d.tiles||[]).length+' tiles · '+new Date().toLocaleTimeString();
  }
  function load(ids){
    status.textContent='loading…';
    var u='/api/queries'+(ids?'?ids='+encodeURIComponent(ids):'');
    var ctrl=new AbortController(),to=setTimeout(function(){ctrl.abort();},10000);
    fetch(u,{signal:ctrl.signal}).then(function(r){return r.json();}).then(function(d){clearTimeout(to);if(!input.value)input.value=(d.defaultIds||[]).join(',');render(d);}).catch(function(e){clearTimeout(to);status.textContent='fetch failed: '+(e&&e.message||e);});
  }
  document.getElementById('go').onclick=function(){load(input.value.trim());};
  input.addEventListener('keydown',function(e){if(e.key==='Enter')load(input.value.trim());});
  Array.prototype.forEach.call(document.querySelectorAll('[data-ids]'),function(b){b.onclick=function(){input.value=b.getAttribute('data-ids');load(input.value);};});
  document.getElementById('catToggle').onclick=function(){document.getElementById('catalog').classList.toggle('show');};
  var p=new URLSearchParams(location.search),initial=p.get('ids')||'';input.value=initial;load(initial);
})();
</script>
</body></html>`;

/**
 * Pack a list of tiles onto a single page of given dimensions via
 * backtracking search. Each tile contributes its `sizeHint.{cols,rows}`
 * as a rectangle, capped at the page size. The algorithm:
 *
 *   1. Find the top-left empty cell.
 *   2. Try placing each unplaced tile that fits there. For each placement,
 *      recurse — this explores all permutations within the time budget.
 *   3. If no tile fits the top-left empty cell, mark that cell "wasted"
 *      and recurse with the next empty cell.
 *   4. Score each leaf by total filled cells; keep the best seen.
 *
 * Time-capped via opts.maxMs (default 200ms). For the typical workload
 * (≤15 tiles per page) the search completes in well under that.
 *
 * Returns { placed, notPlaced, score } — placed tiles carry their assigned
 * row/col + spans; notPlaced overflow to the next page.
 */
// packPage and layoutMosaic moved to src/agent/mosaic-layout.mjs.

/**
 * Walk the assembled diagnostics object and return a list of findings —
 * problems (or confirmations) detected by inspecting the raw data so the
 * user doesn't have to spot them by eye. Each finding has:
 *   - severity: 'ok' | 'info' | 'warn' | 'critical'
 *   - category: 'prompt' | 'fileLog' | 'stderr' | 'gemini' | 'state' | 'config' | 'overall'
 *   - title:    one-line headline
 *   - detail:   longer explanation (optional)
 *
 * Pure function over the diag object — no I/O, no side effects — so it's
 * unit-testable. Add new checks here as we learn new failure modes.
 */
function runDiagnosticChecks(diag) {
  // Every check below ALWAYS emits at least one finding so the panel acts
  // as a system-health dashboard rather than a "errors only" log. Findings
  // are sorted at the end by severity (critical → warn → info → ok) so
  // problems float to the top of the view.
  const findings = [];
  const txs = (diag.transactions?.events ?? []).filter((e) => e.type === 'chat_completions');
  const lastChat = txs[txs.length - 1];

  // 1. System reminder injection. After our recent change, the reminder is
  //    prepended INSIDE the latest user message. If it's missing, Gemini
  //    will never see the code_boss instructions and may answer as the
  //    Gemini Enterprise built-in agent.
  if (!lastChat) {
    findings.push({
      severity: 'info', category: 'prompt',
      title: 'System reminder — no chat request to inspect yet',
      detail: 'Send a message to enable this check.',
    });
  } else if (!lastChat.request?.messages) {
    findings.push({
      severity: 'warn', category: 'prompt',
      title: 'Last chat request had no messages array',
      detail: 'request.messages is missing — adapter may not be capturing the request properly.',
    });
  } else {
    const msgs = lastChat.request.messages;
    let lastUserIdx = -1;
    for (let i = msgs.length - 1; i >= 0; i--) {
      if (msgs[i].role === 'user') { lastUserIdx = i; break; }
    }
    if (lastUserIdx < 0) {
      findings.push({
        severity: 'warn', category: 'prompt',
        title: 'Last request had no user message',
        detail: `request had ${msgs.length} message(s), none with role=user`,
      });
    } else {
      const content = String(msgs[lastUserIdx].content ?? '');
      const hasReminder = content.includes('<system-reminder>');
      const reminderAtStart = content.trimStart().startsWith('<system-reminder>');
      if (hasReminder && reminderAtStart) {
        findings.push({
          severity: 'ok', category: 'prompt',
          title: `System reminder is at the start of the latest user message (${msgs.length} msgs)`,
        });
      } else if (hasReminder) {
        findings.push({
          severity: 'warn', category: 'prompt',
          title: 'System reminder present but NOT at the start of the latest user message',
          detail: 'Instructions buried mid-message are easier for the model to ignore.',
        });
      } else {
        findings.push({
          severity: 'critical', category: 'prompt',
          title: 'System reminder MISSING from the latest user message',
          detail: `Gemini will not see code_boss instructions. Latest user msg starts: "${content.slice(0, 140)}"`,
        });
      }
    }
  }

  // 2. File logging actually working? Catches regressions of the mkdirSync
  //    silent-failure bug.
  if (!diag.project?.logFilePath) {
    findings.push({
      severity: 'info', category: 'fileLog',
      title: 'File logging — no project open, nothing to write',
    });
  } else if (diag.project.logFileExists === false) {
    findings.push({
      severity: 'critical', category: 'fileLog',
      title: 'Log file does NOT exist on disk',
      detail: `Expected at ${diag.project.logFilePath}. fileLog may be silently failing — check stderr for ReferenceError / mkdirSync errors.`,
    });
  } else if (diag.project.logFileBytes === 0 && (diag.server?.uptimeMs ?? 0) > 30_000) {
    findings.push({
      severity: 'warn', category: 'fileLog',
      title: 'Log file is 0 bytes after 30s+ uptime',
      detail: 'No fileLog writes have landed. Either fully idle or writes are failing.',
    });
  } else {
    findings.push({
      severity: 'ok', category: 'fileLog',
      title: `Log file present (${diag.project.logFileBytes} bytes)`,
    });
  }

  // 3. Errors / exceptions surfaced in the stderr ring buffer.
  const stderr = diag.stderr ?? [];
  const errorPatterns = [
    /\bReferenceError\b/, /\bTypeError\b/, /\bSyntaxError\b/, /is not defined/,
    /\bENOENT\b/, /\bEACCES\b/, /\bEADDRINUSE\b/, /FAILED TO START/,
    /\bUncaught\b/, /\[fileLog\] file write FAILED/,
  ];
  const stderrErrors = stderr.filter((e) => errorPatterns.some((re) => re.test(e.line ?? '')));
  if (stderrErrors.length === 0) {
    findings.push({
      severity: 'ok', category: 'stderr',
      title: `Stderr clean (${stderr.length} line(s), 0 error patterns)`,
    });
  } else {
    for (const e of stderrErrors.slice(-3)) {
      findings.push({
        severity: 'critical', category: 'stderr',
        title: 'Error captured in stderr buffer',
        detail: (e.line ?? '').slice(0, 300),
      });
    }
  }

  // 4. Gemini identity drift — assistant responding as Gemini Enterprise
  //    built-in instead of code_boss.
  const identityPatterns = [
    /\bI am Gemini Enterprise\b/i,
    /\bI(?:'| a)m Gemini(?:\b|,|\.)/i,
    /\bMemory Bank agent\b/i,
    /\bI can help you with (?:Jira|Drive|Calendar|Gmail|Confluence)/i,
    /\bGenerate Memories\b/i,
    /\bI am a large language model\b/i,
    /\bI do not have direct access to your remote or local file system\b/i,
  ];
  const recentResponses = txs.slice(-5);
  if (recentResponses.length === 0) {
    findings.push({
      severity: 'info', category: 'gemini',
      title: 'Identity-drift check — no responses to scan yet',
    });
  } else {
    let driftHit = null;
    for (const tx of recentResponses) {
      const content = tx?.response?.choices?.[0]?.message?.content ?? '';
      const matched = identityPatterns.find((re) => re.test(content));
      if (matched) { driftHit = { matched, content }; break; }
    }
    if (driftHit) {
      findings.push({
        severity: 'warn', category: 'gemini',
        title: 'Assistant replied as a Gemini built-in persona, not code_boss',
        detail: `Matched ${driftHit.matched.source}. Reply starts: "${driftHit.content.slice(0, 160)}"`,
      });
    } else {
      findings.push({
        severity: 'ok', category: 'gemini',
        title: `Identity OK — no Gemini-built-in phrasing in last ${recentResponses.length} replies`,
      });
    }
  }

  // 5. finish_reason — length truncation or content filter hits.
  if (recentResponses.length === 0) {
    findings.push({
      severity: 'info', category: 'gemini',
      title: 'finish_reason check — no responses yet',
    });
  } else {
    let frHit = null;
    for (const tx of recentResponses) {
      const fr = tx?.response?.choices?.[0]?.finish_reason;
      if (fr === 'length' || fr === 'content_filter') { frHit = fr; break; }
    }
    if (frHit === 'length') {
      findings.push({
        severity: 'warn', category: 'gemini',
        title: 'A recent response was cut off (finish_reason=length)',
        detail: 'Model hit max_tokens before finishing.',
      });
    } else if (frHit === 'content_filter') {
      findings.push({
        severity: 'critical', category: 'gemini',
        title: 'A recent response was blocked (finish_reason=content_filter)',
      });
    } else {
      findings.push({
        severity: 'ok', category: 'gemini',
        title: 'All recent responses finished cleanly (no length/filter hits)',
      });
    }
  }

  // 6. Stale state hint — many messages on what might be a fresh session.
  const msgCount = lastChat?.request?.messages?.length ?? 0;
  if (msgCount >= 20) {
    findings.push({
      severity: 'info', category: 'state',
      title: `Conversation has ${msgCount} messages — may be stale state from disk`,
      detail: 'If unexpected, you may have loaded a stale .code_boss/tasks/state.json. Reset to start fresh.',
    });
  } else if (msgCount > 0) {
    findings.push({
      severity: 'ok', category: 'state',
      title: `Conversation size OK (${msgCount} messages)`,
    });
  }

  // 7. Chat stuck in 'running' for a long time.
  if (diag.chat?.status === 'running' && (diag.chat.chatStartedAgoMs ?? 0) > 120_000) {
    findings.push({
      severity: 'warn', category: 'state',
      title: `Chat has been 'running' for ${Math.round(diag.chat.chatStartedAgoMs / 1000)}s`,
      detail: 'May be stuck. Consider aborting and retrying.',
    });
  } else {
    findings.push({
      severity: 'ok', category: 'state',
      title: `Chat status: ${diag.chat?.status ?? 'unknown'}`,
    });
  }

  // 8a. Pending unlock — if any LLM call is blocked waiting for the user
  //     to click "I've unlocked", surface it loudly. Without this finding
  //     the page can appear frozen while the user wonders what to do.
  if (diag.auth?.pendingUnlockUrl) {
    findings.push({
      severity: 'critical', category: 'auth',
      title: `BLOCKED: ${diag.auth.pendingUnlockResolverCount} LLM call(s) waiting on API-key unlock`,
      detail: `unlock URL: ${diag.auth.pendingUnlockUrl}`,
    });
  }
  // 8b. SSE subscriber sanity. If the page disconnects (and 0 tabs are
  //     listening), an unlock-needed broadcast will land on no one and
  //     the modal will never appear in any browser.
  if (diag.auth?.pendingUnlockUrl && (diag.chat?.streamSubscribers ?? 0) === 0) {
    findings.push({
      severity: 'critical', category: 'auth',
      title: 'Unlock pending but 0 SSE subscribers — modal cannot reach any browser tab',
      detail: 'Reload the page; the snapshot init will re-show the unlock modal from pendingUnlockUrl.',
    });
  }

  // 8. Basic config sanity.
  if (!diag.project?.activeProject) {
    findings.push({
      severity: 'warn', category: 'config',
      title: 'No active project — chat will fail until one is opened',
    });
  } else {
    findings.push({
      severity: 'ok', category: 'config',
      title: `Active project: ${diag.project.activeProject}`,
    });
  }
  const keyPfx = diag.config?.apiKeyPrefix;
  if (!keyPfx || keyPfx === '(none)' || keyPfx === '') {
    findings.push({
      severity: 'warn', category: 'config',
      title: 'No API key configured',
      detail: 'Edit API_KEY in the bundle CONFIG section.',
    });
  } else {
    findings.push({
      severity: 'ok', category: 'config',
      title: `API key set (${keyPfx})`,
    });
  }

  // 9. LLM / proxy call failures. The adapter throws on non-2xx (the message
  //    carries "HTTP <status>: <body>"), a timeout, or a network error; the
  //    run handler records it as an 'error' transaction. On a flaky air-gapped
  //    proxy this is the single most likely failure, so surface it loudly.
  const errorTxs = (diag.transactions?.events ?? []).filter((e) => e.type === 'error');
  if (errorTxs.length) {
    const last = errorTxs[errorTxs.length - 1];
    findings.push({
      severity: 'critical', category: 'llm',
      title: `Last model call failed (${errorTxs.length} error tx this session)`,
      detail: String(last.message ?? last.summary ?? '').slice(0, 300),
    });
  }

  // 10. Failed tool calls — write/edit/powershell/git that returned an error.
  //     Catches a broken build command, blocked cmd.exe, or git failure that
  //     the worker may have silently moved past.
  const toolErrs = (diag.chatEvents ?? []).filter((e) => e.type === 'tool-result' && e.status === 'error');
  if (toolErrs.length) {
    const last = toolErrs[toolErrs.length - 1];
    findings.push({
      severity: 'warn', category: 'tools',
      title: `${toolErrs.length} tool call(s) failed this session`,
      detail: `last: [${last.verb ?? last.name ?? '?'}] ${String(last.content ?? '').replace(/\s+/g, ' ').slice(0, 200)}`,
    });
  }

  // 11. Anthropic message-shape — only meaningful on the Anthropic backend,
  //     which requires the request to end with a user message and to avoid
  //     same-role runs (the trailing-user rule). Guarded so it stays silent
  //     on the OpenAI backend and in unit tests (no apiFormat set).
  if (diag.config?.apiFormat === 'anthropic' && lastChat?.request?.messages?.length) {
    const m = lastChat.request.messages;
    const shapeIssues = [];
    if (m[m.length - 1]?.role !== 'user') shapeIssues.push('last message is not role=user');
    for (let i = 1; i < m.length; i++) if (m[i].role === m[i - 1].role) { shapeIssues.push(`same-role run at index ${i - 1}-${i} (${m[i].role})`); break; }
    if (shapeIssues.length) {
      findings.push({
        severity: 'critical', category: 'prompt',
        title: 'Anthropic message-shape violation in the last request',
        detail: shapeIssues.join('; ') + ' — the Anthropic Messages API will reject this.',
      });
    }
  }

  // 12. Context-window fill. At/over budget means a large OPEN task is being
  //     dragged to the model every turn (a model that never closes tasks) —
  //     the runaway-context failure that caused slow turns + 502s.
  const cf = diag.context;
  if (cf && (cf.budgetPct ?? 0) >= 100) {
    findings.push({
      severity: 'warn', category: 'state',
      title: `Context at ${cf.budgetPct}% of budget (${cf.totalTokens}/${cf.budgetTokens} tokens)`,
      detail: 'A large open task is dragged to the model each turn. The open task is sliding-window elided to bound the prompt, but this persists when the model never emits <next-task> — consider closing/auto-closing tasks.',
    });
  }

  // Sort: critical first, then warn, then info, then ok — so problems
  // float to the top of the panel where they're most visible in screenshots.
  const order = { critical: 0, warn: 1, info: 2, ok: 3 };
  findings.sort((a, b) => (order[a.severity] ?? 9) - (order[b.severity] ?? 9));

  return findings;
}

/** Open a native folder-picker dialog; resolve to the selected absolute path
 *  or null if the user canceled. */
function pickFolderNative() {
  return new Promise((resolve) => {
    let cmd, args, parseStdout = (s) => s.trim() || null;
    if (process.platform === 'win32') {
      cmd = 'powershell';
      args = ['-NoProfile', '-STA', '-Command',
        "Add-Type -AssemblyName System.Windows.Forms | Out-Null; " +
        "$f = New-Object System.Windows.Forms.FolderBrowserDialog; " +
        "$f.Description = 'Choose a project folder'; " +
        "if ($f.ShowDialog() -eq [System.Windows.Forms.DialogResult]::OK) { Write-Output $f.SelectedPath }"];
    } else if (process.platform === 'darwin') {
      cmd = 'osascript';
      args = ['-e', 'POSIX path of (choose folder with prompt "Choose a project folder")'];
    } else {
      cmd = 'zenity';
      args = ['--file-selection', '--directory', '--title=Choose a project folder'];
    }
    // Do NOT pass windowsHide:true — that suppresses the dialog window too.
    const ps = spawn(cmd, args, { stdio: ['ignore', 'pipe', 'pipe'] });
    let out = '';
    ps.stdout.on('data', (d) => (out += d.toString()));
    ps.on('error', () => resolve(null));
    ps.on('close', () => resolve(parseStdout(out)));
  });
}

const SUMMARIZE = {
  list_models: (d) => `list_models  →  ${d.response?.data?.length ?? '?'} models  (${d.durationMs}ms)`,
  chat_completions: (d) => {
    const fr = d.response?.choices?.[0]?.finish_reason ?? '?';
    const u = d.response?.usage ?? {};
    return `chat_completions  →  finish=${fr}  tokens=${u.total_tokens ?? '?'}  (${d.durationMs}ms)`;
  },
  unlock: (d) => `unlock  →  ${d.url}`,
  'rate-limit': (d) => `rate-limit  →  sleep ${d.ms}ms`,
  usage: (d) => `usage  →  ${JSON.stringify(d)}`,
  error: (d) => `error: ${d.message}`,
};

// Ring buffer that captures everything written to process.stderr so the
// Snapshot UI can show it. Lets the user see what fileLog *would* have
// printed even if the launcher window output got lost/redirected.
const STDERR_BUFFER_MAX = 500;
const stderrBuffer = [];
const _origStderrWrite = process.stderr.write.bind(process.stderr);
process.stderr.write = function (chunk, ...rest) {
  try {
    const s = typeof chunk === 'string' ? chunk : (chunk && chunk.toString) ? chunk.toString() : '';
    if (s) {
      // Split into lines so the buffer entries are line-sized.
      const lines = s.split('\n');
      for (const ln of lines) {
        if (ln === '' && lines.length === 1) continue;
        if (ln === '' && lines.indexOf(ln) === lines.length - 1) continue; // trailing empty after \n
        stderrBuffer.push({ at: new Date().toISOString(), line: ln });
        if (stderrBuffer.length > STDERR_BUFFER_MAX) stderrBuffer.shift();
      }
    }
  } catch {}
  return _origStderrWrite(chunk, ...rest);
};

// BUILD_VERSION is injected by scripts/build.mjs into the bundled HEADER.
// When running src/ directly (dev/tests), it's undefined — fall back to '(dev)'.
const BUILD_VERSION_RUNTIME = (typeof BUILD_VERSION !== 'undefined') ? BUILD_VERSION : '(dev)';

// 16x16 solid-MAGENTA PNG (79 bytes), generated offline with zlib (same
// hand-rolled chunk approach as an icon build). Used by POST /api/probe-images:
// "what color is this image" is an unambiguous yes/no signal for whether a
// given image wire format actually reached the model's eyes.
const MAGENTA_PROBE_PNG_BASE64 =
  'iVBORw0KGgoAAAANSUhEUgAAABAAAAAQCAIAAACQkWg2AAAAFklEQVR4nGP4z/CfJMQwqmFUw/DVAAAg0/4QF5nKuAAAAABJRU5ErkJggg==';
// The candidate wire formats the probe tries, in order. Must stay in sync with
// IMAGE_WIRE_FORMATS in openai-http.mjs (the adapter's pluggable shape table).
const IMAGE_PROBE_FORMATS = ['openai-parts', 'gemini-inline', 'openai-input-image'];

async function startServer({ html, baseURL, apiKey, gitlabCreds, defaultModel, port = 18888, adapter: providedAdapter, modelsList, initialProject = null, apiFormat = null, bundleConversation = false, whatsNewPath = null, imageSupportPath = null }) {
  // Migrate legacy ~/.code-boss/ → ~/.code_boss/ (hyphen → underscore) if a
  // user is upgrading from an older bundle. Best-effort; falls through if
  // the legacy dir is absent or permissions block the rename.
  await migrateLegacyUserDir();
  // If a key was passed in (build-time CONFIG.API_KEY), use it as the default.
  // Otherwise check ~/.code_boss/api-key.txt — populated when the user enters
  // the key via the UI on first run.
  let resolvedKey = apiKey || '';
  if (!resolvedKey) {
    const fromFile = await loadApiKeyFromFile();
    if (fromFile) resolvedKey = fromFile;
  }
  // Same pattern for GitLab credentials: caller (tests / build-time
  // config) wins, otherwise fall back to the on-disk file.
  let resolvedGitlab = gitlabCreds || null;
  if (!resolvedGitlab) {
    resolvedGitlab = await loadGitLabCredsFromFile();
  }

  const session = {
    // Static config
    startedAt: new Date().toISOString(),
    baseURL: baseURL ?? '(dev)',
    defaultModel,
    apiKey: resolvedKey,               // mutable so /api/api-key can update at runtime
    apiKeyPrefix: (resolvedKey || '').slice(0, 8) + (resolvedKey ? '…' : '(empty)'),
    // Optional GitLab credentials for plugins that pull files/trees from
    // a self-hosted GitLab via the v4 API. Loaded from disk if present;
    // user can set/replace via the UI. Mutable so /api/gitlab-creds
    // updates the running session.
    gitlabCreds: resolvedGitlab,
    startMs: Date.now(),
    activeProject: null,
    recent: await loadRecentProjects(),

    // Legacy transaction log (kept for /api/snapshot debugging)
    events: [],
    totalTokens: 0,

    // ── Chat state (Phase 1 of server-as-source-of-truth refactor) ──
    // Monotonic counter — every state change bumps it; browsers detect gaps.
    version: 0,
    // chatEvents: the ordered list the UI renders. Each: { id, appendedAtVersion, taskId, type, ...payload }
    chatEvents: [],
    nextEventId: 1,
    // Lifecycle
    status: 'idle',           // 'idle' | 'running' | 'done' | 'error'
    phase: 'idle',            // 'idle' | 'uploading' | 'thinking' | 'downloading'
    error: null,
    // Token counters maintained server-side (browser used to do this)
    chatTokens: { up: 0, down: 0 },
    // Server-enforced timeout for the current chat (settable per /api/extend-timeout)
    chatTimeoutMs: 60 * 60 * 1000,   // WebLogic full builds run 10-15 min; a run may chain several
    chatStartedAt: null,
    // WebLogic server status when the wl plugin is active: null = not monitored
    // (wl inactive / unknown), true = RUNNING (port 7101 in use), false = STOPPED.
    // Polled by the port monitor, broadcast on change, and injected into every
    // worker turn so the agent knows whether it needs `wl startWebLogic`.
    weblogicRunning: null,
    // Abort signal for the running agent loop (null when idle)
    abortController: null,
    // SSE subscribers (Phase 2 will populate; Set of res objects)
    streamSubscribers: new Set(),
    // Pending API-key unlock. When the LLM endpoint returns 401 with an
    // unlock_url, the adapter calls onUnlock(url) and awaits its Promise.
    // We surface the URL via SSE so the browser shows a modal; the user
    // clicks "I've unlocked" → POST /api/unlock-done → resolvers are called.
    // Concurrent 401s reuse the same pending entry (one modal, many
    // resolvers).
    pendingUnlock: null,  // { url, resolvers: [...] }

    // Mid-run "steer" channel: strings the developer queued via /api/steer
    // while a run is in flight. The worker loop drains this at each turn
    // boundary (see drainSteer below). Reset at the start of every run.
    steerBuffer: [],

    // ── Assignment manager (backlog) ──
    // The durable assignment backlog store (createAssignmentsStore), set per
    // project in openProject. Separate from the conversation + task stores.
    assignments: null,
    // The assignment currently being worked (its id), or null. Bound to closed
    // tasks during a run; cleared in the chat finally block.
    activeAssignment: null,
    // The in-flight conversational-intake AbortController, or null when idle.
    // Doubles as the busy-flag (non-null => an intake turn is running) AND the
    // cancel handle. Intake takes NO running-lock (it is read-only and must not
    // block or be blocked by worker chat), so this is its own concurrency gate.
    intakeAbort: null,
    // True while a post-acceptance memory-consolidation pass is running (a
    // fire-and-forget read-only LLM pass); guards against overlap.
    memoryConsolidating: false,
    // AbortController for the in-flight consolidation pass (so it can be
    // cancelled on project close / shutdown), or null when idle.
    consolidationAbort: null,
  };

  // ── Project cwd: RETIRED (the global process.chdir is gone). ────────────────
  // We no longer mutate the process-wide cwd. Every cwd-dependent path now
  // resolves against an EXPLICIT per-call directory: the worker passes
  // ctx.cwd=projectDir into the tools (toolRoot(ctx) in tools.mjs), the read-only
  // passes thread projectDir (makeRepoResolver + ctx.cwd), the docx/wl plugins
  // read ctx.cwd, and git-workflow's runGit(cwd,…) was always explicit. This is
  // what lets concurrent agent threads target different repos. enterProjectCwd/
  // exitProjectCwd are now no-op stubs so their (vestigial) call sites stay valid
  // — they MUST NOT touch process.cwd(); remove the call sites in a follow-up.
  function enterProjectCwd(project) { return !!project; }
  function exitProjectCwd(_entered) { /* no-op: cwd is per-call now */ }

  // Bumps version, returns the new value. Used by all chat-state setters.
  function bumpVersion() { session.version += 1; return session.version; }

  // Rough token estimate (~4 chars/token) for tracking input/output sizes.
  function estimateTokens(s) { return Math.ceil((s ?? '').length / 4); }

  // ── Chat-state mutators ─────────────────────────────────────────────────
  // Each mutator updates state, bumps version, and (in Phase 2) broadcasts
  // a delta to all SSE subscribers. For now the broadcast is a no-op stub.
  function broadcast(message) {
    // Phase 2 will fill this in. message: { type, version, ...payload }
    for (const res of session.streamSubscribers) {
      try { res.write(`data: ${JSON.stringify(message)}\n\n`); }
      catch { /* dead subscriber; will be cleaned on next iteration */ }
    }
  }
  function setStatus(status) {
    if (session.status === status) return;
    session.status = status;
    broadcast({ type: 'status', version: bumpVersion(), status });
  }
  function setPhase(phase) {
    if (session.phase === phase) return;
    session.phase = phase;
    broadcast({ type: 'phase', version: bumpVersion(), phase });
  }
  function setError(err) {
    session.error = err == null ? null : (err.message || String(err));
    broadcast({ type: 'error', version: bumpVersion(), error: session.error });
  }
  function setTimeout_(ms) {
    session.chatTimeoutMs = Number(ms) || session.chatTimeoutMs;
    broadcast({ type: 'timeout', version: bumpVersion(), timeoutMs: session.chatTimeoutMs });
  }
  function updateTokens(delta) {
    if (delta.up) session.chatTokens.up += delta.up;
    if (delta.down) session.chatTokens.down += delta.down;
    broadcast({ type: 'tokens', version: bumpVersion(), tokens: { ...session.chatTokens } });
  }
  function resetTokens() {
    session.chatTokens = { up: 0, down: 0 };
    broadcast({ type: 'tokens', version: bumpVersion(), tokens: { ...session.chatTokens } });
  }
  // Append a new chat event. Assigns id + appendedAtVersion. Returns the event.
  function appendChatEvent(event) {
    // A message-refile is a re-attribution, not a new row: re-tag an existing
    // event's taskId (used when a just-arrived user message is moved to the task
    // that actually handles it). Patch in place + broadcast — never append. The
    // patch persists to conversation.json so a reload files the message
    // correctly too.
    if (event && event.type === 'message-refile') {
      return patchChatEvent(event.eventId, { taskId: event.toTaskId });
    }
    const id = `e${session.nextEventId++}`;
    const ev = { id, appendedAtVersion: session.version + 1, ...event };
    // Ephemeral events are broadcast for live UI updates but NOT persisted —
    // either because they'd bloat the conversation file (tool-progress
    // streams stdout/stderr from builds) or because there's a canonical
    // final event covering the same content (assistant-text-chunk → the
    // matching assistant-text emitted at segment end).
    // 'context-fill' is a per-turn gauge update (window fill %): broadcast it
    // live but never persist — it fires every turn and would bloat the file.
    // ('compacting-started' IS persisted — it's a rare, meaningful transcript marker.)
    // Sub-agent activity (tagged subagentChannel) is broadcast live to that sub-agent's
    // detail drawer but never persisted to the main chat log — it would pollute the
    // foreground transcript + the reviewer/attribution filters. The completion CARD
    // (type:'subagent-card', untagged) IS persisted.
    // subagentChannel-tagged events are drawer-routed + ephemeral (live-only) EXCEPT 'review'
    // rows, which must persist so a forked task's pending→pass/fail patch can find them, and
    // 'task-end' rows — the compact canonical task record (taskId, userRequest, summary) that
    // BOTH quality gates key on: the end-of-task reviewer trigger and the acceptance <work>
    // evidence filter. Dropping a forked run's task-ends silently disabled both gates.
    const ephemeral = event.type === 'tool-progress' || event.type === 'assistant-text-chunk' || event.type === 'context-fill' || event.type === 'task-progress' || event.type === 'checklist' || event.type === 'rollback-suggested' || event.type === 'cognition-event' || (!!event.subagentChannel && event.type !== 'review' && event.type !== 'task-end');
    // Stash the latest context-window fill so /api/diagnostics (Snapshot query
    // 24) can surface it — a large/over-budget prompt means the agent is
    // dragging a huge open task each turn (e.g. a model that never emits
    // <next-task>), the runaway-context failure that 502'd in production.
    if (event.type === 'context-fill') {
      session.lastContextFill = { totalTokens: event.totalTokens, budgetTokens: event.budgetTokens, budgetPct: event.budgetPct, at: Date.now() };
      // Task-store breakdown rides along on the per-turn gauge (the server can't
      // read the task store without blocking sync I/O). Stashed for the Snapshot.
      if (event.taskStats) session.lastTaskStats = { ...event.taskStats, at: Date.now() };
    }
    // Track the ACTIVE checklist (main chat only) so it auto-drives the live panel: it carries
    // across the propose→approve→implement boundary AND is pinned back into the worker's context
    // each turn (the <checklist> tag is stripped from history, so the agent can't otherwise see
    // its own prior list — without this it would drop/restate it inconsistently). Cleared once
    // every item is done.
    if (event.type === 'checklist' && !event.subagentChannel) {
      const items = Array.isArray(event.items) ? event.items : [];
      session.lastChecklist = (items.length && items.some((i) => !i.done)) ? items : null;
    }
    if (!ephemeral) session.chatEvents.push(ev);
    broadcast({ type: 'event', version: bumpVersion(), event: ev });
    if (!ephemeral) scheduleConversationSave();
    return ev;
  }
  // Patch an existing event in place (e.g., to mute tool result on tier 1→2 demotion).
  function patchChatEvent(eventId, patch) {
    const ev = session.chatEvents.find((e) => e.id === eventId);
    if (!ev) return null;
    Object.assign(ev, patch);
    broadcast({ type: 'event-patch', version: bumpVersion(), eventId, patch });
    scheduleConversationSave();
    return ev;
  }
  // Replace entire chat state (used by /api/reset).
  function resetChatState() {
    session.chatEvents = [];
    session.nextEventId = 1;
    session.lastChecklist = null;   // drop any active checklist on reset/clear
    // Drop the in-memory task store so the NEXT run recreates it (clear deletes the store on disk;
    // with the orchestrator default-on the store is created eagerly, so without this the stale
    // in-memory store would suppress recreation and state.json would never come back).
    session.store = null;
    session.storeReady = null;
    session.status = 'idle';
    session.phase = 'idle';
    session.error = null;
    session.chatTokens = { up: 0, down: 0 };
    session.chatStartedAt = null;
    session.abortController = null;
    broadcast({ type: 'reset', version: bumpVersion() });
    // Don't write an empty conversation back to disk — the caller (clearConversation
    // or project switch) is responsible for cleaning up the file.
  }
  // Per-project conversation persistence. Writes session.chatEvents +
  // nextEventId to <project>/.code_boss/conversation.json so a chat
  // survives restarts. Debounced so a burst of events (tool call, result,
  // assistant text in quick succession) writes once.
  const CONVERSATION_FILE = 'conversation.json';
  let _saveTimer = null;
  function conversationFilePath() {
    return session.activeProject ? path.join(session.activeProject, '.code_boss', CONVERSATION_FILE) : null;
  }
  function scheduleConversationSave() {
    if (!session.activeProject) return;
    if (_saveTimer) return;
    _saveTimer = setTimeout(() => {
      _saveTimer = null;
      flushConversationSave();
    }, 300);
  }
  // Choose a next-event-id counter that can NEVER collide with an already-
  // loaded event id. Event ids are `e<n>`; nextEventId is normally persisted,
  // but ephemeral events (tool-progress / assistant-text-chunk) bump the
  // counter without being saved, so chatEvents.length is an unsafe fallback
  // (it under-counts). Take the max of the persisted counter and (highest
  // existing numeric id + 1).
  function nextIdFor(data) {
    let maxId = 0;
    for (const ev of data.chatEvents) {
      const m = /^e(\d+)$/.exec(ev?.id || '');
      if (m) { const n = Number(m[1]); if (n > maxId) maxId = n; }
    }
    return Math.max(Number(data.nextEventId) || 0, maxId + 1);
  }
  function flushConversationSave() {
    const file = conversationFilePath();
    if (!file) return;
    try {
      mkdirSync(path.dirname(file), { recursive: true });
      const data = {
        version: 1,
        savedAt: new Date().toISOString(),
        nextEventId: session.nextEventId,
        chatEvents: session.chatEvents,
        lastChecklist: session.lastChecklist || null,   // so a server restart restores the live panel + the pin
      };
      // ATOMIC write: serialize to a temp sibling, keep the last-good file as
      // .bak, then rename temp onto the live file. rename is atomic, so a
      // process killed mid-write never leaves conversation.json half-written
      // (the truncated bytes land in .tmp; the live file is replaced in one
      // step or not at all). Without this, a Ctrl-C / power-loss mid-write
      // corrupted the only copy and loadConversation silently dropped the
      // entire history.
      const tmp = file + '.tmp';
      const bak = file + '.bak';
      writeFileSync(tmp, JSON.stringify(data), 'utf8');
      try { if (existsSync(file)) copyFileSync(file, bak); } catch {}
      renameSync(tmp, file);
    } catch (e) {
      fileLog(`conversation save failed: ${e.message}`);
    }
  }
  async function loadConversation() {
    if (_saveTimer) { clearTimeout(_saveTimer); _saveTimer = null; }
    const file = conversationFilePath();
    if (!file) return;
    const tryParse = async (f) => {
      const raw = await readFile(f, 'utf8');
      const data = JSON.parse(raw);
      if (!data || !Array.isArray(data.chatEvents)) throw new Error('invalid shape');
      return data;
    };
    let data = null, recoveredFrom = null;
    try {
      data = await tryParse(file);
    } catch (e) {
      if (e.code === 'ENOENT') return; // no conversation yet — fine
      // Main file is corrupt/half-written. Fall back to the last-good .bak
      // rather than silently losing everything. Do NOT overwrite/delete the
      // corrupt main file — leave it for manual salvage.
      fileLog(`conversation load failed (${e.message}); trying .bak`);
      try { data = await tryParse(file + '.bak'); recoveredFrom = '.bak'; }
      catch { fileLog(`conversation .bak also unusable; starting fresh (corrupt file left in place)`); return; }
    }
    if (data) {
      session.chatEvents = data.chatEvents;
      session.nextEventId = nextIdFor(data);
      // Restore the active checklist so a server restart re-pins it + the UI re-shows the panel.
      session.lastChecklist = (Array.isArray(data.lastChecklist) && data.lastChecklist.length) ? data.lastChecklist : null;
      fileLog(`loaded conversation: ${data.chatEvents.length} event(s) from ${file}${recoveredFrom ? ' (recovered from .bak)' : ''}`);
    }
  }
  async function clearConversation() {
    if (_saveTimer) { clearTimeout(_saveTimer); _saveTimer = null; }
    resetChatState();
    const file = conversationFilePath();
    if (file) {
      try { await unlink(file); fileLog(`conversation cleared: ${file}`); }
      catch (e) { if (e.code !== 'ENOENT') fileLog(`conversation delete failed: ${e.message}`); }
    }
    // ALSO wipe the task store — otherwise the prior session's (possibly
    // over-budget) task stays "current", so the next message appends to that
    // huge task, auto-closes it with a meaningless summary, and the context
    // gauge is full on what should be a fresh chat. The worker's init()
    // recreates a clean T-1 when the dir is empty.
    if (session.activeProject) {
      const tasksDir = path.join(session.activeProject, '.code_boss', 'tasks');
      try {
        const entries = await readdir(tasksDir);
        await Promise.all(entries.map((f) => unlink(path.join(tasksDir, f)).catch(() => {})));
        fileLog(`task store cleared: ${tasksDir} (${entries.length} file(s))`);
      } catch (e) { if (e.code !== 'ENOENT') fileLog(`task store clear failed: ${e.message}`); }
    }
  }
  // Snapshot of chat state for /api/state.
  function chatStateSnapshot({ eventLimit } = {}) {
    const events = eventLimit && eventLimit < session.chatEvents.length
      ? session.chatEvents.slice(-eventLimit)
      : session.chatEvents;
    return {
      version: session.version,
      status: session.status,
      phase: session.phase,
      tokens: { ...session.chatTokens },
      timeoutMs: session.chatTimeoutMs,
      chatStartedAt: session.chatStartedAt,
      error: session.error,
      activeProject: session.activeProject,
      defaultModel: session.defaultModel,
      // Always-responsive: when on (+ git repo), a message sent while the foreground is
      // busy is delegated to a background sub-agent instead of queued/409'd. The client
      // uses this to route Enter-while-running to send() rather than the queue.
      orchestrator: CB_ORCHESTRATOR,
      recent: session.recent,
      events,
      hasMoreOlder: !!(eventLimit && eventLimit < session.chatEvents.length),
      // If an unlock is in flight, include the URL so a freshly-connecting
      // browser can re-show the modal rather than miss the broadcast.
      pendingUnlockUrl: session.pendingUnlock?.url ?? null,
      // WebLogic server status for the footer indicator (null = wl inactive /
      // not monitored, true = running on :7101, false = stopped).
      weblogicRunning: session.weblogicRunning ?? null,
      plugins: pluginManager ? pluginManager.snapshot() : [],
      projectContextFiles: session.projectContextFiles || [],
      git: session.git || { isRepo: false },
      // Surface pause-and-resolve state so the client can show the
      // git-needs-branch modal (and tests can wait for it). Internal
      // resolver Promises aren't serializable — strip to bare metadata.
      gitPending: session.gitPending
        ? Object.fromEntries(Object.entries(session.gitPending).map(([k, v]) => [k, { repoDir: k, currentBranch: v.currentBranch }]))
        : null,
      // The active checklist, so a reload/reconnect restores the live panel (the server is still
      // pinning it into the worker each turn — without this the panel goes blank until the next turn).
      lastChecklist: session.lastChecklist || null,
      // Shadow-git undo availability (non-repo targets): drives the "undo last run" button.
      canUndo: !!(session.shadowUndo && session.shadowUndo.commit),
      undoRequest: (session.shadowUndo && session.shadowUndo.request) || null,
      // Live sub-agent rows (orchestrator mode): the busy-time fork (if any) plus every
      // live orchestrator entry (working / awaiting-review / recovered). Mirrors the
      // lastChecklist precedent above — the SSE broadcasts alone can't rebuild the panel
      // on a reload/second tab, and the crash-restart recoverSubagents broadcast fires
      // before any client is connected, so the snapshot is the only resync path.
      subagents: [
        ...(session.fork ? [{ id: session.fork.id, topic: session.fork.topic, status: 'working' }] : []),
        ...(session.orchestrator ? session.orchestrator.subagents() : []),
      ],
    };
  }
  // Expose helpers so we can use them from /api/chat and from new endpoints later.
  const chatState = {
    bumpVersion, broadcast, setStatus, setPhase, setError, setTimeout: setTimeout_,
    updateTokens, resetTokens, appendChatEvent, patchChatEvent,
    resetChatState, chatStateSnapshot, clearConversation,
  };

  // Append-only project log at <project>/.code_boss/code-boss.log. Synchronous
  // writes so a hang leaves a complete log up to the last operation.
  //
  // Always mirrors to stderr too — so even if the project dir is unwritable
  // (permissions, disk full, etc.), the user can still see what happened in
  // the PowerShell window where the bundle was launched. Logging silence is
  // the worst kind of bug; this guarantees something is always visible.
  let loggedFileWriteWarning = false;       // suppress duplicate "file log unavailable" spam
  const logFilePath = () => (session.activeProject ? path.join(session.activeProject, '.code_boss', 'code-boss.log') : null);
  function fileLog(msg) {
    const line = `${new Date().toISOString()}  ${msg}`;
    // 1) Always print to stderr — guaranteed visible in the launcher window.
    try { process.stderr.write(line + '\n'); } catch {}
    // 2) Append to <project>/.code_boss/code-boss.log if we can. Always
    //    mkdirSync (no caching) so an external `git clean -fdx` that wipes
    //    the dir between writes doesn't leave subsequent writes stranded —
    //    mkdir with recursive:true is a no-op when the dir already exists.
    if (!session.activeProject) return;
    try {
      const dir = path.join(session.activeProject, '.code_boss');
      mkdirSync(dir, { recursive: true });
      appendFileSync(path.join(dir, 'code-boss.log'), line + '\n');
    } catch (e) {
      if (!loggedFileWriteWarning) {
        process.stderr.write(`[fileLog] file write FAILED (will continue with stderr only): ${e.message}\n`);
        process.stderr.write(`[fileLog] attempted path: ${path.join(session.activeProject ?? '?', '.code_boss', 'code-boss.log')}\n`);
        loggedFileWriteWarning = true;
      }
    }
  }

  // ── MULTI-INSTANCE per-project lock ─────────────────────────────────────
  // Two Code Boss processes sharing one <project>/.code_boss store corrupt
  // task + conversation state. Opening a project takes
  // <project>/.code_boss/instance.lock ({pid, port, startedAt}); a lock held
  // by a LIVE different pid refuses the open, a stale lock (dead pid) is
  // taken over silently. The lock is removed on close-project and on clean
  // shutdown — and every failure on the write/release side is tolerated (the
  // lock must never block a close or a shutdown).
  let _instanceLockHeld = null;   // absolute path of the lock file we own
  const instanceLockPathFor = (proj) => path.join(proj, '.code_boss', 'instance.lock');
  function acquireInstanceLock(proj) {
    const lockPath = instanceLockPathFor(proj);
    let other = null;
    try { other = JSON.parse(readFileSync(lockPath, 'utf8')); } catch { /* missing/corrupt → treated as stale */ }
    const otherPid = Number(other?.pid);
    if (otherPid && otherPid !== process.pid) {
      let alive = false;
      try { process.kill(otherPid, 0); alive = true; }
      catch (e) { alive = e?.code === 'EPERM'; }   // EPERM = alive but not ours to signal
      if (alive) {
        // No port number — "port 17xxx" means nothing to the user; point at the other WINDOW.
        throw new Error('This project is already open in another Code Boss window. Use that window, or close it there first.');
      }
    }
    try {
      mkdirSync(path.dirname(lockPath), { recursive: true });
      writeFileSync(lockPath, JSON.stringify({ pid: process.pid, port, startedAt: new Date().toISOString() }));
    } catch { /* unwritable store — never block the open on lock bookkeeping */ }
    return lockPath;
  }
  function releaseInstanceLock(lockPath = _instanceLockHeld) {
    if (lockPath) {
      try {
        // Delete only a lock WE wrote — never another live instance's.
        const cur = JSON.parse(readFileSync(lockPath, 'utf8'));
        if (Number(cur?.pid) === process.pid) unlinkSync(lockPath);
      } catch { /* tolerated: never block close/shutdown on the lock */ }
    }
    if (!lockPath || lockPath === _instanceLockHeld) _instanceLockHeld = null;
  }

  async function openProject(p) {
    if (typeof p !== 'string' || !p) throw new Error('invalid project path');
    try {
      const s = await stat(p);
      if (!s.isDirectory()) throw new Error('not a directory');
    } catch (e) { throw new Error('cannot open: ' + e.message); }
    // One-time migration: if a legacy .agent/ dir exists and the new
    // .code_boss/ doesn't, rename it so prior logs + task state carry over.
    // If both exist, leave them — user can clean up manually.
    try {
      const oldDir = path.join(p, '.agent');
      const newDir = path.join(p, '.code_boss');
      const oldStat = await stat(oldDir).catch(() => null);
      if (oldStat?.isDirectory()) {
        const newStat = await stat(newDir).catch(() => null);
        if (!newStat) {
          await rename(oldDir, newDir);
        }
      }
    } catch { /* migration is best-effort */ }
    // MULTI-INSTANCE: refuse to open a project that a DIFFERENT live Code
    // Boss process already holds (see acquireInstanceLock above). Throwing
    // here leaves the current session completely untouched.
    const newInstanceLock = acquireInstanceLock(p);
    // Flush any pending save for the project we're leaving; otherwise the
    // debounced write would land in the wrong directory.
    if (_saveTimer) { clearTimeout(_saveTimer); _saveTimer = null; flushConversationSave(); }
    // Reset in-memory chat state before loading the new project's so
    // events from the previous project don't leak.
    resetChatState();
    session.activeProject = p;
    session.recent = [p, ...session.recent.filter((x) => x !== p)].slice(0, MAX_RECENT);
    await saveRecentProjects(session.recent);
    logEvent('project-opened', { path: p });
    fileLog(`=== project opened: ${p} ===`);
    // The open is committed: release the lock of the project we just left
    // (if any) and remember the one we now hold.
    if (_instanceLockHeld && _instanceLockHeld !== newInstanceLock) releaseInstanceLock(_instanceLockHeld);
    _instanceLockHeld = newInstanceLock;
    // Restore the conversation from disk (if one was saved previously).
    // Broadcast a snapshot so any connected browser swaps to the restored
    // events instead of the empty post-reset state.
    await loadConversation();
    // Cancel any in-flight manager passes bound to the project we're leaving so
    // they don't keep running (and writing) against a now-stale project.
    try { session.intakeAbort?.abort(); } catch {}
    session.intakeAbort = null;
    try { session.consolidationAbort?.abort(); } catch {}
    session.consolidationAbort = null;
    // Load the durable assignment backlog for this project.
    session.activeAssignment = null;
    try {
      session.assignments = createAssignmentsStore(p);
      await session.assignments.init();
    } catch (e) {
      session.assignments = null;
      fileLog(`assignments store init failed: ${e.message}`);
    }
    broadcast({ type: 'reset', version: bumpVersion() });
    for (const ev of session.chatEvents) {
      broadcast({ type: 'event', version: bumpVersion(), event: ev });
    }
    // Scan for AGENTS.md files (legacy CODE-BOSS.md still honored) and
    // cache the prompt-ready string. Also re-runs every PLUGIN_REDETECT_MS
    // (see the interval below) so edits to those files take effect within
    // ~5 min without a project reopen.
    await refreshProjectContext({ openLog: true });
    // Git workflow: branch automatically onto CODE_BOSS_<timestamp>
    // if we're opening a non-CODE_BOSS branch with a clean tree. If
    // the tree is dirty, set a pending-wizard flag and skip the
    // branch creation — the user resolves via the wizard modal.
    await initGitWorkflow();
    // Crash-resume (R6): surface any leftover sub-agent worktrees/branches from a previous
    // session so they're never silently dropped (orchestrator mode only; no-op otherwise).
    try { await recoverSubagents(session); } catch (e) { fileLog(`recover: ${e.message}`); }
  }
  async function initGitWorkflow() {
    // Phase B: discover EVERY git repo under the active project (the
    // project itself if it has .git, otherwise sub-repos one or more
    // levels down). Each gets its own entry in session.git.repos with
    // current branch + head cached. We don't create branches here —
    // that happens lazily when a tool first writes to a file inside a
    // given repo (see resolveBranchForRepo in /api/message), with a
    // user-resolving modal for the non-CODE_BOSS case.
    session.git = { isRepo: false, repos: {}, repoList: [] };
    if (!session.activeProject) return;
    const repoDirs = await discoverGitRepos(session.activeProject);
    if (repoDirs.length === 0) {
      fileLog('git: no git repos found under active project — workflow disabled');
      return;
    }
    session.git.isRepo = true;
    for (const dir of repoDirs) {
      try {
        const added = await ensureExcluded(dir, '.code_boss/', '.code_boss');
        if (added) fileLog(`git: added .code_boss/ to ${dir}/.git/info/exclude`);
      } catch (e) { fileLog(`git: could not update ${dir}/.git/info/exclude: ${e.message}`); }
      const branch = await getCurrentBranch(dir);
      const head = await getHeadHash(dir);
      // Snapshot whether the repo already had uncommitted changes at open.
      // These are the USER's pre-existing work — we must NOT silently sweep
      // them onto a CODE_BOSS branch. When the agent first needs to change a
      // repo: clean-at-open → auto-create the CODE_BOSS branch; dirty-at-open
      // → pause and let the user decide. (Only meaningful while NOT already on
      // a CODE_BOSS branch.)
      const st0 = await getStatus(dir);
      // dirty===null means git-status FAILED (per getStatus contract) — treat as
      // dirty so a transient failure never lets the auto-branch path sweep
      // pre-existing user work onto a CODE_BOSS branch without pausing.
      // mode: 'direct' = uncommitted user work at open → work on the user's branch, NO sub-branch
      // (a warning + manual-commit row). 'subbranch' = clean at open, or already on a CODE_BOSS
      // branch → fork/use a CODE_BOSS branch (today's behavior, the merge row). A 'direct' repo flips
      // to 'subbranch' once its tree is fully committed (see reconcileGitModes).
      const mode = (isCodeBossBranch(branch) || st0.dirty === false) ? 'subbranch' : 'direct';
      session.git.repos[dir] = { dir, branch, head, dirtyAtOpen: st0.dirty !== false, mode };
      session.git.repoList.push(dir);
    }
    const rel = (d) => path.relative(session.activeProject, d) || '.';
    const summary = session.git.repoList
      .map((d) => `${rel(d)}=${session.git.repos[d].branch}`)
      .join(', ');
    fileLog(`git: found ${repoDirs.length} repo(s): ${summary}`);
  }
  // Flip a 'direct' repo to 'subbranch' once its working tree is fully committed — whether the app
  // committed it (commit-direct) or the user committed externally. Forks a CODE_BOSS branch from the
  // now-clean user branch. Only when IDLE (never switch branches mid-run). Idempotent; called from
  // the merge-status poll + after commit-direct.
  async function reconcileGitModes() {
    if (!session.git?.isRepo || session.status === 'running') return;
    for (const dir of (session.git.repoList || [])) {
      const repo = session.git.repos[dir];
      if (!repo || repo.mode !== 'direct') continue;
      try {
        // Serialize the dirty-check + fork through the per-repo git lock so two overlapping
        // merge-status polls (or a poll racing commit-direct) can't BOTH fork. Re-check mode INSIDE
        // the lock — a reconcile that waited has already flipped it to subbranch.
        await withRepoGitLock(dir, async () => {
          if (repo.mode !== 'direct') return;
          const branch = await getCurrentBranch(dir);
          if (isCodeBossBranch(branch)) { repo.mode = 'subbranch'; repo.branch = branch; return; }   // user switched manually
          const st = await getStatus(dir);
          if (st.dirty !== false) return;   // still dirty (or status failed) → stay direct
          const parent = branch;
          const r = await createBranch(dir);
          if (r.exitCode !== 0) { fileLog(`git: reconcile fork failed on ${dir}: ${r.stderr?.trim?.() || r.exitCode}`); return; }
          repo.parentBranch = parent; repo.branch = r.branch; repo.head = await getHeadHash(dir); repo.mode = 'subbranch';
          fileLog(`git: direct→subbranch — forked ${r.branch} from ${parent} (tree committed) on ${nodePath.relative(session.activeProject, dir) || '.'}`);
        });
      } catch (e) { fileLog(`git: reconcile error on ${dir}: ${e.message}`); }
    }
  }
  async function refreshProjectContext({ openLog = false } = {}) {
    if (!session.activeProject) {
      session.projectContext = '';
      session.projectContextFiles = [];
      return;
    }
    try {
      const { context, files } = await loadProjectContext(session.activeProject);
      const prev = session.projectContext || '';
      session.projectContext = context;
      session.projectContextFiles = files.map((f) => ({
        relDir: f.relDir, bytes: f.originalBytes, truncated: f.truncated,
      }));
      // Log on every project open, plus on any change after that.
      // No log when a periodic scan finds nothing new.
      const changed = context !== prev;
      if (files.length > 0 && (openLog || changed)) {
        fileLog(`project context: ${files.length} AGENTS.md file(s), ${context.length} chars${changed && !openLog ? ' (changed since last scan)' : ''}`);
      } else if (changed && !openLog) {
        fileLog(`project context: cleared (no AGENTS.md files found)`);
      }
    } catch (e) {
      session.projectContext = '';
      session.projectContextFiles = [];
      fileLog(`project context load failed: ${e.message}`);
    }
  }
  function logEvent(type, data) {
    const summary = (SUMMARIZE[type] ?? ((d) => JSON.stringify(d).slice(0, 200)))(data);
    session.events.push({ at: Date.now() - session.startMs, type, summary, ...truncateForSnapshot(data) });
  }
  function truncateForSnapshot(data) {
    // Keep raw request/response for the last transaction view in the modal,
    // but trim huge fields so the snapshot JSON stays manageable.
    if (!data || typeof data !== 'object') return { data };
    const out = { ...data };
    if (out.response && typeof out.response === 'object') {
      const r = out.response;
      const str = JSON.stringify(r);
      if (str.length > 20000) out.response = JSON.parse(str.slice(0, 20000) + '"truncated":true}');
    }
    return out;
  }

  // Plugin manager. Detects which plugins should be active by running each
  // plugin's trigger() against a PowerShell executor. Runs once immediately
  // at startup (no delay per user spec) and re-runs every 5 minutes so
  // newly-installed tools light up without a restart.
  const pluginManager = createPluginManager({ onLog: (m) => fileLog(`[plugin] ${m}`) });
  await pluginManager.detectAll();
  // The 5-min loop refreshes BOTH plugin enablement (in case a tool got
  // installed mid-session) AND project context (in case an AGENTS.md
  // file was edited). Same cadence on purpose — one timer, one source
  // of "stuff that might have changed on disk".
  const _pluginInterval = setInterval(() => {
    pluginManager.detectAll().catch((e) => fileLog(`[plugin] detect error: ${e.message}`));
    refreshProjectContext().catch((e) => fileLog(`[context] refresh error: ${e.message}`));
  }, PLUGIN_REDETECT_MS);
  // Don't keep the event loop alive just for the redetect timer — tests
  // call server.close() and expect the process to exit.
  if (typeof _pluginInterval.unref === 'function') _pluginInterval.unref();

  // ── WebLogic server monitor ────────────────────────────────────────────────
  // When the wl plugin is active, poll port 7101 (a plain TCP connect — no deps,
  // no spawns) so the UI can show a running/stopped indicator and every worker
  // turn can be told whether the WebLogic server is up. Self-gates on wl being
  // enabled; broadcasts only on change. (`wl startWebLogic` launches the server
  // in the background; this is how its readiness becomes visible.)
  const WEBLOGIC_PORT = Number(process.env.CODEBOSS_WL_PORT) || 7101;
  function checkPortInUse(port, host = '127.0.0.1', timeoutMs = 1500) {
    return new Promise((resolve) => {
      const sock = new net.Socket();
      let done = false;
      const finish = (inUse) => { if (done) return; done = true; try { sock.destroy(); } catch {} resolve(inUse); };
      sock.setTimeout(timeoutMs);
      sock.once('connect', () => finish(true));   // something is listening -> server up
      sock.once('timeout', () => finish(false));
      sock.once('error', () => finish(false));    // ECONNREFUSED etc. -> nothing there
      try { sock.connect(port, host); } catch { finish(false); }
    });
  }
  function wlPluginEnabled() {
    try { return (pluginManager.snapshot() || []).some((p) => p.name === 'wl' && p.enabled); }
    catch { return false; }
  }
  async function pollWeblogic() {
    const active = wlPluginEnabled();
    const next = active ? await checkPortInUse(WEBLOGIC_PORT) : null;
    if (next !== session.weblogicRunning) {
      session.weblogicRunning = next;
      broadcast({ type: 'weblogic-status', version: bumpVersion(), running: next, port: WEBLOGIC_PORT });
    }
  }
  pollWeblogic().catch(() => {});
  const _weblogicInterval = setInterval(() => { pollWeblogic().catch(() => {}); }, 5000);
  if (typeof _weblogicInterval.unref === 'function') _weblogicInterval.unref();

  // Use a caller-provided adapter (e.g. the dev claude-code-headless adapter)
  // if given; otherwise default to an HTTP adapter. AGENT_API_FORMAT=anthropic
  // selects the Anthropic Messages adapter (e.g. a local Claude proxy); the
  // default is the OpenAI-compatible adapter (api.genai.mil / Gemini).
  const _adapterOpts = {
    baseURL,
    // Pass a getter so hot-updates to session.apiKey (via POST /api/api-key)
    // take effect on the next request without re-creating the adapter.
    apiKey: () => session.apiKey,
    // The adapter awaits this promise when it hits a 401+unlock_url. We
    // broadcast the URL to the browser, queue a resolver, and the user
    // clicking "I've unlocked" (POST /api/unlock-done) resolves us.
    onUnlock: (url) => new Promise((resolve) => {
      logEvent('unlock', { url });
      if (session.pendingUnlock) {
        fileLog(`unlock-needed (coalesce): adding resolver to existing pending unlock`);
        session.pendingUnlock.resolvers.push(resolve);
        return;
      }
      fileLog(`unlock-needed: broadcasting SSE; url=${url}; subscribers=${session.streamSubscribers.size}`);
      session.pendingUnlock = { url, resolvers: [resolve] };
      chatState.broadcast({
        type: 'unlock-needed',
        version: chatState.bumpVersion(),
        url,
      });
    }),
    onRateLimit: (ms) => logEvent('rate-limit', { ms }),
    // Network retry/recover: surface a visible "Reconnecting…" status (the
    // backoff is otherwise indistinguishable from a slow turn). setPhase
    // broadcasts to the chat status row; recovery flips it back to working.
    onNetworkRetry: ({ attempt, max, cause }) => {
      logEvent('network-retry', { attempt, max, cause });
      chatState.setPhase(`reconnecting:${attempt}/${max}`);
    },
    onNetworkRecover: () => chatState.setPhase('thinking'),
    onUsage: (u) => {
      session.totalTokens += u.total_tokens ?? 0;
      // Feed the response-side token count into chatTokens so the browser's
      // status row can show ↓ <N> tokens instead of stuck at 0. Each agent
      // turn adds its own completion_tokens; resetTokens() at chat start
      // clears the counter so the cumulative count is per-message-not-session.
      // (We don't add prompt_tokens to .up because each agent turn re-sends
      // growing context, which would falsely inflate "↑" across turns. The
      // estimateTokens at message send is good enough for the up-side hint.)
      if (u.completion_tokens) chatState.updateTokens({ down: u.completion_tokens });
      logEvent('usage', u);
    },
    onTransaction: (t) => logEvent(t.type, t),
  };
  // Resolved once so buildDiagnostics (catalog 43 "Backend / adapter") can
  // report which API format the live adapter is actually speaking.
  const resolvedApiFormat = (apiFormat === 'anthropic' || process.env.AGENT_API_FORMAT === 'anthropic')
    ? 'anthropic' : 'openai';
  // Discovered image-format key ({ format, testedAt, model }) — written by
  // POST /api/probe-images, read by the openai-http adapter per request.
  // Tests override the location via the imageSupportPath option.
  const imageSupportFile = imageSupportPath || nodePath.join(USER_CONFIG_DIR, 'image-support.json');
  _adapterOpts.imageSupportPath = imageSupportFile;
  const _makeAdapter = resolvedApiFormat === 'anthropic' ? createAnthropicAdapter : createHttpAdapter;
  const adapter = providedAdapter ?? _makeAdapter(_adapterOpts);

  // ── Always-responsive orchestrator (CB_ORCHESTRATOR) ─────────────────────────
  // A sub-agent runs as a full worker in its OWN git worktree (subagent-worktree.mjs):
  // physically isolated writes, merged back on completion; a merge conflict surfaces on
  // the completion card (the manager re-grounds). The orchestrator creates the per-sub-
  // agent AbortController + passes its signal here; the shared `adapter` is the transport.
  const _seedText = (seed) => (Array.isArray(seed) ? seed.map((m) => (m && m.content) || '').join('\n').trim() : String(seed || ''));
  const _renderConvo = (msgs) => (Array.isArray(msgs)
    ? msgs.slice(-40).map((m) => `${m.role || 'user'}: ${typeof m.content === 'string' ? m.content : JSON.stringify(m.content)}`).join('\n')
    : String(msgs || ''));

  // The sub-agent runWorker: a FULL worker (runPromptedAgent) HELD in its own git worktree
  // (R1). On resume (reject feedback) it re-runs in the SAME worktree. Same shared adapter +
  // plugin tools as the foreground; tool activity tagged onto the sub-agent's own channel.
  function makeWorktreeRunWorker(session) {
    const workerOpts = (id) => ({
      adapter, model: session.defaultModel,
      getGitLabCredentials: () => session.gitlabCreds,
      extraTools: pluginManager.enabledTools(),
      extraVerbs: pluginManager.enabledVerbs(),
      promptAdditions: pluginManager.promptAdditions(),
      projectContext: session.projectContext || '',
      // Sub-agents run autonomously in the background (their output is gated by the acceptance/review
      // loop, not per-edit prompts), so auto-approve consequential ops — without this, a tool that
      // calls ctx.requireApproval (e.g. edit_docx format-flatten) would silently error in a sub-agent.
      requireApproval: async () => true,
      onChatEvent: (ev) => { try { chatState.appendChatEvent({ ...ev, subagentChannel: id }); } catch {} },
      onLog: (m) => fileLog(`[${id}] ${m}`),
      // Each sub-agent carries its OWN parallel reviewer when the developer enabled it (the reviewer
      // runs with parallelCritic:false, so a reviewer never spawns a reviewer — no runaway nesting).
      parallelCritic: session.reviewerOn,
      // deep_think mirrors the reviewer: a held sub-agent gets the verb only when the user's chip
      // was on for the spawning message (session.deepThinkOn snapshot), with its own per-run budget.
      // Unset (no message yet) → undefined → the engine's CB_DEEP_THINK opt-in default (off).
      deepThink: session.deepThinkOn,
    });
    return (seed, { id, topic, signal, resume, handle }) => {
      if (resume && handle) {
        const feedback = (seed && seed[seed.length - 1] && seed[seed.length - 1].content) || 'Revise per the review feedback.';
        return resumeWorktreeSubAgent({ worktree: handle.worktree, branch: handle.branch, feedback, topic, signal, ...workerOpts(id) });
      }
      return runWorktreeSubAgent({ repoDir: session.activeProject, id, topic, held: true, signal, messages: seed, ...workerOpts(id) });
    };
  }

  // ── Adaptive cognition (CB_COGNITION) ──────────────────────────────────────
  // Map the cognition scheduler's injected primitives onto real agents over the orchestrator.
  // Cost is NOMINAL work-units per primitive type (matches the abstract complexity-budget;
  // real-token costing is a later refinement). Workers run isolated in git worktrees + appear
  // in the existing sub-agents panel/drawer; the cognition TREE streams via onEvent.
  const COGNITION_COSTS = { assess: 1, proposer: 3, judge: 4, decompose: 3, worker: 10, verify: 4, synthesize: 2 };
  const COGNITION_PROPOSERS = 3;
  // NOTE: DEEP_THINK_LENSES (src/agent/prompted.mjs) is a deliberate local copy of this list — keep the two in sync.
  const COGNITION_LENSES = ['favor the simplest thing that works', 'favor long-term extensibility and clean design', 'favor minimal risk to existing behavior'];
  function createCognitionPrimitives(session, { signal, onEvent = () => {} } = {}) {
    const model = session.defaultModel;
    const repoRoots = session.git?.repoList || [];
    const projectDir = session.activeProject;
    let seq = 0;
    const workerCfg = (id) => ({
      adapter, model,
      getGitLabCredentials: () => session.gitlabCreds,
      extraTools: pluginManager.enabledTools(), extraVerbs: pluginManager.enabledVerbs(), promptAdditions: pluginManager.promptAdditions(),
      projectContext: session.projectContext || '',
      onChatEvent: (ev) => { try { chatState.appendChatEvent({ ...ev, subagentChannel: id }); } catch {} },
      onLog: (m) => fileLog(`[${id}] ${m}`),
      // NO parallelCritic here: cognition workers run held:false → runSubAgent (engine-only), which has
      // no reviewer machinery — the option would be silently dropped. Cognition workers are covered by
      // the separate verify primitive; the HELD orchestrator path (workerOpts above) does carry one.
      // Same limitation for deepThink: runSubAgent has no deep_think dispatch (its allowlist drops the
      // verb), so the option isn't passed here either — only HELD sub-agents (workerOpts) inherit it.
    });
    return {
      async assess(brief) { const r = await runCognitiveAssess({ adapter, model, signal, request: brief, projectDir, repoRoots, onLog: (m) => fileLog(`[cog assess] ${m}`) }); return { ...r, cost: COGNITION_COSTS.assess }; },
      async explore(brief) {
        // N INDEPENDENT proposers (each via a distinct lens, in parallel) → a JUDGE that
        // picks/synthesizes. Beats one agent listing options. Falls back to the single-agent
        // runExplore if every proposer fails.
        const N = COGNITION_PROPOSERS;
        onEvent({ type: 'explore-start', n: N });
        const proposals = (await Promise.all(Array.from({ length: N }, (_, i) => {
          onEvent({ type: 'proposer', i: i + 1, lens: COGNITION_LENSES[i % COGNITION_LENSES.length] });
          return runProposer({ adapter, model, signal, brief, lens: COGNITION_LENSES[i % COGNITION_LENSES.length], projectDir, repoRoots }).catch(() => null);
        }))).filter((p) => p && p.approach);
        if (!proposals.length) { const r = await runExplore({ adapter, model, signal, brief, projectDir, repoRoots }); return { ...r, cost: COGNITION_COSTS.judge }; }
        onEvent({ type: 'judge', proposals: proposals.length });
        const j = await runExploreJudge({ adapter, model, signal, brief, proposals, projectDir, repoRoots });
        return { brief: j.brief, cost: proposals.length * COGNITION_COSTS.proposer + COGNITION_COSTS.judge };
      },
      async decompose(brief) { const r = await runDecompose({ adapter, model, signal, brief, projectDir, repoRoots }); return { ...r, cost: COGNITION_COSTS.decompose }; },
      async runWorker(brief) {
        const id = `cog-${++seq}`;
        const topic = String(brief).slice(0, 60).replace(/\s+/g, ' ').trim() || 'task';
        onEvent({ type: 'worker-start', id, topic });
        chatState.broadcast({ type: 'subagent-event', version: chatState.bumpVersion(), event: { type: 'subagent-spawned', id, topic } });
        let r;
        try { r = await runWorktreeSubAgent({ repoDir: projectDir, id, topic, held: false, signal, seedMessages: [{ role: 'user', content: String(brief) }], ...workerCfg(id) }); }
        catch (e) { r = { content: `(worker failed: ${e && e.message ? e.message : String(e)})`, error: true }; }
        chatState.broadcast({ type: 'subagent-event', version: chatState.bumpVersion(), event: { type: 'subagent-done', id, status: r.error ? 'error' : 'done' } });
        chatState.appendChatEvent({ type: 'subagent-card', subagentId: id, topic, status: r.error ? 'error' : 'done', content: r.content || '' });
        return { content: r.content || '', cost: COGNITION_COSTS.worker, integrated: !!r.integrated };
      },
      async verify(brief, result) { const r = await runSubAgentAcceptance({ adapter, model, signal, request: brief, conversationChunk: '', workRollup: (result && result.content) || '', worktreePath: projectDir, repoRoots }); return { ok: !!r.approved, feedback: r.feedback, cost: COGNITION_COSTS.verify }; },
      async synthesize(brief, stepResults) { const r = await runSynthesize({ adapter, model, signal, brief, stepResults }); return { content: r.content, cost: COGNITION_COSTS.synthesize }; },
    };
  }
  async function runCognitionForMessage(session, text, signal) {
    const onEvent = (cog) => { try { chatState.appendChatEvent({ type: 'cognition-event', cog }); } catch {} };
    const primitives = createCognitionPrimitives(session, { signal, onEvent });
    const out = await runCognition({ request: text, primitives, k: 0.8, budgetOpts: { base: 30 }, inlineSingle: true, allowDecompose: CB_DECOMPOSE, onEvent, signal });
    if (out.inline) return { inline: true };   // single coherent task → the caller runs the plain inline worker
    let answer;
    if (out.answer != null) answer = out.answer;
    else if (out.result && out.result.content) answer = out.result.content;
    else if (out.result && Array.isArray(out.result.steps)) answer = out.result.steps.map((s) => (s && s.content) || '').filter(Boolean).join('\n\n') || '(completed the decomposed steps)';
    else answer = '(cognition produced no direct answer)';
    return { answer, state: out.state };
  }

  // Lazy per-session orchestrator (built only when CB_ORCHESTRATOR is on). The review loop
  // is engaged because runAcceptance is injected: a held sub-agent → Acceptance agent →
  // approve (import into the MANAGER's single store + integrate + remove) | reject (feedback
  // → resume). session.store is the single-writer import target (the foreground worker keeps
  // its own private store untouched). Verdict + feedback are visible on BOTH sides.
  // The SINGLE long-lived session store (orchestrator mode only). The foreground worker AND
  // the sub-agent import share this ONE instance so there is one nextId + one in-memory state
  // → no two-instance task-id collision / state.json lost-update on the same .code_boss.
  // Returns a promise that resolves once init() completes (root exists before any importThread).
  function ensureSessionStore(session) {
    if (!session.store) { session.store = createTasksStore(session.activeProject); session.storeReady = session.store.init(); }
    return session.storeReady;
  }
  // Crash-resume (R6): re-attach leftover sub-agent worktrees as recovered (awaiting-review)
  // entries so the user can re-review or discard them. Branch-only leftovers (worktree gone)
  // are logged but not adopted (no worktree to review; the branch is kept for manual merge).
  // This runs during project open — BEFORE any client connects on a crash-restart — so the
  // subagent-recovered broadcast usually lands on nobody. Visibility doesn't depend on it:
  // adopt() puts the entry in the orchestrator's live map, and chatStateSnapshot.subagents
  // serves that map to every later /api/state fetch (the UI seeds its panel from there).
  async function recoverSubagents(session) {
    if (!CB_ORCHESTRATOR || !session.git || !session.git.isRepo || !session.activeProject) return;
    const orphans = await listOrphanSubagents(session.activeProject);
    if (!orphans.length) return;
    const orch = ensureOrchestrator(session);
    let adopted = 0, branchOnly = 0;
    for (const o of orphans) {
      if (!o.worktree) { branchOnly++; continue; }
      const r = orch.adopt({ id: o.id, topic: `recovered: ${o.id}`, handle: { branch: o.branch, worktree: o.worktree, storeDir: nodePath.join(o.worktree, '.code_boss', 'tasks'), store: null } });
      if (r) adopted++;
    }
    fileLog(`recover: surfaced ${adopted} recovered sub-agent(s)${branchOnly ? ` (+${branchOnly} branch-only leftover(s) kept for manual merge)` : ''} from a previous session`);
  }
  // The MANAGER TURN (the freed foreground answers inline OR delegates, one read-only LLM
  // call). SINGLE-FLIGHT: messages are queued + drained SERIALLY so two messages fired in
  // quick succession run their manager turns one-at-a-time (each answered/delegated in order,
  // no interleaved prose) rather than concurrently. /api/message returns 200 immediately; the
  // drain runs async, concurrent with the forked task's in-flight tool.
  function runManagerTurnAsync(session, text) {
    session.managerQueue = session.managerQueue || [];
    session.managerQueue.push(text);
    if (session.managerDraining) return;                 // a drain is already processing the queue
    session.managerDraining = true;
    (async () => {
      try { while (session.managerQueue.length) await runOneManagerTurn(session, session.managerQueue.shift()); }
      finally { session.managerDraining = false; session.managerBusy = false; }
    })().catch((e) => { session.managerDraining = false; session.managerBusy = false; fileLog(`[manager] drain leaked: ${e && e.message}`); });
  }
  async function runOneManagerTurn(session, text) {
    const orch = ensureOrchestrator(session);
    // History = the visible foreground transcript (NOT session.store — the forked worker is
    // writing that). Drawer-tagged (sub-agent) events are excluded by design.
    const history = (session.chatEvents || [])
      .filter((e) => (e.type === 'user-message' || e.type === 'assistant-text') && !e.subagentChannel && typeof e.content === 'string')
      .slice(-40)
      .map((e) => ({ role: e.type === 'user-message' ? 'user' : 'assistant', content: e.content }));
    chatState.appendChatEvent({ type: 'user-message', content: text });   // the manager's own foreground row
    const subagentStatus = orch.subagents().map((s) => `${s.id} (${s.topic}): ${s.status}`).join('\n');
    const ac = new AbortController();
    session.managerAbort = ac;   // so /api/cancel can stop an in-flight manager turn (it has its own controller)
    session.managerBusy = true;
    try {
      const result = await runManagerTurn({
        adapter, model: session.defaultModel, signal: ac.signal,
        onLog: (m) => fileLog(`[manager] ${m}`),
        messages: history, newMessage: text, subagentStatus,
        projectDir: session.activeProject, repoRoots: session.git?.repoList || [],
        onProse: ({ turn, segmentIndex, content }) => chatState.appendChatEvent({ type: 'assistant-text-chunk', turn, segmentIndex, content }),
        onProseEnd: ({ turn, segmentIndex, content }) => { if (content && content.trim()) chatState.appendChatEvent({ type: 'assistant-text', turn, segmentIndex, content }); },
      });
      if (result && result.action === 'delegate' && result.brief) {
        const topic = result.brief.slice(0, 60).replace(/\s+/g, ' ').trim() || 'task';
        const { subagentId } = orch.delegate(topic, result.brief);
        chatState.appendChatEvent({ type: 'assistant-text', content: `◇ Delegated to a sub-agent: ${topic}` });
        fileLog(`[manager] delegated to ${subagentId}: "${topic}"`);
      }
    } catch (e) {
      const aborted = e && e.name === 'AbortError';
      chatState.appendChatEvent({ type: 'assistant-text', content: `(manager turn ${aborted ? 'canceled' : 'failed'}: ${e.message})` });
      fileLog(`[manager] turn error: ${e.message}`);
    } finally {
      if (session.managerAbort === ac) session.managerAbort = null;
    }
  }
  function ensureOrchestrator(session) {
    if (!session.orchestrator) {
      const storeReady = ensureSessionStore(session);   // ready before the first importThread (root must exist)
      session.orchestrator = createOrchestrator({
        runWorker: makeWorktreeRunWorker(session),
        runAcceptance: async (ctx) => {
          const h = ctx.handle || {};
          let rollup = '';
          try {
            const sub = createTasksStore(h.worktree); await sub.init();
            const tasks = await sub.exportTasks();
            rollup = tasks.map((t, i) => `#${i + 1} ${t.userRequest || '(no request)'}\n  summary: ${t.summary || '(none)'}`).join('\n');
          } catch (e) { fileLog(`[acceptance ${ctx.id}] rollup read failed: ${e.message}`); }
          return runSubAgentAcceptance({
            adapter, model: session.defaultModel, signal: ctx.signal,
            onLog: (m) => fileLog(`[acceptance ${ctx.id}] ${m}`),
            request: _seedText(ctx.originalRequest),
            conversationChunk: _renderConvo(ctx.conversationChunk),
            workRollup: rollup,
            worktreePath: h.worktree,
          });
        },
        onApprove: async ({ id, topic, handle }) => {
          await storeReady;
          // INTEGRATE FIRST: a conflict re-grounds with NOTHING imported (so a conflict
          // re-ground loop can't duplicate the thread); the single import below runs only
          // once, on the eventually-clean merge.
          const merged = await integrate(session.activeProject, handle.branch);
          if (!merged.merged) return { conflict: true, files: merged.files || [] };
          let imported = true;
          try {
            const sub = handle.store || createTasksStore(handle.worktree);
            if (typeof sub.init === 'function') await sub.init();
            const exported = await sub.exportTasks();
            if (exported.length) await session.store.importThread(exported, { subagentId: id, topic });
          } catch (e) { imported = false; fileLog(`subagent import failed for ${id}: ${e.message} — preserving worktree ${handle.worktree} for recovery`); }
          // Only tear down the worktree if the transcript was safely imported; on import
          // failure keep it so the work is recoverable (the code already merged).
          if (imported) await removeWorktree(session.activeProject, handle.worktree, { deleteBranch: handle.branch });
          return { integrated: true };
        },
        onReject: ({ id, feedback }) => {
          // feedback is visible on BOTH sides: a persisted main-chat row + an ephemeral drawer copy
          try {
            chatState.appendChatEvent({ type: 'acceptance-feedback', subagentId: id, feedback });
            chatState.appendChatEvent({ type: 'acceptance-feedback', subagentId: id, feedback, subagentChannel: id });
          } catch {}
        },
        // despawn cleanup for NON-approve terminals (approve already cleaned up): free the
        // worktree dir; delete the branch only on abort (cancelled) — keep it on
        // error/rejected/exhausted so the work is recoverable for manual follow-up.
        onDespawn: async ({ id, status, handle }) => {
          if (!handle || !handle.worktree) return;
          try {
            await removeWorktree(session.activeProject, handle.worktree, { deleteBranch: status === 'aborted' ? handle.branch : null });
            fileLog(`[onDespawn ${id}] cleaned worktree (status=${status}; branch ${status === 'aborted' ? 'deleted' : 'kept'})`);
          } catch (e) { fileLog(`[onDespawn ${id}] cleanup failed: ${e.message}`); }
        },
        onEvent: (e) => {
          try {
            chatState.broadcast({ type: 'subagent-event', version: chatState.bumpVersion(), event: e });
            if (e.type === 'subagent-verdict') {   // the both-sides bridge: a main-chat row + a drawer copy
              const row = { type: 'acceptance-verdict', subagentId: e.id, topic: e.topic, verdict: e.approved ? 'approve' : 'reject', rationale: e.feedback || '' };
              chatState.appendChatEvent(row);
              chatState.appendChatEvent({ ...row, subagentChannel: e.id });
            }
          } catch {}
        },
        onCard: (card) => {
          try {
            chatState.appendChatEvent({ type: 'subagent-card', subagentId: card.subagentId, topic: card.topic, status: card.status, content: card.content });
          } catch {}
        },
      });
    }
    return session.orchestrator;
  }

  async function readBody(req) {
    const chunks = [];
    for await (const c of req) chunks.push(c);
    return Buffer.concat(chunks).toString('utf8');
  }
  function sendJson(res, status, body) {
    const s = JSON.stringify(body);
    res.writeHead(status, { 'content-type': 'application/json', 'content-length': Buffer.byteLength(s) });
    res.end(s);
  }
  function sendText(res, status, text, type = 'text/plain') {
    // no-store: the bundled HTML changes on every rebuild; Chrome caches
    // localhost HTML aggressively, which makes "I deployed the new bundle but
    // the page looks the same" the most common false-positive bug report.
    res.writeHead(status, {
      'content-type': type,
      'content-length': Buffer.byteLength(text),
      'cache-control': 'no-store, must-revalidate',
    });
    res.end(text);
  }

  const server = createServer(async (req, res) => {
    try {
      const url = new URL(req.url, `http://${req.headers.host}`);
      const path = url.pathname;

      if (req.method === 'GET' && path === '/') {
        return sendText(res, 200, html, 'text/html; charset=utf-8');
      }
      if (req.method === 'GET' && path === '/snapshot') {
        // Standalone diagnostics page — works even when the main UI is wedged.
        return sendText(res, 200, SNAPSHOT_PAGE_HTML, 'text/html; charset=utf-8');
      }
      if (req.method === 'GET' && path === '/api/health') {
        return sendJson(res, 200, { ok: true });
      }
      // API key: status (does the server have a key?) + set (POST a new one).
      // First-run UI calls /api/api-key with status=GET; if hasKey=false, it
      // prompts the user and POSTs the value, which gets persisted to
      // ~/.code_boss/api-key.txt and applied to the running session.
      if (req.method === 'GET' && path === '/api/api-key') {
        return sendJson(res, 200, {
          hasKey: !!session.apiKey,
          prefix: session.apiKeyPrefix,
        });
      }
      if (req.method === 'POST' && path === '/api/unlock-done') {
        // User clicked "I've unlocked" in the browser modal. Resolve every
        // pending adapter call so they all retry their requests.
        if (session.pendingUnlock) {
          const { resolvers } = session.pendingUnlock;
          fileLog(`unlock-done: resolving ${resolvers.length} pending adapter call(s)`);
          session.pendingUnlock = null;
          chatState.broadcast({ type: 'unlock-done', version: chatState.bumpVersion() });
          for (const r of resolvers) { try { r(); } catch {} }
        } else {
          fileLog('unlock-done: no pending unlock (already resolved or stale click)');
        }
        return sendJson(res, 200, { ok: true });
      }
      if (req.method === 'POST' && path === '/api/api-key') {
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const key = (body?.key ?? '').toString().trim();
        if (!key) return sendJson(res, 400, { error: 'key is required' });
        try { await saveApiKeyToFile(key); }
        catch (e) { return sendJson(res, 500, { error: 'could not save key: ' + e.message }); }
        session.apiKey = key;
        session.apiKeyPrefix = key.slice(0, 8) + '…';
        fileLog(`api-key set via UI: prefix=${session.apiKeyPrefix}`);
        return sendJson(res, 200, { ok: true, prefix: session.apiKeyPrefix });
      }
      if (req.method === 'GET' && path === '/api/gitlab-creds') {
        // Same shape as /api/api-key: report presence + a non-secret
        // hint (the username), never the token. Used by the UI's
        // startup-prompt flow.
        return sendJson(res, 200, {
          hasCreds: !!(session.gitlabCreds && session.gitlabCreds.token),
          user: session.gitlabCreds?.user || '',
        });
      }
      if (req.method === 'POST' && path === '/api/gitlab-creds') {
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const user = (body?.user ?? '').toString().trim();
        const token = (body?.token ?? '').toString().trim();
        if (!user || !token) return sendJson(res, 400, { error: 'user and token are required' });
        try { await saveGitLabCredsToFile({ user, token }); }
        catch (e) { return sendJson(res, 500, { error: 'could not save credentials: ' + e.message }); }
        session.gitlabCreds = { user, token };
        fileLog(`gitlab-creds set via UI: user=${user} token_prefix=${token.slice(0, 4)}…`);
        return sendJson(res, 200, { ok: true, user });
      }
      if (req.method === 'GET' && path === '/api/project-state') {
        // Legacy endpoint kept for the existing browser project picker.
        // New code should use /api/state.
        return sendJson(res, 200, { activeProject: session.activeProject, recent: session.recent });
      }
      if (req.method === 'POST' && path === '/api/pick-folder') {
        const chosen = await pickFolderNative();
        return sendJson(res, 200, { path: chosen });
      }
      if (req.method === 'GET' && path === '/api/app-url') {
        // Construct a deployed app's URL (https://host:7102/context-root) WITHOUT
        // opening a browser, for the UI "Open" button. The browser can't read
        // ~/myWlSetup.bat or the app's application.xml; the server can. Returns
        // 200 with {ok:false,reason} (not a 4xx) when the URL can't be computed —
        // that's an expected, non-error outcome (deploy still succeeded).
        const app = (url.searchParams.get('app') || '').trim();
        if (!app) return sendJson(res, 400, { ok: false, error: 'app is required' });
        if (!session.activeProject) return sendJson(res, 409, { ok: false, error: 'no project open' });
        try {
          const r = await computeAppUrl(app, session.activeProject);
          return sendJson(res, 200, r);
        } catch (e) {
          return sendJson(res, 200, { ok: false, reason: 'error', detail: e?.message || String(e) });
        }
      }
      if (req.method === 'GET' && path === '/api/list-dir') {
        // In-browser folder picker. Returns subdirs of `path` (or the home
        // dir if path is empty or '~'), plus parent + breadcrumb so the UI
        // can render navigation without re-parsing paths. Hidden dirs
        // (leading '.') are filtered out — they're noise for picking a
        // project root and would let the user dig into things like
        // .git/objects/ by accident.
        let target = url.searchParams.get('path') || '';
        if (!target) {
          // DEFAULT start path (fresh open, no last-used project): prefer D:\
          // — the conventional dev drive here — and fall back gracefully to
          // the home dir when it doesn't exist (non-Windows, or no D: drive).
          try { target = statSync('D:\\').isDirectory() ? 'D:\\' : homedir(); }
          catch { target = homedir(); }
        }
        if (target === '~') target = homedir();
        // On Windows, an empty path or '/' means "list drives".
        if (process.platform === 'win32' && (target === '/' || target === '\\')) {
          const drives = [];
          for (const letter of 'ABCDEFGHIJKLMNOPQRSTUVWXYZ') {
            const root = letter + ':\\';
            try {
              statSync(root);
              drives.push({ name: letter + ':', path: root });
            } catch {}
          }
          return sendJson(res, 200, {
            path: '',
            parent: null,
            breadcrumb: [{ name: '(drives)', path: '/' }],
            entries: drives,
            isDriveList: true,
          });
        }
        const abs = nodePath.resolve(target);
        let entries = [];
        try {
          const names = await readdir(abs);
          for (const name of names) {
            if (name.startsWith('.')) continue;
            try {
              const s = await stat(nodePath.join(abs, name));
              if (s.isDirectory()) entries.push({ name, path: nodePath.join(abs, name) });
            } catch {}
          }
        } catch (e) {
          return sendJson(res, 400, { error: 'cannot read dir: ' + e.message });
        }
        entries.sort((a, b) => a.name.localeCompare(b.name));
        // Build breadcrumb: each path segment is one hop you can click.
        // On Windows we prepend a "(drives)" hop with path='/' so the user
        // can climb past the drive root back to the drive list.
        const segments = [];
        let cur = abs;
        while (true) {
          const parent = nodePath.dirname(cur);
          const name = nodePath.basename(cur) || cur;
          segments.unshift({ name, path: cur });
          if (parent === cur) break;
          cur = parent;
        }
        if (process.platform === 'win32') {
          segments.unshift({ name: '(drives)', path: '/' });
        }
        // Parent for "Up" button. On Windows root drive (C:\), parent
        // points to the drive list ('/'); on POSIX it's null (no further
        // hop possible).
        let parent = nodePath.dirname(abs);
        if (parent === abs) {
          parent = process.platform === 'win32' ? '/' : null;
        }
        return sendJson(res, 200, {
          path: abs,
          parent,
          breadcrumb: segments,
          entries,
          isDriveList: false,
        });
      }
      if (req.method === 'POST' && path === '/api/open-project') {
        const raw = await readBody(req);
        let body;
        try { body = JSON.parse(raw); }
        catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        try { await openProject(body.path); }
        catch (e) { return sendJson(res, 400, { error: e.message }); }
        return sendJson(res, 200, {
          activeProject: session.activeProject,
          recent: session.recent,
          git: session.git || { isRepo: false },
        });
      }
      if (req.method === 'POST' && path === '/api/git-resolve-batch') {
        // End-of-chat resolution: process one decision per repo listed
        // in a git-pending-resolution event. Body:
        //   { eventId, resolutions: [{ repoDir, action: 'codeboss'|'skip',
        //                              useAI?, message? }] }
        // 'codeboss' commits whatever's dirty (AI subject or typed),
        // forks CODE_BOSS_<ts>. 'skip' leaves the repo alone.
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); }
        catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const evId = body?.eventId;
        const ev = session.chatEvents.find((e) => e.id === evId && e.type === 'git-pending-resolution');
        if (!ev) return sendJson(res, 400, { error: 'no such pending-resolution event' });
        if (ev.resolved) return sendJson(res, 400, { error: 'already resolved' });
        const resolutions = Array.isArray(body.resolutions) ? body.resolutions : [];
        const outcomes = [];
        const emitGit = async (repoDir, cmdLabel, run) => {
          const callId = randomUUID();
          const rel = nodePath.relative(session.activeProject, repoDir) || '.';
          chatState.appendChatEvent({ type: 'tool-call', callId, verb: 'git', name: 'git', args: { command: `[${rel}] ${cmdLabel}` } });
          const r = await run();
          const status = r.exitCode === 0 ? 'ok' : 'error';
          const content = r.exitCode === 0 ? (r.stdout.trim() || '(ok)') : (r.stderr.trim() || `exit ${r.exitCode}`);
          chatState.appendChatEvent({ type: 'tool-result', callId, verb: 'git', name: 'git', status, content });
          return r;
        };
        for (const r of resolutions) {
          const repoDir = r?.repoDir;
          if (!repoDir || !session.git?.repos?.[repoDir]) {
            outcomes.push({ repoDir, status: 'error', error: 'unknown repo' });
            continue;
          }
          const repo = session.git.repos[repoDir];
          if (r.action === 'skip') { outcomes.push({ repoDir, status: 'skipped' }); continue; }
          if (r.action !== 'codeboss') {
            outcomes.push({ repoDir, status: 'error', error: 'unknown action' });
            continue;
          }
          let message = String(r.message || '').trim();
          const status = await getStatus(repoDir);
          if (status.dirty === false) {   // null (status failed) is NOT clean — attempt the commit
            outcomes.push({ repoDir, status: 'noop', note: 'tree clean' });
            continue;
          }
          const add = await emitGit(repoDir, 'git add -A', () => runGit(repoDir, ['add', '-A']));
          if (add.exitCode !== 0) { outcomes.push({ repoDir, status: 'error', error: 'git add failed' }); continue; }
          if (r.useAI) {
            const diff = await runGit(repoDir, ['diff', '--cached', 'HEAD']);
            const sample = (diff.stdout || '').slice(0, 12000);
            if (!sample.trim()) { outcomes.push({ repoDir, status: 'error', error: 'no diff' }); continue; }
            try {
              const aiR = await adapter.complete({
                model: session.defaultModel,
                messages: [
                  { role: 'system', content: 'You summarize git diffs into a single short imperative commit subject (max 70 chars). Respond with ONLY the subject line, no quotes, no prefix.' },
                  { role: 'user', content: `Diff:\n\n${sample}` },
                ],
              });
              message = ((aiR?.choices?.[0]?.message?.content) || '').trim().split('\n')[0].slice(0, 72) || 'pre-existing changes';
            } catch (e) {
              outcomes.push({ repoDir, status: 'error', error: 'AI summary failed: ' + e.message });
              continue;
            }
          }
          if (!message) { outcomes.push({ repoDir, status: 'error', error: 'message required' }); continue; }
          const commit = await emitGit(repoDir, `git commit -m "${message.replace(/"/g, '\\"')}"`, () => runGit(repoDir, ['commit', '-F', '-'], { input: message + '\n' }));
          if (commit.exitCode !== 0) { outcomes.push({ repoDir, status: 'error', error: 'git commit failed' }); continue; }
          const parentBranch = await getCurrentBranch(repoDir);
          // If the repo got moved onto a CODE_BOSS branch between
          // event-emit and resolve (e.g., user did it manually), skip
          // forking another one — commit-only is enough.
          if (isCodeBossBranch(parentBranch)) {
            repo.head = await getHeadHash(repoDir);
            outcomes.push({ repoDir, status: 'ok', commit: message, branch: parentBranch });
            continue;
          }
          const newBranchRes = await createBranch(repoDir);
          if (newBranchRes.exitCode !== 0) { outcomes.push({ repoDir, status: 'error', error: 'branch creation failed' }); continue; }
          const relB = nodePath.relative(session.activeProject, repoDir) || '.';
          const cidB = randomUUID();
          chatState.appendChatEvent({ type: 'tool-call', callId: cidB, verb: 'git', name: 'git', args: { command: `[${relB}] git checkout -b ${newBranchRes.branch}` } });
          chatState.appendChatEvent({ type: 'tool-result', callId: cidB, verb: 'git', name: 'git', status: 'ok', content: `forked from ${parentBranch}` });
          repo.parentBranch = parentBranch;
          repo.branch = newBranchRes.branch;
          repo.head = await getHeadHash(repoDir);
          fileLog(`git-resolve-batch: ${repoDir} → commit "${message}" on ${parentBranch}, forked ${newBranchRes.branch}`);
          outcomes.push({ repoDir, status: 'ok', commit: message, branch: newBranchRes.branch });
        }
        chatState.patchChatEvent(ev.id, { resolved: true, outcomes });
        return sendJson(res, 200, { eventId: ev.id, outcomes });
      }
      if (req.method === 'POST' && path === '/api/close-project') {
        // Reject while a chat is running — mirrors /api/reset's stance.
        // Otherwise the chat's still-running async loop would keep
        // touching session.git, session.activeProject, etc. after
        // they're cleared.
        if (session.status === 'running') {
          return sendJson(res, 409, { error: 'chat is running; cancel first' });
        }
        releaseInstanceLock();   // multi-instance: free the project for other instances
        session.activeProject = null;
        session.git = { isRepo: false };
        return sendJson(res, 200, { activeProject: null, recent: session.recent });
      }
      if (req.method === 'GET' && path === '/api/log') {
        if (!session.activeProject) return sendJson(res, 200, { log: '(no project open)', file: null });
        const file = logFilePath();
        try {
          const content = await readFile(file, 'utf8');
          const tail = content.length > 80000 ? content.slice(-80000) : content;
          return sendJson(res, 200, { log: tail, file, bytes: content.length });
        } catch (e) {
          return sendJson(res, 200, { log: `(no log yet — ${e.code || e.message})`, file });
        }
      }
      if (req.method === 'GET' && path === '/api/models') {
        // Dev mode passes a static modelsList; prod uses the adapter.
        if (modelsList) return sendJson(res, 200, { data: modelsList });
        const data = await adapter.listModels();
        return sendJson(res, 200, data);
      }
      if (req.method === 'GET' && path === '/api/snapshot') {
        // Aggregate parallel-tool-batch stats from every agent_run event.
        const agentRuns = session.events.filter((e) => e.type === 'agent_run');
        const allBatches = agentRuns.flatMap((e) => Array.isArray(e.batches) ? e.batches : []);
        const sum = allBatches.reduce((s, n) => s + n, 0);
        const parallelism = {
          runs: agentRuns.length,
          turnsWithTools: allBatches.length,
          totalToolCalls: sum,
          avgPerTurn: allBatches.length ? +(sum / allBatches.length).toFixed(2) : 0,
          maxParallel: allBatches.length ? Math.max(...allBatches) : 0,
          singleCallTurns: allBatches.filter((n) => n === 1).length,
          multiCallTurns: allBatches.filter((n) => n > 1).length,
          batches: allBatches,
        };
        return sendJson(res, 200, {
          startedAt: session.startedAt,
          baseURL: session.baseURL,
          defaultModel: session.defaultModel,
          apiKeyPrefix: session.apiKeyPrefix,
          activeProject: session.activeProject,
          totalTokens: session.totalTokens,
          parallelism,
          events: session.events,
        });
      }
      if (req.method === 'POST' && path === '/api/reset') {
        if (session.status === 'running') {
          return sendJson(res, 409, { error: 'chat is running; cancel first' });
        }
        session.events = [];
        session.totalTokens = 0;
        session.startMs = Date.now();
        chatState.resetChatState();
        return sendJson(res, 200, { ok: true, version: session.version });
      }
      if (req.method === 'POST' && path === '/api/clear-conversation') {
        // Wipes the persisted chat (in-memory + disk) for the active
        // project. Different from /api/reset, which only resets in-memory
        // state — clear also deletes the on-disk conversation file so it
        // won't reload on next project open.
        if (session.status === 'running') {
          return sendJson(res, 409, { error: 'chat is running; cancel first' });
        }
        await chatState.clearConversation();
        return sendJson(res, 200, { ok: true, version: session.version });
      }
      // Undo the last write-mode run on a NON-repo target (the shadow-git safety net).
      if (req.method === 'POST' && path === '/api/undo-last-run') {
        if (session.status === 'running') return sendJson(res, 409, { error: 'agent is running; cancel first' });
        const u = session.shadowUndo;
        if (!u || !u.commit) return sendJson(res, 200, { ok: false, note: 'nothing to undo' });
        // No post snapshot yet: the finally-time capture may simply not have landed (the
        // button keys on `commit`, so a click right after 'done' can beat it). The run is
        // over (status guard above), so capture ON DEMAND — the delta semantics stay
        // identical, and the no-change case clears + refuses with the friendly note. Only
        // if the capture itself fails do we refuse outright: the blanket reset+clean
        // fallback would delete files the user created by hand, never risk that.
        if (!u.postCommit) {
          try {
            const pc = await shadowSnapshot(session.activeProject, 'post-run');
            if (pc && pc !== u.commit) u.postCommit = pc;
            else if (pc && pc === u.commit) {
              session.shadowUndo = null;
              fileLog(`undo-last-run: on-demand post snapshot shows no changes for "${u.request}" — undo point cleared`);
              return sendJson(res, 200, { ok: false, note: 'nothing to undo (the run left no revertible changes)' });
            }
          } catch (e) { fileLog(`undo-last-run: on-demand post snapshot failed: ${e.message}`); }
        }
        if (!u.postCommit) {
          session.shadowUndo = null;
          fileLog(`undo-last-run refused: no post-run snapshot for "${u.request}" (delta unknown; destructive fallback disabled)`);
          return sendJson(res, 200, { ok: false, note: 'nothing to undo (the run left no revertible changes)' });
        }
        const reverted = await shadowRestore(u.dir, u.commit, u.postCommit);
        if (reverted) {
          session.shadowUndo = null;   // single-level undo for now (re-running re-snapshots)
          fileLog(`undo-last-run: reverted "${u.request}" via shadow-git`);
          return sendJson(res, 200, { ok: true, request: u.request });
        }
        return sendJson(res, 500, { ok: false, error: 'undo failed' });
      }

      // ── New chat-state endpoints ───────────────────────────────────────
      if (req.method === 'GET' && path === '/api/state') {
        const limit = Number(url.searchParams.get('limit')) || 100;
        return sendJson(res, 200, chatState.chatStateSnapshot({ eventLimit: limit }));
      }
      // ── Assignment backlog (the manager's durable store) ──
      if (req.method === 'GET' && path === '/api/assignments/list') {
        if (!session.assignments) return sendJson(res, 200, { assignments: [], activeId: null });
        return sendJson(res, 200, { assignments: await session.assignments.list(), activeId: session.assignments.activeId() });
      }
      if (req.method === 'POST' && path === '/api/assignments/create') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw || '{}'); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const a = await session.assignments.create({
          title: body.title || 'Untitled assignment',
          requirement: body.requirement ?? null,
          acceptanceCriteria: body.acceptanceCriteria ?? null,
          sources: Array.isArray(body.sources) ? body.sources : [],
          status: body.status,
        });
        broadcast({ type: 'backlog', version: bumpVersion() });
        fileLog(`assignment created: ${a.id} "${a.title}"`);
        return sendJson(res, 200, { assignment: a });
      }
      if (req.method === 'POST' && path === '/api/assignments/patch') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        if (!body.id) return sendJson(res, 400, { error: 'id required' });
        let a; try { a = await session.assignments.patch(body.id, body.fields || {}); } catch (e) { return sendJson(res, 400, { error: e.message }); }
        if (!a) return sendJson(res, 404, { error: 'no such assignment' });
        broadcast({ type: 'backlog', version: bumpVersion() });
        return sendJson(res, 200, { assignment: a });
      }
      if (req.method === 'POST' && path === '/api/assignments/reorder') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        try {
          if (Array.isArray(body.order)) await session.assignments.reorder(body.order);
          else if (body.id != null && body.toIndex != null) await session.assignments.move(body.id, body.toIndex);
          else return sendJson(res, 400, { error: 'provide order[] or {id, toIndex}' });
        } catch (e) { return sendJson(res, 400, { error: e.message }); }
        broadcast({ type: 'backlog', version: bumpVersion() });
        return sendJson(res, 200, { ok: true, activeId: session.assignments.activeId() });
      }
      if (req.method === 'POST' && path === '/api/assignments/start') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        if (session.status === 'running') return sendJson(res, 409, { error: 'a chat is already running' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const a = await session.assignments.get(body?.id);
        if (!a) return sendJson(res, 404, { error: 'no such assignment' });
        if (!a.requirement) return sendJson(res, 400, { error: 'assignment has no captured requirement yet — capture it first' });
        await session.assignments.setActive(a.id);
        await session.assignments.patch(a.id, { status: 'active' });
        session.activeAssignment = a.id;   // bind the upcoming run to this assignment
        // Resume = re-arming an assignment that already has work bound to it
        // (e.g. picked up in a later session). Reword the seed so the worker
        // continues rather than restarts.
        const isResume = body.resume === true || (Array.isArray(a.taskIds) && a.taskIds.length > 0);
        const seedText = (isResume
          ? `I'm resuming assignment ${a.id}: ${a.title} — continue where the previous work left off (review what's already done first).\n\nRequirement:\n${a.requirement}`
          : `I'm starting assignment ${a.id}: ${a.title}\n\nRequirement:\n${a.requirement}`)
          + (a.acceptanceCriteria ? `\n\nAcceptance criteria:\n${a.acceptanceCriteria}` : '')
          + `\n\nWork through this, making only the changes it requires, and break the work into <next-task> units as you go.`;
        broadcast({ type: 'backlog', version: bumpVersion() });
        fileLog(`assignment ${isResume ? 'resumed' : 'started'}: ${a.id} "${a.title}"`);
        // The caller now POSTs /api/message with seedText; activeAssignment is
        // already set so that run injects the requirement, stamps tasks, and
        // auto-runs acceptance at the end.
        return sendJson(res, 200, { ok: true, assignmentId: a.id, seedText });
      }
      if (req.method === 'POST' && (path === '/api/assignments/accept' || path === '/api/assignments/park')) {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        if (!body.id) return sendJson(res, 400, { error: 'id required' });
        const isAccept = path.endsWith('/accept');
        const status = isAccept ? 'accepted' : 'parked';
        const a = await session.assignments.patch(body.id, { status });
        if (!a) return sendJson(res, 404, { error: 'no such assignment' });
        if (session.assignments.activeId() === body.id) await session.assignments.setActive(null);
        broadcast({ type: 'backlog', version: bumpVersion() });
        fileLog(`assignment ${body.id} -> ${status}`);
        sendJson(res, 200, { assignment: a });
        // On ACCEPT, consolidate durable codebase facts from the completed work
        // into Tier-1 memory (fire-and-forget, read-only, fail-safe). Only when
        // there was real work and no other consolidation is already running.
        if (isAccept && session.activeProject && Array.isArray(a.taskIds) && a.taskIds.length && !session.memoryConsolidating) {
          session.memoryConsolidating = true;
          const projectAtAccept = session.activeProject;   // snapshot — may change under us
          const consAbort = new AbortController();
          session.consolidationAbort = consAbort;
          (async () => {
            const entered = enterProjectCwd(projectAtAccept);
            try {
              const taskIds = new Set(a.taskIds || []);
              const verdictByTask = new Map();
              for (const ev of session.chatEvents) {
                if (ev.type === 'review' && ev.reviewTaskId) verdictByTask.set(ev.reviewTaskId, ev.state === 'pass' ? { pass: true } : { pass: false, concerns: ev.concerns });
              }
              const tasks = session.chatEvents
                .filter((e) => e.type === 'task-end' && taskIds.has(e.taskId))
                .map((te) => ({ taskId: te.taskId, userRequest: te.userRequest, summary: te.summary, events: session.chatEvents.filter((e) => e.taskId === te.taskId), verdict: verdictByTask.get(te.taskId) }));
              const existingMemory = renderMemory(await loadMemory(projectAtAccept));
              const { facts } = await runConsolidation({
                adapter, model: session.defaultModel,
                assignment: { id: a.id, requirement: a.requirement },
                tasks, existingMemory, signal: consAbort.signal, onLog: fileLog, repoRoots: session.git?.repoList || [], projectDir: projectAtAccept,
                onDiag: (d) => appendChatEvent({ type: 'manager-diagnostic', assignmentId: a.id, ...d }),
              });
              const added = [];
              for (const f of (facts || [])) {
                try { const r = await rememberFact(projectAtAccept, f); if (r.added) added.push({ id: r.entry.id, category: r.entry.category, note: r.entry.note }); } catch {}
              }
              if (added.length) {
                appendChatEvent({ type: 'memory-consolidated', assignmentId: a.id, added });
                broadcast({ type: 'memory', version: bumpVersion() });
                fileLog(`memory consolidation: added ${added.length} fact(s) from ${a.id}`);
              } else {
                fileLog(`memory consolidation: nothing durable from ${a.id}`);
              }
            } catch (e) {
              fileLog(`memory consolidation failed (${a.id}): ${e?.message ?? e}`);
            } finally {
              exitProjectCwd(entered);
              if (session.consolidationAbort === consAbort) session.consolidationAbort = null;
              session.memoryConsolidating = false;
            }
          })();
        }
        return;
      }
      if (req.method === 'POST' && path === '/api/assignments/remove') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        if (!body.id) return sendJson(res, 400, { error: 'id required' });
        await session.assignments.remove(body.id);
        broadcast({ type: 'backlog', version: bumpVersion() });
        return sendJson(res, 200, { ok: true });
      }
      // Edit a closed task's summary (the tier-rollup working memory). Same
      // dual-write as the blank-summary backfill: patch the chat event (footer
      // + persisted transcript + acceptance input) AND the task store (the
      // rollup source that feeds future context).
      if (req.method === 'POST' && path === '/api/tasks/update-summary') {
        if (!session.activeProject) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const eventId = body?.eventId;
        const summary = String(body?.summary ?? '').trim();
        if (!eventId || !summary) return sendJson(res, 400, { error: 'eventId and non-empty summary required' });
        const te = session.chatEvents.find((e) => e.id === eventId && e.type === 'task-end');
        if (!te) return sendJson(res, 404, { error: 'task-end event not found' });
        chatState.patchChatEvent(eventId, { summary });
        if (te.taskId) {
          try {
            const ts = createTasksStore(session.activeProject);
            await ts.init();
            await ts.setSummary(te.taskId, summary);
          } catch (e) { fileLog(`update-summary setSummary failed for ${te.taskId}: ${e.message}`); }
        }
        fileLog(`task summary edited (${te.taskId || eventId}): ${summary.slice(0, 80)}`);
        return sendJson(res, 200, { eventId, summary });
      }
      // Undo a closed task: git revert --no-edit each commit recorded at close
      // (per-repo). Creates revert commits — non-destructive, auditable.
      if (req.method === 'POST' && path === '/api/task-undo') {
        if (!session.activeProject) return sendJson(res, 409, { error: 'no project open' });
        if (session.status === 'running') return sendJson(res, 409, { error: 'chat is running — stop it first' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const taskId = body?.taskId;
        if (!taskId) return sendJson(res, 400, { error: 'taskId required' });
        const te = session.chatEvents.find((e) => e.type === 'task-end' && e.taskId === taskId);
        if (!te) return sendJson(res, 404, { error: 'task not found' });
        // New tasks carry per-repo commits; fall back to the single commitHash
        // for tasks closed before the multi-repo fix.
        let commits = Array.isArray(te.commits) ? te.commits.filter((c) => c && c.hash) : [];
        if (!commits.length && te.commitHash) {
          const repoDir = session.git?.repoList?.[0];
          if (repoDir) commits = [{ repoDir, hash: te.commitHash }];
        }
        if (!commits.length) return sendJson(res, 400, { error: 'no commits recorded for this task' });
        const results = [];
        for (const { repoDir, hash } of commits) {
          if (!repoDir || !session.git?.repos?.[repoDir]) { results.push({ repoDir, hash, status: 'skipped', reason: 'repo not tracked' }); continue; }
          try {
            const r = await runGit(repoDir, ['revert', '--no-edit', hash]);
            if (r.exitCode === 0) { results.push({ repoDir, hash, status: 'ok' }); fileLog(`task-undo: reverted ${hash} in ${nodePath.relative(session.activeProject, repoDir) || '.'}`); }
            else {
              // A conflict (later commits touched the same lines) leaves the repo MID-REVERT with
              // conflict markers — abort so nothing is left half-done, and report it plainly.
              await runGit(repoDir, ['revert', '--abort']).catch(() => {});
              const out = (r.stderr || '') + (r.stdout || '');
              const message = /conflict|after resolving|could not revert/i.test(out)
                ? 'conflicts with later changes — the revert was aborted, nothing changed. Undo the newer task(s) first, or revert by hand.'
                : (out.trim().slice(0, 200) || `revert exit ${r.exitCode}`);
              results.push({ repoDir, hash, status: 'error', message });
              fileLog(`task-undo: revert failed in ${repoDir} (aborted): ${out.slice(0, 200)}`);
            }
          } catch (e) { results.push({ repoDir, hash, status: 'error', message: String(e.message).slice(0, 200) }); }
        }
        chatState.appendChatEvent({ type: 'task-undo', taskId, results });
        return sendJson(res, 200, { taskId, results });
      }
      // Mid-run "steer": queue text to inject at the worker's next turn
      // boundary (drained by drainSteer in runPromptedAgent). Returns at once;
      // the effect lands next turn, not now. Only valid while a run is active.
      if (req.method === 'POST' && path === '/api/steer') {
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const text = String(body?.text ?? '').trim();
        if (!text) return sendJson(res, 400, { error: 'text required' });
        if (session.status !== 'running') return sendJson(res, 409, { error: 'no run in progress to steer' });
        if (session.steerBuffer.length >= 10) return sendJson(res, 429, { error: 'steer buffer full (10) — wait for the agent to catch up' });
        session.steerBuffer.push(text);
        fileLog(`steer queued (${session.steerBuffer.length}): ${text.slice(0, 80)}`);
        return sendJson(res, 200, { ok: true, queued: session.steerBuffer.length });
      }
      // Conversational intake: a write-blocked MANAGER persona turns the user's
      // pasted material into an editable requirement card. NO running-lock (it
      // is read-only and must not block / be blocked by worker chat); its own
      // AbortController (session.intakeAbort) is both the busy-flag and the
      // cancel handle. The reply streams in as intake-message / requirement-card
      // chat events; multi-turn survives across POSTs by replaying the thread
      // persisted on the draft assignment as kind:'intake' sources.
      if (req.method === 'POST' && path === '/api/assignments/intake') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        if (session.intakeAbort) return sendJson(res, 409, { error: 'intake already running' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const text = (body?.text ?? '').toString().trim();
        if (!text) return sendJson(res, 400, { error: 'text required' });
        let a;
        if (body.assignmentId) {
          a = await session.assignments.get(body.assignmentId);
          if (!a) return sendJson(res, 404, { error: 'no such assignment' });
        } else {
          a = await session.assignments.create({ title: '(capturing…)', status: 'capturing' });
        }
        const assignmentId = a.id;
        // Persist this user turn into the durable intake thread + show it.
        await session.assignments.addSource(assignmentId, { kind: 'intake', role: 'user', text });
        appendChatEvent({ type: 'intake-message', assignmentId, senderRole: 'user', content: text });
        // Rebuild the conversation from the persisted thread (multi-turn across
        // separate POSTs); the just-appended user turn is included.
        const fresh = await session.assignments.get(assignmentId);
        const convoMessages = (fresh?.sources || [])
          .filter((s) => s.kind === 'intake')
          .map((s) => ({ role: s.role === 'manager' ? 'assistant' : 'user', content: s.text }));
        const intakeModel = body?.model ?? session.defaultModel;
        session.intakeAbort = new AbortController();
        const signal = session.intakeAbort.signal;
        broadcast({ type: 'backlog', version: bumpVersion() });
        fileLog(`intake START: ${assignmentId} text="${text.slice(0, 120).replace(/\s+/g, ' ')}"`);
        sendJson(res, 200, { ok: true, assignmentId, version: session.version });
        (async () => {
          const entered = enterProjectCwd(session.activeProject);
          try {
            // onTurn is awaited by runIntake (emit is async), so addSource here
            // is sequenced and its rejection is handled — not a floating promise.
            const onTurn = async (ev) => {
              if (ev.kind === 'message') {
                try { await session.assignments.addSource(assignmentId, { kind: 'intake', role: 'manager', text: ev.content }); } catch (e) { fileLog(`intake addSource failed: ${e.message}`); }
                appendChatEvent({ type: 'intake-message', assignmentId, senderRole: 'manager', content: ev.content });
              } else if (ev.kind === 'tools') {
                for (let k = 0; k < ev.calls.length; k++) {
                  const c = ev.calls[k]; const r = ev.results[k] || {};
                  appendChatEvent({ type: 'tool-call', callId: c.id, verb: c.verb, name: c.name ?? c.verb, args: c.args });
                  appendChatEvent({ type: 'tool-result', callId: c.id, verb: c.verb, name: c.name ?? c.verb, status: r.status || 'ok', content: (r.content || '').slice(0, UI_RESULT_PREVIEW), fullLen: (r.content || '').length });
                }
              } else if (ev.kind === 'card') {
                // Persist the card-bearing prose so a later "Refine" turn sees
                // its own prior card; render the editable card in the log.
                try { await session.assignments.addSource(assignmentId, { kind: 'intake', role: 'manager', text: ev.prose }); } catch (e) { fileLog(`intake addSource failed: ${e.message}`); }
                appendChatEvent({ type: 'requirement-card', assignmentId, card: ev.card });
              }
            };
            const result = await runIntake({
              adapter, model: intakeModel, messages: convoMessages, signal,
              onLog: fileLog, onTurn, repoRoots: session.git?.repoList || [], projectDir: session.activeProject,
              onDiag: (d) => appendChatEvent({ type: 'manager-diagnostic', assignmentId, ...d }),
            });
            if (!result.complete && result.reason === 'max-turns') {
              // Best-effort empty card so the user always gets editable fields.
              appendChatEvent({ type: 'requirement-card', assignmentId, card: { title: fresh.title === '(capturing…)' ? '' : (fresh.title || ''), requirement: '', acceptanceCriteria: '', sources: '' } });
            }
          } catch (e) {
            const msg = e?.name === 'AbortError' ? 'Intake cancelled.' : ('Intake error: ' + (e?.message ?? String(e)));
            appendChatEvent({ type: 'intake-message', assignmentId, senderRole: 'manager', content: msg });
            fileLog(`intake error (${assignmentId}): ${e?.message ?? e}`);
          } finally {
            exitProjectCwd(entered);
            session.intakeAbort = null;
          }
        })();
        return;
      }
      if (req.method === 'POST' && path === '/api/assignments/intake-cancel') {
        const raw = await readBody(req);
        let body; try { body = raw ? JSON.parse(raw) : {}; } catch { body = {}; }
        if (session.intakeAbort) { try { session.intakeAbort.abort(); } catch {} }
        // Reap the cancelled draft (the side-panel Cancel passes its assignmentId):
        // a 'capturing' placeholder that never produced a card is backlog noise, not
        // work worth keeping — without this, orphan '(capturing…)' rows accumulate.
        // Deliberately narrow: any draft that progressed past the placeholder title
        // (a card landed / the user renamed it) is left alone. Idempotent — a repeat
        // cancel or an already-gone draft is a quiet no-op.
        const cancelId = body?.assignmentId;
        if (cancelId && session.assignments) {
          try {
            const draft = await session.assignments.get(cancelId);
            if (draft && draft.status === 'capturing' && draft.title === '(capturing…)') {
              await session.assignments.remove(cancelId);
              broadcast({ type: 'backlog', version: bumpVersion() });
              fileLog(`intake-cancel: removed never-carded draft ${cancelId}`);
            }
          } catch (e) { fileLog(`intake-cancel draft cleanup failed: ${e.message}`); }
        }
        return sendJson(res, 200, { ok: true });
      }
      // Two-pane intake (the New-assignment modal): synchronous, NOT persisted and
      // NOT streamed into the main chat. Takes the running mini-chat + the current
      // editable field values (+ a slice of the main thread for relevance) and
      // returns the manager's reply and, when ready, a refined card to fill the
      // fields. The developer's field edits are sent each turn so the agent
      // refines from them rather than clobbering.
      if (req.method === 'POST' && path === '/api/intake-chat') {
        if (!session.activeProject) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const msgs = (Array.isArray(body?.messages) ? body.messages : [])
          .filter((m) => m && typeof m.content === 'string' && (m.role === 'user' || m.role === 'assistant'))
          .map((m) => ({ role: m.role, content: String(m.content) }));
        if (!msgs.length) return sendJson(res, 400, { error: 'messages required' });
        const f = body?.fields || {};
        const recent = session.chatEvents
          .filter((e) => e.type === 'user-message' || e.type === 'assistant-text')
          .slice(-8)
          .map((e) => `${e.type === 'user-message' ? 'Developer' : 'Agent'}: ${String(e.content || '').replace(/\s+/g, ' ').slice(0, 400)}`)
          .join('\n');
        const ctx = [
          'CONTEXT (do not echo this back). The developer is drafting a new assignment. Current editable field values (treat the developer\'s edits as authoritative — refine FROM them):',
          `  title: ${(f.title || '').trim() || '(empty)'}`,
          `  requirement: ${(f.requirement || '').trim() || '(empty)'}`,
          `  acceptance criteria: ${(f.acceptanceCriteria || '').trim() || '(empty)'}`,
          recent ? ('\nRecent main-chat activity (for relevance only, may be unrelated):\n' + recent) : '',
          '\nWhen the requirement is clear, emit the <requirement-card>; otherwise ask one focused question.',
        ].join('\n');
        const intakeModel = body?.model ?? session.defaultModel;
        const ac = new AbortController();
        const entered = enterProjectCwd(session.activeProject);
        // STREAM the planning agent's turns as the SAME event shapes the main
        // chat renders (tool-call / tool-result / assistant-text), one JSON object
        // per line (NDJSON), so the intake modal reuses renderEvent live.
        res.writeHead(200, { 'Content-Type': 'application/x-ndjson; charset=utf-8', 'Cache-Control': 'no-cache', 'X-Accel-Buffering': 'no' });
        res.on('close', () => { try { ac.abort(); } catch {} });
        const emit = (ev) => { try { res.write(JSON.stringify(ev) + '\n'); } catch {} };
        let nextCallId = 0;
        const stripCard = (s) => String(s || '').replace(/<requirement-card>[\s\S]*?<\/requirement-card>/i, '').trim();
        const onTurn = (t) => {
          if (!t) return;
          if (t.kind === 'tools') {
            const calls = t.calls || [], results = t.results || [];
            for (let i = 0; i < calls.length; i++) {
              const c = calls[i], cid = 'intake-' + (nextCallId++);
              emit({ type: 'tool-call', taskId: 'intake', verb: c.verb, name: c.name, args: c.args, callId: cid });
              const r = results[i];
              if (r) emit({ type: 'tool-result', taskId: 'intake', status: r.status, content: r.content, fullLen: (r.content || '').length, callId: cid, verb: c.verb, name: c.name });
            }
          } else if (t.kind === 'prose-chunk') {
            // Live prose: stream into a bubble exactly like the main chat.
            emit({ type: 'assistant-text-chunk', taskId: 'intake', turn: t.turn, segmentIndex: t.segmentIndex, content: t.content });
          } else if (t.kind === 'prose-end') {
            emit({ type: 'assistant-text', taskId: 'intake', turn: t.turn, segmentIndex: t.segmentIndex, content: t.content });
          } else if (t.kind === 'tokens') {
            emit({ type: 'tokens', total: t.total });
          }
          // {kind:'message'} / {kind:'card'} prose is intentionally ignored here —
          // the streaming transport already rendered it via prose-chunk/prose-end,
          // and the card itself fills the form via the final intake-done payload.
        };
        try {
          const result = await runIntake({ adapter, model: intakeModel, messages: msgs, contextNote: ctx, signal: ac.signal, onLog: fileLog, onTurn, repoRoots: session.git?.repoList || [], projectDir: session.activeProject });
          let card = null;
          if (result.complete && result.card) {
            card = { title: result.card.title || '', requirement: result.card.requirement || '', acceptanceCriteria: result.card.acceptanceCriteria || '' };
            // A card with no surrounding prose left nothing on screen — confirm it.
            if (!stripCard(result.prose)) emit({ type: 'assistant-text', content: 'Drafted the fields on the left — review and Add, or keep refining.' });
          }
          // awaiting-user and max-turns already streamed their message via onTurn.
          emit({ type: 'intake-done', card });
        } catch (e) {
          fileLog(`intake-chat error: ${e?.message ?? e}`);
          emit({ type: 'assistant-text', content: 'Intake error: ' + (e?.message ?? String(e)) });
          emit({ type: 'intake-done', card: null });
        } finally { try { res.end(); } catch {} exitProjectCwd(entered); }
        return;
      }
      // Capture-from-card: freeze the (possibly user-edited) card into the
      // assignment. patch(status:'ready') auto-stamps readyAt — THAT is the
      // freeze. Reuses the unchanged store; the draft already sits at the end
      // of the backlog from the create() in /intake, so no reorder is needed.
      if (req.method === 'POST' && path === '/api/assignments/capture') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        if (!body.id) return sendJson(res, 400, { error: 'id required' });
        const requirement = (body.requirement ?? '').toString().trim();
        if (!requirement) return sendJson(res, 400, { error: 'requirement required — capture must freeze a definition of done' });
        const fields = { requirement, acceptanceCriteria: (body.acceptanceCriteria ?? '').toString().trim() || null, status: 'ready' };
        if (body.title && body.title.toString().trim()) fields.title = body.title.toString().trim();
        const a = await session.assignments.patch(body.id, fields);
        if (!a) return sendJson(res, 404, { error: 'no such assignment' });
        await session.assignments.checkpoint(body.id, { kind: 'captured', userAction: 'capture-from-card' });
        broadcast({ type: 'backlog', version: bumpVersion() });
        fileLog(`assignment captured: ${a.id} "${a.title}" (status ready)`);
        return sendJson(res, 200, { assignment: a });
      }
      // Drift feedback: re-dispatch the worker on an assignment the acceptance
      // check found UNFULFILLED, seeding it with the named gaps. User-triggered
      // from the assignment-gate (the user stays the final approver); re-arms
      // activeAssignment so the next run re-runs acceptance and produces a fresh
      // gate. Returns a seedText the caller POSTs to /api/message (mirrors
      // /api/assignments/start).
      if (req.method === 'POST' && path === '/api/assignments/send-fix') {
        if (!session.assignments) return sendJson(res, 409, { error: 'no project open' });
        if (session.status === 'running') return sendJson(res, 409, { error: 'a chat is already running' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const a = await session.assignments.get(body?.id);
        if (!a) return sendJson(res, 404, { error: 'no such assignment' });
        if (!a.requirement) return sendJson(res, 400, { error: 'assignment has no captured requirement' });
        const v = a.lastVerdict || {};
        await session.assignments.setActive(a.id);
        await session.assignments.patch(a.id, { status: 'active' });
        session.activeAssignment = a.id;
        const seedText = `Continuing assignment ${a.id}: ${a.title}\n\nThe acceptance check found the requirement is NOT yet fulfilled. Address the gaps below — make only the changes they require, and close each unit with <next-task> when fixed.\n\nRequirement:\n${a.requirement}`
          + (v.missing ? `\n\nWhat's missing:\n${v.missing}` : '')
          + (v.drift ? `\n\nOut-of-scope drift to avoid or undo:\n${v.drift}` : '')
          + (v.rationale ? `\n\nReviewer rationale:\n${v.rationale}` : '');
        await session.assignments.checkpoint(a.id, { kind: 'fix-dispatched', userAction: 'send-fix' });
        broadcast({ type: 'backlog', version: bumpVersion() });
        fileLog(`assignment fix dispatched: ${a.id}`);
        return sendJson(res, 200, { ok: true, assignmentId: a.id, seedText });
      }
      // ── Tier-1 codebase memory (agent-maintained; developer-curated) ──
      if (req.method === 'GET' && path === '/api/memory') {
        if (!session.activeProject) return sendJson(res, 200, { entries: [], text: '' });
        const mem = await loadMemory(session.activeProject);
        return sendJson(res, 200, { entries: mem.entries, text: renderMemory(mem) });
      }
      if (req.method === 'POST' && path === '/api/memory/replace') {
        if (!session.activeProject) return sendJson(res, 409, { error: 'no project open' });
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const mem = await replaceEntries(session.activeProject, Array.isArray(body.entries) ? body.entries : []);
        broadcast({ type: 'memory', version: bumpVersion() });
        fileLog(`codebase memory replaced: ${mem.entries.length} entr(ies) by developer`);
        return sendJson(res, 200, { entries: mem.entries, text: renderMemory(mem) });
      }
      if (req.method === 'GET' && path === '/api/diagnostics') {
        // One-stop diagnostic dump + investigative findings. Rendered as a
        // full-page view by the Snapshot UI. Findings are surfaced at the top
        // of the view so the user doesn't have to spot problems by eye.
        const diag = buildDiagnostics();
        diag.findings = runDiagnosticChecks(diag);
        return sendJson(res, 200, diag);
      }
      if (req.method === 'GET' && path === '/api/queries') {
        // Programmable mosaic backend: takes ?ids=1,3,7 and runs those queries
        // against the current diagnostic state, returning a list of tile data
        // the UI can pack into a multi-page mosaic. The catalog is also
        // returned so the UI can render the index page without a second call.
        const diag = buildDiagnostics();
        diag.findings = runDiagnosticChecks(diag);
        const raw = (url.searchParams.get('ids') ?? '').trim();
        const ids = raw === ''
          ? DEFAULT_QUERY_IDS.slice()
          : raw.split(',').map((s) => parseInt(s, 10)).filter((n) => Number.isFinite(n));
        const tiles = runQueries(ids, diag);
        // Clamp the grid dimensions to a sane positive-integer range. They come
        // straight off the (unauthenticated) query string and feed
        // `new Array(rows*cols)` in the layout engine: a bare Number()||N let
        // through negatives and non-integers (-> RangeError 500) and unbounded
        // values (cols=rows=99999 -> a ~10^10-cell allocation that OOM-aborts the
        // whole process). Real clients send <= 12x24 (the snapshot page sends no
        // dims at all and uses these defaults); 32 covers any real layout while
        // staying well inside the packer's comfort zone (it degrades past ~48).
        const MAX_DIM = 32;
        const clampDim = (v, def) => {
          const n = Math.trunc(Number(v));
          return Number.isFinite(n) && n > 0 ? Math.min(n, MAX_DIM) : def;
        };
        const pageCols = clampDim(url.searchParams.get('cols'), 12);
        const pageRows = clampDim(url.searchParams.get('rows'), 24);
        const layout = layoutMosaic(tiles, pageCols, pageRows, { maxMs: 250 });
        const catalog = QUERY_CATALOG.map((q) => ({
          id: q.id,
          name: q.name,
          description: q.description,
          category: q.category,
          sizeHint: q.sizeHint,
        }));
        return sendJson(res, 200, {
          tiles,                                    // unpositioned, ordered by requested ids
          layout,                                   // { pages: [...], totalTiles, totalPages }
          catalog,                                  // every available query
          defaultIds: DEFAULT_QUERY_IDS,
          pageCols, pageRows,
          build: { version: BUILD_VERSION_RUNTIME, startedAt: session.startedAt },
        });
      }
      // Kept as a labeled block so we can refactor the body later without
      // re-finding the call sites — buildDiagnostics() is a closure over the
      // session so it can stay inline with the rest of the handler scope.
      function buildDiagnostics() {
        const lf = logFilePath();
        let logExists = false, logBytes = 0, logTail = '';
        if (lf) {
          try {
            const s = statSync(lf);
            logExists = true;
            logBytes = s.size;
            // Read only the TAIL (last ~16 KB), NOT the whole file. This runs
            // on every /api/diagnostics + /api/queries (Snapshot) fetch, and a
            // synchronous readFileSync of a multi-MB log blocks the event loop
            // long enough to make the Snapshot fetch time out ("server
            // unresponsive") on an established project. A bounded fd read is
            // fast regardless of log size.
            const want = 16000;
            const start = s.size > want ? s.size - want : 0;
            const len = s.size - start;
            if (len > 0) {
              const fd = openSync(lf, 'r');
              try {
                const buf = Buffer.allocUnsafe(len);
                readSync(fd, buf, 0, len, start);
                logTail = (start > 0 ? '...' : '') + buf.toString('utf8');
              } finally { closeSync(fd); }
            }
          } catch (e) { logTail = `(log read failed: ${e.message})`; }
        }
        const memUsage = (() => {
          try { const m = process.memoryUsage(); return { rss: m.rss, heapUsed: m.heapUsed, heapTotal: m.heapTotal }; } catch { return null; }
        })();
        const agentRuns = session.events.filter((e) => e.type === 'agent_run');
        const allBatches = agentRuns.flatMap((e) => Array.isArray(e.batches) ? e.batches : []);
        const sum = allBatches.reduce((s, n) => s + n, 0);
        const parallelism = {
          runs: agentRuns.length,
          turnsWithTools: allBatches.length,
          totalToolCalls: sum,
          avgPerTurn: allBatches.length ? +(sum / allBatches.length).toFixed(2) : 0,
          maxParallel: allBatches.length ? Math.max(...allBatches) : 0,
          singleCallTurns: allBatches.filter((n) => n === 1).length,
          multiCallTurns: allBatches.filter((n) => n > 1).length,
          batches: allBatches,
        };
        return {
          build: {
            version: BUILD_VERSION_RUNTIME,
            startedAt: session.startedAt,
          },
          server: {
            uptimeMs: Date.now() - session.startMs,
            pid: process.pid,
            cwd: process.cwd(),
            platform: process.platform,
            nodeVersion: process.version,
            memory: memUsage,
          },
          chat: {
            status: session.status,
            phase: session.phase,
            version: session.version,
            chatStartedAt: session.chatStartedAt,
            chatStartedAgoMs: session.chatStartedAt ? Date.now() - session.chatStartedAt : null,
            timeoutMs: session.chatTimeoutMs,
            error: session.error,
            tokens: { ...session.chatTokens },
            eventCount: session.chatEvents.length,
            abortControllerActive: !!session.abortController,
            streamSubscribers: session.streamSubscribers.size,
          },
          // Latest per-turn context-window fill (catalog 24). Large/over-budget
          // => a big open task dragged every turn (the runaway-context failure).
          context: session.lastContextFill || null,
          // Latest per-turn task-store breakdown (catalog 25): current open task
          // size, finished-task tokens, per-tier task counts. Emitted by the
          // agent (the server can't read the task store without blocking I/O).
          tasks: session.lastTaskStats || null,
          // Auth state — surfaced so the snapshot can show api-key + unlock
          // status as numbered queries (catalog 44–47). When pendingUnlock
          // is non-null, every LLM call is blocked waiting on the user.
          auth: {
            hasKey: !!session.apiKey,
            keyPrefix: session.apiKeyPrefix,
            pendingUnlockUrl: session.pendingUnlock?.url ?? null,
            pendingUnlockResolverCount: session.pendingUnlock?.resolvers?.length ?? 0,
          },
          chatEvents: session.chatEvents,
          project: {
            activeProject: session.activeProject,
            logFilePath: lf,
            logFileExists: logExists,
            logFileBytes: logBytes,
            recent: session.recent,
            // Catalog 39 "Project context loaded": which AGENTS.md / context
            // files the worker is actually being fed, and their sizes.
            contextFiles: (session.projectContextFiles ?? []).map((f) => ({
              name: f.name, relDir: f.relDir, bytes: (f.content ?? '').length,
            })),
            contextChars: (session.projectContext ?? '').length,
          },
          config: {
            baseURL: session.baseURL,
            defaultModel: session.defaultModel,
            apiKeyPrefix: session.apiKeyPrefix,
            modelInUseDefault: session.defaultModel,
            apiFormat: resolvedApiFormat,
            usingProvidedAdapter: !!providedAdapter,
          },
          // Catalog 49 "Git workspace state": per-repo branch/head/dirtyAtOpen,
          // read straight off the cached session.git state (no git spawns).
          git: session.git ? {
            isRepo: !!session.git.isRepo,
            repoList: (session.git.repoList ?? []).map((dir) => {
              const r = session.git.repos?.[dir] || {};
              return {
                relDir: nodePath.relative(session.activeProject || dir, dir) || '.',
                branch: r.branch ?? null,
                head: r.head ? String(r.head).slice(0, 8) : null,
                parentBranch: r.parentBranch ?? null,
                dirtyAtOpen: !!r.dirtyAtOpen,
                codeBoss: isCodeBossBranch(r.branch),
              };
            }),
          } : { isRepo: false, repoList: [] },
          // Catalog 48 "Plugins detected".
          plugins: pluginManager ? pluginManager.snapshot() : [],
          parallelism,
          transactions: {
            count: session.events.length,
            totalTokens: session.totalTokens,
            events: session.events,
          },
          stderr: stderrBuffer.slice(-200),  // last 200 lines
          logTail,
        };
      }
      if (req.method === 'GET' && path === '/api/git-dirty') {
        // Lightweight poll: returns the list of tracked repos that
        // currently have uncommitted changes. Drives the red ⚠
        // indicator in the footer next to the project path.
        // Guard: if a project just closed, session.activeProject is null
        // but session.git may still carry stale repoList — bail early
        // rather than passing null to path.relative.
        if (!session.activeProject || !session.git?.repoList?.length) {
          return sendJson(res, 200, { repos: [] });
        }
        const repos = [];
        for (const dir of session.git.repoList) {
          try {
            const branch = await getCurrentBranch(dir);
            const status = await getStatus(dir);
            if (status.dirty !== false) {   // include on dirty OR unknown (null) — never hide a possibly-dirty repo
              repos.push({
                relDir: nodePath.relative(session.activeProject, dir) || '.',
                branch: branch || '(detached HEAD)',
                codeBoss: isCodeBossBranch(branch),
                changeCount: status.lines.length,
              });
            }
          } catch {
            // Repo got removed under us, .git deleted, etc — skip it.
          }
        }
        return sendJson(res, 200, { repos });
      }
      // Merge-status: per CODE_BOSS-branch repo, the parent branch + how many commits the CODE_BOSS
      // branch is ahead. Drives the pinned "main ← merge · N commits" bar at the bottom of the chat.
      if (req.method === 'GET' && path === '/api/merge-status') {
        await reconcileGitModes();   // direct→subbranch if a tree got fully committed since the last poll
        const repos = [];
        if (session.activeProject && session.git?.repoList?.length) {
          for (const dir of session.git.repoList) {
            try {
              const repo = session.git.repos[dir];
              const branch = await getCurrentBranch(dir);
              const relDir = nodePath.relative(session.activeProject, dir) || '.';
              if (isCodeBossBranch(branch)) {
                const parent = repo?.parentBranch || await getDefaultBranch(dir);
                if (!parent || parent === branch) continue;
                const ahead = await aheadCount(dir, parent, branch);
                repos.push({ relDir, mode: 'subbranch', branch, parent, ahead: ahead ?? 0 });
              } else if (repo?.mode === 'direct') {
                const st = await getStatus(dir);
                repos.push({ relDir, mode: 'direct', branch, dirty: st.dirty !== false, changeCount: st.lines?.length || 0 });
              }
            } catch { /* skip a repo that errors */ }
          }
        }
        return sendJson(res, 200, { repos });
      }
      // Merge the CODE_BOSS branch(es) into their parent — the developer's explicit "ship it" click.
      // Merges every CODE_BOSS-branch repo; refuses if the agent is running (mid-write).
      if (req.method === 'POST' && path === '/api/merge-to-parent') {
        if (session.status === 'running') return sendJson(res, 409, { ok: false, error: 'agent is running — cancel or wait for it to finish first' });
        const results = [];
        for (const dir of (session.git?.repoList || [])) {
          try {
            const branch = await getCurrentBranch(dir);
            if (!isCodeBossBranch(branch)) continue;
            const parent = session.git.repos[dir]?.parentBranch || await getDefaultBranch(dir);
            if (!parent || parent === branch) continue;
            const out = await mergeToParent(dir, branch, parent);
            results.push({ relDir: nodePath.relative(session.activeProject, dir) || '.', branch, parent, ...out });
            fileLog(`merge-to-parent: ${branch} → ${parent}: ${out.merged ? 'merged ' + (out.commit || '') : 'FAILED (' + (out.reason || '') + ')'}`);
          } catch (e) { results.push({ relDir: nodePath.relative(session.activeProject, dir) || '.', merged: false, reason: 'error', message: e.message }); }
        }
        const okAll = results.length > 0 && results.every((r) => r.merged);
        return sendJson(res, okAll ? 200 : (results.length ? 409 : 200), { ok: okAll, results });
      }
      // DIRECT mode: generate an AI commit message for the current uncommitted working-tree changes
      // (for the commit modal — does NOT commit). Uses the first direct-mode repo's diff.
      if (req.method === 'POST' && path === '/api/commit-message') {
        const dir = (session.git?.repoList || []).find((d) => session.git.repos[d]?.mode === 'direct' && !isCodeBossBranch(session.git.repos[d].branch));
        let message = '';
        if (dir) {
          try {
            const diff = ((await runGit(dir, ['diff', 'HEAD'])).stdout || '').trim();
            const status = ((await runGit(dir, ['status', '--porcelain'])).stdout || '').trim();
            // Include BOTH the tracked diff AND the porcelain status — so untracked/new files (which
            // `git diff HEAD` omits but the commit's `git add -A` WILL include) reach the summary.
            const combined = [diff, status && ('--- changed / added / removed files ---\n' + status)].filter(Boolean).join('\n\n');
            const task = { taskId: 'commit', userRequest: 'Summarize these uncommitted working-tree changes for a git commit message.', summary: '', events: [{ type: 'diff', diff: combined || '(no changes)' }] };
            message = await runSummarizer({ adapter, model: session.defaultModel, task, signal: undefined, onLog: (m) => fileLog(m) });
          } catch (e) { fileLog(`commit-message gen failed: ${e.message}`); }
        }
        return sendJson(res, 200, { message: message || 'Update working tree' });
      }
      // DIRECT mode: commit the working tree on the USER's branch with their (AI-seeded, editable)
      // message. After committing, reconcile → fork the CODE_BOSS sub-branch (mode flips to merge row).
      if (req.method === 'POST' && path === '/api/commit-direct') {
        if (session.status === 'running') return sendJson(res, 409, { ok: false, error: 'agent is running — wait for it to finish or cancel first' });
        const raw = await readBody(req); let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { ok: false, error: 'invalid JSON' }); }
        const message = String(body?.message || '').trim() || 'Update working tree';
        const results = [];
        for (const dir of (session.git?.repoList || [])) {
          const repo = session.git.repos[dir];
          if (!repo || repo.mode !== 'direct' || isCodeBossBranch(repo.branch)) continue;
          try {
            const out = await withRepoGitLock(dir, async () => {
              const add = await runGit(dir, ['add', '-A']);
              if (add.exitCode !== 0) return { ok: false, reason: 'add', detail: add.stderr?.trim() };
              const st = await getStatus(dir);
              if (st.dirty === false) return { ok: false, reason: 'nothing-to-commit' };
              const c = await runGit(dir, ['commit', '-F', '-'], { input: message });
              if (c.exitCode !== 0) return { ok: false, reason: 'commit', detail: c.stderr?.trim() };
              return { ok: true, hash: await getHeadHash(dir) };
            });
            results.push({ relDir: nodePath.relative(session.activeProject, dir) || '.', ...out });
            fileLog(`commit-direct: ${out.ok ? 'committed ' + out.hash : 'FAILED (' + out.reason + ')'} on ${nodePath.relative(session.activeProject, dir) || '.'}`);
          } catch (e) { results.push({ relDir: nodePath.relative(session.activeProject, dir) || '.', ok: false, reason: 'error', detail: e.message }); }
        }
        await reconcileGitModes();   // committed → fork the sub-branch now (so the row flips immediately)
        const okAll = results.length > 0 && results.every((r) => r.ok);
        return sendJson(res, okAll ? 200 : (results.length ? 409 : 200), { ok: okAll, results });
      }
      if (req.method === 'GET' && path === '/api/events') {
        const beforeId = url.searchParams.get('before');
        const limit = Math.min(Number(url.searchParams.get('limit')) || 50, 500);
        let endIdx = session.chatEvents.length;
        if (beforeId) {
          const idx = session.chatEvents.findIndex((e) => e.id === beforeId);
          if (idx >= 0) {
            endIdx = idx;
          } else {
            // before-id not found — e.g. a stale tab whose oldest id predates
            // a project switch / reset (ids restart at e1). Returning the
            // NEWEST window here would make the client PREPEND duplicates at
            // the top of the log. Return empty + no-more instead.
            return sendJson(res, 200, { events: [], hasMoreOlder: false, version: session.version });
          }
        }
        const startIdx = Math.max(0, endIdx - limit);
        const slice = session.chatEvents.slice(startIdx, endIdx);
        return sendJson(res, 200, {
          events: slice,
          hasMoreOlder: startIdx > 0,
          version: session.version,
        });
      }
      if (req.method === 'GET' && path === '/api/stream') {
        // SSE subscriber — pushes session state deltas as they happen.
        // Browser opens this once per page load; auto-reconnects on drop.
        res.writeHead(200, {
          'content-type': 'text/event-stream',
          'cache-control': 'no-cache, no-store',
          'connection': 'keep-alive',
        });
        // Send the most recent 100 events on connect. Older events
        // backfill via /api/events?before=<oldestId>&limit=50 when the
        // client scrolls to the top (see maybeLoadOlder in ui.html).
        // hasMoreOlder in the snapshot tells the client to keep the
        // scroll-to-load hook armed.
        const initial = chatState.chatStateSnapshot({ eventLimit: 100 });
        res.write(`data: ${JSON.stringify({ type: 'init', version: session.version, session: initial })}\n\n`);
        // Subscribe to broadcasts.
        session.streamSubscribers.add(res);
        // Heartbeat every 25s so proxies don't close the connection.
        const heartbeat = setInterval(() => {
          try { res.write(`: heartbeat\n\n`); } catch {}
        }, 25000);
        req.on('close', () => {
          clearInterval(heartbeat);
          session.streamSubscribers.delete(res);
        });
        return;
      }
      if (req.method === 'POST' && path === '/api/message') {
        // Accept user text, kick off agent in background, return immediately.
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const text = (body?.text ?? '').toString();
        // Read-only "Discuss" mode: the worker may read/search but not write/
        // edit/run shell (enforced in runPromptedAgent's dispatch + prompt).
        const discussMode = body?.mode === 'discuss';
        const criticMode = body?.critic === true;   // reviewer toggle (off by default)
        const deepThinkMode = body?.deepThink === true;   // deep-think toggle (off by default)
        // NOTE: session.reviewerOn / session.deepThinkOn are committed only AFTER the busy check below —
        // a 409-rejected message must never mutate a running run's reviewer/deep-think state (same
        // hazard as reviewBeforeApply).
        // "Review before applying" (per-message, default off): when on, the worker runs read-only
        // and PROPOSES first. Kept as a LOCAL until we commit to a fresh run — assigning
        // session.reviewBeforeApply here would mutate a CURRENTLY-RUNNING worker's approval gate.
        const reviewBeforeApply = !!body?.review;
        if (!text.trim()) return sendJson(res, 400, { error: 'text is required' });
        // Optional attached images: an array of data:image/... URIs, total
        // payload bounded (12MB of data-URI text). Invalid shapes are a 400 —
        // never silently dropped server-side (the UI strips client-side when
        // the probe found no working format).
        let images = [];
        if (body?.images != null) {
          if (!Array.isArray(body.images)) return sendJson(res, 400, { error: 'images must be an array of data URIs' });
          images = body.images.map((u) => String(u ?? ''));
          if (images.some((u) => !u.startsWith('data:image/'))) return sendJson(res, 400, { error: 'every image must be a data:image/... URI' });
          const totalLen = images.reduce((n, u) => n + u.length, 0);
          if (totalLen > 12 * 1024 * 1024) return sendJson(res, 400, { error: 'images too large (total over 12MB)' });
        }
        if (!session.activeProject) return sendJson(res, 400, { error: 'no project open' });
        if (session.status === 'running') {
          // A cancel is in flight — the run hasn't unwound to idle/error yet. Don't fork/manage a
          // dying run; ask the caller to retry once it settles.
          if (session.abortController?.signal?.aborted) return sendJson(res, 409, { error: 'cancelling — retry in a moment' });
          // Review (propose-first) is interactive (propose → approve → implement) and needs the
          // FOREGROUND. Never fork a review-on message to a background write-mode sub-agent — that
          // would skip the proposal and change files unapproved.
          if (reviewBeforeApply) return sendJson(res, 409, { error: 'agent is busy — finish or cancel the current task before a review-mode (propose-first) request' });
          // ALWAYS-RESPONSIVE (CB_ORCHESTRATOR): the foreground is busy, but instead of
          // rejecting, delegate this message to a background sub-agent isolated in its own
          // git worktree. Off / non-git → today's 409. (discuss + assignment seeds run the
          // foreground inline, so they keep the 409 lock too.)
          if (CB_ORCHESTRATOR && session.git?.isRepo && !discussMode) {
            const orch = ensureOrchestrator(session);
            // THE FORK (R5, once per inline run): the FIRST concurrent message backgrounds the
            // running inline task as a sub-agent — its worker output redirects to a drawer, it
            // folds a card on finish, and the foreground becomes a clean MANAGER. No handoff /
            // WIP-commit: the same worker keeps running in the main dir; only its labeling +
            // output channel change.
            if (!session.fork && session.foregroundRequest != null) {
              session.fork = { id: `fg-${(session.forkSeq = (session.forkSeq || 0) + 1)}`, topic: (session.foregroundRequest || 'current task').slice(0, 60).replace(/\s+/g, ' ').trim() };
              chatState.broadcast({ type: 'subagent-event', version: chatState.bumpVersion(), event: { type: 'subagent-spawned', id: session.fork.id, topic: session.fork.topic } });
              fileLog(`/api/message: FORK — backgrounded the running inline task as ${session.fork.id} ("${session.fork.topic}")`);
            }
            // THE MANAGER TURN (one-call router): instead of blindly delegating, the freed
            // manager runs ONE read-only turn (seeded with the conversation) that either
            // ANSWERS the user inline or emits <delegate> to hand a real task to a sub-agent.
            // A fresh LLM call, concurrent with the forked task's in-flight tool → responds
            // immediately (non-blocking). Returns 200 now; the answer/delegate streams via SSE.
            session.reviewerOn = criticMode;   // accepted message — sub-agents it spawns carry their own reviewer
            session.deepThinkOn = deepThinkMode;   // …and the deep-think snapshot they inherit at spawn
            runManagerTurnAsync(session, text);
            return sendJson(res, 200, { managing: true, forked: !!session.fork });
          }
          return sendJson(res, 409, { error: 'chat is already running' });
        }
        // Fresh inline run (N=1). Record its request (so a later fork can label it) and clear
        // any stale fork state from a prior run.
        session.foregroundRequest = text;
        session.fork = null;
        session.runApproved = false;   // each run needs its own approval when review-before-applying is on
        session.reviewBeforeApply = reviewBeforeApply;   // commit the per-message review flag now that we're starting a fresh run
        session.reviewerOn = criticMode;   // commit the reviewer flag too — sub-agents launched by this run snapshot it at spawn
        session.deepThinkOn = deepThinkMode;   // same snapshot semantics for deep_think (workerOpts reads it at spawn)
        // Fresh run — drop any steer text left over from a prior run.
        session.steerBuffer = [];
        fileLog(`/api/message received: model=${body?.model ?? session.defaultModel}${images.length ? ` images=${images.length}` : ''} text="${text.slice(0, 100).replace(/\s+/g, ' ')}"`);

        // Lock in running state BEFORE we return, so a fast second call sees the lock.
        session.chatStartedAt = Date.now();
        session.abortController = new AbortController();
        // THIS run's controller. The finally below can await a shadow post-snapshot AFTER the
        // terminal status is published (error/abort unwinds), so a fast follow-up /api/message can
        // start run N+1 inside that window — every cleanup/abort here must check ownership or it
        // clobbers the LIVE run (uncancellable run).
        const myController = session.abortController;
        chatState.setStatus('running');
        chatState.setPhase('uploading');
        chatState.setError(null);
        chatState.resetTokens();
        chatState.updateTokens({ up: estimateTokens(text) });

        const signal = myController.signal;
        const model = body?.model ?? session.defaultModel;
        const startedAt = Date.now();

        // Server-side timeout: auto-abort if it runs past chatTimeoutMs.
        const timeoutId = setTimeout(() => {
          if (session.abortController === myController) {
            try { myController.abort(); } catch {}
            chatState.setError(`timed out after ${Math.round(session.chatTimeoutMs / 1000)}s`);
          }
        }, session.chatTimeoutMs);

        // Fire-and-forget: run the agent loop async, return 200 now.
        (async () => {
          let entered = false;
          const chatStart = Date.now();
          // Hoisted so the catch can finalize still-pending review row(s).
          // One combined reviewer call can leave several task footers pending.
          let pendingReviewEvs = [];
          let shadowPre = null;   // this run's pre-edit shadow snapshot (non-repo only); a post snapshot is taken at run end
          // Capture the POST-run shadow snapshot so undo reverts only this run's pre→post delta,
          // not files the user creates afterward (a blanket clean -fd would delete those).
          // A run that changed NOTHING (post == pre) CLEARS the undo point entirely: there is
          // nothing to revert, the pointless button stays hidden, and the "I used Undo"
          // notification run can't re-arm an empty undo on itself. Called from the finally
          // (AFTER the terminal status flip — see the timing note at the success exit);
          // idempotent: once postCommit is set or the undo point is cleared, it's a no-op.
          const captureShadowPost = async () => {
            if (!(shadowPre && session.shadowUndo && session.shadowUndo.commit === shadowPre && !session.shadowUndo.postCommit)) return;
            try {
              const pc = await shadowSnapshot(session.activeProject, 'post-run');
              if (pc && pc === shadowPre) { session.shadowUndo = null; fileLog('shadow post-snapshot: run changed nothing — undo point cleared'); }
              else if (pc) session.shadowUndo.postCommit = pc;
            } catch (e) { fileLog(`shadow post-snapshot failed: ${e.message}`); }
          };
          try {
            entered = enterProjectCwd(session.activeProject);
            if (!entered) throw new Error('cannot enter project directory');
            // SHADOW-GIT safety net: a NON-repo target has no CODE_BOSS branch to revert, so
            // snapshot the project before this write-mode run edits anything — the developer can
            // then undo the whole run. Repos already have the CODE_BOSS branch; read-only
            // (discuss) and propose-first (review) runs don't edit, so skip them. Best-effort.
            if (session.git && !session.git.isRepo && !discussMode && !reviewBeforeApply && session.activeProject) {
              try {
                const c = await shadowSnapshot(session.activeProject, text);
                if (c) { shadowPre = c; session.shadowUndo = { dir: session.activeProject, commit: c, postCommit: null, request: text.slice(0, 80).replace(/\s+/g, ' ').trim(), at: Date.now() }; }
              } catch (e) { fileLog(`shadow snapshot failed: ${e.message}`); }
            }
            fileLog(`chat START: model=${model} q="${text.slice(0, 200).replace(/\s+/g, ' ')}"`);
            // ADAPTIVE COGNITION (CB_COGNITION): route the message through the cognition
            // scheduler (assess → policy → explore/decompose/recurse/worker/verify, budget-
            // bounded) instead of the plain inline worker. Off / non-git / discuss → unchanged.
            if (CB_COGNITION && session.git?.isRepo && !discussMode) {
              await ensureSessionStore(session);
              const cog = await runCognitionForMessage(session, text, signal);
              if (!(cog && cog.inline)) {
                if (cog.answer && cog.answer.trim()) chatState.appendChatEvent({ type: 'assistant-text', content: cog.answer });
                chatState.setStatus('done');
                fileLog(`cognition DONE: ${cog.state ? `spent ${Math.round(cog.state.spent)}/${Math.round(cog.state.ceiling)} (${cog.state.nodes} nodes)` : '(answered directly)'}`);
                return;   // the finally still runs (cleanup: clearTimeout, abortController, phase idle)
              }
              // single coherent task — the assess router declined to decompose; fall through to
              // the plain inline worker (the strong baseline), no sub-agent/worktree overhead.
              fileLog('cognition: single coherent task → handling inline (plain worker)');
            }
            // Auto-commit hook: fires from runPromptedAgent after every
            // <next-task> task-op. Only does anything when the project
            // is a git repo AND we're on a CODE_BOSS_* branch (set up
            // at project open). Emits the git operations as tool-call /
            // tool-result chat events so the user sees them inline,
            // tagged with the just-closed task's id. Returns the new
            // commit's short hash, which the agent attaches to the
            // closed task in the store.
            // Phase B: walk EVERY repo session.git.repos knows about.
            // For each one currently on a CODE_BOSS_* branch with dirty
            // changes, commit. Skip repos not on CODE_BOSS_* (the
            // pre-write hook will have prompted the user already; if
            // they declined we won't have any uncommitted CODE_BOSS
            // work to commit on those).
            const gitAutoCommitHook = async ({ closedTaskId, userRequest, summary }) => {
              // The unit of work ended — drop any active checklist AND hide the live panel.
              // Broadcasting an empty checklist both clears session.lastChecklist (via the capture)
              // and tells the UI to hide it, so a plan the agent finished-but-didn't-tick (it used
              // <task-progress> instead of re-emitting the checklist) doesn't linger as a stale
              // unticked panel. (A read-only propose run doesn't close a task, so the propose→
              // implement carry-over is unaffected.)
              if (session.lastChecklist) { try { chatState.appendChatEvent({ type: 'checklist', items: [] }); } catch {} }
              session.lastChecklist = null;
              // Discuss (read-only) mode: a closed task is just a conversational
              // boundary — no writes happened, so do NOT bind it to the active
              // assignment or commit (and review/acceptance are skipped below).
              if (discussMode) return null;
              // Bind the closed task to the active assignment (independent of git).
              if (session.activeAssignment && session.assignments && closedTaskId) {
                try { await session.assignments.addTask(session.activeAssignment, closedTaskId); } catch (e) { fileLog(`assignment task-stamp failed: ${e.message}`); }
              }
              if (!session.git?.isRepo) return null;
              const msg = buildCommitMessage({ summary, userRequest, assignmentId: session.activeAssignment || null });
              // Walk all repos. Commit each one on a CODE_BOSS_* branch with
              // changes, recording EVERY repo's commit hash. (Previously only
              // the first repo's hash was kept — a multi-repo bug that lost
              // traceability + made undo miss every repo but the first.)
              //
              // NOISE: instead of emitting a tool-call/tool-result PAIR per git
              // op per repo (3 repos × add+commit+branch = up to 9 rows for a
              // one-line change — and another 9 after a build that regenerates
              // checked-in JS), we run the ops silently and emit ONE summary row
              // tagged with the closed task so it nests inside that task.
              const commitsByRepo = [];   // [{ repoDir, hash }]
              const outcomes = [];        // [{ rel, status, hash, branch, detail }]
              for (const repoDir of session.git.repoList) {
                const rel = nodePath.relative(session.activeProject, repoDir) || '.';
                const repo = session.git.repos[repoDir];
                // Refresh branch in case the user resolved a wizard mid-task.
                repo.branch = await getCurrentBranch(repoDir);
                if (!isCodeBossBranch(repo.branch)) {
                  // Not on a CODE_BOSS branch. END-OF-TASK CATCH: if this repo
                  // was CLEAN at open but is now dirty, the agent changed it
                  // via powershell / wl / some path that bypassed the
                  // write_file pre-write hook (which would have auto-branched).
                  // Auto-create the CODE_BOSS branch now (carrying the changes)
                  // so the work is committed below. Repos with PRE-EXISTING
                  // user changes (dirtyAtOpen) are left alone — the end-of-chat
                  // git-pending-resolution warning surfaces those for the user
                  // to decide; we never sweep their work onto a CODE_BOSS branch.
                  if (repo.dirtyAtOpen) continue;
                  const st = await getStatus(repoDir);
                  if (st.dirty !== true) continue;   // clean (and clean-at-open) → nothing to do
                  const parent = repo.branch;
                  const br = await createBranch(repoDir);
                  if (br.exitCode !== 0) { outcomes.push({ rel, status: 'branch-error', detail: br.stderr?.trim() || `exit ${br.exitCode}` }); continue; }
                  repo.parentBranch = parent;
                  repo.branch = br.branch;
                  fileLog(`git: end-of-task auto-branch ${br.branch} on ${rel} (clean at open, dirtied via non-write_file path)`);
                  // fall through to add + commit on the new CODE_BOSS branch
                }
                // Serialize the index-mutating add+commit through the SAME per-repo git lock
                // the orchestrator's integrate/removeWorktree use, so a concurrent sub-agent
                // approval can't collide on .git/index.lock (no-op overhead when uncontended).
                const cr = await withRepoGitLock(repoDir, async () => {
                  const add = await runGit(repoDir, ['add', '-A']);
                  if (add.exitCode !== 0) return { err: 'add-error', detail: add.stderr?.trim() || `exit ${add.exitCode}` };
                  const status = await getStatus(repoDir);
                  // Only skip on a CONFIRMED-clean tree. dirty===null means the status check
                  // failed (e.g. index.lock) — attempt the commit anyway rather than dropping work.
                  if (status.dirty === false) return { skip: true };
                  const commit = await runGit(repoDir, ['commit', '-F', '-'], { input: msg });
                  if (commit.exitCode !== 0) return { err: 'commit-error', detail: commit.stderr?.trim() || `exit ${commit.exitCode}` };
                  return { hash: await getHeadHash(repoDir) };
                });
                if (cr.skip) continue;
                if (cr.err) { outcomes.push({ rel, status: cr.err, detail: cr.detail }); continue; }
                const hash = cr.hash;
                repo.head = hash;
                commitsByRepo.push({ repoDir, hash });
                outcomes.push({ rel, status: 'committed', hash, branch: repo.branch });
              }
              // Record EVERY commit hash on the active assignment (the task
              // itself was bound at the top of this hook, independent of git).
              if (commitsByRepo.length && session.activeAssignment && session.assignments) {
                for (const { hash } of commitsByRepo) {
                  try { await session.assignments.addCommit(session.activeAssignment, hash); } catch {}
                }
              }
              // Successful commits now surface as a "✓ Committed" badge in the
              // task footer (built from the task-end event below), NOT as a chat
              // row — that's the noise reduction. We ONLY emit a visible row when
              // git actually ERRORED, so a real problem is never hidden. The row,
              // when emitted, lists the errors plus any successes for context,
              // tagged with closedTaskId so it nests in the task.
              const committed = outcomes.filter((o) => o.status === 'committed');
              const errors = outcomes.filter((o) => /error/.test(o.status));
              if (errors.length) {
                const callId = randomUUID();
                const label = `git · ${errors.length} error${errors.length > 1 ? 's' : ''}${committed.length ? ` (${committed.length} committed)` : ''}`;
                chatState.appendChatEvent({ type: 'tool-call', callId, verb: 'git', name: 'git', args: { command: label }, taskId: closedTaskId });
                const lines = outcomes.map((o) => {
                  if (o.status === 'committed') return `${o.rel}: committed ${o.hash ? o.hash.slice(0, 9) : ''} on ${o.branch}`;
                  if (o.status === 'branch-error') return `${o.rel}: branch failed — ${o.detail}`;
                  if (o.status === 'add-error') return `${o.rel}: git add failed — ${o.detail}`;
                  if (o.status === 'commit-error') return `${o.rel}: commit failed — ${o.detail}`;
                  return `${o.rel}: ${o.status}`;
                });
                chatState.appendChatEvent({ type: 'tool-result', callId, verb: 'git', name: 'git', status: 'error', content: lines.join('\n'), taskId: closedTaskId });
              }
              // Return: `hash` (first) preserves the existing single-hash task-store
              // attach + undo contract; `commits` carries the full per-repo list.
              // `committedRepos` is a display-friendly list (relative path + hash)
              // and `gitError` a human summary — both ride the task-end event so the
              // footer can render a "✓ Committed" / "⚠ commit issue" badge that
              // survives full re-renders (footer is rebuilt from task-end fields).
              return {
                hash: commitsByRepo[0]?.hash || null,
                commits: commitsByRepo,
                committedRepos: committed.map((o) => ({ rel: o.rel, hash: o.hash, branch: o.branch })),
                gitError: errors.length ? errors.map((e) => `${e.rel}: ${e.status.replace('-error', '')} failed — ${e.detail}`).join('\n') : null,
              };
            };
            // Pre-write hook: tools (write_file, edit_file) call this
            // before modifying any file. If the file's containing repo
            // is on a CODE_BOSS_* branch, proceed silently. Otherwise a
            // dirty-at-open repo now goes to DIRECT mode (no pause).
            // Emit a chat row for an auto-created CODE_BOSS branch so the user
            // can see it happened (mirrors the auto-commit git rows).
            const emitAutoBranch = (repoDir, branchName, parent) => {
              const rel = nodePath.relative(session.activeProject, repoDir) || '.';
              // LOG-ONLY (no chat row): auto-branching onto a CODE_BOSS branch is
              // internal git plumbing. It used to emit a tool-call/result pair that
              // — being untagged — floated OUTSIDE the task wrap (visible as a lone
              // "git checkout -b CODE_BOSS… (auto)" row below the task). That's
              // exactly the git noise we're removing now that a successful commit
              // shows as a "✓ committed" footer badge. Keep a file-log breadcrumb.
              fileLog(`git: auto-branched ${rel} to ${branchName} (forked from ${parent}; changes ride on the new branch)`);
            };
            // Bring a repo onto a CODE_BOSS_* branch so the agent's change is
            // recoverable. Three outcomes:
            //   - already on CODE_BOSS         → 'ok'
            //   - non-CODE_BOSS, no PRE-EXISTING (open-time) changes → AUTO-CREATE
            //     a CODE_BOSS branch (carries any agent-made working-tree changes
            //     onto it) and proceed, no prompt
            //   - non-CODE_BOSS WITH pre-existing user changes → PAUSE + let the
            //     user decide (the changes are theirs; we won't sweep them)
            // Used by the write_file/edit_file pre-write hook AND the post-shell
            // survey. Returns 'ok' | 'aborted'.
            const ensureCodeBossBranch = async (repoDir) => {
              if (!repoDir || !session.git.repos[repoDir]) return 'ok'; // untracked / non-git
              const repo = session.git.repos[repoDir];
              repo.branch = await getCurrentBranch(repoDir);
              if (isCodeBossBranch(repo.branch)) return 'ok';
              if (signal?.aborted) return 'aborted';
              // Already pending for THIS repo? Wait on the same Promise.
              const existing = session.gitPending?.[repoDir];
              if (existing) return await existing.promise;
              if (repo.mode === 'direct') {
                // DIRECT mode: pre-existing user changes at open → do NOT fork and do NOT pause. The
                // agent works on the user's branch directly; reconcileGitModes forks a CODE_BOSS
                // sub-branch once the tree is fully committed (the warning/commit row drives this).
                return 'ok';
              }
              // SUBBRANCH mode (clean at open) → auto-create the CODE_BOSS branch (git checkout -b
              // carries any agent-made working-tree changes onto it). No prompt.
              const parent = repo.branch;
              const r = await createBranch(repoDir);
              if (r.exitCode !== 0) {
                fileLog(`git: auto-branch failed for ${repoDir}: ${r.stderr?.trim?.() || r.exitCode}`);
                return 'aborted';
              }
              repo.parentBranch = parent;
              repo.branch = r.branch;
              repo.head = await getHeadHash(repoDir);
              fileLog(`git: auto-created ${r.branch} on ${nodePath.relative(session.activeProject, repoDir) || '.'} (was ${parent}, clean at open)`);
              emitAutoBranch(repoDir, r.branch, parent);
              return 'ok';
            };
            // Pre-write hook handed to write_file/edit_file: before the agent
            // modifies a file, make sure its repo is on a CODE_BOSS branch.
            const requireBranch = async (absPath) => {
              if (!session.git?.isRepo) return 'ok';                 // not a git workspace
              if (signal?.aborted) return 'aborted';
              const repoDir = await findContainingRepo(absPath);
              if (!repoDir || !session.git.repos[repoDir]) return 'ok'; // non-git / untracked write → let through
              return await ensureCodeBossBranch(repoDir);
            };
            // The per-edit approval modal is RETIRED. Review-OFF (the default) auto-approves, and
            // review-ON runs the worker read-only (propose-first), so a write never reaches here.
            // Revert safety for applied changes now comes from git (the CODE_BOSS branch) and the
            // shadow-git undo, not a per-edit prompt. Kept as a no-op gate for the tool contract.
            const requireApproval = async () => !signal?.aborted;
            // Review loop: run agent → review → if fail, feed back as a
            // synthetic user message and re-run; max 2 rounds. If still
            // failing after round 2, surface the issue to the user.
            const REVIEW_MAX_ROUNDS = 2;
            let round = 0;
            // Fork-aware foreground emit (R5): once this inline run is forked, its review-loop
            // rows redirect to the backgrounded task's drawer + stop driving the foreground
            // phase, so the manager foreground stays clean. No-op until/unless forked.
            const fgAppend = (ev) => (session.fork ? chatState.appendChatEvent({ ...ev, subagentChannel: session.fork.id }) : chatState.appendChatEvent(ev));
            const fgPhase = (p) => { if (!session.fork) chatState.setPhase(p); };
            let userMessage = text;
            let emitInitialUserMessage = true;
            let lastAgentResult = null;
            // If an assignment is being worked, inject its FROZEN requirement
            // into the durable prompt channel every turn (so the worker can't
            // drift even after the seed message compacts out of tier-1).
            const activeAsg = (session.activeAssignment && session.assignments)
              ? await session.assignments.get(session.activeAssignment) : null;
            const assignmentPromptAdditions = activeAsg?.requirement
              ? [`CURRENT ASSIGNMENT (${activeAsg.id}) — your one and only goal:\n${activeAsg.requirement}`
                  + (activeAsg.acceptanceCriteria ? `\n\nAcceptance criteria:\n${activeAsg.acceptanceCriteria}` : '')
                  + `\nStay strictly within this requirement; make only the changes it needs. Break the work into <next-task> units as you go.`]
              : [];
            // Tier-1 CODEBASE MEMORY: load the agent-maintained durable facts
            // and inject them so accrued knowledge compounds across runs. Read
            // fresh at run start (the agent may have updated it on a prior run).
            let codebaseMemory = '';
            try {
              if (session.activeProject) codebaseMemory = renderMemory(await loadMemory(session.activeProject));
            } catch (e) { fileLog(`codebase memory load failed: ${e.message}`); }
            // Band this run in the chat log: one marker row so the user can
            // see which stretch of the conversation belongs to which assignment
            // (worker turns are the default bubbles; manager turns are already
            // styled distinctly).
            if (activeAsg) {
              chatState.appendChatEvent({ type: 'assignment-banner', assignmentId: activeAsg.id, title: activeAsg.title });
            }
            // pendingReviewEvs (hoisted above the try) tracks appended-but-
            // not-yet-resolved 'review' events so the outer catch can finalize
            // them. Otherwise an abort/throw mid-review leaves rows stuck on
            // state:'pending' forever (spinning "reviewing…" rows).
            // Orchestrator mode: the foreground worker shares the SINGLE session store so it
            // and any concurrent sub-agent import are one instance (no two-store race). Off → null.
            let fgStore = null;
            if (CB_ORCHESTRATOR) { await ensureSessionStore(session); fgStore = session.store; }
            while (true) {
              const turnStartIdx = session.chatEvents.length;
              const result = await runPromptedAgent({
                adapter,
                // Attached images ride ONLY the original user message (round 0);
                // reviewer-feedback re-runs are text-only synthetic messages.
                messages: [{ role: 'user', content: userMessage, ...(round === 0 && images.length ? { images } : {}) }],
                model,
                store: fgStore,
                projectDir: session.activeProject,
                maxTurns: 20,
                signal,
                parallelCritic: criticMode,
                deepThink: deepThinkMode,
                // If this inline run got FORKED (R5), its worker output redirects to the
                // backgrounded task's drawer channel + stops driving the foreground phase, so
                // the foreground reads as a clean manager. Until/unless forked → foreground.
                onChatEvent: (ev) => (session.fork ? chatState.appendChatEvent({ ...ev, subagentChannel: session.fork.id }) : chatState.appendChatEvent(ev)),
                onPhase: (p) => { if (!session.fork) chatState.setPhase(p); },
                onLog: (msg) => fileLog(msg),
                emitInitialUserMessage,
                extraTools: pluginManager.enabledTools(),
                extraVerbs: pluginManager.enabledVerbs(),
                promptAdditions: [...pluginManager.promptAdditions(), ...assignmentPromptAdditions],
                projectContext: session.projectContext || '',
                codebaseMemory,
                onTaskClose: gitAutoCommitHook,
                requireBranch,
                requireApproval,
                getGitLabCredentials: () => session.gitlabCreds,
                discussMode,
                // REVIEW (propose-first): review-on (and not already discuss) runs the worker
                // read-only and asks for a PROPOSAL the developer approves before implementing.
                reviewMode: session.reviewBeforeApply && !discussMode,
                // The active checklist, pinned into the prompt each turn so it auto-drives the
                // live panel + carries across the approve→implement boundary (read fresh per turn).
                getActiveChecklist: () => session.lastChecklist || null,
                // Drain queued steer text at each turn boundary. splice(0) is
                // atomic within a JS tick, so a concurrent /api/steer push
                // either lands before the drain (this turn) or after (next).
                drainSteer: () => session.steerBuffer.splice(0),
                // Per-turn WebLogic server status (a getter so each turn sees the
                // latest poll) — injected into the system reminder so the agent
                // knows whether the server is up before it deploys/runs.
                weblogicStatus: () => session.weblogicRunning,
                // Bundle small FREE-CONVERSATION exchanges into one task (the
                // developer's main thread fragments one-task-per-message). Gated by
                // the bundleConversation server option (the production launcher sets
                // it; tests leave it off so close-triggered behavior stays per-task).
                // Always OFF while an assignment is being worked: that task must
                // close on completion to trigger review/acceptance/commit.
                enforceMinTaskSize: bundleConversation && !session.activeAssignment,
              });
              lastAgentResult = result;
              const turnDur = Date.now() - chatStart;
              fileLog(`chat round ${round + 1} done: terminated=${result.terminated} turns=${result.stats.turns} calls=${result.stats.toolCalls} duration=${turnDur}ms`);
              // Discuss (read-only) runs have no durable work to verify — skip the
              // per-task review + acceptance gate entirely (one agent run, then stop).
              if (discussMode) { fileLog('discuss mode: skipping review + acceptance'); break; }

              // #7 ADVERSARIAL PLAN-REVIEW: a propose-first (review) run produced a PLAN read-only
              // (no task closed). Fan out read-only critics to poke holes at the plan against the
              // actual code, feed any concerns back to the SOURCE agent for ONE revision, then let
              // approval proceed on the hardened plan. Best-effort: any failure degrades to the
              // un-hardened plan (the propose flow is never blocked). Skip if forked.
              if (session.reviewBeforeApply && !session.fork) {
                if (CB_PLAN_REVIEW && result.content && result.content.trim() && !signal?.aborted) {
                  try {
                    const proposal = result.content;
                    // Each critic gets its OWN throwaway store: store:null falls back to the
                    // project's shared .code_boss/tasks, so three CONCURRENT critics (+ the
                    // foreground run) cross-contaminate one conversation and the lens focus
                    // races (same class of bug the reviewer had before its isolated store).
                    let criticSeq = 0;
                    const runCritic = async (lens) => {
                      const dir = nodePath.join(session.activeProject, '.code_boss', '_plancritic', `${Date.now().toString(36)}-${++criticSeq}`);
                      const criticStore = createTasksStore(dir);
                      await criticStore.init();
                      try {
                        const r = await runPromptedAgent({
                          adapter, model, store: criticStore, projectDir: session.activeProject, signal,
                          discussMode: true, maxTurns: 6,
                          messages: [{ role: 'user', content: planCriticMessage(lens.focus, proposal) }],
                          onChatEvent: () => {}, onPhase: () => {}, onLog: (m) => fileLog(m),
                          codebaseMemory, projectContext: session.projectContext || '',
                          requireBranch: async () => 'ok', requireApproval: async () => true,
                          getGitLabCredentials: () => session.gitlabCreds,
                        });
                        return (r && r.content) || '';
                      } finally {
                        rm(dir, { recursive: true, force: true }).catch(() => {});
                      }
                    };
                    const concerns = await runAdversarialPlanReview({ runCritic, emit: (e) => fgAppend(e), signal });
                    if (concerns.length && !signal?.aborted) {
                      fileLog(`plan-review: ${concerns.length} concern(s) → source revising the plan once`);
                      fgPhase('thinking');
                      await runPromptedAgent({
                        adapter, model, store: fgStore, projectDir: session.activeProject, signal,
                        messages: [{ role: 'user', content: planRevisionMessage(proposal, concerns) }],
                        maxTurns: 20, reviewMode: true,
                        onChatEvent: (ev) => fgAppend(ev), onPhase: (p) => fgPhase(p), onLog: (m) => fileLog(m),
                        extraTools: pluginManager.enabledTools(), extraVerbs: pluginManager.enabledVerbs(),
                        promptAdditions: [...pluginManager.promptAdditions(), ...assignmentPromptAdditions],
                        codebaseMemory, projectContext: session.projectContext || '',
                        requireBranch, requireApproval, getGitLabCredentials: () => session.gitlabCreds,
                        getActiveChecklist: () => session.lastChecklist || null,
                      });
                    } else {
                      fileLog('plan-review: no concerns — plan stands');
                    }
                  } catch (e) { fileLog(`plan-review failed (degrading to the un-hardened plan): ${e.message}`); }
                }
                break;   // propose run complete → the developer approves the (hardened) plan
              }

              // The CONTINUOUS reviewer (when on) has been verifying throughout the run and its flush
              // is the close-time check, so the separate END-OF-TASK review is RETIRED — break out of
              // the review-fix loop and fall through to commit + acceptance (which still run; acceptance
              // is gated on !discussMode, not the reviewer). No second review pass, no review-driven re-open.
              // Gate on the RUN-CAPTURED criticMode, not live session.reviewerOn — a later message
              // can flip the session flag mid-run, and a critic-OFF run has no continuous reviewer
              // to cover for the retired end-review (that would be a silent review gap).
              if (criticMode) { fileLog('end-of-task review retired — the continuous reviewer covers it'); break; }
              // PER-TASK REVIEW: the reviewer fires ONLY when the agent marked
              // a task done (<next-task> → a 'task-end' event this round), and
              // reviews the WHOLE task. A round that closes no task (pure Q&A /
              // reads, or work left in the still-open current task) gets NO
              // review. This replaces the old per-round dirty/HEAD heuristic.
              const roundSlice = session.chatEvents.slice(turnStartIdx);
              const closedTaskEnds = roundSlice.filter((e) => e.type === 'task-end');
              if (closedTaskEnds.length === 0) {
                fileLog('reviewer skipped: no task closed this round');
                break;
              }
              fgPhase('reviewing');
              // COMBINED REVIEW: ONE reviewer session per round for ALL tasks
              // closed this round. Each task gets its own footer row (pending),
              // and the single runReviewer call returns a per-task verdict that
              // we map back to its row. Whole-task scope per task: every event
              // tagged with that task id across the ENTIRE conversation (a task
              // can span multiple user-message rounds); compaction never deletes
              // from chatEvents so the filter survives demotion.
              const reviewRows = [];      // { te, reviewTaskId, reviewEv }
              const reviewTasks = [];     // runReviewer({ tasks }) input
              for (const te of closedTaskEnds) {
                const reviewTaskId = te.taskId || null;
                const reviewEv = fgAppend({
                  type: 'review',
                  state: 'pending',
                  round: round + 1,
                  reviewTaskId,
                  // Also carry it as taskId so the UI nests this review row
                  // inside the task it reviewed. Reviews are emitted at the END
                  // of the round (after every task-end), so without this they
                  // float at the bottom instead of inside the right task.
                  taskId: reviewTaskId,
                });
                pendingReviewEvs.push(reviewEv);
                reviewRows.push({ te, reviewTaskId, reviewEv });
                const taskEvents = reviewTaskId
                  ? session.chatEvents.filter((e) => e.taskId === reviewTaskId)
                  : roundSlice;
                reviewTasks.push({
                  taskId: reviewTaskId || reviewEv.id,
                  userRequest: te.userRequest || text,
                  summary: te.summary || result.content,
                  events: taskEvents,
                });
              }
              const { verdicts } = await runReviewer({
                adapter, model,
                tasks: reviewTasks,
                signal,
                onLog: (msg) => fileLog(msg),
                onDiag: (d) => fgAppend({ type: 'manager-diagnostic', ...d }),
                // Multi-repo: resolve the reviewer's repo-relative read paths
                // against the actual sub-repos, not just the workspace root.
                repoRoots: session.git?.repoList || [],
                projectDir: session.activeProject,   // cwd-independent path resolution (no process.chdir)
              });
              const verdictById = new Map(verdicts.map((v) => [v.taskId, v]));
              // Map each verdict back to its footer row. Collect ALL failing
              // tasks' concerns into one feedback message for the re-run.
              const failingConcerns = [];
              for (const row of reviewRows) {
                const v = verdictById.get(row.reviewTaskId || row.reviewEv.id) || { pass: true };
                if (v.pass) {
                  chatState.patchChatEvent(row.reviewEv.id, { state: 'pass' });
                } else if (round + 1 >= REVIEW_MAX_ROUNDS) {
                  chatState.patchChatEvent(row.reviewEv.id, { state: 'escalate', concerns: v.concerns });
                  fileLog(`reviewer escalated ${row.reviewTaskId || ''} after ${round + 1} rounds: ${String(v.concerns).slice(0, 200)}`);
                } else {
                  chatState.patchChatEvent(row.reviewEv.id, { state: 'fail', concerns: v.concerns });
                  fileLog(`reviewer found issues in ${row.reviewTaskId || ''} round ${round + 1}: ${String(v.concerns).slice(0, 200)}`);
                  failingConcerns.push({ taskId: row.reviewTaskId, summary: row.te.summary, concerns: v.concerns });
                }
              }
              pendingReviewEvs = [];   // all rows resolved
              // #2 BACKFILL: if the agent closed a task with a BLANK summary, the
              // tier rollup, the task footer, and the acceptance input would show
              // "(no summary)". Synthesize one with a dedicated single-shot
              // summarizer over the task's diff (the auto-commit already used the
              // user-request fallback). Best-effort; only fires on blank summaries.
              if (session.activeProject) {
                for (const row of reviewRows) {
                  if (!row.reviewTaskId || String(row.te.summary || '').trim()) continue;
                  let summary = '';
                  try {
                    const t = reviewTasks.find((x) => x.taskId === row.reviewTaskId);
                    summary = await runSummarizer({ adapter, model, task: t, signal, onLog: (m) => fileLog(m) });
                  } catch (e) { fileLog(`summary backfill failed for ${row.reviewTaskId}: ${e.message}`); }
                  if (summary) {
                    chatState.patchChatEvent(row.te.id, { summary });   // footer + persisted event + acceptance input
                    try {
                      const ts = createTasksStore(session.activeProject);
                      await ts.init();
                      await ts.setSummary(row.reviewTaskId, summary);   // tier-rollup source
                    } catch (e) { fileLog(`setSummary failed for ${row.reviewTaskId}: ${e.message}`); }
                    fileLog(`backfilled blank summary for ${row.reviewTaskId}: ${summary.slice(0, 80)}`);
                  }
                }
              }
              if (failingConcerns.length === 0) break;   // every closed task passed/escalated — round done
              // Build one combined re-run message naming each failing task.
              const failedConcerns = failingConcerns.length === 1
                ? failingConcerns[0].concerns
                : failingConcerns.map((f) => `• Task ${f.taskId || ''}${f.summary ? ` (${f.summary})` : ''}:\n${f.concerns}`).join('\n\n');
              round++;
              // SINGLE flagged task → RE-OPEN it so the review-fix folds into the SAME task and they
              // roll up together (instead of a wasteful separate task for work we already know is
              // about this change). Multi-task rounds keep the combined-fix behavior (can't merge
              // several into one). Needs the foreground store (orchestrator default-on provides it).
              let reopened = false;
              if (failingConcerns.length === 1 && failingConcerns[0].taskId && fgStore && typeof fgStore.reopenTask === 'function') {
                try { reopened = await fgStore.reopenTask(failingConcerns[0].taskId); } catch (e) { fileLog(`reopenTask failed: ${e.message}`); }
              }
              // Re-invoke with the reviewer's concerns as the next agent input. Suppress the
              // user-message event since the review event itself already shows the feedback.
              if (reopened) {
                fileLog(`reviewer: re-opened ${failingConcerns[0].taskId} so the fix folds into it`);
                userMessage = `The reviewer found issues with the task you just closed — it has been RE-OPENED, so your fix lands in the SAME task. Address these concerns, then re-close it with <next-task> when fixed. Do NOT open a new task for this.\n\n${failedConcerns}`;
              } else {
                const taskNoun = failingConcerns.length === 1 ? 'the task you just closed' : 'tasks you just closed';
                userMessage = `The reviewer found issues with ${taskNoun}:\n\n${failedConcerns}\n\nPlease address these concerns, then close the task again with <next-task> when fixed.`;
              }
              emitInitialUserMessage = false;
              fgPhase('uploading');
            }
            const dur = Date.now() - chatStart;
            fileLog(`chat DONE (with review): rounds=${round + 1} duration=${dur}ms`);
            logEvent('agent_run', {
              turns: lastAgentResult.stats.turns,
              toolCalls: lastAgentResult.stats.toolCalls,
              batches: lastAgentResult.stats.batches,
              terminated: lastAgentResult.terminated,
              autoClosed: lastAgentResult.stats.autoClosed,
              reviewRounds: round + 1,
            });
            // Survey every tracked repo. Anything dirty AND not on a
            // CODE_BOSS_ branch surfaces in one consolidated chat row
            // the user can click to trigger a per-repo branch+commit.
            // Guard against the user closing the project mid-chat —
            // session.activeProject can be null here.
            if (session.activeProject && session.git?.repoList?.length) {
              const dirtyRepos = [];
              for (const repoDir of session.git.repoList) {
                const branch = await getCurrentBranch(repoDir);
                if (isCodeBossBranch(branch)) continue;
                const status = await getStatus(repoDir);
                if (status.dirty === false) continue;   // null (status failed) is NOT clean — surface it rather than hide it
                dirtyRepos.push({
                  repoDir,
                  relDir: nodePath.relative(session.activeProject, repoDir) || '.',
                  branch,
                  changeCount: status.lines.length,
                  changes: status.lines.slice(0, 20),
                });
              }
              if (dirtyRepos.length) {
                chatState.appendChatEvent({
                  type: 'git-pending-resolution',
                  resolved: false,
                  repos: dirtyRepos,
                });
                fileLog(`git-pending-resolution: ${dirtyRepos.length} repo(s) need branch+commit (${dirtyRepos.map((r) => r.relDir).join(', ')})`);
              }
            }
            // AUTO-ACCEPTANCE: when the worker run completes and an assignment
            // was being worked, run the requirement-fulfilment check and surface
            // a verdict gate. The user remains the final approver (Accept/Edit/
            // Pause); we only set the verdict + leave the assignment 'active'.
            if (activeAsg && session.assignments && session.activeProject && !discussMode) {
              try {
                chatState.setPhase('manager');
                const asgNow = await session.assignments.get(activeAsg.id);
                const taskIds = new Set(asgNow?.taskIds || []);
                const verdictByTask = new Map();
                for (const ev of session.chatEvents) {
                  if (ev.type === 'review' && ev.reviewTaskId) {
                    if (ev.state === 'pass') verdictByTask.set(ev.reviewTaskId, { pass: true });
                    else if (ev.state === 'escalate') verdictByTask.set(ev.reviewTaskId, { pass: false, escalated: true, concerns: ev.concerns });
                    else verdictByTask.set(ev.reviewTaskId, { pass: false, concerns: ev.concerns });
                  }
                }
                const acceptanceTasks = session.chatEvents
                  .filter((e) => e.type === 'task-end' && taskIds.has(e.taskId))
                  .map((te) => ({
                    taskId: te.taskId,
                    userRequest: te.userRequest,
                    summary: te.summary,
                    events: session.chatEvents.filter((e) => e.taskId === te.taskId),
                    verdict: verdictByTask.get(te.taskId),
                  }));
                const verdict = await runAcceptance({
                  adapter, model,
                  assignment: { id: asgNow.id, requirement: asgNow.requirement, acceptanceCriteria: asgNow.acceptanceCriteria },
                  tasks: acceptanceTasks,
                  signal,
                  onLog: (m) => fileLog(m),
                  onDiag: (d) => chatState.appendChatEvent({ type: 'manager-diagnostic', ...d }),
                  repoRoots: session.git?.repoList || [],
                  projectDir: session.activeProject,   // cwd-independent path resolution (no process.chdir)
                });
                await session.assignments.setVerdict(asgNow.id, { round: round + 1, ...verdict });
                // Surface per-task review state on the gate so the Accept
                // decision is loud when a bound task escalated (review hit the
                // round limit unresolved) — acceptance no longer silently
                // overrides it; the UI warns + requires an explicit confirm.
                // With the continuous reviewer on, the per-task end-review didn't run — so DON'T synthesize
                // fake 'pass' rows (which would read as "a review passed it"). The reviewer's findings
                // already surfaced live in the chat (the 🔍 markers); the gate just shows acceptance.
                const taskVerdicts = criticMode ? [] : acceptanceTasks.map((t) => ({
                  taskId: t.taskId,
                  state: t.verdict?.escalated ? 'escalate' : (t.verdict && t.verdict.pass === false ? 'fail' : 'pass'),
                  concerns: t.verdict?.concerns || '',
                }));
                const escalationCount = taskVerdicts.filter((v) => v.state === 'escalate').length;
                chatState.appendChatEvent({
                  type: 'assignment-gate', assignmentId: asgNow.id,
                  fulfilled: verdict.fulfilled, missing: verdict.missing,
                  drift: verdict.drift, rationale: verdict.rationale,
                  taskVerdicts, escalationCount,
                });
                if (escalationCount) fileLog(`acceptance gate for ${asgNow.id}: ${escalationCount} escalated task(s) flagged`);
                broadcast({ type: 'backlog', version: bumpVersion() });
                fileLog(`acceptance ${asgNow.id}: fulfilled=${verdict.fulfilled}${verdict.missing ? ' — missing: ' + String(verdict.missing).slice(0, 150) : ''}`);
              } catch (e) {
                fileLog(`acceptance check failed: ${e.message}`);
              }
            }
            // NOTE: the post snapshot is taken in the finally, AFTER the terminal status
            // flips. Capturing here (before the flip) would close a cosmetic stale-canUndo
            // race — but it delays 'done' by several git spawns on non-repo projects, widening
            // the window where a fast follow-up (e.g. the gate's "Send fix" click) gets a 409.
            // The stale button is harmless: /api/undo-last-run refuses when no post snapshot
            // exists, so the destructive path is unreachable either way.
            if (session.fork) {
              // This inline run was forked into a background task — fold its result as a card
              // (not the foreground 'done') and free the foreground (manager → inline again).
              const fk = session.fork; session.fork = null;
              chatState.appendChatEvent({ type: 'subagent-card', subagentId: fk.id, topic: fk.topic, status: 'done', content: (lastAgentResult && lastAgentResult.content) || 'done' });
              chatState.broadcast({ type: 'subagent-event', version: chatState.bumpVersion(), event: { type: 'subagent-done', id: fk.id, status: 'done' } });
              chatState.setStatus('idle');
            } else {
              chatState.setStatus('done');
            }
          } catch (e) {
            const dur = Date.now() - chatStart;
            const aborted = e?.name === 'AbortError' || /aborted/i.test(e?.message ?? '');
            // Don't leave 'reviewing…' rows spinning forever if the reviewer
            // was interrupted (abort / network error mid-review). One combined
            // call can have several task footers pending at once.
            if (pendingReviewEvs.length) {
              for (const ev of pendingReviewEvs) {
                try { chatState.patchChatEvent(ev.id, { state: 'error', concerns: aborted ? 'review canceled' : ('review failed: ' + e.message) }); } catch {}
              }
              pendingReviewEvs = [];
            }
            if (session.fork) {
              // A forked (backgrounded) run errored — surface it as the sub-agent's card, not
              // a foreground error; the foreground manager stays usable.
              const fk = session.fork; session.fork = null;
              chatState.appendChatEvent({ type: 'subagent-card', subagentId: fk.id, topic: fk.topic, status: aborted ? 'aborted' : 'error', content: `(${aborted ? 'aborted' : 'failed'}: ${e.message})` });
              chatState.broadcast({ type: 'subagent-event', version: chatState.bumpVersion(), event: { type: 'subagent-done', id: fk.id, status: aborted ? 'aborted' : 'error' } });
              chatState.setStatus('idle');
              fileLog(`chat (forked ${fk.id}) ${aborted ? 'ABORTED' : 'ERROR'} after ${dur}ms: ${e.message}`);
              return;   // skip the foreground error finalization; the finally still cleans up
            }
            chatState.setError(aborted ? (session.error || 'aborted') : e.message);
            chatState.setStatus('error');
            fileLog(`chat ${aborted ? 'ABORTED' : 'ERROR'} after ${dur}ms: ${e.message}`);
            if (!aborted && e?.stack) {
              fileLog(`stack: ${e.stack.split('\n').slice(0, 4).join(' | ')}`);
            }
            logEvent('error', { message: e.message });
          } finally {
            // Record the post-run delta — or clear a no-change undo point (all paths:
            // success, error, abort). Idempotent; see the timing note at the success exit.
            await captureShadowPost();
            clearTimeout(timeoutId);
            // Ownership-guarded: if a follow-up run already replaced session.abortController while we
            // awaited the shadow post-snapshot above, this stale finally must not null the LIVE run's
            // controller (that made it uncancellable) or reset its phase/assignment binding.
            if (session.abortController === myController) {
              session.abortController = null;
              session.chatStartedAt = null;
              // Clear the run-level assignment binding so the next free-form
              // message isn't mis-attributed (the assignment's lifecycle status
              // persists in the store; this only unbinds the current run).
              session.activeAssignment = null;
              chatState.setPhase('idle');
            }
            exitProjectCwd(entered);
          }
        })().catch((e) => {
          // Shouldn't reach here — errors are handled inside. But if it does,
          // make sure we know.
          fileLog(`/api/message background promise leaked an error: ${e?.message ?? e}`);
        });

        return sendJson(res, 200, { ok: true, version: session.version });
      }
      if (req.method === 'POST' && path === '/api/cancel') {
        // Cancel the foreground/forked worker AND an in-flight manager turn (separate controller).
        let any = false;
        if (session.managerAbort) { try { session.managerAbort.abort(); any = true; } catch {} }
        if (session.abortController) { try { session.abortController.abort(); any = true; } catch {} }
        return sendJson(res, 200, { ok: true, version: session.version, ...(any ? {} : { note: 'no chat in progress' }) });
      }
      // "What's new" after a self-update: check-update drops whats-new.json next
      // to the running bundle ({fromBuild, toBuild, entries}); the UI shows it
      // once and the dismiss deletes it. Absent file → {} (the normal case).
      if (req.method === 'GET' && path === '/api/whats-new') {
        const p = whatsNewPath || (process.argv[1] ? nodePath.join(nodePath.dirname(process.argv[1]), 'whats-new.json') : null);
        try {
          if (p && existsSync(p)) return sendJson(res, 200, JSON.parse(await readFile(p, 'utf8')));
        } catch (e) { fileLog(`whats-new read failed: ${e.message}`); }
        return sendJson(res, 200, {});
      }
      if (req.method === 'POST' && path === '/api/whats-new-seen') {
        const p = whatsNewPath || (process.argv[1] ? nodePath.join(nodePath.dirname(process.argv[1]), 'whats-new.json') : null);
        try { if (p && existsSync(p)) await unlink(p); } catch (e) { fileLog(`whats-new-seen unlink failed: ${e.message}`); }
        return sendJson(res, 200, { ok: true });
      }
      // Act on a sub-agent (review loop / crash-resume): re-review or discard. The UI calls
      // this for recovered or awaiting-review sub-agents.
      if (req.method === 'POST' && path === '/api/subagent-action') {
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const id = (body?.id || '').toString();
        const action = (body?.action || '').toString();
        const orch = session.orchestrator;
        if (!orch) return sendJson(res, 409, { error: 'orchestrator not active' });
        if (!id) return sendJson(res, 400, { error: 'id required' });
        if (action === 'review') { const ok = orch.review(id); return sendJson(res, 200, { ok, action }); }
        if (action === 'discard') { const ok = orch.abort(id); return sendJson(res, 200, { ok, action }); }
        return sendJson(res, 400, { error: 'action must be review|discard' });
      }
      if (req.method === 'POST' && path === '/api/probe-thinking') {
        // Diagnostic: fire a battery of "thinking/effort" param shapes at the
        // configured OpenAI-compatible endpoint (the Gemini shim) and report,
        // CONCISELY, which ones the shim honors + whether reasoning tokens are
        // exposed. Runs against ${baseURL}/chat/completions with the same Bearer
        // auth the app uses. Results are for a human to read (screenshot), not a
        // gate. (Gemini 3.1 Pro: thinking is always-on; thinkingLevel/
        // reasoning_effort = low|medium|high; native counts in thoughtsTokenCount,
        // OpenAI-compat in usage.completion_tokens_details.reasoning_tokens.)
        const raw = await readBody(req);
        let body = {}; try { body = raw ? JSON.parse(raw) : {}; } catch {}
        const model = String(body?.model || session.defaultModel || '').trim();
        const base = String(session.baseURL || '').replace(/\/+$/, '');
        if (!base || base === '(dev)') return sendJson(res, 400, { ok: false, error: 'no baseURL configured' });
        if (!model) return sendJson(res, 400, { ok: false, error: 'no model selected' });
        if (!session.apiKey) return sendJson(res, 400, { ok: false, error: 'no API key set (Settings → API key)' });
        // A small multi-step question that reliably induces measurable thinking.
        const PROMPT = 'A bat and a ball cost $1.10 total. The bat costs $1.00 more than the ball. How much does the ball cost? Reply with ONLY the dollar amount.';
        const variants = [
          { label: 'baseline (no param)', extra: {} },
          { label: 'reasoning_effort=low', extra: { reasoning_effort: 'low' } },
          { label: 'reasoning_effort=high', extra: { reasoning_effort: 'high' } },
          { label: 'reasoning_effort=none', extra: { reasoning_effort: 'none' } },
          { label: 'google.thinking_config(high)+thoughts', extra: { google: { thinking_config: { thinking_level: 'high', include_thoughts: true } } } },
          { label: 'thinking_level=high (top-level)', extra: { thinking_level: 'high' } },
        ];
        const dig = (o, p) => { let v = o; for (const k of p.split('.')) v = (v == null ? undefined : v[k]); return v; };
        const firstNum = (...vals) => { for (const v of vals) if (typeof v === 'number') return v; return null; };
        const rows = [];
        let reasoningField = null;
        for (const v of variants) {
          const ac = new AbortController();
          const tid = setTimeout(() => ac.abort(), 30000);
          const t0 = Date.now();
          try {
            const r = await fetch(`${base}/chat/completions`, {
              method: 'POST',
              headers: { 'content-type': 'application/json', authorization: `Bearer ${session.apiKey}` },
              body: JSON.stringify({ model, messages: [{ role: 'user', content: PROMPT }], max_tokens: 2048, ...v.extra }),
              signal: ac.signal,
            });
            const ms = Date.now() - t0;
            const text = await r.text();
            let j = null; try { j = JSON.parse(text); } catch {}
            if (!r.ok) {
              rows.push({ label: v.label, status: r.status, ok: false, err: String(dig(j, 'error.message') || text || '').replace(/\s+/g, ' ').slice(0, 140) });
              continue;
            }
            const u = (j && j.usage) || {};
            const reasoning = firstNum(
              dig(u, 'completion_tokens_details.reasoning_tokens'),
              u.reasoning_tokens, u.thoughts_token_count, u.thoughtsTokenCount,
              dig(j, 'usageMetadata.thoughtsTokenCount'), dig(j, 'usage_metadata.thoughts_token_count'),
            );
            if (reasoning != null && !reasoningField) {
              reasoningField = dig(u, 'completion_tokens_details.reasoning_tokens') != null ? 'usage.completion_tokens_details.reasoning_tokens'
                : u.reasoning_tokens != null ? 'usage.reasoning_tokens'
                : (u.thoughts_token_count != null || u.thoughtsTokenCount != null) ? 'usage.thoughts_token_count'
                : 'usageMetadata.thoughtsTokenCount';
            }
            const out = firstNum(u.completion_tokens, u.output_tokens, dig(j, 'usageMetadata.candidatesTokenCount'));
            const prompt = firstNum(u.prompt_tokens, u.input_tokens, dig(j, 'usageMetadata.promptTokenCount'));
            const total = firstNum(u.total_tokens, dig(j, 'usageMetadata.totalTokenCount'));
            // If no reasoning field is exposed, INFER hidden thinking from the
            // token accounting: total - prompt - visible-output. If that's >0 and
            // scales with effort, the endpoint IS thinking even though it hides
            // the count. (The visible answer is ~4 tokens, so a big total = thinking.)
            const inferred = (reasoning == null && total != null && prompt != null && out != null) ? Math.max(0, total - prompt - out) : null;
            const msg = dig(j, 'choices.0.message') || {};
            const thought = !!(msg.reasoning || msg.reasoning_content);
            // The MODEL'S ANSWER. The prompt is the bat-and-ball CRT: the reasoned
            // answer is $0.05, the intuitive-but-wrong one is $0.10. When token
            // accounting is identical/hidden across variants (the genai.mil case),
            // the answer is the ONLY observable signal of whether reasoning_effort
            // actually changes anything — if low/none give $0.10 but high gives
            // $0.05, reasoning IS being modulated despite hidden tokens.
            const answer = String(msg.content || '').replace(/\s+/g, ' ').trim().slice(0, 48);
            rows.push({ label: v.label, status: r.status, ok: true, out, prompt, total, reasoning, inferred, thought, ms, answer });
          } catch (e) {
            rows.push({ label: v.label, status: 0, ok: false, err: (e && e.name === 'AbortError') ? 'timeout (30s)' : String(e && e.message || e).slice(0, 140) });
          } finally { clearTimeout(tid); }
        }
        return sendJson(res, 200, { ok: true, model, baseURL: base, reasoningField, rows });
      }
      if (req.method === 'GET' && path === '/api/image-support') {
        // Current discovered image-format state for the UI: tested=false means
        // the probe never ran (send images optimistically in the default
        // format); tested=true + format=null means the probe found NOTHING
        // works (the UI strips images from sends with a notice).
        let cfg = null;
        try { cfg = JSON.parse(readFileSync(imageSupportFile, 'utf8')); } catch { /* absent/invalid = untested */ }
        return sendJson(res, 200, {
          tested: !!cfg,
          format: (cfg && typeof cfg.format === 'string') ? cfg.format : null,
          testedAt: cfg?.testedAt ?? null,
          model: cfg?.model ?? null,
        });
      }
      if (req.method === 'POST' && path === '/api/probe-images') {
        // Diagnostic discovery: the genai.mil shim's image support is unknown,
        // so empirically try EACH candidate wire format against the REAL
        // adapter with a tiny solid-magenta PNG and the prompt "what color is
        // this?". A format "works" iff the request succeeds AND the reply names
        // the color — a 200 with a colorblind answer means the shim swallowed
        // the image. First working format is persisted to image-support.json
        // (format:null when none worked) and the adapter picks it up on the
        // next image-carrying request.
        const rawPI = await readBody(req);
        let bodyPI = {}; try { bodyPI = rawPI ? JSON.parse(rawPI) : {}; } catch {}
        const model = String(bodyPI?.model || session.defaultModel || '').trim();
        if (!model) return sendJson(res, 400, { ok: false, error: 'no model selected' });
        const probeImage = 'data:image/png;base64,' + MAGENTA_PROBE_PNG_BASE64;
        const PROMPT = 'Reply with only the dominant color of this image.';
        const results = [];
        let winner = null;
        for (const format of IMAGE_PROBE_FORMATS) {
          const ac = new AbortController();
          const tid = setTimeout(() => { try { ac.abort(); } catch {} }, 30000);
          try {
            const resp = await adapter.complete({
              model,
              messages: [{ role: 'user', content: PROMPT, images: [probeImage] }],
              max_tokens: 512,
              imageFormat: format,     // adapter-level override for this try
              signal: ac.signal,
            });
            const reply = String(resp?.choices?.[0]?.message?.content ?? '').replace(/\s+/g, ' ').trim();
            const hit = /magenta|pink|fuchsia|purple/i.test(reply);
            results.push({ format, ok: hit, status: 200, snippet: reply.slice(0, 120) || '(empty reply)' });
            if (hit && !winner) winner = format;
          } catch (e) {
            const status = Number(e?.status) || 0;
            const error = (e?.name === 'AbortError' ? 'timeout (30s)' : String(e?.message || e)).replace(/\s+/g, ' ').slice(0, 140);
            results.push({ format, ok: false, status, error });
          } finally { clearTimeout(tid); }
        }
        try {
          mkdirSync(nodePath.dirname(imageSupportFile), { recursive: true });
          writeFileSync(imageSupportFile, JSON.stringify({ format: winner, testedAt: new Date().toISOString(), model }, null, 2) + '\n');
        } catch (e) { fileLog(`image-support write failed: ${e.message}`); }
        fileLog(`probe-images: ${winner ? `images enabled via ${winner}` : 'no format worked — images disabled'} (${results.map((r) => `${r.format}=${r.ok ? 'ok' : 'fail'}`).join(', ')})`);
        return sendJson(res, 200, { ok: true, model, format: winner, results });
      }
      if (req.method === 'POST' && path === '/api/extend-timeout') {
        const raw = await readBody(req);
        let body; try { body = JSON.parse(raw); } catch { return sendJson(res, 400, { error: 'invalid JSON' }); }
        const ms = Number(body?.ms);
        if (!ms || ms < 60000 || ms > 6 * 3600 * 1000) {
          return sendJson(res, 400, { error: 'ms must be 60000..21600000' });
        }
        chatState.setTimeout(ms);
        return sendJson(res, 200, { ok: true, version: session.version, timeoutMs: ms });
      }
      if (req.method === 'POST' && path === '/api/chat') {
        // DEPRECATED / DISABLED. Superseded by /api/message. This legacy
        // handler had no running-lock and registered no abortController, so
        // overlapping use (a stale old-build tab, or two tabs) raced the
        // GLOBAL process.cwd() mid-loop → tools read/write the wrong repo,
        // and /api/cancel couldn't stop it. The shipped UI and all tests use
        // /api/message; this endpoint is unreachable from them. Reject it so
        // the racy path can never run. (Dead body below kept for reference.)
        return sendJson(res, 410, { error: 'deprecated endpoint; use /api/message' });
        // eslint-disable-next-line no-unreachable
        const raw = await readBody(req);
        const body = JSON.parse(raw);
        res.writeHead(200, {
          'content-type': 'text/event-stream',
          'cache-control': 'no-cache',
          'connection': 'keep-alive',
        });

        const send = (chunk) => res.write(`data: ${JSON.stringify(chunk)}\n\n`);
        const emitText = (text) => send({
          id: 'agent', object: 'chat.completion.chunk',
          choices: [{ index: 0, delta: { content: text }, finish_reason: null }],
        });
        // Custom UI events (Claude-Code-style dots / collapsible results).
        const emitEvent = (cb) => send({ _cb: cb });
        const emitFinish = (reason, usage) => send({
          id: 'agent', object: 'chat.completion.chunk',
          choices: [{ index: 0, delta: {}, finish_reason: reason }],
          ...(usage ? { usage } : {}),
        });

        // Point the agent's read tools at the active project. Single-user
        // local app — process.chdir for the duration of the request is OK.
        const prevCwd = process.cwd();
        let cwdChanged = false;
        if (session.activeProject) {
          try { cwdChanged = true; /* process.chdir removed — global cwd retired; this endpoint is dead (410) */ }
          catch (e) {
            emitText(`(error: cannot enter project directory: ${e.message})`);
            emitFinish('stop');
            res.write('data: [DONE]\n\n');
            return res.end();
          }
        } else {
          emitText('(no project open — pick one from the picker first)\n');
          emitFinish('stop');
          res.write('data: [DONE]\n\n');
          return res.end();
        }

        const lastUser = [...(body.messages ?? [])].reverse().find((m) => m.role === 'user');
        fileLog(`chat: model=${body.model} q="${String(lastUser?.content ?? '').slice(0, 200).replace(/\s+/g, ' ')}"`);
        try {
          let emittedAnything = false;
          const result = await runPromptedAgent({
            adapter,
            messages: body.messages,
            model: body.model,
            projectDir: session.activeProject,
            maxTurns: 20,
            onText: (delta) => {
              if (delta) { emittedAnything = true; emitText(delta); }
            },
            onToolCalls: (calls) => {
              for (const c of calls) {
                emittedAnything = true;
                // For task ops, send a more readable label.
                const label = c.verb;
                const argStr = Object.entries(c.args)
                  .filter(([k]) => k !== 'content')  // skip body for write
                  .map(([k, v]) => `${k}=${String(v).slice(0, 60)}`)
                  .join(', ');
                emitEvent({ t: 'tool', name: c.name ?? label, verb: label, args: argStr });
              }
            },
            onToolResult: ({ name, verb, status, content, diff }) => {
              const nonEmptyLines = content.split('\n').filter((l) => l.trim()).length;
              const v = verb ?? name;
              let summary;
              if (status === 'error') summary = content.split('\n')[0].slice(0, 140);
              else if (name === 'list_dir' || v === 'list') summary = `${nonEmptyLines} entries`;
              else if (name === 'read_file' || v === 'read') summary = `Read ${content.split('\n').length} lines`;
              else if (name === 'grep' || v === 'search') summary = content.startsWith('(no matches') ? 'No matches' : `${nonEmptyLines} matching lines`;
              else if (name === 'glob' || v === 'find') summary = content.startsWith('(no matches') ? 'No files' : `${nonEmptyLines} files`;
              else if (name === 'write_file' || v === 'write') summary = content.replace(/^wrote\s+/, '');
              else if (name === 'edit_file' || v === 'edit') summary = content.replace(/^edited\s+/, '');
              else if (name === 'powershell' || v === 'powershell') {
                const m = content.match(/^exit (\S+)\s+in\s+(\d+ms)/);
                summary = m ? `exit ${m[1]} in ${m[2]}` : `${content.length} bytes`;
              }
              else if (v === 'next-task') summary = content.replace(/^closed previous task.*?opened new task /, '→ ');
              else if (v === 'list-tasks') summary = `${nonEmptyLines} tasks`;
              else if (v === 'task-detail') summary = 'task detail';
              else if (v === 'search-tasks') summary = `${nonEmptyLines} matches`;
              else summary = `${content.length} chars`;
              emitEvent({ t: 'result', status, summary, full: content.slice(0, UI_RESULT_PREVIEW) });
              if (diff) {
                emitEvent({ t: 'diff', diff, summary });
              }
            },
            onLog: (msg) => fileLog(msg),
          });
          fileLog(`chat done: terminated=${result.terminated} turns=${result.stats.turns} calls=${result.stats.toolCalls} batches=[${result.stats.batches.join(',')}]`);
          // Detect the "model returned nothing useful" case.
          if (!emittedAnything && (!result.content || result.content.trim().length === 0)) {
            emitText('\n⚠️ Model returned an empty response (terminated=' +
              (result.terminated ?? 'unknown') +
              '). Open the Snapshot to see the raw response.\n');
          }
          emitFinish('stop', { total_tokens: result.stats.toolCalls });
          logEvent('agent_run', {
            turns: result.stats.turns,
            toolCalls: result.stats.toolCalls,
            batches: result.stats.batches,
            terminated: result.terminated,
            autoClosed: result.stats.autoClosed,
          });
        } catch (e) {
          logEvent('error', { message: e.message });
          fileLog(`chat ERROR: ${e.message}`);
          send({ error: { message: e.message, stack: e.stack } });
        } finally {
          void cwdChanged; void prevCwd;   /* process.chdir restore removed — global cwd retired (dead 410 path) */
          res.write('data: [DONE]\n\n');
          res.end();
        }
        return;
      }
      sendText(res, 404, 'Not found');
    } catch (e) {
      logEvent('error', { message: e.message });
      const body = JSON.stringify({ error: { message: e.message, stack: e.stack } });
      res.writeHead(500, { 'content-type': 'application/json' });
      res.end(body);
    }
  });

  if (initialProject) {
    try { await openProject(initialProject); }
    catch (e) { console.error('[server] could not open initial project:', e.message); }
  }

  // Flush any pending debounced save on shutdown. Without this, an event
  // appended within the last 300ms before the user closes the PowerShell
  // window / Ctrl-C's the bundle is lost (it lived only in the pending
  // setTimeout). The flush is synchronous so it completes inside the signal
  // handler before the process exits.
  let _shutdownFlushed = false;
  const flushOnShutdown = () => {
    if (_shutdownFlushed) return; _shutdownFlushed = true;
    try { if (_saveTimer) { clearTimeout(_saveTimer); _saveTimer = null; } flushConversationSave(); } catch {}
    // Multi-instance: drop the per-project lock so the next instance can take
    // the project over without waiting for the stale-pid path.
    try { releaseInstanceLock(); } catch {}
  };
  process.once('SIGINT', () => { flushOnShutdown(); process.exit(130); });
  process.once('SIGTERM', () => { flushOnShutdown(); process.exit(143); });
  process.once('beforeExit', flushOnShutdown);

  // Closing the server cancels-and-flushes the pending debounced save FIRST,
  // synchronously. Otherwise the 300ms timer can fire AFTER close() returns —
  // re-creating .code_boss/conversation.json(.tmp) just as a caller tears the
  // project dir down, which surfaces as an ENOTEMPTY rmdir race (and, in prod,
  // a stray write into a directory we're done with). The signal handlers above
  // cover hard kills; this covers the graceful close() path.
  const _rawClose = server.close.bind(server);
  server.close = (cb) => {
    try { if (_saveTimer) { clearTimeout(_saveTimer); _saveTimer = null; flushConversationSave(); } } catch {}
    try { releaseInstanceLock(); } catch {}   // graceful close = clean shutdown for the lock too
    return _rawClose(cb);
  };

  // PACKAGED mode (the VBS launcher sets CB_PACKAGED=1): if the user's Chrome
  // window is gone, the launcher taskkills us — but a crashed launcher or an
  // orphaned start must not leave node processes accumulating. Watchdog: no
  // SSE subscriber AND no run in flight for 30 consecutive minutes → exit.
  if (process.env.CB_PACKAGED === '1') {
    let idleMinutes = 0;
    const watchdog = setInterval(() => {
      const idle = session.streamSubscribers.size === 0 && session.status !== 'running';
      idleMinutes = idle ? idleMinutes + 1 : 0;
      if (idleMinutes >= 30) {
        try { fileLog('packaged-mode watchdog: no UI client + idle for 30 min — exiting'); } catch {}
        // Best-effort flush of the debounced conversation save — exit() skips
        // the beforeExit/signal flushers. Drop the instance lock too.
        try { flushConversationSave(); } catch {}
        try { releaseInstanceLock(); } catch {}
        process.exit(0);
      }
    }, 60000);
    watchdog.unref();
  }

  return new Promise((resolve, reject) => {
    server.once('error', reject);
    server.listen(port, '127.0.0.1', () => resolve({ server, session, port, flushConversationSave }));
  });
}

// ====== deploy-cmd.mjs ======
/**
 * Deployment subcommands for the single-file bundle (wired in the build footer):
 *
 *   node code-boss.mjs extract [dir] [--force] [--prune]
 *     Reconstructs the project source tree (src/ + scripts/ + docs + deploy/)
 *     from the EMBEDDED_SRC_GZ_B64 blob the build embeds — so ONE copied .mjs
 *     can recreate a buildable repo on the air-gapped production box. Warns
 *     about stale map-owned files the release no longer ships (they would be
 *     re-embedded by the next build); --prune deletes exactly those.
 *
 *   node code-boss.mjs check-update
 *     Compares this bundle's BUILD_VERSION against releases/manifest.json at
 *     the UPDATE_TAG ref of the UPDATE_SOURCE GitLab repo (CONFIG consts in the
 *     bundle header). If newer: downloads the release file, validates it with
 *     `node --check`, swaps it in place (keeping code-boss.prev.mjs for
 *     rollback). Single-flight via code-boss.update.lock — concurrent checkers
 *     skip instead of racing the swap. NEVER blocks a launch: every failure
 *     prints a reason and returns 0 — the launcher runs this best-effort
 *     before starting the app.
 *
 * Auth reuses the app's own stored GitLab credentials
 * (~/.code_boss/gitlab-creds.json, set in the UI) — the launcher never
 * collects or reads secrets itself.
 */

// The build embeds the source map as a module-level const AFTER the bundled
// sources (same pattern as DOCX_HELPER_SOURCES); in the un-bundled dev tree it
// simply doesn't exist. typeof keeps the reference safe in both contexts.
function getEmbeddedSourceMap() {
  /* eslint-disable no-undef */
  if (typeof EMBEDDED_SRC_GZ_B64 === 'undefined' || !EMBEDDED_SRC_GZ_B64) return null;
  const json = zlib.gunzipSync(Buffer.from(EMBEDDED_SRC_GZ_B64, 'base64')).toString('utf8');
  return JSON.parse(json);   // { "src/server.mjs": "<base64 bytes>", ... }
}

// Owned roots = the top-level dirs the embed map ships (src/, scripts/, …).
// Root-level map files are always rewritten by the extract loop, so only
// directories can hide stale files. .git/releases/dist/node_modules are never
// owned and never descended into, even when nested inside an owned root.
async function collectExtractStale(target, map) {
  const ownedRoots = new Set();
  for (const rel of Object.keys(map)) {
    const i = rel.indexOf('/');
    if (i > 0) ownedRoots.add(rel.slice(0, i));
  }
  const never = new Set(['.git', 'releases', 'dist', 'node_modules']);
  const stale = [];
  async function walkOwned(relDir) {
    let entries = [];
    try { entries = await readdir(path.join(target, relDir), { withFileTypes: true }); } catch { return; }
    for (const e of entries) {
      const rel = `${relDir}/${e.name}`;
      if (e.isDirectory()) { if (!never.has(e.name)) await walkOwned(rel); }
      else if (!Object.hasOwn(map, rel)) stale.push(rel);
    }
  }
  for (const root of ownedRoots) if (!never.has(root)) await walkOwned(root);
  return stale.sort();
}

async function runExtractCommand({ argv = [] } = {}) {
  const force = argv.includes('--force');
  const prune = argv.includes('--prune');
  const target = path.resolve(argv.find((a) => !a.startsWith('--')) || 'code_boss-src');
  const map = getEmbeddedSourceMap();
  if (!map) {
    console.error('extract: this copy has no embedded source map (running from the dev tree, or built before the embed existed). Build with scripts/build.mjs and use dist/code-boss.mjs.');
    return 1;
  }
  if (existsSync(target)) {
    // A FILE target would slip past the readdir guard (readdir throws →
    // entries stays []) and crash the write loop with a raw ENOTDIR.
    let targetStat = null;
    try { targetStat = await stat(target); } catch {}
    if (targetStat && !targetStat.isDirectory()) {
      console.error(`extract: ${target} exists and is not a directory — choose a directory path.`);
      return 1;
    }
    let entries = [];
    try { entries = await readdir(target); } catch {}
    if (entries.length && !force) {
      console.error(`extract: ${target} is not empty — pass --force to write into it anyway.`);
      return 1;
    }
  }
  const files = Object.keys(map);
  let overwritten = 0;
  for (const rel of files) {
    const abs = path.join(target, rel);
    await mkdir(path.dirname(abs), { recursive: true });
    if (existsSync(abs)) overwritten++;
    await writeFile(abs, Buffer.from(map[rel], 'base64'));
  }
  console.log(`extract: wrote ${files.length} files to ${target} (${overwritten} overwritten, ${files.length - overwritten} new)`);
  // Stale reconcile: a map-owned file this release no longer ships lingers in
  // the production clone forever and gets RE-EMBEDDED by the next release
  // built there. Warn loudly (still exit 0); --prune deletes exactly those.
  const stale = await collectExtractStale(target, map);
  if (stale.length) {
    if (prune) {
      for (const rel of stale) { try { await rm(path.join(target, rel), { force: true }); } catch {} }
      console.log(`extract: pruned ${stale.length} stale file(s): ${stale.join(', ')}`);
    } else {
      console.error(`extract: WARNING — ${stale.length} stale file(s) not part of this release (likely deleted upstream) — remove before building a release (or re-run with --prune):`);
      for (const rel of stale) console.error(`  ${rel}`);
    }
  }
  console.log('next steps there:  npm install (optional, tests only)  →  node scripts/build.mjs --release  →  commit + move the production tag');
  return 0;
}

// ── check-update ────────────────────────────────────────────────────────────

// CB_HOME relocates the user dir for the whole app (see server.mjs) — creds
// must come from the same place the UI writes them.
const cbHomeDir = () => process.env.CB_HOME || homedir();

async function readUpdateCreds() {
  try {
    const raw = await readFile(path.join(cbHomeDir(), '.code_boss', 'gitlab-creds.json'), 'utf8');
    const j = JSON.parse(raw);
    return j && j.token ? j : null;
  } catch { return null; }
}

async function deployApiGet(url, token, { asJson = true, timeoutMs = 15000 } = {}) {
  const res = await fetch(url, {
    headers: token ? { 'PRIVATE-TOKEN': token } : {},
    signal: AbortSignal.timeout(timeoutMs),
  });
  if (!res.ok) throw new Error(`HTTP ${res.status} for ${url.replace(/\?.*$/, '')}`);
  if (!asJson) return res.text();
  const text = await res.text();
  // A sign-in/proxy HTML page where JSON belongs is the classic misconfig symptom.
  if (text.trimStart().startsWith('<')) throw new Error('got an HTML page instead of the manifest — check UPDATE_SOURCE and the stored GitLab token');
  return JSON.parse(text);
}

// Single-flight: two launchers checking at once must not both swap (the loser
// would replace the winner's freshly-installed bundle, or worse delete it
// mid-rename). `wx` is atomic; a lock older than ~10 minutes is a crash
// leftover and may be taken over exactly once.
async function acquireUpdateLock(lockPath) {
  for (let attempt = 0; attempt < 2; attempt++) {
    try { await writeFile(lockPath, String(process.pid), { flag: 'wx' }); return true; }
    catch (e) {
      if (e?.code !== 'EEXIST') return false;
      let ageMs = 0;
      try { ageMs = Date.now() - (await stat(lockPath)).mtimeMs; } catch {}
      if (attempt > 0 || ageMs < 10 * 60 * 1000) return false;
      try { await rm(lockPath, { force: true }); } catch {}
    }
  }
  return false;
}

async function runUpdateCheckCommand({ bundlePath, version, updateSource, updateTag = 'production' } = {}) {
  const skip = (reason) => { console.log(`check-update: skipped — ${reason}`); return 0; };
  try {
    if (!updateSource) return skip('UPDATE_SOURCE not configured in this build (set updateSource in ~/.code_boss/build-config.json and rebuild)');
    if (!bundlePath || !existsSync(bundlePath)) return skip(`bundle path not found (${bundlePath})`);
    const creds = await readUpdateCreds();
    if (!creds) return skip('no GitLab credentials yet (~/.code_boss/gitlab-creds.json — set them in the app UI Settings)');

    // UPDATE_SOURCE is the repo's web URL, e.g. https://gitlab.example.mil/group/code_boss
    const u = new URL(updateSource);
    const projectPath = u.pathname.replace(/^\/+|\/+$|\.git$/g, '');
    const api = `${u.origin}/api/v4/projects/${encodeURIComponent(projectPath)}`;

    const manifest = await deployApiGet(`${api}/repository/files/${encodeURIComponent('releases/manifest.json')}/raw?ref=${encodeURIComponent(updateTag)}`, creds.token);
    const m = typeof manifest === 'string' ? JSON.parse(manifest) : manifest;
    if (!m || !m.build || !m.file) return skip('releases/manifest.json at the tag is malformed');
    if (String(m.build) === String(version)) { console.log(`check-update: up to date (build ${version})`); return 0; }

    const lockPath = path.join(path.dirname(bundlePath), 'code-boss.update.lock');
    if (!(await acquireUpdateLock(lockPath))) return skip('another check-update is in progress');
    try {
      console.log(`check-update: build ${m.build} available (running ${version}) — downloading ${m.file}`);
      let body;
      try {
        body = await deployApiGet(`${api}/repository/files/${encodeURIComponent(m.file)}/raw?ref=${encodeURIComponent(updateTag)}`, creds.token, { asJson: false, timeoutMs: 120000 });
      } catch (e) {
        return skip(`network error while downloading ${m.file} (${e?.message || e}) — will retry next launch`);
      }
      if (!/BUILD_VERSION/.test(body) || body.length < 200000) return skip('downloaded file does not look like a code_boss bundle');

      // Per-process temp name: a concurrent checker must never clobber (or
      // rm) another's download. Must keep an .mjs extension — `node --check`
      // hard-errors on unknown extensions (ERR_UNKNOWN_FILE_EXTENSION) before
      // even parsing.
      const newPath = path.join(path.dirname(bundlePath), `code-boss.next.${process.pid}.mjs`);
      await writeFile(newPath, body, 'utf8');
      const parse = spawnSync(process.execPath, ['--check', newPath], { encoding: 'utf8' });
      if (parse.status !== 0) {
        try { await rm(newPath, { force: true }); } catch {}
        return skip(`downloaded bundle failed node --check (${(parse.stderr || '').slice(0, 200)})`);
      }

      // Another process may have installed this build while we downloaded —
      // the swap must compare against the file on disk, not our own version.
      let installedBuild = null;
      try { installedBuild = (await readFile(bundlePath, 'utf8')).match(/const BUILD_VERSION = '(\d+)'/)?.[1] ?? null; } catch {}
      if (String(installedBuild) === String(m.build)) {
        try { await rm(newPath, { force: true }); } catch {}
        return skip(`already at build ${m.build} (installed by another process)`);
      }

      const prevPath = path.join(path.dirname(bundlePath), 'code-boss.prev.mjs');
      try { await rm(prevPath, { force: true }); } catch {}
      await rename(bundlePath, prevPath);
      try {
        await rename(newPath, bundlePath);
      } catch (e) {
        // bundlePath must NEVER be left empty: put the previous bundle back.
        try { await rename(prevPath, bundlePath); } catch {}
        try { await rm(newPath, { force: true }); } catch {}
        return skip(`swap failed (${e?.message || e}) — previous bundle restored`);
      }
      console.log(`check-update: UPDATED build ${version} → ${m.build} (previous kept as code-boss.prev.mjs)`);

      // "What's new": MERGE the user-facing notes into any un-dismissed
      // whats-new.json — never overwrite notes the user has not seen yet. The
      // floor is the build the user last SAW (existing fromBuild), not the
      // build we just replaced. The server serves the file once via
      // /api/whats-new and the UI shows the banner; dismissing deletes it.
      // Best-effort — a notes failure must not un-do a successful update.
      try {
        const wnFile = path.join(path.dirname(bundlePath), 'whats-new.json');
        let existing = null;
        try { existing = JSON.parse(await readFile(wnFile, 'utf8')); } catch {}
        const floorRaw = Number(existing?.fromBuild);
        const floor = Number.isFinite(floorRaw) ? floorRaw : Number(version);
        let entries = [];
        try {
          const hist = JSON.parse(await deployApiGet(`${api}/repository/files/${encodeURIComponent('releases/notes.json')}/raw?ref=${encodeURIComponent(updateTag)}`, creds.token, { asJson: false }));
          entries = (Array.isArray(hist) ? hist : [])
            .filter((e) => e && typeof e === 'object')   // one bad element must not nuke the whole history
            .filter((e) => Number(e.build) > floor && Number(e.build) <= Number(m.build));
        } catch {
          // fallback only for genuine upgrades past the user's last-seen build
          if (m.notes && Number(m.build) > floor) entries = [{ build: Number(m.build), title: m.notes.title || '', items: m.notes.items || [] }];
        }
        const carried = Array.isArray(existing?.entries) ? existing.entries.filter((e) => e && typeof e === 'object') : [];
        const seenBuilds = new Set();
        const union = [...entries, ...carried]
          .filter((e) => { const b = Number(e.build); if (seenBuilds.has(b)) return false; seenBuilds.add(b); return true; })
          .sort((x, y) => Number(y.build) - Number(x.build));
        // Write whenever the union is non-empty — even when THIS release added
        // nothing, the rewrite fixes a stale toBuild from a carried-over file.
        if (union.length) {
          await writeFile(wnFile,
            JSON.stringify({ fromBuild: floor, toBuild: Number(m.build), at: new Date().toISOString(), entries: union }, null, 2), 'utf8');
          console.log(`check-update: saved what's-new notes for ${union.length} release(s)`);
        }
      } catch {}
      return 0;
    } finally {
      try { await rm(lockPath, { force: true }); } catch {}
    }
  } catch (e) {
    return skip(e?.message || String(e));
  }
}

// ====== embedded docx helper Java sources (written + compiled on first run) ======
const DOCX_HELPER_SOURCES = {"com/codeboss/docx/DocxHelper.java":"package com.codeboss.docx;\r\n\r\nimport org.apache.poi.xwpf.usermodel.IBody;\r\nimport org.apache.poi.xwpf.usermodel.IBodyElement;\r\nimport org.apache.poi.xwpf.usermodel.ParagraphAlignment;\r\nimport org.apache.poi.xwpf.usermodel.UnderlinePatterns;\r\nimport org.apache.poi.xwpf.usermodel.XWPFDocument;\r\nimport org.apache.poi.xwpf.usermodel.XWPFFooter;\r\nimport org.apache.poi.xwpf.usermodel.XWPFHeader;\r\nimport org.apache.poi.xwpf.usermodel.XWPFParagraph;\r\nimport org.apache.poi.xwpf.usermodel.XWPFRun;\r\nimport org.apache.poi.xwpf.usermodel.XWPFSDT;\r\nimport org.apache.poi.xwpf.usermodel.XWPFStyle;\r\nimport org.apache.poi.xwpf.usermodel.XWPFStyles;\r\nimport org.apache.poi.xwpf.usermodel.XWPFTable;\r\nimport org.apache.poi.xwpf.usermodel.XWPFTableCell;\r\nimport org.apache.poi.xwpf.usermodel.XWPFTableRow;\r\n\r\nimport javax.xml.namespace.QName;\r\nimport org.apache.xmlbeans.XmlCursor;\r\nimport org.apache.xmlbeans.XmlObject;\r\nimport org.openxmlformats.schemas.wordprocessingml.x2006.main.CTR;\r\nimport org.openxmlformats.schemas.wordprocessingml.x2006.main.CTRPr;\r\nimport org.openxmlformats.schemas.wordprocessingml.x2006.main.CTRPrChange;\r\nimport org.openxmlformats.schemas.wordprocessingml.x2006.main.CTRunTrackChange;\r\nimport org.openxmlformats.schemas.wordprocessingml.x2006.main.CTText;\r\n\r\nimport java.io.ByteArrayInputStream;\r\nimport java.io.FileOutputStream;\r\nimport java.io.InputStream;\r\nimport java.io.PrintStream;\r\nimport java.math.BigInteger;\r\nimport java.nio.charset.StandardCharsets;\r\nimport java.nio.file.Files;\r\nimport java.nio.file.Path;\r\nimport java.nio.file.Paths;\r\nimport java.nio.file.StandardCopyOption;\r\nimport java.util.ArrayList;\r\nimport java.util.LinkedHashMap;\r\nimport java.util.List;\r\nimport java.util.Map;\r\n\r\n/**\r\n * code_boss full-fidelity .docx edit helper.\r\n *\r\n *   java -cp \"codeboss-docx-helper.jar;lib/*\" com.codeboss.docx.DocxHelper read|edit\r\n *\r\n * Reads ONE JSON request on stdin, writes ONE JSON response on stdout. All\r\n * docx fidelity (formatting, lists, headers/footers, images, tables,\r\n * hyperlinks, TOC fields) is preserved because POI re-serializes every part\r\n * we don't touch byte-faithfully and we only rewrite the text of the runs a\r\n * find/replace actually overlaps.\r\n *\r\n * READ : {\"op\":\"read\",\"docx\":\"<abs>\"}\r\n *        -> {\"ok\":true,\"docx\":..,\"paragraphs\":[{id,scope,location,style,listLevel,readOnly,text}],\"meta\":{hasFields}}\r\n * EDIT : {\"op\":\"edit\",\"docx\":\"<abs>\",\"dryRun\":false,\"ops\":[{\"id\":N,\"find\":\"..\",\"replace\":\"..\",\"allowFormatFlatten\":false}]}\r\n *        -> {\"ok\":true,\"applied\":N,\"updateFieldsOnOpen\":bool,\"ops\":[{id,before,after,runsTouched,flattened}]}\r\n * ERROR: {\"ok\":false,\"error\":\"..\",\"code\":\"..\",\"opIndex\":n|null}  (+ nonzero exit)\r\n */\r\npublic final class DocxHelper {\r\n\r\n  // exit codes\r\n  private static final int EX_OK = 0, EX_VALIDATION = 2, EX_FILE = 3, EX_BAD_REQUEST = 4, EX_VERIFY = 5;\r\n\r\n  private static PrintStream out;\r\n\r\n  public static void main(String[] args) {\r\n    try {\r\n      out = new PrintStream(System.out, true, \"UTF-8\");\r\n    } catch (Exception e) {\r\n      System.err.println(\"cannot init UTF-8 stdout: \" + e);\r\n      System.exit(EX_FILE);\r\n      return;\r\n    }\r\n    try {\r\n      if (args.length < 1) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"missing op (read|edit)\", null);\r\n      String op = args[0];\r\n      String input = readAll(System.in);\r\n      Map<String, Object> spec;\r\n      try {\r\n        spec = Json.obj(Json.parse(input));\r\n      } catch (Exception e) {\r\n        throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"invalid request JSON: \" + e.getMessage(), null);\r\n      }\r\n      Map<String, Object> res = dispatch(op, spec);\r\n      out.println(Json.write(res));\r\n    } catch (Fail f) {\r\n      emitError(f);\r\n      System.exit(f.exit);\r\n    } catch (Throwable t) {\r\n      emitError(new Fail(EX_FILE, \"file-error\", String.valueOf(t.getMessage()), null));\r\n      System.exit(EX_FILE);\r\n    }\r\n  }\r\n\r\n  static Map<String, Object> dispatch(String op, Map<String, Object> spec) throws Exception {\r\n    if (\"read\".equals(op)) return doRead(spec);\r\n    if (\"detail\".equals(op)) return doDetail(spec);\r\n    if (\"edit\".equals(op)) return doEdit(spec);\r\n    throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"unknown op: \" + op, null);\r\n  }\r\n\r\n  // ---------------------------------------------------------------- read ----\r\n  static Map<String, Object> doRead(Map<String, Object> spec) throws Exception {\r\n    Path path = docxPath(spec);\r\n    XWPFDocument doc = open(path);\r\n    try {\r\n      List<ParaRef> refs = enumerate(doc);\r\n      XWPFStyles xs = doc.getStyles();\r\n      boolean hasFields = false;\r\n      int revs = 0, eqs = 0, sdts = 0;\r\n      List<Object> paras = new ArrayList<>();\r\n      for (ParaRef r : refs) {\r\n        if (r.hasField) hasFields = true;\r\n        if (r.hasRevisions) revs++;\r\n        if (r.hasEquation) eqs++;\r\n        if (r.isSdt) sdts++;\r\n        Map<String, Object> m = new LinkedHashMap<>();\r\n        m.put(\"id\", (long) r.id);\r\n        m.put(\"scope\", r.scope);\r\n        m.put(\"location\", r.location);\r\n        m.put(\"style\", r.styleId);\r\n        m.put(\"listLevel\", r.listLevel);   // Long or null\r\n        m.put(\"readOnly\", r.readOnly);\r\n        m.put(\"text\", refText(r));\r\n        if (r.para != null) {\r\n          Integer ol = outlineLevelOf(r.para, xs);\r\n          if (ol != null) m.put(\"outlineLevel\", (long) (int) ol);\r\n          String al = String.valueOf(r.para.getAlignment());\r\n          if (al != null && !\"LEFT\".equals(al)) m.put(\"alignment\", al);\r\n          var nid = r.para.getNumID();\r\n          if (nid != null) m.put(\"numId\", nid.toString());\r\n          String fl = flagsOf(r.para);\r\n          if (!fl.isEmpty()) m.put(\"flags\", fl);\r\n        }\r\n        if (r.hasRevisions) m.put(\"hasRevisions\", Boolean.TRUE);\r\n        if (r.hasEquation) m.put(\"hasEquation\", Boolean.TRUE);\r\n        if (r.isSdt) m.put(\"contentControl\", Boolean.TRUE);\r\n        if (r.deletedText != null && !r.deletedText.isEmpty()) m.put(\"deletedText\", r.deletedText);\r\n        paras.add(m);\r\n      }\r\n      Map<String, Object> res = new LinkedHashMap<>();\r\n      res.put(\"ok\", Boolean.TRUE);\r\n      res.put(\"docx\", path.toString());\r\n      res.put(\"paragraphs\", paras);\r\n      Map<String, Object> meta = new LinkedHashMap<>();\r\n      meta.put(\"hasFields\", hasFields);\r\n      meta.put(\"paragraphCount\", (long) refs.size());\r\n      meta.put(\"revisionParagraphs\", (long) revs);\r\n      meta.put(\"equationParagraphs\", (long) eqs);\r\n      meta.put(\"contentControls\", (long) sdts);\r\n      meta.put(\"hasTrackChanges\", revs > 0);\r\n      meta.put(\"documentProtection\", documentProtection(doc));   // String or null\r\n      res.put(\"meta\", meta);\r\n      return res;\r\n    } finally {\r\n      doc.close();\r\n    }\r\n  }\r\n\r\n  // ---------------------------------------------------------------- edit ----\r\n  static Map<String, Object> doEdit(Map<String, Object> spec) throws Exception {\r\n    Path path = docxPath(spec);\r\n    boolean dryRun = Json.bool(spec.get(\"dryRun\"), false);\r\n    List<Object> opsRaw;\r\n    try {\r\n      opsRaw = Json.arr(spec.get(\"ops\"));\r\n    } catch (Exception e) {\r\n      throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"ops[] is required\", null);\r\n    }\r\n    if (opsRaw.isEmpty()) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"ops[] is empty\", null);\r\n\r\n    XWPFDocument doc = open(path);\r\n    try {\r\n      // Refuse to edit a protection-enforced document — a direct write would be\r\n      // illegitimate (and Word may reject the file).\r\n      String prot = documentProtection(doc);\r\n      if (prot != null) {\r\n        throw new Fail(EX_FILE, \"document-protected\",\r\n            \"document has editing protection enforced (\" + prot + \"); remove protection in Word before editing\", null);\r\n      }\r\n      List<ParaRef> refs = enumerate(doc);\r\n      Map<Integer, ParaRef> byId = new LinkedHashMap<>();\r\n      for (ParaRef r : refs) byId.put(r.id, r);\r\n\r\n      // Snapshot every paragraph's text (for the untouched-paragraphs verify).\r\n      Map<Integer, String> before = new LinkedHashMap<>();\r\n      for (ParaRef r : refs) before.put(r.id, refText(r));\r\n\r\n      // VALIDATE-ALL (atomic): nothing is mutated until every op is legal.\r\n      // expectedById layers multiple ops on the same paragraph so the verify\r\n      // target is exact.\r\n      Map<Integer, String> expectedById = new LinkedHashMap<>();\r\n      List<int[]> opTouched = new ArrayList<>();   // runsTouched per op\r\n      List<Boolean> opFlattened = new ArrayList<>();\r\n      List<Map<String, Object>> postChecks = new ArrayList<>();   // per-op structural post-conditions\r\n      XWPFStyles xs = doc.getStyles();\r\n      boolean hasFields = false;\r\n      for (ParaRef r : refs) if (r.hasField) hasFields = true;\r\n\r\n      // Track-changes config. A replace op is tracked if spec.tracked or\r\n      // op.asTracked is set, OR the doc is already under review (so new edits\r\n      // stay consistent with existing revisions rather than bypassing markup).\r\n      boolean specTracked = Json.bool(spec.get(\"tracked\"), false);\r\n      String trkAuthor = Json.str(spec.get(\"author\"));\r\n      String trkDate = Json.str(spec.get(\"date\"));\r\n      boolean docInReview = false;\r\n      for (ParaRef r : refs) if (r.hasRevisions) { docInReview = true; break; }\r\n      IdAlloc idAlloc = idAllocFor(doc);\r\n      boolean[] anyTracked = { false };\r\n\r\n      for (int oi = 0; oi < opsRaw.size(); oi++) {\r\n        Map<String, Object> op = Json.obj(opsRaw.get(oi));\r\n        String type = opType(op);\r\n        Integer id = Integer.valueOf(Json.intval(op.get(\"id\"), Integer.MIN_VALUE));\r\n        if (id == Integer.MIN_VALUE) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"op missing integer id\", oi);\r\n        ParaRef ref = byId.get(id);\r\n        if (ref == null) throw new Fail(EX_VALIDATION, \"anchor-not-found\", \"no paragraph with id \" + id + \" (re-read the document)\", oi);\r\n        if (ref.readOnly) throw new Fail(EX_VALIDATION, \"protected-region\", \"paragraph \" + id + \" is a field/TOC/tracked/content-control region and cannot be edited\", oi);\r\n\r\n        if (\"replace\".equals(type) || \"setRunFormat\".equals(type)) {\r\n          if (ref.para == null) throw new Fail(EX_VALIDATION, \"protected-region\", \"paragraph \" + id + \" has no editable text runs\", oi);\r\n          String find = Json.str(op.get(\"find\"));\r\n          if (find == null || find.isEmpty()) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"op \" + oi + \" missing non-empty find\", oi);\r\n          String curr = expectedById.getOrDefault(id, before.get(id));\r\n          int s = curr.indexOf(find);\r\n          if (s < 0) throw new Fail(EX_VALIDATION, \"not-found\", \"find not present in paragraph \" + id, oi);\r\n          if (curr.indexOf(find, s + 1) >= 0) throw new Fail(EX_VALIDATION, \"ambiguous-match\", \"find occurs more than once in paragraph \" + id + \" — make it more specific\", oi);\r\n          int e = s + find.length();\r\n\r\n          if (\"replace\".equals(type)) {\r\n            String replace = Json.str(op.get(\"replace\"));\r\n            if (replace == null) replace = \"\";\r\n            boolean allowFlatten = Json.bool(op.get(\"allowFormatFlatten\"), false);\r\n            StraddleInfo st = straddle(ref.para, s, e);\r\n            if (st != null && st.straddles && !allowFlatten) {\r\n              throw new Fail(EX_VALIDATION, \"format-straddle\",\r\n                  \"find spans a formatting change in paragraph \" + id + \" near offset \" + st.changeOffset\r\n                      + \" (\\\"\" + snippet(curr, st.changeOffset) + \"\\\"). Narrow find to stay within one format, split into per-format edits, \"\r\n                      + \"or set allowFormatFlatten:true (requires developer approval).\",\r\n                  oi);\r\n            }\r\n            opTouched.add(new int[]{ st == null ? 0 : st.runsTouched });\r\n            opFlattened.add(st != null && st.straddles && allowFlatten);\r\n            expectedById.put(id, curr.substring(0, s) + replace + curr.substring(e));\r\n          } else {   // setRunFormat — formats the span, no text change\r\n            Map<String, Object> fmt;\r\n            try { fmt = Json.obj(op.get(\"format\")); } catch (Exception ex) { throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"setRunFormat op \" + oi + \" missing format{}\", oi); }\r\n            if (fmt.isEmpty()) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"setRunFormat op \" + oi + \" empty format{}\", oi);\r\n            Map<String, Object> pc = new LinkedHashMap<>();\r\n            pc.put(\"type\", \"setRunFormat\"); pc.put(\"id\", (long) id); pc.put(\"find\", find); pc.put(\"format\", fmt);\r\n            postChecks.add(pc);\r\n            opTouched.add(new int[]{0}); opFlattened.add(Boolean.FALSE);\r\n          }\r\n        } else if (\"setParaStyle\".equals(type)) {\r\n          String style = Json.str(op.get(\"style\"));\r\n          if (style == null || style.isEmpty()) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"setParaStyle op \" + oi + \" missing style\", oi);\r\n          if (xs == null || xs.getStyle(style) == null) throw new Fail(EX_VALIDATION, \"no-such-style\", \"style \\\"\" + style + \"\\\" not found in this document\", oi);\r\n          Map<String, Object> pc = new LinkedHashMap<>();\r\n          pc.put(\"type\", \"setParaStyle\"); pc.put(\"id\", (long) id); pc.put(\"style\", style);\r\n          postChecks.add(pc);\r\n          opTouched.add(new int[]{0}); opFlattened.add(Boolean.FALSE);\r\n        } else if (\"setParaProps\".equals(type)) {\r\n          if (!(op.containsKey(\"alignment\") || op.containsKey(\"indentLeftTwips\") || op.containsKey(\"spacingBeforeTwips\") || op.containsKey(\"spacingAfterTwips\")))\r\n            throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"setParaProps op \" + oi + \" has no recognized property\", oi);\r\n          Map<String, Object> pc = new LinkedHashMap<>();\r\n          pc.put(\"type\", \"setParaProps\"); pc.put(\"id\", (long) id); pc.put(\"op\", op);\r\n          postChecks.add(pc);\r\n          opTouched.add(new int[]{0}); opFlattened.add(Boolean.FALSE);\r\n        } else if (\"setListMembership\".equals(type)) {\r\n          if (op.get(\"numId\") == null) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"setListMembership op \" + oi + \" missing numId\", oi);\r\n          Map<String, Object> pc = new LinkedHashMap<>();\r\n          pc.put(\"type\", \"setListMembership\"); pc.put(\"id\", (long) id); pc.put(\"numId\", op.get(\"numId\").toString());\r\n          postChecks.add(pc);\r\n          opTouched.add(new int[]{0}); opFlattened.add(Boolean.FALSE);\r\n        } else if (\"deleteParagraph\".equals(type)) {\r\n          // Structural: remove the paragraph's content. Tracked → marked as a\r\n          // deletion (struck, reviewer accepts); direct → emptied. Either way the\r\n          // paragraph remains so ids/count stay stable (clean verify).\r\n          if (ref.para == null) throw new Fail(EX_VALIDATION, \"protected-region\", \"paragraph \" + id + \" has no editable text runs\", oi);\r\n          expectedById.put(id, \"\");   // accepted text after deletion is empty\r\n          opTouched.add(new int[]{0}); opFlattened.add(Boolean.FALSE);\r\n        } else {\r\n          throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"unknown op type \\\"\" + type + \"\\\"\", oi);\r\n        }\r\n      }\r\n\r\n      // DRY-RUN: report would-be results, write nothing.\r\n      if (dryRun) {\r\n        return editResult(opsRaw, before, expectedById, opTouched, opFlattened, hasFields, false);\r\n      }\r\n\r\n      // APPLY-ALL in document order; re-find against the live paragraph so\r\n      // multiple ops on one paragraph compose correctly.\r\n      for (int oi = 0; oi < opsRaw.size(); oi++) {\r\n        Map<String, Object> op = Json.obj(opsRaw.get(oi));\r\n        String type = opType(op);\r\n        int id = Json.intval(op.get(\"id\"), Integer.MIN_VALUE);\r\n        ParaRef ref = byId.get(id);\r\n        if (ref == null || ref.para == null) continue;\r\n        switch (type) {\r\n          case \"setRunFormat\": {\r\n            boolean tr = specTracked || Json.bool(op.get(\"asTracked\"), false) || docInReview;\r\n            applyRunFormat(ref.para, Json.str(op.get(\"find\")), Json.obj(op.get(\"format\")), tr, trkAuthor, trkDate, idAlloc);\r\n            if (tr) anyTracked[0] = true;\r\n            break;\r\n          }\r\n          case \"setParaStyle\": ref.para.setStyle(Json.str(op.get(\"style\"))); break;\r\n          case \"setParaProps\": applyParaProps(ref.para, op); break;\r\n          case \"setListMembership\": applyListMembership(ref.para, op); break;\r\n          case \"deleteParagraph\": {\r\n            String whole = text(ref.para);\r\n            if (!whole.isEmpty()) {\r\n              boolean tr = specTracked || Json.bool(op.get(\"asTracked\"), false) || docInReview;\r\n              if (tr) { applyTrackedReplace(ref.para, whole, \"\", trkAuthor, trkDate, idAlloc); anyTracked[0] = true; }\r\n              else applyReplace(ref.para, whole, \"\", oi);\r\n            }\r\n            break;\r\n          }\r\n          default: {\r\n            String find = Json.str(op.get(\"find\"));\r\n            String replace = Json.str(op.get(\"replace\"));\r\n            if (replace == null) replace = \"\";\r\n            boolean tracked = specTracked || Json.bool(op.get(\"asTracked\"), false) || docInReview;\r\n            if (tracked) { applyTrackedReplace(ref.para, find, replace, trkAuthor, trkDate, idAlloc); anyTracked[0] = true; }\r\n            else applyReplace(ref.para, find, replace, oi);\r\n          }\r\n        }\r\n      }\r\n\r\n      // Authoring tracked edits → open the doc in track-changes mode.\r\n      if (anyTracked[0]) { try { doc.setTrackRevisions(true); } catch (Throwable ignore) {} }\r\n\r\n      // Flag TOC/TOF/TOT + other fields to refresh on next open in Word.\r\n      boolean updateFields = false;\r\n      if (hasFields) {\r\n        try { doc.enforceUpdateFields(); updateFields = true; } catch (Throwable ignore) {}\r\n      }\r\n\r\n      // ATOMIC WRITE + VERIFY: write a sibling temp, re-open it, confirm every\r\n      // edited paragraph matches its expected text and every other paragraph\r\n      // is byte-identical, then atomically move it over the original.\r\n      Path tmp = path.resolveSibling(path.getFileName().toString() + \".cb-tmp-\" + Long.toHexString(System.nanoTime()));\r\n      try {\r\n        try (FileOutputStream fos = new FileOutputStream(tmp.toFile())) {\r\n          doc.write(fos);\r\n        }\r\n        verify(tmp, before, expectedById, postChecks);\r\n        try {\r\n          Files.move(tmp, path, StandardCopyOption.REPLACE_EXISTING, StandardCopyOption.ATOMIC_MOVE);\r\n        } catch (Throwable atomicFail) {\r\n          Files.move(tmp, path, StandardCopyOption.REPLACE_EXISTING);\r\n        }\r\n      } finally {\r\n        try { Files.deleteIfExists(tmp); } catch (Throwable ignore) {}\r\n      }\r\n\r\n      Map<String, Object> result = editResult(opsRaw, before, expectedById, opTouched, opFlattened, hasFields, updateFields);\r\n      result.put(\"trackChanges\", anyTracked[0]);\r\n      return result;\r\n    } finally {\r\n      doc.close();\r\n    }\r\n  }\r\n\r\n  private static Map<String, Object> editResult(List<Object> opsRaw, Map<Integer, String> before,\r\n      Map<Integer, String> expectedById, List<int[]> touched, List<Boolean> flattened,\r\n      boolean hasFields, boolean updateFields) {\r\n    List<Object> opResults = new ArrayList<>();\r\n    // before/after per op: track the running text per id so each op's \"before\"\r\n    // reflects prior ops on the same paragraph.\r\n    Map<Integer, String> running = new LinkedHashMap<>(before);\r\n    for (int oi = 0; oi < opsRaw.size(); oi++) {\r\n      Map<String, Object> op = Json.obj(opsRaw.get(oi));\r\n      String type = opType(op);\r\n      int id = Json.intval(op.get(\"id\"), Integer.MIN_VALUE);\r\n      Map<String, Object> m = new LinkedHashMap<>();\r\n      m.put(\"id\", (long) id);\r\n      m.put(\"op\", type);\r\n      if (\"replace\".equals(type)) {\r\n        String find = Json.str(op.get(\"find\"));\r\n        String replace = Json.str(op.get(\"replace\"));\r\n        if (replace == null) replace = \"\";\r\n        String b = running.getOrDefault(id, \"\");\r\n        int s = b.indexOf(find);\r\n        String a = (s < 0) ? b : b.substring(0, s) + replace + b.substring(s + find.length());\r\n        running.put(id, a);\r\n        m.put(\"before\", b);\r\n        m.put(\"after\", a);\r\n        m.put(\"runsTouched\", (long) (touched.get(oi)[0]));\r\n        m.put(\"flattened\", flattened.get(oi));\r\n      } else if (\"setRunFormat\".equals(type)) {\r\n        m.put(\"find\", Json.str(op.get(\"find\")));\r\n        m.put(\"format\", op.get(\"format\"));\r\n      } else if (\"setParaStyle\".equals(type)) {\r\n        m.put(\"style\", Json.str(op.get(\"style\")));\r\n      } else if (\"deleteParagraph\".equals(type)) {\r\n        String b = running.getOrDefault(id, \"\");\r\n        running.put(id, \"\");\r\n        m.put(\"before\", b);\r\n        m.put(\"after\", \"\");\r\n      } else {\r\n        m.put(\"applied\", Boolean.TRUE);\r\n      }\r\n      opResults.add(m);\r\n    }\r\n    Map<String, Object> res = new LinkedHashMap<>();\r\n    res.put(\"ok\", Boolean.TRUE);\r\n    res.put(\"applied\", (long) opsRaw.size());\r\n    res.put(\"updateFieldsOnOpen\", updateFields);\r\n    res.put(\"hasFields\", hasFields);\r\n    res.put(\"ops\", opResults);\r\n    return res;\r\n  }\r\n\r\n  // -------------------------------------------------------- run surgery ----\r\n  /** Re-find `find` in the live paragraph and splice `replace` across runs. */\r\n  private static void applyReplace(XWPFParagraph para, String find, String replace, int opIndex) throws Fail {\r\n    List<XWPFRun> runs = para.getRuns();\r\n    int n = runs.size();\r\n    int[] starts = new int[n];\r\n    String[] texts = new String[n];\r\n    int p = 0;\r\n    StringBuilder full = new StringBuilder();\r\n    for (int i = 0; i < n; i++) {\r\n      String t = runs.get(i).getText(0);\r\n      if (t == null) t = \"\";\r\n      texts[i] = t;\r\n      starts[i] = p;\r\n      p += t.length();\r\n      full.append(t);\r\n    }\r\n    String all = full.toString();\r\n    int s = all.indexOf(find);\r\n    if (s < 0) throw new Fail(EX_VERIFY, \"verify-failed\", \"find vanished before apply in op \" + opIndex, opIndex);\r\n    if (all.indexOf(find, s + 1) >= 0) throw new Fail(EX_VERIFY, \"verify-failed\", \"find became ambiguous before apply in op \" + opIndex, opIndex);\r\n    int e = s + find.length();\r\n\r\n    List<Integer> ov = new ArrayList<>();\r\n    for (int i = 0; i < n; i++) {\r\n      int a = starts[i], b = a + texts[i].length();\r\n      if (texts[i].length() > 0 && a < e && b > s) ov.add(i);\r\n    }\r\n    int fi = ov.get(0), li = ov.get(ov.size() - 1);\r\n    String prefix = texts[fi].substring(0, s - starts[fi]);\r\n    String suffix = texts[li].substring(e - starts[li]);\r\n    if (fi == li) {\r\n      runs.get(fi).setText(prefix + replace + suffix, 0);\r\n    } else {\r\n      runs.get(fi).setText(prefix + replace, 0);\r\n      for (int k = fi + 1; k < li; k++) {\r\n        if (texts[k].length() > 0) runs.get(k).setText(\"\", 0);\r\n      }\r\n      runs.get(li).setText(suffix, 0);\r\n    }\r\n  }\r\n\r\n  // ---- P2 formatting / paragraph edit primitives ------------------------\r\n  static String opType(Map<String, Object> op) {\r\n    String t = Json.str(op.get(\"op\"));\r\n    return (t == null || t.isEmpty()) ? \"replace\" : t;\r\n  }\r\n\r\n  // Apply character formatting to the (unique) `find` span within a paragraph,\r\n  // splitting boundary runs so only the span is affected. The new boundary runs\r\n  // inherit the original run's full rPr (copied via XmlObject.set — lite-safe).\r\n  private static void applyRunFormat(XWPFParagraph p, String find, Map<String, Object> fmt,\r\n      boolean tracked, String who, String date, IdAlloc ids) {\r\n    String all = text(p);\r\n    int s = all.indexOf(find);\r\n    if (s < 0) return;\r\n    int e = s + find.length();\r\n    ensureBoundaryAt(p, e);\r\n    ensureBoundaryAt(p, s);\r\n    java.util.Calendar c0 = tracked ? cal(date) : null;\r\n    String author = (who == null || who.isEmpty()) ? \"code_boss\" : who;\r\n    List<XWPFRun> runs = p.getRuns();\r\n    int pos = 0;\r\n    for (XWPFRun run : runs) {\r\n      String t = run.getText(0);\r\n      if (t == null) t = \"\";\r\n      int a = pos, b = pos + t.length();\r\n      if (t.length() > 0 && a >= s && b <= e) {\r\n        // Snapshot the run's PREVIOUS rPr before mutating, so a tracked format\r\n        // change can record it in an rPrChange.\r\n        CTRPr oldClone = null;\r\n        if (tracked) { try { var cur = run.getCTR().getRPr(); if (cur != null) oldClone = (CTRPr) cur.copy(); } catch (Throwable ig) {} }\r\n        applyFmtToRun(run, fmt);\r\n        if (tracked) {\r\n          try {\r\n            CTRPr nowRpr = run.getCTR().getRPr();\r\n            if (nowRpr == null) nowRpr = run.getCTR().addNewRPr();\r\n            if (!nowRpr.isSetRPrChange()) {\r\n              CTRPrChange rpc = nowRpr.addNewRPrChange();\r\n              rpc.setAuthor(author); rpc.setId(ids.next()); rpc.setDate(c0);\r\n              if (oldClone != null) rpc.addNewRPr().set(oldClone); else rpc.addNewRPr();\r\n            }\r\n          } catch (Throwable ig) {}\r\n        }\r\n      }\r\n      pos = b;\r\n    }\r\n  }\r\n\r\n  // Ensure a run boundary exists at character offset `off` (split the straddling\r\n  // run, cloning its rPr so formatting is preserved on both halves).\r\n  private static void ensureBoundaryAt(XWPFParagraph p, int off) {\r\n    if (off <= 0) return;\r\n    List<XWPFRun> runs = p.getRuns();\r\n    int pos = 0;\r\n    for (int i = 0; i < runs.size(); i++) {\r\n      XWPFRun run = runs.get(i);\r\n      String t = run.getText(0);\r\n      if (t == null) t = \"\";\r\n      int a = pos, b = pos + t.length();\r\n      if (off > a && off < b) {\r\n        String pre = t.substring(0, off - a), suf = t.substring(off - a);\r\n        XWPFRun nr = p.insertNewRun(i + 1);\r\n        try { var src = run.getCTR().getRPr(); if (src != null) nr.getCTR().addNewRPr().set(src); } catch (Throwable ignore) {}\r\n        nr.setText(suf, 0);\r\n        run.setText(pre, 0);\r\n        return;\r\n      }\r\n      pos = b;\r\n    }\r\n  }\r\n\r\n  private static void applyFmtToRun(XWPFRun run, Map<String, Object> fmt) {\r\n    if (fmt.containsKey(\"bold\")) run.setBold(truthy(fmt.get(\"bold\")));\r\n    if (fmt.containsKey(\"italic\")) run.setItalic(truthy(fmt.get(\"italic\")));\r\n    if (fmt.containsKey(\"strike\")) run.setStrikeThrough(truthy(fmt.get(\"strike\")));\r\n    if (fmt.containsKey(\"font\")) { Object v = fmt.get(\"font\"); if (v != null) run.setFontFamily(String.valueOf(v)); }\r\n    if (fmt.containsKey(\"sizePt\")) { double d = numOf(fmt.get(\"sizePt\")); if (d > 0) run.setFontSize(d); }\r\n    if (fmt.containsKey(\"color\")) { Object v = fmt.get(\"color\"); if (v != null) run.setColor(String.valueOf(v).replace(\"#\", \"\")); }\r\n    if (fmt.containsKey(\"underline\")) run.setUnderline(underlineOf(String.valueOf(fmt.get(\"underline\"))));\r\n  }\r\n\r\n  private static boolean truthy(Object o) {\r\n    if (o instanceof Boolean) return (Boolean) o;\r\n    String s = String.valueOf(o).toLowerCase();\r\n    return s.equals(\"true\") || s.equals(\"1\") || s.equals(\"on\") || s.equals(\"yes\");\r\n  }\r\n  private static double numOf(Object o) {\r\n    if (o instanceof Number) return ((Number) o).doubleValue();\r\n    try { return Double.parseDouble(String.valueOf(o)); } catch (Exception e) { return -1; }\r\n  }\r\n  private static UnderlinePatterns underlineOf(String s) {\r\n    if (s == null) return UnderlinePatterns.SINGLE;\r\n    String u = s.trim().toLowerCase();\r\n    if (u.equals(\"none\") || u.equals(\"false\") || u.equals(\"0\")) return UnderlinePatterns.NONE;\r\n    try { return UnderlinePatterns.valueOf(s.trim().toUpperCase()); } catch (Exception e) { return UnderlinePatterns.SINGLE; }\r\n  }\r\n\r\n  private static void applyParaProps(XWPFParagraph p, Map<String, Object> op) {\r\n    if (op.containsKey(\"alignment\")) {\r\n      try { p.setAlignment(ParagraphAlignment.valueOf(String.valueOf(op.get(\"alignment\")).trim().toUpperCase())); } catch (Exception ignore) {}\r\n    }\r\n    if (op.containsKey(\"indentLeftTwips\")) p.setIndentationLeft(Json.intval(op.get(\"indentLeftTwips\"), 0));\r\n    if (op.containsKey(\"spacingBeforeTwips\")) p.setSpacingBefore(Json.intval(op.get(\"spacingBeforeTwips\"), 0));\r\n    if (op.containsKey(\"spacingAfterTwips\")) p.setSpacingAfter(Json.intval(op.get(\"spacingAfterTwips\"), 0));\r\n  }\r\n\r\n  private static void applyListMembership(XWPFParagraph p, Map<String, Object> op) {\r\n    Object numId = op.get(\"numId\");\r\n    if (numId != null) {\r\n      p.setNumID(java.math.BigInteger.valueOf(Json.intval(numId, 0)));\r\n      if (op.containsKey(\"ilvl\")) p.setNumILvl(java.math.BigInteger.valueOf(Json.intval(op.get(\"ilvl\"), 0)));\r\n    }\r\n  }\r\n\r\n  // ---- P3 track-changes (raw OOXML — POI has no revision authoring API) ----\r\n  private static final String WNS = \"http://schemas.openxmlformats.org/wordprocessingml/2006/main\";\r\n  private static final String XML_NS = \"http://www.w3.org/XML/1998/namespace\";\r\n\r\n  // Monotonic w:id allocator, seeded above every existing numeric w:id in the\r\n  // document so a new revision never collides with an existing id.\r\n  static final class IdAlloc {\r\n    long n;\r\n    IdAlloc(long start) { n = start; }\r\n    java.math.BigInteger next() { return java.math.BigInteger.valueOf(n++); }\r\n  }\r\n  static IdAlloc idAllocFor(XWPFDocument doc) {\r\n    long max = 0;\r\n    try {\r\n      String xml = doc.getDocument().xmlText();\r\n      var m = java.util.regex.Pattern.compile(\"[A-Za-z0-9]+:id=\\\"(\\\\d+)\\\"\").matcher(xml);\r\n      while (m.find()) { try { max = Math.max(max, Long.parseLong(m.group(1))); } catch (Exception e) {} }\r\n    } catch (Throwable ignore) {}\r\n    return new IdAlloc(max + 1);\r\n  }\r\n\r\n  private static java.util.Calendar cal(String date) {\r\n    java.util.Calendar c = new java.util.GregorianCalendar();\r\n    if (date != null) { try { c.setTime(java.util.Date.from(java.time.Instant.parse(date))); } catch (Exception e) {} }\r\n    return c;\r\n  }\r\n  private static void preserveSpace(XmlObject ctText) {\r\n    XmlCursor cc = ctText.newCursor();\r\n    try { cc.setAttributeText(new QName(XML_NS, \"space\"), \"preserve\"); } catch (Throwable t) {} finally { cc.dispose(); }\r\n  }\r\n\r\n  // Tracked find/replace: wrap the matched runs in <w:del> (w:t -> w:delText)\r\n  // and add a <w:ins> with the replacement, both stamped author/date/id. Final\r\n  // element order: [prefix][w:del old][w:ins new][suffix]. replace=\"\" = pure\r\n  // tracked deletion.\r\n  private static void applyTrackedReplace(XWPFParagraph p, String find, String replace, String author, String date, IdAlloc ids) throws Fail {\r\n    String all = text(p);\r\n    int s = all.indexOf(find);\r\n    if (s < 0) throw new Fail(EX_VERIFY, \"verify-failed\", \"find vanished before tracked apply\", null);\r\n    if (all.indexOf(find, s + 1) >= 0) throw new Fail(EX_VERIFY, \"verify-failed\", \"find became ambiguous before tracked apply\", null);\r\n    int e = s + find.length();\r\n    ensureBoundaryAt(p, e);\r\n    ensureBoundaryAt(p, s);\r\n    List<XWPFRun> old = runsInSpan(p, s, e);\r\n    if (old.isEmpty()) return;\r\n    java.util.Calendar c0 = cal(date);\r\n    String who = (author == null || author.isEmpty()) ? \"code_boss\" : author;\r\n\r\n    // <w:del> inserted before the first matched run, holding the old runs as delText\r\n    XmlCursor dc = old.get(0).getCTR().newCursor();\r\n    dc.beginElement(new QName(WNS, \"del\", \"w\"));\r\n    dc.toParent();\r\n    CTRunTrackChange del = (CTRunTrackChange) dc.getObject();\r\n    dc.dispose();\r\n    del.setAuthor(who); del.setId(ids.next()); del.setDate(c0);\r\n    for (XWPFRun r : old) {\r\n      CTR nr = del.addNewR();\r\n      var rpr = r.getCTR().getRPr();\r\n      if (rpr != null) nr.addNewRPr().set(rpr);\r\n      CTText dt = nr.addNewDelText();\r\n      String tx = r.getText(0);\r\n      dt.setStringValue(tx == null ? \"\" : tx);\r\n      preserveSpace(dt);\r\n    }\r\n    // <w:ins> with the replacement (skipped for a pure deletion)\r\n    if (replace != null && !replace.isEmpty()) {\r\n      XmlCursor ic = old.get(0).getCTR().newCursor();\r\n      ic.beginElement(new QName(WNS, \"ins\", \"w\"));\r\n      ic.toParent();\r\n      CTRunTrackChange ins = (CTRunTrackChange) ic.getObject();\r\n      ic.dispose();\r\n      ins.setAuthor(who); ins.setId(ids.next()); ins.setDate(c0);\r\n      CTR nr = ins.addNewR();\r\n      var rpr0 = old.get(0).getCTR().getRPr();\r\n      if (rpr0 != null) nr.addNewRPr().set(rpr0);\r\n      CTText t = nr.addNewT();\r\n      t.setStringValue(replace);\r\n      preserveSpace(t);\r\n    }\r\n    // remove the original (now-duplicated) matched runs\r\n    for (XWPFRun r : old) { XmlCursor rc = r.getCTR().newCursor(); rc.removeXml(); rc.dispose(); }\r\n  }\r\n\r\n  private static List<XWPFRun> runsInSpan(XWPFParagraph p, int s, int e) {\r\n    List<XWPFRun> out = new ArrayList<>();\r\n    int pos = 0;\r\n    for (XWPFRun r : p.getRuns()) {\r\n      String t = r.getText(0);\r\n      if (t == null) t = \"\";\r\n      int a = pos, b = pos + t.length();\r\n      if (t.length() > 0 && a >= s && b <= e) out.add(r);\r\n      pos = b;\r\n    }\r\n    return out;\r\n  }\r\n\r\n  /** Does the match span [s,e) cross a run-formatting boundary? */\r\n  private static StraddleInfo straddle(XWPFParagraph para, int s, int e) {\r\n    List<XWPFRun> runs = para.getRuns();\r\n    int n = runs.size();\r\n    int[] starts = new int[n];\r\n    String[] texts = new String[n];\r\n    int p = 0;\r\n    for (int i = 0; i < n; i++) {\r\n      String t = runs.get(i).getText(0);\r\n      if (t == null) t = \"\";\r\n      texts[i] = t;\r\n      starts[i] = p;\r\n      p += t.length();\r\n    }\r\n    String firstSig = null;\r\n    int runsTouched = 0;\r\n    int changeOffset = -1;\r\n    boolean straddles = false;\r\n    for (int i = 0; i < n; i++) {\r\n      int a = starts[i], b = a + texts[i].length();\r\n      if (texts[i].length() > 0 && a < e && b > s) {\r\n        runsTouched++;\r\n        String sig = fmtSig(runs.get(i));\r\n        if (firstSig == null) {\r\n          firstSig = sig;\r\n        } else if (!firstSig.equals(sig)) {\r\n          straddles = true;\r\n          if (changeOffset < 0) changeOffset = Math.max(a, s);\r\n        }\r\n      }\r\n    }\r\n    StraddleInfo info = new StraddleInfo();\r\n    info.straddles = straddles;\r\n    info.runsTouched = runsTouched;\r\n    info.changeOffset = changeOffset < 0 ? s : changeOffset;\r\n    return info;\r\n  }\r\n\r\n  private static String fmtSig(XWPFRun r) {\r\n    StringBuilder b = new StringBuilder();\r\n    b.append(r.isBold() ? '1' : '0');\r\n    b.append(r.isItalic() ? '1' : '0');\r\n    try { b.append(r.isStrikeThrough() ? '1' : '0'); } catch (Throwable t) { b.append('0'); }\r\n    b.append('|').append(String.valueOf(r.getUnderline()));\r\n    b.append('|').append(nz(r.getFontFamily()));\r\n    Object sz;\r\n    try { sz = r.getFontSizeAsDouble(); } catch (Throwable t) { sz = null; }\r\n    b.append('|').append(String.valueOf(sz));\r\n    b.append('|').append(nz(r.getColor()));\r\n    return b.toString();\r\n  }\r\n\r\n  // ----------------------------------------------------------- enumerate ----\r\n  private static List<ParaRef> enumerate(XWPFDocument doc) {\r\n    List<ParaRef> out = new ArrayList<>();\r\n    int[] id = {0};\r\n    walkBody(doc, \"body\", out, id);\r\n    int hi = 0;\r\n    for (XWPFHeader h : doc.getHeaderList()) walkBody(h, \"header[\" + (hi++) + \"]\", out, id);\r\n    int fi = 0;\r\n    for (XWPFFooter f : doc.getFooterList()) walkBody(f, \"footer[\" + (fi++) + \"]\", out, id);\r\n    return out;\r\n  }\r\n\r\n  private static void walkBody(IBody body, String loc, List<ParaRef> out, int[] id) {\r\n    for (IBodyElement el : body.getBodyElements()) {\r\n      if (el instanceof XWPFParagraph) {\r\n        out.add(makeRef(id[0]++, scopeOf(loc), loc, (XWPFParagraph) el));\r\n      } else if (el instanceof XWPFTable) {\r\n        XWPFTable tbl = (XWPFTable) el;\r\n        int ri = 0;\r\n        for (XWPFTableRow row : tbl.getRows()) {\r\n          int ci = 0;\r\n          for (XWPFTableCell cell : row.getTableCells()) {\r\n            walkBody(cell, loc + \"/table/r\" + ri + \"/c\" + ci, out, id);\r\n            ci++;\r\n          }\r\n          ri++;\r\n        }\r\n      } else if (el instanceof XWPFSDT) {\r\n        // Content control (structured document tag). Its text was previously\r\n        // INVISIBLE (getBodyElements doesn't descend into it). Surface it as a\r\n        // read-only node so the AI sees the content; editing SDT values is a\r\n        // later phase.\r\n        ParaRef r = new ParaRef();\r\n        r.id = id[0]++;\r\n        r.scope = \"sdt\";\r\n        r.location = loc + \"/sdt\";\r\n        r.isSdt = true;\r\n        r.readOnly = true;\r\n        try { r.sdtText = ((XWPFSDT) el).getContent().getText(); } catch (Throwable t) { r.sdtText = \"\"; }\r\n        out.add(r);\r\n      }\r\n    }\r\n  }\r\n\r\n  private static String scopeOf(String loc) {\r\n    if (loc.startsWith(\"header\")) return \"header\";\r\n    if (loc.startsWith(\"footer\")) return \"footer\";\r\n    if (loc.contains(\"/table/\")) return \"cell\";\r\n    return \"body\";\r\n  }\r\n\r\n  private static ParaRef makeRef(int id, String scope, String loc, XWPFParagraph para) {\r\n    ParaRef r = new ParaRef();\r\n    r.id = id;\r\n    r.scope = scope;\r\n    r.location = loc;\r\n    r.para = para;\r\n    r.styleId = para.getStyleID();\r\n    BigInteger ilvl = para.getNumIlvl();\r\n    r.listLevel = (ilvl == null) ? null : Long.valueOf(ilvl.longValue());\r\n    r.hasField = hasField(para);\r\n    r.hasRevisions = hasRevisions(para);\r\n    r.hasEquation = hasEquation(para);\r\n    r.deletedText = deletedText(para);\r\n    // Protect TOC/field AND revision-bearing paragraphs from editing for now —\r\n    // editing within tracked changes is handled by the dedicated track-changes\r\n    // phase, not the plain find/replace path (which is blind to del/ins runs).\r\n    r.readOnly = r.hasField || isTocStyle(r.styleId) || r.hasRevisions;\r\n    return r;\r\n  }\r\n\r\n  // A paragraph carries tracked changes if it has ins/del/move runs or a\r\n  // property-change (rPrChange/pPrChange). Detected on the raw CT model so it\r\n  // is robust regardless of how POI's getRuns() treats revision wrappers.\r\n  private static boolean hasRevisions(XWPFParagraph p) {\r\n    try {\r\n      var ctp = p.getCTP();\r\n      if (ctp.sizeOfInsArray() > 0 || ctp.sizeOfDelArray() > 0\r\n          || ctp.sizeOfMoveFromArray() > 0 || ctp.sizeOfMoveToArray() > 0) return true;\r\n      if (ctp.getPPr() != null && ctp.getPPr().isSetPPrChange()) return true;\r\n    } catch (Throwable ignore) {}\r\n    for (XWPFRun run : p.getRuns()) {\r\n      try {\r\n        var rpr = run.getCTR().getRPr();\r\n        if (rpr != null && rpr.isSetRPrChange()) return true;\r\n      } catch (Throwable ignore) {}\r\n    }\r\n    return false;\r\n  }\r\n\r\n  // Recover the text of tracked deletions (w:delText), which plain text() drops\r\n  // entirely — so a doc already under review reads honestly.\r\n  private static String deletedText(XWPFParagraph p) {\r\n    StringBuilder b = new StringBuilder();\r\n    try {\r\n      for (var del : p.getCTP().getDelList()) {\r\n        for (var r : del.getRList()) {\r\n          for (var t : r.getDelTextList()) {\r\n            String v = t.getStringValue();\r\n            if (v != null) b.append(v);\r\n          }\r\n        }\r\n      }\r\n    } catch (Throwable ignore) {}\r\n    return b.toString();\r\n  }\r\n\r\n  // OMML equations carry no w:t, so a math-only paragraph reads as empty. Flag\r\n  // it so the AI doesn't treat the paragraph as missing content.\r\n  private static boolean hasEquation(XWPFParagraph p) {\r\n    try { return p.getCTP().xmlText().contains(\"oMath\"); } catch (Throwable t) { return false; }\r\n  }\r\n\r\n  // Editing-blocking document protection (read-only / comments-only /\r\n  // filling-forms / tracked-changes-enforced). Returns a comma list or null.\r\n  private static String documentProtection(XWPFDocument doc) {\r\n    java.util.List<String> on = new java.util.ArrayList<>();\r\n    try { if (doc.isEnforcedReadonlyProtection()) on.add(\"readOnly\"); } catch (Throwable ignore) {}\r\n    try { if (doc.isEnforcedCommentsProtection()) on.add(\"comments\"); } catch (Throwable ignore) {}\r\n    try { if (doc.isEnforcedFillingFormsProtection()) on.add(\"fillingForms\"); } catch (Throwable ignore) {}\r\n    try { if (doc.isEnforcedTrackedChangesProtection()) on.add(\"trackedChanges\"); } catch (Throwable ignore) {}\r\n    return on.isEmpty() ? null : String.join(\",\", on);\r\n  }\r\n\r\n  // Text of a ParaRef: a real paragraph's run text, or a content-control's text.\r\n  private static String refText(ParaRef r) {\r\n    if (r.para != null) return text(r.para);\r\n    return r.sdtText == null ? \"\" : r.sdtText;\r\n  }\r\n\r\n  // -------------------------------------------------------------- detail ----\r\n  // Layer-1 \"Word open\" detail for specific paragraphs: per-run AUTHORED rPr\r\n  // deltas + EFFECTIVE formatting (resolved through the style cascade, which\r\n  // POI does not do), paragraph properties, and char-offset ranges so any\r\n  // sub-span is addressable.\r\n  static Map<String, Object> doDetail(Map<String, Object> spec) throws Exception {\r\n    Path path = docxPath(spec);\r\n    XWPFDocument doc = open(path);\r\n    try {\r\n      List<ParaRef> refs = enumerate(doc);\r\n      Map<Integer, ParaRef> byId = new LinkedHashMap<>();\r\n      for (ParaRef r : refs) byId.put(r.id, r);\r\n      XWPFStyles xs = doc.getStyles();\r\n      Map<String, Rpr> cache = new LinkedHashMap<>();\r\n\r\n      List<Integer> ids = new ArrayList<>();\r\n      Object a = spec.get(\"anchors\");\r\n      if (a instanceof List) for (Object o : (List<?>) a) ids.add(Json.intval(o, Integer.MIN_VALUE));\r\n      if (ids.isEmpty()) for (ParaRef r : refs) ids.add(r.id);   // default: whole doc (caller should scope)\r\n\r\n      List<Object> out = new ArrayList<>();\r\n      for (int id : ids) {\r\n        ParaRef r = byId.get(id);\r\n        if (r == null) continue;\r\n        Map<String, Object> m = new LinkedHashMap<>();\r\n        m.put(\"id\", (long) id);\r\n        m.put(\"scope\", r.scope);\r\n        m.put(\"location\", r.location);\r\n        m.put(\"readOnly\", r.readOnly);\r\n        if (r.para == null) {                 // content control / non-paragraph node\r\n          m.put(\"contentControl\", Boolean.TRUE);\r\n          m.put(\"text\", refText(r));\r\n          out.add(m);\r\n          continue;\r\n        }\r\n        XWPFParagraph p = r.para;\r\n        m.put(\"style\", r.styleId);\r\n        m.put(\"styleName\", styleName(xs, r.styleId));\r\n        Integer ol = outlineLevelOf(p, xs);\r\n        if (ol != null) m.put(\"outlineLevel\", (long) (int) ol);\r\n        m.put(\"alignment\", String.valueOf(p.getAlignment()));\r\n        var numId = p.getNumID();\r\n        if (numId != null) {\r\n          Map<String, Object> num = new LinkedHashMap<>();\r\n          num.put(\"numId\", numId.toString());\r\n          var il = p.getNumIlvl();\r\n          num.put(\"ilvl\", il == null ? null : Long.valueOf(il.longValue()));\r\n          m.put(\"numbering\", num);\r\n        }\r\n        Map<String, Object> pPr = new LinkedHashMap<>();\r\n        try { int v = p.getIndentationLeft(); if (v != -1) pPr.put(\"indentLeftTwips\", (long) v); } catch (Throwable t) {}\r\n        try { int v = p.getSpacingBefore(); if (v != -1) pPr.put(\"spacingBeforeTwips\", (long) v); } catch (Throwable t) {}\r\n        try { int v = p.getSpacingAfter(); if (v != -1) pPr.put(\"spacingAfterTwips\", (long) v); } catch (Throwable t) {}\r\n        if (!pPr.isEmpty()) m.put(\"pPr\", pPr);\r\n\r\n        List<Object> runs = new ArrayList<>();\r\n        List<XWPFRun> rs = p.getRuns();\r\n        int pos = 0;\r\n        for (int i = 0; i < rs.size(); i++) {\r\n          XWPFRun run = rs.get(i);\r\n          String t = run.getText(0);\r\n          if (t == null) t = \"\";\r\n          Map<String, Object> rm = new LinkedHashMap<>();\r\n          rm.put(\"r\", (long) i);\r\n          rm.put(\"start\", (long) pos);\r\n          rm.put(\"end\", (long) (pos + t.length()));\r\n          rm.put(\"text\", t);\r\n          Map<String, Object> authored = authoredDelta(run);\r\n          if (!authored.isEmpty()) rm.put(\"authored\", authored);\r\n          rm.put(\"effective\", effectiveMap(effectiveRpr(run, r.styleId, xs, cache)));\r\n          runs.add(rm);\r\n          pos += t.length();\r\n        }\r\n        m.put(\"runs\", runs);\r\n        out.add(m);\r\n      }\r\n      Map<String, Object> res = new LinkedHashMap<>();\r\n      res.put(\"ok\", Boolean.TRUE);\r\n      res.put(\"docx\", path.toString());\r\n      res.put(\"paragraphs\", out);\r\n      return res;\r\n    } finally {\r\n      doc.close();\r\n    }\r\n  }\r\n\r\n  // Resolved character-formatting properties (null = inherit/unset).\r\n  private static final class Rpr {\r\n    Boolean bold, italic;\r\n    String underline, font, color;\r\n    Double sizePt;\r\n    Rpr copy() { Rpr r = new Rpr(); r.bold = bold; r.italic = italic; r.underline = underline; r.font = font; r.color = color; r.sizePt = sizePt; return r; }\r\n    void applyFrom(Rpr o) {\r\n      if (o.bold != null) bold = o.bold;\r\n      if (o.italic != null) italic = o.italic;\r\n      if (o.underline != null) underline = o.underline;\r\n      if (o.font != null) font = o.font;\r\n      if (o.color != null) color = o.color;\r\n      if (o.sizePt != null) sizePt = o.sizePt;\r\n    }\r\n  }\r\n\r\n  // --- rPr reading via serialized XML. The deployed poi-ooxml-LITE schema jar\r\n  // trims the singular CTRPr getters (getB/isSetB/...), so we parse the run's\r\n  // or style's xmlText() instead of the typed CT API. Robust on lite AND full. ---\r\n  private static final java.util.regex.Pattern RPR_BLOCK =\r\n      java.util.regex.Pattern.compile(\"<(?:[A-Za-z0-9]+:)?rPr\\\\b[^>]*>(.*?)</(?:[A-Za-z0-9]+:)?rPr>\", java.util.regex.Pattern.DOTALL);\r\n  private static final java.util.regex.Pattern PPR_BLOCK =\r\n      java.util.regex.Pattern.compile(\"<(?:[A-Za-z0-9]+:)?pPr\\\\b[^>]*>.*?</(?:[A-Za-z0-9]+:)?pPr>\", java.util.regex.Pattern.DOTALL);\r\n\r\n  private static final java.util.regex.Pattern RPRCHANGE_BLOCK =\r\n      java.util.regex.Pattern.compile(\"<(?:[A-Za-z0-9]+:)?rPrChange\\\\b.*?</(?:[A-Za-z0-9]+:)?rPrChange>\", java.util.regex.Pattern.DOTALL);\r\n\r\n  private static String rprInnerOf(String containerXml) {\r\n    if (containerXml == null) return \"\";\r\n    // Strip any tracked-format-change block first, so its nested <w:rPr> (the\r\n    // PREVIOUS formatting) isn't mistaken for the run's current rPr.\r\n    String x = RPRCHANGE_BLOCK.matcher(containerXml).replaceAll(\"\");\r\n    var m = RPR_BLOCK.matcher(x);\r\n    return m.find() ? m.group(1) : \"\";\r\n  }\r\n  private static java.util.regex.Matcher tagOf(String xml, String local) {\r\n    return java.util.regex.Pattern.compile(\"<(?:[A-Za-z0-9]+:)?\" + local + \"(?=[ />])([^>]*)>\").matcher(xml);\r\n  }\r\n  private static String attrVal(String attrs, String attr) {\r\n    if (attrs == null) return null;\r\n    var m = java.util.regex.Pattern.compile(\"(?:[A-Za-z0-9]+:)?\" + attr + \"\\\\s*=\\\\s*\\\"([^\\\"]*)\\\"\").matcher(attrs);\r\n    return m.find() ? m.group(1) : null;\r\n  }\r\n  private static Boolean boolTag(String xml, String local) {\r\n    var m = tagOf(xml, local);\r\n    if (!m.find()) return null;\r\n    String v = attrVal(m.group(1), \"val\");\r\n    if (v == null) return Boolean.TRUE;          // presence with no val = on\r\n    String s = v.toLowerCase();\r\n    return !(s.equals(\"false\") || s.equals(\"0\") || s.equals(\"off\"));\r\n  }\r\n  private static String valTag(String xml, String local) {\r\n    var m = tagOf(xml, local);\r\n    return m.find() ? attrVal(m.group(1), \"val\") : null;\r\n  }\r\n  private static String attrTag(String xml, String local, String attr) {\r\n    var m = tagOf(xml, local);\r\n    return m.find() ? attrVal(m.group(1), attr) : null;\r\n  }\r\n  private static void applyRprXml(Rpr r, String rprInner) {\r\n    if (rprInner == null || rprInner.isEmpty()) return;\r\n    Boolean b = boolTag(rprInner, \"b\"); if (b != null) r.bold = b;\r\n    Boolean i = boolTag(rprInner, \"i\"); if (i != null) r.italic = i;\r\n    String u = valTag(rprInner, \"u\"); if (u != null) r.underline = u;\r\n    String f = attrTag(rprInner, \"rFonts\", \"ascii\"); if (f != null) r.font = f;\r\n    String sz = valTag(rprInner, \"sz\"); if (sz != null) { try { r.sizePt = Double.parseDouble(sz) / 2.0; } catch (Exception e) {} }\r\n    String c = valTag(rprInner, \"color\"); if (c != null) r.color = c;\r\n  }\r\n\r\n  // Resolve a style's effective rPr by walking its basedOn chain (cached;\r\n  // cycle-guarded). docDefaults are NOT resolved (acceptable approximation).\r\n  private static Rpr resolveStyleRpr(String styleId, XWPFStyles xs, Map<String, Rpr> cache) {\r\n    if (styleId == null || xs == null) return new Rpr();\r\n    Rpr cached = cache.get(styleId);\r\n    if (cached != null) return cached;\r\n    Rpr base = new Rpr();\r\n    cache.put(styleId, base);   // temp, breaks cycles\r\n    try {\r\n      XWPFStyle st = xs.getStyle(styleId);\r\n      if (st != null) {\r\n        String sx = st.getCTStyle().xmlText();\r\n        String basedOn = attrTag(sx, \"basedOn\", \"val\");\r\n        Rpr b = resolveStyleRpr(basedOn, xs, cache).copy();\r\n        // Strip the pPr block so its paragraph-mark rPr isn't mistaken for the\r\n        // style's character rPr (the style's own rPr is a sibling of pPr).\r\n        String sxNoP = PPR_BLOCK.matcher(sx).replaceAll(\"\");\r\n        applyRprXml(b, rprInnerOf(sxNoP));\r\n        base = b;\r\n      }\r\n    } catch (Throwable ignore) {}\r\n    cache.put(styleId, base);\r\n    return base;\r\n  }\r\n\r\n  private static Rpr effectiveRpr(XWPFRun run, String paraStyleId, XWPFStyles xs, Map<String, Rpr> cache) {\r\n    Rpr eff = resolveStyleRpr(paraStyleId, xs, cache).copy();\r\n    try {\r\n      String rprInner = rprInnerOf(run.getCTR().xmlText());\r\n      String rStyle = attrTag(rprInner, \"rStyle\", \"val\");\r\n      if (rStyle != null) eff.applyFrom(resolveStyleRpr(rStyle, xs, cache));\r\n      applyRprXml(eff, rprInner);\r\n    } catch (Throwable ignore) {}\r\n    return eff;\r\n  }\r\n\r\n  private static Map<String, Object> effectiveMap(Rpr e) {\r\n    Map<String, Object> m = new LinkedHashMap<>();\r\n    m.put(\"bold\", e.bold != null && e.bold);\r\n    m.put(\"italic\", e.italic != null && e.italic);\r\n    m.put(\"underline\", e.underline == null ? \"none\" : e.underline);\r\n    if (e.font != null) m.put(\"font\", e.font);\r\n    if (e.sizePt != null) m.put(\"sizePt\", e.sizePt);\r\n    if (e.color != null) m.put(\"color\", e.color);\r\n    return m;\r\n  }\r\n\r\n  // Only the properties actually SET on the run's own rPr (the authored delta),\r\n  // read from the run's serialized XML (lite-jar-safe).\r\n  private static Map<String, Object> authoredDelta(XWPFRun run) {\r\n    Map<String, Object> m = new LinkedHashMap<>();\r\n    try {\r\n      String x = rprInnerOf(run.getCTR().xmlText());\r\n      if (x.isEmpty()) return m;\r\n      Boolean b = boolTag(x, \"b\"); if (b != null) m.put(\"bold\", b);\r\n      Boolean i = boolTag(x, \"i\"); if (i != null) m.put(\"italic\", i);\r\n      String u = valTag(x, \"u\"); if (u != null) m.put(\"underline\", u);\r\n      Boolean st = boolTag(x, \"strike\"); if (st != null) m.put(\"strike\", st);\r\n      String f = attrTag(x, \"rFonts\", \"ascii\"); if (f != null) m.put(\"font\", f);\r\n      String sz = valTag(x, \"sz\"); if (sz != null) { try { m.put(\"sizePt\", Double.parseDouble(sz) / 2.0); } catch (Exception e) {} }\r\n      String c = valTag(x, \"color\"); if (c != null) m.put(\"color\", c);\r\n      String hl = valTag(x, \"highlight\"); if (hl != null) m.put(\"highlight\", hl);\r\n      String va = valTag(x, \"vertAlign\"); if (va != null) m.put(\"vertAlign\", va);\r\n      Boolean caps = boolTag(x, \"caps\"); if (caps != null) m.put(\"caps\", caps);\r\n      Boolean vanish = boolTag(x, \"vanish\"); if (vanish != null) m.put(\"hidden\", vanish);\r\n      String cs = attrTag(x, \"rStyle\", \"val\"); if (cs != null) m.put(\"charStyle\", cs);\r\n    } catch (Throwable ignore) {}\r\n    return m;\r\n  }\r\n\r\n  private static String styleName(XWPFStyles xs, String id) {\r\n    if (xs == null || id == null) return null;\r\n    try { var st = xs.getStyle(id); if (st != null && st.getCTStyle().isSetName()) return st.getCTStyle().getName().getVal(); } catch (Throwable t) {}\r\n    return null;\r\n  }\r\n\r\n  // Heading/outline level 1-9 from pPr outlineLvl or the style name/id \"heading N\".\r\n  private static Integer outlineLevelOf(XWPFParagraph p, XWPFStyles xs) {\r\n    try {\r\n      var pPr = p.getCTP().getPPr();\r\n      if (pPr != null && pPr.isSetOutlineLvl()) {\r\n        int lv = new java.math.BigDecimal(String.valueOf(pPr.getOutlineLvl().getVal())).intValue();\r\n        return lv + 1;\r\n      }\r\n    } catch (Throwable ignore) {}\r\n    String id = p.getStyleID();\r\n    String nm = styleName(xs, id);\r\n    String probe = (nm != null ? nm : (id != null ? id : \"\")).toLowerCase().replace(\" \", \"\");\r\n    var mm = java.util.regex.Pattern.compile(\"heading(\\\\d)\").matcher(probe);\r\n    if (mm.find()) { try { return Integer.parseInt(mm.group(1)); } catch (Exception e) {} }\r\n    return null;\r\n  }\r\n\r\n  // Compact presence flags for the Layer-0 outline.\r\n  private static String flagsOf(XWPFParagraph p) {\r\n    StringBuilder b = new StringBuilder();\r\n    try { for (XWPFRun r : p.getRuns()) { if (!r.getEmbeddedPictures().isEmpty()) { b.append('I'); break; } } } catch (Throwable t) {}\r\n    try { if (p.getCTP().sizeOfHyperlinkArray() > 0) b.append('H'); } catch (Throwable t) {}\r\n    try { if (p.getCTP().sizeOfBookmarkStartArray() > 0) b.append('B'); } catch (Throwable t) {}\r\n    return b.toString();\r\n  }\r\n\r\n  private static boolean hasField(XWPFParagraph para) {\r\n    try {\r\n      if (para.getCTP().sizeOfFldSimpleArray() > 0) return true;\r\n    } catch (Throwable ignore) {}\r\n    for (XWPFRun run : para.getRuns()) {\r\n      try {\r\n        if (run.getCTR().sizeOfFldCharArray() > 0) return true;\r\n        if (run.getCTR().sizeOfInstrTextArray() > 0) return true;\r\n      } catch (Throwable ignore) {}\r\n    }\r\n    return false;\r\n  }\r\n\r\n  private static boolean isTocStyle(String styleId) {\r\n    if (styleId == null) return false;\r\n    String s = styleId.toLowerCase();\r\n    return s.startsWith(\"toc\") || s.contains(\"tableoffigures\") || s.contains(\"tableofcontents\") || s.contains(\"tableoftables\");\r\n  }\r\n\r\n  // -------------------------------------------------------------- verify ----\r\n  private static void verify(Path tmp, Map<Integer, String> before, Map<Integer, String> expectedById,\r\n      List<Map<String, Object>> postChecks) throws Exception {\r\n    XWPFDocument re = open(tmp);\r\n    try {\r\n      List<ParaRef> refs = enumerate(re);\r\n      if (refs.size() != before.size()) {\r\n        throw new Fail(EX_VERIFY, \"verify-failed\", \"paragraph count changed after write (\" + before.size() + \" -> \" + refs.size() + \")\", null);\r\n      }\r\n      Map<Integer, ParaRef> byId = new LinkedHashMap<>();\r\n      for (ParaRef r : refs) byId.put(r.id, r);\r\n      // TEXT post-conditions: edited paragraphs match expected; others unchanged.\r\n      for (ParaRef r : refs) {\r\n        String actual = refText(r);\r\n        String expect = expectedById.containsKey(r.id) ? expectedById.get(r.id) : before.get(r.id);\r\n        if (expect == null) continue;\r\n        if (!expect.equals(actual)) {\r\n          throw new Fail(EX_VERIFY, \"verify-failed\",\r\n              \"post-write mismatch in paragraph \" + r.id + (expectedById.containsKey(r.id) ? \" (target)\" : \" (untouched)\"), null);\r\n        }\r\n      }\r\n      // STRUCTURAL post-conditions (formatting / paragraph props actually landed).\r\n      XWPFStyles xs = re.getStyles();\r\n      for (Map<String, Object> pc : postChecks) verifyPost(pc, byId, xs);\r\n    } finally {\r\n      re.close();\r\n    }\r\n  }\r\n\r\n  private static void verifyPost(Map<String, Object> pc, Map<Integer, ParaRef> byId, XWPFStyles xs) throws Fail {\r\n    int id = Json.intval(pc.get(\"id\"), -1);\r\n    ParaRef r = byId.get(id);\r\n    if (r == null || r.para == null) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: paragraph \" + id + \" missing after write\", null);\r\n    String type = String.valueOf(pc.get(\"type\"));\r\n    if (\"setParaStyle\".equals(type)) {\r\n      String want = String.valueOf(pc.get(\"style\"));\r\n      if (!want.equals(r.para.getStyleID())) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: paragraph \" + id + \" style not applied (got \" + r.para.getStyleID() + \")\", null);\r\n    } else if (\"setListMembership\".equals(type)) {\r\n      String want = String.valueOf(pc.get(\"numId\"));\r\n      var nid = r.para.getNumID();\r\n      if (nid == null || !want.equals(nid.toString())) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: paragraph \" + id + \" numId not applied\", null);\r\n    } else if (\"setParaProps\".equals(type)) {\r\n      Map<String, Object> op = Json.obj(pc.get(\"op\"));\r\n      if (op.containsKey(\"alignment\")) {\r\n        String want = String.valueOf(op.get(\"alignment\")).trim().toUpperCase();\r\n        if (!want.equals(String.valueOf(r.para.getAlignment()))) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: paragraph \" + id + \" alignment not applied (got \" + r.para.getAlignment() + \")\", null);\r\n      }\r\n      // indent/spacing: best-effort (POI normalizes units), not strictly checked\r\n    } else if (\"setRunFormat\".equals(type)) {\r\n      String find = String.valueOf(pc.get(\"find\"));\r\n      Map<String, Object> fmt = Json.obj(pc.get(\"format\"));\r\n      String all = text(r.para);\r\n      int s = all.indexOf(find);\r\n      if (s < 0) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: format target text vanished in paragraph \" + id, null);\r\n      XWPFRun run = runAtOffset(r.para, s);\r\n      if (run == null) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: no run at format target in paragraph \" + id, null);\r\n      Map<String, Object> auth = authoredDelta(run);\r\n      for (var en : fmt.entrySet()) {\r\n        String k = en.getKey();\r\n        if (k.equals(\"bold\") || k.equals(\"italic\") || k.equals(\"strike\")) {\r\n          if (truthy(en.getValue())) {\r\n            Object got = auth.get(k);\r\n            if (!(got instanceof Boolean && (Boolean) got)) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: \" + k + \" not applied to paragraph \" + id, null);\r\n          }\r\n        } else if (k.equals(\"color\")) {\r\n          String want = String.valueOf(en.getValue()).replace(\"#\", \"\");\r\n          if (!want.equalsIgnoreCase(String.valueOf(auth.get(\"color\")))) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: color not applied to paragraph \" + id, null);\r\n        } else if (k.equals(\"font\")) {\r\n          if (!String.valueOf(en.getValue()).equals(auth.get(\"font\"))) throw new Fail(EX_VERIFY, \"verify-failed\", \"post: font not applied to paragraph \" + id, null);\r\n        }\r\n        // underline / sizePt: best-effort, not strictly checked\r\n      }\r\n    }\r\n  }\r\n\r\n  private static XWPFRun runAtOffset(XWPFParagraph p, int off) {\r\n    int pos = 0;\r\n    for (XWPFRun run : p.getRuns()) {\r\n      String t = run.getText(0);\r\n      if (t == null) t = \"\";\r\n      int a = pos, b = pos + t.length();\r\n      if (off >= a && off < b) return run;\r\n      pos = b;\r\n    }\r\n    List<XWPFRun> rs = p.getRuns();\r\n    return rs.isEmpty() ? null : rs.get(rs.size() - 1);\r\n  }\r\n\r\n  // ----------------------------------------------------------- utilities ----\r\n  private static Path docxPath(Map<String, Object> spec) throws Fail {\r\n    String p = Json.str(spec.get(\"docx\"));\r\n    if (p == null || p.isEmpty()) throw new Fail(EX_BAD_REQUEST, \"bad-request\", \"docx path is required\", null);\r\n    Path path = Paths.get(p);\r\n    if (!Files.isRegularFile(path)) throw new Fail(EX_FILE, \"file-error\", \"not a file: \" + p, null);\r\n    String lower = p.toLowerCase();\r\n    if (lower.endsWith(\".doc\")) throw new Fail(EX_FILE, \"file-error\", \"legacy .doc (binary) is not supported; convert to .docx first\", null);\r\n    return path;\r\n  }\r\n\r\n  private static XWPFDocument open(Path path) throws Fail {\r\n    try {\r\n      byte[] bytes = Files.readAllBytes(path);\r\n      try (InputStream is = new ByteArrayInputStream(bytes)) {\r\n        return new XWPFDocument(is);\r\n      }\r\n    } catch (Throwable t) {\r\n      throw new Fail(EX_FILE, \"file-error\", \"cannot open as .docx: \" + t.getMessage(), null);\r\n    }\r\n  }\r\n\r\n  private static String text(XWPFParagraph para) {\r\n    StringBuilder b = new StringBuilder();\r\n    for (XWPFRun run : para.getRuns()) {\r\n      String t = run.getText(0);\r\n      if (t != null) b.append(t);\r\n    }\r\n    return b.toString();\r\n  }\r\n\r\n  private static String snippet(String s, int off) {\r\n    int a = Math.max(0, off - 8), b = Math.min(s.length(), off + 8);\r\n    return s.substring(a, b).replace(\"\\n\", \" \");\r\n  }\r\n\r\n  private static String nz(String s) { return s == null ? \"\" : s; }\r\n\r\n  private static String readAll(InputStream in) throws Exception {\r\n    java.io.ByteArrayOutputStream bos = new java.io.ByteArrayOutputStream();\r\n    byte[] buf = new byte[8192];\r\n    int r;\r\n    while ((r = in.read(buf)) != -1) bos.write(buf, 0, r);\r\n    return new String(bos.toByteArray(), StandardCharsets.UTF_8);\r\n  }\r\n\r\n  private static void emitError(Fail f) {\r\n    Map<String, Object> m = new LinkedHashMap<>();\r\n    m.put(\"ok\", Boolean.FALSE);\r\n    m.put(\"error\", f.getMessage());\r\n    m.put(\"code\", f.code);\r\n    m.put(\"opIndex\", f.opIndex);\r\n    String json = Json.write(m);\r\n    if (out != null) out.println(json);\r\n    System.err.println(json);\r\n  }\r\n\r\n  // ------------------------------------------------------------- structs ----\r\n  private static final class ParaRef {\r\n    int id;\r\n    String scope;\r\n    String location;\r\n    String styleId;\r\n    Long listLevel;\r\n    boolean readOnly;\r\n    boolean hasField;\r\n    boolean hasRevisions;   // contains tracked changes (ins/del/move/*Change)\r\n    boolean hasEquation;    // contains OMML (m:oMath) — invisible to plain text()\r\n    boolean isSdt;          // a content-control (structured document tag) node\r\n    String deletedText;     // recovered w:delText (tracked deletions), may be \"\"\r\n    String sdtText;         // text inside a content control (for isSdt nodes)\r\n    XWPFParagraph para;     // null for isSdt nodes\r\n  }\r\n\r\n  private static final class StraddleInfo {\r\n    boolean straddles;\r\n    int runsTouched;\r\n    int changeOffset;\r\n  }\r\n\r\n  static final class Fail extends Exception {\r\n    final int exit;\r\n    final String code;\r\n    final Integer opIndex;\r\n    Fail(int exit, String code, String msg, Integer opIndex) {\r\n      super(msg);\r\n      this.exit = exit;\r\n      this.code = code;\r\n      this.opIndex = opIndex;\r\n    }\r\n  }\r\n}\r\n","com/codeboss/docx/Json.java":"package com.codeboss.docx;\r\n\r\nimport java.util.ArrayList;\r\nimport java.util.LinkedHashMap;\r\nimport java.util.List;\r\nimport java.util.Map;\r\n\r\n/**\r\n * Minimal, dependency-free JSON parser + writer. Keeps the helper's offline\r\n * dependency closure to just poi-ooxml. Handles objects, arrays, strings\r\n * (with \\\\uXXXX + standard escapes), numbers, booleans, and null. Output is\r\n * UTF-8-clean: only \", \\\\, and control chars (&lt;0x20) are escaped; non-ASCII\r\n * is emitted verbatim (the caller writes a UTF-8 stream).\r\n */\r\nfinal class Json {\r\n  private Json() {}\r\n\r\n  // ---- parse ----\r\n  static Object parse(String s) {\r\n    P p = new P(s);\r\n    p.ws();\r\n    Object v = p.value();\r\n    p.ws();\r\n    if (!p.end()) throw new RuntimeException(\"trailing characters at \" + p.i);\r\n    return v;\r\n  }\r\n\r\n  @SuppressWarnings(\"unchecked\")\r\n  static Map<String, Object> obj(Object o) {\r\n    if (o instanceof Map) return (Map<String, Object>) o;\r\n    throw new RuntimeException(\"expected object\");\r\n  }\r\n\r\n  @SuppressWarnings(\"unchecked\")\r\n  static List<Object> arr(Object o) {\r\n    if (o instanceof List) return (List<Object>) o;\r\n    throw new RuntimeException(\"expected array\");\r\n  }\r\n\r\n  static String str(Object o) { return o == null ? null : String.valueOf(o); }\r\n\r\n  static boolean bool(Object o, boolean dflt) {\r\n    return (o instanceof Boolean) ? (Boolean) o : dflt;\r\n  }\r\n\r\n  static int intval(Object o, int dflt) {\r\n    if (o instanceof Number) return ((Number) o).intValue();\r\n    if (o instanceof String) { try { return Integer.parseInt((String) o); } catch (Exception e) { return dflt; } }\r\n    return dflt;\r\n  }\r\n\r\n  private static final class P {\r\n    final String s; int i = 0;\r\n    P(String s) { this.s = s; }\r\n    boolean end() { return i >= s.length(); }\r\n    char cur() { return s.charAt(i); }\r\n    void ws() { while (i < s.length()) { char c = s.charAt(i); if (c == ' ' || c == '\\t' || c == '\\n' || c == '\\r') i++; else break; } }\r\n\r\n    Object value() {\r\n      ws();\r\n      if (end()) throw new RuntimeException(\"unexpected end\");\r\n      char c = cur();\r\n      switch (c) {\r\n        case '{': return object();\r\n        case '[': return array();\r\n        case '\"': return string();\r\n        case 't': case 'f': return bool();\r\n        case 'n': return nul();\r\n        default: return number();\r\n      }\r\n    }\r\n\r\n    Map<String, Object> object() {\r\n      Map<String, Object> m = new LinkedHashMap<>();\r\n      i++; // {\r\n      ws();\r\n      if (!end() && cur() == '}') { i++; return m; }\r\n      while (true) {\r\n        ws();\r\n        if (end() || cur() != '\"') throw new RuntimeException(\"expected key string at \" + i);\r\n        String k = string();\r\n        ws();\r\n        if (end() || cur() != ':') throw new RuntimeException(\"expected ':' at \" + i);\r\n        i++;\r\n        Object v = value();\r\n        m.put(k, v);\r\n        ws();\r\n        if (end()) throw new RuntimeException(\"unterminated object\");\r\n        char c = cur();\r\n        if (c == ',') { i++; continue; }\r\n        if (c == '}') { i++; break; }\r\n        throw new RuntimeException(\"expected ',' or '}' at \" + i);\r\n      }\r\n      return m;\r\n    }\r\n\r\n    List<Object> array() {\r\n      List<Object> a = new ArrayList<>();\r\n      i++; // [\r\n      ws();\r\n      if (!end() && cur() == ']') { i++; return a; }\r\n      while (true) {\r\n        Object v = value();\r\n        a.add(v);\r\n        ws();\r\n        if (end()) throw new RuntimeException(\"unterminated array\");\r\n        char c = cur();\r\n        if (c == ',') { i++; continue; }\r\n        if (c == ']') { i++; break; }\r\n        throw new RuntimeException(\"expected ',' or ']' at \" + i);\r\n      }\r\n      return a;\r\n    }\r\n\r\n    String string() {\r\n      StringBuilder b = new StringBuilder();\r\n      i++; // opening quote\r\n      while (true) {\r\n        if (end()) throw new RuntimeException(\"unterminated string\");\r\n        char c = s.charAt(i++);\r\n        if (c == '\"') break;\r\n        if (c == '\\\\') {\r\n          if (end()) throw new RuntimeException(\"unterminated escape\");\r\n          char e = s.charAt(i++);\r\n          switch (e) {\r\n            case '\"': b.append('\"'); break;\r\n            case '\\\\': b.append('\\\\'); break;\r\n            case '/': b.append('/'); break;\r\n            case 'b': b.append('\\b'); break;\r\n            case 'f': b.append('\\f'); break;\r\n            case 'n': b.append('\\n'); break;\r\n            case 'r': b.append('\\r'); break;\r\n            case 't': b.append('\\t'); break;\r\n            case 'u':\r\n              if (i + 4 > s.length()) throw new RuntimeException(\"bad \\\\u escape\");\r\n              b.append((char) Integer.parseInt(s.substring(i, i + 4), 16));\r\n              i += 4;\r\n              break;\r\n            default: throw new RuntimeException(\"bad escape \\\\\" + e);\r\n          }\r\n        } else {\r\n          b.append(c);\r\n        }\r\n      }\r\n      return b.toString();\r\n    }\r\n\r\n    Object bool() {\r\n      if (s.startsWith(\"true\", i)) { i += 4; return Boolean.TRUE; }\r\n      if (s.startsWith(\"false\", i)) { i += 5; return Boolean.FALSE; }\r\n      throw new RuntimeException(\"bad literal at \" + i);\r\n    }\r\n\r\n    Object nul() {\r\n      if (s.startsWith(\"null\", i)) { i += 4; return null; }\r\n      throw new RuntimeException(\"bad literal at \" + i);\r\n    }\r\n\r\n    Object number() {\r\n      int start = i;\r\n      while (i < s.length()) {\r\n        char c = s.charAt(i);\r\n        if ((c >= '0' && c <= '9') || c == '-' || c == '+' || c == '.' || c == 'e' || c == 'E') i++;\r\n        else break;\r\n      }\r\n      String num = s.substring(start, i);\r\n      if (num.isEmpty()) throw new RuntimeException(\"bad value at \" + start);\r\n      if (num.indexOf('.') < 0 && num.indexOf('e') < 0 && num.indexOf('E') < 0) {\r\n        try { return Long.parseLong(num); } catch (Exception ignore) {}\r\n      }\r\n      return Double.parseDouble(num);\r\n    }\r\n  }\r\n\r\n  // ---- write ----\r\n  static String write(Object o) {\r\n    StringBuilder b = new StringBuilder();\r\n    writeValue(b, o);\r\n    return b.toString();\r\n  }\r\n\r\n  @SuppressWarnings(\"unchecked\")\r\n  private static void writeValue(StringBuilder b, Object o) {\r\n    if (o == null) { b.append(\"null\"); return; }\r\n    if (o instanceof String) { writeString(b, (String) o); return; }\r\n    if (o instanceof Boolean || o instanceof Number) { b.append(o.toString()); return; }\r\n    if (o instanceof Map) {\r\n      b.append('{');\r\n      boolean first = true;\r\n      for (Map.Entry<String, Object> e : ((Map<String, Object>) o).entrySet()) {\r\n        if (!first) b.append(',');\r\n        first = false;\r\n        writeString(b, e.getKey());\r\n        b.append(':');\r\n        writeValue(b, e.getValue());\r\n      }\r\n      b.append('}');\r\n      return;\r\n    }\r\n    if (o instanceof Iterable) {\r\n      b.append('[');\r\n      boolean first = true;\r\n      for (Object x : (Iterable<Object>) o) {\r\n        if (!first) b.append(',');\r\n        first = false;\r\n        writeValue(b, x);\r\n      }\r\n      b.append(']');\r\n      return;\r\n    }\r\n    // Fallback: treat as string.\r\n    writeString(b, String.valueOf(o));\r\n  }\r\n\r\n  private static void writeString(StringBuilder b, String s) {\r\n    b.append('\"');\r\n    for (int i = 0; i < s.length(); i++) {\r\n      char c = s.charAt(i);\r\n      switch (c) {\r\n        case '\"': b.append(\"\\\\\\\"\"); break;\r\n        case '\\\\': b.append(\"\\\\\\\\\"); break;\r\n        case '\\n': b.append(\"\\\\n\"); break;\r\n        case '\\r': b.append(\"\\\\r\"); break;\r\n        case '\\t': b.append(\"\\\\t\"); break;\r\n        case '\\b': b.append(\"\\\\b\"); break;\r\n        case '\\f': b.append(\"\\\\f\"); break;\r\n        default:\r\n          if (c < 0x20) {\r\n            b.append(String.format(\"\\\\u%04x\", (int) c));\r\n          } else {\r\n            b.append(c); // non-ASCII emitted verbatim; stream is UTF-8\r\n          }\r\n      }\r\n    }\r\n    b.append('\"');\r\n  }\r\n}\r\n"};

// ====== embedded project source map (for the 'extract' subcommand) ======
const EMBEDDED_SRC_GZ_B64 = '';



// ====== embedded UI HTML ======
const UI_HTML = `<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>code_boss</title>
<!-- The brand spark (coral six-lobe asterisk) — favicon AND the Chrome app-mode
     window/taskbar icon. Traced from deploy/brand-star-source.png by
     scripts/capture-star-icon.mjs; same pixels as deploy/orange-star.ico. -->
<link rel="icon" type="image/png" href="data:image/png;base64,">
<style>
  :root {
    --bg: #0b0805;          /* warm terminal near-black (a touch deeper than the original) */
    --panel: #161410;       /* slightly raised panels */
    --panel-alt: #1b1914;   /* input / boxes */
    --border: #3a372f;      /* dim warm border */
    --fg: #f6f3ec;          /* warm off-white text (a little brighter than the original) */
    --muted: #b3ae9e;       /* secondary text (a little lighter than the original) */
    --accent: #d97757;      /* Claude coral (bright for dark bg) */
    --accent-hover: #e08968;
    --accent-fg: #100e0b;
    --tool: #d97757;        /* tool-call bullet */
    --error: #e5635a;
    --error-bg: #211613;
    --warn-bg: #211b11;
    --mono: ui-monospace, "SF Mono", "Cascadia Code", "JetBrains Mono", Menlo, Consolas, "DejaVu Sans Mono", monospace;
  }
  * { box-sizing: border-box; }
  html, body { height: 100%; margin: 0; }

  /* Global button reset + .btn styles. Without these, anything using
     \`class="btn"\` falls through to the OS-native button rendering — wrong
     font, light gradient background, faint inset shadow from the top-left.
     The contextual \`.settings-row .btn\` / \`.settings-actions .btn\` rules
     below override these where finer tuning is needed; specificity wins. */
  /* Strip OS button chrome from EVERY button — the faint inset shadow
     from \`appearance: button\` was leaking through even on buttons that
     set border/background, since those properties don't override the
     UA's native rendering by themselves. Specific classes (.btn,
     .modal-close, .settings-btn, .btn-sm) layer their look on top of
     this clean slate. */
  button {
    font: inherit;
    -webkit-appearance: none;
    appearance: none;
  }
  .btn {
    -webkit-appearance: none;
    appearance: none;
    font-family: var(--mono);
    font-size: 12.5px;
    font-weight: 500;
    line-height: 1.4;
    padding: 8px 18px;
    border: 1px solid var(--border);
    background: var(--panel);
    color: var(--fg);
    border-radius: 5px;
    cursor: pointer;
    letter-spacing: 0.2px;
    transition: border-color 0.12s, background 0.12s, color 0.12s;
  }
  .btn:hover:not(:disabled) {
    border-color: var(--accent);
    color: var(--accent);
  }
  .btn:active:not(:disabled) { transform: translateY(1px); }
  .btn:disabled { opacity: 0.4; cursor: not-allowed; }
  .btn.primary {
    background: var(--accent);
    color: var(--accent-fg);
    border-color: var(--accent);
    font-weight: 600;
  }
  .btn.primary:hover:not(:disabled) {
    background: var(--accent-hover);
    border-color: var(--accent-hover);
    color: var(--accent-fg);
  }
  .btn.primary:disabled {
    background: transparent;
    color: var(--muted);
    border-color: var(--border);
  }

  /* Floating build-version badge — top-right, translucent, never blocks
     interaction. Always visible so a screenshot of the page also captures
     which bundle is running. */
  #versionBadge {
    position: fixed;
    top: 6px;
    right: 8px;
    z-index: 500;
    pointer-events: none;
    font-family: var(--mono);
    font-size: 10px;
    color: var(--muted);
    opacity: 0.45;
    padding: 1px 6px;
    border: 1px solid var(--border);
    border-radius: 3px;
    background: rgba(0, 0, 0, 0.25);
    user-select: none;
  }
  #versionBadge:empty { display: none; }

  /* Thin, theme-matched scrollbars everywhere. Firefox / modern Chrome use
     \`scrollbar-*\` properties; WebKit uses the older pseudo-elements. The
     \`*\` selector inherits to every scrollable element on the page. */
  * {
    scrollbar-width: thin;
    scrollbar-color: #3a3830 transparent;
  }
  *::-webkit-scrollbar { width: 8px; height: 8px; }
  *::-webkit-scrollbar-track { background: transparent; }
  *::-webkit-scrollbar-thumb {
    background: #3a3830;
    border-radius: 4px;
  }
  *::-webkit-scrollbar-thumb:hover { background: #4d4a3f; }
  *::-webkit-scrollbar-corner { background: transparent; }
  body {
    font: 13.5px/1.55 var(--mono);
    color: var(--fg);
    background: var(--bg);
    display: flex;
    flex-direction: column;
  }
  ::selection { background: rgba(217,119,87,0.3); }
  #landing {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    justify-content: flex-start;
    padding: 48px 24px 32px;
    max-width: 640px;
    margin: 0 auto;
    width: 100%;
    overflow-y: auto;
    min-height: 0;
  }
  #landing.hidden, #chat.hidden, #initView.hidden { display: none; }
  #initView {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 32px 24px;
    max-width: 640px;
    margin: 0 auto;
    width: 100%;
    gap: 8px;
  }
  #initView h1 { font-size: 26px; font-weight: 700; margin: 0 0 6px; letter-spacing: -0.5px; color: var(--accent); }
  #initView h1::before { content: "✻ "; }
  #initView .tagline { color: var(--muted); margin: 0; font-size: 14px; }
  #initRetryBtn.hidden { display: none; }
  /* Init status line — reuses the chat's braille dot-circle spinner + accent
     coloring so initialization looks like the rest of the app. While loading,
     the spinner glyph + label are accent-colored; on error/retry the spinner
     hides and the label drops to muted. */
  #initView .init-status { margin: 8px 0 2px; font-size: 14px; color: var(--muted); font-variant-numeric: tabular-nums; }
  #initView .init-status.loading { color: var(--accent); }
  #initView .init-status .spin { display: none; width: 1ch; text-align: center; }
  #initView .init-status.loading .spin { display: inline-block; }
  #landing h1 { font-size: 26px; font-weight: 700; margin: 0 0 6px; letter-spacing: -0.5px; color: var(--accent); }
  #landing h1::before { content: "✻ "; }
  #landing .tagline { color: var(--muted); margin: 0 0 32px; font-size: 15px; }
  #landing h2 {
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.6px;
    color: var(--muted);
    margin: 40px 0 10px;
    align-self: stretch;
  }
  .recent-list { list-style: none; padding: 0; margin: 0; width: 100%; }
  .recent-item {
    background: var(--panel);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 10px 14px;
    margin-bottom: 8px;
    cursor: pointer;
    transition: border-color 0.1s;
  }
  .recent-item { background: var(--panel-alt); }
  .recent-item:hover { border-color: var(--accent); }
  .recent-item .name { font-weight: 600; }
  .logbox {
    background: #161512;
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 12px;
    font: 12px/1.5 var(--mono);
    white-space: pre-wrap;
    word-break: break-word;
    max-height: 60vh;
    overflow-y: auto;
    color: #b9b5a8;
  }
  .recent-item .path { color: var(--muted); font-size: 12px; font-family: ui-monospace, Menlo, Consolas, monospace; margin-top: 2px; word-break: break-all; }
  .recent-empty { color: var(--muted); font-size: 13px; text-align: center; padding: 12px; }

  /* Folder-picker modal — replaces the native PowerShell folder dialog. */
  #selectFolderBtn {
    font-size: 13.5px;
    padding: 11px 24px;
    border-radius: 8px;
    align-self: flex-start;
  }
  .browser-toolbar {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 8px;
  }
  /* Breadcrumb / path-input HYBRID. The bar is styled like the app's text
     inputs (panel-alt bg + border + 4px radius) in BOTH modes — same box,
     only the interaction differs. Hovering anywhere that is NOT a navigable
     segment highlights the bar (JS toggles .bc-hot) to signal "click to type
     a path"; clicking there swaps the crumbs for a real <input> (.editing)
     pre-filled with the full path. A rejected path (nonexistent, or a file)
     flips on .path-error: red border + the reason in \`title\`. */
  .breadcrumb {
    flex: 1 1 auto;
    min-width: 0;
    font-family: var(--mono);
    font-size: 12px;
    color: var(--muted);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    background: var(--panel-alt);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 5px 8px;
    height: 28px;             /* identical box in breadcrumb AND edit mode */
    line-height: 16px;
    cursor: text;             /* empty space reads as "click to type" */
  }
  .breadcrumb.bc-hot { background: var(--panel); border-color: var(--muted); }
  .breadcrumb.editing { border-color: var(--accent); }
  .breadcrumb.path-error { border-color: var(--error); }
  .breadcrumb .path-input {
    width: 100%;
    height: 16px;
    padding: 0;
    margin: 0;
    border: none;
    outline: none;
    background: transparent;
    color: var(--fg);
    font: inherit;
    line-height: 16px;
    vertical-align: top;
  }
  .breadcrumb .crumb { cursor: pointer; }
  .breadcrumb .crumb:hover { color: var(--accent); text-decoration: underline; }
  /* The LAST crumb is the CURRENT folder — not a navigation hop. It reads as
     the input's current value: text cursor, no link affordance, and clicking
     it opens the path editor just like the empty space does. */
  .breadcrumb .crumb-last { color: var(--fg); cursor: text; }
  .breadcrumb .crumb-last:hover { color: var(--fg); text-decoration: none; }
  .breadcrumb .sep { color: var(--border); margin: 0 4px; }
  .dir-list {
    list-style: none;
    padding: 0;
    margin: 0 0 8px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: var(--panel);
    height: 380px;
    overflow-y: auto;
  }
  .dir-item {
    padding: 8px 12px;
    cursor: pointer;
    font-size: 13px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .dir-item:last-child { border-bottom: none; }
  .dir-item:hover { background: var(--panel-alt); }
  .dir-item:hover .name { color: var(--accent); }
  .dir-item .icon {
    flex-shrink: 0;
    font-size: 15px;
    line-height: 1;
    filter: saturate(0.85);
  }
  .dir-item .name {
    flex: 1 1 auto;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    font-family: var(--mono);
  }
  .dir-empty { color: var(--muted); font-size: 12px; text-align: center; padding: 16px; font-style: italic; }
  .browser-status {
    color: var(--muted);
    font-family: var(--mono);
    font-size: 11px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    padding: 2px 4px;
  }
  .modal-footer {
    display: flex;
    justify-content: flex-end;
    gap: 10px;
    padding: 14px 18px;
    border-top: 1px solid var(--border);
    background: var(--bg);          /* match page bg — darker than panel-alt */
  }
  .project-chip {
    background: transparent;
    border: 1px solid var(--border);
    color: var(--muted);
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 12px;
    font-family: var(--mono);
    max-width: 340px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    cursor: help;
  }
  /* IDE layout: collapsible side panel (left) + chat column (right). */
  #chat { flex: 1; display: flex; flex-direction: row; min-height: 0; overflow: hidden; }
  /* min-width:0 lets the chat column shrink to the remaining width instead of
     forcing a page-wide horizontal scrollbar (flex items default min-width:auto). */
  /* Basis ~= chat max (860) + the logScroll padding (36+20); grow:1 so it fills
     the full width when the sidebar is collapsed, but its low grow vs the sidebar's
     means the sidebar wins the slack while present. Shrinks on narrow screens. */
  #chatMain { flex: 1 1 880px; min-width: 0; display: flex; flex-direction: column; min-height: 0; position: relative; }
  /* Wide-screen behavior: the CHAT keeps a max width (860, set on .log/.input-row)
     and the SIDEBAR absorbs the extra width instead of leaving black gutters
     around the chat. Priorities: the sidebar has a high flex-grow (50) so it eats
     the slack first (up to a 600 max so it never sprawls absurdly), but shrink:0 +
     min-width:300 so on a NARROW screen it holds 300 and the chat (grow:1, shrink:1)
     gives up width instead. Sized via flex-basis/max-width (not width) so the
     collapse animation still works — width:0 would lose to flex-basis. */
  #sidePanel { flex: 50 0 300px; min-width: 300px; max-width: 600px; display: flex; flex-direction: column; min-height: 0; border-right: 1px solid var(--border); background: var(--panel-alt); transition: flex-basis 0.12s ease, min-width 0.12s ease, max-width 0.12s ease; }
  #sidePanel.collapsed { flex-grow: 0; flex-basis: 0; min-width: 0; max-width: 0; border-right: none; overflow: hidden; }
  /* The backlog panel fills the side panel instead of being a top strip. */
  #sidePanel .backlog-panel { max-height: none; border-bottom: none; flex: 1 1 auto; min-height: 0; overflow: auto; background: transparent; }
  /* Collapse control lives INSIDE the tab bar; a reopen pip appears at the chat's
     left edge only while the panel is collapsed. */
  .side-collapse { flex: 0 0 auto; width: 24px; background: none; border: none; border-bottom: 2px solid transparent; color: var(--muted); cursor: pointer; font-size: 14px; padding: 0; }
  .side-collapse:hover { color: var(--accent); }
  #sideReopen { position: absolute; top: 8px; left: 0; z-index: 30; width: 18px; height: 26px; border: 1px solid var(--border); border-left: none; border-radius: 0 5px 5px 0; background: var(--panel); color: var(--muted); cursor: pointer; font-size: 12px; line-height: 24px; padding: 0; display: none; }
  #sideReopen:hover { color: var(--accent); border-color: var(--accent); }
  #sidePanel.collapsed + #chatMain #sideReopen { display: block; }
  /* Side-panel tabs + panes */
  .side-tabs { display: flex; flex-shrink: 0; border-bottom: 1px solid var(--border); overflow: hidden; }
  /* Tabs SHARE the row width (flex-basis 0 + min-width 0 so they can shrink) and
     ellipsize their labels at narrow widths — otherwise a populated tab row grows
     past the 300px panel edge, painting over the chat and pushing the « collapse
     toggle (flex 0 0 auto) out of reach. Full labels live in each button's title. */
  .side-tab { flex: 1 1 0; min-width: 0; overflow: hidden; text-overflow: ellipsis; background: none; border: none; border-bottom: 2px solid transparent; color: var(--muted); cursor: pointer; padding: 8px 4px; font: inherit; font-size: 12px; white-space: nowrap; }
  .side-tab:hover { color: var(--fg); }
  .side-tab.active { color: var(--fg); border-bottom-color: var(--accent); background: var(--panel); }
  .side-tab-count { color: var(--muted); font-size: 11px; }
  .side-panes { flex: 1 1 auto; min-height: 0; overflow: auto; font-size: 12px; }
  .side-pane { padding: 6px; }
  .side-pane.hidden { display: none; }
  .pane-actions { display: flex; gap: 6px; margin-bottom: 6px; }
  .pane-actions button { flex: 1; background: none; border: 1px solid var(--border); color: var(--fg); border-radius: 4px; cursor: pointer; padding: 4px 6px; font: inherit; font-size: 12px; }
  .pane-actions button:hover { border-color: var(--accent); color: var(--accent); }
  .pane-actions button.primary { background: var(--accent); color: var(--accent-fg); border-color: var(--accent); }
  .pane-empty { color: var(--muted); font-size: 12px; padding: 10px 4px; line-height: 1.6; }
  /* The queue stack fills the pane (not the centered 860px chat width). */
  #pane-queue .queue-stack { max-width: none; margin: 0; }
  /* Single compact "+" to start a new assignment. */
  .pane-actions .pane-add { flex: 0 0 auto; width: 28px; text-align: center; font-size: 15px; line-height: 1; }
  .pane-hint { color: var(--muted); font-size: 11px; line-height: 1.5; margin-bottom: 8px; }
  /* Memory: per-category headers; under each, a box styled like ONE textarea
     (edge-to-edge) whose lines are forced bullets. Each bullet is its own
     invisible auto-growing input; nav between bullets feels like a textarea. */
  #pane-memory { position: relative; padding: 6px 0 10px; }
  .mem-cat { margin-bottom: 10px; }
  .mem-cat-label { color: var(--muted); font-size: 11px; margin: 0 0 3px 10px; }
  .bullet-box { background: var(--panel); border: 1px solid var(--border); border-radius: 4px; padding: 4px 0; }
  .bullet-box:focus-within { border-color: var(--accent); }
  .bullet { display: flex; align-items: flex-start; gap: 6px; padding: 1px 8px 1px 10px; }
  .bullet .dash { color: var(--muted); flex-shrink: 0; line-height: 1.55; user-select: none; }
  .bullet .b-input { flex: 1; min-width: 0; background: transparent; border: none; outline: none; color: var(--fg); font: inherit; font-size: 12px; line-height: 1.55; resize: none; overflow: hidden; padding: 0; }
  .mem-saved { position: absolute; top: 2px; right: 10px; z-index: 5; color: #3fb950; font-size: 11px; opacity: 0; pointer-events: none; transition: opacity 0.3s ease; }
  .mem-saved.show { opacity: 1; }
  /* Auto-hiding side-panel scrollbar: invisible until hover/scroll so it never
     sits over the edge-to-edge memory boxes. */
  .side-panes { scrollbar-width: thin; scrollbar-color: transparent transparent; }
  .side-panes:hover, .side-panes:focus-within { scrollbar-color: #3a3830 transparent; }
  .side-panes::-webkit-scrollbar { width: 8px; }
  .side-panes::-webkit-scrollbar-thumb { background: transparent; border-radius: 4px; }
  .side-panes:hover::-webkit-scrollbar-thumb, .side-panes:focus-within::-webkit-scrollbar-thumb { background: #3a3830; }
  .side-panes::-webkit-scrollbar-thumb:hover { background: #4d4a3f; }
  /* Gutter so chat content doesn't touch the panel. Left is wider because the
     .task-wrap bracket overhangs -16px into the gutter — it must clear the panel. */
  /* Top padding gives the first message breathing room; bottom padding clears the
     36px footer gradient so the last message isn't covered by the fade. */
  #logScroll { padding: 16px 20px 48px 36px; }
  /* Bottom-left chrome. Flat flex-wrap: in the PANEL it wraps to two lines
     (path+version / weblogic·edit·gear); DOCKED under the chat input when the
     panel is collapsed it's a single line (path left, controls right). */
  .side-chrome { flex-shrink: 0; border-top: 1px solid var(--border); padding: 6px 8px; font-size: 11px; display: flex; flex-wrap: wrap; align-items: center; gap: 4px 10px; }
  .side-chrome .proj-path { flex: 1 1 auto; min-width: 0; color: var(--muted); cursor: pointer; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-family: var(--mono); }
  .side-chrome .proj-path:hover { color: var(--accent); }
  .chrome-version { flex-shrink: 0; color: var(--muted); }
  .chrome-break { flex-basis: 100%; height: 0; margin: 0; }   /* forces line 2 in the panel */
  .wl-chip { flex-shrink: 0; cursor: help; color: var(--muted); }
  .wl-chip.up { color: #3fb950; }
  .wl-chip.down { color: #d9534f; }
  .edit-toggle { flex-shrink: 0; cursor: pointer; color: #3fb950; }
  .edit-toggle:hover { opacity: 0.8; }
  .edit-toggle.off { color: var(--muted); text-decoration: line-through; }
  .side-chrome .dirty-indicator { flex-shrink: 0; color: #c9a227; cursor: help; }
  .side-chrome .settings-btn { flex-shrink: 0; margin-left: auto; background: none; border: none; color: var(--muted); cursor: pointer; font-size: 14px; padding: 0; line-height: 1; }
  .side-chrome .settings-btn:hover { color: var(--accent); }
  /* Docked under the input (panel collapsed): single line, no version, and
     constrained to the chat input's width (max-width:860px, centered). */
  .side-chrome.docked { border-top: none; flex-wrap: nowrap; padding: 4px 2px 0; max-width: 860px; margin: 0 auto; width: 100%; box-sizing: border-box; }
  .side-chrome.docked .chrome-break { display: none; }
  .side-chrome.docked .chrome-version { display: none; }
  /* New-assignment intake modal: fields (left) + mini-chat (right). */
  /* The new-assignment modal takes as much room as the screen allows, with a
     comfortable margin around it so it's clearly a modal. Flex column so the
     two-pane (and within it the chat + requirement field) fills the height. */
  #intakeModalBg { align-items: center; padding: 4vh 4vw; }
  .modal.intake-modal { width: 100%; max-width: 1500px; height: 92vh; display: flex; flex-direction: column; }
  .intake-two-pane { display: flex; min-height: 0; flex: 1; }
  #im-req { flex: 1 1 auto; min-height: 100px; }
  .intake-fields { flex: 0 0 42%; padding: 12px 16px; border-right: 1px solid var(--border); overflow-y: auto; display: flex; flex-direction: column; gap: 3px; }
  .intake-fields label { font-size: 12px; color: var(--muted); margin-top: 8px; }
  .intake-fields .im-sub { color: var(--muted); opacity: 0.8; }
  .intake-fields input, .intake-fields textarea { background: var(--panel-alt); color: var(--fg); border: 1px solid var(--border); border-radius: 4px; padding: 6px 8px; font: inherit; font-size: 13px; resize: vertical; }
  .intake-fields input:focus, .intake-fields textarea:focus { border-color: var(--accent); outline: none; }
  .intake-chat { flex: 1; display: flex; flex-direction: column; min-width: 0; }
  /* The intake mini-chat reuses the MAIN chat renderer (renderInto → renderEvent),
     so #im-log holds the same .row/.bubble/.row-result rows. Block layout (the
     rows carry their own margins); full width in the narrow pane. */
  .im-log { flex: 1; overflow-y: auto; padding: 8px 14px; display: block; }
  #im-log .row, #im-log .bubble, #im-log .row-result, #im-log .diff-block, #im-log .log { max-width: none; }
  /* Intake input reuses the MAIN chat input (.input-row + the generic textarea
     rule) so it matches exactly: a borderless auto-growing textarea inside a
     rounded bordered row with the ">" prompt + focus-within accent. .im-footer
     gives it the same gradient fade above the input that footer::before gives the
     main chat. The working indicator reuses the main .status row (animated). */
  .im-footer { position: relative; padding: 8px 14px 10px; flex-shrink: 0; }
  .im-footer::before { content: ''; position: absolute; left: 0; right: 0; top: -28px; height: 28px; background: linear-gradient(to bottom, transparent, var(--bg)); pointer-events: none; }
  .im-input-row { max-width: none; margin: 0; }
  #im-log .status { margin: 6px 2px; }
  .im-hint { font-size: 11px; color: var(--muted); max-width: 60%; }

  /* Header removed in build 7 — controls moved to the settings modal,
     project path is shown below the input bar. */

  /* Tokens / timeout chips kept as utility styles in case other code
     references them (e.g., diagnostic / status displays). */
  .chip {
    background: transparent;
    color: var(--muted);
    padding: 4px 8px;
    border: 1px solid var(--border);
    border-radius: 4px;
    font-size: 12px;
    font-variant-numeric: tabular-nums;
  }

  main {
    flex: 1;
    overflow-y: auto;
    padding: 16px 16px 24px;
  }
  .log { max-width: 860px; margin: 0 auto; }

  /* User message: visually mirrors the input bar — same panel background,
     accent-colored border + ">" prompt — so the user can scan their own
     messages at a glance. */
  .bubble.user {
    background: var(--panel-alt);
    border: 1px solid var(--accent);
    border-radius: 6px;
    padding: 6px 10px;
    margin: 16px 0;
    white-space: pre-wrap;
    word-wrap: break-word;
    color: var(--fg);
    display: flex;
    gap: 8px;
    align-items: flex-start;
    position: relative;          /* anchor the hover copy button */
  }
  /* Hover copy button: upper-RIGHT corner, and STICKY so on a long message it
     stays in view near the top of the bubble as you scroll past it. It's a flex
     item (margin-left:auto pushes it right); position:sticky pins it vertically. */
  .bubble.user .bubble-copy {
    position: sticky;
    top: 10px;
    align-self: flex-start;
    margin-left: auto;
    flex: 0 0 auto;
    width: 22px;
    height: 22px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    font-size: 11px;
    line-height: 1;
    background: var(--panel-alt);
    color: var(--muted);
    border: 1px solid var(--border);
    border-radius: 4px;
    cursor: pointer;
    opacity: 0;
    transition: opacity 0.12s ease, color 0.12s ease, border-color 0.12s ease;
    z-index: 2;
  }
  .bubble.user:hover .bubble-copy { opacity: 1; }
  .bubble.user .bubble-copy:hover { color: var(--accent); border-color: var(--accent); }
  .bubble.user .bubble-copy:focus-visible { opacity: 1; outline: 1px solid var(--accent); }
  .bubble.user .bubble-copy.copied { opacity: 1; color: var(--accent); border-color: var(--accent); }
  .bubble.user::before {
    content: ">";
    color: var(--accent);
    font-weight: 700;
    flex-shrink: 0;
    line-height: 1.55;
  }
  .bubble.user .bubble-content {
    flex: 1 1 auto;
    min-width: 0;
    word-wrap: break-word;
  }
  /* Trim default top/bottom margins on the FIRST/LAST markdown block so
     the bubble stays compact when the user starts with a header or ends
     with a list. */
  .bubble.user .bubble-content > *:first-child { margin-top: 0; }
  .bubble.user .bubble-content > *:last-child { margin-bottom: 0; }
  .bubble.system { color: var(--muted); font-size: 12.5px; margin: 12px 0; white-space: pre-wrap; }

  /* Dotted message rows (Claude Code style). */
  .row { display: flex; gap: 8px; margin: 10px 0; align-items: flex-start; }
  /* Tool rows hug each other tighter than chat rows: no bottom margin
     (result sits flush below), and a small top margin so consecutive
     tool calls cluster as one visual unit. */
  .row.tool { margin: 6px 0 0 0; }
  .row .dot { flex-shrink: 0; line-height: 1.55; }
  .row.assistant .dot { color: var(--fg); }
  .row.tool .dot { color: #6cc46c; }
  .row.tool.tool-err .dot { color: var(--error); }   /* failed/cancelled tool */
  /* Blink the green dot while a tool call is in flight (no matching
     tool-result yet). Added when the tool-call row renders, removed
     when its tool-result event arrives. */
  @keyframes tool-blink { 0%, 100% { opacity: 1; } 50% { opacity: 0.25; } }
  .row.tool.tool-running .dot { animation: tool-blink 0.7s ease-in-out infinite; }
  .row .body { flex: 1; white-space: pre-wrap; word-wrap: break-word; min-width: 0; }
  .row.tool .body { color: var(--fg); }
  /* Expandable tool-call: click anywhere on the row to toggle the full
     args (including filtered fields like find/replace/content). Mirrors
     the result-row expansion pattern. */
  .row.tool.expandable { cursor: pointer; }
  .row.tool.expandable:hover > .body > :not(pre),
  .row.tool.expandable:hover > .body { color: var(--accent); }
  .row.tool .tool-call-full {
    margin: 6px 0 0;
    padding: 8px 12px;
    background: #15140f;
    border-left: 2px solid #2f2d27;
    border-radius: 3px;
    white-space: pre-wrap;
    word-break: break-word;
    max-height: 480px;
    overflow-y: auto;
    font-size: 12px;
    color: var(--fg);
    cursor: text;
  }
  /* Inline markdown rendered by renderInline(). Applies to both
     assistant rows (.body) and user bubbles (.bubble.user .bubble-content)
     since both run their content through the same renderer. The block-level
     rules stay scoped to .md so they don't leak onto accidental h/ul/li
     produced by other paths. */
  .body code,
  .bubble.user .bubble-content code { color: var(--accent); }
  /* Fenced \`\`\` blocks: a scrollable literal block, distinct from inline code. */
  .body pre.md,
  .bubble.user .bubble-content pre.md {
    margin: 6px 0; padding: 8px 10px;
    background: var(--panel-alt); border: 1px solid var(--border);
    border-radius: 6px; overflow-x: auto; white-space: pre;
  }
  .body pre.md code,
  .bubble.user .bubble-content pre.md code { color: var(--fg); }
  .body strong,
  .bubble.user .bubble-content strong { color: var(--fg); font-weight: 700; }
  .body h3.md, .body h4.md, .body h5.md,
  .bubble.user .bubble-content h3.md,
  .bubble.user .bubble-content h4.md,
  .bubble.user .bubble-content h5.md {
    margin: 8px 0 2px;
    font-weight: 700;
    line-height: 1.3;
    color: var(--fg);
  }
  .body h3.md,
  .bubble.user .bubble-content h3.md { font-size: 15px; color: var(--accent); }
  .body h4.md,
  .bubble.user .bubble-content h4.md { font-size: 13.5px; }
  .body h5.md,
  .bubble.user .bubble-content h5.md { font-size: 12.5px; color: var(--muted); letter-spacing: 0.3px; text-transform: uppercase; }
  .body ul.md,
  .bubble.user .bubble-content ul.md {
    margin: 4px 0 6px;
    padding-left: 20px;
  }
  .body ul.md li,
  .bubble.user .bubble-content ul.md li { margin: 0 0 2px; }
  .body ul.md li::marker,
  .bubble.user .bubble-content ul.md li::marker { color: var(--accent); }
  /* GitHub-flavored markdown tables. white-space:normal undoes the body's
     pre-wrap inside the table so cell text wraps naturally; the table itself
     is a block so it sits on its own line. */
  .body table.md,
  .bubble.user .bubble-content table.md {
    border-collapse: collapse;
    margin: 6px 0;
    font-size: 12.5px;
    white-space: normal;
    max-width: 100%;
  }
  .body table.md th, .body table.md td,
  .bubble.user .bubble-content table.md th,
  .bubble.user .bubble-content table.md td {
    border: 1px solid var(--border);
    padding: 3px 8px;
    text-align: left;
    vertical-align: top;
  }
  .body table.md th,
  .bubble.user .bubble-content table.md th {
    background: var(--panel-alt);
    color: var(--fg);
    font-weight: 600;
  }
  .body table.md tr:nth-child(even) td,
  .bubble.user .bubble-content table.md tr:nth-child(even) td { background: rgba(255,255,255,0.02); }
  /* Streaming prose: createStreamingMd wraps paragraph lines in .md-p divs while
     a message streams (the finalize event then swaps in renderInline()). Inherits
     the body's pre-wrap; the inter-paragraph gap approximates the blank line. */
  .body .md-p { white-space: pre-wrap; }
  .body .md-p:not(:first-child) { margin-top: 0.5em; }

  /* ── Mermaid diagrams (mermaid-lite) ──────────────────────────────────
     In-chat thumbnail: a bounded card (like .task-wrap chrome) that clips
     tall diagrams; click opens the fullscreen viewer. The same .mm-thumb
     container is reused while the fence streams (mm-streaming). */
  .mm-thumb {
    max-height: 240px;
    overflow: hidden;
    cursor: pointer;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--panel);
    padding: 10px;
    margin: 8px 0;
    white-space: normal;       /* undo the body's pre-wrap inside the card */
  }
  .mm-thumb:hover { border-color: var(--accent); }
  .mm-thumb svg { display: block; max-width: 100%; height: auto; }
  .mm-thumb.mm-streaming { cursor: default; }
  /* Locate pulse: the Artifacts-tab ⌖ scrolls the fence into view and flashes it. */
  .mm-thumb.mm-located { animation: mmLocatePulse 1.5s ease; }
  @keyframes mmLocatePulse {
    0% { box-shadow: 0 0 0 3px var(--accent); border-color: var(--accent); }
    100% { box-shadow: 0 0 0 6px transparent; }
  }
  /* Artifacts tab: one row per diagram, NEWEST EDIT first. Row click opens the
     viewer; the ⌖ on the right scrolls the chat to the fence instead. */
  .art-row { display: flex; align-items: center; gap: 7px; padding: 6px 8px; border-radius: 4px; cursor: pointer; }
  .art-row:hover { background: var(--panel); }
  .art-row .art-glyph { flex: none; display: inline-flex; color: var(--muted); }
  .art-row:hover .art-glyph { color: var(--accent); }
  .art-row .art-title { flex: 1 1 auto; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .art-row .art-locate { flex: none; background: none; border: none; color: var(--muted); cursor: pointer; font: inherit; font-size: 14px; line-height: 1; padding: 1px 3px; border-radius: 3px; }
  .art-row .art-locate:hover { color: var(--accent); background: var(--panel-alt); }
  /* Fullscreen viewer: above every modal (modalBg tops out at 200). */
  #mmViewer {
    position: fixed; inset: 0; z-index: 300;
    background: rgba(12, 10, 8, 0.93);
    display: none;
  }
  #mmViewer.show { display: block; }
  #mmViewerCanvas {
    position: absolute; inset: 0;
    overflow: hidden;
    cursor: grab;
  }
  #mmViewerCanvas.mm-dragging { cursor: grabbing; }
  #mmViewerPan { position: absolute; left: 0; top: 0; will-change: transform; }
  #mmViewerPan svg { display: block; }
  .mm-viewer-tools {
    position: absolute; top: 14px; right: 18px; z-index: 2;
    display: flex; gap: 8px;
  }
  .mm-viewer-btn {
    background: var(--panel); color: var(--fg);
    border: 1px solid var(--border); border-radius: 6px;
    padding: 6px 12px; font-size: 13px; cursor: pointer;
  }
  .mm-viewer-btn:hover { border-color: var(--accent); color: var(--accent); }
  #mmViewerSrc {
    position: absolute; inset: 58px 24px 24px;
    overflow: auto; margin: 0; padding: 14px;
    background: var(--panel-alt); border: 1px solid var(--border);
    border-radius: 8px; color: var(--fg);
    font-size: 12.5px; white-space: pre;
    display: none;
  }
  #mmViewer.mm-show-code #mmViewerSrc { display: block; }
  #mmViewer.mm-show-code #mmViewerCanvas { display: none; }

  /* Result row — "⎿ summary" sits under the body text of the tool row
     above (one char + gap to the right of the bullet column). Hanging
     indent pulls "⎿" back so wrapped lines align under the summary text
     start. The whole row is clickable to expand. */
  .row-result {
    color: var(--muted);
    font-size: 12.5px;
    /* No top margin — the result sits flush against the tool-call row
       above it. Small bottom margin so the next tool-call clusters
       close while still being visually distinct. */
    margin: 0 0 4px 0;
    padding-left: 32px;       /* dot col (~16px) + "⎿ " marker (~16px) */
    text-indent: -16px;       /* pulls "⎿" leftward by its own width → sits at body-text start */
    white-space: pre-wrap;
    word-wrap: break-word;
    cursor: pointer;
  }
  .row-result.err { color: var(--error); }
  .row-result.expandable:hover { color: var(--fg); }
  /* "Running…" placeholder shown while a tool is still executing. Plain
     muted text — no pulse here because the green dot on the tool-call
     row above already blinks; two pulsing things compete. Click to
     expand the live-output buffer below. */
  .row-result.running { color: var(--muted); }
  .row-result.running:hover { color: var(--fg); }
  .row-result pre {
    margin: 6px 0 0;
    text-indent: 0;
    padding: 6px 10px;
    cursor: text;
    background: #15140f;
    border-left: 2px solid #2f2d27;
    border-radius: 3px;
    white-space: pre;
    overflow-x: auto;
    max-height: 480px;
    overflow-y: auto;
    font-size: 12px;
    color: var(--fg);
  }

  /* Diff block — appears after a write/edit tool result, showing the unified
     diff. Visually distinguished from a .task-wrap (which is a rounded box
     around closed tasks) by using a quote-bar style: no surrounding border,
     just a thin accent strip on the left + a darker background. Reads more
     like a code excerpt than a container. */
  .diff-block {
    margin: 4px 0 12px 24px;
    border: none;
    border-left: 2px solid #4a463a;
    border-radius: 0;
    background: #15140f;
    overflow: hidden;
    font-family: var(--mono);
    font-size: 12px;
    line-height: 1.5;
  }
  .diff-block .diff-hdr {
    background: transparent;
    color: var(--muted);
    padding: 2px 10px;
    font-size: 11px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #2a2823;
  }
  .diff-block .diff-hdr .expand { cursor: pointer; text-decoration: underline; color: var(--accent); }
  .diff-block .diff-body { padding: 6px 0; max-height: 320px; overflow-y: auto; }
  .diff-block .diff-body.collapsed { display: none; }
  .diff-block .dl { padding: 0 10px; white-space: pre; }
  .diff-block .dl.add { background: rgba(70, 200, 70, 0.10); color: #a8e6a8; }
  .diff-block .dl.del { background: rgba(220, 80, 80, 0.10); color: #f0a8a8; }
  .diff-block .dl.hunk { color: var(--muted); }
  .diff-block .dl.same { color: var(--muted); }
  /* Grouped memory saves (consecutive <remember> calls). */
  .remember-group { margin: 1px 0; }
  .remember-group .rg-hdr { cursor: pointer; color: var(--muted); user-select: none; }
  .remember-group .rg-hdr::before { content: '▾ '; display: inline-block; width: 1em; }
  .remember-group.collapsed .rg-hdr::before { content: '▸ '; }
  .remember-group .rg-hdr:hover { color: var(--accent); }
  .remember-group.collapsed .rg-body { display: none; }
  .remember-group .rg-body { padding: 2px 0 2px 18px; }
  .remember-group .rg-item { color: var(--muted); font-size: 12px; white-space: pre-wrap; word-break: break-word; }
  /* Tint the 🧠 emoji from its default pink toward our orange accent (kills the clash). The exact
     shade is tunable via these filter values. */
  .remember-group .rg-icon { display: inline-block; filter: grayscale(1) sepia(1) saturate(4) hue-rotate(-18deg) brightness(0.95); }
  /* Adversarial plan-review (#7). */
  .row.plan-review { font-style: italic; }
  .row.plan-review .pr-hdr { color: var(--accent); font-style: normal; }
  .row.plan-review .pr-findings { margin: 3px 0 2px 18px; }
  .row.plan-review .pr-finding { color: var(--muted); font-size: 12px; font-style: normal; padding: 1px 0; white-space: pre-wrap; word-break: break-word; }
  /* Checklists side-tab (scroll-synced). */
  .checklist-tab { padding: 6px 8px; }
  .checklist-empty { color: var(--muted); font-size: 12px; line-height: 1.5; padding: 4px 2px; }
  /* Markdown thematic break (--- / *** / ___ on their own line). */
  hr.md { border: none; border-top: 1px solid var(--border); margin: 12px 0; }
  /* Jump-to-latest arrow — floats above the input when not locked to the bottom + new content arrives. */
  footer { position: relative; }
  .scroll-bottom-btn {
    position: absolute; top: -44px; left: 50%; transform: translateX(-50%); z-index: 20;
    width: 32px; height: 32px; border-radius: 50%; border: 1px solid var(--accent);
    background: var(--panel); color: var(--accent); font-size: 17px; line-height: 1; cursor: pointer;
    display: none; align-items: center; justify-content: center; box-shadow: 0 2px 8px rgba(0,0,0,0.45);
  }
  .scroll-bottom-btn:hover { background: var(--accent); color: var(--accent-fg); }
  .scroll-bottom-btn.show { display: flex; }
  /* Merge prompt — a UI-only row appended to the chat log. Styled as a distinct ACTION row (orange
     wash + border + a merge glyph) so it stands out from chat messages rather than blending in. */
  .merge-row { display: flex; align-items: center; gap: 8px; padding: 7px 11px; margin: 10px 0; font-size: 12px; color: var(--muted); border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent); border-left: 3px solid var(--accent); border-radius: 5px; background: color-mix(in srgb, var(--accent) 9%, var(--panel)); }
  .merge-row .merge-ico { color: var(--accent); flex: none; }
  .merge-row .merge-parent { color: var(--fg); font-weight: 700; font-family: var(--mono); }
  .merge-row .merge-btn { cursor: pointer; border: 1px solid var(--accent); background: transparent; color: var(--accent); border-radius: 4px; padding: 1px 9px; font-size: 12px; font-weight: 600; }
  .merge-row .merge-btn:hover { background: var(--accent); color: var(--accent-fg); }
  .merge-row .merge-btn:disabled { opacity: 0.6; cursor: default; }
  .merge-row .merge-ahead { color: var(--muted); }
  /* subbranch right side: parent  ← merge  ⑃ CODE_BOSS_branch · N commits */
  .merge-row .merge-source { display: inline-flex; align-items: center; gap: 5px; color: var(--muted); }
  .merge-row .merge-cb { color: var(--fg); font-weight: 600; font-family: var(--mono); }
  /* direct mode → an amber WARNING row (working on the user's branch, not isolated) */
  .merge-row.direct { border-color: color-mix(in srgb, #d9a441 45%, transparent); border-left-color: #d9a441; background: color-mix(in srgb, #d9a441 9%, var(--panel)); }
  .merge-row .warn-ico { color: #d9a441; flex: none; }
  .merge-row .merge-warn { color: var(--muted); }
  .merge-row .merge-warn b { color: var(--fg); font-family: var(--mono); font-weight: 600; }
  /* Reviewer marker — a subtle collapsed line; hover for the full feedback, click to expand. HIDDEN
     by default (the reviewer can run silently); the eye toggle reveals them via body.show-reviewer-notes. */
  .reviewer-note { display: none; margin: 4px 0 4px 6px; padding-left: 8px; font-size: 11px; color: var(--muted); font-style: italic; border-left: 2px solid color-mix(in srgb, var(--accent) 40%, transparent); cursor: pointer; }
  body.show-reviewer-notes .reviewer-note { display: block; }
  .reviewer-note .cn-head::after { content: ' ▸'; opacity: 0.6; }
  .reviewer-note.expanded .cn-head::after { content: ' ▾'; }
  .reviewer-note .cn-full { display: none; white-space: pre-wrap; overflow-wrap: anywhere; word-wrap: break-word; min-width: 0; margin-top: 4px; padding-top: 4px; border-top: 1px dashed var(--border); }
  .reviewer-note.expanded .cn-full { display: block; }
  /* The eye toggle next to the Reviewer chip: gray + slashed when OFF (hidden), green + clear when ON.
     Grouped with the chip (.chip-group) so proximity reads them as one control pair; padded for a
     usable hit target around the 13px glyph; dimmed while the Reviewer itself is OFF (inert-ish). */
  .chip-group { display: inline-flex; align-items: center; gap: 4px; }
  .reviewer-eye { cursor: pointer; display: inline-flex; align-items: center; color: var(--muted); flex: none; position: relative; padding: 3px; }
  .reviewer-eye.on { color: #3fb950; }
  .reviewer-eye .eye-slash { opacity: 1; }
  .reviewer-eye.on .eye-slash { opacity: 0; }
  #criticLabel.off ~ .reviewer-eye { opacity: 0.45; }
  /* Findings that landed while the eye was hidden: a tiny green count so the silent reviewer's
     work is discoverable; cleared the moment the eye reveals the notes. */
  .eye-badge { position: absolute; top: -4px; right: -3px; font-size: 9px; line-height: 1; font-weight: 600; color: #3fb950; }
  /* Deep-think marker — same hidden-until-the-eye contract as .reviewer-note, but its OWN classes +
     body class (body.show-deepthink-notes) so the two eyes stay independent. A transient 'start'
     marker (3 perspectives running…) is replaced in place by the 'result' (question + verdict). */
  .dt-note { display: none; margin: 4px 0 4px 6px; padding-left: 8px; font-size: 11px; color: var(--muted); font-style: italic; border-left: 2px solid color-mix(in srgb, var(--accent) 40%, transparent); cursor: pointer; }
  body.show-deepthink-notes .dt-note { display: block; }
  .dt-note .cn-head::after { content: ' ▸'; opacity: 0.6; }
  .dt-note.expanded .cn-head::after { content: ' ▾'; }
  .dt-note[data-dt-phase="start"] .cn-head::after { content: ''; }   /* nothing to expand yet */
  .dt-note .cn-full { display: none; white-space: pre-wrap; overflow-wrap: anywhere; word-wrap: break-word; min-width: 0; margin-top: 4px; padding-top: 4px; border-top: 1px dashed var(--border); }
  .dt-note.expanded .cn-full { display: block; }
  /* The deep-think eye dims while its own chip is OFF (same pattern as the reviewer's). */
  #deepThinkLabel.off ~ .reviewer-eye { opacity: 0.45; }
  /* "What's new" entries inside the post-update modal. The coral accent belongs to the
     modal HEADER alone (one accent = one meaning); entry titles are plain fg with a muted
     build·date label, and a hairline separates releases on a multi-version jump. */
  .wn-entry { margin-bottom: 14px; }
  .wn-entry + .wn-entry { border-top: 1px solid var(--border); padding-top: 12px; }
  .wn-entry:last-child { margin-bottom: 0; }
  .wn-title { font-weight: 600; color: var(--fg); margin-bottom: 6px; display: flex; justify-content: space-between; align-items: baseline; gap: 10px; }
  .wn-title .wn-meta { font-weight: 400; font-size: 11px; color: var(--muted); white-space: nowrap; flex: none; }
  .wn-items { margin: 0; padding-left: 18px; }
  .wn-items li { margin: 4px 0; color: var(--fg); line-height: 1.45; }

  /* ── Task wraps ─────────────────────────────────────────────────────────
     Minimalist: no full box. The request sits at the top (orange + bold),
     the summary at the bottom (italic + muted, prefixed with a "summary"
     label), and a single thin accent-colored bracket on the left curves
     from the vertical middle of the request line, down past all the
     events, and into the vertical middle of the summary line. Visually
     it's a sideways \`(\` connecting the two text rows. */
  .task-wrap {
    position: relative;
    margin: 6px 0;             /* tight — header/summary are hidden when expanded */
    padding: 0;                /* no inner indent — events stay aligned */
    border: none;
    background: transparent;
    overflow: visible;
  }
  .task-wrap.collapsed { margin: 10px 0; }   /* the header+summary view gets a little room */
  /* Bracket overhangs to the LEFT of the wrap so wrapping a task doesn't
     shift its content — events sit at the same x whether inside a task
     or outside. The 16px overhang lives in the gutter to the left of the
     chat column (between the main padding and the .log content). */
  .task-wrap::before {
    content: '';
    position: absolute;
    left: -16px;
    top: 0.4em;                /* near the top of the body (expanded) / header (collapsed) */
    bottom: 0.65em;            /* turns right at the bottom status row */
    width: 10px;
    border-left: 1px solid var(--muted);
    border-top: 1px solid var(--muted);
    border-bottom: 1px solid var(--muted);
    border-radius: 8px 0 0 8px;
    pointer-events: none;
    opacity: 0.5;
  }
  /* Foot (the bottom status row) is ALWAYS shown; the head (user-request) is
     shown ONLY when collapsed. Split into two rules (not a shared selector) so
     the head's display:none reliably wins over the collapsed override. */
  .task-wrap .task-foot {
    display: flex;
    align-items: baseline;
    gap: 8px;
    padding: 0;
    background: transparent;
    border: none;
    overflow: hidden;
    font-size: 11.5px;
    color: var(--muted);
    font-style: italic;
    white-space: normal;
    line-height: 1.45;
  }
  .task-wrap .task-head {
    display: none;             /* expanded (default): no header */
    align-items: baseline;
    gap: 8px;
    padding: 0;
    background: transparent;
    border: none;
    overflow: hidden;
    cursor: pointer;           /* click the header to expand (only visible when collapsed) */
  }
  .task-wrap.collapsed .task-head { display: flex; }
  .task-wrap .task-head .ur {
    color: var(--accent);
    font-weight: 700;
    font-size: 13.5px;
    flex: 1 1 auto;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    min-width: 0;
  }
  .task-wrap .task-head .tag {
    flex-shrink: 0;
    font-size: 9.5px;
    padding: 1px 6px;
    border-radius: 8px;
    background: rgba(217, 119, 87, 0.15);
    color: var(--accent);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.4px;
  }
  .task-wrap .task-body {
    padding: 4px 0 6px 0;
  }
  /* Summary text shown ONLY when collapsed (the old "SUMMARY" label is gone). */
  .task-wrap:not(.collapsed) .task-foot-summary { display: none; }
  /* Small collapse/expand chevron — lives in the foot status row, always visible. */
  .task-foot-toggle {
    cursor: pointer;
    user-select: none;
    color: var(--muted);
    opacity: 0.6;
    font-size: 15px;        /* bigger, easier to hit/see */
    line-height: 1;
    padding: 0 4px;
    font-style: normal;
    flex-shrink: 0;
  }
  /* Thinking-probe results (settings modal): tight monospace table for a screenshot. */
  .think-probe-out {
    margin: 2px 16px 10px;
    padding: 8px 10px;
    background: var(--panel-alt);
    border: 1px solid var(--border);
    border-radius: 6px;
    font-size: 10.5px;
    line-height: 1.45;
    color: var(--fg);
    white-space: pre;
    overflow-x: auto;
    max-height: 280px;
  }
  .task-foot-toggle:hover { opacity: 1; }
  /* Per-task status checks. Success = the muted bracket color; failure = error. */
  .task-foot-review {
    margin-left: 10px;
    font-style: normal;
    font-weight: 600;
    font-size: 11px;
  }
  .task-foot-review.review-pass { color: var(--muted); }
  .task-foot-review.review-fail { color: var(--error); }
  .task-foot-review.review-escalate, .task-foot-review.review-error { color: var(--error); }
  .task-foot-review.review-pending { color: var(--muted); }
  .task-foot-commit { margin-left: 10px; font-style: normal; font-weight: 600; font-size: 11px; }
  .task-foot-commit.commit-ok { color: var(--muted); }
  .task-foot-commit.commit-error { color: var(--error); }
  /* Project (tier-4) rollup: a slightly stronger muted bracket. */
  .task-wrap.project::before {
    opacity: 0.7;
    border-left-width: 1.5px;
    border-top-width: 1.5px;
    border-bottom-width: 1.5px;
  }
  /* Collapsed: hide the body (the header + summary show instead). */
  .task-wrap.collapsed .task-body { display: none; }

  /* A closed task with no body rows still renders as a real .task-wrap (so its
     review verdict + late git/diff rows can nest). :empty collapses the padding
     only while the body is genuinely empty. */
  .task-wrap .task-body:empty { padding: 0; min-height: 0; }
  .task-wrap.empty { margin: 4px 0; }

  /* "Open in browser" button shown after a successful wl deploy. Clearly an
     action that opens a new tab — distinct from the plain '⎿' result text. */
  .open-app-bar { margin: 4px 0 2px 18px; }
  .open-app-btn {
    display: inline-flex; align-items: center; gap: 4px;
    margin: 2px 6px 2px 0;
    padding: 3px 11px;
    font-size: 11px; font-weight: 600;
    color: var(--accent);
    background: var(--panel-alt);
    border: 1px solid var(--accent);
    border-radius: 11px;
    cursor: pointer;
  }
  .open-app-btn:hover:not(:disabled) { background: var(--accent); color: var(--panel); }
  .open-app-btn:disabled { opacity: 0.55; cursor: default; }

  /* Reviewer feedback row. Inline below the just-completed work.
     States: pending (gray + animated ring), pass (green), fail (warn),
     escalate (red). Layout: ring + label on one line, concerns prose
     wraps into a child block beneath when present. */
  .review-row {
    display: grid;
    grid-template-columns: 18px 1fr;
    gap: 6px 8px;
    margin: 10px 0;
    padding: 6px 0;
    font-family: var(--mono);
    font-size: 12px;
    color: var(--muted);
  }
  .review-ring {
    grid-row: 1;
    grid-column: 1;
    width: 18px;
    height: 18px;
    line-height: 18px;
    text-align: center;
    border-radius: 50%;
    font-size: 12px;
    font-weight: 700;
    flex-shrink: 0;
  }
  .review-label {
    grid-row: 1;
    grid-column: 2;
    align-self: center;
    line-height: 1.4;
  }
  .review-concerns {
    grid-row: 2;
    grid-column: 2;
    color: var(--fg);
    white-space: pre-wrap;
    word-wrap: break-word;
    background: var(--panel);
    border-left: 2px solid var(--muted);   /* square + light-gray left border, like the diff view */
    border-radius: 0;
    padding: 6px 10px;
    margin-top: 2px;
    font-size: 12.5px;
    line-height: 1.5;
  }
  /* pending — gray, dotted spinner ring */
  .review-pending .review-ring {
    color: var(--muted);
    border: 1.5px dotted var(--muted);
    animation: review-spin 1.8s linear infinite;
  }
  @keyframes review-spin { to { transform: rotate(360deg); } }
  /* pass — green */
  .review-pass {
    color: #7bb070;
  }
  .review-pass .review-ring {
    color: #7bb070;
    border: 1.5px solid #7bb070;
  }
  /* fail — warn orange (we're fixing it) */
  .review-fail {
    color: var(--accent);
  }
  .review-fail .review-ring {
    color: var(--accent);
    border: 1.5px solid var(--accent);
  }
  /* escalate — red */
  .review-escalate {
    color: var(--error);
  }
  .review-escalate .review-ring {
    color: var(--error);
    border: 1.5px solid var(--error);
  }

  /* Mute: applied to content the LLM no longer sees (results masked at tier 2,
     whole task at tier 3, whole project's children at tier 4). */
  .muted, .muted * { opacity: 0.42; }
  .muted:hover, .muted:hover * { opacity: 0.85; }

  /* Welcome box (rounded terminal banner). Border is muted gray so only
     user messages get the accent (orange) outline — the welcome is a
     system-style banner, not a user message. */
  .welcome {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 14px 18px;
    margin: 12px 0 28px;
  }
  .welcome .wt { color: var(--accent); font-weight: 700; }
  .welcome .wl { color: var(--muted); font-size: 12.5px; margin-top: 3px; }

  /* Inline status row inside the chat log — appears as the next "line" the
     agent is producing. Fixed-width pieces keep the text from jittering. */
  .status {
    color: var(--accent);
    margin: 10px 0;
    font-variant-numeric: tabular-nums;
  }
  .status .spin {
    display: inline-block;
    width: 1ch;
    text-align: center;
  }
  .status .arrow { display: inline-block; color: var(--muted); }
  .status .count { color: var(--muted); }
  .status.thinking .arrow { animation: blinkArrow 1.2s ease-in-out infinite; }
  @keyframes blinkArrow { 50% { opacity: 0.25; } }
  .status .dim { color: var(--muted); }
  .status.hidden { display: none; }
  /* Server unreachable (the SSE is gone AND /api/state keeps failing): the row
     stops being a spinner and becomes a persistent, clearly-an-error line. */
  .status.conn-lost { color: var(--error); }
  .status.conn-lost .dim { color: var(--error); opacity: 0.75; }

  footer {
    /* Continue the page bg straight into the footer — no panel tint,
       no top border. The gradient strip below acts as the visual
       separator and gives a fade as chat content scrolls toward the
       input. */
    background: var(--bg);
    /* Horizontal padding MATCHES #logScroll (36 left / 20 right) so the centered
       .input-row lines up exactly with the centered .log user bubbles above. */
    padding: 10px 20px 8px 36px;
    flex-shrink: 0;
    position: relative;
  }
  /* Gradient fade sitting just above the input row. Anything in the
     scrolling chat that approaches the bottom of \`main\` passes behind
     this overlay and softens out before reaching the input. */
  footer::before {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    top: -36px;
    height: 36px;
    background: linear-gradient(to bottom, transparent, var(--bg));
    pointer-events: none;
    z-index: 1;
  }
  .checklist-panel {
    max-width: 860px;
    margin: 0 auto 6px;
    padding: 7px 10px;
    background: var(--panel, #1a1d23);
    border: 1px solid var(--border, #2a2f3a);
    border-radius: 8px;
    font-size: 12.5px;
    max-height: 30vh;
    overflow-y: auto;
  }
  .checklist-panel .cl-title { color: var(--muted); font-size: 11px; text-transform: uppercase; letter-spacing: .04em; margin-bottom: 4px; }
  .checklist-item { display: flex; gap: 7px; align-items: baseline; padding: 2px 0; line-height: 1.35; }
  .checklist-item .cl-box { flex: none; width: 1.1em; }
  .checklist-item.done { color: var(--muted); }
  .checklist-item.done .cl-text { text-decoration: line-through; }
  .input-row {
    max-width: 860px;
    margin: 0 auto;
    display: flex;
    gap: 8px;
    align-items: flex-start;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--panel-alt);
    /* 6px 10px matches the user bubble's padding so the input is the same height. */
    padding: 6px 10px;
  }
  .input-row:focus-within { border-color: var(--accent); }
  /* Attached-image preview chips: a row above the input. Thumbnail ~48px,
     filename + size, ✕ to remove. Hidden when no images are attached. */
  .img-chips {
    max-width: 860px;
    margin: 0 auto 6px;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }
  .img-chip {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 8px 4px 4px;
    background: var(--panel-alt);
    border: 1px solid var(--border);
    border-radius: 8px;
    font-size: 11px;
    color: var(--fg);
  }
  .img-chip img {
    width: 48px;
    height: 48px;
    object-fit: cover;
    border-radius: 5px;
    display: block;
    background: var(--bg);
  }
  .img-chip .ic-meta { display: flex; flex-direction: column; gap: 1px; max-width: 180px; }
  .img-chip .ic-name { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .img-chip .ic-size { color: var(--muted); font-size: 10px; }
  .img-chip .ic-x {
    background: transparent;
    border: none;
    color: var(--muted);
    cursor: pointer;
    font-size: 13px;
    padding: 2px 4px;
    border-radius: 3px;
    line-height: 1;
  }
  .img-chip .ic-x:hover { color: var(--error); background: var(--bg); }
  /* Drag-drop affordance while a file is over the chat/input area. */
  #chatMain.img-dragover { outline: 2px dashed var(--accent); outline-offset: -6px; }
  /* Queue button in the right gutter of the input. Visible only while the
     agent is busy — clicking it (or pressing Enter) moves the textarea
     contents into the pending-message queue above. */
  .input-row .queue-btn {
    flex-shrink: 0;
    align-self: center;
    background: transparent;
    border: none;
    color: var(--muted);
    cursor: pointer;
    padding: 4px;
    margin-left: 2px;
    line-height: 0;
    border-radius: 3px;
  }
  .input-row .queue-btn:hover { color: var(--accent); background: var(--bg); }
  .input-row .queue-btn svg { display: block; }
  .input-row .queue-btn:disabled { opacity: 0.4; cursor: not-allowed; }

  /* Context-window fill gauge in the status row (item A). */
  .context-bar {
    display: inline-block; width: 90px; height: 6px; margin-left: 8px;
    vertical-align: middle; background: var(--bg); border: 1px solid var(--border);
    border-radius: 3px; overflow: hidden;
  }
  .context-bar > i { display: block; height: 100%; background: var(--accent); transition: width 0.3s ease; }
  .context-bar.hot > i { background: #d9534f; }
  .context-pct { color: var(--muted); font-size: 11px; margin-left: 4px; font-variant-numeric: tabular-nums; }

  /* Visible "compacting…" marker (item A). */
  .compaction-row {
    margin: 6px 0; padding: 4px 10px; font-size: 12px; color: var(--muted);
    background: var(--bg); border: 1px dashed var(--border); border-radius: 4px; text-align: center;
  }

  /* Editable task summary (item D). */
  .task-foot-summary { cursor: text; border-bottom: 1px dotted transparent; }
  .task-foot-summary:hover { border-bottom-color: var(--muted); }
  .task-foot-edit {
    font: inherit; font-family: var(--mono, monospace); background: var(--bg); color: var(--fg);
    border: 1px solid var(--accent); border-radius: 3px; padding: 1px 6px; min-width: 260px;
  }

  /* Undo-task control on a failed/escalated review footer (item F). */
  .task-undo-btn {
    margin-left: 8px; padding: 1px 7px; font: inherit; font-size: 11px; cursor: pointer;
    background: var(--bg); color: var(--muted); border: 1px solid var(--border); border-radius: 3px;
  }
  .task-undo-btn:hover { color: #d9534f; border-color: #d9534f; }
  .task-undo-btn:disabled { opacity: 0.5; cursor: default; }

  /* Steer button — send now, takes effect next turn (item H). */
  .input-row .steer-btn:hover { color: var(--accent); background: var(--bg); }

  /* Read-only "Discuss" toggle in the input subbar (item G). */
  .discuss-toggle {
    display: inline-flex; align-items: center; gap: 4px; font-size: 11px; color: var(--muted);
    cursor: pointer; user-select: none; margin-right: 8px;
  }
  .discuss-toggle input { cursor: pointer; margin: 0; }
  .discuss-toggle:hover { color: var(--fg); }

  /* Loud escalation line on the acceptance gate (item J). */
  .row.assignment-gate .gate-line.escalation { color: #d9534f; font-weight: 500; }

  /* Pending-message queue above the input. Each row is one item; clicking
     anywhere outside the play/delete buttons swaps the row's text with the
     current input contents. */
  .queue-stack {
    max-width: 860px;
    margin: 0 auto 6px;
    display: flex;
    flex-direction: column;
    gap: 3px;
  }
  .queue-stack:empty { display: none; }
  .queue-item {
    display: flex;
    align-items: center;
    gap: 6px;
    background: var(--panel-alt);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 3px 4px 3px 10px;
    font-family: var(--mono);
    font-size: 12px;
    color: var(--muted);
    cursor: pointer;
    transition: border-color 0.1s, color 0.1s;
  }
  .queue-item:hover { border-color: var(--accent); color: var(--fg); }
  .queue-item.selected { border-color: var(--accent); }
  .queue-item:focus, .queue-item:focus-visible { outline: none; border-color: var(--accent); box-shadow: 0 0 0 1px var(--accent) inset; }
  .queue-item .queue-preview {
    flex: 1 1 auto;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    line-height: 1.4;
  }
  .queue-item .queue-play,
  .queue-item .queue-del {
    flex-shrink: 0;
    background: transparent;
    border: none;
    color: var(--muted);
    cursor: pointer;
    font-size: 12px;
    line-height: 1;
    padding: 3px 7px;
    border-radius: 3px;
  }
  .queue-item .queue-play:hover:not(:disabled),
  .queue-item .queue-del:hover { color: var(--accent); background: var(--bg); }
  .queue-item .queue-play:disabled { opacity: 0.35; cursor: not-allowed; }
  .input-row::before {
    content: ">";
    color: var(--accent);
    font-weight: 700;
    line-height: 1.55;
    flex-shrink: 0;
  }
  textarea {
    flex: 1;
    /* min-height = one body line. Auto-grow JS adjusts up to max-height. */
    min-height: calc(1em * 1.55);
    max-height: 220px;
    padding: 0 2px;
    border: none;
    background: transparent;
    font: inherit;
    line-height: 1.55;
    color: var(--fg);
    resize: none;
    outline: none;
    overflow-y: hidden;
  }
  textarea::placeholder { color: var(--muted); }

  /* Sub-bar beneath the input: project path on the left, settings gear on
     the right. 3/4 of the normal font size, muted color. */
  .input-subbar {
    max-width: 860px;
    margin: 6px auto 0;
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 10px;
    color: var(--muted);
    font-family: var(--mono);
  }
  .input-subbar .proj-path {
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    cursor: help;
  }
  .input-subbar .settings-btn {
    background: transparent;
    border: none;
    color: var(--muted);
    cursor: pointer;
    font-size: 14px;
    padding: 0 4px;
    line-height: 1;
  }
  .input-subbar .settings-btn:hover { color: var(--accent); }
  .input-subbar .dirty-indicator {
    color: var(--error);
    font-size: 12px;
    line-height: 1;
    cursor: help;
    flex-shrink: 0;
  }
  /* WebLogic server indicator (only shown when the wl plugin is monitoring the
     server): green + lit when running on :7101, red when stopped. */
  .input-subbar .wl-indicator { display: inline-flex; align-items: center; gap: 5px; font-size: 11px; color: var(--muted); flex-shrink: 0; cursor: help; }
  .input-subbar .wl-indicator .wl-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--muted); display: inline-block; flex-shrink: 0; }
  .input-subbar .wl-indicator.up { color: #3fb950; }
  .input-subbar .wl-indicator.up .wl-dot { background: #3fb950; box-shadow: 0 0 5px #3fb950; }
  .input-subbar .wl-indicator.down { color: #d9534f; }
  .input-subbar .wl-indicator.down .wl-dot { background: #d9534f; }

  /* Settings modal — vertical stack of labeled rows. Reuses .modal-bg. */
  .settings-row {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 6px 0;
    border-bottom: 1px solid var(--border);
    font-size: 13px;
  }
  .settings-row:last-child { border-bottom: none; }
  .settings-row label {
    flex: 0 0 110px;
    color: var(--muted);
    font-size: 12px;
  }
  .settings-row .value { flex: 1; color: var(--fg); word-break: break-all; }
  .settings-row select,
  .settings-row input[type="text"] {
    flex: 1;
    font: inherit;
    font-size: 12px;
    background: var(--panel-alt);
    color: var(--fg);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 3px 6px;
  }
  .settings-row .btn,
  .settings-actions .btn {
    font: inherit;
    font-size: 12px;
    padding: 4px 12px;
    border: 1px solid var(--border);
    background: transparent;
    border-radius: 4px;
    cursor: pointer;
    color: var(--muted);
  }
  .settings-row .btn:hover,
  .settings-actions .btn:hover { border-color: var(--accent); color: var(--fg); }
  .settings-actions {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 12px;
  }

  .modal-bg {
    position: fixed; inset: 0;
    background: rgba(0,0,0,0.6);
    display: none;
    align-items: flex-start;
    justify-content: center;
    padding: 12px;
    overflow-y: auto;
    z-index: 100;
  }
  .modal-bg.show { display: flex; }
  .modal {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 12px;
    max-width: 720px;
    width: 100%;
    overflow: hidden;
    position: relative;   /* anchor the bottom-right snapshot fab */
  }
  /* Snapshot: ONE consistent affordance — a subtle icon in the bottom-right.
     Fixed to the viewport on the landing/init screens; re-docked into the
     bottom-right of whatever modal is open (see dockSnapFab). The main chat
     reaches Snapshot via the Settings gear, so the fab hides there. */
  .snap-fab {
    position: fixed; bottom: 12px; right: 14px; z-index: 95;
    display: flex; align-items: center; justify-content: center;
    width: 24px; height: 24px; padding: 0; line-height: 0;
    color: var(--muted); opacity: 0.45; cursor: pointer;
    background: none; border: none;
    transition: opacity 0.15s ease, color 0.15s ease;
  }
  .snap-fab:hover { opacity: 1; color: var(--accent); }
  .snap-fab.in-modal { position: absolute; bottom: 8px; right: 10px; z-index: 5; }
  .snap-fab.hidden { display: none; }
  .snap-fab svg { display: block; }
  /* Snapshot is a full-screen programmable mosaic. The user activates a set
     of numbered queries (catalog of ~38). Server runs them, packs the
     resulting tiles onto one or more 12×8 grid pages, and returns positions.
     The browser renders one page at a time with absolute grid placement —
     every tile fits within the viewport, every page is screenshot-friendly. */
  .modal-bg.fullpage {
    padding: 0;
    align-items: stretch;
    overflow: hidden;
    height: 100vh;
  }
  .modal-bg.fullpage .modal {
    max-width: none;
    border-radius: 0;
    min-height: 0;
    height: 100vh;
    width: 100vw;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }
  /* Compact mode: every pixel of chrome shrunk to the minimum that's still
     readable in a screenshot. Headers are present (need them to identify
     tiles) but use background distinction only, no border. Padding/gap
     trimmed so tile content butts close to the edges. */
  .modal-bg.fullpage .modal-header {
    padding: 1px 4px;
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    gap: 4px;
    flex-wrap: wrap;
    border-bottom: 1px solid var(--border);
    min-height: 0;
  }
  .modal-bg.fullpage .modal-header h2 { font-size: 9px; margin: 0; }
  .modal-bg.fullpage .modal-close { font-size: 12px; padding: 0 4px; line-height: 1; }
  .modal-bg.fullpage .modal-body {
    padding: 0;
    max-height: none;
    flex: 1 1 auto;
    overflow: hidden;
    min-height: 0;
    position: relative;
  }
  /* Mosaic header controls — activation input, defaults button, page nav. */
  .mosaic-controls {
    display: flex;
    align-items: center;
    gap: 4px;
    font-family: var(--mono);
    font-size: 9px;
    flex-wrap: wrap;
    flex: 1 1 auto;
  }
  .mosaic-controls input {
    background: #15140f;
    color: var(--fg);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 0 4px;
    font-family: var(--mono);
    font-size: 9px;
    min-width: 160px;
    flex: 1 1 160px;
    height: 16px;
  }
  .mosaic-controls .btn-sm {
    background: #2a2823;
    color: var(--fg);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 0 6px;
    font-family: var(--mono);
    font-size: 9px;
    height: 16px;
    cursor: pointer;
    line-height: 1;
  }
  .mosaic-controls .btn-sm:hover { background: var(--accent); color: var(--accent-fg); }
  .mosaic-controls .pageno { color: var(--muted); font-size: 9px; }
  .mosaic-controls .build-pill {
    background: #15140f; color: var(--muted); border: 1px solid var(--border);
    padding: 0 4px; border-radius: 2px; font-size: 8px;
  }
  /* Page = 12 cols × 24 rows grid filling the viewport beneath the header.
     The vertical resolution is intentionally fine — each row is ~37px in a
     typical 1080p viewport, just enough for one line of body content plus a
     fraction of header chrome. This lets short tiles claim 1-2 rows without
     huge empty space below their content. */
  .mosaic-page {
    position: absolute;
    inset: 0;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    grid-template-rows: repeat(24, 1fr);
    gap: 2px;
    padding: 2px;
    box-sizing: border-box;
  }
  .mosaic-page[hidden] { display: none; }
  /* Each tile occupies a rectangle of grid cells, positioned by inline CSS
     variables set during render. 1-indexed to match CSS Grid line numbers. */
  .mosaic-tile {
    grid-column: var(--col) / span var(--cspan);
    grid-row: var(--row) / span var(--rspan);
    background: #15140f;
    border: 1px solid var(--border);
    border-radius: 2px;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    min-width: 0;
    min-height: 0;
  }
  .mosaic-tile.sev-ok       { border-color: rgba(70,160,70,0.6); }
  .mosaic-tile.sev-info     { border-color: rgba(100,140,200,0.6); }
  .mosaic-tile.sev-warn     { border-color: rgba(227,196,106,0.7); }
  .mosaic-tile.sev-critical { border-color: rgba(220,80,80,0.8); }
  .mosaic-tile-head {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 0 4px;
    flex: 0 0 auto;
    background: #1a1814;
    line-height: 1.1;
  }
  .mosaic-tile-head .id {
    color: var(--accent);
    font-family: var(--mono);
    font-size: 8.5px;
    font-weight: bold;
  }
  .mosaic-tile-head .name {
    color: var(--fg);
    font-size: 9px;
    flex: 1 1 auto;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .mosaic-tile-head .sev-tag {
    font-size: 7.5px;
    padding: 0 3px;
    border-radius: 2px;
    text-transform: uppercase;
    font-family: var(--mono);
    line-height: 1.3;
  }
  .mosaic-tile-head .sev-tag.ok       { background: rgba(70,160,70,0.25);  color: #8fd28f; }
  .mosaic-tile-head .sev-tag.info     { background: rgba(100,140,200,0.22); color: #9fc4e8; }
  .mosaic-tile-head .sev-tag.warn     { background: rgba(227,196,106,0.22); color: #e3c46a; }
  .mosaic-tile-head .sev-tag.critical { background: rgba(220,80,80,0.25);   color: #ff8f8f; }
  .mosaic-tile-body {
    flex: 1 1 auto;
    overflow: hidden;
    min-height: 0;
    padding: 1px 4px 2px;
    font-family: var(--mono);
    font-size: 9px;
    line-height: 1.0;
    white-space: pre-wrap;
    word-break: break-word;
    color: #cfc9b8;
  }
  /* Findings rendered inside a tile */
  .mosaic-tile-body .finding {
    display: flex;
    gap: 4px;
    align-items: baseline;
    padding: 0;
    font-size: 8.5px;
    line-height: 1.05;
  }
  .mosaic-tile-body .finding .sev {
    flex: 0 0 auto;
    min-width: 44px;
    padding: 0 3px;
    border-radius: 2px;
    text-align: center;
    font-weight: bold;
    font-size: 8px;
  }
  .mosaic-tile-body .finding .sev.ok       { background: rgba(70,160,70,0.25);  color: #8fd28f; }
  .mosaic-tile-body .finding .sev.info     { background: rgba(100,140,200,0.22); color: #9fc4e8; }
  .mosaic-tile-body .finding .sev.warn     { background: rgba(227,196,106,0.22); color: #e3c46a; }
  .mosaic-tile-body .finding .sev.critical { background: rgba(220,80,80,0.25);   color: #ff8f8f; }
  .mosaic-tile-body .finding .cat { color: var(--muted); flex: 0 0 auto; min-width: 44px; font-size: 8px; }
  .mosaic-tile-body .finding .text { flex: 1 1 auto; word-break: break-word; }
  /* Table tiles */
  .mosaic-tile-body table { width: 100%; border-collapse: collapse; font-size: 8.5px; }
  .mosaic-tile-body th, .mosaic-tile-body td { padding: 0 3px; border-bottom: 1px solid #2a2823; text-align: left; vertical-align: top; line-height: 1.05; }
  .mosaic-tile-body th { color: var(--muted); font-weight: normal; }
  /* Catalog index page tile */
  .mosaic-catalog-tile { grid-column: 1 / span 12; grid-row: 1 / span 24; }
  .mosaic-catalog-tile .mosaic-tile-body { font-size: 9px; padding: 1px 4px; }
  .mosaic-catalog-tile table { font-size: 9px; }
  .mosaic-catalog-tile th, .mosaic-catalog-tile td { padding: 0 4px; line-height: 1.25; }
  .mosaic-catalog-tile td.id { color: var(--accent); font-weight: bold; min-width: 24px; }
  .mosaic-catalog-tile td.cat { color: var(--muted); min-width: 60px; }
  .mosaic-catalog-tile td.size { color: var(--muted); font-size: 8.5px; min-width: 40px; }

  .modal.error { border-top: 4px solid var(--error); }
  .modal-header {
    padding: 14px 18px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .modal-header h2 { margin: 0; font-size: 14px; font-weight: 700; color: var(--accent); }
  .modal.error .modal-header h2 { color: var(--error); }
  .modal-body { padding: 18px 20px; max-height: 70vh; overflow-y: auto; }

  /* Dense diagnostic layout. */
  .diag-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 8px 24px;
    margin: 8px 0 18px;
  }
  .diag-section h3 {
    margin: 14px 0 4px;
    color: var(--accent);
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    border-bottom: 1px solid var(--border);
    padding-bottom: 2px;
  }
  .diag-kv {
    display: grid;
    grid-template-columns: max-content 1fr;
    gap: 2px 12px;
    font-size: 12px;
    font-family: var(--mono);
  }
  .diag-kv .k { color: var(--muted); white-space: nowrap; }
  .diag-kv .v { color: var(--fg); word-break: break-all; }
  .diag-kv .v.ok { color: #8fd28f; }
  .diag-kv .v.warn { color: #e3c46a; }
  .diag-kv .v.err { color: var(--error); }
  .diag-block {
    background: #15140f;
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 6px 10px;
    font-family: var(--mono);
    font-size: 11.5px;
    line-height: 1.45;
    white-space: pre-wrap;
    word-break: break-word;
    max-height: 320px;
    overflow-y: auto;
    color: #cfc9b8;
  }
  .diag-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 11.5px;
    font-family: var(--mono);
  }
  .diag-table th, .diag-table td {
    text-align: left;
    padding: 3px 8px;
    border-bottom: 1px solid #2a2823;
    vertical-align: top;
  }
  .diag-table th { color: var(--muted); font-weight: normal; background: #1a1814; }
  .diag-table td.num { text-align: right; font-variant-numeric: tabular-nums; }
  .diag-summary {
    font-family: var(--mono);
    font-size: 12px;
    line-height: 1.4;
    background: #15140f;
    border: 1px solid var(--accent);
    border-radius: 4px;
    padding: 10px 14px;
    margin: 0 0 8px;
    color: var(--fg);
    white-space: pre;
  }
  .pill { display: inline-block; padding: 1px 7px; border-radius: 99px; font-size: 11px; }
  .pill.idle { background: #2a2823; color: var(--muted); }
  .pill.running { background: rgba(217,119,87,0.2); color: var(--accent); }
  .pill.done { background: rgba(70,160,70,0.2); color: #8fd28f; }
  .pill.error { background: rgba(220,80,80,0.2); color: var(--error); }
  .modal-close {
    background: none;
    border: none;
    cursor: pointer;
    font-size: 20px;
    color: var(--muted);
    padding: 4px 8px;
    line-height: 1;
  }
  .modal-close:hover { color: var(--fg); }

  .kv { display: grid; grid-template-columns: 160px 1fr; gap: 6px 16px; font-size: 13px; }
  .kv .k { color: var(--muted); font-weight: 500; }
  .kv .v { font-family: var(--mono); word-break: break-all; }

  table { border-collapse: collapse; width: 100%; font-size: 12px; margin-top: 8px; }
  th, td { padding: 5px 8px; border-bottom: 1px solid var(--border); text-align: left; vertical-align: top; }
  th { background: var(--panel-alt); font-weight: 600; color: var(--muted); }
  td.t-ms { font-variant-numeric: tabular-nums; color: var(--muted); width: 60px; }
  td.t-type { font-family: var(--mono); font-size: 11px; color: var(--accent); width: 130px; }

  pre {
    background: #161512;
    border: 1px solid var(--border);
    padding: 10px 12px;
    border-radius: 6px;
    overflow-x: auto;
    font: 11px/1.45 var(--mono);
    max-height: 320px;
    color: #b9b5a8;
  }
  h3 { margin: 18px 0 6px; font-size: 12px; font-weight: 600; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; }
  .par-badge {
    display: inline-block;
    font-size: 10px;
    font-weight: 700;
    padding: 2px 8px;
    border-radius: 999px;
    letter-spacing: 0.5px;
    margin-left: 8px;
    vertical-align: middle;
  }
  .par-good { background: #1e3a2e; color: #6ee7b7; }
  .par-warn { background: #3a2f1a; color: #fcd34d; }
  .par-bad  { background: #3a1f1c; color: #fca5a5; }
  .par-na   { background: #2a2823; color: #a8a49a; }
  .par-caption { color: var(--muted); font-size: 12px; margin: 0 0 12px; }
  .batch-strip {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    margin: 10px 0 4px;
    padding: 8px;
    background: #161512;
    border: 1px solid var(--border);
    border-radius: 6px;
  }
  .batch-strip .dot {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 22px;
    height: 22px;
    padding: 0 6px;
    border-radius: 4px;
    font-size: 11px;
    font-weight: 600;
    font-variant-numeric: tabular-nums;
  }
  .dot-single { background: #2a2823; color: #8c887b; }
  .dot-multi  { background: var(--accent); color: var(--accent-fg); }
  .error-banner {
    background: var(--error-bg);
    border-left: 4px solid var(--error);
    padding: 10px 14px;
    margin: 8px 0;
    border-radius: 0 6px 6px 0;
  }
  .error-banner strong { color: var(--error); }
  /* Dense, high-contrast block optimized for a single screenshot (photo of screen). */
  pre.dense {
    background: #fff;
    color: #000;
    border: 2px solid #000;
    font: bold 15px/1.4 ui-monospace, Consolas, Menlo, monospace;
    padding: 12px;
    white-space: pre-wrap;
    word-break: break-word;
    margin: 0 0 12px;
  }
  .modal.error pre.dense { border-color: var(--error); }
  /* Multiple-tabs warning. Each open code_boss tab holds a live SSE
     connection; with several open, the browser's ~6-per-origin HTTP/1.1 cap
     is hit and all new fetches (send, settings, snapshot) silently hang. */
  #tabWarn {
    display: none;
    position: fixed; top: 0; left: 0; right: 0; z-index: 9999;
    background: var(--warn-bg); color: var(--accent);
    border-bottom: 1px solid var(--accent);
    font: 12.5px/1.4 var(--mono);
    padding: 6px 12px; text-align: center;
  }
  #tabWarn.show { display: block; }
</style>
<script>
// ====== injected from agent/mosaic-layout.mjs ======
/**
 * Mosaic layout — pure functions that size and pack tiles on a grid.
 *
 * Shared between the Node server (where layout-on-fetch is the fallback)
 * and the browser script (where the same code runs with measured font
 * metrics + actual viewport dimensions). The build script injects this
 * file's contents into both contexts; keep it pure (no I/O, no Node-only
 * APIs) so it can run unchanged in either.
 *
 * Three exports:
 *   - computeTileSize(tile, opts) → {cols, rows}
 *   - packPage(tiles, cols, rows, opts) → {placed, notPlaced, score, ...}
 *   - layoutMosaic(tiles, pageCols, pageRows, opts) → {pages, ...}
 */

/**
 * Compute the SMALLEST {cols, rows} rectangle that holds the tile's content
 * without scrolling. Per-kind heuristic: for text/json we count chars and
 * lines; for findings we account for the badge chrome; for tables we sum
 * column widths.
 *
 * The conversion to grid cells depends on opts.charsPerCol and
 * opts.linesPerRow, which the CALLER computes from the actual rendered
 * font + viewport. The server passes its baseline (assumes 1400×800 at
 * 9px monospace); the browser passes measured values.
 */
function computeTileSize(tile, opts = {}) {
  const charsPerCol = opts.charsPerCol ?? 20;
  const linesPerRow = opts.linesPerRow ?? 7;
  const minCols     = opts.minCols ?? 2;
  const minRows     = opts.minRows ?? 1;
  const maxCols     = opts.maxCols ?? 12;
  const maxRows     = opts.maxRows ?? 8;
  // Chrome budget in lines (header bar + body padding amortized as text lines).
  const chromeLines = opts.chromeLines ?? 2;

  const kind = tile?.kind ?? 'text';
  const body = tile?.body;
  let maxLine = 0;
  let lineCount = 1;

  if (kind === 'json') {
    let text;
    try { text = JSON.stringify(body, null, 2); }
    catch { text = String(body); }
    const lines = text.split('\\n');
    lineCount = lines.length;
    for (const line of lines) if (line.length > maxLine) maxLine = line.length;
  } else if (kind === 'findings') {
    const findings = Array.isArray(body) ? body : [];
    lineCount = Math.max(1, findings.reduce((n, f) => n + (f?.detail ? 2 : 1), 0));
    let widest = 50;
    for (const f of findings) {
      const t = (f?.title?.length ?? 0) + 14;
      if (t > widest) widest = t;
      if (f?.detail) {
        const d = (String(f.detail).length ?? 0) + 4;
        if (d > widest) widest = d;
      }
    }
    maxLine = widest;
  } else if (kind === 'table') {
    const t = body ?? { columns: [], rows: [] };
    lineCount = (t.rows?.length ?? 0) + 1;
    const cols = t.columns ?? [];
    let width = 0;
    for (let i = 0; i < cols.length; i++) {
      let colW = String(cols[i] ?? '').length;
      for (const row of (t.rows ?? [])) {
        const cellStr = String(row[i] ?? '');
        if (cellStr.length > colW) colW = cellStr.length;
      }
      width += Math.min(colW, 40) + 2;
    }
    maxLine = Math.max(width, 30);
  } else {
    const text = String(body ?? '');
    const lines = text.split('\\n');
    lineCount = lines.length;
    for (const line of lines) if (line.length > maxLine) maxLine = line.length;
  }

  if (maxLine === 0) maxLine = 10;

  let cols = Math.ceil(maxLine / charsPerCol);
  let rows = Math.ceil((lineCount + chromeLines) / linesPerRow);

  cols = Math.max(minCols, Math.min(maxCols, cols));
  rows = Math.max(minRows, Math.min(maxRows, rows));
  return { cols, rows };
}

/**
 * Pack a list of tiles onto a single page via backtracking search.
 * Score is the lexicographic pair (tileCount, cellsFilled) — placing
 * more tiles always wins; within equal tile counts the denser packing
 * wins. Time-capped via opts.maxMs (default 200ms).
 */
function packPage(tiles, cols, rows, opts = {}) {
  const maxMs = opts.maxMs ?? 200;
  // Defense-in-depth: the grid is allocated as rows*cols cells below, so a
  // non-positive / non-integer / runaway dimension would RangeError or OOM.
  // Callers should clamp (the /api/queries handler caps at 32); bound it here
  // too at 48 — past ~48x48 the anytime packer can't finish even one arrangement
  // within its time budget and would return an empty layout.
  cols = Math.max(1, Math.min(Math.trunc(Number(cols)) || 1, 48));
  rows = Math.max(1, Math.min(Math.trunc(Number(rows)) || 1, 48));
  const startMs = Date.now();
  const sized = tiles.map((t) => {
    const w = Math.max(1, Math.min(t.sizeHint?.cols ?? 3, cols));
    const h = Math.max(1, Math.min(t.sizeHint?.rows ?? 2, rows));
    return { tile: t, w, h, area: w * h };
  });

  const grid = Array.from({ length: rows }, () => new Array(cols).fill(-1));
  const placed = [];
  const usedIdx = new Set();
  let best = { placedIdx: [], placements: [], tileCount: -1, cellsFilled: -1 };

  const findEmpty = () => {
    for (let r = 0; r < rows; r++) {
      for (let c = 0; c < cols; c++) {
        if (grid[r][c] === -1) return [r, c];
      }
    }
    return null;
  };
  const fits = (r, c, w, h) => {
    if (r + h > rows || c + w > cols) return false;
    for (let i = 0; i < h; i++) for (let j = 0; j < w; j++) if (grid[r + i][c + j] !== -1) return false;
    return true;
  };
  const fill = (r, c, w, h, v) => {
    for (let i = 0; i < h; i++) for (let j = 0; j < w; j++) grid[r + i][c + j] = v;
  };
  const countFilled = () => {
    let n = 0; for (const row of grid) for (const v of row) if (v >= 0) n++; return n;
  };
  const isBetter = (tileCount, cellsFilled) =>
    tileCount > best.tileCount ||
    (tileCount === best.tileCount && cellsFilled > best.cellsFilled);

  function recurse() {
    if (Date.now() - startMs > maxMs) return;
    const empty = findEmpty();
    const tileCount = placed.length;
    if (!empty) {
      const cellsFilled = countFilled();
      if (isBetter(tileCount, cellsFilled)) {
        best = { placedIdx: [...usedIdx], placements: placed.map((p) => ({ ...p })), tileCount, cellsFilled };
      }
      return;
    }
    const [r, c] = empty;

    let emptyCount = 0;
    for (const row of grid) for (const v of row) if (v === -1) emptyCount++;
    let smallestArea = Infinity;
    for (let i = 0; i < sized.length; i++) {
      if (!usedIdx.has(i)) smallestArea = Math.min(smallestArea, sized[i].area);
    }
    const maxAdditionalTiles = smallestArea === Infinity ? 0 : Math.floor(emptyCount / smallestArea);
    if (tileCount + maxAdditionalTiles < best.tileCount) return;
    if (tileCount + maxAdditionalTiles === best.tileCount &&
        countFilled() + emptyCount <= best.cellsFilled) return;

    const candidates = sized
      .map((s, i) => ({ s, i }))
      .filter(({ i }) => !usedIdx.has(i))
      .sort((a, b) => b.s.area - a.s.area);

    let triedAny = false;
    for (const { s, i } of candidates) {
      if (fits(r, c, s.w, s.h)) {
        triedAny = true;
        fill(r, c, s.w, s.h, i);
        usedIdx.add(i);
        placed.push({ tileIdx: i, row: r, col: c, colSpan: s.w, rowSpan: s.h });
        recurse();
        placed.pop();
        usedIdx.delete(i);
        fill(r, c, s.w, s.h, -1);
        if (Date.now() - startMs > maxMs) return;
      }
    }
    if (!triedAny) {
      grid[r][c] = -2;
      recurse();
      grid[r][c] = -1;
    }
  }

  recurse();

  const placedIdxSet = new Set(best.placedIdx);
  const placedTiles = best.placements.map((p) => ({
    ...tiles[p.tileIdx],
    row: p.row,
    col: p.col,
    colSpan: p.colSpan,
    rowSpan: p.rowSpan,
  }));
  const notPlaced = tiles.filter((_, i) => !placedIdxSet.has(i));
  return {
    placed: placedTiles,
    notPlaced,
    tileCount: best.tileCount,
    cellsFilled: best.cellsFilled,
    score: best.cellsFilled,
  };
}

/**
 * Lay out tiles across one or more pages, packing each page tightly via
 * \`packPage\`. Tiles that don't fit overflow to the next page; an oversize
 * tile is force-placed on its own page.
 */
function layoutMosaic(tiles, pageCols = 12, pageRows = 8, opts = {}) {
  const pages = [];
  let remaining = tiles.slice();
  let safety = 30;
  while (remaining.length > 0 && safety-- > 0) {
    const r = packPage(remaining, pageCols, pageRows, opts);
    if (r.placed.length === 0) {
      const t = remaining.shift();
      pages.push({
        tiles: [{
          ...t, row: 0, col: 0,
          colSpan: Math.min(t.sizeHint?.cols ?? pageCols, pageCols),
          rowSpan: Math.min(t.sizeHint?.rows ?? pageRows, pageRows),
          oversize: true,
        }],
        fillRatio: 1.0,
      });
      continue;
    }
    pages.push({
      tiles: r.placed,
      fillRatio: r.score / (pageCols * pageRows),
    });
    remaining = r.notPlaced;
  }
  return { pages, totalTiles: tiles.length, totalPages: pages.length };
}
// ====== injected from mermaid-lite.mjs ======
/**
 * mermaid-lite — a hand-rolled, dependency-free renderer for a SUBSET of
 * Mermaid, producing SVG strings. No npm package, no vendored lib.
 *
 * Injected into ui.html's <head> via SHARED_BROWSER_SOURCES (scripts/build.mjs),
 * so it runs in the browser's single window scope: every top-level name here is
 * prefixed mm/_mm to avoid collisions (same rule as mosaic-layout.mjs). Keep it
 * PURE (no DOM reads, no Node APIs) except mmRenderInto's innerHTML write.
 *
 * Supported v1:
 *   FLOWCHART  'graph'/'flowchart' + TD|TB|LR|BT|RL; shapes [rect] (rounded)
 *              ((circle)) {diamond} >flag]; edges --> --- -.-> ==> with
 *              optional |label|; chains A --> B --> C; ';' statement separator.
 *   SEQUENCE   'sequenceDiagram'; participant/actor (with 'as'); A->>B: text,
 *              A-->>B: text (dashed); Note over/left of/right of; unknown
 *              lines (activate/loop/alt/…) are skipped gracefully.
 *
 * TOLERANT / STREAMING: mmParse NEVER throws — it goes line-by-line, SKIPS
 * lines it can't understand, and with {streaming:true} treats a trailing
 * line without a newline as not-yet-arrived. Unknown diagram types → null
 * (callers fall back to a plain code block).
 *
 * Exports: mmParse(text, opts) → model|null
 *          mmRender(text, opts) → svg string|null
 *          mmRenderInto(el, text, opts) → boolean
 */

// ── shared helpers ─────────────────────────────────────────────────────────

function mmEsc(s) {
  return String(s == null ? '' : s).replace(/[&<>"']/g, (c) => (
    { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
  ));
}

// Approximate rendered text width at font-size 12 (sans). Good enough for
// box sizing; the SVG never clips because boxes derive from this estimate.
function _mmTextW(s) {
  return Math.max(1, String(s).length) * 7.2;
}

// Strip optional surrounding quotes mermaid allows in labels: A["text"].
function _mmCleanLabel(s) {
  const t = String(s == null ? '' : s).trim();
  const q = t.match(/^"([\\s\\S]*)"$/);
  return q ? q[1] : t;
}

// Unique-per-render suffix for SVG marker ids — two diagrams on one page
// must not share marker defs (the hidden one's marker would win by id).
let _mmIdSeq = 0;

// ── parse ──────────────────────────────────────────────────────────────────

/**
 * Parse mermaid source into a model, or null when the diagram type is
 * unknown/unsupported or nothing usable parsed. Never throws.
 * opts.streaming: a trailing line WITHOUT a newline is "not yet arrived".
 */
function mmParse(text, opts) {
  try {
    return _mmParseInner(text, opts || {});
  } catch {
    return null;   // belt-and-suspenders: tolerant by contract
  }
}

function _mmParseInner(text, opts) {
  const raw = String(text == null ? '' : text);
  let lines = raw.split('\\n');
  if (opts.streaming && !raw.endsWith('\\n')) lines = lines.slice(0, -1);
  const body = [];
  for (const ln of lines) {
    const t = ln.trim();
    if (!t || t.startsWith('%%')) continue;
    body.push(t);
  }
  if (!body.length) return null;
  const head = body[0];
  let m = head.match(/^(?:graph|flowchart)\\b\\s*(TD|TB|LR|BT|RL)?\\s*;?\\s*(.*)$/i);
  if (m) {
    const rest = m[2] ? [m[2]] : [];
    return _mmParseFlow(rest.concat(body.slice(1)), (m[1] || 'TD').toUpperCase());
  }
  if (/^sequenceDiagram\\b/i.test(head)) return _mmParseSeq(body.slice(1));
  return null;   // unknown diagram type (gitGraph, pie, …)
}

function _mmParseFlow(stmtLines, dir) {
  const g = { type: 'flowchart', dir, nodes: [], edges: [], _byId: new Map() };
  for (const line of stmtLines) {
    for (const stmt of line.split(';')) _mmParseFlowStmt(stmt, g);
  }
  delete g._byId;
  if (!g.nodes.length) return null;
  return g;
}

function _mmAddNode(g, n) {
  let cur = g._byId.get(n.id);
  if (!cur) {
    cur = { id: n.id, label: n.label != null ? n.label : n.id, shape: n.shape || 'rect' };
    g._byId.set(n.id, cur);
    g.nodes.push(cur);
  } else if (n.shape) {
    // A later explicit definition refines an earlier bare reference.
    cur.shape = n.shape;
    if (n.label != null) cur.label = n.label;
  }
  return cur;
}

/**
 * One flowchart statement: NODE (EDGE NODE)*. Scans left-to-right; whatever
 * parses cleanly is kept, an unparseable tail is dropped (streaming-friendly:
 * "A --> B --" keeps A, B and the edge).
 */
function _mmParseFlowStmt(stmt, g) {
  const s = stmt.trim();
  if (!s) return;
  // Recognized-but-unsupported keywords: skip the whole statement.
  if (/^(subgraph|end|classDef|class|style|linkStyle|click|direction|accTitle|accDescr)\\b/i.test(s)) return;
  let i = 0;
  const skipWs = () => { while (i < s.length && /\\s/.test(s[i])) i++; };
  const readNode = () => {
    skipWs();
    const idm = /^[A-Za-z0-9_]+/.exec(s.slice(i));
    if (!idm) return null;
    const id = idm[0];
    i += id.length;
    const rest = s.slice(i);
    // Shape openers (order matters: '((' before '(', '>…]' before bare).
    const shapes = [
      ['((', '))', 'circle'],
      ['[', ']', 'rect'],
      ['(', ')', 'rounded'],
      ['{', '}', 'diamond'],
      ['>', ']', 'flag'],
    ];
    for (const [open, close, shape] of shapes) {
      if (!rest.startsWith(open)) continue;
      const end = rest.indexOf(close, open.length);
      if (end === -1) return { id, label: null, shape: null };  // unclosed → bare ref
      return { id, label: _mmCleanLabel(rest.slice(open.length, end)), shape, _adv: end + close.length };
    }
    return { id, label: null, shape: null };
  };
  const readEdge = () => {
    skipWs();
    const em = /^(-\\.->|-->|---|==>)/.exec(s.slice(i));
    if (!em) return null;
    i += em[0].length;
    const style = em[0] === '-->' ? 'arrow' : em[0] === '---' ? 'line' : em[0] === '-.->'
      ? 'dotted' : 'thick';
    skipWs();
    let label = null;
    if (s[i] === '|') {
      const end = s.indexOf('|', i + 1);
      if (end === -1) return null;          // label still streaming in → drop edge
      label = _mmCleanLabel(s.slice(i + 1, end));
      i = end + 1;
    }
    return { style, label };
  };

  let prev = readNode();
  if (!prev) return;
  if (prev._adv != null) { i += prev._adv; delete prev._adv; }
  _mmAddNode(g, prev);
  for (;;) {
    const e = readEdge();
    if (!e) break;
    const next = readNode();
    if (!next) break;                       // dangling edge → keep nodes so far
    if (next._adv != null) { i += next._adv; delete next._adv; }
    _mmAddNode(g, next);
    g.edges.push({ from: prev.id, to: next.id, style: e.style, label: e.label });
    prev = next;
  }
}

function _mmParseSeq(lines) {
  const seq = { type: 'sequence', participants: [], items: [], _byId: new Map() };
  const addP = (id, label, actor) => {
    let p = seq._byId.get(id);
    if (!p) {
      p = { id, label: label != null ? label : id, actor: !!actor };
      seq._byId.set(id, p);
      seq.participants.push(p);
    } else if (label != null) {
      p.label = label;
      if (actor) p.actor = true;
    }
    return p;
  };
  for (const ln of lines) {
    let m = ln.match(/^(participant|actor)\\s+([A-Za-z0-9_]+)(?:\\s+as\\s+(.+))?$/i);
    if (m) { addP(m[2], m[3] ? m[3].trim() : null, /^actor$/i.test(m[1])); continue; }
    m = ln.match(/^([A-Za-z0-9_]+)\\s*(-->>|->>|-->|->|--x|-x)\\s*([A-Za-z0-9_]+)\\s*:\\s*(.*)$/);
    if (m) {
      addP(m[1]); addP(m[3]);
      seq.items.push({ kind: 'msg', from: m[1], to: m[3], text: m[4].trim(), dashed: m[2].startsWith('--') });
      continue;
    }
    m = ln.match(/^[Nn]ote\\s+(over|left of|right of)\\s+([A-Za-z0-9_]+(?:\\s*,\\s*[A-Za-z0-9_]+)?)\\s*:\\s*(.*)$/i);
    if (m) {
      const targets = m[2].split(/\\s*,\\s*/).filter(Boolean);
      for (const t of targets) addP(t);
      seq.items.push({ kind: 'note', pos: m[1].toLowerCase(), targets, text: m[3].trim() });
      continue;
    }
    // activate/deactivate/autonumber/loop/alt/else/opt/end/title/… → skip.
  }
  if (!seq.participants.length) return null;
  delete seq._byId;
  return seq;
}

// ── flowchart layout + render ──────────────────────────────────────────────

function _mmNodeDims(n) {
  const tw = _mmTextW(n.label);
  if (n.shape === 'circle') {
    const d = Math.max(tw + 18, 36);
    return { w: d, h: d };
  }
  if (n.shape === 'diamond') return { w: tw + 36, h: 42 };
  if (n.shape === 'flag') return { w: tw + 30, h: 30 };
  return { w: tw + 22, h: 30 };             // rect / rounded
}

/**
 * Layered layout: longest-path layering from roots, one barycenter ordering
 * pass, then center each layer on a shared cross axis. Returns positioned
 * nodes {x,y,w,h} (top-left) keyed by id, plus total {W,H}.
 */
function _mmLayoutFlow(g) {
  const N = g.nodes.length;
  const layer = new Map(g.nodes.map((n) => [n.id, 0]));
  // Longest-path relaxation, capped at N passes so cycles terminate.
  for (let pass = 0; pass < N; pass++) {
    let changed = false;
    for (const e of g.edges) {
      if (e.from === e.to) continue;
      const want = (layer.get(e.from) ?? 0) + 1;
      if (want <= N && (layer.get(e.to) ?? 0) < want) { layer.set(e.to, want); changed = true; }
    }
    if (!changed) break;
  }
  const maxLayer = Math.max(0, ...layer.values());
  const layers = [];                         // layer index → [node]
  for (let i = 0; i <= maxLayer; i++) layers.push([]);
  for (const n of g.nodes) layers[layer.get(n.id)].push(n);

  // One barycenter pass: order each layer by avg position of predecessors.
  const preds = new Map();
  for (const e of g.edges) {
    if (!preds.has(e.to)) preds.set(e.to, []);
    preds.get(e.to).push(e.from);
  }
  for (let li = 1; li <= maxLayer; li++) {
    const posInPrev = new Map(layers[li - 1].map((n, idx) => [n.id, idx]));
    const score = (n) => {
      const ps = (preds.get(n.id) || []).map((p) => posInPrev.get(p)).filter((v) => v != null);
      return ps.length ? ps.reduce((a, b) => a + b, 0) / ps.length : 1e9;
    };
    const scored = layers[li].map((n, idx) => ({ n, idx, s: score(n) }));
    scored.sort((a, b) => (a.s - b.s) || (a.idx - b.idx));
    layers[li] = scored.map((x) => x.n);
  }

  const horizontal = g.dir === 'LR' || g.dir === 'RL';
  const reversed = g.dir === 'BT' || g.dir === 'RL';
  const MAIN_GAP = 52, CROSS_GAP = 36, PAD = 14;

  // Main-axis extent of each layer band (max node main-dim in the layer).
  const dims = new Map(g.nodes.map((n) => [n.id, _mmNodeDims(n)]));
  const mainOf = (d) => (horizontal ? d.w : d.h);
  const crossOf = (d) => (horizontal ? d.h : d.w);
  const bandMain = layers.map((ns) => Math.max(0, ...ns.map((n) => mainOf(dims.get(n.id)))));
  const bandStart = [];
  let acc = PAD;
  for (let i = 0; i <= maxLayer; i++) { bandStart.push(acc); acc += bandMain[i] + MAIN_GAP; }
  const totalMain = acc - MAIN_GAP + PAD;

  // Cross extents: stack each layer's nodes, then center on the widest layer.
  const layerCross = layers.map((ns) =>
    ns.reduce((sum, n) => sum + crossOf(dims.get(n.id)), 0) + Math.max(0, ns.length - 1) * CROSS_GAP);
  const totalCross = Math.max(0, ...layerCross) + PAD * 2;

  const pos = new Map();
  for (let li = 0; li <= maxLayer; li++) {
    let c = (totalCross - layerCross[li]) / 2;
    for (const n of layers[li]) {
      const d = dims.get(n.id);
      let main = bandStart[li] + (bandMain[li] - mainOf(d)) / 2;
      if (reversed) main = totalMain - main - mainOf(d);
      pos.set(n.id, horizontal
        ? { x: main, y: c, w: d.w, h: d.h }
        : { x: c, y: main, w: d.w, h: d.h });
      c += crossOf(d) + CROSS_GAP;
    }
  }
  return {
    pos,
    W: horizontal ? totalMain : totalCross,
    H: horizontal ? totalCross : totalMain,
  };
}

function _mmShapeSvg(n, p) {
  const cx = p.x + p.w / 2, cy = p.y + p.h / 2;
  const attrs = 'fill="var(--panel, #161410)" stroke="var(--accent, #d97757)" stroke-width="1.3"';
  let shape;
  if (n.shape === 'circle') {
    shape = \`<circle cx="\${cx}" cy="\${cy}" r="\${p.w / 2}" \${attrs}/>\`;
  } else if (n.shape === 'diamond') {
    shape = \`<polygon points="\${cx},\${p.y} \${p.x + p.w},\${cy} \${cx},\${p.y + p.h} \${p.x},\${cy}" \${attrs}/>\`;
  } else if (n.shape === 'flag') {
    const notch = 9;
    shape = \`<polygon points="\${p.x},\${p.y} \${p.x + p.w},\${p.y} \${p.x + p.w},\${p.y + p.h} \${p.x},\${p.y + p.h} \${p.x + notch},\${cy}" \${attrs}/>\`;
  } else {
    const rx = n.shape === 'rounded' ? 14 : 4;
    shape = \`<rect x="\${p.x}" y="\${p.y}" width="\${p.w}" height="\${p.h}" rx="\${rx}" \${attrs}/>\`;
  }
  const text = \`<text x="\${cx}" y="\${cy}" text-anchor="middle" dominant-baseline="central" fill="var(--fg, #f2efe6)">\${mmEsc(n.label)}</text>\`;
  return \`<g class="mm-node" data-mm-node="\${mmEsc(n.id)}">\${shape}\${text}</g>\`;
}

// Border anchor point of node box \`p\` facing toward (tx, ty). Axis-aligned:
// pick the side whose direction dominates.
function _mmAnchor(p, tx, ty) {
  const cx = p.x + p.w / 2, cy = p.y + p.h / 2;
  const dx = tx - cx, dy = ty - cy;
  if (Math.abs(dx) * p.h > Math.abs(dy) * p.w) {
    return { x: dx > 0 ? p.x + p.w : p.x, y: cy };
  }
  return { x: cx, y: dy > 0 ? p.y + p.h : p.y };
}

function _mmRenderFlow(g) {
  const { pos, W, H } = _mmLayoutFlow(g);
  const mid = \`mm\${++_mmIdSeq}\`;
  const parts = [];
  parts.push(\`<defs><marker id="\${mid}-arr" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="var(--accent, #d97757)"/></marker></defs>\`);
  for (const e of g.edges) {
    const a = pos.get(e.from), b = pos.get(e.to);
    if (!a || !b) continue;
    const bcx = b.x + b.w / 2, bcy = b.y + b.h / 2;
    const acx = a.x + a.w / 2, acy = a.y + a.h / 2;
    const p1 = _mmAnchor(a, bcx, bcy);
    const p2 = _mmAnchor(b, acx, acy);
    // Gentle elbow: cubic with control points pushed along the dominant axis.
    const dx = p2.x - p1.x, dy = p2.y - p1.y;
    const d = Math.abs(dy) >= Math.abs(dx)
      ? \`M \${p1.x} \${p1.y} C \${p1.x} \${p1.y + dy / 2}, \${p2.x} \${p2.y - dy / 2}, \${p2.x} \${p2.y}\`
      : \`M \${p1.x} \${p1.y} C \${p1.x + dx / 2} \${p1.y}, \${p2.x - dx / 2} \${p2.y}, \${p2.x} \${p2.y}\`;
    const dash = e.style === 'dotted' ? ' stroke-dasharray="4 3"' : '';
    const width = e.style === 'thick' ? 2.6 : 1.4;
    const marker = e.style === 'line' ? '' : \` marker-end="url(#\${mid}-arr)"\`;
    parts.push(\`<path class="mm-edge" d="\${d}" fill="none" stroke="var(--accent, #d97757)" stroke-width="\${width}"\${dash}\${marker}/>\`);
    if (e.label) {
      const lx = (p1.x + p2.x) / 2, ly = (p1.y + p2.y) / 2;
      const lw = _mmTextW(e.label) + 8;
      parts.push(\`<g class="mm-edge-label"><rect x="\${lx - lw / 2}" y="\${ly - 9}" width="\${lw}" height="18" rx="4" fill="var(--panel-alt, #1b1914)" opacity="0.92"/><text x="\${lx}" y="\${ly}" text-anchor="middle" dominant-baseline="central" fill="var(--muted, #a8a394)" font-size="11">\${mmEsc(e.label)}</text></g>\`);
    }
  }
  for (const n of g.nodes) parts.push(_mmShapeSvg(n, pos.get(n.id)));
  return _mmSvgRoot(W, H, parts.join(''));
}

// ── sequence layout + render ───────────────────────────────────────────────

function _mmRenderSeq(seq) {
  const mid = \`mm\${++_mmIdSeq}\`;
  const PAD = 14, BOX_H = 30, GAP = 46, ROW = 36;
  // Participant box widths + x centers.
  let x = PAD;
  const cx = new Map();
  const boxes = [];
  for (const p of seq.participants) {
    const w = Math.max(_mmTextW(p.label) + 22, 64);
    boxes.push({ p, x, w });
    cx.set(p.id, x + w / 2);
    x += w + GAP;
  }
  const W = x - GAP + PAD;
  let y = PAD + BOX_H + 22;
  const parts = [];
  for (const it of seq.items) {
    if (it.kind === 'msg') {
      const x1 = cx.get(it.from), x2 = cx.get(it.to);
      if (x1 == null || x2 == null) continue;
      const dash = it.dashed ? ' stroke-dasharray="4 3"' : '';
      if (it.from === it.to) {
        // Self message: a small loop to the right.
        const d = \`M \${x1} \${y} C \${x1 + 46} \${y - 4}, \${x1 + 46} \${y + 16}, \${x1 + 2} \${y + 14}\`;
        parts.push(\`<path class="mm-msg" d="\${d}" fill="none" stroke="var(--accent, #d97757)" stroke-width="1.4"\${dash} marker-end="url(#\${mid}-arr)"/>\`);
        parts.push(\`<text x="\${x1 + 52}" y="\${y + 5}" fill="var(--fg, #f2efe6)" font-size="11.5">\${mmEsc(it.text)}</text>\`);
        y += ROW + 12;
      } else {
        parts.push(\`<text x="\${(x1 + x2) / 2}" y="\${y - 6}" text-anchor="middle" fill="var(--fg, #f2efe6)" font-size="11.5">\${mmEsc(it.text)}</text>\`);
        parts.push(\`<line class="mm-msg" x1="\${x1}" y1="\${y}" x2="\${x2}" y2="\${y}" stroke="var(--accent, #d97757)" stroke-width="1.4"\${dash} marker-end="url(#\${mid}-arr)"/>\`);
        y += ROW;
      }
    } else if (it.kind === 'note') {
      const xs = it.targets.map((t) => cx.get(t)).filter((v) => v != null);
      if (!xs.length) continue;
      const w = Math.max(_mmTextW(it.text) + 18, 60);
      let nx;
      if (it.pos === 'left of') nx = Math.min(...xs) - w - 8;
      else if (it.pos === 'right of') nx = Math.max(...xs) + 8;
      else nx = (Math.min(...xs) + Math.max(...xs)) / 2 - w / 2;
      parts.push(\`<g class="mm-note"><rect x="\${nx}" y="\${y - 12}" width="\${w}" height="24" rx="3" fill="var(--panel-alt, #1b1914)" stroke="var(--border, #3a372f)"/><text x="\${nx + w / 2}" y="\${y}" text-anchor="middle" dominant-baseline="central" fill="var(--muted, #a8a394)" font-size="11">\${mmEsc(it.text)}</text></g>\`);
      y += ROW;
    }
  }
  const H = y + PAD;
  const head = [\`<defs><marker id="\${mid}-arr" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="var(--accent, #d97757)"/></marker></defs>\`];
  for (const b of boxes) {
    const c = b.x + b.w / 2;
    head.push(\`<line class="mm-lifeline" x1="\${c}" y1="\${PAD + BOX_H}" x2="\${c}" y2="\${H - PAD}" stroke="var(--border, #3a372f)" stroke-dasharray="4 4"/>\`);
    head.push(\`<g class="mm-participant"><rect x="\${b.x}" y="\${PAD}" width="\${b.w}" height="\${BOX_H}" rx="\${b.p.actor ? 14 : 4}" fill="var(--panel, #161410)" stroke="var(--accent, #d97757)" stroke-width="1.3"/><text x="\${c}" y="\${PAD + BOX_H / 2}" text-anchor="middle" dominant-baseline="central" fill="var(--fg, #f2efe6)">\${mmEsc(b.p.label)}</text></g>\`);
  }
  return _mmSvgRoot(W, H, head.join('') + parts.join(''));
}

function _mmSvgRoot(W, H, inner) {
  const w = Math.max(40, Math.ceil(W)), h = Math.max(30, Math.ceil(H));
  return \`<svg class="mm-svg" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 \${w} \${h}" width="\${w}" height="\${h}" font-family="ui-sans-serif, system-ui, sans-serif" font-size="12" role="img">\${inner}</svg>\`;
}

// ── public render API ──────────────────────────────────────────────────────

/** Render mermaid source → SVG string, or null (unknown type / nothing parsed). */
function mmRender(text, opts) {
  const model = mmParse(text, opts);
  if (!model) return null;
  try {
    if (model.type === 'flowchart') return _mmRenderFlow(model);
    if (model.type === 'sequence') return _mmRenderSeq(model);
  } catch {
    return null;
  }
  return null;
}

/** Render into an element. Returns true on success; leaves el untouched on failure. */
function mmRenderInto(el, text, opts) {
  if (!el) return false;
  const svg = mmRender(text, opts);
  if (!svg) return false;
  el.innerHTML = svg;
  return true;
}

// The agent-facing capability brief — the mermaid PLUGIN injects this into the
// system prompt so the model only emits markup this renderer can draw. It lives
// HERE so the brief can never drift from the parser above: extend the grammar,
// update this in the same commit.
const MM_CAPABILITIES = \`## Diagrams (mermaid)
You may include diagrams in replies as fenced \\\`\\\`\\\`mermaid blocks — the UI renders them live while you stream (a built-in SUBSET renderer, NOT full mermaid).
CAN DO — flowcharts: \\\`graph TD\\\` or \\\`flowchart LR\\\` (directions TD/TB/LR/BT/RL); node shapes A[rect], B(rounded), C((circle)), D{diamond}, E>flag]; edges A --> B, A --- B, A -.-> B, A ==> B with optional labels A -->|label| B; chains A --> B --> C; several statements on one line separated by ';'.
CAN DO — sequence diagrams: \\\`sequenceDiagram\\\`; participant X / actor X (with \\\`as Label\\\`); messages A->>B: text and dashed replies A-->>B: text; Note over A,B: text / Note left of A: / Note right of A:.
CANNOT DO (never emit): subgraphs, style/classDef/linkStyle/click directives, gantt, gitGraph, pie, classDiagram, stateDiagram, erDiagram, mindmap, timeline. Unsupported lines are silently dropped and unknown diagram types fall back to a plain code block — so restructure the idea as a flowchart or sequence diagram instead, or use a table/code block.
Keep diagrams small and labeled: one idea per diagram, short node text.\`;
</script>
</head>
<body>

<!-- Floating build-version badge, top-right. Translucent so it doesn't
     compete with content but stays visible enough to read in a screenshot.
     pointer-events: none so it can never block a click. -->
<!-- version moved into the side-panel chrome (#chromeVersion) -->

<div id="landing">
  <h1>code_boss</h1>
  <p class="tagline">Pick a project folder to work on.</p>

  <button class="btn primary" id="selectFolderBtn" onclick="openFolderPicker()">Select folder</button>

  <h2>Recent projects</h2>
  <ul class="recent-list" id="recentList"></ul>
</div>

<!-- Folder-picker modal. Triggered by Select folder on landing or by Change
     project in settings. Looks like an OS folder picker: title, breadcrumb
     toolbar, scrollable list with folder icons, footer with Select / Cancel. -->
<div class="modal-bg" id="folderModalBg" onclick="if (event.target === this) closeFolderPicker()" style="z-index: 180;">
  <div class="modal" id="folderModal" style="max-width: 640px; width: 92%;">
    <div class="modal-header">
      <h2>Select folder</h2>
      <button class="modal-close" onclick="closeFolderPicker()">&times;</button>
    </div>
    <div class="modal-body" style="padding: 12px 16px;">
      <div class="browser-toolbar">
        <button class="btn" id="upBtn" onclick="browserUp()" title="Go to parent folder">↑ Up</button>
        <div class="breadcrumb" id="breadcrumb"></div>
      </div>
      <ul class="dir-list" id="dirList"></ul>
      <div class="browser-status">
        <span class="muted" id="browserStatus"></span>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn" onclick="closeFolderPicker()">Cancel</button>
      <button class="btn primary" id="openHereBtn" onclick="openCurrentBrowserDir()" disabled>Select</button>
    </div>
  </div>
</div>

<!-- "What's new" after a self-update: check-update saved the release notes for
     every build between the one replaced and the one installed; shown ONCE
     (dismiss tells the server to delete the file). ONE labeled dismiss control
     ("Got it" — no header ×: two buttons made users unsure which one "counted");
     backdrop click matches it, same seen-semantics. -->
<div class="modal-bg" id="whatsNewModalBg" onclick="if (event.target === this) dismissWhatsNew()">
  <div class="modal" style="max-width: 520px;">
    <div class="modal-header">
      <h2><img src="data:image/png;base64," width="14" height="14" style="vertical-align: -2px; margin-right: 6px;" alt=""> Updated to build <span id="wnBuild"></span></h2>
    </div>
    <div class="modal-body" id="whatsNewBody" style="padding: 12px 16px; max-height: 60vh; overflow-y: auto;"></div>
    <div class="modal-footer">
      <button class="btn primary" onclick="dismissWhatsNew()">Got it</button>
    </div>
  </div>
</div>

<!-- Init view: shown while we collect the API key, load the models list,
     and handle any 401 unlock so the user doesn't see a chat they can't
     actually use yet. Hidden after init completes. -->
<div id="initView" class="hidden">
  <h1>code_boss</h1>
  <p class="init-status" id="initStatusLine"><span class="spin" id="initSpin">⠋</span> <span id="initStatus">Initializing…</span></p>
  <div style="display: flex; gap: 8px; margin-top: 12px;">
    <button id="initRetryBtn" class="btn primary hidden" onclick="initialize()">Retry</button>
  </div>
</div>

<div id="chat" class="hidden">
  <style>
    .backlog-panel { border-bottom: 1px solid var(--border); background: var(--panel-alt); font-size: 12px; max-height: 38vh; overflow: auto; }
    .backlog-panel.hidden { display: none; }
    .backlog-head { display: flex; align-items: center; gap: 8px; padding: 6px 10px; position: sticky; top: 0; background: var(--panel-alt); border-bottom: 1px solid var(--border); }
    .backlog-title { font-weight: 600; }
    .backlog-count { color: var(--muted); }
    .backlog-head button { background: none; border: 1px solid var(--border); color: var(--fg); border-radius: 4px; cursor: pointer; padding: 1px 7px; }
    .backlog-head .backlog-new-btn { margin-left: auto; }
    .backlog-form { display: flex; flex-direction: column; gap: 6px; padding: 8px 10px; border-bottom: 1px solid var(--border); }
    .backlog-form.hidden { display: none; }
    .backlog-form input, .backlog-form textarea { background: var(--panel); color: var(--fg); border: 1px solid var(--border); border-radius: 4px; padding: 5px 7px; font: inherit; box-sizing: border-box; width: 100%; }
    .backlog-form-actions { display: flex; gap: 6px; justify-content: flex-end; }
    .backlog-row { display: flex; align-items: center; gap: 8px; padding: 5px 10px; border-bottom: 1px solid var(--border); }
    .backlog-row .asg-pill { font-size: 10px; text-transform: uppercase; padding: 1px 6px; border-radius: 8px; background: var(--border); color: var(--fg); }
    .backlog-row .asg-pill.ready { background: #2d4a2d; }
    .backlog-row .asg-pill.active { background: #43431e; }
    .backlog-row .asg-pill.accepted { background: #1e3a3a; }
    .backlog-row .asg-pill.capturing { background: #3a2d1e; }
    .backlog-row .asg-title { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .backlog-row button { background: none; border: 1px solid var(--border); color: var(--fg); border-radius: 4px; cursor: pointer; padding: 1px 6px; }
    .backlog-row .asg-work { background: #2d4a2d; }
    .backlog-empty { padding: 8px 10px; color: var(--muted); font-size: 12px; }
    .backlog-head .backlog-mem-btn { margin-left: auto; }
    .memory-entry { display: flex; gap: 6px; align-items: flex-start; padding: 6px 0; border-bottom: 1px solid var(--border); }
    .memory-entry select { background: var(--panel); color: var(--fg); border: 1px solid var(--border); border-radius: 4px; padding: 3px; font: inherit; flex: 0 0 130px; }
    .memory-entry textarea { flex: 1; background: var(--panel); color: var(--fg); border: 1px solid var(--border); border-radius: 4px; padding: 4px 6px; font: inherit; resize: vertical; }
    .memory-entry .mem-del { background: none; border: 1px solid var(--border); color: var(--fg); border-radius: 4px; cursor: pointer; padding: 2px 7px; }
    .memory-empty { color: var(--muted); font-size: 12px; padding: 8px 0; }
    .intake-status { flex: 1; color: var(--muted); font-size: 11px; align-self: center; }
    .bubble.intake-manager { border-left: 3px solid #6a8db0; }
    .bubble.intake-manager::before { content: 'Intake manager'; display: block; font-size: 10px; text-transform: uppercase; color: var(--muted); margin-bottom: 3px; }
    .row.requirement-card { border-left: 3px solid #6a8db0; padding: 8px 10px; margin: 6px 0; background: var(--panel-alt); }
    .row.requirement-card .rc-title { font-weight: 600; margin-bottom: 6px; }
    .row.requirement-card label { display: block; font-size: 11px; text-transform: uppercase; color: var(--muted); margin: 6px 0 2px; }
    .row.requirement-card input, .row.requirement-card textarea { width: 100%; box-sizing: border-box; background: var(--panel); color: var(--fg); border: 1px solid var(--border); border-radius: 4px; padding: 5px 7px; font: inherit; }
    .row.requirement-card .rc-actions { display: flex; gap: 6px; margin-top: 8px; }
    .row.requirement-card.captured { opacity: 0.7; }
    .row.assignment-banner { color: var(--muted); font-size: 12px; border-top: 1px solid var(--border); padding: 6px 10px; margin-top: 8px; }
    .row.memory-consolidated { border-left: 3px solid #6a8db0; padding: 6px 10px; margin: 6px 0; font-size: 12px; }
    .row.memory-consolidated .mc-head { color: var(--fg); margin-bottom: 3px; }
    .row.memory-consolidated .mc-line { color: var(--muted); }
    .row.manager-diagnostic { border-left: 3px solid #c9a227; padding: 6px 10px; margin: 6px 0; font-size: 12px; }
    .row.manager-diagnostic summary { color: #c9a227; cursor: pointer; }
    .row.manager-diagnostic .md-raw { white-space: pre-wrap; word-break: break-word; margin: 6px 0 0; padding: 6px 8px; background: var(--panel); border: 1px solid var(--border); border-radius: 4px; max-height: 280px; overflow: auto; color: var(--fg); }
    .row.assignment-gate { border-left: 3px solid var(--muted); padding: 8px 10px; margin: 6px 0; }
    .row.assignment-gate.ok { border-left-color: #4a9d4a; }
    .row.assignment-gate.warn { border-left-color: #c9a227; }
    .row.assignment-gate .gate-title { font-weight: 600; margin-bottom: 4px; }
    .row.assignment-gate .gate-line { margin: 2px 0; }
    .row.assignment-gate .gate-line.missing { color: #c9a227; }
    .row.assignment-gate .gate-line.drift { color: var(--muted); }
    .row.assignment-gate .gate-actions { display: flex; gap: 6px; margin-top: 6px; }
    /* ── Sub-agents (orchestrator delegation) ── */
    .sa-list { display: flex; flex-direction: column; gap: 6px; padding: 6px; }
    .sa-empty { color: var(--muted); font-size: 12px; padding: 10px 8px; line-height: 1.4; }
    .sa-item { display: flex; align-items: center; gap: 8px; padding: 7px 9px; border: 1px solid var(--border); border-radius: 6px; cursor: pointer; background: var(--panel); }
    .sa-item:hover { border-color: var(--accent); }
    .sa-item .sa-dot { width: 8px; height: 8px; border-radius: 50%; flex: 0 0 auto; background: var(--muted); }
    .sa-item.working .sa-dot, .sa-item.reviewing .sa-dot { background: var(--accent); animation: sa-pulse 1.1s ease-in-out infinite; }
    .sa-item.awaiting-review .sa-dot { background: #c9a227; }
    .sa-item.done .sa-dot { background: #4a9d4a; }
    .sa-item.error .sa-dot, .sa-item.aborted .sa-dot, .sa-item.rejected .sa-dot { background: #c0524a; }
    .sa-item.conflict .sa-dot, .sa-item.unmergeable .sa-dot { background: #c9a227; }
    .sa-item.recovered .sa-dot { background: #c9a227; }
    .cog-budget { padding: 6px 8px; }
    .cog-budget:empty { display: none; }
    .cog-bar { height: 6px; border-radius: 3px; background: var(--border); overflow: hidden; }
    .cog-bar-fill { height: 100%; background: var(--accent); transition: width .2s; }
    .cog-bar-label { font-size: 10px; color: var(--muted); margin-top: 3px; }
    .cog-tree { font-family: var(--mono, monospace); font-size: 11px; line-height: 1.7; overflow-y: auto; }
    .cog-row { white-space: nowrap; color: var(--fg); }
    .cog-row.cog-node { color: var(--accent); }
    .cog-row.cog-decompose, .cog-row.cog-explore { color: #c9a227; }
    .cog-row.cog-stopped { color: #c0524a; }
    .cog-row.cog-verify { color: var(--muted); }
    .cog-row.cog-done { color: #4a9e6a; }
    .sa-item .sa-act-btn { flex: 0 0 auto; font-size: 10px; padding: 2px 7px; border: 1px solid var(--border); border-radius: 4px; background: var(--panel); color: var(--fg); cursor: pointer; }
    .sa-item .sa-act-btn:hover { border-color: var(--accent); }
    .sa-item .sa-act-btn.danger:hover { border-color: #c0524a; color: #c0524a; }
    /* acceptance verdict / feedback rows in the main chat */
    .row.acceptance-verdict { border-left: 3px solid #4a9d4a; padding: 7px 10px; margin: 6px 0; cursor: pointer; }
    .row.acceptance-verdict.reject { border-left-color: #c9a227; }
    .row.acceptance-verdict .av-head { font-weight: 600; margin-bottom: 2px; }
    .row.acceptance-verdict .av-body { color: var(--muted); font-size: 12px; }
    .sa-act.sa-act-verdict { border-left: 2px solid #4a9d4a; padding-left: 8px; }
    .sa-act.sa-act-verdict.reject { border-left-color: #c9a227; }
    .sa-act.sa-act-feedback { border-left: 2px solid #c9a227; padding-left: 8px; color: var(--fg); }
    .sa-item .sa-topic { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: 13px; }
    .sa-item .sa-state { font-size: 10px; text-transform: uppercase; letter-spacing: .04em; color: var(--muted); flex: 0 0 auto; }
    @keyframes sa-pulse { 0%,100% { opacity: 1; } 50% { opacity: .35; } }
    /* collapsed-panel fallback list under the input */
    .sa-fallback { display: none; border-top: 1px solid var(--border); max-height: 32vh; overflow-y: auto; background: var(--panel); }
    .sa-fallback.show { display: block; }
    .sa-fallback .sa-fallback-head { font-size: 10px; text-transform: uppercase; letter-spacing: .05em; color: var(--muted); padding: 7px 12px 2px; }
    /* right slide-out drawer: one sub-agent's live activity, covers most of the screen */
    .sa-drawer-bg { position: fixed; inset: 0; background: rgba(0,0,0,.45); z-index: 120; display: none; justify-content: flex-end; }
    .sa-drawer-bg.show { display: flex; }
    .sa-drawer { width: 78vw; max-width: 1100px; height: 100%; background: var(--panel); border-left: 1px solid var(--border); display: flex; flex-direction: column; box-shadow: -8px 0 24px rgba(0,0,0,.35); transform: translateX(100%); transition: transform .18s ease; }
    .sa-drawer-bg.show .sa-drawer { transform: translateX(0); }
    .sa-drawer-head { display: flex; align-items: center; gap: 10px; padding: 12px 16px; border-bottom: 1px solid var(--border); }
    .sa-drawer-head .sa-drawer-title { flex: 1; font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .sa-drawer-head .sa-drawer-state { font-size: 11px; text-transform: uppercase; color: var(--muted); }
    .sa-drawer-head button { background: none; border: none; color: var(--fg); font-size: 22px; cursor: pointer; line-height: 1; padding: 0 4px; }
    .sa-drawer-body { flex: 1; overflow-y: auto; padding: 12px 16px; }
    .sa-drawer-result { border: 1px solid var(--border); border-radius: 6px; padding: 10px 12px; margin-bottom: 12px; background: var(--bg, var(--panel)); }
    .sa-act { font-family: ui-monospace, monospace; font-size: 12px; white-space: pre-wrap; word-break: break-word; padding: 4px 0; border-bottom: 1px solid var(--border); color: var(--fg); }
    .sa-act .sa-act-verb { color: var(--accent); font-weight: 600; }
    .sa-act-empty { color: var(--muted); font-size: 12px; padding: 8px 0; }
    /* inline completion card in the main chat */
    .row.subagent-card { border-left: 3px solid var(--accent); padding: 8px 10px; margin: 6px 0; }
    .row.subagent-card.done { border-left-color: #4a9d4a; }
    .row.subagent-card.error, .row.subagent-card.aborted { border-left-color: #c0524a; }
    .row.subagent-card.conflict { border-left-color: #c9a227; }
    .row.subagent-card .sa-card-head { font-weight: 600; margin-bottom: 3px; }
    .row.subagent-card .sa-card-preview { color: var(--muted); font-size: 12px; }
  </style>
  <aside id="sidePanel">
    <div class="side-tabs" role="tablist">
      <!-- Order (left→right): Checklists, Assignments, Memory, Artifacts, Sub-agents. Cognition/Queue hidden. -->
      <button class="side-tab active" id="tabbtn-checklists" onclick="selectSideTab('checklists')" title="Checklists">Checklists<span class="side-tab-count" id="checklistCount"></span></button>
      <button class="side-tab" id="tabbtn-assignments" onclick="selectSideTab('assignments')" title="Assignments">Assignments<span class="side-tab-count" id="backlogCount"></span></button>
      <button class="side-tab" id="tabbtn-memory" onclick="selectSideTab('memory')" title="Memory">Memory</button>
      <button class="side-tab" id="tabbtn-artifacts" onclick="selectSideTab('artifacts')" title="Artifacts">Artifacts<span class="side-tab-count" id="artifactCount"></span></button>
      <button class="side-tab" id="tabbtn-subagents" onclick="selectSideTab('subagents')" style="display:none;" title="Sub-agents">Sub-agents<span class="side-tab-count" id="subagentCount"></span></button>
      <button class="side-tab" id="tabbtn-cognition" onclick="selectSideTab('cognition')" style="display:none;" title="Cognition">Cognition<span class="side-tab-count" id="cognitionCount"></span></button>
      <!-- Queue tab retired (#9): busy messages are delegated async by the orchestrator, not queued. -->
      <button class="side-tab" id="tabbtn-queue" onclick="selectSideTab('queue')" style="display:none;" title="Queue">Queue<span class="side-tab-count" id="queueCount"></span></button>
      <button id="sideToggle" class="side-collapse" onclick="toggleSidePanel()" title="Collapse the side panel">«</button>
    </div>
    <div class="side-panes">
      <!-- CHECKLISTS — scroll-synced: shows the checklist of the task in the viewport middle. -->
      <div class="side-pane" id="pane-checklists">
        <div id="checklistTab" class="checklist-tab"></div>
      </div>
      <!-- ASSIGNMENTS -->
      <div class="side-pane hidden" id="pane-assignments">
        <div class="pane-actions">
          <button class="pane-add" onclick="openIntakeModal()" title="New assignment — describe it and the intake manager drafts the requirement (you can edit it)">+</button>
        </div>
        <div class="backlog-form hidden" id="backlogForm">
          <input id="asgTitle" placeholder="Assignment title" />
          <textarea id="asgReq" placeholder="Requirement — the definition of done (leave blank to capture later)…" rows="2"></textarea>
          <div class="backlog-form-actions">
            <button class="btn" onclick="toggleAssignmentForm()">Cancel</button>
            <button class="btn primary" onclick="createAssignmentFromForm()">Capture</button>
          </div>
        </div>
        <div class="backlog-form hidden" id="intakeForm">
          <textarea id="intakeText" placeholder="Paste the email / ticket / spec / notes. The intake manager will ask questions, read the code read-only, and draft a requirement card you can edit and capture…" rows="3"></textarea>
          <div class="backlog-form-actions">
            <span id="intakeStatus" class="intake-status"></span>
            <button class="btn" onclick="closeIntake()">Cancel</button>
            <button class="btn primary" id="intakeSendBtn" onclick="sendIntake()">Send</button>
          </div>
        </div>
        <div class="backlog-list" id="backlogList"></div>
      </div>
      <!-- QUEUE -->
      <div class="side-pane hidden" id="pane-queue">
        <div class="queue-stack" id="queueStack"></div>
        <div class="pane-empty" id="queueEmpty">No queued messages.<br>In the chat box, press <b>⇧←</b> to queue your current draft. Click a queued item and press <b>⇧→</b> to pull it back into the box.</div>
      </div>
      <!-- MEMORY -->
      <div class="side-pane hidden" id="pane-memory">
        <div class="mem-saved" id="memSaved">✓ saved</div>
        <div class="pane-hint">Durable facts about this codebase, grouped by type. One fact per line — edits autosave.</div>
        <div id="memoryList"></div>
      </div>
      <!-- ARTIFACTS — diagrams rendered in this conversation, newest edit first.
           Same title = same artifact: a re-emitted diagram REPLACES its entry. -->
      <div class="side-pane hidden" id="pane-artifacts">
        <div id="artifactList"></div>
        <div class="pane-empty" id="artifactEmpty">No diagrams yet — ask for one.</div>
      </div>
      <!-- SUB-AGENTS -->
      <div class="side-pane hidden" id="pane-subagents">
        <div class="sa-list" id="saList"></div>
      </div>
      <div class="side-pane hidden" id="pane-cognition">
        <div class="cog-budget" id="cogBudget"></div>
        <div class="cog-tree" id="cogTree"><div class="sa-empty">When a message runs in cognition mode, its thinking tree streams here — how it sized the problem, which strategies engaged, and how the budget was spent.</div></div>
      </div>
    </div>
    <div class="side-chrome" id="sideChrome">
      <span class="proj-path" id="projectPath" title="Click to switch project" onclick="changeProject()">(no project)</span>
      <span class="chrome-version" id="chromeVersion"></span>
      <span class="chrome-break"></span>
      <span class="wl-chip" id="wlIndicator" title="" style="display:none;">Weblogic</span>
      <span class="edit-toggle" id="editToggle" onclick="toggleEditAllowed()" title="Edit — the agent edits files and runs commands. Click to switch to read-only (discuss): it investigates and explains but won't modify files or run shell commands.">Edit</span>
      <input type="checkbox" id="discussToggle" style="display:none;">
      <span class="edit-toggle" id="reviewLabel" onclick="toggleReview()" title="Auto-approve ON — changes apply directly. Click to turn it OFF: the agent proposes a detailed plan + diffs first and waits for your approval.">Auto-approve</span>
      <input type="checkbox" id="reviewToggle" style="display:none;">
      <span class="chip-group"><span class="edit-toggle off" id="criticLabel" onclick="toggleCritic()" title="Reviewer (OFF). When ON, a parallel reviewer with READ-ONLY tools investigates each agent step and only flags a problem it can PROVE with evidence from the code; the agent gets ONE chance to fix it (a reworked message isn't re-reviewed). Click to toggle.">Reviewer</span>
      <input type="checkbox" id="criticToggle" style="display:none;">
      <span class="reviewer-eye off" id="reviewerEye" onclick="toggleReviewerEye()" title="Show the reviewer's messages in the chat (OFF = hidden). The reviewer can run silently; flip this on to watch what it flags.">
        <svg viewBox="0 0 16 16" width="13" height="13" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" aria-hidden="true"><path d="M1 8s2.5-4.5 7-4.5 7 4.5 7 4.5-2.5 4.5-7 4.5S1 8 1 8z"/><circle cx="8" cy="8" r="1.7"/><line class="eye-slash" x1="2.5" y1="13.5" x2="13.5" y2="2.5"/></svg>
        <span class="eye-badge" id="reviewerEyeBadge" style="display:none;"></span>
      </span></span>
      <span class="chip-group"><span class="edit-toggle off" id="deepThinkLabel" onclick="toggleDeepThink()" title="Deep think (OFF). When ON, the agent may pause on a genuine design decision mid-run: three independent perspectives + a judge examine the design question against the repo, and the agent gets the verdict as a tool result. Availability is limited (a few uses per run, with a cooldown). Click to toggle.">Deep think</span>
      <input type="checkbox" id="deepThinkToggle" style="display:none;">
      <span class="reviewer-eye off" id="deepThinkEye" onclick="toggleDeepThinkEye()" title="Show deep-think markers in the chat (OFF = hidden). Deep think can run silently; flip this on to watch the questions and verdicts.">
        <svg viewBox="0 0 16 16" width="13" height="13" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" aria-hidden="true"><path d="M1 8s2.5-4.5 7-4.5 7 4.5 7 4.5-2.5 4.5-7 4.5S1 8 1 8z"/><circle cx="8" cy="8" r="1.7"/><line class="eye-slash" x1="2.5" y1="13.5" x2="13.5" y2="2.5"/></svg>
        <span class="eye-badge" id="deepThinkEyeBadge" style="display:none;"></span>
      </span></span>
      <span class="edit-toggle" id="approveImplementBtn" onclick="approveImplement()" style="display:none;" title="Approve the proposal above and implement it — this one run applies the changes.">✓ approve &amp; implement</span>
      <!-- The footer "uncommitted changes" ⚠ indicator was retired: the direct/subbranch git row at
           the bottom of the chat now owns git status (a direct-mode warning, or the merge row). -->
      <span class="edit-toggle" id="undoRunBtn" onclick="undoLastRun()" style="display:none;" title="Undo the last run's file changes (this folder isn't a git project, so code_boss kept an internal snapshot).">⤺ undo last run</span>
      <button class="settings-btn" id="settingsBtn" onclick="openSettings()" title="Settings — model, API key, GitLab, timeout, Snapshot, Log, Clear conversation, Probe">⚙</button>
    </div>
  </aside>
  <div id="chatMain">
  <button id="sideReopen" onclick="toggleSidePanel()" title="Show the side panel">»</button>
  <main id="logScroll">
    <div class="log" id="log">
      <div class="bubble system">Type a question and press Enter (Shift+Enter for newline).</div>
    </div>
  </main>

  <footer>
    <!-- Jump-to-latest: floats above the input when new content arrives while you're scrolled up
         (not locked to the bottom). Click → scroll down + re-lock. -->
    <button id="scrollBottomBtn" class="scroll-bottom-btn" onclick="jumpToBottom()" title="Jump to latest">↓</button>
    <!-- The merge prompt (parent ← merge · N commits) is appended to the chat log as a UI-only row
         (see refreshMergeBar), so it scrolls with history — NOT pinned here. -->
    <!-- The pending-message queue now lives in the side panel's Queue tab. -->
    <!-- Live checklist: the agent's <checklist> for multi-step work, ticking off in place. -->
    <div id="checklistPanel" class="checklist-panel" style="display:none;"></div>
    <!-- Attached-image preview chips (paste or drag-drop onto the chat). -->
    <div class="img-chips" id="imgChips" style="display:none;"></div>
    <div class="input-row">
      <textarea id="input" placeholder="Ask…" rows="1"></textarea>
      <button id="steerBtn"
              class="queue-btn steer-btn"
              title="Send now — steers the running agent at its next step (no restart, no lost work)"
              onclick="steerCurrentInput()"
              style="display: none;">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none"
             stroke="currentColor" stroke-width="1.3"
             stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M2 7 L11 7 M7.5 3.5 L11 7 L7.5 10.5"/>
        </svg>
      </button>
    </div>
  </footer>
  <!-- Sub-agents fallback: shown under the input when the left panel is collapsed. -->
  <div class="sa-fallback" id="saFallback"></div>
  </div>
</div>

<!-- Settings modal: model, project, timeout, tokens display + action buttons. -->
<div class="modal-bg" id="settingsModalBg" onclick="if (event.target === this) closeSettings()">
  <div class="modal" id="settingsModal" style="max-width: 540px;">
    <div class="modal-header">
      <h2>Settings</h2>
      <button class="modal-close" onclick="closeSettings()">&times;</button>
    </div>
    <div class="modal-body">
      <div class="settings-row">
        <label for="model">Model</label>
        <select id="model" title="Model"></select>
      </div>
      <div class="settings-row">
        <label>Project</label>
        <span class="value" id="settingsProject">(none)</span>
      </div>
      <div class="settings-row">
        <label>API key</label>
        <span class="value" id="settingsApiKey">(not set)</span>
        <button class="btn" onclick="changeApiKey()">Change…</button>
      </div>
      <div class="settings-row">
        <label>GitLab</label>
        <span class="value" id="settingsGitLab">(not set)</span>
        <button class="btn" onclick="changeGitLabCreds()">Change…</button>
      </div>
      <div class="settings-row">
        <label>Timeout</label>
        <span class="value" id="settingsTimeout">—</span>
        <button class="btn" onclick="changeTimeout()">Change…</button>
      </div>
      <div class="settings-row">
        <label>Tokens this session</label>
        <span class="value" id="settingsTokens">0</span>
      </div>
      <div class="settings-row">
        <label>Thinking</label>
        <span class="value" style="font-size: 11px; color: var(--muted);">Probe which reasoning/effort knobs this endpoint honors</span>
        <button class="btn" id="thinkProbeBtn" onclick="probeThinking()">Probe</button>
      </div>
      <pre id="thinkProbeOut" class="think-probe-out" style="display: none;"></pre>
      <div class="settings-row">
        <label>Images</label>
        <span class="value" style="font-size: 11px; color: var(--muted);" id="imgSupportLabel">Discover which image format this endpoint accepts</span>
        <button class="btn" id="imgProbeBtn" onclick="probeImages()">Test image support</button>
      </div>
      <pre id="imgProbeOut" class="think-probe-out" style="display: none;"></pre>
      <div class="settings-actions">
        <button class="btn" onclick="openSnapshot()">Snapshot</button>
        <button class="btn" onclick="openLog()">Log</button>
        <button class="btn" onclick="changeProject()">Change project…</button>
        <button class="btn" onclick="clearConversation()">Clear conversation</button>
      </div>
    </div>
  </div>
</div>

<!-- Codebase memory: the agent-maintained Tier-1 facts about this codebase.
     The developer can edit/prune them here; the agent reads them every run and
     maintains them with <remember>/<forget>. -->
<!-- Codebase memory now lives in the side panel's Memory tab (no modal). -->

<!-- New-assignment intake: fields (left) + a read-only mini-chat manager (right). -->
<div class="modal-bg" id="intakeModalBg" onclick="if (event.target === this) closeIntakeModal()">
  <div class="modal intake-modal">
    <div class="modal-header">
      <h2>New assignment</h2>
      <button class="modal-close" onclick="closeIntakeModal()">&times;</button>
    </div>
    <div class="intake-two-pane">
      <div class="intake-fields">
        <label for="im-title">Title</label>
        <input id="im-title" placeholder="Short title" />
        <label for="im-req">Requirement <span class="im-sub">— the definition of done</span></label>
        <textarea id="im-req" rows="6" placeholder="What does done look like?"></textarea>
        <label for="im-acc">Acceptance criteria <span class="im-sub">— optional checklist</span></label>
        <textarea id="im-acc" rows="4" placeholder="What the acceptance check should verify"></textarea>
      </div>
      <div class="intake-chat">
        <div class="im-log" id="im-log"></div>
        <div class="im-footer">
          <div class="input-row im-input-row">
            <textarea id="im-input" rows="1" placeholder="Describe it or paste a ticket/email…"></textarea>
          </div>
        </div>
      </div>
    </div>
    <div class="modal-footer">
      <span class="im-hint">The fields are yours to edit; the manager reads the code (read-only) and refines them as you chat. No worker runs here.</span>
      <span style="flex:1"></span>
      <button class="btn" onclick="closeIntakeModal()">Cancel</button>
      <button class="btn primary" onclick="addFromIntake()">Add assignment</button>
    </div>
  </div>
</div>

<!-- Sub-agent detail: a right slide-out covering most of the screen with one sub-agent's live activity. -->
<div class="sa-drawer-bg" id="saDrawerBg" onclick="if (event.target === this) closeSubagentDetail()">
  <div class="sa-drawer">
    <div class="sa-drawer-head">
      <span class="sa-dot" id="saDrawerDot"></span>
      <span class="sa-drawer-title" id="saDrawerTitle">Sub-agent</span>
      <span class="sa-drawer-state" id="saDrawerState"></span>
      <button onclick="closeSubagentDetail()" title="Close">&times;</button>
    </div>
    <div class="sa-drawer-body" id="saDrawerBody"></div>
  </div>
</div>

<!-- Init modal: shown for set-api-key and unlock flows. Cannot be dismissed
     (no × close button, no backdrop-click handler) because there's nowhere
     to go from these states — the user must complete the action or close
     the tab. A ⚙ in the header opens the snapshot OVER this modal so the
     user can still capture diagnostics if init freezes. -->
<div class="modal-bg" id="initModalBg" style="z-index: 150;">
  <div class="modal" id="initModal">
    <div class="modal-header">
      <h2 id="initModalTitle">Setup</h2>
    </div>
    <div class="modal-body" id="initModalBody"></div>
  </div>
</div>

<!-- Commit working tree (DIRECT git mode): commits the user's own branch with an AI-seeded,
     editable message. After committing, code_boss forks a CODE_BOSS sub-branch. -->
<div class="modal-bg" id="commitModalBg" onclick="if (event.target === this) closeCommitModal()" style="z-index: 175;">
  <div class="modal" style="max-width: 560px;">
    <div class="modal-header">
      <h2>Commit changes</h2>
      <button class="modal-close" onclick="closeCommitModal()">&times;</button>
    </div>
    <div class="modal-body">
      <p class="dim" style="margin: 0 0 8px; font-size: 12px;">Commits all current changes on your branch. Afterward code_boss starts an isolated sub-branch. Edit the AI-drafted message:</p>
      <textarea id="commitMsgInput" rows="5" spellcheck="false" style="width: 100%; box-sizing: border-box; font-family: var(--mono); font-size: 12px; padding: 8px; resize: vertical;"></textarea>
      <p id="commitErr" style="color: #d9534f; margin: 8px 0 0; font-size: 12px; min-height: 1em;"></p>
    </div>
    <div class="modal-footer">
      <button class="btn" onclick="closeCommitModal()">Cancel</button>
      <button class="btn primary" id="commitDoBtn" onclick="submitCommit()">Commit</button>
    </div>
  </div>
</div>

<!-- Snapshot/diagnostic modal — stacks over EVERYTHING when open. -->
<div class="modal-bg" id="modalBg" onclick="if (event.target === this) closeModal()" style="z-index: 200;">
  <div class="modal" id="modal">
    <div class="modal-header">
      <h2 id="modalTitle">Snapshot</h2>
      <button class="modal-close" onclick="closeModal()">&times;</button>
    </div>
    <div class="modal-body" id="modalBody"></div>
  </div>
</div>

<!-- Single snapshot affordance. dockSnapFab() positions it: viewport bottom-right
     on the landing/init screens, or the bottom-right of whatever modal is open. -->
<div id="snapFab" class="snap-fab hidden" role="button" tabindex="0"
     title="Snapshot (diagnostics) — F1" aria-label="Snapshot" onclick="openSnapshot()">
  <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor"
       stroke-width="1.2" aria-hidden="true">
    <rect x="1" y="1" width="5" height="5" rx="1"/><rect x="8" y="1" width="5" height="5" rx="1"/>
    <rect x="1" y="8" width="5" height="5" rx="1"/><rect x="8" y="8" width="5" height="5" rx="1"/>
  </svg>
</div>

<!-- Fullscreen mermaid-diagram viewer: opened by clicking an .mm-thumb card.
     Click-drag pans, wheel scrolls (Shift+wheel = horizontal), "Code" swaps
     to the raw mermaid source, ✕ / Esc closes. -->
<div id="mmViewer">
  <div class="mm-viewer-tools">
    <button id="mmViewerCopyBtn" class="mm-viewer-btn" onclick="mmViewerCopy()" title="Copy the mermaid source">Copy</button>
    <button id="mmViewerSaveBtn" class="mm-viewer-btn" onclick="mmViewerSave()" title="Save the diagram as a PNG">Save</button>
    <button id="mmViewerCodeBtn" class="mm-viewer-btn" onclick="mmViewerToggleCode()" title="Show mermaid source">Code</button>
    <button class="mm-viewer-btn" onclick="mmCloseViewer()" title="Close (Esc)">&#10005;</button>
  </div>
  <div id="mmViewerCanvas"><div id="mmViewerPan"></div></div>
  <pre id="mmViewerSrc"></pre>
</div>

<script>
  const $landing = document.getElementById('landing');
  const $chat = document.getElementById('chat');
  const $recentList = document.getElementById('recentList');
  const $upBtn = document.getElementById('upBtn');
  const $breadcrumb = document.getElementById('breadcrumb');
  const $dirList = document.getElementById('dirList');
  const $browserStatus = document.getElementById('browserStatus');
  const $openHereBtn = document.getElementById('openHereBtn');
  const $projectPath = document.getElementById('projectPath');
  const _logEl = document.getElementById('log');
  const _logScrollEl = document.getElementById('logScroll');
  // Render context: every chat-log row is built into RC.log and scrolled via
  // RC.scroll. Defaults to the MAIN chat; renderInto() swaps it SYNCHRONOUSLY so
  // the SAME renderEvent + row builders render into the intake modal's #im-log.
  // isIntake gates main-chat-only chrome (the status spinner / context-fill bar).
  // The ambient render context. \`renderInto\` swaps RC while rendering into a
  // given chat; the shared render functions call RC.afterRow (pin the active
  // chat's status row to the bottom) and RC.clearStatus (drop its row ref on
  // clearLog). Those hooks are installed per-chat by createChatComponent.
  let RC = { log: _logEl, scroll: _logScrollEl, isIntake: false, afterRow: null, clearStatus: null };
  function renderInto(rc, fn) { const prev = RC; RC = rc; try { return fn(); } finally { RC = prev; } }
  const $input = document.getElementById('input');
  const $model = document.getElementById('model');
  const $settingsModalBg = document.getElementById('settingsModalBg');
  const $settingsProject = document.getElementById('settingsProject');
  const $settingsTimeout = document.getElementById('settingsTimeout');
  const $settingsTokens = document.getElementById('settingsTokens');
  // Init modal — set-api-key + unlock. Separate from snapshot $modalBg so
  // snapshot can stack OVER an open init modal (z-index 200 vs 150) and
  // both stay alive across opens.
  const $initModalBg    = document.getElementById('initModalBg');
  const $initModalTitle = document.getElementById('initModalTitle');
  const $initModalBody  = document.getElementById('initModalBody');
  const $modalBg = document.getElementById('modalBg');
  const $modal = document.getElementById('modal');
  const $modalTitle = document.getElementById('modalTitle');
  const $modalBody = document.getElementById('modalBody');

  const SERVER = window.SERVER_CONFIG || {};
  // Reviewer/Deep-think eye badge counters. Declared HERE (not next to their
  // bump/toggle functions far below) because the boot-restore block just under
  // this calls toggleReviewerEye/toggleDeepThinkEye at script-eval time — a
  // later \`let\` would still be in its temporal dead zone and the assignment
  // would throw, silently killing the rest of the chip restore.
  let _hiddenReviewerNotes = 0;    // findings that landed while the eye was hidden (badge count)
  let _hiddenDeepThinkNotes = 0;   // verdicts that landed while the eye was hidden (badge count)
  // Populate the floating version badge once the DOM is ready. SERVER.
  // buildVersion is injected at bundle-build time; if absent (dev/test
  // server didn't pass it), the badge stays empty (CSS hides it).
  {
    const v = SERVER.buildVersion;
    const $vb = document.getElementById('chromeVersion');
    if ($vb) $vb.textContent = v ? ('v' + v) : '';
    syncEditToggle();   // initialise the edit-allowed label
    try { if (localStorage.getItem('cb-side-collapsed') === '1') setSidePanelCollapsed(true); else updateChecklistViews(); } catch {}
    // Restore the Reviewer chip + eye across reloads (a mid-session reload used to silently drop
    // the reviewer from the next message). Reuses the toggles so classes/titles/storage stay coherent.
    try {
      if (localStorage.getItem('cb-reviewer-on') === '1') toggleCritic();
      if (localStorage.getItem('cb-reviewer-eye') === '1') toggleReviewerEye();
      if (localStorage.getItem('cb-deepthink-on') === '1') toggleDeepThink();
      if (localStorage.getItem('cb-deepthink-eye') === '1') toggleDeepThinkEye();
    } catch (e) { console.warn('toggle restore failed', e); }
    maybeShowWhatsNew();   // post-update release notes (fire-and-forget; never gates the UI)
  }
  // Server-as-source-of-truth state mirror. Local UI state is kept minimal.
  let session = null;             // { version, status, phase, tokens, timeoutMs, events, ... }
  let expectedNextVersion = 0;
  let chatStartedAtLocal = null;  // for elapsed-time display
  let conversation = [];          // legacy — still used by /api/chat path. New flow ignores.
  let totalTokens = 0;            // legacy
  let activeProject = null;

  // ---- Inline status row (spinner + verb + timer + tokens) ----
  const SPINNER = ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏'];
  const VERB = 'Working';
  const PHASE = { UPLOADING: 'uploading', THINKING: 'thinking', DOWNLOADING: 'downloading', DONE: 'done', CANCELED: 'canceled' };
  const STALL_MS = 1500;   // no traffic this long → switch to THINKING (blinking)
  let _unlockPending = false;    // an unlock is awaiting the user (gates the models-fetch abort)
  let _modelsTimer = null;       // loadModels' abort timer — cancelled while parked on an unlock
  // #initModalBg ownership handshake: the change-key / GitLab-creds dialogs register
  // here ({ kind, initial, getDraft, cancel }) so the unlock modal — which rewrites
  // the SAME modal body — can preempt them CLEANLY (resolve their promise, keep the
  // half-typed draft) instead of silently discarding the user's typing.
  let _initModalOwner = null;
  let _unlockPreempted = null;   // { kind, initial, draft } — re-opened after unlock-done
  // Diff collapse state keyed by event id (item K) — survives the clearLog()
  // + full re-render that an event-patch triggers (the DOM is wiped, so the
  // state can't live on the element).
  const _diffCollapsed = new Map();
  // Task-wrap collapse state keyed by taskId. Lives off-DOM so it survives the
  // clearLog() + full re-render cycle (which rebuilds every wrap from scratch),
  // mirroring _diffCollapsed. Without this, collapse is lost on every re-render.
  const _taskCollapsed = new Map();

  // ── The chat status spinner — ONE implementation, instantiated per chat ──
  // The braille spinner + token arrows + elapsed clock + (optionally) the
  // context-fill bar and the <task-progress> chip. ALL state is per-INSTANCE,
  // so the main chat and the intake mini-chat each drive their OWN status row
  // in their OWN log without a shared global. \`getLog()\` returns the container
  // to append the status row into; \`showExtras\` gates the context bar + the
  // progress chip (main chat only — the intake has no server-pushed context /
  // progress signals). The main chat keeps thin module-level wrappers
  // (setUploading / renderStatus / …) over its instance so the SSE call sites
  // below stay byte-identical; the intake drives its own instance directly.
  function createStatusSpinner({ getLog, showExtras = false }) {
    let statusVerb = VERB;   // "Working" / "Reviewing" / "Checking the assignment" / …
    let statusTimer = null, upAnimTimer = null, tickTimer = null;
    let statusStart = 0, statusFrame = 0;
    let doneElapsed = 0;
    let statusRow = null;
    let phase = 'idle';
    let lastDir = 'up';      // direction of the most recent activity (for THINKING arrow)
    let lastDeltaTime = 0;   // timestamp of last content delta — for stall detection
    let upTokens = 0, downTokens = 0;
    let contextFill = null;        // { totalTokens, budgetTokens, budgetPct } or null
    let taskProgress = null;       // latest <task-progress> signal: { percent, note }

    function ensureRow() {
      const log = getLog();
      if (statusRow && statusRow.parentNode === log) return statusRow;
      statusRow = document.createElement('div');
      statusRow.className = 'status';
      log.appendChild(statusRow);
      scrollToBottom();
      return statusRow;
    }
    function pinToBottom() {
      const log = getLog();
      if (statusRow && statusRow.parentNode === log && statusRow !== log.lastChild) {
        log.appendChild(statusRow);
      }
    }
    function startSpinner() {
      statusStart = Date.now();
      statusFrame = 0;
      if (statusTimer) clearInterval(statusTimer);
      statusTimer = setInterval(() => {
        statusFrame++;
        // Stall detection: if we've been "downloading" or "uploading" but no
        // delta in a while, flip to THINKING (blinking arrow, same direction).
        if ((phase === PHASE.DOWNLOADING || phase === PHASE.UPLOADING) && lastDeltaTime > 0
            && Date.now() - lastDeltaTime > STALL_MS) {
          phase = PHASE.THINKING;
          if (statusRow) statusRow.classList.add('thinking');
        }
        renderStatus();
      }, 120);
    }
    function stopSpinnerOnly() {
      if (statusTimer) { clearInterval(statusTimer); statusTimer = null; }
      if (upAnimTimer) { cancelAnimationFrame(upAnimTimer); upAnimTimer = null; }
    }
    // The elapsed-clock re-render tick (separate from the 120ms glyph timer):
    // keeps the "Ns" counter live while a turn runs.
    function startTick() { if (tickTimer) return; tickTimer = setInterval(renderStatus, 100); }
    function stopTick() { if (tickTimer) { clearInterval(tickTimer); tickTimer = null; } renderStatus(); }
    function animateUpTo(target, ms) {
      if (upAnimTimer) cancelAnimationFrame(upAnimTimer);
      const from = upTokens;
      const t0 = Date.now();
      const tick = () => {
        if (phase !== PHASE.UPLOADING) { upAnimTimer = null; return; }
        const t = Math.min(1, (Date.now() - t0) / ms);
        upTokens = Math.round(from + (target - from) * t);
        renderStatus();
        if (t < 1) upAnimTimer = requestAnimationFrame(tick);
        else { upAnimTimer = null; setThinking(); }
      };
      upAnimTimer = requestAnimationFrame(tick);
    }
    function setUploading(target, ms) {
      ensureRow();
      if (!statusTimer) startSpinner();
      phase = PHASE.UPLOADING;
      statusVerb = VERB;   // a fresh turn always starts as the worker "Working"
      lastDir = 'up';
      lastDeltaTime = Date.now();
      statusRow.classList.remove('thinking');
      if (target == null || target <= upTokens) {
        if (target != null) upTokens = Math.max(upTokens, target);
        renderStatus();
        setTimeout(() => { if (phase === PHASE.UPLOADING) setThinking(); }, 80);
      } else {
        animateUpTo(target, ms ?? 700);
      }
    }
    function setThinking() {
      phase = PHASE.THINKING;
      if (statusRow) statusRow.classList.add('thinking');
      renderStatus();
    }
    function setDownloading() {
      if (phase !== PHASE.DOWNLOADING) downTokens = 0;
      phase = PHASE.DOWNLOADING;
      lastDir = 'down';
      lastDeltaTime = Date.now();
      if (statusRow) statusRow.classList.remove('thinking');
      renderStatus();
    }
    function setDone() {
      doneElapsed = Math.floor((Date.now() - statusStart) / 1000);
      phase = PHASE.DONE;
      stopSpinnerOnly();
      if (statusRow) statusRow.classList.remove('thinking');
      renderStatus();
      // status row persists; do NOT remove
    }
    function setCanceled() {
      doneElapsed = Math.floor((Date.now() - statusStart) / 1000);
      phase = PHASE.CANCELED;
      stopSpinnerOnly();
      if (statusRow) statusRow.classList.remove('thinking');
      renderStatus();
      // Row persists until the user submits a new message; the next chat
      // start reuses this same statusRow DOM element and replaces the
      // canceled content with the fresh spinner via setUploading().
    }
    function renderStatus() {
      if (!statusRow) return;
      const fmt = (n) => (n >= 1000 ? (n / 1000).toFixed(1) + 'k' : String(n));
      if (phase === PHASE.DONE) {
        statusRow.innerHTML =
          '<span class="arrow">↓</span> <span class="count">' + fmt(downTokens) + '</span>' +
          ' <span class="dim">tokens · ' + doneElapsed + 's</span>';
        return;
      }
      if (phase === PHASE.CANCELED) {
        statusRow.innerHTML =
          '<span class="arrow">⊘</span> <span class="dim">canceled by user · ' + doneElapsed + 's</span>';
        return;
      }
      const sp = SPINNER[statusFrame % SPINNER.length];
      const secs = Math.floor((Date.now() - statusStart) / 1000);
      // Arrow direction follows the most recent activity. During THINKING
      // (stalled) it stays in that direction and blinks.
      const isDown = (phase === PHASE.DOWNLOADING) || (phase === PHASE.THINKING && lastDir === 'down');
      const arrow = isDown ? '↓' : '↑';
      const count = isDown ? downTokens : upTokens;
      statusRow.innerHTML =
        '<span class="spin">' + sp + '</span> ' + statusVerb + '… ' +
        '<span class="arrow">' + arrow + '</span> <span class="count">' + fmt(count) + '</span>' +
        ' <span class="dim">tokens · ' + secs + 's · esc to interrupt</span>' +
        contextBarHtml() + progressChipHtml();
    }
    // The agent's latest per-turn completion estimate (the <task-progress> signal).
    function progressChipHtml() {
      return '';   // task ~N% chip removed from the load line (kept as a no-op to avoid call-site churn)
    }
    // Context-window fill gauge: total context vs the agent's total budget (the
    // binding constraint that triggers compaction). Hidden until the first
    // context-fill arrives. Turns red as it approaches the compaction threshold.
    function contextBarHtml() {
      if (!showExtras || !contextFill || contextFill.budgetPct == null) return '';
      const pct = Math.max(0, Math.min(100, contextFill.budgetPct));
      const hot = pct >= 80 ? ' hot' : '';
      const tip = \`context: \${contextFill.totalTokens} / \${contextFill.budgetTokens} tokens (\${pct}%)\`;
      // Match the rest of the load line: the same " · " separator (not a "•" bullet) + the word
      // "context" after the number → "… esc to interrupt · 49% context".
      return \` <span class="context-pct\${hot}" title="\${tip}">· \${pct}% context</span>\`;
    }
    // Server-pushed signals, folded in so the SSE call sites stay thin.
    function setVerb(v) { statusVerb = v; }
    function applyTokens(up, down) {
      // Infer DOWNLOADING from which side moved (phase events can lag/skip).
      if (down > downTokens && phase !== PHASE.DOWNLOADING && phase !== PHASE.DONE) setDownloading();
      upTokens = up; downTokens = down; lastDeltaTime = Date.now();
    }
    function setContextFill(cf) { contextFill = cf; }
    function setTaskProgress(tp) { taskProgress = tp; }
    // The log DOM was wiped (clearLog / full re-render) — drop the stale row ref
    // so the next ensureRow() builds a fresh one.
    function clearRow() { statusRow = null; }
    function reset() {
      stopSpinnerOnly(); if (tickTimer) { clearInterval(tickTimer); tickTimer = null; }
      statusVerb = VERB; statusStart = 0; statusFrame = 0; doneElapsed = 0;
      statusRow = null; phase = 'idle'; lastDir = 'up'; lastDeltaTime = 0;
      upTokens = 0; downTokens = 0; contextFill = null; taskProgress = null;
    }
    return {
      ensureRow, pinToBottom, startSpinner, stopSpinnerOnly, startTick, stopTick,
      setUploading, setThinking, setDownloading, setDone, setCanceled, renderStatus,
      setVerb, applyTokens, setContextFill, setTaskProgress, clearRow, reset,
      get phase() { return phase; },
    };
  }

  // ── createChatComponent — the reusable chat SHELL, instantiated per chat ──
  // The pieces every chat shares: its render context (the log + the per-chat
  // afterRow/clearStatus hooks the shared render functions call), a status
  // spinner instance, a render() that pipes an event through the shared
  // renderEvent into THIS chat's log, and (optionally) the input behavior
  // (auto-grow + Enter→submit). The TRANSPORT is NOT here — it's the injected
  // adapter each caller wires up (the main chat's SSE / the intake's NDJSON)
  // and feeds in via render()/spinner. The main chat passes inputEl:null and
  // keeps its richer composer (queue/steer), so only the genuinely-shared core
  // lives here; the intake passes its textarea and uses the whole shell.
  function createChatComponent({ rc, inputEl = null, showContextBar = false, onSubmit = null }) {
    const spinner = createStatusSpinner({ getLog: () => rc.log, showExtras: showContextBar });
    // Per-chat hooks the shared render functions call (renderInto swaps the
    // ambient RC to this rc, so addRow/addBubble pin + clear the RIGHT spinner).
    rc.afterRow = () => spinner.pinToBottom();
    rc.clearStatus = () => spinner.clearRow();
    function render(ev) { renderInto(rc, () => renderEvent(ev)); }
    if (inputEl) {
      inputEl.addEventListener('input', () => autoGrowEl(inputEl));
      inputEl.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); onSubmit?.(); }
      });
    }
    return { rc, spinner, render, focus: () => { try { inputEl?.focus(); } catch {} } };
  }

  // The MAIN chat as a component instance. inputEl:null — the main composer has
  // its own queue/steer/⇧-key wiring (below), so it keeps that and shares only
  // the core (spinner + rc hooks + render). \`status\` aliases its spinner so the
  // thin module wrappers + every existing SSE call site stay byte-identical.
  const mainChat = createChatComponent({ rc: RC, inputEl: null, showContextBar: true });
  const status = mainChat.spinner;
  function ensureStatusRow() { return status.ensureRow(); }
  function startSpinner() { return status.startSpinner(); }
  function stopSpinnerOnly() { return status.stopSpinnerOnly(); }
  function setUploading(t, ms) { return status.setUploading(t, ms); }
  function setThinking() { return status.setThinking(); }
  function setDownloading() { return status.setDownloading(); }
  function setDone() { return status.setDone(); }
  function setCanceled() { return status.setCanceled(); }
  // While the "lost contact" row owns the status line, stray repaints (a late
  // tick, a token delta from a half-dead socket) must not put the spinner back.
  function renderStatus() { if (_connLost) return; return status.renderStatus(); }
  function startSpinTick() { return status.startTick(); }
  function stopSpinTick() { return status.stopTick(); }
  function estimateTokens(s) { return Math.ceil((s || '').length / 4); }

  function basename(p) {
    if (!p) return '';
    return p.split(/[\\\\/]/).filter(Boolean).pop() || p;
  }

  // Loaded by the inline script at the bottom on page load. Decides which
  // view to show. If no project is open yet, go straight to the landing
  // picker. Otherwise hand off to initialize() which gates the chat behind
  // API-key + models-loaded readiness.
  async function loadState() {
    try {
      const r = await fetch('/api/project-state');
      const s = await r.json();
      if (!s.activeProject) {
        showLanding(s.recent ?? []);
        return;
      }
      await initialize(s.activeProject);
    } catch (e) {
      showError('Failed to load state', e);
    }
  }

  // Initialization view. Shown as soon as a project is selected and stays
  // visible until we can guarantee the chat is actually usable:
  //   1. API key present (or just entered via the first-run modal).
  //   2. /api/models returns successfully (any 401 unlock_url is handled
  //      via the SSE unlock-needed modal, which blocks here on the
  //      models fetch until the user clicks "I've unlocked").
  // If any step fails, the init view shows the error + a Retry button.
  async function initialize(projectPath) {
    const $init = document.getElementById('initView');
    const $status = document.getElementById('initStatus');
    const $retry = document.getElementById('initRetryBtn');
    activeProject = projectPath ?? activeProject;
    $landing.classList.add('hidden');
    $chat.classList.add('hidden');
    $init.classList.remove('hidden');
    $retry.classList.add('hidden');
    setInitSpinner(true);   // keep motion through every init step (also covers the Retry path)
    const t0 = Date.now();
    const tick = () => \`\${Math.round((Date.now() - t0) / 1000)}s\`;
    try {
      console.log('[init] start, project=', projectPath);
      $status.textContent = 'Checking API key…';
      const ak = await fetch('/api/api-key').then((r) => r.json()).catch(() => ({ hasKey: true }));
      console.log('[init] api-key', JSON.stringify(ak));
      if (!ak.hasKey) {
        $status.textContent = 'API key required — see modal…';
        const ok = await promptForApiKey({ initial: true });
        console.log('[init] key prompt resolved', ok);
        if (!ok) {
          $status.textContent = 'API key was not set. Click Retry to try again.';
          setInitSpinner(false);
          $retry.classList.remove('hidden');
          return;
        }
      }
      $status.textContent = 'Checking GitLab credentials…';
      const gc = await fetch('/api/gitlab-creds').then((r) => r.json()).catch(() => ({ hasCreds: true }));
      if (!gc.hasCreds) {
        $status.textContent = 'GitLab credentials required — see modal…';
        const ok = await promptForGitLabCreds({ initial: true });
        if (!ok) {
          $status.textContent = 'GitLab credentials were not set. Click Retry to try again.';
          setInitSpinner(false);
          $retry.classList.remove('hidden');
          return;
        }
      }
      $status.textContent = 'Loading models (this may pause for unlock)…';
      await loadModels();  // blocks here if a 401 unlock is needed
      console.log('[init] loadModels done after', tick());
      // Models loaded → any API-key/unlock dance is definitively complete, so
      // make sure the init modal ($initModalBg — the api-key/unlock overlay,
      // a DIFFERENT element from the init view) can't stay stuck over the
      // now-ready chat. Its own close paths (the /api/unlock-done fetch and the
      // SSE 'unlock-done' message) can be missed if the response is slow; this
      // is the backstop.
      if ($initModalBg.classList.contains('show')) {
        console.log('[init] closing leftover init/unlock modal after successful model load');
        $initModalBg.classList.remove('show');
      }
      $status.textContent = 'Ready.';
      setInitSpinner(false);
      $init.classList.add('hidden');
      _initialized = true;
      // If SSE init arrived during initialization, render its captured
      // session now. Otherwise, if SSE delivered the restored
      // conversation while we were loading models (project-switch path:
      // server's openProject broadcasts a reset + each restored event,
      // those land in session.events while initialize is awaiting the
      // models fetch), render the live session. Only if neither path
      // populated the session do we show an empty chat.
      if (_pendingSession) {
        renderSessionInChat(_pendingSession);
        _pendingSession = null;
      } else if (session && Array.isArray(session.events) && session.events.length > 0) {
        renderSessionInChat(session);
      } else {
        showChat(activeProject);
      }
      // Post-init diagnostic: log the state of every modal + whether the
      // gear is the topmost element at its coordinates. If the user later
      // reports a "frozen chat", this line should expose what's covering
      // the page (or confirm nothing is, and the freeze is elsewhere).
      setTimeout(() => {
        const btn = document.getElementById('settingsBtn');
        const rect = btn?.getBoundingClientRect();
        const topEl = rect ? document.elementFromPoint(rect.x + rect.width / 2, rect.y + rect.height / 2) : null;
        console.log('[init] post-state', JSON.stringify({
          initModalShown: $initModalBg?.classList.contains('show'),
          mainModalShown: $modalBg?.classList.contains('show'),
          settingsModalShown: $settingsModalBg?.classList.contains('show'),
          initViewHidden: $init?.classList.contains('hidden'),
          chatHidden: $chat?.classList.contains('hidden'),
          settingsBtnTopEl: topEl?.id || topEl?.className || topEl?.tagName || '(none)',
          settingsBtnIsTop: topEl === btn || (btn && btn.contains(topEl)),
          bodyClasses: document.body.className,
        }));
      }, 100);
    } catch (e) {
      setInitSpinner(false);
      $status.textContent = 'Initialization failed: ' + (e?.message || e);
      $retry.classList.remove('hidden');
    }
  }

  // ── API key prompt / change ─────────────────────────────────────────────
  // Shown automatically on first run when ~/.code_boss/api-key.txt is absent,
  // or on demand from the settings modal. Resolves true on save, false on
  // cancel.
  function promptForApiKey({ initial, draft } = {}) {
    return new Promise((resolve) => {
      const title = initial ? 'Set API key' : 'Change API key';
      const subtitle = initial
        ? 'code_boss needs an API key for the LLM endpoint. It will be saved at <code>~/.code_boss/api-key.txt</code> so this prompt only appears once.'
        : 'Replace the API key. It overwrites <code>~/.code_boss/api-key.txt</code>.';
      $initModalTitle.textContent = title;
      // Change-key (from settings) gets a Cancel; first-run does NOT —
      // there's no usable app behind the modal if a key isn't set yet.
      const cancelHtml = initial ? '' :
        '<button id="apiKeyCancel" class="btn">Cancel</button>';
      $initModalBody.innerHTML = \`
        <p style="margin: 0 0 12px; color: var(--muted); font-size: 12.5px;">\${subtitle}</p>
        <input id="apiKeyInput" type="password" autocomplete="off" spellcheck="false"
               placeholder="paste key" style="width: 100%; padding: 8px 10px; font: inherit;
               font-size: 13px; background: var(--panel-alt); color: var(--fg);
               border: 1px solid var(--border); border-radius: 4px;" />
        <div style="margin-top: 12px; display: flex; gap: 8px; justify-content: flex-end;">
          \${cancelHtml}
          <button id="apiKeySave" class="btn primary">Save</button>
        </div>
        <p id="apiKeyMsg" style="margin: 8px 0 0; color: var(--error); font-size: 12px; min-height: 14px;"></p>
      \`;
      $initModalBg.classList.add('show');
      const inp = document.getElementById('apiKeyInput');
      const msg = document.getElementById('apiKeyMsg');
      const save = document.getElementById('apiKeySave');
      const cancel = document.getElementById('apiKeyCancel');
      if (draft && draft.key) inp.value = draft.key;   // restored after an unlock preemption
      setTimeout(() => inp?.focus(), 50);

      const done = (ok) => { _initModalOwner = null; $initModalBg.classList.remove('show'); resolve(ok); };
      // Register as the modal's owner so an unlock can preempt without orphaning us.
      _initModalOwner = { kind: 'apiKey', initial: !!initial, getDraft: () => ({ key: inp.value }), cancel: () => done(false) };
      if (cancel) cancel.onclick = () => done(false);
      const submit = async () => {
        const key = (inp.value || '').trim();
        if (!key) { msg.textContent = 'Key cannot be empty.'; return; }
        save.disabled = true;
        try {
          const r = await fetch('/api/api-key', {
            method: 'POST',
            headers: { 'content-type': 'application/json' },
            body: JSON.stringify({ key }),
          });
          if (!r.ok) {
            const body = await r.text();
            msg.textContent = 'Save failed: ' + body.slice(0, 200);
            save.disabled = false;
            return;
          }
          done(true);
        } catch (e) {
          msg.textContent = 'Save failed: ' + e.message;
          save.disabled = false;
        }
      };
      save.onclick = submit;
      inp.addEventListener('keydown', (e) => { if (e.key === 'Enter') submit(); });
    });
  }
  function promptForGitLabCreds({ initial, draft } = {}) {
    return new Promise((resolve) => {
      const title = initial ? 'Set GitLab credentials' : 'Change GitLab credentials';
      const subtitle = initial
        ? 'code_boss can fetch files from your internal GitLab without cloning. Enter the username + personal access token (classic). Saved to <code>~/.code_boss/gitlab-creds.json</code>.'
        : 'Replace the GitLab username and personal access token. Overwrites <code>~/.code_boss/gitlab-creds.json</code>.';
      $initModalTitle.textContent = title;
      const cancelHtml = initial ? '' :
        '<button id="glCredsCancel" class="btn">Cancel</button>';
      $initModalBody.innerHTML = \`
        <p style="margin: 0 0 12px; color: var(--muted); font-size: 12.5px;">\${subtitle}</p>
        <input id="glCredsUser" type="text" autocomplete="off" spellcheck="false"
               placeholder="username" style="width: 100%; padding: 8px 10px; font: inherit;
               font-size: 13px; background: var(--panel-alt); color: var(--fg);
               border: 1px solid var(--border); border-radius: 4px; margin-bottom: 8px;" />
        <input id="glCredsToken" type="password" autocomplete="off" spellcheck="false"
               placeholder="personal access token (classic)" style="width: 100%; padding: 8px 10px; font: inherit;
               font-size: 13px; background: var(--panel-alt); color: var(--fg);
               border: 1px solid var(--border); border-radius: 4px;" />
        <div style="margin-top: 12px; display: flex; gap: 8px; justify-content: flex-end;">
          \${cancelHtml}
          <button id="glCredsSave" class="btn primary">Save</button>
        </div>
        <p id="glCredsMsg" style="margin: 8px 0 0; color: var(--error); font-size: 12px; min-height: 14px;"></p>
      \`;
      $initModalBg.classList.add('show');
      const u = document.getElementById('glCredsUser');
      const t = document.getElementById('glCredsToken');
      const msg = document.getElementById('glCredsMsg');
      const save = document.getElementById('glCredsSave');
      const cancel = document.getElementById('glCredsCancel');
      if (draft) { if (draft.user) u.value = draft.user; if (draft.token) t.value = draft.token; }   // restored after an unlock preemption
      setTimeout(() => u?.focus(), 50);

      const done = (ok) => { _initModalOwner = null; $initModalBg.classList.remove('show'); resolve(ok); };
      // Register as the modal's owner so an unlock can preempt without orphaning us.
      _initModalOwner = { kind: 'gitlab', initial: !!initial, getDraft: () => ({ user: u.value, token: t.value }), cancel: () => done(false) };
      if (cancel) cancel.onclick = () => done(false);
      const submit = async () => {
        const user = (u.value || '').trim();
        const token = (t.value || '').trim();
        if (!user || !token) { msg.textContent = 'Username and token are required.'; return; }
        save.disabled = true;
        try {
          const r = await fetch('/api/gitlab-creds', {
            method: 'POST',
            headers: { 'content-type': 'application/json' },
            body: JSON.stringify({ user, token }),
          });
          if (!r.ok) {
            const body = await r.text();
            msg.textContent = 'Save failed: ' + body.slice(0, 200);
            save.disabled = false;
            return;
          }
          done(true);
        } catch (e) {
          msg.textContent = 'Save failed: ' + e.message;
          save.disabled = false;
        }
      };
      save.onclick = submit;
      [u, t].forEach((el) => el.addEventListener('keydown', (e) => { if (e.key === 'Enter') submit(); }));
    });
  }
  // Browser-side unlock flow. Shown when the LLM endpoint returns 401 with an
  // unlock_url. TWO stages: (1) just "Open unlock page" (primary/orange). After
  // it's opened, (2) "I'm finished" (primary/orange) + "Reopen" (gray). The
  // in-flight request is parked server-side until the user clicks "I'm finished".
  function showUnlockModal(url) {
    _unlockPending = true;
    // A settings dialog (change key / GitLab creds) may be open on this SAME
    // modal with half-typed input. Capture its draft and cancel it CLEANLY
    // (its promise resolves false — no orphan), then re-open it with the
    // draft restored once the unlock finishes.
    if (_initModalOwner) {
      const owner = _initModalOwner;
      let draft = null; try { draft = owner.getDraft(); } catch {}
      _unlockPreempted = { kind: owner.kind, initial: owner.initial, draft };
      try { owner.cancel(); } catch {}
    }
    // The models fetch (and any other parked adapter call) is legitimately
    // WAITING on this unlock, not hung — so cancel its abort timer. Otherwise a
    // slow human unlock (the user took ~90s last time) trips the timeout and we
    // wrongly show "Failed to load models: signal is aborted". unlock-done
    // retries loadModels if the parked fetch didn't resolve on its own.
    if (_modelsTimer) { clearTimeout(_modelsTimer); _modelsTimer = null; }
    $initModalTitle.textContent = 'API key locked';
    $initModalBody.innerHTML = \`
      <p style="margin: 0 0 10px; color: var(--fg); font-size: 13px;">
        The endpoint needs you to unlock the API key in your browser. The request
        in flight is paused — it resumes the moment you click <strong>I'm finished</strong>.
      </p>
      <div style="margin: 0 0 12px; padding: 6px 8px; background: var(--panel-alt);
                  border: 1px solid var(--border); border-radius: 4px;
                  font-family: var(--mono); font-size: 11px; color: var(--muted);
                  word-break: break-all;">\${escapeHtml(url)}</div>
      <div id="unlockStage1" style="display: flex; gap: 8px; justify-content: flex-end;">
        <button id="unlockOpen" class="btn primary">Open unlock page</button>
      </div>
      <div id="unlockStage2" style="display: none; gap: 8px; justify-content: flex-end;">
        <button id="unlockReopen" class="btn">Reopen</button>
        <button id="unlockDone" class="btn primary">I'm finished</button>
      </div>
      <p id="unlockMsg" style="margin: 8px 0 0; color: var(--muted); font-size: 11px; min-height: 14px;"></p>
    \`;
    $initModalBg.classList.add('show');
    const openPage = () => { window.open(url, '_blank', 'noopener'); };
    document.getElementById('unlockOpen').onclick = () => {
      openPage();
      document.getElementById('unlockStage1').style.display = 'none';
      document.getElementById('unlockStage2').style.display = 'flex';
      document.getElementById('unlockMsg').textContent = 'Opened in a new tab. Sign in / approve there, then click "I\\'m finished". (Didn\\'t open? Use Reopen.)';
    };
    document.getElementById('unlockReopen').onclick = openPage;
    document.getElementById('unlockDone').onclick = async () => {
      const btn = document.getElementById('unlockDone');
      btn.disabled = true;
      document.getElementById('unlockMsg').textContent = 'Resuming the paused request…';
      console.log('[unlock] user clicked finished, posting /api/unlock-done');
      // Close the modal OPTIMISTICALLY — the server resolves the parked request
      // the instant it RECEIVES this POST; a bounded fetch never hangs the UI.
      const ctrl = new AbortController();
      const tid = setTimeout(() => { try { ctrl.abort(); } catch {} }, 8000);
      fetch('/api/unlock-done', { method: 'POST', signal: ctrl.signal })
        .then(() => console.log('[unlock] /api/unlock-done returned'))
        .catch((e) => console.log('[unlock] /api/unlock-done signal error (non-fatal):', e?.message))
        .finally(() => clearTimeout(tid));
      _unlockPending = false;
      $initModalBg.classList.remove('show');
      // Re-open the dialog the unlock displaced, half-typed draft restored.
      if (_unlockPreempted) {
        const p = _unlockPreempted; _unlockPreempted = null;
        if (p.kind === 'apiKey') promptForApiKey({ initial: p.initial, draft: p.draft });
        else if (p.kind === 'gitlab') promptForGitLabCreds({ initial: p.initial, draft: p.draft });
      }
    };
  }

  async function changeApiKey() {
    const ok = await promptForApiKey({ initial: false });
    if (ok && typeof openSettings === 'function') openSettings();   // re-open settings with the new prefix
  }

  async function changeGitLabCreds() {
    const ok = await promptForGitLabCreds({ initial: false });
    if (ok && typeof openSettings === 'function') openSettings();
  }

  // (The mid-task per-edit developer-approval modal was retired — review-off auto-applies, review-on
  // is propose-first, and revert safety comes from git / the shadow-git undo.)

  // Window title = the open project's path, LEFT-ellipsized (last ~45 chars,
  // cut at a path separator) so the END of the path stays visible in the
  // Chrome app-mode title bar / taskbar. No project → 'code_boss'.
  function cbSetWindowTitle(projectPath) {
    const p = String(projectPath || '');
    if (!p) { document.title = 'code_boss'; return; }
    const MAX = 45;
    if (p.length <= MAX) { document.title = p; return; }
    let tail = p.slice(-MAX);
    const sep = tail.search(/[\\\\/]/);
    if (sep >= 0) tail = tail.slice(sep);   // don't lead with a half segment
    document.title = '…' + tail;
  }

  // Tracks the most-recent project so the folder picker starts at its
  // parent (siblings = one click). Falls back to home dir.
  let _lastRecentProject = null;
  function showLanding(recent) {
    activeProject = null;
    cbSetWindowTitle(null);
    $chat.classList.add('hidden');
    $landing.classList.remove('hidden');
    // Modals live at body root, so they'd otherwise float above the
    // landing view. Close the git resolution wizard if it's open —
    // its repos belong to the project we just closed.
    const $gwBg = document.getElementById('gitWizardBg');
    if ($gwBg) {
      $gwBg.classList.remove('show');
      const $gwBody = document.getElementById('gitWizardBody');
      if ($gwBody) $gwBody.innerHTML = '';
    }
    $recentList.innerHTML = '';
    if (!recent || recent.length === 0) {
      $recentList.innerHTML = '<li class="recent-empty">No recent projects yet.</li>';
      _lastRecentProject = null;
    } else {
      for (const p of recent) {
        const li = document.createElement('li');
        li.className = 'recent-item';
        li.innerHTML = '<div class="name">' + escapeHtml(basename(p)) + '</div>'
          + '<div class="path">' + escapeHtml(p) + '</div>';
        li.addEventListener('click', () => openProject(p));
        $recentList.appendChild(li);
      }
      _lastRecentProject = recent[0];
    }
  }

  function showChat(projectPath) {
    activeProject = projectPath;
    cbSetWindowTitle(projectPath);
    $landing.classList.add('hidden');
    $chat.classList.remove('hidden');
    $projectPath.textContent = projectPath;
    $projectPath.title = projectPath;
    if ($settingsProject) $settingsProject.textContent = projectPath;
    // Fresh chat on project switch
    conversation = [];
    totalTokens = 0;
    if ($settingsTokens) $settingsTokens.textContent = '0';
    // No chat header (project lives in the side panel, model in settings) —
    // just a faint one-line hint so an empty chat isn't blank.
    RC.log.innerHTML =
      '<div class="welcome">' +
        '<div class="wl">Ask a question below. ⏎ to send, shift+⏎ for newline, esc to interrupt.</div>' +
      '</div>';
    if ($model.options.length === 0 || $model.value === '') loadModels();
    // The assignment side panel belongs to the project, not the conversation —
    // load it whenever the project view is shown (incl. a fresh/empty project,
    // which used to fall through this path without ever loading the backlog).
    loadBacklog();
    renderArtifacts();   // prune artifacts whose fences the log reset just removed
    $input.focus();
    // Drop the previous project's dirty-indicator state synchronously
    // so the tooltip doesn't flash with stale repo names while the
    // first /api/git-dirty call is in flight.
    const $gd = document.getElementById('gitDirtyIndicator');
    if ($gd) { $gd.style.display = 'none'; $gd.title = ''; }
    refreshGitDirty();
    refreshMergeBar();
  }

  // Side-panel tabs: Assignments | Queue | Memory. Clicking a tab marks it
  // active and shows its pane; Memory/Queue refresh their content on open.
  const SIDE_TABS = ['checklists', 'subagents', 'cognition', 'queue', 'assignments', 'memory', 'artifacts'];
  function selectSideTab(name) {
    for (const t of SIDE_TABS) {
      const btn = document.getElementById('tabbtn-' + t);
      const pane = document.getElementById('pane-' + t);
      const on = (t === name);
      if (btn) btn.classList.toggle('active', on);
      if (pane) pane.classList.toggle('hidden', !on);
    }
    if (name === 'memory') loadMemory();
    if (name === 'queue') renderQueue();
    if (name === 'subagents') renderSubagents();
    if (name === 'cognition') renderCognitionTree();
    if (name === 'checklists') renderChecklistTab();
    if (name === 'artifacts') renderArtifacts();
  }

  // ── Cognition tree (transparency for CB_COGNITION runs) ──────────────────
  // Streamed live via ephemeral 'cognition-event's: how the agent sized the problem, which
  // strategy layers engaged, the recursion, and the salience×budget spend.
  const cogState = { rows: [], budget: null };
  function handleCognitionEvent(cog) {
    if (!cog) return;
    if (cog.type === 'budget') { cogState.rows = []; cogState.budget = { complexity: cog.complexity }; }
    if (cog.spent != null) cogState.budget = Object.assign(cogState.budget || {}, { spent: cog.spent, ceiling: cog.ceiling, nodes: cog.nodes, overhead: cog.overhead });
    cogState.rows.push(cog);
    const cnt = document.getElementById('cognitionCount');
    if (cnt) cnt.textContent = cog.type === 'done' ? '' : String(cogState.budget?.nodes || '');
    const ctab = document.getElementById('tabbtn-cognition');   // reveal the tab once cognition is actually used
    if (ctab) ctab.style.display = '';
    renderCognitionTree();
  }
  function renderCognitionTree() {
    const b = document.getElementById('cogBudget');
    if (b) {
      if (cogState.budget && cogState.budget.ceiling) {
        const { spent = 0, ceiling = 0, nodes = 0, overhead = 0, complexity } = cogState.budget;
        const pct = ceiling ? Math.min(100, (spent / ceiling) * 100) : 0;
        b.innerHTML = '<div class="cog-bar"><div class="cog-bar-fill" style="width:' + pct.toFixed(0) + '%"></div></div>'
          + '<div class="cog-bar-label">' + Math.round(spent) + ' / ' + Math.round(ceiling) + ' work-units · ' + nodes + ' nodes · overhead ' + Math.round(overhead) + (complexity ? ' · ' + complexity + ' complexity' : '') + '</div>';
      } else b.innerHTML = '';
    }
    const tree = document.getElementById('cogTree'); if (!tree) return;
    if (!cogState.rows.length) return;
    tree.innerHTML = '';
    for (const cog of cogState.rows) tree.appendChild(cogRowEl(cog));
    tree.scrollTop = tree.scrollHeight;
  }
  function cogRowEl(cog) {
    const d = document.createElement('div');
    d.className = 'cog-row cog-' + (cog.type || '');
    d.style.paddingLeft = (6 + (cog.depth || 0) * 14) + 'px';
    const sal = (cog.salience != null) ? (+cog.salience).toFixed(2) : null;
    let t;
    switch (cog.type) {
      case 'budget': t = '▣ budget: ' + (cog.complexity || '?') + ' → ' + Math.round(cog.ceiling || 0) + ' units'; break;
      case 'node': t = '◆ assess [' + ((cog.layers || []).join(' → ')) + ']' + (cog.verify ? ' +verify' : '') + (cog.metrics ? ' · ' + cog.metrics.complexity + '/' + cog.metrics.creativity + (cog.metrics.accuracy === 'critical' ? '/crit' : '') : '') + (sal ? ' · sal ' + sal : ''); break;
      case 'explore-start': t = '✦ explore: ' + (cog.n || '') + ' parallel proposers'; break;
      case 'proposer': t = '· proposer ' + (cog.i || '') + (cog.lens ? ' — ' + cog.lens : ''); break;
      case 'judge': t = '⚖ judge: synthesizing ' + (cog.proposals || '') + ' approaches'; break;
      case 'explore': t = '✦ explored → approach chosen'; break;
      case 'decompose': t = '⑃ decompose → ' + ((cog.steps || []).length) + ' steps'; break;
      case 'worker-start': t = '▶ worker: ' + (cog.topic || cog.id || ''); break;
      case 'worker': t = '● worked (' + (cog.mode || 'single') + ')'; break;
      case 'verify': t = cog.ok ? '✓ verified' : '✗ verify failed → re-run'; break;
      case 'rerun': t = '↻ re-run #' + (cog.round || ''); break;
      case 'stopped': t = '■ stopped — budget' + (sal ? ' (salience ' + sal + ' too low for the spend)' : ''); break;
      case 'answer': t = '💬 answered directly'; break;
      case 'done': t = '✔ done — ' + (cog.nodes || 0) + ' nodes, ' + Math.round(cog.spent || 0) + ' units'; break;
      default: t = cog.type || '';
    }
    d.textContent = t;
    return d;
  }

  // Collapse / expand the left side panel (chat reclaims the width).
  // Collapse/expand the side panel. When collapsed, the chrome (path/weblogic/
  // edit/gear) DOCKS under the chat input; expanding moves it back into the panel.
  function setSidePanelCollapsed(collapsed) {
    const p = document.getElementById('sidePanel');
    if (!p) return;
    p.classList.toggle('collapsed', collapsed);
    const chrome = document.getElementById('sideChrome');
    const footer = document.querySelector('#chatMain footer');
    if (chrome && footer) {
      chrome.classList.toggle('docked', collapsed);
      (collapsed ? footer : p).appendChild(chrome);   // dock under input / back into panel
    }
    updateChecklistViews();   // panel OPEN → the Checklists tab (scroll-synced); CLOSED → the footer (current)
    try { localStorage.setItem('cb-side-collapsed', collapsed ? '1' : '0'); } catch {}
  }

  // ── Per-task checklists ───────────────────────────────────────────────────
  // Checklists arrive per task (the 'checklist' event carries its taskId). The Checklists TAB shows
  // the checklist of the task at the viewport MIDDLE (scroll-synced — go back in time as you scroll
  // up); the footer panel above the input shows ONLY the CURRENT checklist, and ONLY when the side
  // panel is collapsed. (Live within a session; ephemeral events aren't replayed on reload.)
  const _checklistsByTask = new Map();   // taskId → items
  let _currentChecklistTaskId = null;
  function _checklistHtml(items) {
    const done = items.filter((i) => i && i.done).length;
    return \`<div class="cl-title">checklist · \${done}/\${items.length} done</div>\` + items.map((i) =>
      \`<div class="checklist-item\${i && i.done ? ' done' : ''}"><span class="cl-box">\${i && i.done ? '✓' : '☐'}</span><span class="cl-text">\${escapeHtml((i && i.text) || '')}</span></div>\`
    ).join('');
  }
  // Entry point for a checklist signal (taskId may be undefined → '_').
  function onChecklist(taskId, items) {
    const key = taskId || '_';
    if (Array.isArray(items) && items.length) { _checklistsByTask.set(key, items); _currentChecklistTaskId = key; }
    else { _checklistsByTask.delete(key); if (_currentChecklistTaskId === key) _currentChecklistTaskId = null; }
    const c = document.getElementById('checklistCount');
    if (c) c.textContent = _checklistsByTask.size ? \` (\${_checklistsByTask.size})\` : '';
    updateChecklistViews();
  }
  // Which task's checklist to show in the tab: the wrapped task straddling the viewport middle,
  // else the current one (the still-open task below the last wrap).
  function checklistTaskAtViewportMiddle() {
    const log = RC.log; if (!log) return _currentChecklistTaskId;
    // Middle of the VISIBLE portion of the chat (clamped to the window) — robust whether #log is the
    // scroller or an ancestor is. Using log.clientHeight alone breaks when #log isn't the scroller.
    const lr = log.getBoundingClientRect();
    const winH = window.innerHeight || document.documentElement.clientHeight || lr.height;
    const midY = (Math.max(lr.top, 0) + Math.min(lr.bottom, winH)) / 2;
    for (const w of log.querySelectorAll('.task-wrap[data-task-id]')) {
      const wr = w.getBoundingClientRect();
      if (wr.top <= midY && wr.bottom >= midY) return w.dataset.taskId;
    }
    return _currentChecklistTaskId;
  }
  function renderChecklistTab() {
    const tab = document.getElementById('checklistTab'); if (!tab) return;
    const taskId = checklistTaskAtViewportMiddle();
    const items = (taskId && _checklistsByTask.get(taskId)) || null;
    if (!items || !items.length) { tab.innerHTML = '<div class="checklist-empty">No checklist for the task here. The agent adds one for multi-step work — scroll the chat to see each task\\'s checklist.</div>'; return; }
    tab.innerHTML = _checklistHtml(items);
  }
  // Footer (#checklistPanel): CURRENT checklist, only when the side panel is collapsed.
  function updateChecklistViews() {
    const collapsed = !!document.getElementById('sidePanel')?.classList.contains('collapsed');
    renderChecklist(collapsed && _currentChecklistTaskId ? _checklistsByTask.get(_currentChecklistTaskId) : null);
    renderChecklistTab();
  }
  // Scroll-sync: while the panel is open + the Checklists tab active, the tab follows the task at
  // the viewport middle as you scroll the chat. Attached once (rAF-throttled, passive).
  // NOTE: the listener goes on #logScroll — the overflow:auto SCROLLER (same element as
  // _logScrollEl/RC.scroll) — not #log, which never scrolls itself (scroll events don't bubble).
  let _checklistScrollAttached = false;
  function attachChecklistScrollSync() {
    if (_checklistScrollAttached) return;
    const log = document.getElementById('logScroll');
    if (!log) return;
    _checklistScrollAttached = true;
    let raf = 0;
    log.addEventListener('scroll', () => {
      if (raf) return;
      raf = requestAnimationFrame(() => {
        raf = 0;
        const open = !document.getElementById('sidePanel')?.classList.contains('collapsed');
        const active = document.getElementById('tabbtn-checklists')?.classList.contains('active');
        if (open && active) renderChecklistTab();
      });
    }, { passive: true });
  }
  function toggleSidePanel() {
    const p = document.getElementById('sidePanel');
    setSidePanelCollapsed(!(p && p.classList.contains('collapsed')));   // « (in panel) collapses; » pip reopens
    renderSubagents();   // the collapsed-panel fallback list appears/hides with the panel
  }

  // ── Sub-agents (orchestrator delegation) ──────────────────────────────────
  // A message that arrives while the foreground is busy is delegated to a background
  // sub-agent running in its own git worktree. We track each one's status + live tool
  // activity here, list them in the left-panel "Sub-agents" tab (or a fallback list
  // under the input when the panel is collapsed), and show full activity in a right
  // slide-out on click. Tool activity arrives tagged with subagentChannel (kept OUT of
  // the main chat); the bounded result folds in as a subagent-card chat event.
  const saStore = new Map();   // id -> { id, topic, status, content, files, activity: [] }
  let saDrawerId = null;
  const SA_STATE_LABEL = { working: 'working', 'awaiting-review': 'awaiting review', reviewing: 'reviewing', done: 'done', error: 'failed', aborted: 'aborted', conflict: 'conflict', rejected: 'rejected', unmergeable: 'needs manual merge', recovered: 'recovered' };
  async function subagentAction(id, action) {
    try { await fetch('/api/subagent-action', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id, action }) }); } catch {}
  }

  function saUpsert(id, patch) {
    if (!id) return;
    const cur = saStore.get(id) || { id, topic: id, status: 'working', content: '', files: [], activity: [] };
    Object.assign(cur, patch);
    saStore.set(id, cur);
    renderSubagents();
    if (saDrawerId === id) renderSubagentDetail(id);
  }
  function saAddActivity(id, ev) {
    const cur = saStore.get(id) || { id, topic: id, status: 'working', content: '', files: [], activity: [] };
    cur.activity.push(ev);
    saStore.set(id, cur);
    if (saDrawerId === id) appendDrawerActivity(ev);
  }
  function saItemEl(sa) {
    const item = document.createElement('div');
    item.className = 'sa-item ' + (sa.status || 'working');
    item.onclick = () => openSubagentDetail(sa.id);
    const dot = document.createElement('span'); dot.className = 'sa-dot';
    const topic = document.createElement('span'); topic.className = 'sa-topic'; topic.textContent = sa.topic || sa.id; topic.title = sa.topic || sa.id;
    const state = document.createElement('span'); state.className = 'sa-state'; state.textContent = SA_STATE_LABEL[sa.status] || sa.status || '';
    item.appendChild(dot); item.appendChild(topic); item.appendChild(state);
    // Recovered (crash-resume) sub-agents need an explicit decision: re-review or discard.
    if (sa.status === 'recovered') {
      const mk = (label, action, cls) => { const b = document.createElement('button'); b.className = 'sa-act-btn' + (cls ? ' ' + cls : ''); b.textContent = label; b.onclick = (ev) => { ev.stopPropagation(); subagentAction(sa.id, action); }; return b; };
      item.appendChild(mk('Review', 'review'));
      item.appendChild(mk('Discard', 'discard', 'danger'));
    }
    return item;
  }
  function renderSubagents() {
    const list = [...saStore.values()].sort((a, b) => (a.id < b.id ? 1 : -1));   // newest first
    const working = list.filter((s) => s.status === 'working').length;
    const pane = document.getElementById('saList');
    if (pane) {
      pane.innerHTML = '';
      if (!list.length) {
        const e = document.createElement('div'); e.className = 'sa-empty';
        e.textContent = 'No sub-agents yet. While the agent is busy, a new message is delegated to a background sub-agent (isolated in its own git worktree) and appears here.';
        pane.appendChild(e);
      } else list.forEach((s) => pane.appendChild(saItemEl(s)));
    }
    const count = document.getElementById('subagentCount');
    if (count) count.textContent = working ? String(working) : '';
    const stab = document.getElementById('tabbtn-subagents');   // reveal the tab once a sub-agent actually exists
    if (stab) stab.style.display = '';
    // collapsed-panel fallback under the input
    const fb = document.getElementById('saFallback');
    if (fb) {
      const collapsed = document.getElementById('sidePanel')?.classList.contains('collapsed');
      fb.innerHTML = '';
      if (collapsed && list.length) {
        const head = document.createElement('div'); head.className = 'sa-fallback-head';
        head.textContent = working ? \`Sub-agents — \${working} working\` : 'Sub-agents';
        fb.appendChild(head);
        const inner = document.createElement('div'); inner.className = 'sa-list';
        list.forEach((s) => inner.appendChild(saItemEl(s)));
        fb.appendChild(inner);
        fb.classList.add('show');
      } else fb.classList.remove('show');
    }
  }
  function openSubagentDetail(id) {
    saDrawerId = id;
    document.getElementById('saDrawerBg')?.classList.add('show');
    renderSubagentDetail(id);
  }
  function closeSubagentDetail() {
    saDrawerId = null;
    document.getElementById('saDrawerBg')?.classList.remove('show');
  }
  // Collapsed 🔍 marker for a verified reviewer finding — shared by the main chat (renderEvent) and
  // the sub-agent drawer, so the hidden-until-the-eye contract holds everywhere a reviewer appears.
  // Collapsed head carries a one-line preview (scannable without expanding); hover = full, click = expand.
  function reviewerNoteEl(content) {
    const full = (content || '').trim();
    const d = document.createElement('div'); d.className = 'reviewer-note';
    const head = document.createElement('div'); head.className = 'cn-head';
    const oneLine = full.replace(/\\s+/g, ' ');
    head.textContent = '🔍 Reviewer · ' + oneLine.slice(0, 80) + (oneLine.length > 80 ? '…' : '');
    head.title = full;   // hover → full feedback
    const body = document.createElement('div'); body.className = 'cn-full'; body.textContent = full;
    d.appendChild(head); d.appendChild(body);
    d.onclick = () => d.classList.toggle('expanded');   // click → expand inline
    return d;
  }
  // Collapsed 🧠 marker for a deep_think fan-out — shared by the main chat (renderEvent) and the
  // sub-agent drawer, same hidden-until-the-eye contract as reviewerNoteEl (its own eye/body class:
  // body.show-deepthink-notes). phase 'start' is a transient "running…" line; the matching 'result'
  // (same taskId+question) REPLACES it in place via placeDeepThinkNote. textContent everywhere.
  function deepThinkKey(ev) { return (ev.taskId || '') + '|' + (ev.question || ''); }
  function deepThinkNoteEl(ev) {
    const d = document.createElement('div'); d.className = 'dt-note';
    d.dataset.dtKey = deepThinkKey(ev);
    d.dataset.dtPhase = ev.phase === 'start' ? 'start' : 'result';
    const head = document.createElement('div'); head.className = 'cn-head';
    head.title = ev.question || '';   // hover → the full question
    if (ev.phase === 'start') {
      head.textContent = '🧠 Deep think · 3 perspectives running…';
      d.appendChild(head);
      return d;
    }
    const q = (ev.question || '').replace(/\\s+/g, ' ');
    head.textContent = '🧠 Deep think · ' + q.slice(0, 80) + (q.length > 80 ? '…' : '');
    const body = document.createElement('div'); body.className = 'cn-full';
    const parts = [];
    if (ev.verdict) parts.push('RECOMMENDED: ' + ev.verdict);   // '' = the judge was unavailable
    for (const p of (ev.proposals || [])) parts.push('Perspective (' + ((p && p.lens) || '') + '):\\n' + ((p && p.text) || ''));
    body.textContent = parts.join('\\n\\n');
    d.appendChild(head); d.appendChild(body);
    d.onclick = () => d.classList.toggle('expanded');   // click → expand inline
    return d;
  }
  // Append-or-replace into \`container\`: a 'result' replaces its matching transient 'start' marker
  // in place (the running… line becomes the verdict); no match (or a 'start') → append.
  function placeDeepThinkNote(container, ev) {
    const el = deepThinkNoteEl(ev);
    if (el.dataset.dtPhase === 'result') {
      const match = Array.from(container.querySelectorAll('.dt-note[data-dt-phase="start"]'))
        .find((n) => n.dataset.dtKey === el.dataset.dtKey);
      if (match) { match.replaceWith(el); return el; }
    }
    container.appendChild(el);
    return el;
  }
  function drawerActivityEl(ev) {
    if (ev.type === 'reviewer-note') return reviewerNoteEl(ev.content);   // same marker + eye gating as the main chat
    if (ev.type === 'deep-think') return deepThinkNoteEl(ev);             // 🧠 marker (placeDeepThinkNote handles start→result replacement at the call sites)
    const d = document.createElement('div'); d.className = 'sa-act';
    if (ev.type === 'tool-call') {
      const v = document.createElement('span'); v.className = 'sa-act-verb'; v.textContent = ev.name || ev.verb || 'tool';
      d.appendChild(v); d.appendChild(document.createTextNode(' ' + (ev.summary || ev.argsPreview || ev.path || '')));
    } else if (ev.type === 'tool-progress') { d.textContent = ev.chunk || ''; }
    else if (ev.type === 'tool-result') { d.textContent = '→ ' + (ev.summary || (ev.content || '').slice(0, 200)); }
    else if (ev.type === 'assistant-text' || ev.type === 'assistant-text-chunk') { d.textContent = ev.content || ev.chunk || ''; }
    else if (ev.type === 'acceptance-verdict') { d.className = 'sa-act sa-act-verdict' + (ev.verdict === 'reject' ? ' reject' : ''); d.textContent = \`Verdict: \${ev.verdict === 'reject' ? 'REJECT' : 'APPROVE'}\${ev.rationale ? ' — ' + ev.rationale : ''}\`; }
    else if (ev.type === 'acceptance-feedback') { d.className = 'sa-act sa-act-feedback'; d.textContent = 'Feedback: ' + (ev.feedback || ''); }
    else if (ev.type === 'feedback-round') { d.className = 'sa-act sa-act-feedback'; d.textContent = \`↻ revision round \${ev.round || ''}: \${ev.feedback || ''}\`; }
    else if (ev.type === 'review') { const st = ev.state || 'pending'; d.className = 'sa-act sa-act-review ' + st; d.textContent = \`Review (round \${ev.round || 1}): \${st === 'pending' ? 'reviewing…' : st}\${ev.concerns ? ' — ' + String(ev.concerns).slice(0, 200) : ''}\`; }
    else { d.textContent = (ev.type || '') + ' ' + (ev.content || ev.summary || ''); }
    return d;
  }
  function appendDrawerActivity(ev) {
    if (ev.type === 'reviewer-note') bumpReviewerEyeBadge();   // live arrival only (re-renders don't recount)
    if (ev.type === 'deep-think' && ev.phase === 'result') bumpDeepThinkEyeBadge();   // verdicts count; the transient start doesn't
    const body = document.getElementById('saDrawerBody');
    if (!body) return;
    if (ev.type === 'deep-think') placeDeepThinkNote(body, ev);   // a result replaces its start marker in place
    else body.appendChild(drawerActivityEl(ev));
    body.scrollTop = body.scrollHeight;
  }
  function renderSubagentDetail(id) {
    const sa = saStore.get(id); if (!sa) return;
    const t = document.getElementById('saDrawerTitle'); if (t) t.textContent = sa.topic || sa.id;
    const st = document.getElementById('saDrawerState'); if (st) st.textContent = SA_STATE_LABEL[sa.status] || sa.status || '';
    const dot = document.getElementById('saDrawerDot'); if (dot) dot.className = 'sa-dot ' + (sa.status || 'working');
    const body = document.getElementById('saDrawerBody'); if (!body) return;
    body.innerHTML = '';
    if (sa.status && sa.status !== 'working' && sa.content) {
      const r = document.createElement('div'); r.className = 'sa-drawer-result';
      r.innerHTML = '<div class="sa-card-head">Result</div>' + renderInline(sa.content);
      body.appendChild(r);
    }
    if (!sa.activity.length) {
      const e = document.createElement('div'); e.className = 'sa-act-empty';
      e.textContent = sa.status === 'working' ? 'Working… activity will stream here.' : 'No recorded activity for this sub-agent.';
      body.appendChild(e);
    } else sa.activity.forEach((ev) => (ev.type === 'deep-think' ? placeDeepThinkNote(body, ev) : body.appendChild(drawerActivityEl(ev))));
    body.scrollTop = body.scrollHeight;
  }

  // ── Snapshot fab: one consistent bottom-right icon ───────────────────────
  // Re-parents #snapFab so Snapshot is reachable the same way everywhere:
  //  - a modal is open → dock it into that modal's bottom-right corner
  //  - no modal, on the landing/init screen → fixed viewport bottom-right
  //  - main chat (no modal) → hidden (Snapshot lives behind the Settings gear)
  //  - the Settings modal keeps its own labeled "Snapshot" button → fab hidden
  //  - the Snapshot view itself is open → hidden (you're already in it)
  function dockSnapFab() {
    const fab = document.getElementById('snapFab');
    if (!fab) return;
    if (document.getElementById('modalBg')?.classList.contains('show')) { fab.classList.add('hidden'); return; }
    const open = [...document.querySelectorAll('.modal-bg.show')]
      .filter((m) => m.id !== 'modalBg')
      .sort((a, b) => (parseInt(getComputedStyle(a).zIndex, 10) || 0) - (parseInt(getComputedStyle(b).zIndex, 10) || 0));
    const top = open[open.length - 1];
    if (top) {
      if (top.id === 'settingsModalBg') { fab.classList.add('hidden'); return; }   // settings has its own button
      // What's-new is a read-and-dismiss moment with one primary action; the docked fab's
      // corner position lands exactly on "Got it" and steals its clicks — keep it out.
      if (top.id === 'whatsNewModalBg') { fab.classList.add('hidden'); return; }
      const box = top.querySelector('.modal');
      if (box) { fab.classList.add('in-modal'); fab.classList.remove('hidden'); box.appendChild(fab); return; }
    }
    fab.classList.remove('in-modal');
    document.body.appendChild(fab);
    const chat = document.getElementById('chat');
    fab.classList.toggle('hidden', !!(chat && !chat.classList.contains('hidden')));
  }
  // Re-dock whenever a screen or modal toggles visibility (class/style changes).
  try {
    const _snapObs = new MutationObserver(() => dockSnapFab());
    ['landing', 'initView', 'chat', 'settingsModalBg', 'intakeModalBg', 'initModalBg', 'folderModalBg', 'modalBg', 'whatsNewModalBg', 'commitModalBg']
      .forEach((id) => { const el = document.getElementById(id); if (el) _snapObs.observe(el, { attributes: true, attributeFilter: ['class', 'style'] }); });
    dockSnapFab();
  } catch { /* observer unsupported — fab stays on the viewport */ }

  // ── In-UI file browser (landing) ────────────────────────────────────────
  // Replaces the native PowerShell folder-dialog flow. Server returns subdirs
  // + breadcrumb for any path; we render rows the user can click to navigate
  // into, and a primary button to open the currently-displayed folder as a
  // project. No fs access in the browser — every navigation hop is a single
  // /api/list-dir GET.
  let _browserPath = '';
  // Path-input hybrid state: the breadcrumb bar doubles as a text input.
  let _pathEditing = false;       // bar currently shows the <input>
  let _pathSubmitting = false;    // Enter pressed; suppress blur-cancel while validating
  let _lastCrumbs = { segments: [], isDriveList: false };  // restored on Esc/blur
  // Monotonic token. loadState + SSE init can both fire showLanding on page
  // load, kicking off two concurrent loadBrowser('') calls; a user click
  // mid-flight adds a third. We only apply the result of the LATEST call so
  // older responses can't overwrite newer state.
  let _browserLoadToken = 0;
  async function loadBrowser(target) {
    const myToken = ++_browserLoadToken;
    $dirList.innerHTML = '<li class="dir-empty">Loading…</li>';
    $openHereBtn.disabled = true;
    try {
      const url = '/api/list-dir' + (target ? '?path=' + encodeURIComponent(target) : '');
      const r = await fetch(url);
      if (myToken !== _browserLoadToken) return;
      if (!r.ok) {
        const data = await r.json().catch(() => ({}));
        throw new Error(data.error ?? ('HTTP ' + r.status));
      }
      const data = await r.json();
      if (myToken !== _browserLoadToken) return;
      _browserPath = data.path || '';
      renderBreadcrumb(data.breadcrumb || [], data.isDriveList);
      renderDirList(data.entries || [], data.isDriveList);
      $browserStatus.textContent = _browserPath || '(drives)';
      $browserStatus.title = _browserPath || '';
      // "Open this folder" only enabled when we're inside a real directory
      // (not the drive list).
      $openHereBtn.disabled = !_browserPath;
      $upBtn.disabled = (data.parent === null || data.parent === undefined);
      $upBtn.dataset.parent = data.parent || '';
    } catch (e) {
      if (myToken !== _browserLoadToken) return;
      $dirList.innerHTML = '<li class="dir-empty">Failed to load: ' + escapeHtml(e.message) + '</li>';
      $browserStatus.textContent = target || '(home)';
    }
  }
  function renderBreadcrumb(segments, isDriveList) {
    // Any full re-render leaves path-edit mode — this covers Enter on a valid
    // path (loadBrowser re-renders on success) and plain click-navigation.
    // Reset the edit flag BEFORE clearing innerHTML: removing a focused
    // <input> can fire its blur handler, which must see "not editing".
    _lastCrumbs = { segments, isDriveList };
    _pathEditing = false;
    $breadcrumb.classList.remove('editing', 'path-error', 'bc-hot');
    $breadcrumb.title = '';
    $breadcrumb.innerHTML = '';
    if (isDriveList) {
      const span = document.createElement('span');
      span.textContent = '(pick a drive)';
      $breadcrumb.appendChild(span);
      return;
    }
    segments.forEach((seg, i) => {
      if (i > 0) {
        const sep = document.createElement('span');
        sep.className = 'sep';
        sep.textContent = '›';
        $breadcrumb.appendChild(sep);
      }
      const crumb = document.createElement('span');
      crumb.textContent = seg.name;
      if (i === segments.length - 1) {
        // Current folder: NOT a navigation hop. Clicking it falls through to
        // the bar's click handler and opens the path editor. Keeps the
        // \`crumb\` class so existing selectors keep matching.
        crumb.className = 'crumb crumb-last';
        crumb.title = seg.path + ' — click to edit the path';
      } else {
        crumb.className = 'crumb';
        crumb.title = seg.path;
        crumb.onclick = () => loadBrowser(seg.path);
      }
      $breadcrumb.appendChild(crumb);
    });
  }

  // ── Path-input hybrid: the breadcrumb bar doubles as a text input ───────
  // Clicking anywhere on the bar that is NOT a navigable segment (empty
  // space, separators, the last crumb) swaps the crumbs for a real <input>
  // pre-filled with the full path: Enter navigates (after the server
  // validates the path), Esc/blur cancels back to the breadcrumb unchanged.
  function _overNavigableCrumb(e) {
    const seg = e.target && e.target.closest ? e.target.closest('.crumb') : null;
    return !!(seg && !seg.classList.contains('crumb-last'));
  }
  $breadcrumb.addEventListener('click', (e) => {
    if (_pathEditing) return;
    if (_overNavigableCrumb(e)) return;   // the crumb's own onclick navigates
    enterPathEdit();
  });
  // Hover affordance: highlight the bar ONLY while the pointer is over
  // editable space — never over a navigable crumb (those keep their own
  // link-style hover).
  $breadcrumb.addEventListener('mousemove', (e) => {
    if (_pathEditing) { $breadcrumb.classList.remove('bc-hot'); return; }
    $breadcrumb.classList.toggle('bc-hot', !_overNavigableCrumb(e));
  });
  $breadcrumb.addEventListener('mouseleave', () => $breadcrumb.classList.remove('bc-hot'));
  function enterPathEdit() {
    if (_pathEditing) return;
    _pathEditing = true;
    $breadcrumb.classList.add('editing');
    $breadcrumb.classList.remove('bc-hot', 'path-error');
    $breadcrumb.title = '';
    $breadcrumb.innerHTML = '';
    const input = document.createElement('input');
    input.type = 'text';
    input.className = 'path-input';
    input.value = _browserPath || '';
    input.spellcheck = false;
    input.setAttribute('aria-label', 'Folder path — Enter to go, Esc to cancel');
    input.onkeydown = (e) => {
      if (e.key === 'Enter') { e.preventDefault(); submitPathEdit(input); }
      else if (e.key === 'Escape') {
        // Don't let the document-level Escape handler also fire.
        e.preventDefault(); e.stopPropagation(); cancelPathEdit();
      }
    };
    // Editing again after a rejected path clears the error state.
    input.oninput = () => { $breadcrumb.classList.remove('path-error'); $breadcrumb.title = ''; };
    input.onblur = () => { if (_pathEditing && !_pathSubmitting) cancelPathEdit(); };
    $breadcrumb.appendChild(input);
    input.focus();
    const end = input.value.length;
    try { input.setSelectionRange(end, end); } catch {}
  }
  function cancelPathEdit() {
    if (!_pathEditing) return;
    _pathEditing = false;   // before the re-render: removing the input fires blur
    renderBreadcrumb(_lastCrumbs.segments, _lastCrumbs.isDriveList);
  }
  async function submitPathEdit(input) {
    const typed = (input.value || '').trim();
    if (!typed) { cancelPathEdit(); return; }
    // Validate first WITHOUT leaving edit mode, so a bad path (nonexistent,
    // or a file) becomes an inline error on the bar — the dir list below
    // stays on the current folder instead of showing a failure row.
    _pathSubmitting = true;
    try {
      const r = await fetch('/api/list-dir?path=' + encodeURIComponent(typed));
      if (!r.ok) {
        const data = await r.json().catch(() => ({}));
        pathEditError(input, data.error || ('HTTP ' + r.status));
        return;
      }
    } catch (e) {
      pathEditError(input, e.message || 'cannot reach the server');
      return;
    } finally {
      _pathSubmitting = false;
    }
    // Path is good: navigate through the normal pipeline — its breadcrumb
    // re-render is what exits edit mode.
    loadBrowser(typed);
  }
  function pathEditError(input, msg) {
    $breadcrumb.classList.add('path-error');
    $breadcrumb.title = msg;
    try { input.focus(); } catch {}
  }
  function renderDirList(entries, isDriveList) {
    $dirList.innerHTML = '';
    if (entries.length === 0) {
      $dirList.innerHTML = '<li class="dir-empty">(no subfolders)</li>';
      return;
    }
    for (const e of entries) {
      const li = document.createElement('li');
      li.className = 'dir-item';
      const icon = document.createElement('span');
      icon.className = 'icon';
      // Folder icon for normal entries; drive disc for the Windows drive
      // list. Emoji renders consistently across Windows/macOS/Linux without
      // bundling an icon font.
      icon.textContent = isDriveList ? '💽' : '📁';
      li.appendChild(icon);
      const label = document.createElement('span');
      label.className = 'name';
      label.textContent = e.name;
      li.appendChild(label);
      li.onclick = () => loadBrowser(e.path);
      // Double-click on a dir selects it as the project (OS-picker behavior).
      li.ondblclick = () => {
        loadBrowser(e.path).then(() => openCurrentBrowserDir());
      };
      $dirList.appendChild(li);
    }
  }
  function browserUp() {
    const parent = $upBtn.dataset.parent;
    if (parent === undefined) return;
    loadBrowser(parent);
  }
  function openCurrentBrowserDir() {
    if (!_browserPath) return;
    closeFolderPicker();
    openProject(_browserPath);
  }
  function openFolderPicker() {
    const $bg = document.getElementById('folderModalBg');
    $bg.classList.add('show');
    // Start at the parent of the most recent project, else home dir.
    const start = _lastRecentProject
      ? _lastRecentProject.replace(/[\\\\\\/][^\\\\\\/]+[\\\\\\/]?$/, '')
      : '';
    loadBrowser(start);
  }
  function closeFolderPicker() {
    const $bg = document.getElementById('folderModalBg');
    $bg.classList.remove('show');
  }

  // Init loading indicator: the SAME braille dot-circle spinner + accent
  // coloring the chat status row uses (reuses the SPINNER frames). Ticks at the
  // chat's 120ms cadence while loading; the \`loading\` class drives the accent
  // color + spinner visibility (see #initView .init-status CSS).
  let _initSpinFrame = 0, _initSpinTimer = null;
  function setInitSpinner(on) {
    document.getElementById('initStatusLine')?.classList.toggle('loading', !!on);
    if (on) {
      if (_initSpinTimer) return;
      const glyph = document.getElementById('initSpin');
      _initSpinTimer = setInterval(() => {
        _initSpinFrame = (_initSpinFrame + 1) % SPINNER.length;
        if (glyph) glyph.textContent = SPINNER[_initSpinFrame];
      }, 120);
    } else if (_initSpinTimer) {
      clearInterval(_initSpinTimer);
      _initSpinTimer = null;
    }
  }
  async function openProject(p) {
    // Instant feedback: reveal the init view with a live spinner BEFORE the
    // (server-side git-scan + context-load) /api/open-project round-trip, so
    // selecting a project shows motion immediately instead of a silent hang on
    // the landing screen until the next screen pops in.
    const $init = document.getElementById('initView');
    const $status = document.getElementById('initStatus');
    document.getElementById('initRetryBtn')?.classList.add('hidden');
    closeFolderPicker();
    $landing.classList.add('hidden');
    $chat.classList.add('hidden');
    $init.classList.remove('hidden');
    setInitSpinner(true);
    const name = String(p || '').replace(/[\\\\/]+$/, '').split(/[\\\\/]/).pop() || p;
    $status.textContent = 'Opening ' + name + '…';
    try {
      const r = await fetch('/api/open-project', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ path: p }),
      });
      if (!r.ok) {
        const data = await r.json();
        throw new Error(data.error ?? ('HTTP ' + r.status));
      }
      const data = await r.json();
      // Reset the init gate so a new project re-runs models load (the key
      // and model dropdown may differ per environment).
      _initialized = false;
      _pendingSession = null;
      // Phase B: no open-time git wizard. The "needs a CODE_BOSS
      // branch" prompt fires mid-task, when a write/edit tool needs
      // to touch a file inside a non-CODE_BOSS repo. See the
      // git-needs-branch SSE handler.
      await initialize(p);
    } catch (e) {
      setInitSpinner(false);
      showError('Could not open project', e);
    }
  }

  async function changeProject() {
    if (!confirm('Switch projects? This will clear the current chat.')) return;
    try { await fetch('/api/close-project', { method: 'POST' }); } catch {}
    const r = await fetch('/api/project-state');
    const s = await r.json();
    showLanding(s.recent ?? []);
  }

  function escapeHtml(s) {
    if (s == null) return '';
    return String(s).replace(/[&<>"']/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  }

  // Copy plain text to the clipboard. Prefers the async Clipboard API; falls
  // back to a hidden textarea + execCommand for non-secure contexts (the app is
  // served over plain http on 127.0.0.1, where navigator.clipboard is blocked).
  function copyText(str) {
    const s = String(str == null ? '' : str);
    if (navigator.clipboard && window.isSecureContext) {
      navigator.clipboard.writeText(s).catch(() => fallbackCopy(s));
      return;
    }
    fallbackCopy(s);
  }
  function fallbackCopy(s) {
    try {
      const ta = document.createElement('textarea');
      ta.value = s;
      ta.setAttribute('readonly', '');
      ta.style.position = 'fixed';
      ta.style.top = '-1000px';
      ta.style.opacity = '0';
      document.body.appendChild(ta);
      ta.select();
      ta.setSelectionRange(0, s.length);
      document.execCommand('copy');
      document.body.removeChild(ta);
    } catch { /* best-effort; the affordance was already shown */ }
  }

  function addBubble(role, text) {
    const div = document.createElement('div');
    div.className = 'bubble ' + role;
    // User bubbles use flex layout (\`>\` prefix via ::before, content
    // beside it). Render markdown into a single child so any block
    // elements (h3, ul, …) become one flex item, not many.
    if (role === 'user') {
      const content = document.createElement('div');
      content.className = 'bubble-content';
      content.innerHTML = renderInline(text);
      div.appendChild(content);
      // Hover copy button — copies the RAW message text (the \`text\` arg, the
      // un-rendered source) so you can re-paste it (e.g. to restart an assignment:
      // copy → X the assignment row → New → paste). Works live + on re-render
      // since addBubble is the single funnel for user-message rendering.
      const copyBtn = document.createElement('button');
      copyBtn.type = 'button';
      copyBtn.className = 'bubble-copy';
      copyBtn.textContent = '⧉';
      copyBtn.title = 'Copy message text';
      copyBtn.setAttribute('aria-label', 'Copy message text');
      copyBtn.onclick = (e) => {
        e.stopPropagation();
        copyText(text || '');
        copyBtn.textContent = '✓';
        copyBtn.classList.add('copied');
        copyBtn.title = 'Copied!';
        setTimeout(() => { copyBtn.textContent = '⧉'; copyBtn.classList.remove('copied'); copyBtn.title = 'Copy message text'; }, 1100);
      };
      div.appendChild(copyBtn);
    } else if (role === 'assistant') {
      div.innerHTML = renderInline(text);
    } else {
      div.textContent = text;
    }
    RC.log.appendChild(div);
    // If the status row already exists (SSE 'status: running' often arrives
    // BEFORE the user-message event), keep it pinned to the bottom — without
    // this the spinner can end up above the first user bubble.
    RC.afterRow?.();
    scrollToBottom();
    return div;
  }
  // ── Stick-to-bottom (chat auto-scroll) ───────────────────────────────────
  // Modeled on the use-stick-to-bottom pattern (recompute on scroll, no source-detection needed):
  // auto-scroll the MAIN chat on new content ONLY when the user is within STICK_THRESHOLD of the
  // bottom ("locked"); otherwise surface a floating "jump to latest" arrow. The intake mini-chat
  // (short, modal) always scrolls.
  const STICK_THRESHOLD = 120;   // px from the bottom that still counts as "at the bottom"
  let _stickBottom = true;
  function _atBottom() { const el = _logScrollEl; return (el.scrollHeight - el.scrollTop - el.clientHeight) <= STICK_THRESHOLD; }
  function showScrollArrow() { document.getElementById('scrollBottomBtn')?.classList.add('show'); }
  function hideScrollArrow() { document.getElementById('scrollBottomBtn')?.classList.remove('show'); }
  // Recompute the lock on every scroll (a user scroll-up unlocks; scrolling back to the bottom
  // re-locks). Reaching the bottom clears the "new content below" arrow.
  function updateStickState() { _stickBottom = _atBottom(); if (_stickBottom) hideScrollArrow(); }
  // Arrow click (and message send): jump to the bottom + re-lock. INSTANT, not smooth —
  // the smooth animation fired scroll events of its own, each running updateStickState
  // while still in transit (_atBottom() false), which dropped the lock the click just set;
  // with a stream growing the log the jump then landed short and silently failed.
  function jumpToBottom() {
    _stickBottom = true; hideScrollArrow();
    _logScrollEl.scrollTop = _logScrollEl.scrollHeight;
  }
  function scrollToBottom() {
    if (RC.scroll === _logScrollEl) {              // main chat: honor the lock
      if (_stickBottom) RC.scroll.scrollTop = RC.scroll.scrollHeight;
      else showScrollArrow();                      // new content arrived while scrolled up
      return;
    }
    RC.scroll.scrollTop = RC.scroll.scrollHeight;  // intake mini-chat: always
  }
  // Render assistant text with Claude Code line styling: ⏺ tool calls in
  // coral, ⎿ results dimmed, prose plain.
  function renderAssistant(el, text) {
    el.innerHTML = text.split('\\n').map((line) => {
      const t = line.trimStart();
      if (t.startsWith('⏺')) return '<span class="tool-call">' + escapeHtml(line) + '</span>';
      if (t.startsWith('⎿')) return '<span class="tool-result">' + escapeHtml(line) + '</span>';
      return escapeHtml(line);
    }).join('\\n');
  }

  async function loadModels() {
    // Bounded so a genuinely HUNG connection surfaces an error instead of a
    // frozen init view. But if an unlock is needed, showUnlockModal CANCELS
    // this timer (the call is parked, not hung) and unlock-done retries — so a
    // slow human unlock never aborts it. The timer lives at module scope
    // (_modelsTimer) so the unlock flow can reach it.
    console.log('[loadModels] fetching /api/models');
    const _ac = new AbortController();
    if (_modelsTimer) clearTimeout(_modelsTimer);
    _modelsTimer = setTimeout(() => _ac.abort(), 120_000);
    try {
      const r = await fetch('/api/models', { signal: _ac.signal });
      clearTimeout(_modelsTimer); _modelsTimer = null;
      console.log('[loadModels] response', r.status);
      if (!r.ok) {
        const body = await r.text();
        throw new Error('GET /api/models failed: ' + r.status + ' ' + body.slice(0, 300));
      }
      const data = await r.json();
      const list = Array.isArray(data?.data) ? data.data : Array.isArray(data) ? data : [];
      $model.innerHTML = '';
      if (list.length === 0) {
        $model.innerHTML = '<option>(no models)</option>';
        return;
      }
      for (const m of list) {
        const id = m.id ?? m.name ?? String(m);
        const opt = document.createElement('option');
        opt.value = id;
        opt.textContent = id;
        if (id === SERVER.defaultModel) opt.selected = true;
        $model.appendChild(opt);
      }
      // Success → clear any leftover "Failed to load models" error box (e.g. a
      // retry after the unlock dance finished).
      if ($modalBg.classList.contains('show') && $modalTitle.textContent === 'Failed to load models') closeModal();
    } catch (e) {
      clearTimeout(_modelsTimer); _modelsTimer = null;
      // Aborted while an unlock is still pending? That's not a failure — the
      // call was parked waiting on the user. Stay quiet; unlock-done retries.
      if (e?.name === 'AbortError' && _unlockPending) {
        $model.innerHTML = '<option>(waiting for unlock…)</option>';
        return;
      }
      $model.innerHTML = '<option>(load failed)</option>';
      showError('Failed to load models', e);
    }
  }

  let currentAbort = null;

  // Append a dotted row. kind: 'assistant' | 'tool'. Returns the .body element.
  function addRow(kind, initial) {
    const row = document.createElement('div');
    row.className = 'row ' + kind;
    const dot = document.createElement('span');
    dot.className = 'dot';
    dot.textContent = '●';
    const body = document.createElement('span');
    body.className = 'body';
    if (initial != null) body.textContent = initial;
    row.appendChild(dot);
    row.appendChild(body);
    RC.log.appendChild(row);
    RC.afterRow?.();
    scrollToBottom();
    return body;
  }
  // Per-call live-output buffers. Keyed by tool-call ev.callId. tool-progress
  // events append; tool-result removes the entry. Survives only in memory —
  // a page reload won't restore mid-flight output (the server doesn't persist
  // tool-progress events on purpose; they'd flood the conversation file).
  const _progressBuffers = new Map();
  const PROGRESS_MAX_BYTES = 1024 * 1024;   // 1 MB cap per call

  function addRunningResultRow(callId) {
    const row = document.createElement('div');
    row.className = 'row-result running expandable';
    if (callId) row.dataset.callId = callId;
    row.title = 'click to view live output';
    const sum = document.createElement('span');
    sum.className = 'run-sum';
    sum.textContent = '⎿ Running…';
    row.appendChild(sum);
    let open = false;
    row.addEventListener('click', (e) => {
      if (e.target.tagName === 'PRE') return;   // selecting text in the pre shouldn't collapse
      open = !open;
      row.dataset.open = open ? '1' : '';
      row.title = open ? 'click to collapse' : 'click to view live output';
      let pre = row.querySelector('pre');
      if (open) {
        if (!pre) {
          pre = document.createElement('pre');
          row.appendChild(pre);
        }
        pre.textContent = _progressBuffers.get(callId) || '(no output yet)';
        pre.style.display = '';
        // Scroll the pre to the bottom so the freshest output is visible
        // (matches tail -f intuition for a running command).
        requestAnimationFrame(() => { pre.scrollTop = pre.scrollHeight; });
      } else if (pre) {
        pre.style.display = 'none';
      }
    });
    RC.log.appendChild(row);
    RC.afterRow?.();
    scrollToBottom();
    return row;
  }

  function appendProgress(callId, chunk) {
    if (!callId) return;
    const prev = _progressBuffers.get(callId) || '';
    let next = prev + (chunk || '');
    // Cap the per-call buffer so a runaway build doesn't eat the page's
    // memory. Keeps the LAST PROGRESS_MAX_BYTES — tail is more useful
    // than head for a running build's "what's happening RIGHT NOW".
    if (next.length > PROGRESS_MAX_BYTES) {
      next = '… [earlier output trimmed] …\\n' + next.slice(-PROGRESS_MAX_BYTES);
    }
    _progressBuffers.set(callId, next);
    const placeholder = RC.log.querySelector(\`.row-result.running[data-call-id="\${CSS.escape(callId)}"]\`);
    if (!placeholder) return;
    const sum = placeholder.querySelector('.run-sum');
    if (sum) sum.textContent = \`⎿ Running… \${fmtBytes(next.length)}\`;
    if (placeholder.dataset.open) {
      const pre = placeholder.querySelector('pre');
      if (pre) {
        pre.textContent = next;
        requestAnimationFrame(() => { pre.scrollTop = pre.scrollHeight; });
      }
    }
  }
  function fmtBytes(n) {
    if (n < 1024) return n + ' B';
    if (n < 1024 * 1024) return (n / 1024).toFixed(1) + ' KB';
    return (n / 1024 / 1024).toFixed(2) + ' MB';
  }

  function addResultRow(ev, callId) {
    const row = document.createElement('div');
    row.className = 'row-result' + (ev.status === 'error' ? ' err' : '');
    if (callId) row.setAttribute('data-call-id', callId);
    row.innerHTML = '⎿ ' + escapeHtml(ev.summary);
    if (ev.full && ev.full.length) {
      // No "(expand)" link — clicking anywhere on the row toggles the full
      // output. Clicks inside the expanded <pre> are ignored so the user
      // can select text without collapsing.
      row.classList.add('expandable');
      row.title = 'click to expand';
      let open = false;
      row.addEventListener('click', (e) => {
        if (e.target.tagName === 'PRE') return;  // don't collapse when interacting with the body
        open = !open;
        row.title = open ? 'click to collapse' : 'click to expand';
        let pre = row.querySelector('pre');
        if (open) {
          if (!pre) { pre = document.createElement('pre'); pre.textContent = ev.full; row.appendChild(pre); }
          pre.style.display = '';
        } else if (pre) {
          pre.style.display = 'none';
        }
      });
    }
    // Place the result row directly AFTER the matching tool-call row so
    // the chat log reads "tool X → its result → next tool" instead of
    // every result piling up at the bottom. This matters most for
    // multi-block replies where calls fire interleaved with prose.
    let inserted = false;
    if (callId) {
      const callRow = RC.log.querySelector(\`.row.tool[data-call-id="\${CSS.escape(callId)}"]\`);
      if (callRow) {
        callRow.insertAdjacentElement('afterend', row);
        inserted = true;
      }
    }
    if (!inserted) RC.log.appendChild(row);
    RC.afterRow?.();
    scrollToBottom();
    return row;
  }

  // wl actions that DEPLOY an app (so the result warrants an "Open" button).
  const WL_DEPLOY_ACTIONS = new Set(['deploy', 'd', 'all', 'a', 'quickall', 'qa']);
  // Pull the deployed app name(s) out of a wl args string, handling ' . ' chains
  // like "quickbuild teb . projprops teb . deploy teb" → ["teb"].
  function wlDeployApps(argsStr) {
    const apps = [];
    for (const seg of String(argsStr || '').split(/\\s+\\.\\s+/)) {
      const toks = seg.trim().split(/\\s+/);
      if (toks.length >= 2 && WL_DEPLOY_ACTIONS.has(toks[0].toLowerCase()) && !apps.includes(toks[1])) {
        apps.push(toks[1]);
      }
    }
    return apps;
  }
  // After a SUCCESSFUL wl deploy, attach an "Open" button that opens the app in
  // a NEW browser tab — the URL is constructed server-side (no browser launched
  // during the deploy). Clearly labeled as opening a tab. Disables itself with a
  // reason if the URL can't be constructed (the deploy still succeeded).
  function maybeAttachOpenButton(row, ev) {
    if (!row || (ev.verb || ev.name) !== 'wl' || ev.status === 'error') return;
    const call = (session?.events || []).find((e) => e.type === 'tool-call' && e.callId === ev.callId);
    const apps = wlDeployApps(call?.args?.args || '');
    if (!apps.length) return;
    const bar = document.createElement('div');
    bar.className = 'open-app-bar';
    for (const app of apps) {
      const btn = document.createElement('button');
      btn.className = 'open-app-btn';
      btn.textContent = \`Open \${app} ↗\`;
      btn.title = \`Opens the deployed app "\${app}" in a NEW browser tab (resolves the URL without launching a browser).\`;
      btn.addEventListener('click', async (e) => {
        e.stopPropagation();
        btn.disabled = true;
        try {
          const r = await fetch('/api/app-url?app=' + encodeURIComponent(app));
          const j = await r.json().catch(() => ({}));
          if (j && j.ok && j.url) {
            window.open(j.url, '_blank', 'noopener');
            btn.title = 'Opened ' + j.url;
            btn.disabled = false;
          } else {
            btn.textContent = \`Can't open \${app}\`;
            btn.title = 'Could not construct the URL: ' + (j?.detail || j?.reason || 'unknown') + ' — the deploy still succeeded. Open it manually.';
          }
        } catch (err) {
          btn.disabled = false;
          btn.title = 'Open failed: ' + (err?.message || err);
        }
      });
      bar.appendChild(btn);
    }
    row.appendChild(bar);
  }

  // Render a unified diff as a code block with + / - / hunk highlights.
  // Visible "compacting…" marker (item A) — compaction masks/drops old tasks,
  // a quality cliff, so we surface it rather than doing it silently.
  function addCompactionRow(ev) {
    const row = document.createElement('div');
    row.className = 'compaction-row';
    const pct = ev.budgetPct != null ? \` (\${ev.budgetPct}% of window)\` : '';
    row.textContent = \`↔ Context window full\${pct} — compacting: older tasks are being summarized/archived to free room.\`;
    RC.log.appendChild(row);
    RC.afterRow?.();
    scrollToBottom();
    return row;
  }

  // Result row for a task undo (item F).
  function addTaskUndoRow(ev) {
    const row = document.createElement('div');
    row.className = 'review-row review-ok';
    const ring = document.createElement('span'); ring.className = 'review-ring'; ring.textContent = '↶'; row.appendChild(ring);
    const label = document.createElement('span'); label.className = 'review-label';
    const r = ev.results || [];
    const ok = r.filter((x) => x.status === 'ok').length;
    const skip = r.filter((x) => x.status === 'skipped').length;
    const err = r.filter((x) => x.status === 'error').length;
    label.textContent = \`Task \${ev.taskId || ''} reverted — \${ok} repo(s)\`
      + (skip ? \`, \${skip} skipped\` : '') + (err ? \`, \${err} error(s)\` : '') + '.';
    row.appendChild(label);
    if (r.length) {
      const list = document.createElement('div'); list.className = 'review-concerns';
      list.textContent = r.map((x) => \`\${basename(x.repoDir || '') || 'repo'}: \${x.status}\` + (x.message ? \` — \${x.message}\` : (x.reason ? \` — \${x.reason}\` : ''))).join('\\n');
      row.appendChild(list);
    }
    RC.log.appendChild(row);
    RC.afterRow?.();
    scrollToBottom();
    return row;
  }

  // Revert a closed task's commits (item F). Creates git revert commits.
  async function handleTaskUndo(taskId, btn) {
    if (session?.status === 'running') { showError('Undo', new Error('stop the running chat first')); return; }
    if (!confirm('Revert this task\\'s commits? This creates git revert commits on the affected repo(s) — the rest of your history is untouched.')) return;
    if (btn) { btn.disabled = true; btn.textContent = '↶ reverting…'; }
    try {
      const r = await fetch('/api/task-undo', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ taskId }) });
      const data = await r.json().catch(() => ({}));
      if (!r.ok) throw new Error(data.error || ('HTTP ' + r.status));
    } catch (e) {
      showError('Undo failed', e);
      if (btn) { btn.disabled = false; btn.textContent = '↶ undo'; }
    }
  }

  // Steer the running agent (item H): queue text to inject at the next turn
  // boundary. Returns at once; the text lands as a fresh user row when the
  // agent reaches its next step.
  async function steerCurrentInput() {
    const text = $input.value.trim();
    if (!text) return;
    try {
      const r = await fetch('/api/steer', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ text }) });
      const data = await r.json().catch(() => ({}));
      if (!r.ok) throw new Error(data.error || ('HTTP ' + r.status));
      $input.value = '';
      autoGrowInput();
    } catch (e) { showError('Steer failed', e); }
  }

  // Inline edit of a closed task's summary (item D). Dual-writes via
  // /api/tasks/update-summary (chat event + tier-rollup store).
  function beginEditTaskSummary(foot, summarySpan, ev) {
    if (!ev.id || foot.querySelector('.task-foot-edit')) return;
    const input = document.createElement('input');
    input.type = 'text';
    input.className = 'task-foot-edit';
    input.value = ev.summary || '';
    let done = false;
    const finish = (save) => {
      if (done) return; done = true;
      const val = input.value.trim();
      input.remove();
      summarySpan.style.display = '';
      if (save && val && val !== (ev.summary || '')) {
        ev.summary = val;
        summarySpan.textContent = val;
        fetch('/api/tasks/update-summary', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ eventId: ev.id, summary: val }) })
          .then((r) => { if (!r.ok) return r.json().catch(() => ({})).then((d) => { throw new Error(d.error || ('HTTP ' + r.status)); }); })
          .catch((e) => showError('Summary update failed', e));
      }
    };
    input.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') { e.preventDefault(); finish(true); }
      else if (e.key === 'Escape') { e.preventDefault(); finish(false); }
    });
    input.addEventListener('blur', () => finish(true));
    summarySpan.style.display = 'none';
    foot.insertBefore(input, summarySpan);
    input.focus();
    input.select();
  }

  // Remove ORPHANED streaming prose bubbles whose content is ONLY tool-tag noise — the model's
  // <remote-workspace>/<powershell .../> streams as prose text before the block is parsed, and
  // that bubble never gets a finalize event. Called on each tool-call (flush=false, so a legit
  // bubble still streaming isn't disturbed) AND on terminal status (flush=true to finish renderers).
  function sweepOrphanToolTagBubbles(flush) {
    RC.log.querySelectorAll('.row.assistant[data-streaming="true"]').forEach((r) => {
      if (flush) { try { r._smd?.finish(); } catch {} r._smd = null; }
      const stripped = (r.textContent || '')
        .replace(/<remote-workspace\\b[\\s\\S]*?<\\/remote-workspace>/gi, '')
        .replace(/<\\/?(?:remote-workspace|read|write|edit|list|find|search|info|powershell|next-task|list-tasks|task-detail|search-tasks|remember|forget)\\b[^>]*>/gi, '')
        .replace(/[\\s●.]+/g, '');   // strip the leading marker + whitespace + stray dots
      if (!stripped) r.remove();
    });
  }

  // Strip tool/meta tags from STREAMING prose so raw XML (<remote-workspace>, but also a bare <edit>/
  // <write>/<powershell …> some models emit) never shows while a turn streams — each renders as its
  // own clean tool-call row at turn-end. We cut at the FIRST such tag: within one prose segment the
  // remainder is the tool block. Also drop a trailing partial tag. A "<" followed by a non-tag char
  // (e.g. "a < b", "x<5") is kept. Kept prose only ever GROWS → safe to feed incrementally.
  const _TOOL_TAG_RE = /<(?:remote-workspace|powershell|read|write|edit|list|find|search|info|next-task|task-progress|checklist|list-tasks|task-detail|search-tasks|remember|forget)\\b/i;
  function stripToolTagsLive(text) {
    const m = _TOOL_TAG_RE.exec(text);
    const out = m ? text.slice(0, m.index) : text;
    return out.replace(/<[/a-z][^<>]*$/i, '').replace(/<$/, '');   // drop a trailing partial tag
  }

  // Fold consecutive <remember> tool-calls into ONE collapsible "🧠 remembered N" row
  // (click to expand the individual saves). Breaks the moment a non-remember row intervenes.
  function appendRememberItem(ev, display) {
    let group = RC.log.lastElementChild;
    if (!group || !group.classList.contains('remember-group')) {
      group = document.createElement('div');
      group.className = 'row remember-group collapsed';
      if (ev.taskId) group.dataset.taskId = ev.taskId;
      group._count = 0;
      const hdr = document.createElement('div');
      hdr.className = 'rg-hdr';
      hdr.onclick = () => group.classList.toggle('collapsed');
      const bodyEl = document.createElement('div');
      bodyEl.className = 'rg-body';
      group.appendChild(hdr);
      group.appendChild(bodyEl);
      RC.log.appendChild(group);
    }
    group._count = (group._count || 0) + 1;
    const item = document.createElement('div');
    item.className = 'rg-item';
    item.textContent = String(display || '').replace(/^remember\\(?/, '').replace(/\\)$/, '').trim() || 'memory';
    group.querySelector('.rg-body').appendChild(item);
    group.querySelector('.rg-hdr').innerHTML = \`<span class="rg-icon">🧠</span> remembered \${group._count} \${group._count === 1 ? 'memory' : 'memories'}\`;
    RC.afterRow?.();
  }

  function addDiffBlock(ev) {
    const block = document.createElement('div');
    block.className = 'diff-block';
    if (ev.id) block.dataset.eventId = String(ev.id);

    const hdr = document.createElement('div');
    hdr.className = 'diff-hdr';
    const left = document.createElement('span');
    left.textContent = 'diff ' + (ev.summary || '');
    const exp = document.createElement('span');
    exp.className = 'expand';
    exp.textContent = 'collapse';
    hdr.appendChild(left);
    hdr.appendChild(exp);

    const body = document.createElement('div');
    body.className = 'diff-body';
    // Skip the file-header lines (--- / +++) — the row above already names
    // the file. Hunk markers (@@) and content lines stay.
    const kept = String(ev.diff || '').split('\\n').filter((l) => !l.startsWith('+++') && !l.startsWith('---'));
    const appendDL = (line) => {
      const ln = document.createElement('div');
      ln.className = 'dl';
      if (line.startsWith('@@')) ln.classList.add('hunk');
      else if (line.startsWith('+')) ln.classList.add('add');
      else if (line.startsWith('-')) ln.classList.add('del');
      else ln.classList.add('same');
      ln.textContent = line || ' ';
      body.appendChild(ln);
    };
    // Cap initial render: a multi-thousand-line diff (full-file rewrite of a
    // big generated XML/Java file) would build thousands of DOM nodes — and
    // every event-patch / reload re-renders the whole log. Render the first
    // DIFF_LINE_CAP lines and lazily reveal the rest on click.
    const DIFF_LINE_CAP = 400;
    const head = kept.slice(0, DIFF_LINE_CAP);
    for (const line of head) appendDL(line);
    if (kept.length > DIFF_LINE_CAP) {
      const more = document.createElement('div');
      more.className = 'dl same diff-more';
      more.style.cursor = 'pointer';
      more.textContent = \`… \${kept.length - DIFF_LINE_CAP} more lines (click to expand)\`;
      more.onclick = (e) => {
        e.stopPropagation();
        more.remove();
        for (const line of kept.slice(DIFF_LINE_CAP)) appendDL(line);
      };
      body.appendChild(more);
    }

    // Restore collapse state across event-patch re-renders (item K). The state
    // lives in _diffCollapsed (keyed by event id) because clearLog() wipes the
    // DOM before re-rendering, so it can't be read back off the old element.
    let collapsed = ev.id ? (_diffCollapsed.get(String(ev.id)) || false) : false;
    if (collapsed) { body.classList.add('collapsed'); exp.textContent = 'expand'; }
    exp.onclick = () => {
      collapsed = !collapsed;
      body.classList.toggle('collapsed', collapsed);
      exp.textContent = collapsed ? 'expand' : 'collapse';
      if (ev.id) _diffCollapsed.set(String(ev.id), collapsed);
    };

    block.appendChild(hdr);
    block.appendChild(body);
    // Place the diff directly UNDER its edit's tool row (the last element carrying this callId —
    // the result row), so several edits in a row each show their own diff in place instead of all
    // diffs piling up at the end. Fall back to the log tail if the row isn't found.
    const cid = ev.callId && CSS.escape(String(ev.callId));
    const anchors = cid ? RC.log.querySelectorAll(\`[data-call-id="\${cid}"]\`) : [];
    const anchor = anchors[anchors.length - 1];
    if (anchor && anchor.parentNode) anchor.parentNode.insertBefore(block, anchor.nextSibling);
    else RC.log.appendChild(block);
    RC.afterRow?.();
    scrollToBottom();
    return block;
  }

  // Review event row. State transitions: 'pending' → 'pass' / 'fail' /
  // 'escalate'. The whole .log re-renders on event-patch (see applyDelta
  // case 'event-patch') so this function just builds the current state.
  function addReviewRow(ev) {
    const row = document.createElement('div');
    row.className = 'review-row review-' + (ev.state || 'pending');
    if (ev.round) row.dataset.round = ev.round;

    const ring = document.createElement('span');
    ring.className = 'review-ring';
    // States — chosen so a screenshot still tells you what happened
    // without the animation playing:
    //   pending  → ◌ gray, dotted animated ring
    //   pass     → ✓ green
    //   fail     → ✗ orange/warn (we're going to fix it)
    //   escalate → ⚠ red (we're giving up and surfacing to user)
    if (ev.state === 'pass') ring.textContent = '✓';
    else if (ev.state === 'fail') ring.textContent = '✗';
    else if (ev.state === 'escalate') ring.textContent = '⚠';
    else if (ev.state === 'error') ring.textContent = '⚠';
    else ring.textContent = '◌';
    row.appendChild(ring);

    const label = document.createElement('span');
    label.className = 'review-label';
    if (ev.state === 'pass') label.textContent = \`Review passed (round \${ev.round || 1})\`;
    else if (ev.state === 'fail') label.textContent = \`Review found issues — handing back for fix (round \${ev.round || 1}):\`;
    else if (ev.state === 'escalate') label.textContent = \`Reviewer still has concerns after \${ev.round || 2} attempts — your call:\`;
    else if (ev.state === 'error') label.textContent = ev.concerns || 'Review interrupted.';
    else label.textContent = \`Reviewing (round \${ev.round || 1})…\`;
    row.appendChild(label);

    if (ev.concerns && (ev.state === 'fail' || ev.state === 'escalate')) {
      const concerns = document.createElement('div');
      concerns.className = 'review-concerns';
      // Render through renderInline so backtick/bold/etc. work in the
      // reviewer's prose.
      concerns.innerHTML = renderInline(String(ev.concerns));
      row.appendChild(concerns);
    }

    RC.log.appendChild(row);
    RC.afterRow?.();
    scrollToBottom();
    return row;
  }

  // Render a per-task review verdict INTO the task's footer (the ✓/✗ the user
  // expects to see on the task box). Returns false if no matching task-wrap is
  // present, so the caller can fall back to a standalone row.
  function renderReviewIntoFooter(ev) {
    const wrap = RC.log.querySelector(\`.task-wrap[data-task-id="\${CSS.escape(String(ev.reviewTaskId))}"]\`);
    if (!wrap) return false;
    const foot = wrap.querySelector('.task-foot');
    if (!foot) return false;
    // Re-render safety: drop any prior badge/concerns/undo for this wrap.
    foot.querySelector('.task-foot-review')?.remove();
    foot.querySelector('.task-undo-btn')?.remove();
    wrap.querySelector(':scope > .review-concerns')?.remove();
    const st = ev.state || 'pending';
    const glyph = st === 'pass' ? '✓' : st === 'fail' ? '✗' : (st === 'escalate' || st === 'error') ? '⚠' : '◌';
    const labelByState = {
      pass: 'reviewed', fail: 'review: issues found',
      escalate: 'review: unresolved', error: 'review interrupted', pending: 'reviewing…',
    };
    const badge = document.createElement('span');
    badge.className = 'task-foot-review review-' + st;
    badge.textContent = \`\${glyph} \${labelByState[st] || st}\`;
    if (ev.concerns) badge.title = ev.concerns;
    foot.appendChild(badge);
    // Offer one-click undo when a task's review failed/escalated (item F): its
    // commits can be reverted (git revert) without leaving the UI.
    if ((st === 'fail' || st === 'escalate') && ev.reviewTaskId) {
      const undo = document.createElement('button');
      undo.className = 'task-undo-btn';
      undo.textContent = '↶ undo';
      undo.title = "Revert this task's commits (creates revert commits)";
      undo.onclick = (e) => { e.stopPropagation(); handleTaskUndo(ev.reviewTaskId, undo); };
      foot.appendChild(undo);
    }
    // Surface concerns text under the footer for fail/escalate so the user
    // (and the screenshot) sees what's wrong without hovering.
    if (ev.concerns && (st === 'fail' || st === 'escalate')) {
      const c = document.createElement('div');
      c.className = 'review-concerns';
      c.innerHTML = renderInline(String(ev.concerns));
      wrap.appendChild(c);
    }
    scrollToBottom();
    return true;
  }

  // Inline warning when the task-close auto-commit found a repo that
  // had changes but wasn't on a CODE_BOSS_* branch (so we couldn't
  // commit it). Typically means a tool like powershell touched files
  // outside the pre-write hook's reach.
  function addPendingResolutionRow(ev) {
    const row = document.createElement('div');
    row.className = 'review-row review-fail';
    const ring = document.createElement('span');
    ring.className = 'review-ring';
    ring.textContent = ev.resolved ? '✓' : '⚠';
    row.appendChild(ring);
    const label = document.createElement('span');
    label.className = 'review-label';
    if (ev.resolved) {
      const okCount = (ev.outcomes || []).filter((o) => o.status === 'ok').length;
      const skipCount = (ev.outcomes || []).filter((o) => o.status === 'skipped').length;
      label.textContent = \`Resolved: \${okCount} repo(s) branched + committed, \${skipCount} skipped.\`;
    } else {
      const n = (ev.repos || []).length;
      label.textContent = \`Uncommitted changes in \${n} repo(s) without a CODE_BOSS branch. Click to resolve.\`;
      row.style.cursor = 'pointer';
      row.addEventListener('click', () => showGitPendingResolutionModal(ev));
    }
    row.appendChild(label);
    if (Array.isArray(ev.repos) && ev.repos.length) {
      const list = document.createElement('div');
      list.className = 'review-concerns';
      list.textContent = ev.repos.map((r) => \`\${r.relDir} (\${r.branch}) — \${r.changeCount} change(s)\`).join('\\n');
      row.appendChild(list);
    }
    RC.log.appendChild(row);
    RC.afterRow?.();
    scrollToBottom();
    return row;
  }

  function showGitPendingResolutionModal(ev) {
    if (ev.resolved) return;
    const $bg = document.getElementById('gitWizardBg');
    const $body = document.getElementById('gitWizardBody');
    const repoCards = (ev.repos || []).map((r, i) => \`
      <div class="settings-row" style="display:block; padding: 10px; border: 1px solid var(--border); border-radius: 4px; margin-bottom: 8px;">
        <div style="font-weight: 600; margin-bottom: 4px;">\${escapeHtml(r.relDir)}
          <span style="color: var(--muted); font-weight: normal; font-size: 12px;">on \${escapeHtml(r.branch)} — \${r.changeCount} change(s)</span>
        </div>
        <pre style="margin: 4px 0; padding: 6px 8px; background: var(--panel-alt); border-radius: 3px; max-height: 120px; overflow: auto; font-size: 12px;">\${escapeHtml((r.changes || []).map((c) => \`\${c.status}  \${c.path}\`).join('\\n'))}</pre>
        <label style="display:flex; gap:6px; align-items:center; cursor:pointer; margin-top: 4px;">
          <input type="radio" name="gpr-\${i}" value="codeboss" checked>
          <span>Commit on <code>\${escapeHtml(r.branch)}</code> &amp; fork CODE_BOSS branch</span>
        </label>
        <label style="display:flex; gap:6px; align-items:center; cursor:pointer; margin-left: 18px;">
          <input type="checkbox" name="gpr-ai-\${i}" checked>
          <span>Generate commit message with AI</span>
        </label>
        <textarea name="gpr-msg-\${i}" rows="2" placeholder="Commit message…"
                  style="display:none; width:100%; box-sizing:border-box; margin-top: 4px; padding:6px 8px; border:1px solid var(--border); background:var(--panel-alt); color:var(--fg); border-radius:4px; font:inherit; resize:vertical;"></textarea>
        <label style="display:flex; gap:6px; align-items:center; cursor:pointer; margin-top: 4px;">
          <input type="radio" name="gpr-\${i}" value="skip">
          <span>Skip — leave this repo alone</span>
        </label>
      </div>
    \`).join('');
    $body.innerHTML = \`
      <p style="margin: 0 0 12px; color: var(--fg); font-size: 13px;">
        These repos have uncommitted changes but aren't on a <code>CODE_BOSS_*</code> branch.
        Pick how to handle each one:
      </p>
      \${repoCards}
      <p id="gpr-err" style="margin: 8px 0 0; color: var(--error); font-size: 12px; min-height: 14px;"></p>
      <div style="display:flex; gap:8px; justify-content:flex-end; margin-top: 14px;">
        <button id="gpr-cancel" class="btn">Close</button>
        <button id="gpr-submit" class="btn primary">Apply</button>
      </div>
    \`;
    $bg.classList.add('show');

    // Per-repo AI checkbox toggles its textarea visibility.
    (ev.repos || []).forEach((_r, i) => {
      const $ai = $body.querySelector(\`input[name="gpr-ai-\${i}"]\`);
      const $msg = $body.querySelector(\`textarea[name="gpr-msg-\${i}"]\`);
      const $action = $body.querySelectorAll(\`input[name="gpr-\${i}"]\`);
      function sync() {
        const action = $body.querySelector(\`input[name="gpr-\${i}"]:checked\`)?.value;
        const enabled = action === 'codeboss';
        $ai.disabled = !enabled;
        $msg.style.display = (enabled && !$ai.checked) ? 'block' : 'none';
      }
      $ai.addEventListener('change', sync);
      $action.forEach((r) => r.addEventListener('change', sync));
      sync();
    });

    const $err = document.getElementById('gpr-err');
    const $cancel = document.getElementById('gpr-cancel');
    const $submit = document.getElementById('gpr-submit');
    function close() { $bg.classList.remove('show'); $body.innerHTML = ''; }
    $cancel.onclick = close;
    $submit.onclick = async () => {
      $err.textContent = '';
      const resolutions = (ev.repos || []).map((r, i) => {
        const action = $body.querySelector(\`input[name="gpr-\${i}"]:checked\`)?.value || 'skip';
        const out = { repoDir: r.repoDir, action };
        if (action === 'codeboss') {
          const useAI = $body.querySelector(\`input[name="gpr-ai-\${i}"]\`)?.checked;
          if (useAI) out.useAI = true;
          else {
            const msg = ($body.querySelector(\`textarea[name="gpr-msg-\${i}"]\`)?.value || '').trim();
            if (msg) out.message = msg;
          }
        }
        return out;
      });
      $submit.disabled = true;
      $submit.textContent = 'Applying…';
      try {
        const r = await fetch('/api/git-resolve-batch', {
          method: 'POST',
          headers: { 'content-type': 'application/json' },
          body: JSON.stringify({ eventId: ev.id, resolutions }),
        });
        const data = await r.json();
        if (!r.ok) throw new Error(data.error || ('HTTP ' + r.status));
        const errs = (data.outcomes || []).filter((o) => o.status === 'error');
        if (errs.length) throw new Error(errs.map((e) => \`\${e.repoDir}: \${e.error}\`).join('\\n'));
        close();
        refreshGitDirty();
      } catch (e) {
        $submit.disabled = false;
        $submit.textContent = 'Apply';
        $err.textContent = 'Failed: ' + e.message;
      }
    };
  }

  // ── Image attachments: paste + drag-drop → preview chips → send() ───────
  // Images are read as data URIs and ride the /api/message POST as
  // images:[dataURI,...]. The server threads them to the agent's seeded user
  // message; the openai-http adapter emits them in the wire format discovered
  // by the Settings "Test image support" probe (default: OpenAI content parts).
  let imgPending = [];                       // [{ name, size, type, dataURI }]
  // Discovered-support state (GET /api/image-support at boot; updated by the
  // probe). tested && !format → the probe found NOTHING works: send() strips
  // images with a notice instead of failing the send.
  let imgSupport = { tested: false, format: null };
  const IMG_MAX_BYTES = 4 * 1024 * 1024;     // per-image cap
  const IMG_ACCEPTED_TYPES = ['image/png', 'image/jpeg', 'image/webp', 'image/gif'];

  function imgNote(text) { addBubble('system', text); }
  function imgHumanSize(n) {
    if (n >= 1024 * 1024) return (n / (1024 * 1024)).toFixed(1) + ' MB';
    if (n >= 1024) return Math.round(n / 1024) + ' KB';
    return n + ' B';
  }
  function imgReadAsDataURI(file) {
    return new Promise((resolve, reject) => {
      const fr = new FileReader();
      fr.onload = () => resolve(String(fr.result));
      fr.onerror = () => reject(fr.error || new Error('could not read file'));
      fr.readAsDataURL(file);
    });
  }
  async function imgAttachFiles(files) {
    for (const file of files || []) {
      if (!file) continue;
      if (!IMG_ACCEPTED_TYPES.includes(file.type)) {
        imgNote(\`"\${file.name || 'file'}" not attached — only png, jpeg, webp or gif images can be attached.\`);
        continue;
      }
      if (file.size > IMG_MAX_BYTES) {
        imgNote(\`"\${file.name || 'image'}" not attached — \${imgHumanSize(file.size)} is over the 4 MB per-image limit.\`);
        continue;
      }
      try {
        const dataURI = await imgReadAsDataURI(file);
        imgPending.push({ name: file.name || 'image', size: file.size, type: file.type, dataURI });
      } catch (e) {
        imgNote(\`Could not read "\${file.name || 'image'}": \${e?.message || e}\`);
      }
    }
    imgRenderChips();
  }
  function imgRenderChips() {
    const box = document.getElementById('imgChips');
    if (!box) return;
    box.innerHTML = '';
    box.style.display = imgPending.length ? 'flex' : 'none';
    imgPending.forEach((p, i) => {
      const chip = document.createElement('div');
      chip.className = 'img-chip';
      const thumb = document.createElement('img');
      thumb.src = p.dataURI;
      thumb.alt = p.name;
      const meta = document.createElement('div');
      meta.className = 'ic-meta';
      const name = document.createElement('span');
      name.className = 'ic-name';
      name.textContent = p.name;
      name.title = p.name;
      const size = document.createElement('span');
      size.className = 'ic-size';
      size.textContent = imgHumanSize(p.size);
      meta.appendChild(name); meta.appendChild(size);
      const x = document.createElement('button');
      x.type = 'button';
      x.className = 'ic-x';
      x.textContent = '✕';
      x.title = 'Remove image';
      x.onclick = () => imgRemoveAt(i);
      chip.appendChild(thumb); chip.appendChild(meta); chip.appendChild(x);
      box.appendChild(chip);
    });
  }
  function imgRemoveAt(i) {
    imgPending.splice(i, 1);
    imgRenderChips();
  }
  function imgClearChips() {
    imgPending = [];
    imgRenderChips();
  }
  // Paste into the input: clipboard image files (e.g. a screenshot) become
  // chips. Text pastes are untouched; when image files are present we
  // preventDefault so the browser doesn't also paste a filename.
  $input.addEventListener('paste', (e) => {
    const cd = e.clipboardData;
    if (!cd) return;
    let files = [...(cd.files || [])];
    if (!files.length) {
      files = [...(cd.items || [])]
        .filter((it) => it.kind === 'file')
        .map((it) => it.getAsFile())
        .filter(Boolean);
    }
    const imgs = files.filter((f) => /^image\\//.test(f.type || ''));
    if (!imgs.length) return;                 // plain text paste — leave it alone
    e.preventDefault();
    imgAttachFiles(imgs);
  });
  // Drag-drop anywhere over the chat column (log + input area).
  (() => {
    const zone = document.getElementById('chatMain');
    if (!zone) return;
    let depth = 0;
    const hasFiles = (e) => [...(e.dataTransfer?.types || [])].includes('Files');
    zone.addEventListener('dragenter', (e) => {
      if (!hasFiles(e)) return;
      e.preventDefault();
      depth++;
      zone.classList.add('img-dragover');
    });
    zone.addEventListener('dragover', (e) => {
      if (!hasFiles(e)) return;
      e.preventDefault();                     // required to allow the drop
    });
    zone.addEventListener('dragleave', () => {
      if (depth > 0) depth--;
      if (depth === 0) zone.classList.remove('img-dragover');
    });
    zone.addEventListener('drop', (e) => {
      if (!hasFiles(e)) return;
      e.preventDefault();
      depth = 0;
      zone.classList.remove('img-dragover');
      imgAttachFiles([...(e.dataTransfer?.files || [])]);
    });
  })();
  // Boot: learn the persisted probe verdict (non-blocking; absent = untested).
  async function imgLoadSupport() {
    try {
      const r = await fetchWithTimeout('/api/image-support', {}, 6000);
      const j = await r.json();
      imgSupport = { tested: !!j.tested, format: j.format || null };
      imgUpdateSupportLabel();
    } catch { /* offline — stay optimistic/untested */ }
  }
  function imgUpdateSupportLabel() {
    const el = document.getElementById('imgSupportLabel');
    if (!el) return;
    if (!imgSupport.tested) el.textContent = 'Discover which image format this endpoint accepts';
    else el.textContent = imgSupport.format ? ('images enabled via ' + imgSupport.format) : 'images disabled — no working format found';
  }
  // Settings → "Test image support": empirically try each wire format against
  // the endpoint (server-side, real adapter, magenta test image) and render a
  // per-format ✓/✗ list + summary. The server persists the first working
  // format to image-support.json for the adapter.
  async function probeImages() {
    const btn = document.getElementById('imgProbeBtn');
    const out = document.getElementById('imgProbeOut');
    if (!out) return;
    const model = (document.getElementById('model')?.value) || '';
    out.style.display = 'block';
    out.textContent = 'Testing image formats against ' + (model || '(model)') + ' … up to 3 requests, ~5–90s';
    if (btn) { btn.disabled = true; btn.textContent = 'Testing…'; }
    try {
      const r = await fetch('/api/probe-images', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ model }) });
      const j = await r.json();
      if (!j.ok) { out.textContent = 'Probe failed: ' + (j.error || ('HTTP ' + r.status)); return; }
      imgSupport = { tested: true, format: j.format || null };
      out.textContent = renderImgProbe(j);
      imgUpdateSupportLabel();
    } catch (e) {
      out.textContent = 'Probe error: ' + (e?.message || e);
    } finally {
      if (btn) { btn.disabled = false; btn.textContent = 'Test image support'; }
    }
  }
  function renderImgProbe(j) {
    const pad = (s, n) => { s = String(s); return s.length >= n ? s : s + ' '.repeat(n - s.length); };
    const L = [];
    for (const r of (j.results || [])) {
      L.push((r.ok ? '✓ ' : '✗ ') + pad(r.format, 20) + (r.ok ? ('"' + (r.snippet || '') + '"') : (r.error || ('HTTP ' + (r.status || '?')))));
    }
    L.push('');
    L.push(j.format ? ('images enabled via ' + j.format) : 'no format worked — images disabled');
    return L.join('\\n');
  }

  // Send a user message. Server pushes back the user-message event via SSE,
  // then drives the agent and emits all subsequent events. Browser is a view.
  // fetch() with a hard timeout. A bare fetch to 127.0.0.1 can HANG forever
  // (no error, no resolve) when the browser's ~6-connections-per-origin pool
  // is exhausted — which happens when several code_boss tabs are open (each
  // holds a live SSE connection). Without a timeout, send()/openSettings()
  // just silently do nothing. This turns that into a clear, recoverable error.
  const TOO_MANY_TABS_HINT = 'If you have more than one code_boss tab open, close all but one (each tab holds a live connection and the browser allows only ~6 to 127.0.0.1), then retry.';
  async function fetchWithTimeout(url, opts = {}, ms = 12000) {
    const ctrl = new AbortController();
    const t = setTimeout(() => { try { ctrl.abort(); } catch {} }, ms);
    try { return await fetch(url, { ...opts, signal: ctrl.signal }); }
    finally { clearTimeout(t); }
  }

  async function send() {
    const text = $input.value.trim();
    if (!text) return;
    $input.value = '';
    autoGrowInput();  // shrink the textarea back to one line
    jumpToBottom();   // sending re-locks to the bottom — you want to see your message + the reply
    const discuss = document.getElementById('discussToggle')?.checked;
    const review = document.getElementById('reviewToggle')?.checked;
    const critic = document.getElementById('criticToggle')?.checked;   // adversarial parallel critic (off by default)
    const deepThink = document.getElementById('deepThinkToggle')?.checked;   // deep_think design tool (off by default)
    _lastSendReview = !!review && !discuss;   // a propose run iff review-on and not discuss
    setApproveButton(false);                  // hide the old proposal's approve button while this run goes
    // Attached images ride along as data URIs — unless the discovery probe
    // established that NO format works on this endpoint, in which case strip
    // them with a visible notice instead of failing the send.
    let images = imgPending.map((p) => p.dataURI);
    if (images.length && imgSupport.tested && !imgSupport.format) {
      imgNote('Images are disabled — the image-support probe found no format this endpoint accepts (Settings → Test image support). Sending the text without the image' + (images.length > 1 ? 's' : '') + '.');
      imgClearChips();
      images = [];
    }
    try {
      const r = await fetchWithTimeout('/api/message', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ text, model: $model.value, mode: discuss ? 'discuss' : undefined, review: review || undefined, critic: critic || undefined, deepThink: deepThink || undefined, ...(images.length ? { images } : {}) }),
      }, 15000);
      if (!r.ok) {
        const body = await r.text();
        throw new Error('HTTP ' + r.status + ' — ' + body.slice(0, 400));
      }
      if (images.length) imgClearChips();   // accepted — clear the chips
    } catch (e) {
      $input.value = text;   // restore the input (and keep chips) for retry
      const msg = e?.name === 'AbortError'
        ? new Error('Send timed out — the server did not respond. ' + TOO_MANY_TABS_HINT)
        : e;
      showError('Send failed', msg);
    }
  }

  async function cancelChat() {
    try {
      const r = await fetch('/api/cancel', { method: 'POST' });
      const data = await r.json().catch(() => null);
      // The server found nothing to abort while the UI believes a run is active → say so instead of
      // a silent no-op (a dead Escape otherwise reads as a frozen app).
      if (data?.note && session?.status === 'running') showError('Cancel did nothing', new Error(data.note));
    } catch (e) { showError('Cancel failed', e); }
  }

  // ── Assignment backlog ──────────────────────────────────────────────────
  let _backlog = [];
  async function loadBacklog() {
    try {
      const r = await fetch('/api/assignments/list');
      const data = await r.json();
      _backlog = data.assignments || [];
      renderBacklog(data.activeId || null);
    } catch { /* offline / no project */ }
  }
  function renderBacklog(activeId) {
    const list = document.getElementById('backlogList');
    const count = document.getElementById('backlogCount');
    if (!list) return;
    // The side panel owns its own visibility (shown with the project view); here
    // we only populate the Assignments tab list + its tab-count badge.
    if (count) count.textContent = _backlog.length ? \` (\${_backlog.length})\` : '';
    if (!activeProject) { list.innerHTML = ''; return; }
    list.innerHTML = '';
    if (!_backlog.length) {
      const empty = document.createElement('div');
      empty.className = 'backlog-empty';
      empty.textContent = 'No assignments yet — click ＋ New to capture one.';
      list.appendChild(empty);
      return;
    }
    _backlog.forEach((a, i) => {
      const row = document.createElement('div');
      row.className = 'backlog-row';
      const pill = document.createElement('span');
      pill.className = 'asg-pill ' + (a.status || '');
      pill.textContent = a.status || '';
      const title = document.createElement('span');
      title.className = 'asg-title';
      title.textContent = \`\${a.id}  \${a.title}\`;
      title.title = a.requirement || '(no requirement yet)';
      row.appendChild(pill); row.appendChild(title);
      const mkBtn = (label, fn, t) => { const b = document.createElement('button'); b.textContent = label; if (t) b.title = t; b.onclick = fn; return b; };
      if (i > 0) row.appendChild(mkBtn('▲', () => reorderAssignment(a.id, i - 1), 'Move up'));
      if (i < _backlog.length - 1) row.appendChild(mkBtn('▼', () => reorderAssignment(a.id, i + 1), 'Move down'));
      if (a.requirement && a.status !== 'active' && a.status !== 'accepted') {
        const work = mkBtn('Work this', () => startAssignment(a.id), 'Start working this assignment');
        work.className = 'asg-work';
        row.appendChild(work);
      } else if (a.requirement && a.status === 'active') {
        // An active assignment with no run in flight (e.g. picked up in a new
        // session) — let the user resume it.
        const resume = mkBtn('Resume', () => startAssignment(a.id, true), 'Resume working this assignment');
        resume.className = 'asg-work';
        row.appendChild(resume);
      }
      row.appendChild(mkBtn('✕', () => removeAssignment(a.id), 'Remove'));
      list.appendChild(row);
    });
  }
  function toggleBacklogForm(show) {
    const f = document.getElementById('backlogForm');
    if (f) f.classList.toggle('hidden', show === undefined ? undefined : !show);
  }
  function toggleAssignmentForm() {
    const f = document.getElementById('backlogForm');
    if (f) f.classList.toggle('hidden');
  }
  async function createAssignmentFromForm() {
    const title = (document.getElementById('asgTitle').value || '').trim();
    const requirement = (document.getElementById('asgReq').value || '').trim();
    if (!title) { showError('New assignment', new Error('title is required')); return; }
    try {
      await fetch('/api/assignments/create', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ title, requirement: requirement || null, status: requirement ? 'ready' : 'capturing' }),
      });
      document.getElementById('asgTitle').value = '';
      document.getElementById('asgReq').value = '';
      toggleAssignmentForm();
      loadBacklog();
    } catch (e) { showError('Create assignment failed', e); }
  }

  // ── New-assignment intake modal (the "+" button) ──────────────────────────
  // Two panes: editable fields (left) + a read-only mini-chat manager (right)
  // that refines those fields. Not the worker; no files edited, no run.
  let _intakeMessages = [];
  // The intake mini-chat is a createChatComponent INSTANCE — it shares the SAME
  // renderer (renderEvent), the SAME status spinner, and the SAME input behavior
  // (auto-grow + Enter→send) as the main chat. Only its TRANSPORT differs: the
  // NDJSON /api/intake-chat adapter in sendIntakeChat below (vs the main chat's
  // SSE). #im-log is its render context; #im-input its composer; the form on the
  // left is its only bespoke extra. No server-pushed context/progress signals,
  // so showContextBar:false.
  const intakeChat = createChatComponent({
    rc: { log: document.getElementById('im-log'), scroll: document.getElementById('im-log'), isIntake: true },
    inputEl: document.getElementById('im-input'),
    showContextBar: false,
    onSubmit: () => sendIntakeChat(),
  });
  const imRender = (ev) => intakeChat.render(ev);
  function openIntakeModal() {
    _intakeMessages = [];
    for (const id of ['im-title', 'im-req', 'im-acc', 'im-input']) { const el = document.getElementById(id); if (el) el.value = ''; }
    intakeChat.spinner.reset();
    intakeChat.rc.log.innerHTML = '';
    imRender({ type: 'assistant-text', content: 'Tell me what you want done — paste a ticket/email or just describe it. I\\'ll read the project (read-only) and draft the fields on the left. You can also edit them yourself; hit Add when it looks right.' });
    document.getElementById('intakeModalBg').classList.add('show');
    setTimeout(() => document.getElementById('im-input')?.focus(), 50);
  }
  function closeIntakeModal() { document.getElementById('intakeModalBg')?.classList.remove('show'); }
  async function sendIntakeChat() {
    const inp = document.getElementById('im-input');
    const text = (inp.value || '').trim();
    if (!text) return;
    inp.value = ''; autoGrowEl(inp);
    imRender({ type: 'user-message', content: text });
    _intakeMessages.push({ role: 'user', content: text });
    intakeChat.spinner.setUploading();   // same animated spinner as the main chat
    intakeChat.spinner.startTick();
    const assistantParts = [];
    try {
      const fields = {
        title: document.getElementById('im-title').value,
        requirement: document.getElementById('im-req').value,
        acceptanceCriteria: document.getElementById('im-acc').value,
      };
      const r = await fetch('/api/intake-chat', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ messages: _intakeMessages, fields, model: $model.value }),
      });
      // NDJSON stream: one event per line, rendered live through the shared renderer.
      const reader = r.body.getReader(); const dec = new TextDecoder(); let buf = '';
      const handle = (ev) => {
        if (ev.type === 'intake-done') {
          if (ev.card) {
            if (ev.card.title) document.getElementById('im-title').value = ev.card.title;
            if (ev.card.requirement) document.getElementById('im-req').value = ev.card.requirement;
            if (ev.card.acceptanceCriteria) document.getElementById('im-acc').value = ev.card.acceptanceCriteria;
          }
          return;
        }
        if (ev.type === 'tokens') { intakeChat.spinner.applyTokens(ev.total || 0, 0); return; }
        if (ev.type === 'assistant-text') assistantParts.push(ev.content || '');
        imRender(ev);   // the spinner re-pins itself to the bottom via rc.afterRow
      };
      for (;;) {
        const { done, value } = await reader.read();
        if (done) break;
        buf += dec.decode(value, { stream: true });
        let nl;
        while ((nl = buf.indexOf('\\n')) >= 0) {
          const line = buf.slice(0, nl).trim(); buf = buf.slice(nl + 1);
          if (!line) continue;
          let ev; try { ev = JSON.parse(line); } catch { continue; }
          handle(ev);
        }
      }
      if (assistantParts.length) _intakeMessages.push({ role: 'assistant', content: assistantParts.join('\\n\\n') });
    } catch (e) {
      imRender({ type: 'assistant-text', content: 'Error: ' + (e?.message || e) });
    } finally {
      intakeChat.spinner.setDone();   // persists the final ↑tokens · Ns row, like the main chat
      intakeChat.spinner.stopTick();
    }
  }
  async function addFromIntake() {
    const requirement = (document.getElementById('im-req').value || '').trim();
    if (!requirement) { showError('Add assignment', new Error('a requirement is required — describe it or let the manager draft it')); return; }
    try {
      const r = await fetch('/api/assignments/create', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({
          title: (document.getElementById('im-title').value || '').trim() || 'Untitled assignment',
          requirement,
          acceptanceCriteria: (document.getElementById('im-acc').value || '').trim() || null,
          status: 'ready',
        }),
      });
      if (!r.ok) { const er = await r.json().catch(() => ({})); throw new Error(er.error || ('HTTP ' + r.status)); }
      closeIntakeModal();
      selectSideTab('assignments');
      loadBacklog();
    } catch (e) { showError('Add assignment failed', e); }
  }
  async function startAssignment(id, resume) {
    if (session?.status === 'running') { showError('Start assignment', new Error('a chat is already running')); return; }
    try {
      const r = await fetch('/api/assignments/start', {
        method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id, resume: !!resume }),
      });
      const data = await r.json();
      if (!r.ok) throw new Error(data.error || ('HTTP ' + r.status));
      // Kick the worker with the seed message (the run is already bound to the assignment).
      await fetch('/api/message', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ text: data.seedText, model: $model.value }),
      });
    } catch (e) { showError('Start assignment failed', e); }
  }
  async function reorderAssignment(id, toIndex) {
    try {
      await fetch('/api/assignments/reorder', {
        method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id, toIndex }),
      });
    } catch (e) { showError('Reorder failed', e); }
  }
  async function removeAssignment(id) {
    // Verify before deleting — removing an assignment permanently drops its
    // backlog record (requirement, sources, acceptance criteria, verdicts, and
    // its task/commit links). It does NOT touch your conversation, the tasks,
    // or any git commits the work produced — and it can't be undone.
    const a = _backlog.find((x) => x.id === id) || {};
    const label = a.title ? \`\${id} "\${a.title}"\` : id;
    const taskN = Array.isArray(a.taskIds) ? a.taskIds.length : 0;
    const commitN = Array.isArray(a.commitHashes) ? a.commitHashes.length : 0;
    const workNote = (taskN || commitN)
      ? \`\\n\\nThis assignment has \${taskN} task(s) and \${commitN} commit(s) bound to it — deleting removes those links only (the commits and chat history remain).\`
      : '';
    if (!confirm(\`Delete assignment \${label}?\\n\\nThis permanently removes its requirement, sources, and acceptance criteria from the backlog. Your conversation and any commits it produced are NOT affected, and this can't be undone.\${workNote}\`)) return;
    // If we're removing the draft an open intake thread points at, drop the
    // pointer so the next Send doesn't 404 against a deleted assignment.
    if (id === __intakeAssignmentId) __intakeAssignmentId = null;
    try {
      await fetch('/api/assignments/remove', {
        method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id }),
      });
    } catch (e) { showError('Remove failed', e); }
  }
  async function gateAction(id, action, node) {
    try {
      const r = await fetch('/api/assignments/' + action, {
        method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id }),
      });
      if (!r.ok) throw new Error('HTTP ' + r.status);
      if (node) { const a = node.querySelector('.gate-actions'); if (a) a.innerHTML = \`<span class="gate-line">\${action === 'accept' ? 'Accepted ✓' : 'Paused'}</span>\`; }
      if (action === 'accept') offerNextReady(node);
    } catch (e) { showError('Assignment ' + action + ' failed', e); }
  }
  // After accepting an assignment, surface the next ready one so the user can
  // chain into it without hunting the backlog.
  async function offerNextReady(node) {
    if (!node) return;
    try {
      const data = await (await fetch('/api/assignments/list')).json();
      const next = (data.assignments || []).find((a) => a.status === 'ready' && a.requirement);
      const actions = node.querySelector('.gate-actions');
      if (!next || !actions) return;
      const line = document.createElement('span');
      line.className = 'gate-line';
      line.textContent = \` Next ready: \${next.id} \${next.title} \`;
      const work = document.createElement('button');
      work.className = 'btn primary';
      work.textContent = 'Work it';
      work.onclick = () => startAssignment(next.id);
      actions.appendChild(line);
      actions.appendChild(work);
    } catch { /* best-effort */ }
  }
  function acceptAssignment(id, node) { return gateAction(id, 'accept', node); }
  function parkAssignment(id, node) { return gateAction(id, 'park', node); }

  function addAssignmentGate(ev) {
    const row = document.createElement('div');
    // An open escalation (a bound task whose review never resolved) makes the
    // gate a WARN even if the acceptance check said fulfilled — acceptance no
    // longer silently overrides the per-task reviewer (item J).
    const escalations = (ev.taskVerdicts || []).filter((v) => v.state === 'escalate');
    const hasEscalations = escalations.length > 0;
    row.className = 'row assignment-gate ' + (ev.fulfilled && !hasEscalations ? 'ok' : 'warn');
    const title = document.createElement('div');
    title.className = 'gate-title';
    if (hasEscalations) {
      title.textContent = \`⚠ Assignment \${ev.assignmentId}: \${escalations.length} task(s) have UNRESOLVED review concerns — review before accepting\`;
    } else {
      title.textContent = ev.fulfilled
        ? \`✓ Assignment \${ev.assignmentId}: acceptance check passed — does it meet the requirement?\`
        : \`⚠ Assignment \${ev.assignmentId}: not yet fulfilled\`;
    }
    row.appendChild(title);
    const line = (cls, text) => { if (!text) return; const d = document.createElement('div'); d.className = 'gate-line ' + cls; d.textContent = text; row.appendChild(d); };
    for (const v of escalations) line('escalation', \`Task \${v.taskId}: \${String(v.concerns || '(no detail)').slice(0, 200)}\`);
    line('', ev.rationale || '');
    line('missing', ev.missing ? ('Missing: ' + ev.missing) : '');
    line('drift', ev.drift ? ('Drift (out of scope): ' + ev.drift) : '');
    const actions = document.createElement('div');
    actions.className = 'gate-actions';
    // When unfulfilled, the primary action is to send the gaps back to the
    // worker; Accept is still offered (the user may accept despite the gap).
    if (!ev.fulfilled) {
      const fix = document.createElement('button'); fix.className = 'btn primary'; fix.textContent = 'Send fix to worker';
      fix.onclick = () => sendFixToWorker(ev.assignmentId, row);
      actions.appendChild(fix);
    }
    const accept = document.createElement('button');
    accept.className = 'btn' + (ev.fulfilled && !hasEscalations ? ' primary' : '');
    accept.textContent = hasEscalations ? 'Accept anyway' : 'Accept';
    // Require an explicit confirm to accept over an open escalation.
    accept.onclick = () => {
      if (hasEscalations && !confirm(\`\${escalations.length} task(s) still have unresolved review concerns. Accept the assignment anyway?\`)) return;
      acceptAssignment(ev.assignmentId, row);
    };
    const park = document.createElement('button'); park.className = 'btn'; park.textContent = 'Pause'; park.onclick = () => parkAssignment(ev.assignmentId, row);
    actions.appendChild(accept); actions.appendChild(park);
    row.appendChild(actions);
    RC.log.appendChild(row);
    return row;
  }
  async function sendFixToWorker(id, node) {
    if (session?.status === 'running') { showError('Send fix', new Error('a chat is already running')); return; }
    try {
      const r = await fetch('/api/assignments/send-fix', {
        method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id }),
      });
      const data = await r.json();
      if (!r.ok) throw new Error(data.error || ('HTTP ' + r.status));
      if (node) { const a = node.querySelector('.gate-actions'); if (a) a.innerHTML = '<span class="gate-line">Sent back to the worker…</span>'; }
      // Kick the worker with the gap-seeded fix message (run is already armed).
      await fetch('/api/message', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ text: data.seedText, model: $model.value }),
      });
    } catch (e) { showError('Send fix failed', e); }
  }

  function addAssignmentBanner(ev) {
    const row = document.createElement('div');
    row.className = 'row assignment-banner';
    row.textContent = \`▸ Working assignment \${ev.assignmentId}\${ev.title ? ': ' + ev.title : ''}\`;
    RC.log.appendChild(row);
    scrollToBottom();
    return row;
  }
  function addManagerDiagnostic(ev) {
    // A read-only manager pass (reviewer/acceptance/intake/consolidation) could
    // not parse its expected tag and fell back to its safe default. Surface the
    // raw model output (collapsed) so prompts can be tuned to the deployed model.
    const row = document.createElement('div');
    row.className = 'row manager-diagnostic';
    const det = document.createElement('details');
    const sum = document.createElement('summary');
    sum.textContent = \`⚠ \${ev.kind || 'manager'}: \${ev.reason || 'parse miss'} — show raw model output\`;
    det.appendChild(sum);
    const pre = document.createElement('pre');
    pre.className = 'md-raw';
    pre.textContent = ev.raw || '(empty)';
    det.appendChild(pre);
    row.appendChild(det);
    RC.log.appendChild(row);
    scrollToBottom();
    return row;
  }
  function addMemoryConsolidated(ev) {
    const added = Array.isArray(ev.added) ? ev.added : [];
    const row = document.createElement('div');
    row.className = 'row memory-consolidated';
    const head = document.createElement('div');
    head.className = 'mc-head';
    head.textContent = \`🧠 Recorded \${added.length} codebase fact\${added.length === 1 ? '' : 's'} from \${ev.assignmentId} into memory\`;
    row.appendChild(head);
    for (const f of added) {
      const line = document.createElement('div');
      line.className = 'mc-line';
      line.textContent = \`[\${f.category || 'general'}] \${f.note}\`;
      row.appendChild(line);
    }
    RC.log.appendChild(row);
    scrollToBottom();
    return row;
  }

  // ── Conversational intake ─────────────────────────────────────────────────
  let __intakeAssignmentId = null;   // the draft assignment the intake thread targets
  function toggleIntakeForm() {
    const f = document.getElementById('intakeForm');
    if (!f) return;
    f.classList.toggle('hidden');
    if (!f.classList.contains('hidden')) { const ta = document.getElementById('intakeText'); if (ta) ta.focus(); }
  }
  // Fresh capture (the "Chat" button): start a NEW draft, never continue a stale
  // one left over from a prior cancel/session.
  function openIntake() {
    __intakeAssignmentId = null;
    const f = document.getElementById('intakeForm');
    if (f) f.classList.remove('hidden');
    const ta = document.getElementById('intakeText'); if (ta) { ta.value = ''; ta.focus(); }
  }
  // Cancel: abort the in-flight manager turn + let the server reap a never-carded
  // '(capturing…)' draft (best-effort, fire-and-forget — closing must never block
  // on the network), clear the busy spinner, hide the form, AND drop the draft
  // pointer so the next capture is fresh.
  function closeIntake() {
    const cancelId = __intakeAssignmentId;
    try {
      fetch('/api/assignments/intake-cancel', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ assignmentId: cancelId || undefined }),
      }).catch(() => {});
    } catch {}
    setIntakeBusy(false, '');
    __intakeAssignmentId = null;
    const f = document.getElementById('intakeForm');
    if (f) f.classList.add('hidden');
  }
  function startIntake() {
    // Used by "Refine" — caller sets __intakeAssignmentId first; just ensure shown.
    const f = document.getElementById('intakeForm');
    if (f && f.classList.contains('hidden')) toggleIntakeForm();
  }
  function setIntakeBusy(busy, msg) {
    const btn = document.getElementById('intakeSendBtn');
    const st = document.getElementById('intakeStatus');
    if (btn) btn.disabled = busy;
    if (st) st.textContent = msg !== undefined ? msg : (busy ? 'Manager is thinking…' : '');
  }
  async function sendIntake() {
    const ta = document.getElementById('intakeText');
    const text = (ta?.value || '').trim();
    if (!text) return;
    setIntakeBusy(true);
    try {
      const r = await fetch('/api/assignments/intake', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ text, assignmentId: __intakeAssignmentId || undefined, model: $model.value }),
      });
      const data = await r.json();
      if (!r.ok) throw new Error(data.error || ('HTTP ' + r.status));
      __intakeAssignmentId = data.assignmentId;
      if (ta) ta.value = '';
    } catch (e) {
      setIntakeBusy(false, '');
      showError('Intake failed', e);
    }
  }
  function addIntakeMessage(ev) {
    // Clear the busy spinner only on the MANAGER's reply — not on the user-echo
    // event the server broadcasts before the manager turn even starts.
    if (ev.senderRole === 'manager') setIntakeBusy(false, '');
    const div = addBubble(ev.senderRole === 'manager' ? 'assistant' : 'user', ev.content || '');
    if (ev.senderRole === 'manager') div.classList.add('intake-manager');
    return div;
  }
  function addEditableRequirementCard(ev) {
    setIntakeBusy(false, '');
    const card = ev.card || {};
    const row = document.createElement('div');
    row.className = 'row requirement-card';
    row.dataset.assignmentId = ev.assignmentId || '';
    const head = document.createElement('div');
    head.className = 'rc-title';
    head.textContent = \`Requirement card — \${ev.assignmentId || ''} (edit, then Capture)\`;
    row.appendChild(head);
    const field = (labelText, value, tag, cls, rows) => {
      const label = document.createElement('label'); label.textContent = labelText; row.appendChild(label);
      const el = document.createElement(tag);
      el.className = cls;
      if (tag === 'textarea') el.rows = rows || 2;
      el.value = value || '';
      row.appendChild(el);
      return el;
    };
    const titleEl = field('Title', card.title, 'input', 'rc-field-title');
    const reqEl = field('Requirement (definition of done)', card.requirement, 'textarea', 'rc-field-requirement', 3);
    const accEl = field('Acceptance criteria', card.acceptanceCriteria, 'textarea', 'rc-field-acceptance', 3);
    const actions = document.createElement('div'); actions.className = 'rc-actions';
    const capture = document.createElement('button'); capture.className = 'btn primary'; capture.textContent = 'Capture';
    capture.onclick = async () => {
      const requirement = (reqEl.value || '').trim();
      if (!requirement) { showError('Capture', new Error('requirement is required')); return; }
      try {
        const r = await fetch('/api/assignments/capture', {
          method: 'POST', headers: { 'content-type': 'application/json' },
          body: JSON.stringify({ id: ev.assignmentId, title: (titleEl.value || '').trim(), requirement, acceptanceCriteria: (accEl.value || '').trim() }),
        });
        if (!r.ok) { const d = await r.json().catch(() => ({})); throw new Error(d.error || ('HTTP ' + r.status)); }
        row.classList.add('captured');
        actions.innerHTML = '<span class="intake-status">Captured ✓ — added to the backlog as ready</span>';
        __intakeAssignmentId = null;
        loadBacklog();
      } catch (e) { showError('Capture failed', e); }
    };
    const refine = document.createElement('button'); refine.className = 'btn'; refine.textContent = 'Refine';
    refine.onclick = () => { __intakeAssignmentId = ev.assignmentId; startIntake(); };
    const discard = document.createElement('button'); discard.className = 'btn'; discard.textContent = 'Discard';
    discard.onclick = async () => {
      try { await fetch('/api/assignments/remove', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id: ev.assignmentId }) }); }
      catch (e) { showError('Discard failed', e); return; }
      row.classList.add('captured');
      actions.innerHTML = '<span class="intake-status">Discarded</span>';
      __intakeAssignmentId = null;
      loadBacklog();
    };
    actions.appendChild(capture); actions.appendChild(refine); actions.appendChild(discard);
    row.appendChild(actions);
    RC.log.appendChild(row);
    scrollToBottom();
    return row;
  }

  // ── Codebase memory (Tier-1) viewer / editor ──────────────────────────────
  const MEM_CATEGORIES = ['architecture', 'conventions', 'gotchas', 'where-things-live', 'build-deploy', 'general'];
  // Memory renders into the side panel's Memory tab (no modal).
  function loadMemory() {
    fetch('/api/memory')
      .then((r) => r.json())
      .then((d) => renderMemoryEditor(d.entries || []))
      .catch((e) => showError('Load memory failed', e));
  }
  function openMemory() { selectSideTab('memory'); }   // legacy callers → open the tab
  function closeMemory() { /* no modal anymore — no-op */ }
  // Per category: a box styled like ONE textarea whose lines are forced bullets.
  // Each bullet is its own invisible auto-growing input = one memory entry.
  // Enter → new bullet; Backspace at start → merge up; arrows hop between
  // bullets at the edges; everything debounce-autosaves. No buttons.
  function renderMemoryEditor(entries) {
    const list = document.getElementById('memoryList');
    if (!list) return;
    const byCat = {};
    for (const c of MEM_CATEGORIES) byCat[c] = [];
    for (const e of (entries || [])) {
      const c = MEM_CATEGORIES.includes(e.category) ? e.category : 'general';
      const note = (e.note || '').trim();
      if (note) byCat[c].push(note);
    }
    list.innerHTML = '';
    for (const c of MEM_CATEGORIES) {
      const sec = document.createElement('div'); sec.className = 'mem-cat';
      const lab = document.createElement('div'); lab.className = 'mem-cat-label'; lab.textContent = c;
      const box = document.createElement('div'); box.className = 'bullet-box'; box.dataset.cat = c;
      const notes = byCat[c].length ? byCat[c] : [''];   // always one (empty) bullet to type into
      for (const n of notes) box.appendChild(makeBullet(n));
      sec.appendChild(lab); sec.appendChild(box);
      list.appendChild(sec);
    }
    list.querySelectorAll('.b-input').forEach(autogrowBullet);
    // Delegated handlers (the #memoryList element persists across renders).
    if (!list._memWired) {
      list.addEventListener('input', (e) => { if (e.target.classList.contains('b-input')) { autogrowBullet(e.target); scheduleMemorySave(); } });
      list.addEventListener('keydown', onBulletKeydown);
      list._memWired = true;
    }
  }
  function makeBullet(text) {
    const row = document.createElement('div'); row.className = 'bullet';
    const dash = document.createElement('span'); dash.className = 'dash'; dash.textContent = '–';
    const ta = document.createElement('textarea'); ta.className = 'b-input'; ta.rows = 1; ta.value = text || '';
    row.appendChild(dash); row.appendChild(ta);
    return row;
  }
  function autogrowBullet(ta) { ta.style.height = 'auto'; ta.style.height = ta.scrollHeight + 'px'; }
  function focusBullet(ta, pos) { if (!ta) return; ta.focus(); const p = (pos == null) ? ta.value.length : pos; try { ta.setSelectionRange(p, p); } catch {} }
  function onBulletKeydown(e) {
    const ta = e.target;
    if (!ta.classList || !ta.classList.contains('b-input')) return;
    const box = ta.closest('.bullet-box');
    const row = ta.closest('.bullet');
    const inputs = Array.from(box.querySelectorAll('.b-input'));
    const idx = inputs.indexOf(ta);
    const atStart = ta.selectionStart === 0 && ta.selectionEnd === 0;
    const atEnd = ta.selectionStart === ta.value.length && ta.selectionEnd === ta.value.length;
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();                                  // Enter = new bullet (shift+Enter = newline within a bullet)
      const after = ta.value.slice(ta.selectionStart);
      ta.value = ta.value.slice(0, ta.selectionStart); autogrowBullet(ta);
      const nb = makeBullet(after); row.after(nb);
      const ni = nb.querySelector('.b-input'); autogrowBullet(ni); focusBullet(ni, 0);
      scheduleMemorySave();
    } else if (e.key === 'Backspace' && atStart && idx > 0) {
      e.preventDefault();                                  // merge into the previous bullet
      const prev = inputs[idx - 1]; const joinAt = prev.value.length;
      prev.value = prev.value + ta.value; row.remove();
      autogrowBullet(prev); focusBullet(prev, joinAt);
      scheduleMemorySave();
    } else if ((e.key === 'ArrowRight' || e.key === 'ArrowDown') && atEnd && idx < inputs.length - 1) {
      e.preventDefault(); focusBullet(inputs[idx + 1], 0);
    } else if ((e.key === 'ArrowLeft' || e.key === 'ArrowUp') && atStart && idx > 0) {
      e.preventDefault(); focusBullet(inputs[idx - 1]);
    }
  }
  let _memSaveTimer = null;
  function scheduleMemorySave() {
    clearTimeout(_memSaveTimer);
    _memSaveTimer = setTimeout(saveMemory, 800);   // debounce while typing
  }
  async function saveMemory() {
    const list = document.getElementById('memoryList');
    if (!list) return;
    const entries = [];
    for (const box of list.querySelectorAll('.bullet-box[data-cat]')) {
      const cat = box.dataset.cat;
      for (const ta of box.querySelectorAll('.b-input')) {
        const note = ta.value.trim();
        if (note) entries.push({ category: cat, note });
      }
    }
    try {
      const r = await fetch('/api/memory/replace', {
        method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ entries }),
      });
      if (!r.ok) { const d = await r.json().catch(() => ({})); throw new Error(d.error || ('HTTP ' + r.status)); }
      flashMemSaved();
    } catch (e) { showError('Save memory failed', e); }
  }
  function flashMemSaved() {
    const el = document.getElementById('memSaved');
    if (!el) return;
    el.classList.add('show');
    clearTimeout(el._fadeT);
    el._fadeT = setTimeout(() => el.classList.remove('show'), 1300);
  }

  // Minimal markdown rendering for assistant text. Supports:
  //   - \`inline code\`
  //   - **bold**
  //   - # header   (## and ### map to smaller headers)
  //   - - bullet / * bullet
  //   - GitHub-flavored tables (header row + |---| delimiter row, optional
  //     :--- / ---: / :---: alignment; leading/trailing pipes optional)
  // Anything else passes through with newlines preserved by the body's
  // white-space: pre-wrap. Block elements (h, ul, li, table) carry their own
  // line breaks so we omit \\n around them when reassembling.
  // Split a table row into trimmed cells: strip the optional outer pipes, then
  // split on unescaped '|' (so an escaped \\| stays literal inside a cell).
  function _mdSplitRow(s) {
    let t = s.trim();
    if (t.startsWith('|')) t = t.slice(1);
    if (t.endsWith('|')) t = t.slice(0, -1);
    return t.split(/(?<!\\\\)\\|/).map((c) => c.replace(/\\\\\\|/g, '|').trim());
  }
  // A delimiter row is all cells matching :?-+:? (the --- separator line).
  function _mdIsDelimRow(s) {
    if (!s || s.indexOf('|') === -1 && !/^[\\s:|-]+$/.test(s)) return false;
    const cells = _mdSplitRow(s);
    return cells.length > 0 && cells.every((c) => /^:?-{1,}:?$/.test(c));
  }
  function _mdAlign(delimCell) {
    const c = (delimCell || '').trim();
    const l = c.startsWith(':'), r = c.endsWith(':');
    return (l && r) ? 'center' : r ? 'right' : l ? 'left' : '';
  }
  function renderInline(text) {
    // Protect code FIRST so the Markdown block/inline transforms below never
    // re-parse text inside backticks — a code line like \`# Comment\` must stay
    // literal, not become a heading; \`- x\` must not become a bullet; \`a | b\`
    // must not become a table; \`**x**\` must not become bold. Extract fenced
    // blocks + inline spans into inert NUL-delimited placeholders (with their
    // contents HTML-escaped), run the normal transforms, then restore.
    const codeStore = [];
    const PH = (i) => \`\\u0000\${i}\\u0000\`;   // sentinel: no #,-,*,|,\`,<,> so no transform matches it
    let work = String(text == null ? '' : text);
    // Fenced \`\`\`lang\\n…\`\`\` → one placeholder for the whole block (literal inside).
    // A \`\`\`mermaid fence renders as a diagram thumbnail when mermaid-lite can
    // parse it; otherwise (unknown type, nothing parsed, renderer missing) it
    // falls back to the plain code block below.
    work = work.replace(/\`\`\`([^\\n]*)\\n([\\s\\S]*?)\`\`\`/g, (m, lang, body) => {
      const i = codeStore.length;
      const diagram = mmMaybeDiagramHtml(lang, body);
      codeStore.push(diagram || \`<pre class="md"><code>\${escapeHtml(body.replace(/\\n$/, ''))}</code></pre>\`);
      return PH(i);
    });
    // Inline \`…\` — a run of N backticks delimits, so \`\` \`a\` \`\` (backtick in code) works.
    work = work.replace(/(\`+)([\\s\\S]+?)\\1/g, (m, ticks, body) => {
      const i = codeStore.length;
      codeStore.push(\`<code>\${escapeHtml(body.trim())}</code>\`);
      return PH(i);
    });
    const escaped = escapeHtml(work);
    const lines = escaped.split('\\n');
    const tokens = [];   // { kind: 'text' | 'block', value: string }
    let inList = false;
    const closeList = () => {
      if (inList) { tokens.push({ kind: 'block', value: '</ul>' }); inList = false; }
    };
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      // TABLE: a row containing '|' immediately followed by a delimiter row.
      if (line.indexOf('|') !== -1 && i + 1 < lines.length && _mdIsDelimRow(lines[i + 1])) {
        closeList();
        const headers = _mdSplitRow(line);
        const aligns = _mdSplitRow(lines[i + 1]).map(_mdAlign);
        const th = (s, ci) => \`<th\${aligns[ci] ? \` style="text-align:\${aligns[ci]}"\` : ''}>\${s}</th>\`;
        const td = (s, ci) => \`<td\${aligns[ci] ? \` style="text-align:\${aligns[ci]}"\` : ''}>\${s ?? ''}</td>\`;
        let html = '<table class="md"><thead><tr>' + headers.map(th).join('') + '</tr></thead><tbody>';
        let j = i + 2;
        for (; j < lines.length; j++) {
          if (lines[j].indexOf('|') === -1 || !lines[j].trim()) break;
          const cells = _mdSplitRow(lines[j]);
          html += '<tr>' + headers.map((_, ci) => td(cells[ci], ci)).join('') + '</tr>';
        }
        html += '</tbody></table>';
        tokens.push({ kind: 'block', value: html });
        i = j - 1;   // resume after the consumed table rows
        continue;
      }
      // THEMATIC BREAK: a line of only ---, ***, or ___ (3+, optional spaces) → <hr>. Checked
      // before the bullet rule so "- - -" / "***" don't get mis-read as a list item.
      if (/^\\s*([-*_])(\\s*\\1){2,}\\s*$/.test(line)) {
        closeList();
        tokens.push({ kind: 'block', value: '<hr class="md">' });
        continue;
      }
      const bullet = line.match(/^\\s*[-*]\\s+(.+)$/);
      if (bullet) {
        if (!inList) { tokens.push({ kind: 'block', value: '<ul class="md">' }); inList = true; }
        tokens.push({ kind: 'block', value: '<li>' + bullet[1] + '</li>' });
        continue;
      }
      closeList();
      const h = line.match(/^(#{1,3})\\s+(.+)$/);
      if (h) {
        const lvl = h[1].length + 2;     // #=h3, ##=h4, ###=h5
        tokens.push({ kind: 'block', value: \`<h\${lvl} class="md">\${h[2]}</h\${lvl}>\` });
        continue;
      }
      tokens.push({ kind: 'text', value: line });
    }
    closeList();
    // Join: keep \\n between adjacent text tokens (so pre-wrap renders the
    // line break); blocks need no separator since they're already block.
    let out = '';
    for (let i = 0; i < tokens.length; i++) {
      if (i > 0 && tokens[i].kind === 'text' && tokens[i - 1].kind === 'text') out += '\\n';
      out += tokens[i].value;
    }
    // Inline bold — applied AFTER block extraction so it works inside both
    // plain text and block-element bodies (e.g., bold in a header). Code spans
    // were already extracted to placeholders up top, so a \`**x**\` INSIDE
    // backticks stays literal.
    out = out.replace(/\\*\\*([^*]+)\\*\\*/g, '<strong>$1</strong>');
    // Restore the protected code spans / fenced blocks (their contents were
    // HTML-escaped at capture time, so they render literally).
    out = out.replace(/\\u0000(\\d+)\\u0000/g, (m, i) => codeStore[Number(i)] || '');
    return out;
  }

  // ── Streaming markdown (incremental emit) ───────────────────────────────
  // Inline-only markdown (escape + inline \`code\` + **bold**) for the streaming
  // renderer below, which emits blocks element-by-element and only needs inline
  // formatting per cell / item / line. Mirrors renderInline's inline pass; no
  // block parsing.
  function mdInline(text) {
    const codeStore = [];
    let work = String(text == null ? '' : text);
    work = work.replace(/(\`+)([\\s\\S]+?)\\1/g, (m, t, body) => {
      const i = codeStore.length; codeStore.push('<code>' + escapeHtml(body.trim()) + '</code>'); return ' ' + i + ' ';
    });
    let out = escapeHtml(work).replace(/\\*\\*([^*]+)\\*\\*/g, '<strong>$1</strong>');
    return out.replace(/ (\\d+) /g, (m, i) => codeStore[Number(i)] || '');
  }

  // Streaming markdown renderer: feed text chunks; it emits/patches DOM block-by-
  // block as lines complete — a true incremental emit, not a re-parse of the whole
  // buffer. A fenced block opens a <pre><code> and appends each line (renders even
  // while still unclosed); a table emits <thead> on the header+delimiter then one
  // <tr> per row line; a list appends <li>s; headings are one element each. The
  // in-progress trailing line renders live so prose still streams token-by-token;
  // a |-line that might open a table is held one line for the delimiter lookahead.
  // Mirrors renderInline's feature set. finish() flushes the last partial line.
  function createStreamingMd(container) {
    let pending = '';            // received text not yet ending in a newline
    const lineQ = [];            // complete lines awaiting processing (1-line table lookahead)
    let state = 'none';          // none | code | table | list
    let codeEl = null, codeText = '';
    let mmEl = null;             // streaming \`\`\`mermaid fence → diagram container
    let tbody = null, aligns = [], ncol = 0;
    let ul = null;
    let para = null, paraText = '';
    let live = null;             // transient element for the trailing partial line

    const clearLive = () => { if (live) { live.remove(); live = null; } };
    const endPara = () => { para = null; paraText = ''; };
    const endText = () => { endPara(); ul = null; tbody = null; aligns = []; ncol = 0; if (state !== 'code') state = 'none'; };

    function startTable(headerLine, delimLine) {
      endPara(); ul = null;
      const headers = _mdSplitRow(headerLine);
      aligns = _mdSplitRow(delimLine).map(_mdAlign);
      ncol = headers.length;
      const table = document.createElement('table'); table.className = 'md';
      const thead = document.createElement('thead'); const tr = document.createElement('tr');
      headers.forEach((h, ci) => { const th = document.createElement('th'); if (aligns[ci]) th.style.textAlign = aligns[ci]; th.innerHTML = mdInline(h); tr.appendChild(th); });
      thead.appendChild(tr); table.appendChild(thead);
      tbody = document.createElement('tbody'); table.appendChild(tbody);
      container.appendChild(table); state = 'table';
    }
    function addTableRow(line) {
      const cells = _mdSplitRow(line);
      const tr = document.createElement('tr');
      for (let ci = 0; ci < ncol; ci++) { const td = document.createElement('td'); if (aligns[ci]) td.style.textAlign = aligns[ci]; td.innerHTML = mdInline(cells[ci] != null ? cells[ci] : ''); tr.appendChild(td); }
      tbody.appendChild(tr);
    }
    function addListItem(text) {
      if (state !== 'list' || !ul) { endPara(); tbody = null; ul = document.createElement('ul'); ul.className = 'md'; container.appendChild(ul); state = 'list'; }
      const li = document.createElement('li'); li.innerHTML = mdInline(text); ul.appendChild(li);
    }
    function addHeading(level, text) {
      endText();
      const h = document.createElement('h' + level); h.className = 'md'; h.innerHTML = mdInline(text); container.appendChild(h);
    }
    function appendParaLine(text) {
      if (state !== 'none' || !para) { ul = null; tbody = null; para = document.createElement('div'); para.className = 'md-p'; container.appendChild(para); state = 'none'; paraText = ''; }
      paraText += (paraText ? '\\n' : '') + text;
      para.innerHTML = mdInline(paraText);
    }

    // Process one COMPLETE line (no trailing newline).
    function commitLine(ln) {
      if (state === 'code') {
        if (/^\\s*\`\`\`/.test(ln)) {
          if (mmEl) mmFinishStream(mmEl, codeText);   // fence closed → final render (or code-block fallback)
          codeEl = null; mmEl = null; codeText = ''; state = 'none'; return;
        }
        codeText += (codeText ? '\\n' : '') + ln;
        if (codeEl) codeEl.textContent = codeText;
        else if (mmEl) mmStreamRender(mmEl, codeText);  // re-render the partial diagram
        return;
      }
      const fence = ln.match(/^\\s*\`\`\`(.*)$/);
      if (fence) {
        endText();
        // A \`\`\`mermaid fence streams into a live diagram container instead of a
        // <pre>: the tolerant parser re-renders on every completed line, so the
        // diagram grows as the source arrives. Anything else keeps the literal
        // code-block path (and so does mermaid when the renderer isn't loaded).
        if (fence[1].trim().toLowerCase() === 'mermaid' && typeof mmRenderInto === 'function') {
          mmEl = document.createElement('div');
          mmEl.className = 'mm-thumb mm-streaming';
          container.appendChild(mmEl);
          codeEl = null;
        } else {
          const pre = document.createElement('pre'); pre.className = 'md';
          codeEl = document.createElement('code'); pre.appendChild(codeEl); container.appendChild(pre);
          mmEl = null;
        }
        codeText = ''; state = 'code'; return;
      }
      if (state === 'table') {
        if (ln.indexOf('|') !== -1 && ln.trim()) { addTableRow(ln); return; }
        tbody = null; aligns = []; ncol = 0; state = 'none';   // table ended; handle ln below
      }
      if (!ln.trim()) { endText(); return; }
      const h = ln.match(/^(#{1,3})\\s+(.+)$/);
      if (h) { addHeading(h[1].length + 2, h[2]); return; }
      const b = ln.match(/^\\s*[-*]\\s+(.+)$/);
      if (b) { addListItem(b[1]); return; }
      if (state === 'list') ul = null;
      appendParaLine(ln);
    }

    // Drain complete lines. A |-line that could open a table needs the NEXT line
    // (a delimiter?) to decide → if it's the last buffered line, hold it (unless
    // finishing).
    function drainLines(isFinal) {
      while (lineQ.length) {
        const ln = lineQ[0];
        const maybeHeader = state !== 'code' && state !== 'table'
          && ln.indexOf('|') !== -1 && ln.trim()
          && !/^(#{1,3})\\s+/.test(ln) && !/^\\s*[-*]\\s+/.test(ln) && !/^\\s*\`\`\`/.test(ln);
        if (maybeHeader) {
          if (lineQ.length >= 2) {
            if (_mdIsDelimRow(lineQ[1])) { startTable(lineQ[0], lineQ[1]); lineQ.splice(0, 2); continue; }
          } else if (!isFinal) { break; }   // hold one line; wait for the delimiter lookahead
        }
        commitLine(lineQ.shift());
      }
    }

    // The trailing in-progress partial line, rendered live where natural (inside
    // the open code block, else as a provisional paragraph).
    function renderLive() {
      if (state === 'code') {
        if (codeEl) codeEl.textContent = codeText + (codeText && pending ? '\\n' : '') + (pending || '');
        // mermaid (mmEl): the trailing partial line is "not yet arrived" — the
        // committed lines were already rendered in commitLine. Either way the
        // in-progress text must NOT leak into a live .md-p below the fence.
        return;
      }
      clearLive();
      const tail = [lineQ.join('\\n'), pending].filter(Boolean).join('\\n');
      if (!tail.trim()) return;
      live = document.createElement('div'); live.className = 'md-p'; live.innerHTML = mdInline(tail);
      container.appendChild(live);
    }

    function feed(text) {
      if (!text) return;
      clearLive();
      pending += text;
      let nl;
      while ((nl = pending.indexOf('\\n')) >= 0) { lineQ.push(pending.slice(0, nl)); pending = pending.slice(nl + 1); }
      drainLines(false);
      renderLive();
    }
    function finish() {
      clearLive();
      if (pending.length) { lineQ.push(pending); pending = ''; }
      drainLines(true);
    }
    return { feed, finish };
  }

  // ── Mermaid diagrams (mermaid-lite integration) ─────────────────────────
  // The renderer itself (mmParse/mmRender/mmRenderInto) lives in
  // src/mermaid-lite.mjs and is injected into <head> by the build (same
  // mechanism as mosaic-layout). Everything here is the DOM glue: the
  // \`\`\`mermaid fence hooks in renderInline/createStreamingMd, the in-chat
  // thumbnail, and the fullscreen pan/zoom viewer. Dev servers that serve
  // the raw ui.html (tests) may not inject the renderer — every entry point
  // guards on typeof, falling back to the plain code block.
  const _mmSrcStore = new Map();   // data-mm-id → exact fence source (viewer "Code" view)
  let _mmSrcSeq = 0;
  function _mmStoreSrc(src) {
    const id = String(++_mmSrcSeq);
    _mmSrcStore.set(id, src);
    return id;
  }
  // renderInline's fence hook: HTML for a \`\`\`mermaid fence, or null to fall
  // back to the literal code block (unknown type, parse produced nothing,
  // renderer not loaded). FINAL content never shows a broken half-diagram.
  function mmMaybeDiagramHtml(lang, body) {
    if (String(lang || '').trim().toLowerCase() !== 'mermaid') return null;
    if (typeof mmRender !== 'function') return null;
    const src = String(body).replace(/\\n$/, '');
    let svg = null;
    try { svg = mmRender(src); } catch { svg = null; }
    if (!svg) return null;
    const id = _mmStoreSrc(src);
    // The caller inserts this HTML after we return — register the LIVE element
    // a tick later (an mm-id is unique per render, so the lookup can't alias).
    setTimeout(() => {
      const el = document.querySelector(\`.mm-thumb[data-mm-id="\${id}"]\`);
      if (el) mmRegisterArtifact(src, el);
    }, 0);
    return \`<div class="mm-thumb" data-mm-id="\${id}" title="Click to expand">\${svg}</div>\`;
  }
  // Streaming re-render while the fence is still open (tolerant parser makes
  // partial source safe). Keeps the previous render when the new partial
  // parses to nothing — the diagram only ever grows.
  function mmStreamRender(el, partialSrc) {
    try { mmRenderInto(el, partialSrc); } catch {}
  }
  // Fence closed during streaming: final render; on failure swap the
  // container for a normal code block so no half-diagram persists.
  function mmFinishStream(el, src) {
    let rendered = false;
    try { rendered = mmRenderInto(el, src); } catch { rendered = false; }
    if (!rendered) {
      const pre = document.createElement('pre'); pre.className = 'md';
      const code = document.createElement('code'); code.textContent = src;
      pre.appendChild(code);
      el.replaceWith(pre);
      return;
    }
    el.classList.remove('mm-streaming');
    el.setAttribute('data-mm-id', _mmStoreSrc(src));
    el.title = 'Click to expand';
    mmRegisterArtifact(src, el);
  }

  // ── Artifacts (the side-panel Artifacts tab) ──
  // One entry per diagram TITLE (normalized): re-emitting a diagram with the
  // same title REPLACES the entry's fenceEl — the artifact always points at
  // its LATEST edit — and bubbles it to the top. The registry is in-memory
  // only: a reload re-renders the fences from the event log, which re-registers
  // them here; entries whose fence left the DOM (Clear, project switch) are
  // pruned on render.
  const _mmArtifacts = [];   // { id, title, fenceEl } — id = normalized title
  let _mmArtSeq = 0;         // 'Diagram N' fallback for label-less diagrams
  // First meaningful label: flowchart → first node's text, sequence → first
  // participant label. Reuses mmParse so the title can't drift from the renderer.
  function mmArtifactTitle(src) {
    let model = null;
    try { model = (typeof mmParse === 'function') ? mmParse(src) : null; } catch {}
    const label = model?.type === 'flowchart' ? model.nodes?.[0]?.label
      : model?.type === 'sequence' ? model.participants?.[0]?.label : null;
    return String(label == null ? '' : label).trim() || null;
  }
  function mmRegisterArtifact(src, fenceEl) {
    if (!fenceEl) return;
    const title = mmArtifactTitle(src) || \`Diagram \${++_mmArtSeq}\`;
    const id = title.toLowerCase();
    const i = _mmArtifacts.findIndex((a) => a.id === id);
    if (i >= 0) _mmArtifacts.splice(i, 1);
    _mmArtifacts.unshift({ id, title, fenceEl });
    renderArtifacts();
  }
  function renderArtifacts() {
    // Prune entries whose fence is gone (cleared log / switched project).
    for (let i = _mmArtifacts.length - 1; i >= 0; i--) {
      if (!_mmArtifacts[i].fenceEl?.isConnected) _mmArtifacts.splice(i, 1);
    }
    const cnt = document.getElementById('artifactCount');
    if (cnt) cnt.textContent = _mmArtifacts.length ? \` (\${_mmArtifacts.length})\` : '';
    const empty = document.getElementById('artifactEmpty');
    if (empty) empty.style.display = _mmArtifacts.length ? 'none' : '';
    const list = document.getElementById('artifactList');
    if (!list) return;
    list.innerHTML = '';
    for (const a of _mmArtifacts) {
      const row = document.createElement('div');
      row.className = 'art-row';
      row.title = 'Open in the viewer';
      row.innerHTML = '<span class="art-glyph"><svg viewBox="0 0 16 16" width="13" height="13" fill="none" stroke="currentColor" stroke-width="1.3" aria-hidden="true"><rect x="1.5" y="1.5" width="6" height="5" rx="1"/><rect x="8.5" y="9.5" width="6" height="5" rx="1"/><path d="M4.5 6.5 v6 h4"/></svg></span>'
        + \`<span class="art-title">\${escapeHtml(a.title)}</span>\`
        + '<button class="art-locate" title="Show in chat">⌖</button>';
      row.addEventListener('click', () => mmOpenViewer(a.fenceEl?.getAttribute('data-mm-id')));
      row.querySelector('.art-locate').addEventListener('click', (e) => { e.stopPropagation(); mmLocateArtifact(a); });
      list.appendChild(row);
    }
  }
  // ⌖ → scroll the chat to the fence + a brief highlight pulse; NO viewer.
  function mmLocateArtifact(a) {
    const el = a?.fenceEl;
    if (!el || !el.isConnected) { renderArtifacts(); return; }
    el.scrollIntoView({ block: 'center' });
    el.classList.remove('mm-located');
    void el.offsetWidth;   // restart the CSS animation on a repeat click
    el.classList.add('mm-located');
    setTimeout(() => el.classList.remove('mm-located'), 1600);
  }

  // ── Fullscreen viewer ──
  const _mmViewer = { x: 0, y: 0, dragging: false, lastX: 0, lastY: 0, moved: false, src: null, title: null };
  function _mmViewerEl() { return document.getElementById('mmViewer'); }
  function _mmPanEl() { return document.getElementById('mmViewerPan'); }
  function _mmApplyPan() {
    const pan = _mmPanEl();
    if (pan) pan.style.transform = \`translate(\${_mmViewer.x}px, \${_mmViewer.y}px)\`;
  }
  function mmOpenViewer(id) {
    const src = _mmSrcStore.get(String(id));
    if (src == null) return;
    const v = _mmViewerEl(), pan = _mmPanEl();
    if (!v || !pan) return;
    let svg = null;
    try { svg = (typeof mmRender === 'function') ? mmRender(src) : null; } catch {}
    pan.innerHTML = svg || '';
    _mmViewer.src = src;
    _mmViewer.title = mmArtifactTitle(src) || 'diagram';   // PNG filename + Copy source
    document.getElementById('mmViewerSrc').textContent = src;
    document.getElementById('mmViewerCodeBtn').textContent = 'Code';
    document.getElementById('mmViewerCopyBtn').textContent = 'Copy';
    v.classList.remove('mm-show-code');
    v.classList.add('show');
    document.body.style.overflow = 'hidden';   // lock body scroll while open
    // Center the diagram in the viewport (panning takes over from there).
    const el = pan.querySelector('svg');
    const w = el ? (parseFloat(el.getAttribute('width')) || el.getBoundingClientRect().width) : 0;
    const h = el ? (parseFloat(el.getAttribute('height')) || el.getBoundingClientRect().height) : 0;
    _mmViewer.x = Math.max(24, (window.innerWidth - w) / 2);
    _mmViewer.y = Math.max(58, (window.innerHeight - h) / 2);
    _mmApplyPan();
  }
  function mmCloseViewer() {
    const v = _mmViewerEl();
    if (v) v.classList.remove('show');
    document.body.style.overflow = '';
    _mmViewer.dragging = false;
  }
  function mmViewerToggleCode() {
    const v = _mmViewerEl();
    if (!v) return;
    const showCode = v.classList.toggle('mm-show-code');
    document.getElementById('mmViewerCodeBtn').textContent = showCode ? 'Diagram' : 'Code';
  }
  // Copy: the EXACT mermaid source to the clipboard. navigator.clipboard needs
  // a secure context — fall back to the textarea/execCommand path elsewhere.
  async function mmViewerCopy() {
    const src = _mmViewer.src;
    if (src == null) return;
    let copied = false;
    try { await navigator.clipboard.writeText(src); copied = true; } catch {}
    if (!copied) {
      try {
        const ta = document.createElement('textarea');
        ta.value = src;
        ta.style.cssText = 'position:fixed; left:-9999px; top:0;';
        document.body.appendChild(ta);
        ta.select();
        copied = document.execCommand('copy');
        ta.remove();
      } catch {}
    }
    const btn = document.getElementById('mmViewerCopyBtn');
    if (btn) {
      btn.textContent = copied ? 'Copied' : 'Copy failed';
      setTimeout(() => { btn.textContent = 'Copy'; }, 1200);
    }
  }
  // Save: rendered SVG → 2x-scale canvas → PNG download named after the
  // diagram title. The SVG carries concrete width/height attrs and every
  // var(--x) has a #hex fallback (mermaid-lite), so it rasterizes standalone.
  function mmViewerSave() {
    const svgEl = _mmPanEl()?.querySelector('svg');
    if (!svgEl) return;
    const w = parseFloat(svgEl.getAttribute('width')) || svgEl.getBoundingClientRect().width || 40;
    const h = parseFloat(svgEl.getAttribute('height')) || svgEl.getBoundingClientRect().height || 30;
    const xml = new XMLSerializer().serializeToString(svgEl);
    const url = URL.createObjectURL(new Blob([xml], { type: 'image/svg+xml;charset=utf-8' }));
    const img = new Image();
    img.onload = () => {
      try {
        const canvas = document.createElement('canvas');
        canvas.width = Math.ceil(w * 2);
        canvas.height = Math.ceil(h * 2);
        const ctx = canvas.getContext('2d');
        ctx.scale(2, 2);
        ctx.drawImage(img, 0, 0, w, h);
        const a = document.createElement('a');
        a.href = canvas.toDataURL('image/png');
        a.download = \`\${String(_mmViewer.title || 'diagram').replace(/[^\\w \\-.]+/g, '_')}.png\`;
        document.body.appendChild(a);
        a.click();
        a.remove();
      } catch {}
      URL.revokeObjectURL(url);
    };
    img.onerror = () => URL.revokeObjectURL(url);
    img.src = url;
  }
  // Thumbnail click → viewer (delegated: thumbnails are re-created on every
  // re-render/reload). Streaming thumbs have no data-mm-id yet and don't open.
  document.addEventListener('click', (e) => {
    const thumb = e.target && e.target.closest ? e.target.closest('.mm-thumb[data-mm-id]') : null;
    if (thumb) mmOpenViewer(thumb.getAttribute('data-mm-id'));
  });
  // Pan: click-drag moves the diagram. Wheel scrolls vertically; Shift+wheel
  // scrolls horizontally. (Capture-phase Esc so the global Escape handler —
  // which may cancel a running chat — never sees it while the viewer is open.)
  document.addEventListener('mousedown', (e) => {
    const canvas = document.getElementById('mmViewerCanvas');
    if (!canvas || !_mmViewerEl()?.classList.contains('show')) return;
    if (!canvas.contains(e.target)) return;
    _mmViewer.dragging = true;
    _mmViewer.moved = false;   // a press is a pan until it actually moves
    _mmViewer.lastX = e.clientX; _mmViewer.lastY = e.clientY;
    canvas.classList.add('mm-dragging');
    e.preventDefault();
  });
  document.addEventListener('mousemove', (e) => {
    if (!_mmViewer.dragging) return;
    if (Math.abs(e.clientX - _mmViewer.lastX) + Math.abs(e.clientY - _mmViewer.lastY) > 2) _mmViewer.moved = true;
    _mmViewer.x += e.clientX - _mmViewer.lastX;
    _mmViewer.y += e.clientY - _mmViewer.lastY;
    _mmViewer.lastX = e.clientX; _mmViewer.lastY = e.clientY;
    _mmApplyPan();
  });
  // Backdrop click closes (like unfocusing an input): only when the click hit
  // the canvas ITSELF — clicks/drags ON the SVG keep panning, and a drag that
  // ended on the backdrop (moved) is a pan, not a close.
  document.addEventListener('click', (e) => {
    if (!_mmViewerEl()?.classList.contains('show')) return;
    if (e.target !== document.getElementById('mmViewerCanvas')) return;
    if (_mmViewer.moved) { _mmViewer.moved = false; return; }
    mmCloseViewer();
  });
  document.addEventListener('mouseup', () => {
    if (!_mmViewer.dragging) return;
    _mmViewer.dragging = false;
    document.getElementById('mmViewerCanvas')?.classList.remove('mm-dragging');
  });
  document.addEventListener('wheel', (e) => {
    const v = _mmViewerEl();
    if (!v || !v.classList.contains('show') || v.classList.contains('mm-show-code')) return;
    const canvas = document.getElementById('mmViewerCanvas');
    if (!canvas || !canvas.contains(e.target)) return;
    e.preventDefault();
    if (e.shiftKey) _mmViewer.x -= (e.deltaY || 0) + (e.deltaX || 0);
    else { _mmViewer.y -= e.deltaY || 0; _mmViewer.x -= e.deltaX || 0; }
    _mmApplyPan();
  }, { passive: false });
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && _mmViewerEl()?.classList.contains('show')) {
      e.stopImmediatePropagation();
      e.preventDefault();
      mmCloseViewer();
    }
  }, true);

  // ── Mosaic snapshot ─────────────────────────────────────────────────────
  // Lives in localStorage so the active query set persists across page loads.
  // Server-side default loads when no localStorage entry exists.
  const MOSAIC_LS_KEY = 'cb.mosaic.activeIds';
  let _mosaicState = {
    currentPage: 0,
    catalog: [],
    tiles: [],           // raw tile data from server (kind + body etc.)
    layout: null,        // {pages: [...]} computed locally with measured metrics
    metrics: null,       // {charsPerCol, linesPerRow, ...} from _measureCellMetrics
    build: null,
    activeIdsRaw: '',
  };
  function _getActiveIds() {
    try { return localStorage.getItem(MOSAIC_LS_KEY) ?? ''; }
    catch { return ''; }
  }
  function _setActiveIds(raw) {
    try { localStorage.setItem(MOSAIC_LS_KEY, raw); } catch {}
  }

  function openSnapshot() {
    // Diagnostics now live on a STANDALONE page (/snapshot) with its own
    // connection — so it works even when THIS tab's UI is wedged, and you can
    // also browse straight to http://127.0.0.1:<port>/snapshot. Open it in a
    // separate tab (named target so repeated clicks reuse the same tab). The
    // in-app mosaic (_fetchAndRenderMosaic) is kept below as a fallback but is
    // no longer the primary path.
    try {
      const w = window.open('/snapshot', 'cb-snapshot');
      if (!w) location.href = '/snapshot';   // popup blocked → navigate
    } catch { location.href = '/snapshot'; }
  }

  async function _fetchAndRenderMosaic() {
    // Hard timeout so a hung server doesn't leave the modal stuck on
    // "measuring…" forever — if the user opened the snapshot because
    // something else froze, /api/queries may not come back.
    const ctrl = new AbortController();
    const timeoutId = setTimeout(() => ctrl.abort(), 10000);
    try {
      const raw = _getActiveIds().trim();
      const url = raw ? \`/api/queries?ids=\${encodeURIComponent(raw)}\` : '/api/queries';
      const r = await fetch(url, { signal: ctrl.signal });
      clearTimeout(timeoutId);
      if (!r.ok) throw new Error('queries fetch failed: ' + r.status);
      const data = await r.json();
      _mosaicState.tiles = data.tiles ?? [];
      _mosaicState.catalog = data.catalog ?? [];
      _mosaicState.build = data.build;
      _mosaicState.activeIdsRaw = raw || (data.defaultIds ?? []).join(',');
      // Re-layout in the browser with measured metrics. The server's layout
      // (in data.layout) is discarded — the browser knows the actual cell
      // dimensions for the real viewport / font size / zoom level.
      _relayoutAndRender();
    } catch (e) {
      clearTimeout(timeoutId);
      const msg = e?.name === 'AbortError'
        ? new Error('snapshot fetch timed out after 10s — server may be unresponsive')
        : e;
      showError('Snapshot fetch failed', msg);
    }
  }

  // ── Browser-side measurement & layout ───────────────────────────────────
  // The shared mosaic-layout.mjs is injected as a <script> in the HTML head
  // by the build, so computeTileSize, packPage, and layoutMosaic are global
  // functions in browser scope. We compute charsPerCol and linesPerRow from
  // the actual rendered cell dimensions, then re-pack on every resize.
  function _measureCellMetrics() {
    // The mosaic-page grid splits the modal body into 12 cols × 8 rows.
    // We need to know how much TEXT fits inside one cell after tile chrome.
    // Strategy: render a hidden probe into the body with the real tile-body
    // styles, measure a single character's width and a single line's height.
    const probe = document.createElement('div');
    probe.style.cssText = 'position:absolute;visibility:hidden;left:-9999px;top:-9999px;' +
      'font-family:var(--mono);font-size:9px;line-height:1.0;white-space:pre;';
    probe.textContent = 'X\\nX';
    document.body.appendChild(probe);
    const rect2 = probe.getBoundingClientRect();
    probe.textContent = 'X';
    const rect1 = probe.getBoundingClientRect();
    const charW   = rect1.width;
    const lineH   = rect2.height - rect1.height;  // delta between 1 and 2 lines
    document.body.removeChild(probe);

    // Available area for the mosaic = modal body minus its padding. The page
    // grid splits that area into 12×24 cells. We pass charsPerCol and
    // linesPerRow as FLOATS (not floored) — the layout uses ceil() so a
    // tile that needs 1.1 rows worth doesn't get rounded down then forced
    // up to 2 rows by the integer arithmetic.
    const bodyRect = $modalBody.getBoundingClientRect();
    const pageRows = 24;
    const gap = 2;    // .mosaic-page gap
    const pad = 2;    // .mosaic-page padding
    const cellW = (bodyRect.width  - 2 * pad - 11 * gap) / 12;
    const cellH = (bodyRect.height - 2 * pad - (pageRows - 1) * gap) / pageRows;
    const tileBorderX = 2;       // 1px × 2
    const tileBorderY = 2;
    const tileBodyPadX = 8;      // 4px × 2
    const tileBodyPadY = 3;      // 1px top + 2px bottom
    const tileHeaderH = 14;      // measured from CSS: ~9px font × 1.1 LH + small padding
    const innerW = cellW - tileBorderX - tileBodyPadX;
    // charsPerCol = how many chars fit horizontally in one grid column.
    // linesPerRow = how many lines of body content fit in one grid row's
    // worth of vertical space, AFTER deducting the chrome that the tile
    // header occupies in the topmost row. Pass through chromeLines so the
    // layout knows the tile header eats some of the first row's capacity.
    const charsPerCol = Math.max(1, innerW / Math.max(1, charW));
    const linesPerRow = Math.max(1, cellH / Math.max(1, lineH));
    const chromeLines = (tileBorderY + tileBodyPadY + tileHeaderH) / Math.max(1, lineH);
    return { charsPerCol, linesPerRow, chromeLines, pageRows, charW, lineH, cellW, cellH };
  }

  function _relayoutAndRender() {
    const metrics = _measureCellMetrics();
    _mosaicState.metrics = metrics;
    // Recompute size for every tile using the measured metrics. maxRows
    // matches the CSS grid (24 rows of fine vertical resolution).
    const sized = (_mosaicState.tiles ?? []).map((t) => ({
      ...t,
      sizeHint: computeTileSize(t, {
        charsPerCol: metrics.charsPerCol,
        linesPerRow: metrics.linesPerRow,
        chromeLines: metrics.chromeLines,
        maxRows: metrics.pageRows,
      }),
    }));
    _mosaicState.layout = layoutMosaic(sized, 12, metrics.pageRows, { maxMs: 250 });
    renderMosaic();
  }

  // Debounced window-resize handler. Reflow whenever the viewport changes
  // size so tile dimensions scale with the browser without a manual reload.
  let _resizeTimer = null;
  window.addEventListener('resize', () => {
    if (!$modalBg?.classList.contains('fullpage') || !$modalBg.classList.contains('show')) return;
    clearTimeout(_resizeTimer);
    _resizeTimer = setTimeout(() => {
      if (_mosaicState.tiles?.length) _relayoutAndRender();
    }, 150);
  });

  function _gotoPage(n) {
    const total = _mosaicState.layout?.pages?.length ?? 0;
    const catalogPageCount = 1;
    const max = total + catalogPageCount - 1;  // last page = catalog index
    _mosaicState.currentPage = Math.max(0, Math.min(max, n));
    _renderCurrentPage();
  }

  function _onActiveIdsChange(e) {
    const v = (e.target.value ?? '').trim();
    _setActiveIds(v);
    _mosaicState.currentPage = 0;
    _fetchAndRenderMosaic();
  }

  function _resetMosaicDefaults() {
    _setActiveIds('');  // empty = use server defaults
    _mosaicState.currentPage = 0;
    _fetchAndRenderMosaic();
  }

  function _gotoCatalog() {
    const total = _mosaicState.layout?.pages?.length ?? 0;
    _gotoPage(total);  // catalog page is one past the last data page
  }

  function renderMosaic() {
    $modal.classList.remove('error');
    $modalBg.classList.add('fullpage');
    $modalBg.classList.add('show');

    const { layout, catalog, build, activeIdsRaw } = _mosaicState;
    const totalDataPages = layout?.pages?.length ?? 0;

    // The modal-header gets rebuilt only on FIRST render (or when the modal
    // was previously closed). Re-layouts triggered by window resize only
    // refresh the body so the header's buttons aren't detached mid-click.
    const headerExists = document.getElementById('mosaicActiveInput') != null;
    if (!headerExists) {
      const headerHtml = \`
        <h2>Mosaic</h2>
        <div class="mosaic-controls">
          <label>active:</label>
          <input type="text" id="mosaicActiveInput" placeholder="e.g. 1,13,20,50,51"
                 title="Comma-separated query IDs from the catalog (last page).">
          <button class="btn-sm" onclick="_resetMosaicDefaults()">defaults</button>
          <button class="btn-sm" onclick="_gotoCatalog()">catalog</button>
          <span class="pageno" id="mosaicPageNo"></span>
          <button class="btn-sm" onclick="_gotoPage(_mosaicState.currentPage - 1)">‹</button>
          <button class="btn-sm" onclick="_gotoPage(_mosaicState.currentPage + 1)">›</button>
          <button class="btn-sm" onclick="_fetchAndRenderMosaic()">↻</button>
          <span class="build-pill" id="mosaicBuildPill"></span>
        </div>
        <button class="modal-close" onclick="closeModal()">×</button>
      \`;
      document.querySelector('#modalBg .modal-header').innerHTML = headerHtml;
      const input = document.getElementById('mosaicActiveInput');
      if (input) {
        input.addEventListener('change', _onActiveIdsChange);
        input.addEventListener('keydown', (e) => { if (e.key === 'Enter') _onActiveIdsChange(e); });
      }
    }
    // Update header dynamic bits — value of active input, build pill text —
    // without recreating the elements (preserves listeners and click targets).
    const inputEl = document.getElementById('mosaicActiveInput');
    if (inputEl && inputEl.value !== activeIdsRaw) inputEl.value = activeIdsRaw;
    const buildPill = document.getElementById('mosaicBuildPill');
    if (buildPill) buildPill.textContent = build?.version ?? '?';

    // Pre-render every page; only the current one is visible. This keeps
    // the DOM stable so flipping pages is instant.
    const dataPagesHtml = (layout?.pages ?? []).map((p, idx) => _renderPageHtml(p, idx)).join('');
    const catalogHtml = \`<div class="mosaic-page" data-page-idx="\${totalDataPages}" hidden>\${_renderCatalogPageHtml(catalog)}</div>\`;
    $modalBody.innerHTML = dataPagesHtml + catalogHtml;

    _renderCurrentPage();
  }

  function _renderCurrentPage() {
    const total = (_mosaicState.layout?.pages?.length ?? 0) + 1;
    const cur = _mosaicState.currentPage;
    $modalBody.querySelectorAll('.mosaic-page').forEach((el) => {
      el.hidden = (Number(el.dataset.pageIdx) !== cur);
    });
    const pn = document.getElementById('mosaicPageNo');
    if (pn) {
      const isCatalog = cur === total - 1;
      pn.textContent = isCatalog
        ? \`page \${cur + 1}/\${total} (catalog)\`
        : \`page \${cur + 1}/\${total}\`;
    }
  }

  function _renderPageHtml(page, idx) {
    const tiles = (page.tiles ?? []).map((t) => _renderTileHtml(t)).join('');
    return \`<div class="mosaic-page" data-page-idx="\${idx}" hidden>\${tiles}</div>\`;
  }

  function _renderTileHtml(tile) {
    const sev = tile.severity ? \`sev-\${tile.severity}\` : '';
    const sevTag = tile.severity ? \`<span class="sev-tag \${tile.severity}">\${escapeHtml(tile.severity)}</span>\` : '';
    // Server returns 0-indexed row/col; CSS Grid is 1-indexed.
    const styleVars = \`--row: \${(tile.row ?? 0) + 1}; --col: \${(tile.col ?? 0) + 1}; --rspan: \${tile.rowSpan ?? 2}; --cspan: \${tile.colSpan ?? 3};\`;
    const body = _renderTileBody(tile);
    return \`<div class="mosaic-tile \${sev}" style="\${styleVars}">
      <div class="mosaic-tile-head">
        <span class="id">#\${tile.id}</span>
        <span class="name">\${escapeHtml(tile.name ?? '')}</span>
        \${sevTag}
      </div>
      <div class="mosaic-tile-body">\${body}</div>
    </div>\`;
  }

  function _renderTileBody(tile) {
    const kind = tile.kind ?? 'text';
    if (kind === 'json') {
      try { return escapeHtml(JSON.stringify(tile.body, null, 2)); }
      catch { return escapeHtml(String(tile.body)); }
    }
    if (kind === 'table') {
      const t = tile.body ?? { columns: [], rows: [] };
      const head = (t.columns ?? []).map((c) => \`<th>\${escapeHtml(c)}</th>\`).join('');
      const rows = (t.rows ?? []).map((r) =>
        '<tr>' + r.map((cell) => \`<td>\${escapeHtml(String(cell ?? ''))}</td>\`).join('') + '</tr>'
      ).join('');
      return \`<table><thead><tr>\${head}</tr></thead><tbody>\${rows}</tbody></table>\`;
    }
    if (kind === 'findings') {
      const findings = Array.isArray(tile.body) ? tile.body : [];
      if (!findings.length) return '<span style="color:var(--muted)">(no findings)</span>';
      return findings.map((f) => {
        const sev = String(f.severity ?? 'info').toLowerCase();
        const det = f.detail ? \`<br><span style="color:var(--muted);font-size:8.5px;">— \${escapeHtml(String(f.detail))}</span>\` : '';
        return \`<div class="finding">
          <span class="sev \${sev}">\${escapeHtml(sev.toUpperCase())}</span>
          <span class="cat">\${escapeHtml(String(f.category ?? ''))}</span>
          <span class="text">\${escapeHtml(String(f.title ?? ''))}\${det}</span>
        </div>\`;
      }).join('');
    }
    // Default: text
    return escapeHtml(String(tile.body ?? ''));
  }

  function _renderCatalogPageHtml(catalog) {
    // One full-page tile listing every available query with its ID + name +
    // category + sizeHint + description. Sorted by ID so screenshots line up.
    const sorted = [...catalog].sort((a, b) => a.id - b.id);
    const rows = sorted.map((q) =>
      \`<tr><td class="id">\${q.id}</td>
        <td class="cat">\${escapeHtml(q.category ?? '')}</td>
        <td class="size">\${q.sizeHint?.cols ?? '?'}×\${q.sizeHint?.rows ?? '?'}</td>
        <td>\${escapeHtml(q.name ?? '')}</td>
        <td style="color:var(--muted)">\${escapeHtml(q.description ?? '')}</td></tr>\`
    ).join('');
    return \`<div class="mosaic-tile mosaic-catalog-tile">
      <div class="mosaic-tile-head">
        <span class="id">CATALOG</span>
        <span class="name">all \${sorted.length} available queries — type IDs above to activate</span>
      </div>
      <div class="mosaic-tile-body">
        <table>
          <thead><tr><th>id</th><th>category</th><th>size</th><th>name</th><th>description</th></tr></thead>
          <tbody>\${rows}</tbody>
        </table>
      </div>
    </div>\`;
  }

  function _fmtBytes(n) {
    if (n == null) return '?';
    if (n < 1024) return n + ' B';
    if (n < 1024 * 1024) return (n / 1024).toFixed(1) + ' KB';
    return (n / 1024 / 1024).toFixed(2) + ' MB';
  }
  function _fmtMs(ms) {
    if (ms == null) return '—';
    if (ms < 1000) return ms + ' ms';
    if (ms < 60_000) return (ms / 1000).toFixed(1) + ' s';
    if (ms < 3_600_000) return (ms / 60_000).toFixed(1) + ' min';
    return (ms / 3_600_000).toFixed(2) + ' hr';
  }
  function _kv(rows) {
    return '<div class="diag-kv">' + rows.map(([k, v, cls]) =>
      \`<div class="k">\${escapeHtml(k)}</div><div class="v\${cls ? ' ' + cls : ''}">\${v == null ? '<span style="color:var(--muted)">—</span>' : escapeHtml(String(v))}</div>\`
    ).join('') + '</div>';
  }

  async function openLog() {
    $modal.classList.remove('error');
    $modalTitle.textContent = 'Project log';
    $modalBody.innerHTML = '<p class="par-caption">Loading…</p>';
    $modalBg.classList.add('show');
    await refreshLog();
  }

  async function refreshLog() {
    try {
      const r = await fetch('/api/log');
      const d = await r.json();
      const path = d.file ? escapeHtml(d.file) : '(no project)';
      const bytes = d.bytes != null ? \` · \${d.bytes} bytes\` : '';
      $modalBody.innerHTML =
        '<div class="par-caption">' + path + bytes + ' &nbsp; <a href="#" onclick="refreshLog();return false;">refresh</a></div>' +
        '<pre class="logbox" id="logBox">' + escapeHtml(d.log ?? '') + '</pre>';
      const box = document.getElementById('logBox');
      if (box) box.scrollTop = box.scrollHeight; // tail
    } catch (e) {
      $modalBody.innerHTML = '<div class="error-banner"><strong>Could not load log:</strong> ' + escapeHtml(e.message) + '</div>';
    }
  }

  function showError(title, err) {
    $modal.classList.add('error');
    $modalTitle.textContent = title;
    const message = err?.message ?? String(err);
    const stack = (err?.stack ?? '').split('\\n').slice(0, 6).join('\\n');
    const dense = [
      'ERR   ' + title,
      'msg   ' + message,
      'proj  ' + (activeProject || '(none)'),
      'model ' + ($model.value || SERVER.defaultModel || '?'),
      'tok   ' + totalTokens,
      'time  ' + new Date().toISOString().slice(0, 19) + 'Z',
      stack ? 'stack ' + stack.replace(/\\n/g, '\\n      ') : '',
    ].filter(Boolean).join('\\n');
    $modalBody.innerHTML = '<pre class="dense">' + escapeHtml(dense) + '</pre>';
    $modalBg.classList.add('show');
  }

  function closeModal() {
    $modalBg.classList.remove('show');
    $modalBg.classList.remove('fullpage');
    // Clear mosaic state so the debounced resize handler doesn't re-render
    // the modal after we just closed it.
    if (typeof _mosaicState !== 'undefined') {
      _mosaicState.tiles = [];
      _mosaicState.layout = null;
    }
    if (typeof _resizeTimer !== 'undefined') clearTimeout(_resizeTimer);
  }

  async function clearConversation() {
    if (!confirm('Clear the conversation? This deletes the saved chat for this project — the agent won\\'t remember anything from before.')) return;
    try {
      const r = await fetch('/api/clear-conversation', { method: 'POST' });
      if (!r.ok && r.status === 409) {
        alert('Cannot clear while agent is running. Cancel first.');
        return;
      }
    } catch (e) { showError('Clear failed', e); }
    // The server broadcasts a 'reset' delta that wipes our render.
  }

  // ── SSE stream — pushes every server-side state change here ──
  // Browser is a view: we mirror session, render from session.events.
  let _es = null;
  let _lastSseAt = 0;            // wallclock ms of most recent SSE message
  // Tab leadership: only the ACTIVE tab holds an SSE connection. Multiple live
  // SSE connections (one per tab) exhaust the browser's ~6-per-origin pool and
  // silently hang every fetch. When this tab is passive we keep no connection
  // and skip all (re)connect paths. See setupTabLeadership below.
  let _tabActive = true;
  let _ssePaused = false;
  function connectStream() {
    if (_ssePaused || !_tabActive) { console.log('[sse] passive tab — not connecting'); return; }
    if (_es) { try { _es.close(); } catch {} _es = null; }
    _es = new EventSource('/api/stream');
    console.log('[sse] connecting');
    _es.onopen = () => { _lastSseAt = Date.now(); clearConnLost(); console.log('[sse] open'); };
    _es.onmessage = (e) => {
      _lastSseAt = Date.now();
      let msg;
      try { msg = JSON.parse(e.data); } catch { return; }
      if (msg.type === 'init') {
        console.log('[sse] init v=' + msg.session.version + ' status=' + msg.session.status);
        onInit(msg.session);
        expectedNextVersion = msg.session.version + 1;
        return;
      }
      // Gap detection
      if (typeof msg.version !== 'number') return;
      console.log('[sse] msg type=' + msg.type + ' v=' + msg.version + (msg.status ? ' status=' + msg.status : ''));
      if (msg.version === expectedNextVersion) {
        applyDelta(msg);
        expectedNextVersion = msg.version + 1;
      } else if (msg.version > expectedNextVersion) {
        // Missed some — refetch full state
        console.warn(\`SSE gap: expected v\${expectedNextVersion}, got v\${msg.version} — refetching\`);
        try { _es.close(); } catch {}
        _es = null;
        refetchAndReconnect();
      }
      // else: stale, ignore
    };
    _es.onerror = () => {
      // EventSource auto-reconnects on its own; the watchdog below will
      // refetch state if reconnect doesn't deliver messages in time.
      console.warn('[sse] error (readyState=' + _es?.readyState + ')');
    };
  }
  // Server-death visibility: when the SSE is gone AND /api/state keeps failing,
  // the spinner must stop pretending progress. After ~3 consecutive bounded-fetch
  // failures the status row flips to a persistent "lost contact" error (NEVER a
  // modal, never a blocking fetch — connection-fragility rules); any successful
  // refetch clears it and the normal re-render takes over.
  let _refetchFails = 0;        // consecutive /api/state failures
  let _refetchInFlight = false; // dedupe — the watchdog + retry timer must not stack loops
  let _connLost = false;
  function showConnLost() {
    if (_connLost) return;
    _connLost = true;
    // Stop the timers FIRST (stopTick repaints once on its way out), then own the row.
    status.stopSpinnerOnly();
    status.stopTick();
    const row = ensureStatusRow();
    row.classList.remove('thinking');
    row.classList.add('conn-lost');
    row.innerHTML = '<span class="arrow">✕</span> <strong>Lost contact with the server</strong> <span class="dim">— it may have stopped; reload once it is back. Retrying in the background…</span>';
  }
  function clearConnLost() {
    if (!_connLost) return;
    _connLost = false;
    document.querySelector('#log .status.conn-lost')?.classList.remove('conn-lost');
  }
  async function refetchAndReconnect() {
    if (_ssePaused || !_tabActive) return;   // passive tab holds no connection
    if (_refetchInFlight) return;
    _refetchInFlight = true;
    try {
      const r = await fetch('/api/state', { signal: AbortSignal.timeout(5000) });   // bounded — never hangs the loop
      const s = await r.json();
      _refetchFails = 0;
      clearConnLost();
      onInit(s);
      expectedNextVersion = s.version + 1;
      connectStream();
    } catch (e) {
      console.error('refetch failed:', e);
      _refetchFails++;
      if (_refetchFails >= 3) showConnLost();   // ~3 fails over ~6-15s of unreachable
      // Retry in a few seconds
      setTimeout(refetchAndReconnect, 2000);
    } finally {
      _refetchInFlight = false;
    }
  }

  // SSE watchdog. EventSource auto-reconnect is supposed to handle drops,
  // but we've seen the chat get stuck in 'running' (input disabled, no final
  // assistant message, no 'status: done' delta) — meaning the server-side
  // state moved on but the client never heard about it. Every 5s, if the
  // session is 'running' and no SSE message has arrived in 30s, refetch
  // /api/state directly and reconnect. Cheap insurance against silent SSE
  // drops; doesn't run when idle.
  setInterval(() => {
    if (_ssePaused || !_tabActive) return;   // passive tab intentionally has no SSE
    if (!session) return;
    if (session.status !== 'running') return;
    if (!_lastSseAt) return;     // never connected yet — connectStream will handle it
    const silence = Date.now() - _lastSseAt;
    if (silence > 30000) {
      console.warn('[sse] watchdog: ' + silence + 'ms since last message while running — refetching');
      try { _es?.close(); } catch {}
      _es = null;
      refetchAndReconnect();
    }
  }, 5000);

  // The footer "uncommitted changes" ⚠ indicator was RETIRED — the direct/subbranch git row at the
  // bottom of the chat owns git status now (a direct-mode warning, or the merge row). Kept as a no-op
  // so the existing call sites (init, chat-end, the 8s poll) stay harmless.
  function refreshGitDirty() {}
  // ── Pinned merge bar (CODE_BOSS branch → parent) ──────────────────────────
  // The merge prompt is a UI-ONLY row appended to the chat log (NOT a chat event — never persisted,
  // never sent to the LLM). It's kept as the LAST row so it scrolls with history; removed while a
  // run is active (the ahead-count is changing) and re-added when it settles.
  let _mergeBarBusy = false;
  async function refreshMergeBar() {
    const drop = () => document.getElementById('mergeRow')?.remove();
    if (!activeProject) { drop(); return; }   // keep it pinned + visible even during a run
    if (_mergeBarBusy) return;            // don't overlap a slow in-flight poll
    _mergeBarBusy = true;
    try {
      const data = await (await fetch('/api/merge-status')).json();
      const repos = data.repos || [];
      // ALWAYS show the current git state (a direct repo, else a CODE_BOSS sub-branch repo).
      const repo = repos.find((r) => r.mode === 'direct') || repos.find((r) => r.mode === 'subbranch') || null;
      if (!repo) { drop(); return; }   // not a tracked git project → nothing to show
      let row = document.getElementById('mergeRow');
      if (!row) { row = document.createElement('div'); row.id = 'mergeRow'; }
      const BRANCH = '<svg class="merge-ico" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="4" cy="3.3" r="1.5"/><circle cx="4" cy="12.7" r="1.5"/><circle cx="12" cy="3.3" r="1.5"/><path d="M4 4.8v6.4"/><path d="M12 4.8C12 9 8.8 9.4 5.4 11.4"/></svg>';
      if (repo.mode === 'direct') {
        // On the user's branch directly — just name it; show the uncommitted count + Commit only if dirty.
        const n = repo.changeCount || 0;
        row.className = 'merge-row' + (repo.dirty ? ' direct' : '');
        row.innerHTML =
          \`<span class="merge-source">\${BRANCH}<span class="merge-cb">\${escapeHtml(repo.branch)}</span>\` +
          (repo.dirty
            ? \`<span class="merge-ahead"> · \${n ? n + ' ' : ''}uncommitted change\${n === 1 ? '' : 's'}</span></span><button class="merge-btn" onclick="openCommitModal()">Commit…</button>\`
            : \`<span class="merge-ahead"> · up to date</span></span>\`);
      } else {
        // CODE_BOSS sub-branch → ⑃ parent (target) · [← merge] · ⑃ CODE_BOSS branch (source) · N commits.
        const ahead = repo.ahead || 0;
        row.className = 'merge-row';
        row.innerHTML =
          \`<span class="merge-source">\${BRANCH}<span class="merge-parent">\${escapeHtml(repo.parent)}</span></span>\` +
          (ahead > 0
            ? \`<button class="merge-btn" onclick="mergeBranchToParent()" title="Merge \${escapeHtml(repo.branch)} into \${escapeHtml(repo.parent)}">← merge</button>\`
            : \`<span class="merge-ahead">←</span>\`) +
          \`<span class="merge-source">\${BRANCH}<span class="merge-cb">\${escapeHtml(repo.branch)}</span><span class="merge-ahead"> · \${ahead ? ahead + ' commit' + (ahead === 1 ? '' : 's') : 'up to date'}</span></span>\`;
      }
      if (row.parentElement !== _logEl) _logEl.appendChild(row);   // add if new; the observer keeps it LAST (don't move it here — that caused a scroll jump)
      if (_stickBottom) scrollToBottom();
    } catch { /* offline / no project */ }
    finally { _mergeBarBusy = false; }
  }
  // Keep the pinned merge row as the LAST element of the chat log: whenever new content is appended
  // after it, move it back to the bottom. So new messages always appear ABOVE it, and it never jumps
  // up from a mid-position (which scrolled the viewport when refreshMergeBar later re-pinned it).
  try {
    new MutationObserver(() => {
      const mr = document.getElementById('mergeRow');
      if (mr && mr.parentElement === _logEl && mr !== _logEl.lastElementChild) _logEl.appendChild(mr);
    }).observe(_logEl, { childList: true });
  } catch {}
  async function mergeBranchToParent() {
    const btn = document.querySelector('#mergeRow .merge-btn');
    if (btn) { if (btn.dataset.busy) return; btn.dataset.busy = '1'; btn.disabled = true; btn.textContent = 'merging…'; }
    try {
      const data = await (await fetch('/api/merge-to-parent', { method: 'POST' })).json();
      const r = (data.results || [])[0];
      if (data.ok && r?.merged) {
        addBubble('system', \`✓ Merged \\\`\${r.branch}\\\` into \\\`\${r.parent}\\\`\${r.commit ? \` — \${r.commit}\` : ''}.\`);
      } else {
        const f = (data.results || []).find((x) => !x.merged) || {};
        addBubble('system', \`✗ Merge failed: \${f.message || f.reason || data.error || 'unknown error'}\`);
      }
      if (_stickBottom) scrollToBottom();
    } catch (e) { addBubble('system', \`✗ Merge failed: \${e.message || e}\`); }
    refreshMergeBar();   // re-render — the pinned row stays (now "up to date")
  }
  // DIRECT-mode commit modal: AI-drafts a message, lets the user edit it, commits the working tree.
  async function openCommitModal() {
    const bg = document.getElementById('commitModalBg'); const ta = document.getElementById('commitMsgInput'); const err = document.getElementById('commitErr');
    if (!bg || !ta) return;
    if (bg.classList.contains('show')) return;   // already open — don't re-fetch on a double-click
    if (err) err.textContent = ''; ta.value = 'Generating a commit message…'; ta.disabled = true;
    bg.classList.add('show');
    try { const data = await (await fetch('/api/commit-message', { method: 'POST' })).json(); ta.value = data.message || 'Update working tree'; }
    catch { ta.value = 'Update working tree'; }
    ta.disabled = false; ta.focus(); ta.select();
  }
  function closeCommitModal() { document.getElementById('commitModalBg')?.classList.remove('show'); }
  async function submitCommit() {
    const ta = document.getElementById('commitMsgInput'); const err = document.getElementById('commitErr'); const btn = document.getElementById('commitDoBtn');
    const message = (ta?.value || '').trim();
    if (!message) { if (err) err.textContent = 'Enter a commit message.'; return; }
    if (btn) { btn.disabled = true; btn.textContent = 'Committing…'; } if (err) err.textContent = '';
    try {
      const data = await (await fetch('/api/commit-direct', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ message }) })).json();
      // ok — OR nothing to commit (already committed / transitioned to subbranch) → just close + refresh.
      if (data.ok || (data.results && data.results.length === 0)) { closeCommitModal(); refreshMergeBar(); }
      else if (err) err.textContent = (data.results || []).find((r) => !r.ok)?.detail || data.error || 'Commit failed.';
    } catch (e) { if (err) err.textContent = e.message || 'Commit failed.'; }
    if (btn) { btn.disabled = false; btn.textContent = 'Commit'; }
  }
  setInterval(() => { refreshGitDirty(); refreshMergeBar(); }, 8000);

  // WebLogic server indicator in the footer. running: true=up (green+lit),
  // false=down (red), null=not monitored (wl plugin inactive) → hidden.
  function setWeblogicStatus(running) {
    // "weblogic" chip in the chrome — green when up, red when down, hidden when
    // unknown (no wl plugin). No bullet; hover shows the status.
    const el = document.getElementById('wlIndicator');
    if (!el) return;
    if (running == null) { el.style.display = 'none'; el.classList.remove('up', 'down'); return; }
    el.style.display = '';
    el.classList.toggle('up', running === true);
    el.classList.toggle('down', running === false);
    el.title = running
      ? 'WebLogic is RUNNING (port 7101).'
      : 'WebLogic is STOPPED (nothing on port 7101). The agent can start it with "wl startWebLogic".';
  }
  // "edit allowed" (blue) ↔ "no edit allowed" (gray, struck). Backs the hidden
  // #discussToggle the send path reads (checked = read-only/discuss = NO edits).
  function syncEditToggle() {
    const cb = document.getElementById('discussToggle');
    const el = document.getElementById('editToggle');
    if (!cb || !el) return;
    const noEdit = cb.checked;
    el.textContent = 'Edit';   // always "Edit"; .off strikes it through when read-only (no edit)
    el.classList.toggle('off', noEdit);
    // Discuss (read-only) overrides Review server-side, so hide the now-inert review controls.
    const rl = document.getElementById('reviewLabel'); if (rl) rl.style.display = noEdit ? 'none' : '';
    const ai = document.getElementById('approveImplementBtn');
    if (ai && noEdit) ai.style.display = 'none';
  }
  function toggleEditAllowed() {
    const cb = document.getElementById('discussToggle');
    if (!cb) return;
    cb.checked = !cb.checked;
    syncEditToggle();
  }
  let _lastSendReview = false;   // was the last run a review (propose) run? → a proposal is waiting
  function setApproveButton(show) { const b = document.getElementById('approveImplementBtn'); if (b) b.style.display = show ? '' : 'none'; }
  function toggleReview() {
    // The label reads "Auto-approve" (default ON = apply directly). The hidden #reviewToggle is the
    // INVERSE: checked === true → review/propose-first ON → Auto-approve OFF (gray + struck through).
    const cb = document.getElementById('reviewToggle'); const lbl = document.getElementById('reviewLabel');
    if (!cb || !lbl) return;
    cb.checked = !cb.checked;
    lbl.classList.toggle('off', cb.checked);   // auto-approve OFF → grayed + crossed out (reuses .edit-toggle.off)
    // The approve button appears only AFTER a propose run produces a proposal (see run-completion);
    // turning Auto-approve back ON (review off) hides it.
    if (!cb.checked) { _lastSendReview = false; setApproveButton(false); }
  }
  // Adversarial parallel critic toggle (default OFF). The label shows .off (struck) when OFF.
  function toggleCritic() {
    const cb = document.getElementById('criticToggle'); const lbl = document.getElementById('criticLabel');
    if (!cb) return;
    cb.checked = !cb.checked;
    if (lbl) {
      lbl.classList.toggle('off', !cb.checked);   // OFF → grayed + struck through (reuses .edit-toggle.off)
      lbl.title = cb.checked
        ? 'Reviewer (ON) — a parallel reviewer with READ-ONLY tools investigates each agent step and only flags a problem it can PROVE with evidence from the code; the agent gets ONE chance to fix it (a reworked message isn\\'t re-reviewed). Click to turn off.'
        : 'Reviewer (OFF). When ON, a parallel reviewer with READ-ONLY tools investigates each agent step and only flags a problem it can PROVE with evidence from the code; the agent gets ONE chance to fix it (a reworked message isn\\'t re-reviewed). Click to toggle.';
    }
    try { localStorage.setItem('cb-reviewer-on', cb.checked ? '1' : '0'); } catch {}
  }
  // The eye next to the Reviewer chip: show/hide the reviewer's messages in the chat (independent of
  // whether the reviewer is running). OFF by default — the reviewer can run silently.
  // (_hiddenReviewerNotes is declared near the top of the script — the boot-restore
  // block runs before this point and toggleReviewerEye assigns it.)
  function bumpReviewerEyeBadge() {
    if (document.body.classList.contains('show-reviewer-notes')) return;   // visible → nothing hidden
    _hiddenReviewerNotes++;
    const b = document.getElementById('reviewerEyeBadge');
    if (b) { b.textContent = String(_hiddenReviewerNotes); b.style.display = ''; }
  }
  function toggleReviewerEye() {
    const on = document.body.classList.toggle('show-reviewer-notes');
    const eye = document.getElementById('reviewerEye');
    if (eye) {
      eye.classList.toggle('on', on);
      eye.classList.toggle('off', !on);
      eye.title = on
        ? 'Reviewer messages are VISIBLE in the chat. Click to hide them again (the reviewer keeps running either way).'
        : 'Show the reviewer\\'s messages in the chat (OFF = hidden). The reviewer can run silently; flip this on to watch what it flags.';
    }
    if (on) {   // revealed → the hidden-note count is now seen
      _hiddenReviewerNotes = 0;
      const b = document.getElementById('reviewerEyeBadge');
      if (b) { b.textContent = ''; b.style.display = 'none'; }
    }
    try { localStorage.setItem('cb-reviewer-eye', on ? '1' : '0'); } catch {}
  }
  // Deep-think chip (default OFF). Rides send() as the per-message \`deepThink\` flag; the server
  // snapshots it per run (and into each held sub-agent spawn), exactly like the Reviewer chip.
  function toggleDeepThink() {
    const cb = document.getElementById('deepThinkToggle'); const lbl = document.getElementById('deepThinkLabel');
    if (!cb) return;
    cb.checked = !cb.checked;
    if (lbl) {
      lbl.classList.toggle('off', !cb.checked);   // OFF → grayed + struck through (reuses .edit-toggle.off)
      lbl.title = cb.checked
        ? 'Deep think (ON) — on a genuine design decision the agent may pause mid-run: three independent perspectives + a judge examine the design question against the repo, and the agent gets the verdict as a tool result. Availability is limited (a few uses per run, with a cooldown). Click to turn off.'
        : 'Deep think (OFF). When ON, the agent may pause on a genuine design decision mid-run: three independent perspectives + a judge examine the design question against the repo, and the agent gets the verdict as a tool result. Availability is limited (a few uses per run, with a cooldown). Click to toggle.';
    }
    try { localStorage.setItem('cb-deepthink-on', cb.checked ? '1' : '0'); } catch {}
  }
  // The eye next to the Deep think chip: show/hide the 🧠 markers (independent of whether deep
  // think is enabled). OFF by default — deep think can run silently; results badge up while hidden.
  // (_hiddenDeepThinkNotes is declared near the top of the script — same TDZ story as the reviewer's.)
  function bumpDeepThinkEyeBadge() {
    if (document.body.classList.contains('show-deepthink-notes')) return;   // visible → nothing hidden
    _hiddenDeepThinkNotes++;
    const b = document.getElementById('deepThinkEyeBadge');
    if (b) { b.textContent = String(_hiddenDeepThinkNotes); b.style.display = ''; }
  }
  function toggleDeepThinkEye() {
    const on = document.body.classList.toggle('show-deepthink-notes');
    const eye = document.getElementById('deepThinkEye');
    if (eye) {
      eye.classList.toggle('on', on);
      eye.classList.toggle('off', !on);
      eye.title = on
        ? 'Deep-think markers are VISIBLE in the chat. Click to hide them again (deep think keeps working either way).'
        : 'Show deep-think markers in the chat (OFF = hidden). Deep think can run silently; flip this on to watch the questions and verdicts.';
    }
    if (on) {   // revealed → the hidden-verdict count is now seen
      _hiddenDeepThinkNotes = 0;
      const b = document.getElementById('deepThinkEyeBadge');
      if (b) { b.textContent = ''; b.style.display = 'none'; }
    }
    try { localStorage.setItem('cb-deepthink-eye', on ? '1' : '0'); } catch {}
  }
  // Live checklist panel: the agent's <checklist> for multi-step work, ticking off in place.
  // Re-rendered each time a fresh <checklist> arrives (the agent re-emits the full list with
  // updated [x]); hidden when empty/absent. One panel for the main chat.
  function renderChecklist(items) {
    const panel = document.getElementById('checklistPanel');
    if (!panel) return;
    clearTimeout(panel._hideT);
    if (!Array.isArray(items) || !items.length) { panel.style.display = 'none'; panel.innerHTML = ''; return; }
    const done = items.filter((i) => i && i.done).length;
    const rows = items.map((i) =>
      \`<div class="checklist-item\${i && i.done ? ' done' : ''}"><span class="cl-box">\${i && i.done ? '✓' : '☐'}</span><span class="cl-text">\${escapeHtml((i && i.text) || '')}</span></div>\`
    ).join('');
    panel.innerHTML = \`<div class="cl-title">checklist · \${done}/\${items.length} done</div>\${rows}\`;
    panel.style.display = '';
    // All steps done → show the completed list briefly, then auto-hide (the work is finished).
    if (done === items.length) panel._hideT = setTimeout(() => { panel.style.display = 'none'; panel.innerHTML = ''; }, 6000);
  }
  // Approve the proposal the agent just made and implement it: one run with review OFF (write
  // mode), so it applies exactly what it proposed. The review toggle stays on for the next cycle.
  async function approveImplement() {
    _lastSendReview = false;     // this run IMPLEMENTS (review off) — no new proposal results
    setApproveButton(false);
    // Mirror send(): the chips the user left ON govern the run they approve —
    // the implementing run keeps its continuous reviewer + deep_think offer.
    // review stays false (intentional: this run must WRITE, not re-propose).
    const critic = document.getElementById('criticToggle')?.checked;
    const deepThink = document.getElementById('deepThinkToggle')?.checked;
    try {
      await fetchWithTimeout('/api/message', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ text: 'Approved — go ahead and implement the proposal you just described.', model: $model.value, review: false, critic: critic || undefined, deepThink: deepThink || undefined }),
      }, 15000);
    } catch (e) { /* surfaced via SSE/status as usual */ }
  }

  // ── Shadow-git "undo last run" (non-repo targets) ───────────────────────
  function setUndoButton(canUndo, request) {
    const b = document.getElementById('undoRunBtn');
    if (!b) return;
    b.style.display = canUndo ? '' : 'none';
    if (canUndo && request) b.title = \`Undo the last run ("\${request}") — this folder isn't a git project, so code_boss kept an internal snapshot.\`;
  }
  async function refreshUndoButton() {
    try { const s = await (await fetch('/api/state?limit=1')).json(); setUndoButton(!!s.canUndo, s.undoRequest); } catch {}
  }
  async function undoLastRun() {
    const b = document.getElementById('undoRunBtn');
    if (b && b.dataset.busy) return; if (b) b.dataset.busy = '1';
    try {
      const j = await (await fetch('/api/undo-last-run', { method: 'POST' })).json().catch(() => ({}));
      if (j.ok) {
        setUndoButton(false);
        // Files are reverted; now TELL the agent (a fresh turn — history stays intact) so it can
        // acknowledge/adapt instead of assuming its changes are still there.
        try {
          await fetchWithTimeout('/api/message', { method: 'POST', headers: { 'content-type': 'application/json' },
            body: JSON.stringify({ text: \`I used Undo to revert the file changes from the last run\${j.request ? \` ("\${j.request}")\` : ''} — those edits are gone, the files are back to how they were before that run.\`, model: $model.value }) }, 15000);
        } catch (e) { /* the revert already happened; the notify is best-effort */ }
      } else { alert(j.note || j.error || 'Nothing to undo.'); }
    } catch (e) { showError('Undo failed', e); }
    finally { if (b) delete b.dataset.busy; }
  }
  // The agent suggested reverting the last run — offer Allow (runs the undo) / Keep changes.
  function showRollbackPrompt(reason) {
    const log = document.getElementById('log');
    if (!log) return;
    const old = document.getElementById('rollbackPrompt'); if (old) old.remove();
    const d = document.createElement('div');
    d.id = 'rollbackPrompt'; d.className = 'bubble system';
    d.appendChild(document.createTextNode('↶ The agent suggests rolling back the last run: ' + (reason || '') + '  '));
    const allow = document.createElement('button'); allow.className = 'btn'; allow.textContent = 'Allow rollback';
    const keep = document.createElement('button'); keep.className = 'btn'; keep.textContent = 'Keep changes';
    allow.onclick = () => { d.remove(); undoLastRun(); };
    keep.onclick = () => { d.remove(); };
    d.appendChild(allow); d.appendChild(document.createTextNode(' ')); d.appendChild(keep);
    log.appendChild(d); d.scrollIntoView({ block: 'end' });
  }

  // ── Tab leadership (single live SSE) ────────────────────────────────────
  // Each open code_boss tab would hold its own SSE connection; several tabs
  // exhaust the browser's ~6-connections-per-origin pool and silently hang
  // EVERY fetch (send, settings, snapshot). So only ONE tab is ACTIVE (holds
  // the SSE) at a time — newest wins. Opening or re-activating a tab broadcasts
  // a CLAIM; any tab with an older claim steps down to PASSIVE: it closes its
  // SSE (freeing the connection) and shows a "use this tab" banner. Clicking it
  // reclaims leadership. Single-user app — one live view is all that's needed.
  // Falls back to no-op (current single-tab behavior) if BroadcastChannel is
  // unavailable.
  (function setupTabLeadership() {
    let bc;
    try { bc = new BroadcastChannel('code_boss_tabs'); } catch { return; }
    const TAB_ID = Math.random().toString(36).slice(2);
    let myClaim = 0;                          // higher = newer; newest wins
    const banner = document.createElement('div');
    banner.id = 'tabWarn';
    document.body.appendChild(banner);
    function broadcastClaim() {
      myClaim = Date.now() + Math.random();
      try { bc.postMessage({ type: 'claim', id: TAB_ID, t: myClaim }); } catch {}
    }
    function showPassiveBanner(text) {
      banner.innerHTML = escapeHtml(text) + ' <button id="tabReclaim" class="btn-sm">Use this tab here</button>';
      banner.classList.add('show');
      const b = document.getElementById('tabReclaim');
      if (b) b.onclick = becomeActive;
    }
    function becomeActive() {
      _tabActive = true;
      _ssePaused = false;
      banner.classList.remove('show');
      broadcastClaim();                      // tell the other tabs to step down
      refetchAndReconnect();                 // reopen SSE + refresh to current state
    }
    function goPassive() {
      _tabActive = false;
      _ssePaused = true;
      try { _es?.close(); } catch {}
      _es = null;
      showPassiveBanner('Another code_boss tab is now the active one (only one runs at a time to avoid stalls).');
    }
    bc.onmessage = (e) => {
      const m = e.data;
      if (!m || m.id === TAB_ID) return;
      // A newer claim than ours → we step down. (Ties broken by the random
      // fractional part of t, so two tabs never both stay active or both drop.)
      if (m.type === 'claim' && typeof m.t === 'number' && m.t > myClaim) goPassive();
      else if (m.type === 'bye' && !_tabActive) showPassiveBanner('The active code_boss tab closed.');
    };
    // Claim leadership on load (newest wins). connectStream() runs later in
    // init and will open the SSE because this tab is active.
    broadcastClaim();
    window.addEventListener('beforeunload', () => { try { bc.postMessage({ type: 'bye', id: TAB_ID }); } catch {} });
  })();

  // The spinner tick (elapsed clock) now lives INSIDE the spinner instance;
  // startSpinTick / stopSpinTick are thin wrappers over status.startTick /
  // status.stopTick (defined with the spinner). Callers set chatStartedAtLocal
  // before startSpinTick (renderSessionInChat / the running-status case).

  let _initialized = false;       // initialize() flips this once chat is allowed
  let _pendingSession = null;     // SSE init arriving during init is parked here

  function renderSessionInChat(s) {
    showChat(activeProject);
    // Re-arm infinite-scroll from THIS snapshot. The snapshot is the most
    // recent <=100 events and carries hasMoreOlder. Without resetting here,
    // an SSE reconnect / refetch that replaces session.events would leave
    // _noMoreOlder stuck from a prior full scroll-back, permanently killing
    // scroll-to-load-older for the rest of the session.
    _loadingOlder = false;
    _noMoreOlder = (s.hasMoreOlder === false);
    // Only wipe the welcome banner if there are real events to render in
    // its place. Empty-session initialization should leave the welcome
    // visible so the user has something to read while typing.
    if (s.events && s.events.length > 0) {
      clearLog();
      for (const ev of s.events) renderEvent(ev);
    }
    applyControlsForStatus();
    renderTimeoutChip();
    if (s.status === 'running') {
      chatStartedAtLocal = s.chatStartedAt || Date.now();
      // A page that loads/reconnects MID-RUN gets its status row NOW — without
      // this the spinner/elapsed/esc-hint only appear on the NEXT phase event.
      // Mirrors the status-delta running branch in applyDelta; setThinking is
      // the best guess for an unknown mid-run phase (the next phase/token
      // delta corrects it to uploading/downloading automatically).
      ensureStatusRow();
      startSpinner();
      setThinking();
      startSpinTick();
    } else {
      stopSpinTick();
    }
    renderStatus();
    loadBacklog();
  }

  function onInit(s) {
    session = s;
    window.__sessionRef = s;     // for tests; mirror the live session object
    activeProject = s.activeProject;
    setWeblogicStatus(s.weblogicRunning ?? null);   // restore the footer indicator on connect/reload
    // If a 401 unlock is in flight when we connect, re-show the modal so
    // a reload or late-arriving tab doesn't leave the user stranded.
    if (s.pendingUnlockUrl) showUnlockModal(s.pendingUnlockUrl);
    // Re-show a mid-task developer-approval prompt so a reload / late tab
    // doesn't strand the paused agent.
    // Restore the live checklist panel on reload/reconnect (the server is still driving it).
    onChecklist(null, s.lastChecklist || null);
    attachChecklistScrollSync();   // the Checklists tab follows the task at the viewport middle as you scroll
    setUndoButton(!!s.canUndo, s.undoRequest);   // restore the shadow-git "undo last run" button
    // Re-seed the Sub-agents panel from the snapshot's LIVE entries (the fork +
    // orchestrator.subagents()). Lifecycle pings are broadcast-only, so without
    // this a reload/late tab loses every working sub-agent — including the
    // crash-recovery rows adopted before any browser connected. Same mapping as
    // the subagent-event SSE handlers; recovered entries held awaiting-review
    // surface as 'recovered' so the Review/Discard affordance renders.
    for (const sa of (s.subagents || [])) {
      if (!sa || !sa.id) continue;
      const st = sa.recovered && sa.status === 'awaiting-review' ? 'recovered' : (sa.status || 'working');
      saUpsert(sa.id, { topic: sa.topic || sa.id, status: st, recovered: !!sa.recovered });
    }
    if (!activeProject) {
      showLanding(s.recent || []);
      stopSpinTick();
      return;
    }
    // Chat shouldn't be visible until initialize() has confirmed the system
    // is usable (API key + models). If init isn't done, just remember the
    // session — initialize() will render it on completion.
    if (_initialized) renderSessionInChat(s);
    else _pendingSession = s;
  }

  function applyDelta(msg) {
    if (!session) return;
    session.version = msg.version;
    switch (msg.type) {
      case 'backlog':
        // The assignment backlog changed server-side — refetch + re-render.
        loadBacklog();
        break;
      case 'memory':
        // Codebase memory changed server-side (consolidation/curation). Do NOT
        // re-fetch into an OPEN editor — that would wipe the developer's
        // in-progress edits. openMemory() always fetches fresh on next open.
        break;
      case 'event':
        // Sub-agent tool activity is routed to that sub-agent's detail drawer, NOT the
        // main chat (the foreground stays uncluttered; the bounded result folds in later
        // as a subagent-card). Tagged by the server with subagentChannel.
        // Cognition tree (CB_COGNITION): live thinking-tree updates → the Cognition tab.
        if (msg.event && msg.event.type === 'cognition-event') { handleCognitionEvent(msg.event.cog); break; }
        if (msg.event && msg.event.subagentChannel) { saAddActivity(msg.event.subagentChannel, msg.event); break; }
        // tool-progress streams live stdout from a running tool — render
        // it but do NOT keep it in session.events. The server doesn't
        // persist it either (see appendChatEvent in server.mjs). Pushing
        // here would replay every chunk on full re-renders (event-patch
        // case below) — visually fine but wasteful, and worse: when the
        // conversation is reloaded from disk after a restart we'd be
        // replaying chunks for a tool that has long since finished.
        // context-fill is a per-turn gauge update, not a chat row — update the
        // bar and stop (the server never persists it; don't push it either).
        if (msg.event.type === 'context-fill') {
          status.setContextFill({ totalTokens: msg.event.totalTokens, budgetTokens: msg.event.budgetTokens, budgetPct: msg.event.budgetPct });
          renderStatus();
          break;
        }
        // task-progress is a per-turn signal (the agent's completion estimate +
        // what remains), not a chat row — update the chip and stop.
        if (msg.event.type === 'task-progress') {
          status.setTaskProgress({ percent: msg.event.percent, note: msg.event.note });
          renderStatus();
          break;
        }
        // checklist is a live, in-place signal (the agent's <checklist> for multi-step work,
        // ticking off as it goes) — update the pinned panel, don't push a chat row. (Main chat
        // only for now; sub-agent checklists ride their drawer later.)
        if (msg.event.type === 'checklist') {
          if (!msg.event.subagentChannel) onChecklist(msg.event.taskId, msg.event.items);
          break;
        }
        // The agent suggests reverting the last run → offer Allow / Cancel (main chat only).
        if (msg.event.type === 'rollback-suggested') {
          if (!msg.event.subagentChannel) showRollbackPrompt(msg.event.reason);
          break;
        }
        if (msg.event.type !== 'tool-progress') session.events.push(msg.event);
        renderEvent(msg.event);
        break;
      case 'subagent-event': {
        // orchestrator lifecycle pings (spawn → working → awaiting-review → reviewing →
        // [resume → working]* → done/rejected). Patch the sub-agent's status in saStore.
        const e = msg.event || {};
        if (e.type === 'subagent-spawned') saUpsert(e.id, { topic: e.topic, status: 'working' });
        else if (e.type === 'subagent-result') saUpsert(e.id, { status: 'awaiting-review' });
        else if (e.type === 'subagent-reviewing') saUpsert(e.id, { status: 'reviewing' });
        else if (e.type === 'subagent-resume') { saUpsert(e.id, { status: 'working' }); saAddActivity(e.id, { type: 'feedback-round', round: e.round, feedback: e.feedback }); }
        else if (e.type === 'subagent-review-exhausted') saUpsert(e.id, { status: 'rejected' });
        else if (e.type === 'subagent-recovered') saUpsert(e.id, { topic: e.topic, status: 'recovered', recovered: true });
        else if (e.type === 'subagent-done') saUpsert(e.id, { status: e.status || 'done' });
        break;
      }
      case 'event-patch': {
        const ev = session.events.find((e) => e.id === msg.eventId);
        if (ev) {
          Object.assign(ev, msg.patch);
          // Simple Phase 4 strategy: re-render all events (Phase 5 will be smarter)
          clearLog();
          for (const e2 of session.events) renderEvent(e2);
        } else {
          // A drawer-routed event (a forked task's review row lives in saStore, not
          // session.events) — patch it there + re-render that sub-agent's drawer.
          for (const sa of saStore.values()) {
            const a = sa.activity.find((e) => e.id === msg.eventId);
            if (a) { Object.assign(a, msg.patch); if (saDrawerId === sa.id) renderSubagentDetail(sa.id); break; }
          }
        }
        break;
      }
      case 'status':
        session.status = msg.status;
        applyControlsForStatus();
        if (msg.status === 'running') {
          chatStartedAtLocal = Date.now();
          // Create the status DOM row and kick off the spinner. Without this
          // call, renderStatus bails out (statusRow is null) and the user
          // never sees the spinner / arrow / token counts.
          // (ensureStatusRow reuses the prior row, so any canceled/done
          // content from the last turn gets replaced by setUploading.)
          ensureStatusRow();
          startSpinner();
          setUploading();           // initial phase = uploading
          startSpinTick();
        } else if (msg.status === 'done' || msg.status === 'error') {
          // User-initiated cancel surfaces as status=error with
          // session.error === 'aborted' (server-side flag set in
          // /api/message catch when the AbortController fires from
          // /api/cancel). Show a dedicated "canceled by user" line
          // instead of the regular down-arrow done-state.
          if (msg.status === 'error' && session.error === 'aborted') {
            setCanceled();
          } else {
            setDone();
          }
          stopSpinTick();
          refreshUndoButton();   // a write-mode run may have created an undo point (non-repo targets)
          // The post-run shadow snapshot lands in the server's finally AFTER this status
          // flip (a no-change run CLEARS the undo point there) — re-check once it settles.
          setTimeout(refreshUndoButton, 2500);
          refreshMergeBar();     // a finished run likely added commits → refresh the ahead-count
          if (msg.status === 'done') setApproveButton(_lastSendReview);   // a completed PROPOSE run → a proposal awaits approval
          // Defensive: any tool row still marked running (e.g., agent
          // aborted mid-tool) should stop blinking now.
          RC.log.querySelectorAll('.row.tool.tool-running').forEach((r) => r.classList.remove('tool-running'));
          // Sweep ORPHANED streaming bubbles whose content is ONLY tool-tag noise (see
          // sweepOrphanToolTagBubbles). On terminal status, flush each streaming renderer first.
          sweepOrphanToolTagBubbles(true);
          // Repos most likely changed during the chat — refresh the
          // footer indicator now rather than waiting for the next poll.
          refreshGitDirty();
        } else {
          stopSpinTick();
        }
        renderStatus();
        break;
      case 'phase':
        session.phase = msg.phase;
        // Map server phase strings to the UI's status row phase functions.
        if (msg.phase === 'uploading')        { status.setVerb(VERB); setUploading(); }
        else if (msg.phase === 'thinking')    { status.setVerb(VERB); setThinking(); }
        else if (msg.phase === 'downloading') { status.setVerb(VERB); setDownloading(); }
        // Manager phases: the worker is done; the reviewer / acceptance manager
        // is now running. Keep the spinner alive but relabel WHO is busy.
        else if (msg.phase === 'reviewing')   { status.setVerb('Reviewing'); setThinking(); }
        else if (msg.phase === 'manager')     { status.setVerb('Checking the assignment'); setThinking(); }
        // Network retry/backoff: the LLM endpoint was unreachable and we're
        // retrying. "reconnecting:N/M" carries the attempt count. Make it visibly
        // distinct from a normal slow turn (recovery flips back to "Working").
        else if (msg.phase.indexOf('reconnecting') === 0) {
          const m = (msg.phase.split(':')[1] || '').trim();
          status.setVerb('Reconnecting to the LLM' + (m ? \` (attempt \${m})\` : ''));
          setThinking();
        }
        renderStatus();
        break;
      case 'tokens':
        session.tokens = msg.tokens;
        // Mirror server-side token counts into the spinner (which also infers
        // DOWNLOADING from which side moved — phase events can lag/skip).
        const newUp = msg.tokens.up || 0;
        const newDown = msg.tokens.down || 0;
        status.applyTokens(newUp, newDown);
        if ($settingsTokens) $settingsTokens.textContent = (newUp + newDown).toLocaleString();
        renderStatus();
        break;
      case 'error':
        session.error = msg.error;
        // User-initiated cancel surfaces as error='aborted'; suppress
        // the modal since the canceled status line already conveys it.
        if (msg.error && msg.error !== 'aborted') {
          showError('Agent error', new Error(msg.error));
        }
        break;
      case 'weblogic-status':
        if (session) session.weblogicRunning = msg.running;
        setWeblogicStatus(msg.running);
        break;
      case 'timeout':
        session.timeoutMs = msg.timeoutMs;
        renderTimeoutChip();
        break;
      case 'reset': {
        session.events = [];
        clearLog();
        _stickBottom = true; hideScrollArrow();   // cleared chat → re-lock to the bottom
        _checklistsByTask.clear(); _currentChecklistTaskId = null;   // drop all per-task checklists on Clear/reset
        { const c = document.getElementById('checklistCount'); if (c) c.textContent = ''; }
        updateChecklistViews();   // hide the footer + clear the tab
        setUndoButton(false);    // and the undo button
        setApproveButton(false); _lastSendReview = false;   // and the approve button
        // re-hide the on-demand side-tabs (Cognition / Sub-agents) so a cleared session is clean
        for (const id of ['tabbtn-cognition', 'tabbtn-subagents']) { const t = document.getElementById(id); if (t) t.style.display = 'none'; }
        break;
      }
      case 'unlock-needed':
        console.log('[sse] unlock-needed', msg.url);
        showUnlockModal(msg.url);
        break;
      case 'unlock-done':
        console.log('[sse] unlock-done');
        _unlockPending = false;
        // Server signals the unlock dance is complete. Close the init
        // modal if it's still on the unlock screen (could have been
        // closed already by the local click handler).
        if ($initModalBg.classList.contains('show') && $initModalTitle.textContent === 'API key locked') {
          $initModalBg.classList.remove('show');
        }
        // If the models fetch aborted/failed while we waited on the unlock,
        // retry it now (the key is unlocked, so it succeeds fast and clears the
        // "Failed to load models" error). Skip if the list is already populated.
        {
          const opts = Array.from($model?.options || []).map((o) => o.value);
          const loaded = opts.length && !opts.some((v) => /waiting for unlock|load failed|no models/i.test(v));
          if (!loaded) loadModels();
        }
        break;
    }
    // Some compaction events the server emits as regular chat events via
    // appendChatEvent — they show up as msg.type === 'event' with the inner
    // event carrying a different type. Handle those here too.
  }

  // When the server's compaction demotes a task or rolls it up, the agent loop
  // emits special events through onChatEvent. These come through as normal
  // 'event' deltas, but their inner type triggers UI-only effects (mute, wrap).
  function handleCompactionEvent(ev) {
    if (!ev || !ev.type) return;
    if (ev.type === 'task-tier-change') {
      muteTaskRows(ev.taskId, ev.muted);
      return true;
    }
    if (ev.type === 'task-rollup') {
      wrapRollup(ev);
      return true;
    }
    if (ev.type === 'task-archived') {
      // Mute the whole wrap heavily as an "archived" indicator
      const wrap = RC.log.querySelector(\`.task-wrap[data-task-id="\${ev.taskId}"]\`);
      if (wrap) wrap.classList.add('muted');
      return true;
    }
    return false;
  }

  function muteTaskRows(taskId, mode) {
    const rows = RC.log.querySelectorAll(\`[data-task-id="\${taskId}"]\`);
    rows.forEach((r) => {
      if (mode === 'results') {
        if (r.classList.contains('row-result') || r.classList.contains('diff-block')) {
          r.classList.add('muted');
        }
      } else if (mode === 'all') {
        r.classList.add('muted');
      }
    });
  }

  function wrapRollup(ev) {
    // Find the existing task-wraps for the child task IDs.
    const children = (ev.childTaskIds || [])
      .map((id) => RC.log.querySelector(\`.task-wrap[data-task-id="\${id}"]\`))
      .filter(Boolean);
    if (children.length === 0) return;

    // Mute all child wraps — agent now only sees the project summary.
    children.forEach((c) => c.classList.add('muted'));

    // Build a project wrap that contains all the existing child wraps.
    const project = document.createElement('div');
    project.className = 'task-wrap project';
    project.dataset.taskId = ev.parentTaskId;

    const head = document.createElement('div');
    head.className = 'task-head';
    const tag = document.createElement('span');
    tag.className = 'tag';
    tag.textContent = 'project';
    head.appendChild(tag);

    if (ev.userRequest) {
      const ur = document.createElement('span');
      ur.className = 'ur';
      ur.textContent = ev.userRequest;
      head.appendChild(ur);
    }
    project.appendChild(head);

    const body = document.createElement('div');
    body.className = 'task-body';
    project.appendChild(body);

    // Insert the project wrap at the position of the FIRST child wrap.
    const firstChild = children[0];
    RC.log.insertBefore(project, firstChild);
    // Move all child wraps into the project body.
    for (const c of children) body.appendChild(c);

    // Footer: chevron + the project summary. The summary is the rollup's primary
    // content (the LLM sees only it), so it uses its OWN class (.task-foot-project)
    // that stays visible in BOTH states — unlike a leaf .task-foot-summary.
    const foot = document.createElement('div');
    foot.className = 'task-foot';
    const chev = document.createElement('span');
    chev.className = 'task-foot-toggle';
    chev.title = 'Collapse / expand';
    chev.onclick = (e) => { e.stopPropagation(); setTaskCollapsed(project, foot, !project.classList.contains('collapsed'), true); };
    foot.appendChild(chev);
    const sum = document.createElement('span');
    sum.className = 'task-foot-project';
    sum.textContent = ev.summary || '(no summary)';
    foot.appendChild(sum);
    project.appendChild(foot);

    head.onclick = () => setTaskCollapsed(project, foot, false, true);
    setTaskCollapsed(project, foot, _taskCollapsed.get(String(ev.parentTaskId)) || false);

    scrollToBottom();
  }

  function clearLog() {
    RC.log.innerHTML = '';
    RC.clearStatus?.();   // drop the active chat's spinner row ref (its DOM is gone)
  }

  function renderEvent(ev) {
    if (!ev || !ev.type) return;
    // Sub-agent activity never renders in the main chat — it's routed to that
    // sub-agent's drawer (defensive: live events are intercepted in the SSE dispatch;
    // this also catches any that arrive via a full re-render).
    if (ev.subagentChannel) { saAddActivity(ev.subagentChannel, ev); return; }

    // Compaction events drive UI-only effects (mute classes, project wrap).
    if (ev.type === 'task-tier-change' || ev.type === 'task-rollup' || ev.type === 'task-archived') {
      handleCompactionEvent(ev);
      return;
    }

    // task-end is special: it wraps prior events instead of appending a row.
    if (ev.type === 'task-end') {
      wrapTaskRetroactively(ev);
      return;
    }

    let node = null;
    switch (ev.type) {
      case 'user-message': node = addBubble('user', ev.content || ''); break;
      case 'reviewer-note': {
        const d = reviewerNoteEl(ev.content);
        RC.log.appendChild(d); node = d;
        bumpReviewerEyeBadge();   // landed while hidden → count it on the eye
        if (_stickBottom) scrollToBottom();
        break;
      }
      case 'deep-think': {
        // 🧠 marker, hidden behind its own eye. 'start' appends a transient
        // running… line; the matching 'result' replaces it in place.
        const d = placeDeepThinkNote(RC.log, ev); node = d;
        if (ev.phase === 'result') bumpDeepThinkEyeBadge();   // a verdict landed while hidden → count it on the eye
        if (_stickBottom) scrollToBottom();
        break;
      }
      case 'assistant-text': {
        // If a streaming bubble for this (taskId, turn, segmentIndex) is
        // already visible from chunk events, replace its content with the
        // canonical (sanitized + renderInline'd) version instead of
        // duplicating it. \`turn\` is required: segmentIndex resets per turn.
        const segKey = (ev.taskId || '') + ':' + (ev.turn ?? 0) + ':' + (ev.segmentIndex ?? 0);
        const existing = RC.log.querySelector(\`.row.assistant[data-streaming="true"][data-segment-key="\${CSS.escape(segKey)}"]\`);
        if (existing) {
          const body = existing.querySelector('.body');
          if (body) body.innerHTML = renderInline(ev.content || '');
          existing._smd = null;   // streaming renderer done; canonical render now owns the body
          existing.removeAttribute('data-streaming');
          node = existing;
          break;
        }
        const body = addRow('assistant', '');
        body.innerHTML = renderInline(ev.content || '');
        node = body.parentElement;     // the .row
        break;
      }
      case 'assistant-text-chunk': {
        // Live token streaming. Same (taskId, turn, segmentIndex) → same
        // bubble; a different turn/segmentIndex starts a fresh one. Marked
        // data-streaming so the matching assistant-text event can find +
        // finalize it. \`turn\` prevents turn N+1 from appending onto a
        // dangling turn-N bubble (segmentIndex resets to 0 each turn).
        const segKey = (ev.taskId || '') + ':' + (ev.turn ?? 0) + ':' + (ev.segmentIndex ?? 0);
        let row = RC.log.querySelector(\`.row.assistant[data-streaming="true"][data-segment-key="\${CSS.escape(segKey)}"]\`);
        const incoming = ev.content || '';
        if (!row) {
          // Defer creating the bubble until there is NON-whitespace content. A
          // whitespace-only prose segment (e.g. a newline between two tool
          // blocks) otherwise leaves an empty "•" bubble for the whole turn:
          // sanitize drops empty segments, so the finalize event that would
          // replace/remove it never arrives, and the terminal-status sweep only
          // runs when the turn ends. No row until there's something to show.
          if (!incoming.trim()) { node = null; break; }
          const body = addRow('assistant', '');
          row = body.parentElement;
          row.setAttribute('data-streaming', 'true');
          row.setAttribute('data-segment-key', segKey);
        }
        const body = row.querySelector('.body');
        if (body) {
          // Incremental markdown: feed each chunk to a per-bubble streaming
          // renderer that emits blocks (tables/lists/code/headings) element-by-
          // element as lines complete, instead of showing raw markdown until the
          // finalize event snaps it to renderInline(). The final assistant-text
          // still replaces with renderInline() as the authoritative end state.
          if (!row._smd) { body.textContent = ''; row._smd = createStreamingMd(body); row._rawStream = ''; row._fedLen = 0; }
          // Strip tool-block tags LIVE — feed only the confirmed-prose delta, so the raw
          // <remote-workspace> XML never appears while it streams (it becomes a tool-call row).
          row._rawStream += incoming;
          const clean = stripToolTagsLive(row._rawStream);
          if (clean.length > row._fedLen) { row._smd.feed(clean.slice(row._fedLen)); row._fedLen = clean.length; }
        }
        node = row;
        scrollToBottom();
        break;
      }
      case 'tool-call': {
        // The block just parsed — drop any orphan prose bubble that streamed its raw tag, so the
        // raw <powershell …>/<edit …> tag doesn't linger mid-run (it used to clear only at run-end).
        sweepOrphanToolTagBubbles(false);
        // Suppress next-task chrome from the chat — the just-closed task
        // gets a wrap right after, which conveys the switch by itself.
        if ((ev.verb || ev.name) === 'next-task') break;
        // Newlines → "↵ " marker so a multi-line command stays on one
        // chat row instead of breaking with confusing indentation.
        const fmtVal = (v) => String(v).replace(/\\s+$/, '').replace(/\\r?\\n/g, ' ↵ ');
        // Args that are usually huge (file contents, find/replace bodies).
        // The diff row right after an edit already shows what changed; the
        // full text is available via the expand-on-click below.
        const NOISY_ARG_KEYS = new Set(['content', 'find', 'replace']);
        const args = ev.args || {};
        const visibleArgs = Object.entries(args).filter(([k]) => !NOISY_ARG_KEYS.has(k));
        let argStr;
        if (visibleArgs.length === 1 && (visibleArgs[0][0] === 'path' || visibleArgs[0][0] === 'command')) {
          // Single-path tools (read, write, edit, list_dir): drop the
          // \`path=\` prefix — \`edit(hello.txt)\` reads cleaner.
          argStr = fmtVal(visibleArgs[0][1]);
        } else {
          argStr = visibleArgs.map(([k, v]) => \`\${k}=\${fmtVal(v)}\`).join(', ');
        }
        const verb = ev.verb || ev.name;
        // Drop the parens entirely when there are no meaningful args
        // (visibleArgs is empty, or the single arg's value is blank).
        // \`wl(args=)\` reads worse than just \`wl\`.
        const fullLine = argStr.trim() === '' ? verb : \`\${verb}(\${argStr})\`;
        // Same cap for EVERY tool — was 120 per-arg, which gave
        // edit(path=…, find=long, replace=long) ~360 chars but
        // powershell(command=long) only 120 + the wrapper. Now both top
        // out at the same width with a trailing "…" so the user knows
        // there's more to see.
        const TOOL_DISPLAY_MAX = 200;
        const truncated = fullLine.length > TOOL_DISPLAY_MAX;
        const display = truncated ? fullLine.slice(0, TOOL_DISPLAY_MAX - 1) + '…' : fullLine;
        // Group consecutive memory saves into ONE collapsible row instead of N tool rows.
        if (verb === 'remember') { appendRememberItem(ev, display); break; }
        const body = addRow('tool', display);
        const row = body.parentElement;
        // Tag the row with its callId so the matching tool-result event
        // can find it (to stop the blinking dot). Mark as running for
        // now; the tool-result render below will clear the class once
        // it arrives.
        if (ev.callId) row.dataset.callId = ev.callId;
        row.classList.add('tool-running');
        // Filtered noisy args still exist on ev.args; surface them on
        // click so the user can audit what a long powershell command or
        // a multi-line edit actually contained.
        const hasHidden = visibleArgs.length < Object.keys(args).length;
        if (truncated || hasHidden) {
          row.classList.add('expandable');
          const pre = document.createElement('pre');
          pre.className = 'tool-call-full';
          pre.style.display = 'none';
          // Pretty-print all args with their REAL multi-line content.
          const fullArgs = Object.entries(args)
            .map(([k, v]) => \`  \${k}: \${String(v)}\`)
            .join('\\n\\n');
          pre.textContent = \`\${verb}(\\n\${fullArgs}\\n)\`;
          body.appendChild(pre);
          row.addEventListener('click', (e) => {
            if (e.target.tagName === 'PRE') return;   // text selection inside
            pre.style.display = pre.style.display === 'none' ? 'block' : 'none';
          });
        }
        node = row;
        // Append a "running…" placeholder result row right after the
        // tool call. tool-progress events will update its summary +
        // live-output pre; tool-result will remove it and a final
        // result row will appear in its place. If we're re-rendering
        // an old log where the matching tool-result already exists in
        // session.events, the result event will fire next in this same
        // loop and remove the placeholder — net effect: no placeholder
        // for completed tools.
        if (ev.callId) addRunningResultRow(ev.callId);
        break;
      }
      case 'tool-progress':
        appendProgress(ev.callId, ev.chunk);
        break;
      case 'tool-result': {
        // Pair with the next-task tool-call suppression above.
        if ((ev.verb || ev.name) === 'next-task') break;
        // remember results are folded into the grouped memory row (no standalone result row).
        if ((ev.verb || ev.name) === 'remember') break;
        // The matching tool-call row's blinking dot should stop now.
        if (ev.callId) {
          const cid = CSS.escape(ev.callId);
          const callRow = RC.log.querySelector(\`.row.tool[data-call-id="\${cid}"]\`);
          if (callRow) {
            callRow.classList.remove('tool-running');
            // Reflect a failed/cancelled result on the CALL row's dot too, so a
            // tool that errored (or was cancelled via Escape) doesn't keep a
            // green "success" bullet next to a red result line.
            callRow.classList.toggle('tool-err', ev.status === 'error');
          }
          // Drop the running placeholder and its live-output buffer —
          // the proper result row below carries the full final content.
          const placeholder = RC.log.querySelector(\`.row-result.running[data-call-id="\${cid}"]\`);
          if (placeholder) placeholder.remove();
          _progressBuffers.delete(ev.callId);
        }
        node = addResultRow({
          status: ev.status,
          summary: summarizeResult(ev),
          full: ev.content || '',
        }, ev.callId);
        maybeAttachOpenButton(node, ev);
        break;
      }
      case 'diff':
        node = addDiffBlock(ev);
        break;
      case 'plan-review-start': {
        const n = ev.count || 0;
        node = addRow('plan-review', \`⚖ hardening the plan — \${n} adversarial critic\${n === 1 ? '' : 's'} reading the code…\`).parentElement;
        break;
      }
      case 'plan-review-done': {
        const findings = ev.findings || [];
        const row = document.createElement('div');
        row.className = 'row plan-review' + (findings.length ? ' has-findings' : '');
        const hdr = document.createElement('div');
        hdr.className = 'pr-hdr';
        hdr.textContent = findings.length
          ? \`⚖ plan review: \${findings.length} concern\${findings.length === 1 ? '' : 's'} found → revising the plan\`
          : '⚖ plan review: no concerns — plan stands';
        row.appendChild(hdr);
        if (findings.length) {
          const body = document.createElement('div');
          body.className = 'pr-findings';
          for (const f of findings) {
            const it = document.createElement('div');
            it.className = 'pr-finding';
            it.textContent = \`[\${f.lens}] \${String(f.concerns || '').replace(/\\s+/g, ' ').slice(0, 400)}\`;
            body.appendChild(it);
          }
          row.appendChild(body);
        }
        RC.log.appendChild(row); RC.afterRow?.(); scrollToBottom();
        node = row;
        break;
      }
      case 'review':
        // Per-task reviews render their verdict in the task's footer. Fall
        // back to a standalone row when there's no matching task-wrap (e.g.
        // a legacy review with no reviewTaskId, or a task with no body rows).
        if (ev.reviewTaskId && renderReviewIntoFooter(ev)) { node = null; break; }
        node = addReviewRow(ev);
        break;
      case 'git-pending-resolution':
        node = addPendingResolutionRow(ev);
        break;
      case 'assignment-gate':
        node = addAssignmentGate(ev);
        break;
      case 'assignment-banner':
        node = addAssignmentBanner(ev);
        break;
      case 'memory-consolidated':
        node = addMemoryConsolidated(ev);
        break;
      case 'manager-diagnostic':
        node = addManagerDiagnostic(ev);
        break;
      case 'intake-message':
        node = addIntakeMessage(ev);
        break;
      case 'requirement-card':
        node = addEditableRequirementCard(ev);
        break;
      case 'compacting-started':
        // Just the marker row. Do NOT seed the live gauge from this event: it is
        // persisted and gets replayed on every full re-render/reload, where its
        // numbers are historical. The per-turn ephemeral 'context-fill' SSE
        // (emitted alongside it live) owns the gauge.
        node = addCompactionRow(ev);
        break;
      case 'task-undo':
        node = addTaskUndoRow(ev);
        break;
      case 'subagent-card': {
        // the bounded labeled result of a delegated sub-agent, folded into the chat;
        // also seeds/updates the store so the list + drawer reflect the final state.
        saUpsert(ev.subagentId, { topic: ev.topic, status: ev.status || 'done', content: ev.content || '' });
        const row = document.createElement('div');
        row.className = 'row subagent-card ' + (ev.status || 'done');
        row.style.cursor = 'pointer';
        row.title = 'Click to view this sub-agent\\'s activity';
        row.onclick = () => openSubagentDetail(ev.subagentId);
        const head = document.createElement('div'); head.className = 'sa-card-head';
        head.textContent = '◇ sub-agent finished: ' + (ev.topic || ev.subagentId || '');
        const prev = document.createElement('div'); prev.className = 'sa-card-preview';
        prev.textContent = (ev.content || '').replace(/\\s+/g, ' ').trim().slice(0, 180) + ' — click to view activity';
        row.appendChild(head); row.appendChild(prev);
        RC.log.appendChild(row);
        RC.afterRow?.();
        scrollToBottom();
        node = row;
        break;
      }
      case 'acceptance-verdict': {
        // the Acceptance agent's verdict on a sub-agent's work — visible in the main chat
        // (the both-sides bridge); click → the sub-agent's drawer.
        const reject = ev.verdict === 'reject';
        const row = document.createElement('div');
        row.className = 'row acceptance-verdict' + (reject ? ' reject' : '');
        row.style.cursor = 'pointer';
        row.onclick = () => openSubagentDetail(ev.subagentId);
        const head = document.createElement('div'); head.className = 'av-head';
        head.textContent = (reject ? '✎ review: changes requested' : '✓ review: approved') + (ev.topic ? \` — \${ev.topic}\` : '');
        const body = document.createElement('div'); body.className = 'av-body';
        body.textContent = (ev.rationale || '').replace(/\\s+/g, ' ').trim().slice(0, 200);
        row.appendChild(head); if (body.textContent) row.appendChild(body);
        RC.log.appendChild(row); RC.afterRow?.(); scrollToBottom();
        node = row;
        break;
      }
      case 'acceptance-feedback': {
        const row = document.createElement('div');
        row.className = 'row acceptance-verdict reject';
        row.style.cursor = 'pointer';
        row.onclick = () => openSubagentDetail(ev.subagentId);
        const head = document.createElement('div'); head.className = 'av-head'; head.textContent = '✎ feedback → sub-agent';
        const body = document.createElement('div'); body.className = 'av-body'; body.textContent = (ev.feedback || '').replace(/\\s+/g, ' ').trim().slice(0, 240);
        row.appendChild(head); row.appendChild(body);
        RC.log.appendChild(row); RC.afterRow?.(); scrollToBottom();
        node = row;
        break;
      }
    }

    // Tag the rendered node with its task id so task-end can wrap it later.
    if (node && ev.taskId) {
      node.dataset.taskId = ev.taskId;
      // LATE rows: some events for a task are emitted AFTER it closed and got
      // wrapped — the collapsed git-commit summary (tagged with the closed
      // task) and any other tail row. Without this they'd float at the bottom,
      // landing visually between later tasks instead of inside the task they
      // belong to. If the wrap already exists, move the row into its body so
      // it nests correctly (review verdicts use renderReviewIntoFooter above).
      if (!node.closest('.task-wrap')) {
        const wrap = RC.log.querySelector(\`.task-wrap[data-task-id="\${CSS.escape(String(ev.taskId))}"]\`);
        const body = wrap && wrap.querySelector('.task-body');
        if (body) { body.appendChild(node); scrollToBottom(); }
      }
    }
  }

  // When <next-task> closes a task, wrap all the just-rendered rows that belong
  // to that task in a .task-wrap with a header (user-request) and footer (summary).
  // The events stay at the same horizontal position; the wrap adds decoration only.
  function wrapTaskRetroactively(ev) {
    const taskId = ev.taskId;
    if (!taskId) return;
    // Find all rows for this task that are NOT already inside a wrap.
    const rows = Array.from(RC.log.querySelectorAll(\`[data-task-id="\${taskId}"]\`))
      .filter((r) => !r.closest('.task-wrap'));
    // NOTE: a 0-row task (e.g. the last/thinnest task, whose only rows were
    // already consumed, or a task with no standalone body rows) STILL gets a
    // real .task-wrap below — NOT a bare pill. A pill had no data-task-id and
    // no .task-foot, so a later review verdict (emitted after every task-end)
    // and late git/diff rows for that task had nothing to nest into and floated
    // at the bottom. Every closed task is now a wrap so renderReviewIntoFooter,
    // the late-row relocation above, and wrapRollup can all find it. The body
    // may just be empty (styled compactly via .task-wrap.empty).
    const wrap = document.createElement('div');
    wrap.className = 'task-wrap' + (rows.length === 0 ? ' empty' : '');
    wrap.dataset.taskId = taskId;

    // Header (user-request) — shown ONLY when collapsed (CSS); clicking it
    // expands. No toggle glyph here anymore; the chevron lives in the foot so
    // it's reachable in the expanded view where the header is hidden.
    const head = document.createElement('div');
    head.className = 'task-head';
    if (ev.userRequest) {
      const ur = document.createElement('span');
      ur.className = 'ur';
      ur.textContent = ev.userRequest;
      head.appendChild(ur);
    }
    wrap.appendChild(head);

    // Body container (events are moved here, preserving original order)
    const body = document.createElement('div');
    body.className = 'task-body';
    wrap.appendChild(body);

    // Insert wrap at the position of the first row. With no rows, firstRow is
    // undefined → insertBefore(wrap, null) appends at end-of-log, which is the
    // correct spot for a freshly-closed task.
    const firstRow = rows[0];
    RC.log.insertBefore(wrap, firstRow || null);
    // Move rows into the body (no-op for an empty task)
    for (const r of rows) body.appendChild(r);

    // Footer — the bottom status row. ALWAYS shows: a small collapse/expand
    // chevron + the status checks (✓ committed / ✓ reviewed). The summary text
    // is here too but CSS hides it unless collapsed (and it stays click-to-edit).
    const foot = document.createElement('div');
    foot.className = 'task-foot';
    if (ev.id) foot.dataset.eventId = String(ev.id);
    const chev = document.createElement('span');
    chev.className = 'task-foot-toggle';
    chev.title = 'Collapse / expand';
    chev.onclick = (e) => { e.stopPropagation(); setTaskCollapsed(wrap, foot, !wrap.classList.contains('collapsed'), true); };
    foot.appendChild(chev);
    const summarySpan = document.createElement('span');
    summarySpan.className = 'task-foot-summary';
    summarySpan.textContent = ev.summary || '(no summary)';
    summarySpan.title = 'Click to edit this summary';
    summarySpan.addEventListener('click', () => beginEditTaskSummary(foot, summarySpan, ev));
    foot.appendChild(summarySpan);
    // "✓ Committed" badge (or "⚠ commit issue") in the status row. Derives purely
    // from the persisted task-end fields so it survives full re-renders.
    renderCommitIntoFooter(foot, ev);
    wrap.appendChild(foot);

    // Clicking the (collapsed-only) header expands the task.
    head.onclick = () => setTaskCollapsed(wrap, foot, false, true);
    // Restore persisted collapse state (lost otherwise on the re-render cycle).
    setTaskCollapsed(wrap, foot, _taskCollapsed.get(String(taskId)) || false);

    scrollToBottom();
  }

  // Toggle a task wrap's collapse state, sync the foot chevron glyph, and
  // remember it (keyed by taskId) so it survives the clearLog() re-render.
  function setTaskCollapsed(wrap, foot, collapsed, scroll = false) {
    wrap.classList.toggle('collapsed', collapsed);
    const ch = foot && foot.querySelector('.task-foot-toggle');
    if (ch) ch.textContent = collapsed ? '▶' : '▼';
    const id = wrap.dataset.taskId;
    if (id) _taskCollapsed.set(String(id), collapsed);
    // On a USER click, keep the task's FOOT (where the chevron is) at the viewport bottom — AFTER
    // the expand/collapse reflow (rAF), or scrollIntoView targets the stale pre-reflow layout. This
    // makes expand reveal the task's bottom (latest) and collapse scroll UP so the now-collapsed
    // task stays in view instead of leaving you parked where its bottom used to be.
    if (scroll && foot) {
      requestAnimationFrame(() => requestAnimationFrame(() => {
        try { foot.scrollIntoView({ block: 'end', behavior: 'smooth' }); } catch {}
      }));
    }
  }

  // Render the commit status into a task footer from the task-end event's
  // committedRepos (rel+hash) / commits (absolute repoDir fallback) / commitError.
  function renderCommitIntoFooter(foot, ev) {
    if (!foot) return;
    foot.querySelector('.task-foot-commit')?.remove();   // re-render safety
    const list = (Array.isArray(ev.committedRepos) && ev.committedRepos.length)
      ? ev.committedRepos.map((c) => ({ label: c.rel || basename(c.repoDir || '') || 'repo', hash: c.hash }))
      : (Array.isArray(ev.commits) ? ev.commits.map((c) => ({ label: basename(c.repoDir || '') || 'repo', hash: c.hash })) : []);
    if (!list.length && !ev.commitError) return;   // nothing committed, no error → no badge
    const badge = document.createElement('span');
    if (ev.commitError) {
      badge.className = 'task-foot-commit commit-error';
      badge.textContent = '⚠ commit issue';
      badge.title = ev.commitError + (list.length ? '\\n\\nCommitted:\\n' + list.map((c) => \`\${c.label}@\${(c.hash || '').slice(0, 9)}\`).join('\\n') : '');
    } else {
      badge.className = 'task-foot-commit commit-ok';
      badge.textContent = list.length > 1 ? \`✓ committed · \${list.length} repos\` : '✓ committed';
      badge.title = list.map((c) => \`\${c.label}@\${(c.hash || '').slice(0, 9)}\`).join('\\n');
    }
    foot.appendChild(badge);
  }

  function summarizeResult(ev) {
    const c = ev.content || '';
    const nonEmpty = c.split('\\n').filter((l) => l.trim()).length;
    // The row content is truncated for display (UI_RESULT_PREVIEW chars); ev.fullLen
    // is the true length the MODEL received. Show "N of M chars" so a big doc/output
    // doesn't read as a hard limit (it isn't — the model got all M).
    const sizeLabel = (ev.fullLen != null && ev.fullLen > c.length)
      ? \`\${c.length} of \${ev.fullLen} chars\`
      : \`\${c.length} chars\`;
    const v = ev.verb || ev.name;
    if (ev.status === 'error') return c.split('\\n')[0].slice(0, 140);
    if (v === 'list' || ev.name === 'list_dir') return \`\${nonEmpty} entries\`;
    if (v === 'read' || ev.name === 'read_file') return \`Read \${c.split('\\n').length} lines\`;
    if (v === 'search' || ev.name === 'grep') return c.startsWith('(no matches') ? 'No matches' : \`\${nonEmpty} matching lines\`;
    if (v === 'find' || ev.name === 'glob') return c.startsWith('(no matches') ? 'No files' : \`\${nonEmpty} files\`;
    if (v === 'write' || ev.name === 'write_file') return c.replace(/^wrote\\s+/, '');
    if (v === 'edit' || ev.name === 'edit_file') return c.replace(/^edited\\s+/, '');
    if (v === 'powershell' || ev.name === 'powershell') {
      const m = c.match(/^exit (\\S+)\\s+in\\s+(\\d+ms)/);
      return m ? \`exit \${m[1]} in \${m[2]}\` : sizeLabel;
    }
    // docx readers: show the paragraph count from the result header (the model
    // got the full doc; this isn't a truncation).
    if (v === 'read_docx' || ev.name === 'read_docx' || v === 'read_docx_full' || ev.name === 'read_docx_full') {
      const m = c.match(/\\((\\d+)\\s+(?:editable\\s+)?paragraphs/);
      return m ? \`\${m[1]} paragraphs\` : sizeLabel;
    }
    if (v === 'next-task') return c.replace(/^closed previous task.*?opened new task /, '→ ');
    if (v === 'list-tasks') return \`\${nonEmpty} tasks\`;
    if (v === 'task-detail') return 'task detail';
    if (v === 'search-tasks') return \`\${nonEmpty} matches\`;
    return sizeLabel;
  }

  function applyControlsForStatus() {
    const running = session?.status === 'running';
    // Input is NEVER disabled now — the user can compose at any time.
    // While the agent is busy: Enter (or the queue button on the right)
    // adds to the queue above. While idle: Enter submits as before.
    $input.disabled = false;
    $input.placeholder = running
      ? (session?.orchestrator
          ? 'Agent busy — Enter delegates to a sub-agent · Esc to stop'
          : 'Agent running — Esc to stop')
      : 'Ask… (Enter to send · Shift+Space toggles the panel)';
    if (!running) {
      // Polite fallback, not a steal: only take focus when the user isn't
      // typing in some OTHER editor (path-input, task summary, memory bullets…)
      // — yanking focus there blur-cancels it and discards the typing.
      const ae = document.activeElement;
      const typing = ae && ae !== $input && (ae.tagName === 'INPUT' || ae.tagName === 'TEXTAREA' || ae.isContentEditable);
      if (!typing) { try { $input.focus(); } catch {} }
    }
    // Queue button shows in BOTH states now: while idle you can stack up
    // messages (⤓) and fire them one at a time with ▶ — type one, queue it,
    // do another, come back. Only the steer button is mid-run-only.
    if ($queueBtn) $queueBtn.style.display = '';
    if ($steerBtn) $steerBtn.style.display = running ? '' : 'none';   // steer only matters mid-run (item H)
    renderQueue();        // play buttons enable/disable when status flips
  }

  // ── Pending-message queue ───────────────────────────────────────────────
  // Holds text the user prepared while the agent was busy. Items don't
  // auto-fire — the user explicitly clicks ▶ to send each one, or clicks
  // a row to swap it back into the input box for editing. Memory only;
  // dropped on page reload by design.
  const $queueStack = document.getElementById('queueStack');
  const $queueBtn   = document.getElementById('queueBtn');
  const $steerBtn   = document.getElementById('steerBtn');
  let _inputQueue = [];     // [{ id, text }]
  let _queueIdCounter = 0;
  let _selectedQueueId = null;   // highlighted queue item (for ⇧→ pull-back)

  function enqueueText(text) {
    text = (text ?? '').trim();
    if (!text) return;
    _inputQueue.push({ id: ++_queueIdCounter, text });
    renderQueue();
  }
  function enqueueCurrentInput() {
    const text = $input.value;
    if (!text.trim()) return;
    $input.value = '';
    autoGrowInput();
    $input.focus();
    revealQueueThenAdd(text);
  }
  // Surface the Queue when something is queued: open the side panel if collapsed,
  // switch to the Queue tab, then add the item — waiting ~1s after OPENING so the
  // panel is fully visible (and the new item draws the eye) instead of appearing
  // mid-animation. If the panel is already open, add immediately.
  function revealQueueThenAdd(text) {
    const p = document.getElementById('sidePanel');
    const wasCollapsed = p && p.classList.contains('collapsed');
    selectSideTab('queue');
    if (wasCollapsed) {
      // Open the panel, let it become visible, THEN add (so the item draws the eye).
      setSidePanelCollapsed(false);
      setTimeout(() => enqueueText(text), 1000);
    } else {
      enqueueText(text);   // already open → add immediately (synchronous, no deferral)
    }
  }
  function removeFromQueue(id) {
    _inputQueue = _inputQueue.filter((q) => q.id !== id);
    renderQueue();
  }
  function dispatchQueueItem(id) {
    if (session?.status === 'running') return;     // button should be disabled anyway
    const item = _inputQueue.find((q) => q.id === id);
    if (!item) return;
    removeFromQueue(id);
    $input.value = item.text;
    autoGrowInput();
    send();
  }
  // Click a queue row → swap it with the current input contents. Lets the
  // user resume an earlier draft without losing what they're typing now.
  function swapQueueWithInput(id) {
    const idx = _inputQueue.findIndex((q) => q.id === id);
    if (idx === -1) return;
    const item = _inputQueue[idx];
    const currentText = $input.value;
    if (currentText.trim()) {
      _inputQueue[idx] = { id: ++_queueIdCounter, text: currentText };
    } else {
      _inputQueue.splice(idx, 1);
    }
    $input.value = item.text;
    autoGrowInput();
    renderQueue();
    $input.focus();
  }
  // Highlight a queued item (so ⇧→ pulls it back). Click selects + focuses it.
  function selectQueueItem(id) {
    _selectedQueueId = id;
    renderQueue();
    const row = $queueStack?.querySelector(\`.queue-item[data-qid="\${id}"]\`);
    if (row) row.focus();
  }
  // Pull a queued item back into the input box (editable), removing it from the
  // queue — the inverse of ⇧← (enqueue). Used by ⇧→ on a highlighted item.
  function pullQueueItem(id) {
    const item = _inputQueue.find((q) => q.id === id);
    if (!item) return;
    removeFromQueue(id);
    if (_selectedQueueId === id) _selectedQueueId = null;
    $input.value = item.text;
    autoGrowInput();
    $input.focus();
  }
  function renderQueue() {
    const count = document.getElementById('queueCount');
    if (count) count.textContent = _inputQueue.length ? \` (\${_inputQueue.length})\` : '';
    const empty = document.getElementById('queueEmpty');
    if (empty) empty.style.display = _inputQueue.length ? 'none' : '';
    if (!$queueStack) return;
    if (_selectedQueueId != null && !_inputQueue.some((q) => q.id === _selectedQueueId)) _selectedQueueId = null;
    $queueStack.innerHTML = '';
    const running = session?.status === 'running';
    for (const item of _inputQueue) {
      const row = document.createElement('div');
      row.className = 'queue-item' + (item.id === _selectedQueueId ? ' selected' : '');
      row.title = item.text;
      row.dataset.qid = item.id;
      row.tabIndex = 0;
      row.onclick = () => selectQueueItem(item.id);   // click highlights; ⇧→ pulls it back
      row.onkeydown = (e) => {
        if (e.shiftKey && e.key === 'ArrowRight') { e.preventDefault(); pullQueueItem(item.id); }
        else if (e.key === 'Enter') { e.preventDefault(); pullQueueItem(item.id); }
      };

      const preview = document.createElement('span');
      preview.className = 'queue-preview';
      preview.textContent = item.text.replace(/\\s+/g, ' ').slice(0, 200);
      row.appendChild(preview);

      const play = document.createElement('button');
      play.className = 'queue-play';
      play.textContent = '▶';
      play.title = running ? 'Agent busy — wait or stop' : 'Send this message';
      play.disabled = running;
      play.onclick = (e) => { e.stopPropagation(); dispatchQueueItem(item.id); };
      row.appendChild(play);

      const del = document.createElement('button');
      del.className = 'queue-del';
      del.textContent = '×';
      del.title = 'Remove';
      del.onclick = (e) => { e.stopPropagation(); removeFromQueue(item.id); };
      row.appendChild(del);

      $queueStack.appendChild(row);
    }
  }

  function renderTimeoutChip() {
    // Old chip removed from the chrome; timeout now lives in the settings
    // modal. Update both the chip (if still in DOM) and the settings value.
    const $c = document.getElementById('timeoutChip');
    if (session && $c) {
      const mins = Math.round((session.timeoutMs || 0) / 60000);
      $c.textContent = \`⏱ \${mins}m\`;
    }
    if (session && $settingsTimeout) {
      $settingsTimeout.textContent = fmtTimeoutMins(session.timeoutMs);
    }
  }

  async function changeTimeout() {
    if (!session) return;
    const cur = Math.round((session.timeoutMs || 1800000) / 60000);
    const raw = prompt(\`Set agent timeout in minutes (current: \${cur})\`, String(cur));
    if (!raw) return;
    const mins = Number(raw);
    if (!Number.isFinite(mins) || mins < 1 || mins > 360) {
      alert('Enter a number between 1 and 360 (minutes).');
      return;
    }
    try {
      await fetch('/api/extend-timeout', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ ms: Math.round(mins * 60 * 1000) }),
      });
    } catch (e) { showError('Update timeout failed', e); }
  }

  // Thinking/effort probe: fire the candidate reasoning-param shapes at the
  // configured endpoint and render a tight table (which shapes the shim honors,
  // whether reasoning tokens are exposed, and how they vary by effort).
  async function probeThinking() {
    const btn = document.getElementById('thinkProbeBtn');
    const out = document.getElementById('thinkProbeOut');
    if (!out) return;
    const model = (document.getElementById('model')?.value) || '';
    out.style.display = 'block';
    out.textContent = 'Probing ' + (model || '(model)') + ' … 6 requests, ~10–30s';
    if (btn) { btn.disabled = true; btn.textContent = 'Probing…'; }
    try {
      const r = await fetch('/api/probe-thinking', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ model }) });
      const j = await r.json();
      out.textContent = j.ok ? renderThinkProbe(j) : ('Probe failed: ' + (j.error || ('HTTP ' + r.status)));
    } catch (e) {
      out.textContent = 'Probe error: ' + (e?.message || e);
    } finally {
      if (btn) { btn.disabled = false; btn.textContent = 'Probe'; }
    }
  }
  function renderThinkProbe(j) {
    const pad = (s, n) => { s = String(s); return s.length >= n ? s.slice(0, n) : s + ' '.repeat(n - s.length); };
    const num = (v) => (v == null ? '—' : String(v));
    // think column: the exposed reasoning count, else ~inferred (total−prompt−out), else —
    const think = (row) => row.reasoning != null ? String(row.reasoning) : (row.inferred != null ? '~' + row.inferred : '—');
    const L = [];
    L.push('model: ' + j.model);
    L.push('reasoning field: ' + (j.reasoningField || "(none exposed — 'think' is inferred = total−prompt−out)"));
    L.push("prompt: bat-and-ball CRT — reasoned answer is $0.05, intuitive-but-WRONG is $0.10.");
    L.push("→ if effort variants give different answers, reasoning IS modulated even when tokens look identical.");
    L.push('');
    L.push(pad('variant', 37) + pad('http', 5) + pad('out', 5) + pad('total', 7) + pad('think', 7) + pad('ms', 7) + 'answer');
    for (const row of (j.rows || [])) {
      if (row.ok) L.push(pad(row.label, 37) + pad(row.status, 5) + pad(num(row.out), 5) + pad(num(row.total), 7) + pad(think(row), 7) + pad(num(row.ms), 7) + (row.answer || ''));
      else L.push(pad(row.label, 37) + pad(row.status || 'ERR', 5) + 'ERR: ' + (row.err || ''));
    }
    return L.join('\\n');
  }

  $input.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      if (!$input.value.trim()) return;        // empty Enter = no-op
      // Busy or not, just send: when the foreground is busy the server delegates the message to
      // a background sub-agent (orchestrator, default-on). The old client-side queue is retired —
      // async delegation replaces it. (A non-repo busy target still 409s; the error surfaces.)
      send();
    }
  });
  // Auto-grow textarea: start at one line, grow with content up to its CSS
  // max-height. The overflow-y: hidden on the textarea is flipped to auto
  // only when content exceeds the cap.
  function autoGrowEl(el) {
    if (!el) return;
    el.style.height = 'auto';
    const sh = el.scrollHeight, max = 220;
    el.style.height = Math.min(sh, max) + 'px';
    el.style.overflowY = sh > max ? 'auto' : 'hidden';
  }
  function autoGrowInput() { autoGrowEl($input); }
  $input.addEventListener('input', autoGrowInput);
  // (The intake textarea's auto-grow + Enter→send is wired by its
  // createChatComponent instance — see \`intakeChat\` above.)
  document.addEventListener('keydown', (e) => {
    // F1 → open snapshot. Works even if every UI click is being eaten by
    // an invisible overlay — handy when the page seems "frozen" and the
    // user needs to capture diagnostics anyway.
    if (e.key === 'F1') { e.preventDefault(); openSnapshot(); return; }
    // Shift+Space → collapse/expand the left side panel. Skip when typing in a
    // text field OTHER than the main chat input (so modals/forms keep their space).
    if (e.shiftKey && e.code === 'Space') {
      const t = e.target;
      const otherField = t && t !== $input && (t.tagName === 'TEXTAREA' || t.tagName === 'INPUT' || t.isContentEditable);
      if (!otherField) { e.preventDefault(); toggleSidePanel(); return; }
    }
    if (e.key === 'Escape') {
      // Layered modals: snapshot closes first, then settings, then we cancel
      // the running chat as a last resort.
      if ($modalBg.classList.contains('show')) { closeModal(); return; }
      if ($settingsModalBg.classList.contains('show')) { closeSettings(); return; }
      if (session?.status === 'running') cancelChat();
    }
  });

  // ── Settings modal ─────────────────────────────────────────────────────
  async function openSettings() {
    // Open the modal IMMEDIATELY — never gate it on a network fetch. A stalled
    // fetch (e.g. connection pool exhausted by multiple tabs) must not make the
    // gear button look dead; this is also the route to the Snapshot, which is
    // exactly what you need when things are wedged.
    if ($settingsProject) $settingsProject.textContent = activeProject || '(none)';
    if ($settingsTokens)  $settingsTokens.textContent = totalTokens.toLocaleString();
    if ($settingsTimeout) $settingsTimeout.textContent = fmtTimeoutMins(session?.timeoutMs);
    $settingsModalBg.classList.add('show');
    // Then populate the key/creds prefixes asynchronously (bounded so a hung
    // request leaves them blank rather than blocking the modal).
    try {
      const ak = await fetchWithTimeout('/api/api-key', {}, 6000).then((r) => r.json());
      const el = document.getElementById('settingsApiKey');
      if (el) el.textContent = ak.hasKey ? (ak.prefix || '(set)') : '(not set)';
    } catch {}
    try {
      const gc = await fetchWithTimeout('/api/gitlab-creds', {}, 6000).then((r) => r.json());
      const el = document.getElementById('settingsGitLab');
      if (el) el.textContent = gc.hasCreds ? (gc.user || '(set)') : '(not set)';
    } catch {}
  }
  function closeSettings() {
    $settingsModalBg.classList.remove('show');
  }
  // "What's new" after a self-update: check-update dropped whats-new.json next to
  // the bundle; the server serves it via /api/whats-new until the user dismisses
  // (POST whats-new-seen deletes it). Bounded fetch, fire-and-forget — a hung or
  // absent endpoint must never gate the UI (the empty {} response is the norm).
  async function maybeShowWhatsNew(attempt = 0) {
    try {
      // Another modal already open (Settings, intake, …): both backdrops share z-index 100 and
      // DOM order would paint us UNDERNEATH it. Wait it out instead of showing an invisible modal.
      if (document.querySelector('.modal-bg.show') && attempt < 20) { setTimeout(() => maybeShowWhatsNew(attempt + 1), 4000); return; }
      const wn = await fetchWithTimeout('/api/whats-new', {}, 6000).then((r) => r.json());
      if (!wn || !Array.isArray(wn.entries) || !wn.entries.length) return;
      const b = document.getElementById('wnBuild'); if (b) b.textContent = wn.toBuild || wn.entries[0]?.build || '';
      const body = document.getElementById('whatsNewBody'); if (!body) return;
      body.innerHTML = '';
      for (const e of wn.entries.filter((x) => x && typeof x === 'object')) {   // newest first; textContent everywhere (notes are data, not markup)
        const sec = document.createElement('div'); sec.className = 'wn-entry';
        const h = document.createElement('div'); h.className = 'wn-title';
        const t = document.createElement('span'); t.textContent = e.title || 'Changes'; h.appendChild(t);
        // The header already says "Updated to build N" — repeat the build only on a
        // multi-release jump, where it (plus the date) is the entry's identity.
        if (wn.entries.length > 1 && e.build) {
          const meta = document.createElement('span'); meta.className = 'wn-meta';
          meta.textContent = 'build ' + e.build + (e.date ? ' · ' + e.date : '');
          h.appendChild(meta);
        }
        sec.appendChild(h);
        const ul = document.createElement('ul'); ul.className = 'wn-items';
        const items = Array.isArray(e.items) ? e.items : (e.items == null ? [] : [e.items]);   // a string here would iterate per-CHARACTER
        for (const it of items) { const li = document.createElement('li'); li.textContent = String(it); ul.appendChild(li); }
        sec.appendChild(ul);
        body.appendChild(sec);
      }
      document.getElementById('whatsNewModalBg')?.classList.add('show');
    } catch {}
  }
  function dismissWhatsNew() {
    document.getElementById('whatsNewModalBg')?.classList.remove('show');
    try { fetch('/api/whats-new-seen', { method: 'POST' }); } catch {}
  }
  function fmtTimeoutMins(ms) {
    if (!ms) return '—';
    const m = ms / 60000;
    return (m >= 1 ? m.toFixed(0) + ' min' : (ms / 1000).toFixed(0) + ' s');
  }

  // ── Infinite scroll for older events ───────────────────────────────────
  // When the user scrolls near the top of the log, fetch the next batch of
  // older events from /api/events?before=<id>&limit=50 and prepend them.
  let _loadingOlder = false;
  let _noMoreOlder = false;
  async function maybeLoadOlder() {
    if (_loadingOlder || _noMoreOlder) return;
    if (!session || !session.events || session.events.length === 0) return;
    // Trigger when scrolled within 120px of the top
    if (RC.scroll.scrollTop > 120) return;
    _loadingOlder = true;
    const oldestId = session.events[0].id;
    // Save scroll metrics so we can preserve position after prepending
    const prevScrollHeight = RC.scroll.scrollHeight;
    const prevScrollTop = RC.scroll.scrollTop;
    try {
      const r = await fetch(\`/api/events?before=\${encodeURIComponent(oldestId)}&limit=50\`);
      if (!r.ok) throw new Error('HTTP ' + r.status);
      const data = await r.json();
      const older = Array.isArray(data.events) ? data.events : [];
      if (older.length === 0) {
        _noMoreOlder = true;
        return;
      }
      // Prepend to session.events
      session.events = older.concat(session.events);
      // Re-render from scratch (small N; expanded state lives on DOM so it's lost on rerender)
      clearLog();
      for (const ev of session.events) renderEvent(ev);
      // Restore scroll position so the user's current view doesn't jump
      const newScrollHeight = RC.scroll.scrollHeight;
      RC.scroll.scrollTop = prevScrollTop + (newScrollHeight - prevScrollHeight);
      if (!data.hasMoreOlder) _noMoreOlder = true;
    } catch (e) {
      console.error('load older failed:', e);
    } finally {
      _loadingOlder = false;
    }
  }
  // Throttled scroll handler
  let _scrollT = null;
  RC.scroll.addEventListener('scroll', () => {
    updateStickState();   // immediate: lock/unlock + hide the arrow when back at the bottom
    if (_scrollT) return;
    _scrollT = setTimeout(() => { _scrollT = null; maybeLoadOlder(); }, 150);
  });

  // Initial load
  loadState();
  connectStream();
  imgLoadSupport();   // non-blocking: learn the persisted image-probe verdict
</script>
</body>
</html>
`;

function openInDefaultBrowser(url) {
  // Prefer Chrome; fall back to the OS default if Chrome isn't installed.
  if (process.platform === 'win32') {
    const candidates = [
      (process.env.LOCALAPPDATA || '') + '\\Google\\Chrome\\Application\\chrome.exe',
      'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe',
      'C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe',
    ];
    const chrome = candidates.find((p) => { try { return p && existsSync(p); } catch { return false; } });
    if (chrome) spawn(chrome, [url], { stdio: 'ignore', detached: true }).unref();
    else spawn('cmd', ['/c', 'start', '""', url], { stdio: 'ignore', detached: true, windowsHide: true }).unref();
  } else if (process.platform === 'darwin') {
    spawn('open', ['-a', 'Google Chrome', url], { stdio: 'ignore', detached: true }).unref();
  } else {
    spawn('google-chrome', [url], { stdio: 'ignore', detached: true })
      .on('error', () => spawn('xdg-open', [url], { stdio: 'ignore', detached: true }).unref())
      .unref();
  }
}

// startServer was inlined above (from server.mjs). Wire defaults from CONFIG.
// buildVersion goes into SERVER_CONFIG too so the UI can render a floating
// version badge in the corner without an extra fetch round-trip.
const htmlWithConfig = UI_HTML.replace(
  '</head>',
  '<script>window.SERVER_CONFIG=' + JSON.stringify({ defaultModel: MODEL, baseURL: BASE_URL, buildVersion: BUILD_VERSION }) + ';</script></head>'
);

// Boot: no CLI picker — the browser shows an in-UI file browser on the
// landing view and the user picks a project there. Server starts with
// no active project; /api/open-project drives the transition.
//
// Subcommand: `node code-boss.mjs package --jars <dir> ...` packages THIS
// bundle (+ the supplied jars) into an extract-and-run zip, then exits. Lets a
// single copied .mjs build its own deployable on the production box.
(async () => {
  if (process.argv[2] === 'package') {
    try {
      const code = await runPackageCommand({ argv: process.argv.slice(3), bundlePath: fileURLToPath(import.meta.url), version: BUILD_VERSION });
      process.exit(code || 0);
    } catch (e) { console.error('[code_boss] package failed:', e?.stack || e?.message || e); process.exit(1); }
    return;
  }
  // Subcommand: `node code-boss.mjs extract [dir] [--force]` — recreate the
  // project source tree from the source map embedded in this bundle, then exit.
  if (process.argv[2] === 'extract') {
    try { process.exit(await runExtractCommand({ argv: process.argv.slice(3) })); }
    catch (e) { console.error('[code_boss] extract failed:', e?.stack || e?.message || e); process.exit(1); }
    return;
  }
  // Subcommand: `node code-boss.mjs check-update` — best-effort self-update
  // from UPDATE_SOURCE; ALWAYS exits 0 (a launcher runs this before starting
  // the app and must never be blocked by it).
  if (process.argv[2] === 'check-update') {
    try { process.exit(await runUpdateCheckCommand({ bundlePath: fileURLToPath(import.meta.url), version: BUILD_VERSION, updateSource: UPDATE_SOURCE, updateTag: UPDATE_TAG })); }
    catch (e) { console.error('check-update: skipped —', e?.message || e); process.exit(0); }
    return;
  }
  try {
    const { port } = await startServer({
      html: htmlWithConfig,
      baseURL: BASE_URL,
      apiKey: API_KEY,
      defaultModel: MODEL,
      port: PORT,
      initialProject: null,
      // Production: bundle small free-conversation exchanges into one task so the
      // main chat thread stops fragmenting one-task-per-message. Tests call
      // startServer without this, so their close-triggered assertions are unchanged.
      bundleConversation: true,
    });
    const url = 'http://127.0.0.1:' + port + '/';
    console.error('[code_boss] listening on ' + url);
    console.error('[code_boss] base URL: ' + BASE_URL);
    console.error('[code_boss] model:    ' + MODEL);
    if (process.env.OPEN_BROWSER !== '0') {
      openInDefaultBrowser(url);
      console.error('[code_boss] opening browser; press Ctrl+C to stop.');
    } else {
      console.error('[code_boss] OPEN_BROWSER=0 — not opening a browser. Visit ' + url + ' manually.');
    }
  } catch (e) {
    console.error('[code_boss] FAILED TO START:', e.message);
    if (e.code === 'EADDRINUSE') {
      console.error('  Port ' + PORT + ' is already in use. Set PORT=<other> and rerun.');
    }
    process.exit(1);
  }
})();
